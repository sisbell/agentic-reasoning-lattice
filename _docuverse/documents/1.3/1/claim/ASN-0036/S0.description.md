Posits that the content store is append-only: once a value is written at address `a`, that address remains in the domain and its value is frozen across all future state transitions. The arrangement stream `M(d)` is explicitly unconstrained, so this immutability guarantee is scoped to content alone.
