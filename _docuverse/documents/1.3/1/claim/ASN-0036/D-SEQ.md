**D-SEQ (SequentialPositions).** For each document d, if V_1(d) is non-empty, then there exists n ≥ 1 such that:

`V_1(d) = {[1, 1, ..., 1, k] : 1 ≤ k ≤ n}`

where the tuple has length m, the common V-position depth in the text subspace (S8-depth). By S8a, every V-position has depth `≥ 2`, so `m ≥ 2`; the derivation below relies on this lower bound. At depth 2 this gives V_1(d) = {[1, k] : 1 ≤ k ≤ n}, a single unbroken block of ordinals — the form contiguity (D-CTG, Step 3) acquires once the arrangement's finiteness (S8-fin, Step 4) bounds it — matching Nelson's "addresses 1 through n."

*Proof.* Let V_1(d) be non-empty and let m be the common depth of all V-positions in the text subspace (S8-depth guarantees a common depth exists). By S8a, every V-position has `#v ≥ 2`, so `m ≥ 2`.

**Step 1: shared prefix.** We show that every position in V_1(d) has the form [1, 1, …, 1, k] — that is, components 2 through m − 1 are all equal to 1, with only the last component varying.

*Case m = 2.* Every position has exactly two components. By the definition `V_1(d) = {v ∈ dom(M(d)) : subspace(v) = 1}` together with `subspace(v) = v₁`, every position in V_1(d) has `v₁ = 1` — the subspace identifier sits at component 1. The second component is a single ordinal. There are no intermediate components (components 2 through m − 1 is the empty range 2 through 1), so the shared-prefix condition holds vacuously. Every position is [1, k] for some k, which is [1, 1, …, 1, k] with zero intervening 1s.

*Case m ≥ 3.* By the definition `V_1(d) = {v ∈ dom(M(d)) : subspace(v) = 1}` together with `subspace(v) = v₁`, every position has `v₁ = 1` — the subspace identifier sits at component 1. By D-CTG-depth (SharedPrefixReduction), all positions in V_1(d) share components 2 through m − 1. By D-MIN (VMinimumPosition), the minimum element of V_1(d) is [1, 1, …, 1] — a tuple of length m with every component equal to 1. Since the minimum shares components 2 through m − 1 with every other position, and those components of the minimum are all 1, every position in V_1(d) has components 2 through m − 1 equal to 1. Together with `v₁ = 1`, every position is therefore [1, 1, …, 1, k] for some value k at the m-th component.

**Step 2: minimum k.** By D-MIN, min(V_1(d)) = [1, 1, …, 1] of length m. In the representation [1, 1, …, 1, k], the minimum has k = 1 at the last component. Since the minimum is in V_1(d), the set of k-values attained by positions in V_1(d) includes 1.

**Step 3: contiguity of k-values.** Let k₁ < k₂ be two values attained by positions v₁ = [1, 1, …, 1, k₁] and v₂ = [1, 1, …, 1, k₂] in V_1(d); the comparison `<` here — and every comparison among k-values below — is the strict total order on ℕ supplied by NAT-order (NatStrictTotalOrder, ASN-0034), the order on component values that T1's lexicographic rule consumes at the first differing component but does not itself re-export for arbitrary naturals. Both have subspace 1 and depth m. By T1(i) (LexicographicOrder, ASN-0034), v₁ < v₂ since they agree on components 1 through m − 1 and differ first at component m where k₁ < k₂. For any k ∈ ℕ with k₁ < k < k₂, form the tuple w = [1, 1, …, 1, k] of length m: its leading m − 1 components are all 1 ∈ ℕ — the constant `1` lying in ℕ by NAT-closure (NatArithmeticClosureAndIdentity, ASN-0034) — and its last component is k ∈ ℕ, so by T0's comprehension clause (CarrierSetDefinition, ASN-0034), instantiated at length m and this component map, w ∈ T. This w satisfies subspace(w) = 1, #w = m, and v₁ < w < v₂ (again by T1(i), since w agrees with both on components 1 through m − 1 and k₁ < k < k₂ at component m). Moreover we must discharge D-CTG's remaining guard `zeros(w) = 0`, where `zeros` is T4's zero-count `zeros(t) = |{i : 1 ≤ i ≤ #t ∧ tᵢ = 0}|` (HierarchicalParsing, ASN-0034). By NAT-card's empty-set characterization `|S| = 0 ⟺ S = ∅` (NatFiniteSetCardinality, ASN-0034), this guard holds exactly when the zero-filter `{i : 1 ≤ i ≤ #w ∧ wᵢ = 0}` is empty — that is, when no component of w is zero. Every component of w is strictly positive: the leading m − 1 components are each the constant `1`, positive by NAT-closure's *Consequence* `0 < 1` (read as `1 > 0`); and the last component k exceeds k₁ — the last component of v₁ ∈ V_1(d) ⊆ dom(M(d)), itself positive by S8a's positivity *Consequence* — so k > k₁ > 0 gives k > 0 by NAT-order's transitivity of `<` (`0 < k₁ < k ⟹ 0 < k`). Hence `(A i : 1 ≤ i ≤ #w : wᵢ > 0)`; each positive component is in particular non-zero, so the zero-filter is empty and NAT-card reads its cardinality as 0 — this is `zeros(w) = 0`. The depth half #w = m ≥ 2 inherits the bound S8a places on v₁. With w ∈ T grounding D-CTG's inner v ∈ T quantifier, D-CTG (VContiguity) gives w ∈ V_1(d). Therefore every k ∈ ℕ between any two attained k-values is itself attained — the k-values form a contiguous range.

**Step 4: finiteness.** By S8-fin (Finite arrangement), dom(M(d)) is finite, so V_1(d) ⊆ dom(M(d)) is finite. The k-values form a finite contiguous range.

**Assembly.** The k-values form a finite contiguous set of positive integers (Step 3, Step 4) that contains 1 (Step 2). To pin the postcondition's bound n we must exhibit a *greatest* k-value, and the strict total order alone does not hand us one: NAT-order's irreflexivity, transitivity, and trichotomy order any two naturals but carry no greatest-element principle for a finite set — ℕ has no top, so a greatest element must be *built* from finiteness, not read off the order — and NAT-wellorder furnishes least, not greatest, elements. We build it by induction on the enumeration S8-fin already supplies.

By S8-fin (FiniteArrangement) there are an N ∈ ℕ and a bijection f : {j ∈ ℕ : 1 ≤ j ≤ N} → dom(Σ.M(d)); since V_1(d) ≠ ∅ and V_1(d) ⊆ dom(Σ.M(d)), the codomain is non-empty, so N ≥ 1. Define e : {j ∈ ℕ : 1 ≤ j ≤ N} → ℕ by e.j = k when f.j ∈ V_1(d) carries the Step 1 form [1, 1, …, 1, k] (its last component, an attained k-value), and e.j = 1 otherwise (1 is attained, Step 2). The image {e.j : 1 ≤ j ≤ N} is exactly the set of k-values: each e.j is an attained value by construction, and conversely every attained k is the last component of some v ∈ V_1(d), which is f.j for some j by f's surjectivity, whence e.j = k.

We now establish, by induction on N, the greatest-element principle in the form we need — *for every N ≥ 1 and every h : {j ∈ ℕ : 1 ≤ j ≤ N} → ℕ there is an index J with 1 ≤ J ≤ N and `(A j : 1 ≤ j ≤ N : h.j ≤ h.J)`*. The recursion is the from-`1` specialization of NAT-induction's (NatInduction) generation-from-`0` principle: over the NAT-* axioms induction is not derivable from the order, so well-ordering — which would in any case extract a *least* element — cannot drive it. *Base N = 1:* the lone index has h.1 ≤ h.1 by reflexivity of `≤`, the equality disjunct of NAT-order's `≤`-definition `a ≤ b ⟺ a < b ∨ a = b`; J = 1 serves. *Step N → N + 1:* the induction hypothesis, applied to h restricted to {j ∈ ℕ : 1 ≤ j ≤ N}, hands an index J′ ≤ N with h.j ≤ h.J′ for all j ≤ N. NAT-order's trichotomy on (h.J′, h.(N + 1)) leaves h.(N + 1) < h.J′, h.(N + 1) = h.J′, or h.J′ < h.(N + 1). The first two give h.(N + 1) ≤ h.J′, so J = J′ still maximizes: every j ≤ N has h.j ≤ h.J′, and the new index satisfies h.(N + 1) ≤ h.J′. The third gives J = N + 1: every j ≤ N has h.j ≤ h.J′ < h.(N + 1), hence h.j ≤ h.(N + 1) by the mixed `≤`-then-`<` step — split on the `≤`-definition, `h.j < h.J′` closing through NAT-order's pure `<`-transitivity and `h.j = h.J′` through indiscernibility of `=` — while h.(N + 1) ≤ h.(N + 1) by reflexivity. The principle holds at N + 1, and the induction closes.

Instantiating this principle at h := e and this N yields an index J with e.J ≥ e.j throughout; set n = e.J. As the image of e is exactly the k-values, n is a k-value and dominates every k-value — the greatest element sought. Then n ≥ 1, since the attained value 1 gives 1 ≤ n. By Step 3 applied between the attained endpoints 1 and n, every integer with 1 < k < n is attained, so with the endpoints themselves {1, …, n} ⊆ k-values; and n dominating every k-value gives k-values ⊆ {1, …, n}. Hence the k-values are exactly {1, 2, …, n}. By Step 1, V_1(d) = {[1, 1, …, 1, k] : 1 ≤ k ≤ n}. ∎

D-CTG is a design constraint on well-formed document states. We verify the base case: before any operations, dom(M(d)) = ∅ for all d (the arrangement is a partial function; no content has been allocated, so no V-mapping exists), so V_1(d) = ∅. D-CTG holds vacuously (no u, q exist to trigger its antecedent), and D-MIN holds vacuously (its antecedent requires V_1(d) non-empty).

*Formal Contract:*

- *Preconditions:* V_1(d) is non-empty. The document state is well-formed, so the contiguity constraint D-CTG (VContiguity) and the minimality property D-MIN (VMinimumPosition) hold. All V-positions in the text subspace share a common depth m (S8-depth), with `m ≥ 2` (S8a), and dom(M(d)) is finite (S8-fin).
- *Postconditions:* There exists n ≥ 1 such that V_1(d) = {[1, 1, …, 1, k] : 1 ≤ k ≤ n}, where each tuple has length m. Equivalently, the m-th-component values attained by the positions of V_1(d) are exactly the contiguous set {1, 2, …, n}.
- *Invariant:* In every well-formed state reachable from the empty base state (dom(M(d)) = ∅), D-CTG and D-MIN hold, and consequently V_1(d) is either empty or of the sequential form {[1, 1, …, 1, k] : 1 ≤ k ≤ n}. Across each transition s → s' that preserves well-formedness, this characterization is preserved; the base state satisfies it vacuously since V_1(d) = ∅.

- *Depends:*
  - V-sub (SubspaceProjection) — supplies the definition of the text-subspace projection `V_1(d) = {v ∈ dom(Σ.M(d)) : subspace(v) = 1}` this claim characterizes: the postcondition `V_1(d) = {[1, 1, …, 1, k] : 1 ≤ k ≤ n}` and the precondition `V_1(d) ≠ ∅` are assertions about this set, and Step 1 unfolds the membership `v ∈ V_1(d)` to `v ∈ dom(Σ.M(d)) ∧ subspace(v) = 1` in both the m = 2 and m ≥ 3 cases — the set-defining premise the separately-cited `subspace` selector (`subspace(v) = v₁`) then acts on to conclude `v₁ = 1`
  - S8-depth (FixedDepthVPositions) — supplies the common-depth guarantee for V_1(d); the proof opens by fixing m as that common depth and invokes S8-depth to assert it exists
  - S8a (ArrangementDomainRestriction) — supplies the depth ≥ 2 lower bound (m ≥ 2); the claim states "the derivation below relies on this lower bound" and Step 3 re-invokes it to qualify the intermediate witness w (as #w = m ≥ 2). It also supplies its positivity *Consequence* `dom(Σ.M(d)) ⊆ {t ∈ T : (A i : 1 ≤ i ≤ #t : tᵢ > 0) ∧ #t ≥ 2}`: applied to v₁ ∈ V_1(d) ⊆ dom(M(d)) it gives v₁'s last component k₁ > 0, the floor on which the witness's last component (k > k₁) rests in Step 3's zeros(w) = 0 check — the same Consequence D-CTG-depth reads component-positivity off
  - D-CTG-depth (SharedPrefixReduction) — supplies the shared-prefix result (components 2 through m − 1 identical) used in Step 1 Case m ≥ 3 to pin all but the last component to 1
  - D-MIN (VMinimumPosition) — supplies min(V_1(d)) = [1, 1, …, 1]; Step 1 (Case m ≥ 3) uses it to fix the shared-prefix value to 1, and Step 2 uses it to establish k = 1 as an attained value
  - T1 (LexicographicOrder, ASN-0034) — supplies the lexicographic comparison rule; Step 3 invokes it twice to establish v₁ < v₂ and v₁ < w < v₂ from component-wise comparisons
  - NAT-order (NatStrictTotalOrder, ASN-0034) — supplies the strict total order `<` on ℕ (with companions `≤`, `>`, `≥`) under which Step 3 compares the k-values: `k₁ < k₂` for two attained values, `k₁ < k < k₂` for the intermediate, and `k > k₁` qualifying the witness w. These are comparisons between component values (naturals), distinct from the tumbler comparisons `v₁ < v₂` and `v₁ < w < v₂` that T1 furnishes — T1's lexicographic rule consumes this ℕ-order at the first differing component but does not re-export it for arbitrary naturals, so this consumer grounds it here directly. Its transitivity of `<`, `(A a, b, c ∈ ℕ : a < b ∧ b < c : a < c)`, is instantiated at `(0, k₁, k)` in Step 3's zeros(w) = 0 check, lifting v₁'s last-component positivity `k₁ > 0` (S8a's positivity *Consequence*) along `k > k₁` to `k > 0`, so w's last component is shown non-zero. The same order also drives the Assembly's greatest-element induction, but only as its *comparison* engine, never as the source of the maximum: its trichotomy on each pair (h.J′, h.(N + 1)) selects which index maximizes, its `≤`-reflexivity (the equality disjunct of the `≤`-definition) grounds the base case and the new index's self-comparison, and its pure `<`-transitivity — with the `≤`-definition split — chains `h.j ≤ h.J′ < h.(N + 1)` to `h.j ≤ h.(N + 1)`. The *existence* of the greatest element n is not the order's to give: a finite non-empty subset of ℕ has a greatest element by induction on the enumeration length (NAT-induction, below), the strict total order furnishing no greatest-element principle of its own — ℕ has no top. Once n is in hand, the range bounds `1 ≤ k ≤ n` are read in this order
  - NAT-induction (NatInduction) — supplies the induction principle, in its from-`1` specialization of the generation-from-`0` principle, that establishes the greatest-element existence the Assembly turns on: a finite non-empty subset of ℕ has a greatest element, proved here by induction on the length N of S8-fin's enumerating bijection (base N = 1 a lone index; step N → N + 1 comparing the running maximum against the new value by NAT-order's trichotomy). This existence is exactly what NAT-order cannot furnish — the strict total order orders any two naturals but exposes no greatest element of a finite set, ℕ having no top — and NAT-wellorder would extract a *least*, not a greatest, element; that the NAT-* order-and-addition axioms do not by themselves entail this induction is the classical independence fact NAT-induction is posited to close, exactly as in D-PRED and D-INJ. The maximum n = max(k-values) on which the postcondition's bound rests is grounded here, not in the order
  - T0 (CarrierSetDefinition, ASN-0034) — supplies the comprehension clause that grounds the carrier membership w ∈ T of the explicitly-constructed intermediate w = [1, 1, …, 1, k] (length m, every component ℕ-valued) in Step 3, the prerequisite for instantiating D-CTG's inner v ∈ T quantifier at w
  - NAT-closure (NatArithmeticClosureAndIdentity, ASN-0034) — supplies the axiom clause `1 ∈ ℕ`, the constant component value Step 3 writes directly into the intermediate w = [1, 1, …, 1, k] (its leading m − 1 components are each the literal `1`); this membership is what makes the component map ℕ-valued at every index, the standing precondition for T0's comprehension to represent w ∈ T. T0 writes the same constant `1` into its own index domains but grounds it through T0's own dependence on NAT-closure rather than re-exporting it, so this consumer — writing `1` as a first-class component value — grounds it here directly. Its *Consequence* `0 < 1`, read as `1 > 0`, additionally discharges the positivity of those leading components in Step 3's zeros(w) = 0 check — each `wᵢ = 1 > 0`, one of the two cases (with the last component k > 0) establishing `(A i : 1 ≤ i ≤ #w : wᵢ > 0)` and so the empty zero-filter. The same constant floors the Assembly's maximum, where n ≥ 1 because 1 is an attained k-value (Step 2)
  - T4 (HierarchicalParsing, ASN-0034) — supplies the symbol `zeros` and its zero-count definition `zeros(t) = |{i : 1 ≤ i ≤ #t ∧ tᵢ = 0}|`, the function named in D-CTG's guard `zeros(w) = 0` and unfolded in Step 3's zero-freeness check to turn the constructed w's component-positivity into `zeros(w) = 0`; this grounds `zeros` exactly as S8a and D-CTG, the other consumers of the symbol here, do
  - NAT-card (NatFiniteSetCardinality, ASN-0034) — supplies the cardinality operator `|·|` through its empty-set characterization `|S| = 0 ⟺ S = ∅` (the `k = 0` case of its axiom): in Step 3's zero-freeness check every component of the constructed w is positive, hence none is zero, so the zero-filter `{i : 1 ≤ i ≤ #w ∧ wᵢ = 0}` is empty and NAT-card reads its cardinality as 0, discharging D-CTG's zero-freeness guard. This is the sole NAT-card use here — the finiteness bound is consumed through S8-fin and D-CTG-depth, not through any cardinality collision in this claim
  - D-CTG (VContiguity) — supplies the contiguity constraint; Step 3 applies it to conclude the intermediate witness w ∈ V_1(d), making the k-values a contiguous range
  - S8-fin (FiniteArrangement) — consumed twice. Step 4 reads it as the bare finiteness of dom(Σ.M(d)) to conclude V_1(d) ⊆ dom(Σ.M(d)) is finite, bounding the k-value range. The Assembly then consumes its actual content — the enumerating bijection f : {j ∈ ℕ : 1 ≤ j ≤ N} → dom(Σ.M(d)) — as the substrate over whose length N the greatest-element induction runs: composing f with the last-component reading of Step 1 (filled to the attained value 1 off V_1(d)) presents the k-values as the image of an initial segment, the form the induction maximizes, and V_1(d) ≠ ∅ forces the codomain non-empty so N ≥ 1
  - subspace (VPositionSubspaceIdentifier) — supplies the projection `subspace(v) = v₁`, the definition Step 1 unfolds in both the m = 2 and m ≥ 3 cases to read `subspace(v) = 1` as `v₁ = 1`, pinning the subspace identifier to component 1