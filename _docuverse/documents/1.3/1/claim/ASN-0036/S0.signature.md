- `C` — the content-stream component of a state; `Σ.C` maps I-addresses to their stored content values, and this claim asserts both the domain and values of `C` are fixed across every state transition
- `M` — the arrangement-stream component of a state; `Σ.M(d)` gives the arrangement for document `d`; cited in the frame clause as explicitly unconstrained by this claim
- `dom` — domain of a partial function; used to identify which I-addresses carry assigned content in a given state
