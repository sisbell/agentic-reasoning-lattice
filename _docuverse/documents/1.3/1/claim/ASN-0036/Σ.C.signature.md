- `Σ.C` — the content store; a partial function from tumblers to content values, constituting the primary state object posited by this claim
- `Val` — the opaque set of content values; the codomain of `Σ.C`, left unspecified at this abstraction level
- `⇀` — partial-function arrow; denotes that `Σ.C` is defined only on a subset of `T`, not on all tumblers
- `dom` — domain operator; extracts the set of tumblers at which `Σ.C` is defined, i.e., the set of I-addresses with stored content
