- `Σ.C` — the content store; a partial function from Istream (tumbler) addresses to content values, representing the component of state Σ at which stored content resides
- `Val` — the opaque set of content values; the codomain of `Σ.C`, left unspecified at this level of abstraction
