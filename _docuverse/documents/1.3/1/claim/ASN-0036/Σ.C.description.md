Defines the content store Σ.C as a partial function from tumbler addresses to opaque content values, establishing which I-addresses have stored content via its domain. The content values are intentionally left abstract at this level, making the store applicable across any concrete storage representation.
