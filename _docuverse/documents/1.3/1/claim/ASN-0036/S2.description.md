Proves that the arrangement map Σ.M(d) is a genuine partial function: each V-position in its domain maps to exactly one I-address, so two queries on the same V-position cannot return different answers. This single-image guarantee is unfolded directly from the partial-function declaration with no additional premises.
