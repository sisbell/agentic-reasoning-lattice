- `succ` — the lockstep successor partial function on `dom(M(d))`; `succ(v) = shift(v, 1)` exactly when `shift(v, 1) ∈ dom(M(d))` and `M(d)(shift(v, 1)) = shift(M(d)(v), 1)`, undefined otherwise; its orbits are the maximal correspondence runs
