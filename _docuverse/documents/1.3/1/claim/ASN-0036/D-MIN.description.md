Proves that the minimum V-position in any non-empty document is the all-ones tuple of length m, meaning every document's text block begins at the origin of its subspace. The result follows from three converging constraints: V-positions are 1-indexed (so [1,1,...,1] lower-bounds everything), the block is contiguous (so it must start at that lower bound), and finiteness guarantees the minimum exists.
