Posits that V-positions within a subspace are all the same tumbler depth, making the ordinal (last) component the sole varying digit between consecutive positions in that subspace. This uniformity is what allows "consecutive V-positions" to be defined precisely: two positions are consecutive when they differ only at the final component by one unit. Distinct subspaces may carry distinct depths, but within any single subspace the depth is fixed.
