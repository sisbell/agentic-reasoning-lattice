**AX-1 (InitialEmpty).** The protocol designates a base state `Σ₀` — the configuration that holds before any operation — in which no arrangement maps any V-position:

`(A d ∈ T :: dom(Σ₀.M(d)) = ∅)`

The outer universal binds `d` over the tumbler carrier `T`, exactly the document-level tumblers to which `Σ.M`'s declaration scopes its assignment `d ↦ Σ.M(d)`, so that each `Σ₀.M(d)` — and with it the domain `dom(Σ₀.M(d))` this posit fixes empty — is a defined object, admitting no `d ∉ T` for which `Σ₀.M(d)` would be undefined and that domain ill-formed. Every reachable state is generated from `Σ₀` by the transition relation `Σ → Σ'`. Before any content has been allocated, and before any document arranges any position, each arrangement `Σ₀.M(d)` is the empty partial function, so its domain is empty. We are not deriving this from anything; we are fixing the starting configuration. The point of naming it is methodological: an invariant on `M` proved by induction on transitions needs an explicit, citable anchor for its base case, and the empty base state is that anchor — the configuration on which every such invariant holds vacuously, there being no mapped position to witness a violation.

*Formal Contract:*
- *Axiom:* The base state `Σ₀` satisfies `(A d ∈ T :: dom(Σ₀.M(d)) = ∅)` — no V-position is mapped by any arrangement in the initial configuration. The outer universal binds `d` over the tumbler carrier `T`, exactly the document-level tumblers to which `Σ.M`'s declaration scopes its assignment `d ↦ Σ.M(d)`, so that for every admitted `d` the arrangement `Σ₀.M(d)` — and with it the domain `dom(Σ₀.M(d))` this axiom fixes empty — is a defined object, with no `d ∉ T` let in for which `Σ₀.M(d)` would be undefined and that domain ill-formed. This is a protocol design posit on the designated start state of the state signature, not derived from any other claim.

- *Depends:*
  - Σ.M(d) (Arrangement) — supplies the arrangement family `Σ.M` and its per-document domain `dom(Σ.M(d))`, here read at the base state `Σ₀` as `dom(Σ₀.M(d))`; this axiom fixes that domain empty in the initial configuration. It is likewise this claim's declaration — `Σ.M` assigns an arrangement to each document `d ∈ T` — that scopes the outer universal `(A d ∈ T :: ...)`: restricting `d` to precisely the carrier on which `Σ.M` is defined is what keeps `Σ₀.M(d)`, and with it its domain `dom(Σ₀.M(d))`, a defined object throughout the quantifier, admitting no `d ∉ T` for which `Σ₀.M(d)` would be undefined and that domain ill-formed — the same `d ∈ T` restriction S8-fin's and S8-depth's `(A d ∈ T :: ...)` carry for the identical reason. The emptiness is posited, not derived, but the arrangement-domain symbol it constrains is grounded here at its defining claim, as V-sub grounds the `dom(Σ.M(d))` it ranges over
  - T0 (CarrierSetDefinition, ASN-0034) — furnishes the tumbler carrier `T` over which the outer universal ranges its document index `d ∈ T` — the *restriction* of `d` to `T` read from Σ.M(d)'s declaration above, the carrier `T` it is restricted to grounded here, exactly as the structurally parallel S8-fin grounds the `T` of its own `(A d ∈ T :: ...)` against this same defining claim
