Defines the *strand*: the two-component state object `(Σ.C, Σ.M)` that pairs an immutable content store with a mutable family of per-document arrangements referencing it. Nelson's explicit alternative to destructive whole-file replacement — separating permanent content from mutable arrangement — this is the central state abstraction on which every invariant in the ASN is imposed.
