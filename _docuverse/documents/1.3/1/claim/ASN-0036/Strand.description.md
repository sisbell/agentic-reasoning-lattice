Defines the *strand*: the two-component state object `(Σ.C, Σ.M)` that pairs an immutable content store with the family of mutable per-document arrangements referencing it. This is the central state abstraction of the ASN — Nelson's alternative to destructive whole-file replacement, separating permanent content from mutable arrangement — and the object that every invariant in the ASN constrains.
