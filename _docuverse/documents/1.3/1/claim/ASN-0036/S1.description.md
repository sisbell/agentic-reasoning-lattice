Proves that the content store C grows monotonically across state transitions: any address present in C before a transition remains present after it. This follows directly from S0 (content immutability) and establishes C as an append-only log — new entries may be added at fresh addresses, but no existing entry can be removed or overwritten.
