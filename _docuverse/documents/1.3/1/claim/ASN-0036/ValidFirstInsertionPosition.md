**ValidFirstInsertionPosition (ValidFirstInsertionPosition).** For a document `d` with `V_1(d) = ∅`, the *ternary* predicate `ValidFirstInsertionPosition(d, v, m)` is satisfied when `m ∈ ℕ` with `m ≥ 2` — the depth floor S8a places on every well-formed V-position, so the sole first text position is itself well-formed (a subspace identifier followed by at least one within-subspace ordinal) rather than a bare identifier, which is why the bound is `m ≥ 2` and not the `m ≥ 1` of mere tumbler nonemptiness — and `v = [1, 1, ..., 1]` of depth `m`.

### Valid insertion position examples

**Non-empty case (binary predicate).** Let subspace S = 1 and suppose V₁(d) = {[1, 1], [1, 2], [1, 3]}, so N = 3 and min(V₁(d)) = [1, 1]. The depth `m = 2` is read from state via S8-depth. The values of `v` satisfying `ValidInsertionPosition(d, v)` are:

- j = 0: v = min(V₁(d)) = [1, 1]
- j = 1: v = shift([1, 1], 1) = [1, 2]
- j = 2: v = shift([1, 1], 2) = [1, 3]
- j = 3: v = shift([1, 1], 3) = [1, 4]

That gives N + 1 = 4 positions. Any successor state whose `V₁(d)` gains a position at, say, [1, 2] must still satisfy D-CTG and D-MIN.

**Empty case (ternary predicate).** V₁(d) = ∅. Choosing depth m = 2, the unique `v` satisfying `ValidFirstInsertionPosition(d, v, 2)` is `[1, 1]`. D-MIN requires min(V₁(d)) = [1, 1] once the subspace becomes non-empty, so the position is exactly the one D-MIN demands. Choosing m = 3 instead, `ValidFirstInsertionPosition(d, v, 3)` is satisfied uniquely by `v = [1, 1, 1]`; by T3, this is a different tumbler.

- *Depends:*
  - S8a (ArrangementDomainRestriction) — supplies the depth floor on well-formed V-positions, `dom(Σ.M(d)) ⊆ {t ∈ T : zeros(t) = 0 ∧ #t ≥ 2}`, the source of this predicate's `m ≥ 2` constraint. The sole first text position `[1, …, 1]` of depth `m` is the key D-MIN will require once `V₁(d)` becomes non-empty, so it must be a well-formed V-position — a subspace identifier followed by at least one within-subspace ordinal — and inherits S8a's `#t ≥ 2` as `m ≥ 2`. This is the most concrete symbol the otherwise-cited D-MIN and T3 cannot supply: D-MIN gives only `m ≥ 1` (every tumbler nonempty) and T3 nothing on depth, so the `m ≥ 2` floor — and the rationale for `2` rather than `1` — is grounded here directly, exactly as the companion ValidInsertionPosition cites S8a for the `m ≥ 2` it carries in its preconditions
  - D-MIN (VMinimumPosition) — supplies the axiom that min(V₁(d)) = [1,…,1] once non-empty, establishing that the ternary predicate's choice of v is exactly the position D-MIN demands
  - T0 (CarrierSetDefinition, ASN-0034) — supplies the tumbler carrier `T` and the length operator `#·`, the foundation under which `v = [1, …, 1]` of depth `m` is a tumbler `v ∈ T` with `#v = m`. T0's comprehension — every length `p ≥ 1` paired with a component map `r : {j : 1 ≤ j ≤ p} → ℕ` is realized by some `t ∈ T` with `#t = p` and `tᵢ = r(i)` — is what *places* `[1, …, 1]` in `T`: at length `m` and the constant map `r(i) = 1` it yields the depth-`m` tumbler whose every component is `1`, fixed uniquely by extensionality. D-MIN reaches `[1, …, 1] ∈ T` only transitively, through `min(V₁(d))`; the ASN's discipline — subspace and S8-fin each cite T0 for the `#` and carrier they write even when transitively reachable — grounds the carrier and the depth `#v = m` here at the site `v = [1, …, 1]` of depth `m` is written, so a consumer can independently place `v` in `T` from this claim's Depends alone
  - T3 (CanonicalRepresentation) — supplies tumbler equality (component-wise identity) used to confirm that depth-m [1,…,1] and depth-m' [1,…,1] are distinct tumblers when m ≠ m'
  - NAT-carrier (NatCarrierSet, ASN-0034) — supplies `ℕ`, the carrier in the predicate's typing `m ∈ ℕ` of the depth parameter. `m` is a free argument of the ternary predicate, constrained to lie in ℕ before `m ≥ 2` orders it and `v = [1, …, 1]` of depth `m` consumes it; the membership `m ∈ ℕ` writes ℕ first-class, so its carrier is grounded here directly, as S8-fin grounds the `n ∈ ℕ` of its own witness rather than leaving ℕ ambient
  - NAT-closure (NatArithmeticClosureAndIdentity, ASN-0034) — supplies `1 ∈ ℕ` and the addition `+ : ℕ × ℕ → ℕ`, hence the two numeric constants the predicate writes first-class: the component value `1` filling every position of the all-ones tuple `[1, …, 1]`, and the numeral `2 := 1 + 1` bounding the depth in `m ≥ 2`. T0 uses the same `1` in its index domains but grounds it through its own dependence on NAT-closure rather than exporting it; the predicate writes `1` and `2` as first-class constants, so they are grounded here directly, exactly as subspace and S8-fin ground their own `1`
  - NAT-order (NatStrictTotalOrder, ASN-0034) — supplies the strict total order on ℕ and in particular its non-strict reverse companion `≥`, defined by `m ≥ n ⟺ n ≤ m`, written first-class into the depth bound `m ≥ 2`. The operands are grounded elsewhere — the depth `m` and its `∈ ℕ` typing by NAT-carrier above, the numeral `2` by NAT-closure above — but the relation symbol `≥` joining them is a first-class operator of NAT-order, grounded here directly, exactly as the structurally parallel S8a grounds the `≥` of its own `#t ≥ 2` and as subspace and S8-fin ground their `≤` from this same foundation
- *Forward References:*
  - ValidInsertionPosition (ValidInsertionPosition) — the non-empty companion binary predicate; illustrated in the examples as the parallel case this claim contrasts against
  - S8-depth (FixedDepthVPositions) — supplies the mechanism by which depth m is read from state in the non-empty companion predicate; referenced as explanatory context for how m is obtained in practice
  - D-CTG (VContiguity) — named as a successor-state constraint any post-insertion V₁(d) must satisfy; downstream obligation, not consumed by this claim's definition