**D-CTG-depth (SharedPrefixReduction).** For depth m ≥ 3, all positions in a non-empty V_1(d) — the text-subspace projection `{v ∈ dom(Σ.M(d)) : subspace(v) = 1}` defined in V-sub (SubspaceProjection) — share every interior component: every component i with `2 ≤ i ∧ i + 1 ≤ m`. Contiguity reduces to contiguity of the last component alone — structurally identical to the depth 2 case.

*Proof.* Let V_1(d) be non-empty with common depth `m` (S8-depth) and `m ≥ 3` — a scope restriction ensuring the interior index range `2 ≤ i ∧ i + 1 ≤ m` is non-empty, so the shared-prefix claim has content. We write the interior bound additively as `i + 1 ≤ m` throughout, never as a predecessor of m: the NAT-* foundations of ASN-0034 furnish `+`, `<`, `≤`, `1` on ℕ but no subtraction or predecessor symbol. The `m = 2` case, where that range is empty and the claim is vacuous, is handled separately in D-SEQ's `m = 2` case. Suppose for contradiction that V_1(d) contains two positions u and x (both depth m) that disagree somewhere in the interior range. Negating the postcondition presents them as an unordered pair, so we first fix their ordering. They differ at an interior component, hence by T3 (CanonicalRepresentation, ASN-0034) — tumblers differing in any component are distinct — u ≠ x, which excludes the u = x branch of T1's trichotomy on T (postcondition (b): for any a, b ∈ T exactly one of a < b, a = b, b < a holds, applicable since u, x ∈ V_1(d) ⊆ T). The two surviving branches u < x and x < u are interchanged by relabeling the pair, and the construction is invariant under that relabeling: the disagreement predicate uᵢ ≠ xᵢ is symmetric in u and x, so the interior disagreement set is unchanged by the swap, and the witness construction below is anchored entirely on the smaller member of the pair — copying that member's prefix and raising one component above it — reaching the pair only through its smaller and larger members. A refutation of one ordering is therefore a refutation of the other, and we take u < x without loss of generality. The set `{i : 2 ≤ i ∧ i + 1 ≤ m ∧ uᵢ ≠ xᵢ}` of interior components at which they differ is then a non-empty subset of ℕ, so by NAT-wellorder (NatWellOrdering, ASN-0034) — the least-element principle for ℕ — it has a least element. Let j be that first point of disagreement, with 2 ≤ j ∧ j + 1 ≤ m (the bound holds because j belongs to the interior set). Then uᵢ = xᵢ for all i < j — for interior i by the minimality of j, and at component 1 by the shared subspace value u₁ = x₁ = 1. We now read uⱼ < xⱼ off u < x. From u < x, T1 furnishes a witness position k with uᵢ = xᵢ for all i < k together with either (i) k ≤ min(m, m) ∧ uₖ < xₖ or (ii) k = m + 1 ≤ m; since u and x share depth m, clause (ii) would demand m + 1 ≤ m — impossible, since NAT-addcompat's strict successor inequality `n < n + 1` (NatAdditionOrderAndSuccessor, ASN-0034) at `n := m` gives m < m + 1, which by NAT-order's trichotomy excludes m + 1 ≤ m. So clause (i) holds and uₖ < xₖ. It remains to pin this witness to k = j. Were k < j, then k satisfies 1 ≤ k < j, where we have already established agreement, giving uₖ = xₖ and contradicting uₖ < xₖ; so k ≥ j. Were k > j, then the witness's own agreement clause `(A i : 1 ≤ i < k : uᵢ = xᵢ)` covers i = j, giving uⱼ = xⱼ and contradicting j's standing as a point of disagreement; so k ≤ j. Hence k = j, and clause (i) at this position reads uⱼ < xⱼ.

We construct infinitely many intermediates. For any natural number n > uⱼ₊₁, define w of length m by:

- wᵢ = uᵢ for 1 ≤ i ≤ j (agreeing with u on the first j components),
- wⱼ₊₁ = n,
- wᵢ = 1 for j + 2 ≤ i ≤ m (an empty range when j + 1 = m, in which case wⱼ₊₁ = w_m is already the last component; otherwise this clause fills components j + 2 through m).

Every component of w lies in ℕ — wᵢ = uᵢ ∈ ℕ for 1 ≤ i ≤ j since u ∈ T, wⱼ₊₁ = n ∈ ℕ, and wᵢ = 1 ∈ ℕ for j + 2 ≤ i ≤ m, the constant `1` lying in ℕ by NAT-closure (NatArithmeticClosureAndIdentity, ASN-0034) — and w has length m ≥ 3 ≥ 1, so by T0's comprehension clause (CarrierSetDefinition, ASN-0034), instantiated at length m and this component map, w ∈ T: the construction yields a genuine tumbler, which is what later lets us instantiate D-CTG's inner v ∈ T quantifier at the constructed w. Then w has depth m (it has m components by construction), and subspace(w) = w₁ = u₁ = 1 (since j ≥ 2, the first component is copied from u). We verify u < w < x:

- **w > u**: w agrees with u on components 1 through j. At component j + 1, wⱼ₊₁ = n > uⱼ₊₁. Since j + 1 ≤ m = min(m, m), by T1(i), w > u.
- **w < x**: w agrees with x on every component i with 1 ≤ i < j (since u and x agree on these components by the definition of j). At component j, wⱼ = uⱼ < xⱼ. T1(i)'s position condition asks for j ≤ min(m, m) = m, which we obtain in the sharper form j < m directly from the additive interior bound j + 1 ≤ m: NAT-addcompat's strict successor inequality `n < n + 1` (NatAdditionOrderAndSuccessor, ASN-0034) at `n := j` gives j < j + 1, and chaining this with j + 1 ≤ m through NAT-order's transitivity yields j < m. Hence j < m = min(m, m), so by T1(i), w < x.

Before invoking D-CTG we must check its inner-quantifier guard on w, of which the only conjunct not yet in hand is the zero-freeness `zeros(w) = 0` — where `zeros` is T4's zero-count `zeros(t) = |{i : 1 ≤ i ≤ #t ∧ tᵢ = 0}|` (HierarchicalParsing, ASN-0034). By NAT-card's empty-set characterization `|S| = 0 ⟺ S = ∅` (NatFiniteSetCardinality, ASN-0034), `zeros(t) = 0` holds exactly when the component-filter `{i : 1 ≤ i ≤ #t ∧ tᵢ = 0}` is empty — that is, when no component of t is zero. We use this zeros-to-positivity bridge in the direction that turns the constructed w's component-positivity into `zeros(w) = 0`. By construction, every component of w is positive: wᵢ = uᵢ > 0 for i ≤ j by S8a's positivity *Consequence*, which exports `(A i : 1 ≤ i ≤ #u : uᵢ > 0)` and so gives u's components their strict positivity directly (u ∈ V_1(d) ⊆ dom(M(d)), so S8a applies); for the new component, wⱼ₊₁ = n with n > uⱼ₊₁ by construction and uⱼ₊₁ > 0 by that same S8a Consequence, so NAT-order's transitivity of `<` (`0 < uⱼ₊₁ < n ⟹ 0 < n`, NatStrictTotalOrder, ASN-0034), instantiated at the ℕ-valued uⱼ₊₁ and n, gives wⱼ₊₁ = n > 0; and wᵢ = 1 > 0 for j + 2 ≤ i ≤ m, the strict inequality `1 > 0` being NAT-closure's *Consequence* `0 < 1` (NatArithmeticClosureAndIdentity, ASN-0034). Hence `(A i : 1 ≤ i ≤ #w : wᵢ > 0)`; each positive component is in particular non-zero, so the zero-filter `{i : 1 ≤ i ≤ #w ∧ wᵢ = 0}` is empty and its cardinality is 0 by NAT-card — this is `zeros(w) = 0`, so w meets D-CTG's guard. S8a itself is not among those guards: its depth half #w ≥ 2 plays no part in the contiguity step, and in any case holds independently, since #w = m ≥ 3 ≥ 2. The dependence in fact runs the other way — D-CTG's conclusion places w in V_1(d) ⊆ dom(M(d)), where every position satisfies S8a, so w's satisfying the full S8a is a consequence of the membership we are about to derive, not a precondition for deriving it.

Since u < w < x, subspace(w) = 1, #w = m = #u, and zeros(w) = 0, D-CTG requires w ∈ V_1(d). We now exhibit infinitely many admissible values of n, each read off T0(a)'s postcondition. That postcondition delivers not a bare natural but a tumbler: instantiated at a supplied tumbler t, a component index i, and a bound M ∈ ℕ, T0(a) (UnboundedComponentValues, ASN-0034) furnishes a tumbler t′ ∈ T with #t′ = #t that agrees with t at every component k ≠ i and has t′ᵢ > M. We instantiate it at t = u and i = j + 1 — placing the large value at exactly the component our witness w varies — and read off the natural n := t′ⱼ₊₁ ∈ ℕ (a component of a tumbler, hence in the carrier ℕ by T0), which satisfies n > M. Iterating the bound: starting from M₀ = uⱼ₊₁, this instantiation supplies a tumbler whose (j + 1)-th component n₁ > M₀; setting M₁ = n₁ and re-instantiating at t = u, i = j + 1, M = M₁ supplies a further tumbler whose (j + 1)-th component n₂ > M₁ ≥ n₁; continuing, we obtain a strictly increasing sequence n₁ < n₂ < n₃ < … of natural numbers, all exceeding uⱼ₊₁. The sequence is infinite and pairwise distinct, and distinct values of n yield distinct witnesses: writing w⁽ᵏ⁾ for the depth-m tumbler the construction builds at n := nₖ — its (j + 1)-th component being nₖ — indices k ≠ ℓ give nₖ ≠ nₗ, so w⁽ᵏ⁾ and w⁽ˡ⁾ differ at component j + 1 and are unequal by T3 (CanonicalRepresentation, ASN-0034). Each w⁽ᵏ⁾ is an instance of the generic witness w just placed in V_1(d), so D-CTG deposits every w⁽ᵏ⁾ in V_1(d) ⊆ dom(Σ.M(d)).

To finish we must turn this abundance into a contradiction with S8-fin, and we must draw it from S8-fin's actual content — an enumerating bijection from an initial segment of ℕ — not from the bare word *finite*: the absurdity is that dom(Σ.M(d)), enumerated by a finite segment, would carry more distinct positions than that segment has indices, and we extract it arithmetically. Instantiate S8-fin at our document d. It furnishes a bound N ∈ ℕ and a bijection f : {r ∈ ℕ : 1 ≤ r ≤ N} → dom(Σ.M(d)) — total into the domain, injective by its clause `(A i, r : 1 ≤ i < r ≤ N : f.i ≠ f.r)`, and surjective by its clause `(A v : v ∈ dom(Σ.M(d)) : (E r : 1 ≤ r ≤ N : f.r = v))`; we name the bound N, holding it apart from the sequence values nₖ. The sequence n₁ < n₂ < … being infinite has at least N + 1 terms, so we keep the first N + 1 witnesses w⁽¹⁾, …, w⁽ᴺ⁺¹⁾, all distinct and all in dom(Σ.M(d)). By the surjectivity clause each is enumerated, so for 1 ≤ k ≤ N + 1 we may choose rₖ ∈ {r ∈ ℕ : 1 ≤ r ≤ N} with f.rₖ = w⁽ᵏ⁾. The assignment k ↦ rₖ is injective: were rₖ = rₗ for some k ≠ ℓ, the one value f assigns that index would be both w⁽ᵏ⁾ and w⁽ˡ⁾, forcing w⁽ᵏ⁾ = w⁽ˡ⁾ against their distinctness — this is f read as the single-valued function its typing f : {r ∈ ℕ : 1 ≤ r ≤ N} → dom(Σ.M(d)) makes it, the injectivity clause being the companion half of the same bijection. Hence its image {rₖ : 1 ≤ k ≤ N + 1} is a subset of {r ∈ ℕ : 1 ≤ r ≤ N} carrying N + 1 pairwise-distinct members, and on this set an upper bound and an exact count collide. NAT-card's (NatFiniteSetCardinality, ASN-0034) upper-bound clause `(A n ∈ ℕ, S : S ⊆ {r ∈ ℕ : 1 ≤ r ≤ n} :: |S| ≤ n)`, at n := N and S := {rₖ : 1 ≤ k ≤ N + 1}, gives |{rₖ : 1 ≤ k ≤ N + 1}| ≤ N. For the exact count we invoke D-INJ (InjectiveImageCardinality), instantiated at P := N + 1 and n := N: the assignment k ↦ rₖ is an injection from {k ∈ ℕ : 1 ≤ k ≤ N + 1} into {r ∈ ℕ : 1 ≤ r ≤ N} — injective by the single-valuedness of f just used, and landing in that segment because each rₖ was selected from {r ∈ ℕ : 1 ≤ r ≤ N} — so D-INJ reads its image's cardinality as |{rₖ : 1 ≤ k ≤ N + 1}| = N + 1. D-INJ discharges, by an explicit induction on the segment length {k ∈ ℕ : 1 ≤ k ≤ N + 1}, exactly the step a bare counting argument would leave circular: it *builds* the strictly increasing length-(N + 1) enumeration NAT-card's value clause demands — deleting the least value, renumbering the surviving indices to apply the induction hypothesis, and prepending the least value to the sorted remainder — with the number of enumeration slots fixed by the induction, not by positing that a successive-minimum draw runs for N + 1 steps (which would presuppose the very cardinality N + 1 being derived). The lemma carries no `N + 1 ≤ N` hypothesis, so it applies in this regime where the segment {1 ≤ k ≤ N + 1} is longer than its target {1 ≤ r ≤ N}. Putting the upper bound against the exact count, N + 1 = |{rₖ : 1 ≤ k ≤ N + 1}| ≤ N, that is N + 1 ≤ N. But NAT-addcompat's strict successor inequality `(A n ∈ ℕ :: n < n + 1)` (NatAdditionOrderAndSuccessor, ASN-0034) at n := N gives N < N + 1, and chaining N < N + 1 with N + 1 ≤ N through NAT-order's transitivity yields N < N, which NAT-order's irreflexivity `(A n ∈ ℕ :: ¬(n < n))` (NatStrictTotalOrder, ASN-0034) forbids. This is the contradiction the construction was built to reach: no initial segment {r ∈ ℕ : 1 ≤ r ≤ N} enumerates a dom(Σ.M(d)) holding the N + 1 distinct positions w⁽¹⁾, …, w⁽ᴺ⁺¹⁾ that D-CTG forces into V_1(d).

Therefore the supposed disagreeing pair cannot exist: the case u < x is refuted by the contradiction just reached, x < u collapses onto it by the relabeling fixed at the outset, and u = x was excluded by the disagreement itself. No two positions in V_1(d) can disagree at any component j with 2 ≤ j ∧ j + 1 ≤ m; every pair agrees on every interior component. This is the shared-prefix result, and it is the whole content the lemma carries downstream.

Since subspace(p) = p₁ = 1 for every p ∈ V_1(d), this agreement extends downward to component 1: any two positions of V_1(d) agree on their entire prefix — every component but the last — and so differ from one another only at the last component m. Contiguity among the positions of V_1(d) is thereby settled at component m alone — structurally identical to the depth-2 case, where the shared prefix is the lone subspace component v₁ = 1 (at m = 2 only the first component precedes the last). We record this reduction to the last component as an immediate reading-off of the shared prefix, not as a separate result: once every position is known to share every component but the last, the last component is the only site of variation, and any ordering or contiguity question among the positions is decided there. ∎

Nelson's statement specifies not just contiguity but also the starting ordinal: "addresses 1 through 100," not "42 through 141." All ordinal numbering in the tumbler system starts at 1: the first child is always .1 (LM 4/20), link positions within a document begin at 1 (LM 4/31), and position 0 is structurally unavailable since zero serves as a field separator (T4, ASN-0034). V-positions follow the same convention.

*Formal Contract:*

- *Preconditions:*
  - V_1(d) ≠ ∅ (non-empty).
  - All positions in V_1(d) share a common depth m (S8-depth), with m ≥ 3 — a scope restriction, not a derived bound: at m ≥ 3 the interior index range 2 ≤ i ∧ i + 1 ≤ m is non-empty, so the shared-prefix claim has content, whereas the m = 2 case, where that range is empty and the claim is vacuous, is handled separately in D-SEQ's m = 2 case.
  - Every position p ∈ V_1(d) has subspace(p) = p₁ = 1.
  - V_1(d) ⊆ dom(M(d)), and every position in dom(M(d)) satisfies S8a, whose positivity *Consequence* supplies `#p ≥ 2 ∧ (A i : 1 ≤ i ≤ #p : pᵢ > 0)` — the strict-positivity reading of S8a's Axiom `zeros(p) = 0 ∧ #p ≥ 2`.
  - V_1(d) is contiguous in the position order (D-CTG): if u, x ∈ V_1(d), u < w < x, subspace(w) = 1, #w = #u, and zeros(w) = 0, then w ∈ V_1(d).
  - dom(M(d)) is finite (S8-fin), in the grounded form S8-fin posits: for the document d there exist a bound N ∈ ℕ and a bijection f : {r ∈ ℕ : 1 ≤ r ≤ N} → dom(Σ.M(d)) — total, injective (`(A i, r : 1 ≤ i < r ≤ N : f.i ≠ f.r)`), and surjective (`(A v : v ∈ dom(Σ.M(d)) : (E r : 1 ≤ r ≤ N : f.r = v))`). The proof instantiates this bijection and pulls N + 1 selected positions back through its surjectivity to N + 1 pairwise-distinct indices in {r ∈ ℕ : 1 ≤ r ≤ N}, bounding the number of distinct positions the construction can deposit.
  - Component values are unbounded (T0(a), ASN-0034): instantiated at a tumbler t, a component index i, and a bound M ∈ ℕ, T0(a) supplies a tumbler t′ ∈ T with #t′ = #t agreeing with t at every component k ≠ i and with t′ᵢ > M; the i-th component is then a natural n = t′ᵢ > M. The proof instantiates this at t = u and i = j + 1, extracting n = t′ⱼ₊₁.

- *Postconditions:*
  - `(A u, x : u ∈ V_1(d) ∧ x ∈ V_1(d) : (A i : 2 ≤ i ∧ i + 1 ≤ m : uᵢ = xᵢ))` — every pair of positions in V_1(d) agrees on every interior component (those i with 2 ≤ i ∧ i + 1 ≤ m). This is the shared-prefix result D-SEQ consumes (Step 1, Case m ≥ 3) to pin every position to the form [1, …, 1, k]. Combined with subspace(p) = p₁ = 1, which fixes component 1, the agreement covers the whole prefix (every component but the last — those i with 1 ≤ i ∧ i + 1 ≤ m); the positions of V_1(d) then differ only at the last component, so their contiguity is settled at component m alone — the same structural condition as the depth-2 case. This last reduction is an immediate reading-off of the shared prefix, not a separately consumed result.

- *Definition:* For positions u, x ∈ V_1(d) (u < x, both depth m) whose first disagreement is at component j with 2 ≤ j ∧ j + 1 ≤ m, and for any n > uⱼ₊₁, the intermediate witness w of depth m is constructed by: wᵢ = uᵢ for 1 ≤ i ≤ j; wⱼ₊₁ = n; wᵢ = 1 for j + 2 ≤ i ≤ m (an empty clause when j + 1 = m).

- *Depends:*
  - V-sub (SubspaceProjection) — supplies the definition of the text-subspace projection `V_1(d) = {v ∈ dom(Σ.M(d)) : subspace(v) = 1}` the proof reasons about; the preconditions `V_1(d) ≠ ∅` and `V_1(d) ⊆ dom(M(d))`, and the membership tests `u, x ∈ V_1(d)` and `w ∈ V_1(d)`, are all stated against this set, each membership unfolding to `· ∈ dom(Σ.M(d)) ∧ subspace(·) = 1`
  - S8-depth (FixedDepthVPositions) — supplies the shared depth `m` for all positions in V_1(d), consumed as the proof's starting invariant that all elements have a common depth before the contradiction is constructed
  - T1 (LexicographicOrder, ASN-0034) — supplies two postconditions of the lexicographic order. Its trichotomy on T (postcondition (b): for any a, b ∈ T exactly one of a < b, a = b, b < a) is invoked at the contradiction's outset to fix the ordering of the negated postcondition's unordered pair: with u = x excluded by T3, the two ordered branches u < x and x < u remain, and relabeling collapses x < u onto u < x — the disagreement predicate and the witness construction being symmetric under the swap — so the single ordering u < x is treated without loss of generality. Its component-comparison clause (i) is invoked in the shared-prefix construction to read uⱼ < xⱼ off u < x and to derive u < w and w < x for the constructed intermediate w from its first differing component (component j + 1 for w > u, component j for w < x)
  - NAT-order (NatStrictTotalOrder, ASN-0034) — supplies the transitivity of `<` on ℕ, `(A m, n, p ∈ ℕ : m < n ∧ n < p : m < p)`, instantiated at `(0, uⱼ₊₁, n)` in the zero-freeness check to conclude the new component wⱼ₊₁ = n is positive (`0 < uⱼ₊₁ < n ⟹ 0 < n`) from S8a's positivity *Consequence* (`uⱼ₊₁ > 0`) and the construction's n > uⱼ₊₁; this component-positivity of wⱼ₊₁ is one of the three cases establishing `(A i : 1 ≤ i ≤ #w : wᵢ > 0)`, i.e. the `zeros(w) = 0` guard D-CTG requires. The same order supplies, alongside NAT-addcompat's strict successor inequality, two further steps: its trichotomy makes `m + 1 ≤ m` false against `m < m + 1` (ruling out T1 clause (ii) when #u = #x = m), and its transitivity chains `j < j + 1` with the interior bound `j + 1 ≤ m` into `j < m` (which the w < x branch consumes for T1 clause (i)'s position condition). The closing finiteness step draws on the same order twice more: its transitivity chains `N < N + 1` (NAT-addcompat) with the counting bound `N + 1 ≤ N` — NAT-card's upper bound set against D-INJ's exact count — into `N < N`; and its irreflexivity `(A n ∈ ℕ :: ¬(n < n))` rejects `N < N`, completing the contradiction. The trichotomy that verifies the constructed enumeration strictly increases now operates inside D-INJ, where that enumeration is built from `{rₖ : 1 ≤ k ≤ N + 1}`
  - NAT-addcompat (NatAdditionOrderAndSuccessor, ASN-0034) — supplies the strict successor inequality `(A n ∈ ℕ :: n < n + 1)`, instantiated at three points. At `n := m` it gives `m < m + 1`, which by NAT-order's trichotomy makes `m + 1 ≤ m` false; this discharges the impossibility of T1 clause (ii)'s `k = m + 1 ≤ m` when u and x share depth m, forcing clause (i) and so uₖ < xₖ. At `n := j` it gives `j < j + 1`, which chained with the interior bound `j + 1 ≤ m` through NAT-order's transitivity yields `j < m`, consumed in the w < x branch to meet T1 clause (i)'s position condition (witness index k = j ≤ min(m, m) = m). At `n := N` — S8-fin's enumeration bound — it gives `N < N + 1`, which set against the counting bound `N + 1 ≤ N` (NAT-card's upper bound on the pulled-back index set against D-INJ's exact count) yields `N < N` through NAT-order's transitivity, the contradiction that closes the finiteness step
  - NAT-wellorder (NatWellOrdering, ASN-0034) — supplies the least-element principle for ℕ (every non-empty subset of ℕ has a least element). In the contradiction setup it names a least point of disagreement: applied to the interior components `{i : 2 ≤ i ∧ i + 1 ≤ m ∧ uᵢ ≠ xᵢ}` where u and x differ, it furnishes the first disagreement j — extracting the minimum of a non-empty subset of ℕ, the same construction TumblerAdd (ASN-0034) makes in its divergence sub-case. The closing finiteness step's successive-minimum sort, which also rests on this least-element principle (and on it as the source of an induction on the segment length), is now carried by D-INJ rather than inline here: D-INJ is where the strictly increasing length-(N + 1) enumeration NAT-card's value clause requires is built and its length fixed
  - S8a (ArrangementDomainRestriction) — supplies, through its positivity *Consequence* `dom(Σ.M(d)) ⊆ {t ∈ T : (A i : 1 ≤ i ≤ #t : tᵢ > 0) ∧ #t ≥ 2}` (the strict-positivity reading of S8a's `zeros(t) = 0` Axiom, exported by S8a so this consumer cites a formal-contract export rather than body content), the well-formedness predicate `#p ≥ 2 ∧ (A i : 1 ≤ i ≤ #p : pᵢ > 0)`; applied to u ∈ V_1(d) ⊆ dom(M(d)) it gives u's components their strict positivity (each > 0), which the witness construction propagates to w to discharge D-CTG's `zeros(w) = 0` guard
  - D-CTG (VContiguity) — supplies the contiguity axiom applied to force w ∈ V_1(d) from u < w < x with matching subspace, depth (#w = #u), and zero-freeness (zeros(w) = 0); the claim's contradiction rests on producing infinitely many such w
  - NAT-closure (NatArithmeticClosureAndIdentity, ASN-0034) — supplies (a) the axiom clause `1 ∈ ℕ`, the constant component value written directly into the witness construction's component map (`wᵢ = 1` for j + 2 ≤ i ≤ m); this membership is what makes the component map ℕ-valued at every index, the standing precondition for T0's comprehension `(A r : {j ∈ ℕ : 1 ≤ j ≤ p} → ℕ :: (E t ∈ T :: …))` to represent w in T. T0 writes the same constant `1` into its own index domains but grounds it through T0's own dependence on NAT-closure rather than re-exporting it as a postcondition, so this consumer — writing `1` as a first-class component value — grounds it here directly. And (b) the *Consequence* `0 < 1`, read as `1 > 0` in the zeros step to discharge `wᵢ = 1 > 0` for j + 2 ≤ i ≤ m, one of the three cases establishing `(A i : 1 ≤ i ≤ #w : wᵢ > 0)`, i.e. zeros(w) = 0, that meets D-CTG's zero-freeness guard
  - T4 (HierarchicalParsing, ASN-0034) — supplies the symbol `zeros` and its zero-count definition `zeros(t) = |{i : 1 ≤ i ≤ #t ∧ tᵢ = 0}|`, the function carried in D-CTG's restated guard `zeros(w) = 0` (a precondition above) and unfolded in the proof's zero-freeness check to turn the constructed w's component-positivity into `zeros(w) = 0` (u's positivity is read directly off S8a's positivity *Consequence*, so only this w-direction crosses the zeros-to-positivity equivalence here); this grounds `zeros` exactly as S8a and D-CTG, the other direct consumers of the symbol, do. The same `zeros` underlies the post-proof remark that V-position component 0 is structurally unavailable because zero is T4's field separator, fixing the 1-based ordinal convention for V-positions
  - NAT-card (NatFiniteSetCardinality, ASN-0034) — supplies the cardinality operator `|·|` in two steps. In the zero-freeness check it is read through the empty-set characterization `|S| = 0 ⟺ S = ∅`: every component of the constructed w is positive, hence none is zero, so the zero-filter `{i : 1 ≤ i ≤ #w ∧ wᵢ = 0}` is empty and NAT-card reads its cardinality as 0, discharging D-CTG's guard (u's own positivity needs no cardinality step here — it is S8a's positivity *Consequence* `(A i : uᵢ > 0)` read directly off u ∈ dom(M(d)) — so NAT-card enters that step only in the w-direction, grounding it exactly as S8a does). In the closing finiteness step its upper-bound clause `(A n ∈ ℕ, S : S ⊆ {r ∈ ℕ : 1 ≤ r ≤ n} :: |S| ≤ n)`, at n := N on the index set `{rₖ : 1 ≤ k ≤ N + 1} ⊆ {r ∈ ℕ : 1 ≤ r ≤ N}` pulled back through S8-fin's bijection, gives `|{rₖ : 1 ≤ k ≤ N + 1}| ≤ N`; set against the exact count `|{rₖ : 1 ≤ k ≤ N + 1}| = N + 1` that D-INJ derives, the two together force `N + 1 ≤ N`, the arithmetic absurdity the contradiction rests on. NAT-card's value clause — `|S|` is the unique q admitting a strictly increasing length-q enumeration of S — is the engine of that exact count, but it is now exercised inside D-INJ (which builds the length-(N + 1) enumeration by induction on the segment length) rather than directly here
  - D-INJ (InjectiveImageCardinality) — supplies the exact count of the pulled-back index set in the closing finiteness step: with k ↦ rₖ an injection from `{k ∈ ℕ : 1 ≤ k ≤ N + 1}` into `{r ∈ ℕ : 1 ≤ r ≤ N}`, D-INJ reads `|{rₖ : 1 ≤ k ≤ N + 1}| = N + 1`, *building* the strictly increasing length-(N + 1) enumeration NAT-card's value clause requires by an explicit induction on the segment length rather than by a step count that would presuppose the cardinality being derived. Set against NAT-card's upper bound `|·| ≤ N` this yields the impossible `N + 1 ≤ N`. The successive-minimum sorting and its termination, formerly carried inline in this proof, now live in D-INJ; the lemma carries no `P ≤ n` hypothesis, so it applies in the longer-into-shorter regime this contradiction needs
  - T0 (CarrierSetDefinition, ASN-0034) — supplies the comprehension clause that grounds the carrier membership w ∈ T of the explicitly-constructed witness w (length m, every component ℕ-valued), the prerequisite for instantiating D-CTG's inner v ∈ T quantifier at w
  - T0(a) (UnboundedComponentValues, ASN-0034) — instantiated at the tumbler u and component index i = j + 1 with successive bounds M, supplies a tumbler t′ ∈ T agreeing with u off component j + 1 and with t′ⱼ₊₁ > M; the extracted natural n = t′ⱼ₊₁ drives the strictly increasing sequence n₁ < n₂ < … of admissible intermediates that contradicts S8-fin
  - T3 (CanonicalRepresentation, ASN-0034) — supplies tumbler equality as component-wise identity, used in two places: at the contradiction's outset, its reverse direction (tumblers differing in any component are distinct) gives u ≠ x from the interior disagreement, excluding the u = x branch of T1's trichotomy and leaving only the two ordered cases; and in the divergence step, to conclude that distinct values of n yield distinct depth-m positions w (differing at component j + 1)
  - S8-fin (FiniteArrangement) — supplies the finiteness of dom(Σ.M(d)) in its grounded form: a bound N ∈ ℕ and an enumerating bijection f : {r ∈ ℕ : 1 ≤ r ≤ N} → dom(Σ.M(d)), with the injectivity clause `(A i, r : 1 ≤ i < r ≤ N : f.i ≠ f.r)` and the surjectivity clause `(A v : v ∈ dom(Σ.M(d)) : (E r : 1 ≤ r ≤ N : f.r = v))`. The proof is by contradiction and consumes this bijection, not the bare word *finite*: from the strictly increasing sequence n₁ < n₂ < … it keeps the first N + 1 witnesses w⁽¹⁾, …, w⁽ᴺ⁺¹⁾ (distinct positions of dom(Σ.M(d))), uses the surjectivity clause to choose indices rₖ ∈ {r ∈ ℕ : 1 ≤ r ≤ N} with f.rₖ = w⁽ᵏ⁾, and reads off N + 1 pairwise-distinct such indices (k ↦ rₖ injective because f is single-valued, distinct images forcing distinct indices). NAT-card's upper bound caps |{rₖ : 1 ≤ k ≤ N + 1}| at N while D-INJ's injective-image count sets it at N + 1 (k ↦ rₖ being an injection from {k ∈ ℕ : 1 ≤ k ≤ N + 1} into {r ∈ ℕ : 1 ≤ r ≤ N}), and N + 1 ≤ N collapses against N < N + 1 (NAT-addcompat) by NAT-order. Finiteness is thus consumed as the bijection, never as a numeric cardinality value, exactly as S8-fin's contract advertises for its consumers
  - subspace (VPositionSubspaceIdentifier) — supplies the projection `subspace(p) = p₁`, unfolded in the witness construction's step `subspace(w) = w₁ = u₁ = 1`, where subspace(w) = 1 is what the constructed witness w must satisfy to meet D-CTG's `subspace(v) = 1` guard, and again in reading `subspace(p) = p₁ = 1` for every p ∈ V_1(d) to extend the established agreement downward from the interior components (those i with 2 ≤ i ∧ i + 1 ≤ m) to the full prefix (every component but the last)