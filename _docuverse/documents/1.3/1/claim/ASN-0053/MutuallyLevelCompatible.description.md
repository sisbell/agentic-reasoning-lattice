Defines the condition under which a collection of spans inhabits a single consistent tumbler level: all start tumblers must share a common length L. When the individual spans are also level-uniform, this single condition lifts to a global guarantee — every boundary tumbler across the entire set (start, width, and reach of every span) has length L, so any two endpoints drawn from any pair of spans are directly comparable.
