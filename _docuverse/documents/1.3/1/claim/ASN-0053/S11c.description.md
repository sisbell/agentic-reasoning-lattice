Proves that for two level-uniform spans in proper overlap (SC case iii), their set difference is always expressible as exactly one span. In both sub-cases — where α leads into β, or β leads into α — the difference is a contiguous interval whose endpoints are drawn from the non-overlapping boundary, and an explicit witness span is constructed to cover it.
