Proves that every finite set of positions P can be covered by a span-set Σ of equal cardinality (one span per position), and that this covering guarantee is the strongest available: because every span denotes infinitely many positions — each base tumbler has infinitely many depth-extended successors that fall inside it — no non-empty finite P can ever be exactly represented as the denotation of any span-set.
