Proves that spans are convex: every position falling between two positions that belong to a span also belongs to that span, so a span can never skip interior positions. This holds because the tumbler order is total and lexicographic, meaning sub-addresses that are numerically intermediate between two endpoints are genuinely interior — no position between two members of a span can be excluded.
