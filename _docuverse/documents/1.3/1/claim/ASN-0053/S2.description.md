Proves that no span denotes the empty set of positions — every well-formed span covers at least one position. As a consequence, "no span" and "empty span" are distinct concepts: when two disjoint spans fail to overlap, the result is the absence of a span, not a zero-width one.
