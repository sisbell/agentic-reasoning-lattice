Defines level-uniformity as the invariant that a span's start and limit tumblers share the same length, collapsing start, width, and reach into a single common tumbler depth. For a level-uniform span, the result-length identity guarantees that all three components are commensurable without any additional computation. The implementation enforces this invariant at the split operation, aborting when the cut and width do not share a tumbler length.
