Proves that when two level-uniform spans are separated or adjacent — the cases where SC classifies their position sets as disjoint — subtracting one from the other leaves the minuend unchanged: ⟦α⟧ \ ⟦β⟧ = ⟦α⟧. Because disjointness means no positions are shared, the subtraction removes nothing, and the result is always a single span.
