Proves that for any two level-uniform spans, their set difference is always expressible as a span-set of at most 2 spans. The bound is tight — the containment case achieves exactly 2 — and no configuration requires more, confirming that the span-set mechanism suffices to represent arbitrary pairwise differences.
