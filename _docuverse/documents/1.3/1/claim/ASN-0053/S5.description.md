Proves that when a span σ is split at midpoint p, the two resulting widths d and d' compose under tumbler addition to exactly the original width ℓ. The result follows by chaining the two half-span displacement equations through tumbler associativity, then canceling the shared base address via left-cancellation to isolate d ⊕ d' = ℓ.
