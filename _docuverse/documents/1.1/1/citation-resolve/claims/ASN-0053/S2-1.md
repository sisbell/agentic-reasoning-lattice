# Citation Resolve — ASN-0053/S2 — run 1

*2026-06-20T23:48:27Z*
*Model: sonnet*

## Output

S7's `*Depends:*` block confirms both labels' home ASN. Both TA-strict and T12 belong to ASN-0034.

**TA-strict** appears twice in the prose: once as the source of ℓ > 0, and once in the closing paragraph ("TA-strict forbids" ℓ = 0). Both uses are load-bearing — the proof's starting condition and the final closure argument each rest on it. → `depends`

**T12** is invoked as the explicit theorem that converts ℓ > 0 and k ≤ #s into the strict inequality s ⊕ ℓ > s, which is the single active proof step. → `depends`

No forward references appear in the prose. Currently-classified lists are empty, so no retractions.

```
CLASSIFICATIONS:
- label: TA-strict
  direction: depends
  bullet: "- TA-strict (StrictIncrease, ASN-0034) — supplies the axiom ℓ > 0 (strictly positive length for every well-formed span), the well-formedness precondition the proof starts from and the closure argument that zero-width spans are forbidden"

- label: T12
  direction: depends
  bullet: "- T12 (SpanWellDefinedness, ASN-0034) — supplies strict monotonicity of advancement (s ⊕ ℓ > s given ℓ > 0 and k ≤ #s), the single proof step from which s ∈ [s, s ⊕ ℓ) is derived"

RETRACTIONS: []
```
