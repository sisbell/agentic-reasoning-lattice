# Citation Resolve — ASN-0053/S3 — run 1

*2026-06-20T23:34:13Z*
*Model: sonnet*

## Output

The prose contains three label references: `S6`, `WF`, and `S1`. I've read all three. Now classifying:

- **S6** (*LevelConstraint*) — invoked in the proof as "Level-uniformity and S6 ensure #s = #r" and in the Formal Contract "by S6, #s = #r". S6 supplies the level-uniformity property that all boundary tumblers share one length, which the proof needs to satisfy the `#s = #r` precondition for WF. The claim's correctness rests on it. → **depends**

- **WF** (*WellFormedSpanFromEndpoints*) — invoked in the proof as "by WF the pair γ = (s, r ⊖ s) is a well-formed level-uniform span with reach(γ) = r". WF supplies the well-formedness construction the proof explicitly invokes to conclude γ is valid. → **depends**

- **S1** (*IntersectionClosure*) — appears only as "reusing S1's spans" in the concrete example. S3's correctness does not rest on S1; the spans α and β could be any valid example. S1 is a reader navigation pointer. → **forward**

```
CLASSIFICATIONS:
- label: S6
  direction: depends
  bullet: "- S6 (LevelConstraint) — supplies the level-uniformity property (#s = #r for all boundary tumblers) invoked in the proof to satisfy the equal-length precondition passed to WF"

- label: WF
  direction: depends
  bullet: "- WF (WellFormedSpanFromEndpoints) — supplies the well-formedness construction invoked in the proof step concluding that γ = (s, r ⊖ s) is a valid level-uniform span with reach(γ) = r"

- label: S1
  direction: forward
  bullet: "- S1 (IntersectionClosure) — its concrete spans α and β are reused in the example; S1 is downstream context, not a dependency of this claim's proof"

RETRACTIONS: []
```
