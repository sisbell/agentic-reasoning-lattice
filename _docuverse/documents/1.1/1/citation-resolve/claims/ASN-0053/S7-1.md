# Citation Resolve — ASN-0053/S7 — run 1

*2026-06-20T23:39:23Z*
*Model: sonnet*

## Output

I've confirmed all five claim-label references in S7's prose. All exist in ASN-0034: T0, T1, T12, TA-strict, and TumblerAdd. Let me verify the T0 comprehension sub-clause that S7 calls "T0(b)".

The T0 axiom block has: nonemptiness, component projection, comprehension (population by every nonempty finite sequence), and extensionality. The "infinitely many proper deeper extensions" property follows from the comprehension clause — for any `a` of length `m`, choosing `p = m+1` with any component map constructs a proper extension. T0(b) is a sub-clause reference to T0.

All five are `depends`: the proof's correctness rests on each in a load-bearing way. None are forward references (no referenced claim is downstream scaffolding for the reader; all are invoked in proof steps or listed as axioms in the Formal Contract).

```
CLASSIFICATIONS:
- label: T12
  direction: depends
  bullet: "- T12 (SpanWellDefinedness, ASN-0034) — supplies the span well-formedness predicate (Pos(ℓ) and actionPoint(ℓ) ≤ #t) that certifies the constructed span (t, ℓ) is valid, invoked in the covering construction and again in the exact-representation argument"

- label: TA-strict
  direction: depends
  bullet: "- TA-strict (StrictIncrease, ASN-0034) — supplies t ⊕ ℓ > t, establishing t ∈ [t, t ⊕ ℓ) and confirming each covering span contains its target position"

- label: TumblerAdd
  direction: depends
  bullet: "- TumblerAdd (TumblerAdd, ASN-0034) — supplies the component-level definition reach(s, ℓ)ₖ = sₖ + ℓₖ used to show rₖ > sₖ when establishing that every proper deeper extension lies strictly below reach(s, ℓ)"

- label: T1
  direction: depends
  bullet: "- T1 (LexicographicOrder, ASN-0034) — supplies case (ii) (prefix convention) to place every deeper extension e strictly above s, and case (i) (component divergence) to place e strictly below reach(s, ℓ), together confirming e ∈ ⟦(s, ℓ)⟧"

- label: T0
  direction: depends
  bullet: "- T0 (CarrierSetDefinition, ASN-0034) — sub-clause (b), the comprehension axiom, supplies infinitely many proper deeper extensions for any tumbler, grounding the infinite-span argument that forces the finite-vs-infinite mismatch"

RETRACTIONS: []
```
