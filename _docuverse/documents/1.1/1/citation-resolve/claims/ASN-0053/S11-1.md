# Citation Resolve — ASN-0053/S11 — run 1

*2026-06-20T23:20:44Z*
*Model: sonnet*

## Output

All four labels are confirmed. S0, S2, WF are in ASN-0053; T1 is cross-ASN from ASN-0034 (confirmed via S0's existing depends entry). All four are consumed by the proof's reasoning — no forward references appear in the prose.

```
CLASSIFICATIONS:
- label: T1
  direction: depends
  bullet: "- T1 (LexicographicOrder, ASN-0034) — supplies the strict total order whose totality partitions ⟦α⟧ into the three sub-ranges (L)/(M)/(R) and underwrites the containment boundary derivation"

- label: S2
  direction: depends
  bullet: "- S2 (EmptyDistinction) — supplies non-emptiness of β (start(β) < reach(β)), used in the tightness argument to obtain a witness t ∈ ⟦β⟧ lying between the two members of ⟦γ⟧"

- label: WF
  direction: depends
  bullet: "- WF (WellFormedSpanFromEndpoints) — supplies the construction that turns endpoint pairs into well-formed level-uniform spans; applied twice, once each for λ and ρ, to guarantee the difference spans are valid"

- label: S0
  direction: depends
  bullet: "- S0 (Convexity) — supplies the convexity property invoked in the tightness contradiction: a point t ∈ ⟦β⟧ lying between two members of ⟦γ⟧ must belong to ⟦γ⟧, yielding the contradiction t ∉ ⟦λ⟧ ∪ ⟦ρ⟧"

RETRACTIONS: []
```
