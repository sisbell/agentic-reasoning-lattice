# Citation Resolve — ASN-0053/S11c — run 1

*2026-06-20T23:22:13Z*
*Model: sonnet*

## Output

Both `SC` and `WF` are real claim labels in ASN-0053. The prose contains no other claim-label tokens — `level_compat` has no corresponding file and is just a predicate name.

**SC** (`SpanClassification`): S11c's precondition is defined as "SC case (iii)" and the proof splits into sub-cases dictated by SC's proper-overlap definition. The claim cannot stand without SC supplying the case structure. → **depends**

**WF** (`WellFormedSpanFromEndpoints`): Both cases of the proof invoke "WF gives a well-formed level-uniform span" to license the constructed witness spans γ and γ'. The proof's correctness rests on WF's guarantee. → **depends**

```
CLASSIFICATIONS:
- label: SC
  direction: depends
  bullet: "- SC (SpanClassification) — supplies the case taxonomy (case iii = proper overlap) that defines S11c's precondition and whose sub-case split structures the entire proof"

- label: WF
  direction: depends
  bullet: "- WF (WellFormedSpanFromEndpoints) — supplies the well-formedness guarantee invoked in both Case 1 and Case 2 to licence the constructed witness spans γ and γ' as well-formed level-uniform spans"

RETRACTIONS: []
```
