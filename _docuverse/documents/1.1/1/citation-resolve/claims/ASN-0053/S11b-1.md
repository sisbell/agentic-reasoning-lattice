# Citation Resolve — ASN-0053/S11b — run 1

*2026-06-20T23:32:32Z*
*Model: sonnet*

## Output

The only claim-label token in S11b's prose is `SC` (the same `SpanClassification` that S11a and S11c both depend on). It appears as "SC case (v) (equal)", and S11b's precondition — `start(α) = start(β)` and `reach(α) = reach(β)` — is defined by SC case (v). The denotation axiom S11b invokes (`⟦·⟧` determined by start and reach) is also supplied by SC. No forward-reference tokens appear.

```
CLASSIFICATIONS:
- label: SC
  direction: depends
  bullet: "- SC (SpanClassification) — supplies the case (v) (equal) definition whose endpoint-equality conditions form S11b's precondition and whose denotation convention (⟦γ⟧ = { p : start(γ) ≤ p < reach(γ) }) grounds the axiom that equal endpoints yield equal denotations"

RETRACTIONS: []
```
