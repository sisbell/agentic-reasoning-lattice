# Citation Resolve — ASN-0053/S10 — run 2

*2026-06-21T00:42:59Z*
*Model: sonnet*

## Output

Reading the claim prose, the only label not yet classified is **Q8**, which appears twice: once in the closing sentence attributing set-theoretic semantics to Nelson, and once inside the Formal Contract axiom as the warrant for `⟦Σᵢ ∪ Σⱼ⟧ = ⟦Σᵢ⟧ ∪ ⟦Σⱼ⟧`. That axiom is load-bearing — without it the denotation-equality chain in both sub-proofs collapses. So Q8 is `depends`.

S8 and S9 remain in the prose with correct direction; no retractions.

```
CLASSIFICATIONS:
- label: Q8
  direction: depends
  bullet: "- Q8 (Nelson, set-theoretic semantics) — supplies the axiom that a span-set designates a byte collection, grounding ⟦Σᵢ ∪ Σⱼ⟧ = ⟦Σᵢ⟧ ∪ ⟦Σⱼ⟧; the denotation-equality chain in both sub-proofs depends on this semantic commitment"

RETRACTIONS: []
```
