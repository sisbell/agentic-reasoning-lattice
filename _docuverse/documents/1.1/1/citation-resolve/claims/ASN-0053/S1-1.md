# Citation Resolve — ASN-0053/S1 — run 1

*2026-06-21T00:40:13Z*
*Model: sonnet*

## Output

Three label tokens appear in the prose — `S6`, `WF`, and `Q10`. I've confirmed S6 and WF exist as same-ASN claim files; Q10 has no file but is cited in the prose. Now classifying:

- **S6** (*LevelConstraint*): the proof body invokes it to derive that all four boundary tumblers share a common length, giving `#s' = #r'` — the precondition needed to call WF. Used again in the Formal Contract Axiom. The claim's correctness rests on it. → **depends**

- **WF** (*WellFormedSpanFromEndpoints*): the proof explicitly calls it to construct γ = (s', r' ⊖ s') and establish well-formedness and level-uniformity. Used again in the Formal Contract Axiom. → **depends**

- **Q10**: cited as "Gregory confirms this from the implementation…(Q10)" — a corroborating reader note. The mathematical proof is complete before it appears; the claim stands without it. → **forward**

```
CLASSIFICATIONS:
- label: S6
  direction: depends
  bullet: "- S6 (LevelConstraint) — supplies the level-uniformity consequence that all four boundary tumblers share a common length, giving #s' = #r', which is the precondition fed to WF in the proof's final step"

- label: WF
  direction: depends
  bullet: "- WF (WellFormedSpanFromEndpoints) — supplies the well-formedness construction invoked to produce γ = (s', r' ⊖ s') from s' < r' and #s' = #r', establishing that γ is a well-formed level-uniform span with reach(γ) = r'"

- label: Q10
  direction: forward
  bullet: "- Q10 — implementation corroboration that intersecting two spans yields at most one output span; cited as a reader note after the proof is closed, not a proof dependency"

RETRACTIONS: []
```
