# Citation Resolve — ASN-0053/S10 — run 1

*2026-06-20T23:43:46Z*
*Model: sonnet*

## Output

`Q8` can't be found in any claim directory — it's a Nelson bibliographic citation, not a claim label in the system. The two real claim-label references are `S8` and `S9`, both invoked directly in the proof steps.

```
CLASSIFICATIONS:
- label: S8
  direction: depends
  bullet: "- S8 (NormalizationExistence) — supplies existence of a normalized equivalent for each union span-set; invoked in both the commutativity and associativity sub-proofs to assert that A and B are normalized equivalents with ⟦A⟧ = ⟦B⟧"

- label: S9
  direction: depends
  bullet: "- S9 (NormalizationUniqueness) — supplies uniqueness of the normalized form; the proof concludes A = B in both sub-proofs by applying S9 to two normalized span-sets with equal denotation"

RETRACTIONS: []
```
