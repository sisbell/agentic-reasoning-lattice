# Citation Resolve — ASN-0053/S4a — run 1

*2026-06-20T23:35:02Z*
*Model: sonnet*

## Output

All three labels — **S3**, **S4**, **WR** — are confirmed real claims in ASN-0053. Now I'll classify each:

- **S4**: The proof consumes S4's split output (λ, ρ) directly; the claim *is* S3∘S4, so S4 supplies the operand.  → `depends`
- **S3**: The proof invokes "so S3 applies" and traces through S3's merge construction to derive γ; the claim's correctness rests on S3's output.  → `depends`
- **WR**: The proof step "by WR (σ is level-uniform)" uses WR to equate reach(σ) ⊖ start(σ) = ℓ; also listed as an axiom in the Formal Contract.  → `depends`

```
CLASSIFICATIONS:
- label: S4
  direction: depends
  bullet: "- S4 (SplitPartition) — supplies the split operation and its outputs λ = (s, p ⊖ s), ρ = (p, reach(σ) ⊖ p) whose properties the proof traces through to recover σ"

- label: S3
  direction: depends
  bullet: "- S3 (MergeEquivalence) — supplies the merge operation invoked in the proof step concluding that γ = (s_m, r_m ⊖ s_m) with s_m = s and r_m = reach(σ)"

- label: WR
  direction: depends
  bullet: "- WR (WidthRecovery) — supplies the identity reach(σ) ⊖ start(σ) = ℓ used in the proof step equating the merged width to the original width"

RETRACTIONS: []
```
