# Citation Resolve — ASN-0053/S3b — run 1

*2026-06-20T23:08:32Z*
*Model: sonnet*

## Output

All four referenced labels are clear. Let me classify each:

- **S3** — supplies the merge operation and the formula for γ; the proof constructs γ directly from S3's definition.
- **S4** — supplies the split operation; the proof invokes split(γ, p) and uses S4's output structure (λ, ρ) to conclude the postcondition.
- **S3a** — the Case B step explicitly invokes "By S3a (merge commutativity)" to reduce to Case A; correctness of Case B rests on it.
- **WR** — used twice in the Case A calculation to recover width from `reach ⊖ start`; both identities α = λ and β = ρ depend on it.

```
CLASSIFICATIONS:
- label: S3
  direction: depends
  bullet: "- S3 (MergeEquivalence) — supplies the merge operation and the endpoint formula γ = (start(α), r ⊖ start(α)) used throughout the proof"

- label: S4
  direction: depends
  bullet: "- S4 (SplitPartition) — supplies the split operation; the proof invokes split(γ, p) and reads off λ and ρ from S4's output structure"

- label: S3a
  direction: depends
  bullet: "- S3a (MergeCommutativity) — supplies merge commutativity invoked in Case B to reduce to the Case A configuration with roles of α and β exchanged"

- label: WR
  direction: depends
  bullet: "- WR (WidthRecovery) — supplies reach(σ) ⊖ start(σ) = width(σ), used twice in Case A to identify λ = α and ρ = β"

RETRACTIONS: []
```
