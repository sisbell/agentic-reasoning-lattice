# Citation Resolve — ASN-0053/S4 — run 1

*2026-06-21T00:36:42Z*
*Model: sonnet*

## Output

Now I have enough to classify. The commit message `fix(citation-resolve): drop source-quote citations (Q1,Q2,…) before validation` confirms that Q-prefixed labels like Q2 are source-quote citations, not classifiable claim references. The actual claim labels in S4's prose are **T12**, **WF**, and **D1**.

- **T12** (SpanWellDefinedness, ASN-0034) — invoked explicitly ("we verify T12 for both constructed spans"; "well-formed level-uniform spans (by T12)" in the Formal Contract) to supply the well-formedness predicate the proof verifies. → `depends`
- **WF** (WellFormedSpanFromEndpoints, same ASN) — called twice in the proof ("WF gives a well-formed level-uniform span with reach(λ) = p"; "WF gives a well-formed level-uniform span with reach(ρ) = reach(σ)") to construct λ and ρ as valid spans. → `depends`
- **D1** (DisplacementRoundTrip, ASN-0034) — supplies the round-trip identity "s ⊕ (p ⊖ s) = p" used in the proof of part (c). → `depends`

```
CLASSIFICATIONS:
- label: T12
  direction: depends
  bullet: "- T12 (SpanWellDefinedness, ASN-0034) — supplies the well-formedness predicate (Pos(ℓ) and actionPoint ≤ #s) that the proof verifies for both λ and ρ, and the Formal Contract's postcondition targets"

- label: WF
  direction: depends
  bullet: "- WF (WellFormedSpanFromEndpoints) — supplies the construction invoked twice in the proof to conclude that λ = (s, d) and ρ = (p, d') are well-formed level-uniform spans with the stated reaches"

- label: D1
  direction: depends
  bullet: "- D1 (DisplacementRoundTrip, ASN-0034) — supplies the round-trip identity s ⊕ (p ⊖ s) = p used in the proof of part (c) to establish reach(λ) = p = start(ρ)"

RETRACTIONS: []
```
