# Citation Resolve — ASN-0036/S4 — run 1

*2026-06-25T20:28:42Z*
*Model: sonnet*

## Output

All three labels exist and are verified. Now I can classify them.

The claim references three labels from ASN-0034:

1. **GlobalUniqueness** — The proof directly invokes it to derive `a₁ ≠ a₂`. Core `depends`.
2. **T10a** — The proof's precondition requires T10a conformance; GlobalUniqueness's invariant only fires within a T10a-conforming system. `depends`.
3. **T3** — Cited in both the informal body ("decidable from the addresses alone (T3, ASN-0034)") and the Frame condition ("identity test reads addresses alone (T3, ASN-0034)"). T3 supplies canonical representation — tumbler equality ≡ position-wise equality — which grounds the Frame condition. `depends`.

---

```
CLASSIFICATIONS:
- label: GlobalUniqueness
  direction: depends
  bullet: "- GlobalUniqueness (GlobalUniqueness) — supplies the invariant that distinct allocation events produce distinct addresses, which the proof instantiates directly to obtain `a₁ ≠ a₂`"

- label: T10a
  direction: depends
  bullet: "- T10a (AllocatorDiscipline) — supplies the allocator-discipline precondition under which GlobalUniqueness's invariant holds; required as a precondition in both the proof and the Formal Contract"

- label: T3
  direction: depends
  bullet: "- T3 (CanonicalRepresentation) — supplies the canonical-representation property (tumbler equality ≡ position-wise equality) that grounds the Frame condition: the identity test reads addresses alone without comparing stored values"

RETRACTIONS: []
```
