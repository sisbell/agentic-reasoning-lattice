# Citation Resolve — ASN-0036/S1 — run 1

*2026-06-25T02:22:24Z*
*Model: sonnet*

## Output

I now have everything needed. The prose has three label references:

- **S0** — used in the proof step ("By S0 (content immutability), `a ∈ dom(Σ.C)` implies...") and in the formal preconditions. S1's correctness rests on it. → `depends`
- **T9** (ForwardAllocation, ASN-0034) — appears only in explanatory prose ("fresh address guaranteed unique by T9 and T10"). Not invoked in the proof. → `forward`
- **T10** (PartitionIndependence, ASN-0034) — same sentence as T9. → `forward`

```
CLASSIFICATIONS:
- label: S0
  direction: depends
  bullet: "- S0 (ContentImmutability) — supplies the domain-persistence conjunct `a ∈ dom(Σ'.C)` that S1's proof extracts directly to conclude every pre-state address survives to the post-state"

- label: T9
  direction: forward
  bullet: "- T9 (ForwardAllocation, ASN-0034) — supplies the per-allocator monotonic-ordering guarantee; cited as context explaining how new addresses enter dom(C) at strictly increasing positions, not used in S1's proof"

- label: T10
  direction: forward
  bullet: "- T10 (PartitionIndependence, ASN-0034) — supplies coordination-free uniqueness across concurrent allocators; cited as context for the fresh-address uniqueness claim, not used in S1's proof"

RETRACTIONS: []
```
