# Citation Resolve — ASN-0036/S1 — run 1

*2026-06-25T20:05:20Z*
*Model: sonnet*

## Output

S1's proof rests on a single step from S0 (the implication `a ∈ dom(Σ.C) ⟹ a ∈ dom(Σ'.C)`). T9 and T10 appear only in the motivating paragraph as reader scaffolding — the proof does not invoke them.

```
CLASSIFICATIONS:
- label: S0
  direction: depends
  bullet: "- S0 (ContentImmutability) — supplies the implication `a ∈ dom(Σ.C) ⟹ a ∈ dom(Σ'.C)` used as the single proof step in the body of S1"

- label: T9
  direction: forward
  bullet: "- T9 (ForwardAllocation) — cited as guaranteeing that fresh addresses allocated in new transitions are strictly increasing within each allocator's stream; not used in S1's proof"

- label: T10
  direction: forward
  bullet: "- T10 (PartitionIndependence) — cited alongside T9 as guaranteeing cross-allocator address uniqueness for fresh entries; not used in S1's proof"

RETRACTIONS: []
```
