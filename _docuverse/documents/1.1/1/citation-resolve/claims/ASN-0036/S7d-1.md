# Citation Resolve — ASN-0036/S7d — run 1

*2026-06-25T20:11:42Z*
*Model: sonnet*

## Output

T10a lives in ASN-0034 (confirmed). The prose has two tokens: `T10a` (a claim label) and `ASN-0034` (an ASN identifier, not a claim label — no classification needed).

For `T10a`: S7d asserts that every document tumbler arises from an allocation event *under T10a's allocator discipline* — the claim's correctness rests on T10a supplying and constraining what the allocator does. This is `depends`.

```
CLASSIFICATIONS:
- label: T10a
  direction: depends
  bullet: "- T10a (AllocatorDiscipline) — supplies the allocator discipline (including the child-spawning rule and T4-preservation invariant) that governs the allocation events from which every document-level tumbler arises"

RETRACTIONS: []
```
