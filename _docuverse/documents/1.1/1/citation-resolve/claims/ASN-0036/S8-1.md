# Citation Resolve — ASN-0036/S8 — run 1

*2026-06-25T20:22:04Z*
*Model: sonnet*

## Output

All referenced labels are confirmed real. Now I have what I need to classify all ten label references from the prose.

Every reference in S8 is consumed by the proof (axiom preconditions, injectivity argument, acyclicity argument, induction step) — none are named as downstream refinements. All are `depends`.

```
CLASSIFICATIONS:
- label: S2
  direction: depends
  bullet: "- S2 (ArrangementFunctionality) — supplies the single-valuedness of M(d) used as a precondition throughout (well-defined label conjunct, Formal Contract precondition)"

- label: S3
  direction: depends
  bullet: "- S3 (ReferentialIntegrity) — supplies `ran(M(d)) ⊆ dom(Σ.C)` used to verify that every lockstep image `shift(a, k)` lies in `dom(Σ.C)` (conjunct (b) and the succ-preservation argument)"

- label: S8a
  direction: depends
  bullet: "- S8a (ArrangementDomainRestriction) — supplies the well-formedness predicate (`zeros(t)=0`, `#t≥2`) that every `v ∈ dom(M(d))` satisfies; consumed by OrdShiftHom (b) to propagate well-formedness along lockstep links"

- label: S8-depth
  direction: depends
  bullet: "- S8-depth (FixedDepthVPositions) — supplies the common depth `m` shared by all V-positions in a subspace; used to apply TS2 at a fixed depth when proving `succ` is injective"

- label: S8-fin
  direction: depends
  bullet: "- S8-fin (FiniteArrangement) — supplies finiteness of `dom(M(d))`; used to guarantee that forward walks under `succ` terminate and that the maximal-run decomposition is finite"

- label: OrdShiftHom
  direction: depends
  bullet: "- OrdShiftHom (OrdinalShiftPreservation) — supplies parts (a) and (b): subspace preservation and S8a-preservation under `shift(v,1)`; used to show every lockstep link stays within one subspace at the same depth"

- label: TS2
  direction: depends
  bullet: "- TS2 (ShiftInjectivity, ASN-0034) — supplies injectivity of `shift` at a common depth; used to conclude `succ(u)=succ(u')` implies `u=u'`, establishing that `succ` is injective"

- label: TS3
  direction: depends
  bullet: "- TS3 (ShiftComposition, ASN-0034) — supplies `shift(shift(v,i),1)=shift(v,i+1)` for shift amounts ≥1; used in the induction step to collapse nested shifts and establish the displacement identity at every `k`"

- label: TS4
  direction: depends
  bullet: "- TS4 (ShiftStrictIncrease, ASN-0034) — supplies `shift(v,1)>v`; used to show each `succ`-step strictly increases under T1, ruling out cycles"

- label: T1
  direction: depends
  bullet: "- T1 (StrictTotalOrder, ASN-0034) — supplies irreflexivity (`v < v` is false); used to close the acyclicity argument by contradiction once TS4 gives `shift(v,1)>v`"

RETRACTIONS: []
```
