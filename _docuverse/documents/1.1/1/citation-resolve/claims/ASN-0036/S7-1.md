# Citation Resolve — ASN-0036/S7 — run 1

*2026-06-25T20:20:26Z*
*Model: sonnet*

## Output

I have all the information needed. All referenced claims are dependency-type — the proof steps consume symbols or results supplied by each one.

```
CLASSIFICATIONS:
- label: S0
  direction: depends
  bullet: "- S0 (ContentImmutability) — supplies the persistence guarantee `a ∈ dom(Σ.C) ⟹ a ∈ dom(Σ'.C)` used in the Permanence step to establish that `origin(a)` is unchanged across all successor states"

- label: S4
  direction: depends
  bullet: "- S4 (OriginBasedIdentity) — cited as co-establishing that I-addresses are unique, grounding the claim in the body that origin-based attribution is permanent and unseverable"

- label: S7a
  direction: depends
  bullet: "- S7a (DocumentScopedAllocation) — supplies the allocation rule that the document-level prefix of `a` equals the allocating document's tumbler, used in the Identification step to ground `origin(a) IS the allocating document's tumbler`"

- label: S7b
  direction: depends
  bullet: "- S7b (ElementLevelIAddresses) — supplies `zeros(a) = 3` for every `a ∈ dom(Σ.C)`, the precondition that activates T4's field-decomposition machinery in Well-definedness"

- label: S7d
  direction: depends
  bullet: "- S7d (DocumentAllocationDiscipline) — supplies the postcondition that distinct documents have distinct document-level tumblers, used in the Uniqueness step to establish `origin(a₁) ≠ origin(a₂)`"

- label: T0
  direction: depends
  bullet: "- T0 (CarrierSetDefinition, ASN-0034) — supplies the carrier ℕ and the strict-positivity reading of non-separator components (`tᵢ ≠ 0 ⇔ tᵢ > 0`), used in Well-definedness to discharge positivity of each field's components"

- label: T3
  direction: depends
  bullet: "- T3 (CanonicalRepresentation, ASN-0034) — supplies decidable component-wise comparison, used in Uniqueness to confirm `origin(a₁) ≠ origin(a₂)` is decidable from the components of `a₁` and `a₂` alone"

- label: T4
  direction: depends
  bullet: "- T4 (HierarchicalParsing, ASN-0034) — supplies the zero-count field-decomposition machinery and the document-level hierarchy (`zeros = 2`) used throughout Well-definedness and the Formal Contract"

- label: T4a
  direction: depends
  bullet: "- T4a (SyntacticEquivalence, ASN-0034) — supplies the equivalence that every field segment is non-empty, used in Well-definedness to confirm N(a), U(a), and D(a) each contain at least one strictly positive component"

- label: T4b
  direction: depends
  bullet: "- T4b (UniqueParse, ASN-0034) — supplies the partial projections N(a), U(a), D(a), E(a) used throughout to extract the node, user, document, and element fields from `a`"

- label: T10a.4
  direction: depends
  bullet: "- T10a.4 (T4PreservationUnderDiscipline, ASN-0034) — supplies T4-validity of every `a ∈ dom(Σ.C)`, the precondition that licenses T4's field-decomposition machinery in Well-definedness"

RETRACTIONS: []
```
