# Citation Resolve — ASN-0036/S7 — run 1

*2026-06-25T02:13:04Z*
*Model: sonnet*

## Output

All referenced labels are confirmed real (the ASN-0034 ones appear in S7a's and S7b's own depends lists I already read). Now I can classify — every reference in S7's proof is consumed by a proof step, none are downstream pointers.

```
CLASSIFICATIONS:
- label: S0
  direction: depends
  bullet: "- S0 (ContentImmutability) — supplies domain persistence (`a ∈ dom(Σ'.C)` for all successor states `Σ'`) used in the Permanence section to conclude `origin(a)` is invariant across all states"

- label: S4
  direction: depends
  bullet: "- S4 (OriginBasedIdentity) — supplies I-address uniqueness cited as premise in the introductory 'Since I-addresses are permanent (S0) and unique (S4), this attribution is permanent and unseverable'"

- label: S7a
  direction: depends
  bullet: "- S7a (DocumentScopedAllocation) — supplies the axiom that every I-address's document-level prefix is the tumbler of its allocating document; the Identification section concludes `origin(a)` IS that tumbler by invoking S7a directly"

- label: S7b
  direction: depends
  bullet: "- S7b (ElementLevelIAddresses) — supplies the axiom `zeros(a) = 3` for every `a ∈ dom(Σ.C)`; the Well-definedness section opens with this fact to license T4's field-decomposition machinery"

- label: S7d
  direction: depends
  bullet: "- S7d (DocumentAllocationDiscipline) — supplies the postcondition that distinct documents have distinct document-level tumblers; the Uniqueness section invokes S7d's postcondition to conclude `origin(a₁) ≠ origin(a₂)` for addresses from distinct documents"

- label: T0
  direction: depends
  bullet: "- T0 (CarrierSetDefinition, ASN-0034) — supplies the carrier ℕ and strict positivity (`tᵢ ≠ 0 ⇔ tᵢ > 0`) used in Well-definedness to discharge that non-separator components are strictly positive"

- label: T3
  direction: depends
  bullet: "- T3 (CanonicalRepresentation, ASN-0034) — supplies decidability of component-wise address comparison used in the Uniqueness section to establish that `origin` discriminates allocating documents without ambiguity"

- label: T4
  direction: depends
  bullet: "- T4 (HierarchicalParsing, ASN-0034) — supplies the separator/zero-count structure (`zeros(a) = 3` means exactly three zero-valued field separators) and the field-decomposition machinery applied throughout Well-definedness"

- label: T4a
  direction: depends
  bullet: "- T4a (SyntacticEquivalence, ASN-0034) — supplies the non-empty-field reading of each present field (each has at least one component) used in Well-definedness to confirm `N(a)`, `U(a)`, `D(a)` are non-empty"

- label: T4b
  direction: depends
  bullet: "- T4b (UniqueParse, ASN-0034) — supplies the projection functions `N(a)`, `U(a)`, `D(a)`, `E(a)` from which `origin(a)` is computed; the Well-definedness section's truncation construction depends on these projections being well-defined"

- label: T10a
  direction: depends
  bullet: "- T10a (AllocatorDiscipline, ASN-0034) — listed as formal precondition; the system must conform to T10a's allocator discipline for S7a and S7d to fire"

- label: T10a.4
  direction: depends
  bullet: "- T10a.4 (T4PreservationUnderDiscipline, ASN-0034) — supplies T4-validity of every `a ∈ dom(Σ.C)`, licensing T4's field-decomposition machinery in Well-definedness"

RETRACTIONS: []
```
