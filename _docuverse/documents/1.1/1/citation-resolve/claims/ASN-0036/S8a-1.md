# Citation Resolve — ASN-0036/S8a — run 1

*2026-06-25T20:23:24Z*
*Model: sonnet*

## Output

The prose contains one label reference: `T0 (ASN-0034)`. The claim's domain restriction proof invokes T0 to equate `zeros(t) = 0` with all-positive components — the claim's correctness rests on that characterization, making this a `depends`.

```
CLASSIFICATIONS:
- label: T0
  direction: depends
  bullet: "- T0 (ASN-0034) — supplies the characterization that `zeros(t) = 0` holds exactly when every component is positive, used to restate the domain restriction in terms of strict positivity"

RETRACTIONS: []
```
