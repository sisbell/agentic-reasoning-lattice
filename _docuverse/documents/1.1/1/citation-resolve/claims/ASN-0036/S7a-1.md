# Citation Resolve — ASN-0036/S7a — run 1

*2026-06-25T20:18:05Z*
*Model: sonnet*

## Output

The claim body is confirmed. T4b is the only label referenced. The claim's prose explicitly states that `N(a)`, `U(a)`, `D(a)` are "partial projections supplied by T4b (UniqueParse, ASN-0034)" — the claim's formula `N(a).0.U(a).0.D(a)` directly consumes those projections, so T4b is a `depends` target.

```
CLASSIFICATIONS:
- label: T4b
  direction: depends
  bullet: "- T4b (UniqueParse, ASN-0034) — supplies the partial projections N(a), U(a), D(a) used to form the document-level tumbler prefix N(a).0.U(a).0.D(a)"

RETRACTIONS: []
```
