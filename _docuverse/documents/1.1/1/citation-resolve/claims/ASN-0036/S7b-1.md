# Citation Resolve — ASN-0036/S7b — run 1

*2026-06-25T02:11:04Z*
*Model: sonnet*

## Output

The claim file confirms all three label references. All appear in the explicit `*Depends:*` line and are consumed by the claim's axiom/postconditions — the claim's correctness rests on each. No labels remain unclassified, and none of the currently-classified entries need retraction (the currently-classified lists are both empty).

```
CLASSIFICATIONS:
- label: T4
  direction: depends
  bullet: "- T4 (HierarchicalParsing, ASN-0034) — supplies the field correspondence used to interpret `zeros(a) = 3` as all four identifying fields being present"

- label: T4b
  direction: depends
  bullet: "- T4b (UniqueParse, ASN-0034) — supplies the projection definitions `N(a)`, `U(a)`, `D(a)`, `E(a)` asserted well-defined in the postconditions"

- label: T10a.4
  direction: depends
  bullet: "- T10a.4 (T4PreservationUnderDiscipline, ASN-0034) — supplies T4-validity (no adjacent zeros, `a₁ ≠ 0 ∧ a_{#a} ≠ 0`) and the bound `zeros(a) ≤ 3` that make the axiom tight"

RETRACTIONS: []
```
