# Citation Resolve — ASN-0036/S7b — run 1

*2026-06-25T20:30:25Z*
*Model: sonnet*

## Output

T4 can't be found at the ASN-0034 path, but S7.md's proof confirms it as `T4 (HierarchicalParsing, ASN-0034)`. The prose references are clear: `Σ.C` and `T4`. Both are consumed by S7b's reasoning:

- `Σ.C` — supplies `dom(Σ.C)`, the set the universal quantifier ranges over; without it the axiom has no subject.
- `T4` — supplies the field-correspondence interpretation that `zeros(a) = 3` means all four fields are present; S7b invokes it explicitly to ground what the axiom means.

Both are `depends`.

```
CLASSIFICATIONS:
- label: Σ.C
  direction: depends
  bullet: "- Σ.C (ContentStore) — supplies `dom(Σ.C)`, the set over which the universal quantifier `(A a ∈ dom(Σ.C) :: zeros(a) = 3)` ranges"

- label: T4
  direction: depends
  bullet: "- T4 (HierarchicalParsing, ASN-0034) — supplies the field-correspondence interpretation that `zeros(a) = 3` means all four identifying fields are present, used to ground the meaning of the axiom"

RETRACTIONS: []
```
