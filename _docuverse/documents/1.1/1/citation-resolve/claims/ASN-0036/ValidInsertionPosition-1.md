# Citation Resolve — ASN-0036/ValidInsertionPosition — run 1

*2026-06-25T02:24:46Z*
*Model: sonnet*

## Output

The claim prose (as given in the prompt) contains one label reference: `S8-depth`. The citation-resolve file from a prior run analyzed a much fuller version of the claim body. I'll work from the prose as provided.

`S8-depth` appears as "state-determined via S8-depth" — it supplies the common depth `m` of `V_1(d)` that the entire predicate definition is built over. The claim's correctness rests on this value existing and being unique. → `depends`.

```
CLASSIFICATIONS:
- label: S8-depth
  direction: depends
  bullet: "- S8-depth (Fixed-depth V-positions) — supplies the common depth `m` of V_1(d) whose existence this predicate assumes when constructing valid insertion positions"

RETRACTIONS: []
```
