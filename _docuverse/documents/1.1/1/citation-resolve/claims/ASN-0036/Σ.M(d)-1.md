# Citation Resolve — ASN-0036/Σ.M(d) — run 1

*2026-06-25T02:05:02Z*
*Model: sonnet*

## Output

Two label references appear in the prose: **T0 (ASN-0034)** and **Σ.C**. Let me classify each.

**T0 (ASN-0034):** The S8a definition uses "By T0 (ASN-0034), `zeros(t) = 0` holds exactly when every component is positive" as an explicit proof step to establish the per-component equivalence. The claim's correctness of that equivalence rests on T0. → `depends`

**Σ.C:** The closing paragraph defines "strand" as the two-component object `(Σ.C, Σ.M)`. The claim's core motivation — "The separation is the premise" — explicitly rests on Σ.C being the immutable counterpart to M(d). The strand definition consumes Σ.C's identity. → `depends`

```
CLASSIFICATIONS:
- label: T0
  direction: depends
  bullet: "- T0 (ASN-0034) — supplies the equivalence `zeros(t) = 0` iff every component is positive, used in the S8a per-component reformulation of the domain restriction"

- label: Σ.C
  direction: depends
  bullet: "- Σ.C (ContentStore) — supplies the immutable content-store component; the strand is defined as the paired object `(Σ.C, Σ.M)`, and the separation `C` invariant / `M(d)` mutable is the premise this claim's invariants rest on"

RETRACTIONS: []
```
