# Citation Resolve — ASN-0036/D-MIN — run 1

*2026-06-25T20:16:09Z*
*Model: sonnet*

## Output

S8-depth supplies the "common depth m" that the claim directly invokes in its statement and proof. This is a `depends` relationship — D-MIN's correctness rests on the fixed-depth guarantee.

```
CLASSIFICATIONS:
- label: S8-depth
  direction: depends
  bullet: "- S8-depth (FixedDepthVPositions) — supplies the common depth m shared by all V-positions in a subspace, used in the statement (tuple of length m) and Step 1 (lexicographic order on integer m-tuples)"

RETRACTIONS: []
```
