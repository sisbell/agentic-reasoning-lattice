# Citation Resolve — ASN-0036/S0 — run 1

*2026-06-25T02:31:29Z*
*Model: sonnet*

## Output

The prose references two claim labels: `Σ.C` (used throughout in the axiom and postconditions) and `Σ.M(d)` (used in the Frame clause). Both are defined in ASN-0036 claims I just read.

- `Σ.C` supplies the partial-function symbol `dom(Σ.C)` and `Σ.C(a)` that the axiom and both postconditions are written in terms of — the claim's correctness rests on it. → **depends**
- `Σ.M(d)` appears in the Frame to name what is *not* constrained ("arbitrary `Σ'.M(d)`"). The Frame uses the symbol defined by that claim to bound the scope of S0; the claim's meaning rests on knowing what `Σ.M(d)` is. → **depends**

```
CLASSIFICATIONS:
- label: Σ.C
  direction: depends
  bullet: "- Σ.C (ContentStore) — supplies the partial-function symbol dom(Σ.C) and the application Σ.C(a) used in the axiom and both postconditions"

- label: Σ.M(d)
  direction: depends
  bullet: "- Σ.M(d) (Arrangement) — supplies the arrangement symbol referenced in the Frame to state that the postcondition imposes no condition on Σ'.M(d)"

RETRACTIONS: []
```
