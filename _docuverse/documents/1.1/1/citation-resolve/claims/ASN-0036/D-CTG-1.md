# Citation Resolve — ASN-0036/D-CTG — run 1

*2026-06-25T20:15:21Z*
*Model: sonnet*

## Output

`S8-fin` appears in the phrase *"Combined with S8-fin (dom(M(d)) is finite), contiguity at depth 2 says V_1(d) occupies a single unbroken block of ordinals."* The claim's own conclusion about "a single unbroken block" is derived by combining the contiguity quantifier with S8-fin's finiteness guarantee — S8-fin is needed to close the argument. That makes it a `depends`.

```
CLASSIFICATIONS:
- label: S8-fin
  direction: depends
  bullet: "- S8-fin (FiniteArrangement) — supplies the finiteness of dom(M(d)), used to strengthen contiguity at depth 2 to a single unbroken block of ordinals"

RETRACTIONS: []
```
