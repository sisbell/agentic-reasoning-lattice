# Citation Resolve — ASN-0036/S3 — run 1

*2026-06-25T20:05:55Z*
*Model: sonnet*

## Output

Both claims are clear. Now I can classify:

- **S1** — the prose says "S1 (store monotonicity) then guarantees that once `a` enters `dom(C)` it remains." S3's reasoning *invokes* S1's guarantee as a step in its own argument about why valid references cannot become dangling. → `depends`.

- **S0** — the prose says "Since S0's antecedent is `a ∈ dom(Σ.C)` alone, not conditioned on whether `a` appears in any `ran(M(d))`..." S3 reasons directly from S0's stated antecedent to justify that unreferenced content persists. → `depends`.

```
CLASSIFICATIONS:
- label: S1
  direction: depends
  bullet: "- S1 (StoreMonotonicity) — supplies the guarantee that once `a ∈ dom(C)` it cannot be removed, used to conclude a valid reference cannot become dangling through subsequent transitions"

- label: S0
  direction: depends
  bullet: "- S0 (ContentImmutability) — supplies the antecedent `a ∈ dom(Σ.C)` alone (not conditioned on membership in any `ran(M(d))`), used to establish that unreferenced content is never reclaimed"

RETRACTIONS: []
```
