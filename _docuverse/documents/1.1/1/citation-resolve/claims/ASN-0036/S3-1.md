# Citation Resolve — ASN-0036/S3 — run 1

*2026-06-25T02:23:21Z*
*Model: sonnet*

## Output

Both S0 and S1 are cited in the prose. S1 appears in the body and the formal contract's Depends note. S0 is cited to establish that unreferenced content persists because its antecedent is unconditional on referencing status.

```
CLASSIFICATIONS:
- label: S1
  direction: depends
  bullet: "- S1 (StoreMonotonicity) — supplies the domain-growth guarantee that once `a ∈ dom(C)` it cannot be removed, grounding the claim that a valid V-reference can never become dangling through a subsequent transition"

- label: S0
  direction: depends
  bullet: "- S0 (ContentImmutability) — supplies the unconditional domain-persistence rule (`a ∈ dom(Σ.C)` alone triggers preservation, with no conditioning on referencing status), grounding the claim that unreferenced content is never reclaimed"

RETRACTIONS: []
```
