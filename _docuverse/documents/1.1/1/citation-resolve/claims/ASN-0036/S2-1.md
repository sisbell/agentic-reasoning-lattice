# Citation Resolve — ASN-0036/S2 — run 1

*2026-06-25T20:17:14Z*
*Model: sonnet*

## Output

Reading the claim carefully, I need to identify any claim-label tokens (e.g., `S0`, `T1`, `R3`) embedded in the prose. The only letter-plus-number tokens present are within the formal notation (`T ⇀ T`), where `T` is a type variable in the partial-function signature — not a claim label. The claim itself explicitly states: *"This holds by design of the state signature, not by derivation from other claims."*

There are no claim-label references in this claim's prose.

```
CLASSIFICATIONS: []
RETRACTIONS: []
```
