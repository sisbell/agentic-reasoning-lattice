# Citation Resolve — ASN-0036/S2 — run 1

*2026-06-25T02:07:12Z*
*Model: sonnet*

## Output

The claim prose references `Σ.M(d)` by label — the partial-function axiom it unfolds. That claim exists in the same ASN and supplies the `Σ.M(d) : T ⇀ T` declaration, which is what S2's proof step ("unfolding the meaning of partial function") rests on.

```
CLASSIFICATIONS:
- label: Σ.M(d)
  direction: depends
  bullet: "- Σ.M(d) (Arrangement) — supplies the `Σ.M(d) : T ⇀ T` partial-function axiom that S2 unfolds to derive the single-image postcondition"

RETRACTIONS: []
```
