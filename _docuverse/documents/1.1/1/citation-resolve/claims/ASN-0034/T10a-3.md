# Citation Resolve — ASN-0034/T10a — run 3

*2026-04-30T05:29:38Z*
*Model: sonnet*

## Output

Labels in prose not yet classified: `T4`, `TA5`, `TA5a`, `Prefix`, `T0`, `NAT-addcompat`, `NAT-zero`, `NAT-discrete`, `NAT-closure`. All appear in the Formal Contract's `*Depends:*` section and are consumed by the axiom, consequences, or necessity argument — all `depends`. The four already-classified labels (`NAT-order`, `T1`, `T3`, `TA5-SigValid`) remain present and correctly directed.

```
CLASSIFICATIONS:
- label: T4
  direction: depends
  bullet: "- T4 (HierarchicalParsing) — supplies the field-segment constraint (T4-validity) and the `zeros(·)` function. T10a's axiom requires the root's base to satisfy T4 and imposes `zeros(t) ≤ 2` at child-spawning step `k' = 2`; T10a.4 establishes that every conforming allocator output satisfies T4."

- label: TA5
  direction: depends
  bullet: "- TA5 (HierarchicalIncrement) — supplies the `inc` operator, positional agreement under `k > 0` (TA5(b)), length preservation under `k = 0` (TA5(c)), strict order `inc(t, k) > t` (TA5(a)), and length increment by `k` (TA5(d)). Threaded through every consequence and the necessity argument."

- label: TA5a
  direction: depends
  bullet: "- TA5a (IncrementPreservesT4) — supplies the T4-preservation envelope: unconditional under `inc(·, 0)` and `inc(·, 1)` on T4-valid inputs, conditional under `inc(·, 2)` when `zeros(t) ≤ 2`. Drives T10a.4's induction step."

- label: Prefix
  direction: depends
  bullet: "- Prefix (PrefixRelation) — supplies the prefix relation `≼` and its positional-agreement clause. Used in T10a.2's collapse of sibling prefix-relation to equality and in T10a.5's closure step contradicting `x ≼ y` from a divergence position."

- label: T0
  direction: depends
  bullet: "- T0 — fixes the carrier ℕ as the index domain for tumbler positions and the spawn parameter `k' ∈ {1, 2}`. Underpins ℕ-typed quantification throughout."

- label: NAT-addcompat
  direction: depends
  bullet: "- NAT-addcompat (NatAdditionOrderAndSuccessor) — supplies the strict successor `n < n + 1` and left order-compatibility on `<`. Used in T10a.3's length-separation chain (`#t < #t + 1 ≤ #t + k'`), in T10a-N's necessity argument, and in T10a.8's positivity propagation."

- label: NAT-zero
  direction: depends
  bullet: "- NAT-zero (NatZeroMinimum) — supplies `0 ≤ n` for `n ∈ ℕ`. Used in T10a.3 to lift `k' > 0` to `1 ≤ k'`, and in T10a.5 / T10a.8 to ground positivity in the divergence and zero-count arguments."

- label: NAT-discrete
  direction: depends
  bullet: "- NAT-discrete (NatDiscreteOrder) — sharpens `n > 0` to `n ≥ 1` on ℕ. Used in T10a.3 and T10a-N to obtain `1 ≤ k'` from `k' > 0`, and in T10a.8 to derive `(tₙ)_{#tₙ} ≥ 1`."

- label: NAT-closure
  direction: depends
  bullet: "- NAT-closure (NatArithmeticClosureAndIdentity) — supplies addition closure (`n + 1 ∈ ℕ`) and the literal `1 ∈ ℕ`. Used in T10a.5 and T10a.8 to keep successor-positivity arguments inside ℕ."

RETRACTIONS: []
```
