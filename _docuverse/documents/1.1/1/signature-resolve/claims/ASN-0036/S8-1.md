# Signature Resolve — ASN-0036/S8 — run 1

*2026-06-25T19:52:07Z*
*Model: sonnet*

## Output

The Formal Contract defines exactly one new symbol explicitly in its Definition section: `succ`, the lockstep successor partial function. Everything else is either cited from upstream (`shift` from ASN-0034 via TS2/TS3/TS4, `M`/`C`/`Σ` from S2/S3, `subspace` from OrdShiftHom, `dom`/`ran` as function-theory primitives) or a defined concept named in prose (correspondence run, maximal chain, maximal run) without introducing a dedicated symbol.

INTRODUCES:
- bullet: "- `succ` — the lockstep successor partial function on `dom(M(d))`; `succ(v) = shift(v, 1)` exactly when `shift(v, 1) ∈ dom(M(d))` and `M(d)(shift(v, 1)) = shift(M(d)(v), 1)`, undefined otherwise; its orbits are the maximal correspondence runs"

REMOVES: []
