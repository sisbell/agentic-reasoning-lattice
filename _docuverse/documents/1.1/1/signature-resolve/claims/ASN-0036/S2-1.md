# Signature Resolve — ASN-0036/S2 — run 1

*2026-06-25T19:48:57Z*
*Model: sonnet*

## Output

INTRODUCES:
- bullet: "- `Σ.M` — the arrangement component of the strand state; declared as a partial function `T ⇀ T` mapping each document's V-positions to I-addresses"
- bullet: "- `T` — the common carrier type serving as both domain and codomain of the arrangement mapping"
- bullet: "- `⇀` — the partial-function type constructor; `A ⇀ B` denotes the type of partial functions from `A` to `B` (at most one image per domain element)"
- bullet: "- `dom` — the domain-of-definition operator; `dom(f)` is the set of arguments on which partial function `f` is defined"

REMOVES: []
