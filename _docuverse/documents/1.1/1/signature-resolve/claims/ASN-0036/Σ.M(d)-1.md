# Signature Resolve — ASN-0036/Σ.M(d) — run 1

*2026-06-25T01:36:52Z*
*Model: sonnet*

## Output

INTRODUCES:
- bullet: "- `Σ.M` — the arrangement of document `d`; a partial function `T ⇀ T` mapping V-position tumblers to I-address tumblers, representing the mutable layout component of the strand"
- bullet: "- `S8a` — the V-position well-formedness predicate; the named domain restriction requiring every active key of `Σ.M(d)` to be a zero-free tumbler of depth at least 2"

REMOVES: []
