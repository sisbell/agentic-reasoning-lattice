# Signature Resolve — ASN-0036/ValidFirstInsertionPosition — run 1

*2026-06-25T02:00:32Z*
*Model: sonnet*

## Output

INTRODUCES:
- bullet: "- `ValidFirstInsertionPosition` — ternary predicate `ValidFirstInsertionPosition(d, v, m)` governing the empty-document base case: holds when the depth-1 position set is empty, depth m ≥ 2, and v is the all-ones tuple of depth m"

REMOVES: []
