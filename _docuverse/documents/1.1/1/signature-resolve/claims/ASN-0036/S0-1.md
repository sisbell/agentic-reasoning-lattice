# Signature Resolve — ASN-0036/S0 — run 1

*2026-06-25T01:55:25Z*
*Model: sonnet*

## Output

INTRODUCES:
- bullet: "- `C` — the content-stream component of a state; `Σ.C` maps I-addresses to their stored content values, and this claim asserts both the domain and values of `C` are fixed across every state transition"
- bullet: "- `M` — the arrangement-stream component of a state; `Σ.M(d)` gives the arrangement for document `d`; cited in the frame clause as explicitly unconstrained by this claim"
- bullet: "- `dom` — domain of a partial function; used to identify which I-addresses carry assigned content in a given state"

REMOVES: []
