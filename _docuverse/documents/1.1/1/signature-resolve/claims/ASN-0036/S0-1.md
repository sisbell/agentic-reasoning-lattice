# Signature Resolve — ASN-0036/S0 — run 1

*2026-06-25T19:47:33Z*
*Model: sonnet*

## Output

INTRODUCES:
- bullet: "- `→` — binary state-transition relation; `Σ → Σ'` asserts that `Σ'` is a valid successor state of `Σ`"
- bullet: "- `C` — content-map component of a state; `Σ.C` is the function mapping stored addresses to their content values in state `Σ`"
- bullet: "- `dom` — domain operator on a map; `dom(Σ.C)` is the set of addresses at which content has been stored in state `Σ`"

REMOVES: []
