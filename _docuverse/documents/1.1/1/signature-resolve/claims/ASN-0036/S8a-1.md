# Signature Resolve — ASN-0036/S8a — run 1

*2026-06-25T01:41:38Z*
*Model: sonnet*

## Output

INTRODUCES:
- bullet: "- `Σ.M(d)` — the arrangement mapping function whose domain is axiomatically restricted to V-positions by this claim"
- bullet: "- `dom` — operator returning the domain of a function; used to state the restriction `dom(Σ.M(d)) ⊆ …`"
- bullet: "- `T` — carrier set of tuple elements over which the arrangement mapping and its domain constraint are stated"
- bullet: "- `zeros` — function on elements of `T` returning the count of zero entries; used in the filter predicate `zeros(t) = 0`"
- bullet: "- `#` — size/length function on elements of `T`; used in the cardinality bound `#t ≥ 2`"
- bullet: "- `≥` — order relation on the naturals; used in the cardinality condition `#t ≥ 2`"

REMOVES: []
