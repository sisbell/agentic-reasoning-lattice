# Signature Resolve — ASN-0036/Σ.C — run 1

*2026-06-25T01:54:02Z*
*Model: sonnet*

## Output

INTRODUCES:
- bullet: "- `Σ.C` — the content store; a partial function from tumblers to content values, constituting the primary state object posited by this claim"
- bullet: "- `Val` — the opaque set of content values; the codomain of `Σ.C`, left unspecified at this abstraction level"
- bullet: "- `⇀` — partial-function arrow; denotes that `Σ.C` is defined only on a subset of `T`, not on all tumblers"
- bullet: "- `dom` — domain operator; extracts the set of tumblers at which `Σ.C` is defined, i.e., the set of I-addresses with stored content"

REMOVES: []
