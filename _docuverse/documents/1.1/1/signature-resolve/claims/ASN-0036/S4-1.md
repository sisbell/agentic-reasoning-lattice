# Signature Resolve — ASN-0036/S4 — run 1

*2026-06-25T01:40:36Z*
*Model: sonnet*

## Output

`Σ.C` is owned by the `Σ.C` claim and `Σ.M(d)` by the `Σ.M(d)` claim — both within ASN-0036 and therefore upstream of S4. S4's formal contract uses both (`dom(Σ.C)`, `Σ.C(a₁)`, `Σ.M(d₁)(v₁)`) but introduces neither. The claim is a pure theorem derived from GlobalUniqueness (ASN-0034); it posits no new symbol.

INTRODUCES: []
REMOVES: []
