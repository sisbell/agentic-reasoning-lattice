# Signature Resolve — ASN-0036/S7b — run 1

*2026-06-25T19:58:57Z*
*Model: sonnet*

## Output

INTRODUCES:
- bullet: "- `dom` — domain-extraction function; `dom(Σ.C)` yields the set of addresses on which the content store is defined"
- bullet: "- `Σ.C` — the content store component of the system state; a mapping whose domain this claim constrains to element-level tumblers"
- bullet: "- `zeros` — function on addresses returning the count of zero-valued identifying fields; used here to characterise element-level tumblers as those satisfying `zeros(a) = 3`"

REMOVES: []
