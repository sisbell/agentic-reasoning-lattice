# Signature Resolve — ASN-0036/ValidFirstInsertionPosition,-empty-case — run 1

*2026-06-25T01:56:20Z*
*Model: sonnet*

## Output

INTRODUCES:
- bullet: "- `ValidFirstInsertionPosition` — ternary predicate on document `d`, V-position `v`, and depth `m`; holds iff `V₁(d) = ∅`, `m ∈ ℕ` with `m ≥ 2`, and `v = [1, 1, ..., 1]` of depth `m`"

REMOVES: []
