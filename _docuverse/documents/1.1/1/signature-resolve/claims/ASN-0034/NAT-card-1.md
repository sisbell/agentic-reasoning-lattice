# Signature Resolve — ASN-0034/NAT-card — run 1

*2026-04-30T20:15:11Z*
*Model: sonnet*

## Output

INTRODUCES:
- bullet: "- `|·|` — primitive cardinality operator; `|S|` is the unique `k ∈ ℕ` enumerating the elements of `S` via a strictly increasing function"

REMOVES: []
