# Signature Resolve — ASN-0053/S11 — run 1

*2026-06-20T22:26:27Z*
*Model: sonnet*

## Output

INTRODUCES:
- bullet: "- `λ` — the left difference span, defined as `(start(α), start(β) ⊖ start(α))` when `start(α) < start(β)`; a named constructed witness for the left component of the set-difference decomposition"
- bullet: "- `ρ` — the right difference span, defined as `(reach(β), reach(α) ⊖ reach(β))` when `reach(β) < reach(α)`; a named constructed witness for the right component of the set-difference decomposition"

REMOVES: []
