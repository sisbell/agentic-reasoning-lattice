# Signature Resolve — ASN-0053/S9 — run 1

*2026-06-20T22:23:50Z*
*Model: sonnet*

## Output

INTRODUCES:
- bullet: "- `⟦·⟧` — denotation function mapping a span sequence to the union of its constituent half-open intervals; defined as `⟦⟨γ₁,…,γₖ⟩⟧ = ⋃ₗ ⟦γₗ⟧` and `⟦γₗ⟧ = [start(γₗ), reach(γₗ))`"
- bullet: "- `start` — function returning the start position of a span; primitive identifying parameter used throughout N1, N2, and all case analyses"
- bullet: "- `reach` — function returning the exclusive upper-bound position of a span; defined as `reach(γ) = start(γ) ⊕ width(γ)`"
- bullet: "- `width` — function returning the width of a span; the primitive identifying parameter alongside start, from which reach is derived"
- bullet: "- `⊕` — position monoid addition; used to express reach in terms of start and width, and axiomatized by left-cancellation TA-LC"
- bullet: "- `<` — strict order on positions; governs the normalization conditions N1 (strictly increasing starts) and N2 (strict gap between reach and next start)"
- bullet: "- `≡` — span-equivalence relation on sequences; holds iff the two sequences have equal denotations under `⟦·⟧`"

REMOVES: []
