# Signature Resolve — ASN-0053/InteriorPoint — run 1

*2026-06-20T22:47:50Z*
*Model: sonnet*

## Output

INTRODUCES:
- bullet: "- `start` — function extracting the lower-bound position of a span; appears in the interior condition start(σ) < p"
- bullet: "- `reach` — function extracting the upper-bound position of a span; appears in the interior condition p < reach(σ)"
- bullet: "- `⟦·⟧` — denotation of a span as a set of positions, defined as ⟦σ⟧ = {t : start(σ) ≤ t < reach(σ)}"
- bullet: "- `<` — strict order on positions; used in both the interior-point condition and the span denotation bound"
- bullet: "- `≤` — non-strict order on positions; used in the closed lower bound of the span denotation"

REMOVES: []
