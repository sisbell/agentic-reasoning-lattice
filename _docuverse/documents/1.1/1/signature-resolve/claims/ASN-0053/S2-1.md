# Signature Resolve — ASN-0053/S2 — run 1

*2026-06-20T22:47:03Z*
*Model: sonnet*

## Output

INTRODUCES:
- bullet: "- `⟦·,·⟧` — the denotation map; `⟦s, ℓ⟧` is the half-open set of positions `{ p : s ≤ p < s ⊕ ℓ }` denoted by the well-formed span `(s, ℓ)`"
- bullet: "- `⊕` — position-advancement operator; `s ⊕ ℓ` is the end offset obtained by advancing start position `s` by length `ℓ`"
- bullet: "- `#` — stream-size function; `#s` is the upper bound that the end offset `k` must not exceed in the fit-within-stream precondition `k ≤ #s`"
- bullet: "- `≤` — non-strict ordering on positions; appears in the denotation comprehension (`s ≤ p`) and the fit-within-stream condition (`k ≤ #s`)"
- bullet: "- `<` — strict ordering on positions; appears in the denotation comprehension (`p < s ⊕ ℓ`) and in the strict-positivity axiom TA-strict (`ℓ > 0`, i.e. `0 < ℓ`)"
- bullet: "- `∅` — the empty set of positions; this claim establishes it has no preimage under the denotation map"

REMOVES: []
