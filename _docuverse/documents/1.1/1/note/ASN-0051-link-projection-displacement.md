# ASN-0051: Link Projection and Discovery Survivability

*2026-03-23*

*Editorial note (revision).* The original title "Link Projection Displacement" did not match the body, which formalises no "displacement" concept. The retitle "Link Projection and Discovery Survivability" reflects what the body actually develops — the survivability of a link's projection (where its endset content appears in a document's arrangement) and of its discovery (which links a content query reaches), under elementary state transitions. The inquiry's framing question — "what survives, what changes, and what can a link holder rely on?" — is the survivability question, and the SV-labelled claims throughout (SV2–SV11, SV13, SV14; SV0/SV1/SV12 withdrawn — see Properties Introduced) are the survivability properties. The filename `ASN-0051-link-projection-displacement.md` is retained for stability of external references and lattice paths.

We are looking for the invariants that govern what a link holder can rely on across state changes. A link has been created — its endsets are fixed (L12, LinkImmutability), its address is permanent (T8, AllocationPermanence). The endsets reference I-addresses in the content store, which is itself immutable (S0, ContentImmutability). So the link, structurally, is as permanent as anything in the system.

Yet the question of survivability is not about the link's *structure*. It is about the link's *utility*. A link is useful when its endpoints can be resolved to observable content in some document's current arrangement. Arrangements change — content is inserted, deleted, rearranged. What do these changes do to the link's observable behaviour?

The answer has two parts: a *discovery* question (can the link be found?) and a *resolution* question (can the link's endpoints be followed to visible content?). These are independent questions with independent answers. We develop each in turn.

*Notation.* Throughout the body, the symbol `+` between a tumbler and a nonnegative integer denotes the OrdinalShiftBase convention from ASN-0058: `a + k = shift(a, k)`, the k-th ordinal advance of a within its parent's sequence (TumblerAdd applied at the trailing position #a, which for T4-valid a coincides with the position of the last nonzero component; at k = 1 the result is the next sibling under a's prefix). OrdinalShiftBase is one of ASN-0058's M-conventions; it is not the M-auxiliary lemma (OrdinalIncrementAssociativity). The `⊕` symbol denotes TumblerAdd of a tumbler and a span-length: `s ⊕ ℓ` reaches the post-end of the span (s, ℓ). Component-wise natural-number addition on integers — e.g., `sₖ + ℓₖ` appearing inside TumblerAdd's definition — is written `+` as on ordinary numbers. The two `+` usages are disambiguated throughout by argument types: tumbler + integer is OrdinalShiftBase; integer + integer is natural-number addition.


## Endset Projection

To reason about survivability we need to formalize how a link's endsets relate to a document's current state. The link's endsets are sets of spans over I-addresses (L3, NEndsetStructure; L4, EndsetGenerality). A document's arrangement M(d) maps V-positions to I-addresses (ASN-0036). The question "what does this endset look like in document d right now?" has a precise answer.

**Definition — Endset Projection.** For an endset e ∈ Endset and a document d ∈ E_doc, the *projection* of e onto d is:

`π(e, d) = coverage(e) ∩ ran(M(d))`

This is the set of I-addresses that the endset references and that d currently contains in its arrangement. Two boundary cases: when d's arrangement shares no I-addresses with the endset, π(e, d) = ∅; when d's arrangement contains every I-address the endset references, π(e, d) = coverage(e).

**Definition — Endset Location.** For an endset e and document d, the *location* of e in d is:

`locate(e, d) = {v ∈ dom(M(d)) : M(d)(v) ∈ coverage(e)}`

These are the V-positions in d whose content is part of the endset.

**Definition — Resolution.** The *resolution* of endset e in document d is the function `locate(e, d)` — the set of V-positions in d whose content is part of e. Throughout this note, "resolution" names this function (and, by extension, the act of evaluating it); "projection" names π(e, d). The two answer different questions: resolution gives the positions a reader would see; projection gives the underlying content identities. Section titles and SV-claim names that refer to "resolution" (e.g., SV13(e) "*Resolution is arrangement-dependent*") read this function-name definition, not an informal gloss.

The two are related by M(d)'s function property (S2, ArrangementFunctionality): for all v ∈ dom(M(d)), v ∈ locate(e, d) iff M(d)(v) ∈ π(e, d). The restriction to dom(M(d)) is essential — M(d)(v) is undefined when v ∉ dom(M(d)), so the biconditional is well-formed only on this domain; and locate(e, d) ⊆ dom(M(d)) by definition, so no V-position outside dom(M(d)) is lost to the relation. Since M(d) need not be injective — within-document sharing is permitted (S5, UnrestrictedSharing) — we may have |locate(e, d)| ≥ |π(e, d)|. Multiple V-positions in d can show the same I-address, and a reader sees each occurrence.

**Definition — Text-Subspace Projection.** A document's arrangement may span more than one subspace (content under K.μ⁺, link under K.μ⁺_L; ASN-0047). For analyses that need the content-subspace contribution in isolation, define the *text-subspace projection* of e onto d:

`π_text(e, d) = coverage(e) ∩ ran_text(M(d))`

where `ran_text(M(d)) = {M(d)(v) : v ∈ dom(M(d)) ∧ subspace(v) = s_C}` is the content-subspace portion of M(d)'s range. Here s_C is the content-subspace identifier supplied by ASN-0047's K.μ⁺ amendment, and `subspace(v)` selects the subspace component of a V-position (ASN-0036, S8a). The text-subspace projection is a sub-projection of the full projection — `π_text(e, d) ⊆ π(e, d)` since `ran_text(M(d)) ⊆ ran(M(d))` by definition — with equality when ran(M(d)) carries no link-subspace addresses (the typical case for content-only arrangements). The block decomposition `ran_text(M(d)) = ⋃_k I(β_k)` used in the Partial Survival section is established there once B is in scope; the definition itself does not depend on B.

The architectural status of resolution state deserves explicit treatment. A naive implementation might intend to cache V-positions at link creation time, leaving stale entries when the document is rearranged. The schema precludes any such cache by three converging foundation facts.

*Schema closure (NoStaleResolutionState).* (i) *Link-store signature [L3, ASN-0043; K.λ, ASN-0047].* The link value Σ.L(a) = (F, G, Θ) stores I-space content only — endsets are sets of spans (s, ℓ) over T (L3, NEndsetStructure); no V-address, no per-document arrangement, no creation-time snapshot is recorded in the link value at allocation (K.λ). (ii) *State-schema (present-state property) [Σ = (C, L, E, M, R), ASN-0047].* In the current ASN-0047 state schema, M(d) is the *current* arrangement; no component carries a historical M_k. R holds per-mapping provenance over I-addresses only (J0/J1/J1★, ASN-0047), not over V-addresses. No field of the current schema caches V-positions. The property is read against ASN-0047's schema *as it stands*; this is not a closure claim about future schema extensions. Any future ASN extending Σ with a new state component carries a *forward obligation* to re-discharge NoStaleResolutionState against the augmented schema and any new transitions — i.e., to verify that the new component does not cache V-positions and that no new transition writes a V-position into a cached field — before the property as written can be applied to the augmented state. (iii) *Operational closure [K = {K.α, K.δ, K.λ, K.μ⁺, K.μ⁺_L, K.μ⁻, K.μ~, K.ρ}, ASN-0047].* No elementary transition writes a V-address into Σ.L, into a link value, or into any historical-M slot — direct inspection of each transition's effect (ASN-0047) confirms this. The per-transition checks, one line each: K.α modifies C only; K.δ modifies E and seeds M(d_new) = ∅; K.λ writes Σ.L(a_new) = (F, G, Θ), which by L3 (NEndsetStructure, ASN-0043) carries spans over T with no V-fields; K.μ⁺, K.μ⁺_L, and K.μ⁻ modify M only (and only at its current slot); K.μ~ — a distinguished composite of K.μ⁻ + K.μ⁺ under ASN-0047 rather than an elementary transition — inherits M-only modification from its two elementary stages, since each stage modifies M only and the sequential composition preserves the M-only frame; K.ρ modifies R only. No transition writes into a link value, into a historical-M slot, or into any auxiliary V-cache field — there is consequently no operation that *could* establish a stale V-position field.

The structural conclusion: there is no field in which a stale V-position could persist, and no transition that could populate one. No state component external to (coverage(e), current M(d)) participates in resolution, because no such component exists in the schema. This is an *architectural* claim — a reading of the foundation schema rather than a transition-induced property — and is therefore not assigned an SV label.

*Functional consequence.* Definitionally, `locate(e, d) = {v ∈ dom(M(d)) : M(d)(v) ∈ coverage(e)}` references only M(d) and the supplied endset, so `Σ₁.M(d) = Σ₂.M(d) ⇒ locate_{Σ₁}(e, d) = locate_{Σ₂}(e, d)` is immediate from the definition; no L-equality precondition is required, since locate does not consult Σ.L. The resolution is always *fresh* — computed from the current state, with no creation-time arrangement participating. The architectural content above is what guarantees this definitional reading is not undermined by some schema field overlooked.

**Definition — Endset Vitality.** An endset e is *vital in document d* when π(e, d) ≠ ∅ — at least one I-address that the endset references appears in d's current arrangement. Equivalently, locate(e, d) ≠ ∅.

*Scoping note.* For the remainder of this note we work within the standard-triple framework of ASN-0043: every link value is treated as Σ.L(a) = (F, G, Θ) — from-endset F, to-endset G, type-endset Θ — which is the arity-3 floor admitted by L3 (NEndsetStructure, ASN-0043). L3 admits higher-arity links with |Σ.L(a)| ≥ 3; treatment of those additional endset slots is deferred to ASN-0043. All claims below — slotwise vitality, bilateral vitality, SV2–SV13, the wp analysis — are stated for the standard triple; the generalisation to arity N > 3 follows the same projection/discovery machinery applied slot-wise, with content/type slot status determined by L3's slot-3 convention.

We define two related vitality predicates over a link's content endset slots — a liberal *slotwise* form that admits empty endsets vacuously, and a strict *bilateral* form that asserts visibility on both sides. The split avoids overloading the word "bilateral" with cases where one side is empty by construction.

A link at address a with Σ.L(a) = (F, G, Θ) is *slotwise vital in d* when each non-empty content endset is vital in d — every non-empty endset projects to at least one I-address in d's arrangement:

`F = ∅ ∨ π(F, d) ≠ ∅`   and   `G = ∅ ∨ π(G, d) ≠ ∅`

It is *bilaterally vital in d* when both content endsets are non-empty *and* each projects non-emptily — Nelson's "anything left at each end" condition read literally, with both ends required to exist:

`F ≠ ∅ ∧ π(F, d) ≠ ∅`   and   `G ≠ ∅ ∧ π(G, d) ≠ ∅`

*Equivalent compact form.* The conjuncts `F ≠ ∅` and `G ≠ ∅` are mathematically redundant given the projection conjuncts: when `F = ∅`, `coverage(F) = ⋃_{(s, ℓ) ∈ ∅} ⟦(s, ℓ)⟧ = ∅` (vacuous union over the empty index set), so `π(F, d) = ∅ ∩ ran(M(d)) = ∅`, forcing `π(F, d) ≠ ∅ ⟹ F ≠ ∅`; symmetrically for G. The predicate is therefore equivalent to its two-conjunct compact form `π(F, d) ≠ ∅ ∧ π(G, d) ≠ ∅`. We retain the four-conjunct form above for *emphasis*: stating `F ≠ ∅` and `G ≠ ∅` explicitly makes the empty-side failure mode visible to readers tracking Nelson's literal "both ends must exist" reading without forcing them to derive endset non-emptiness from projection non-emptiness as a side check. Either form may be cited downstream; consumers preferring the minimal predicate may read the compact form, and SV13(h)'s statement in this note uses the four-conjunct form for the same emphasis reason.

Bilateral vitality is strictly stronger than slotwise vitality: every bilaterally vital link is slotwise vital, but a slotwise-vital link with one empty content endset (e.g., a link (∅, G, Θ) acting as a one-sided annotation on G) is not bilaterally vital. The two predicates coincide on the substantive case where both content endsets are non-empty — which is the case the survivability analysis below targets.

*Consumer note — bilateral vitality is exposed for downstream consumers; SV13(h) is the only internal site that names it explicitly.* The SV2–SV11 claims below state per-side vitality conditions directly (e.g., `π(F, d) ≠ ∅`, `π(G, d) ≠ ∅`, `coverage(e) ∩ ran(M'(d)) = ∅`) rather than invoking BilateralVitality as a packaged predicate, and the synthesis clause SV13(h) is the sole internal site that reads the predicate by name. We introduce the predicate here for downstream consumers — link-semantics ASNs and link-discovery policy notes that require the literal-Nelson reading "anything left at each end" demanding both ends to be non-empty *and* both to project — without forcing those consumers to re-derive the conjunction at use site. Internal proofs in SV2–SV11 therefore work with `π(e, d) ≠ ∅` slot-by-slot, and SV13(h) composes the slotwise conclusions back into the bilateral form for the downstream-consumer interface; readers tracking a strict bilateral guarantee should read SV13(h) directly or compose BilateralVitality from the slotwise SV2–SV5 conclusions per side.

*Exclusion of the type endset.* We exclude Θ from both vitality predicates for a semantic, not a structural, reason. L4 (EndsetGenerality, ASN-0043) permits any endset — including F and G — to reference any I-address, ghost or otherwise, so the mere admissibility of ghost addresses in Θ (codified separately as L9, TypeGhostPermission) does not by itself distinguish Θ from F or G. The distinction is the slot's *role*: F and G are endpoints — Nelson's "links between bytes can survive deletions, insertions and rearrangements, **if anything is left at each end**" [LM 4/43] — whose content visibility in d is what makes the link useful as a navigable connection. Θ is a type annotation — Nelson: "the system needs to apply a TYPE to a span, in order to associate it with a STAGE: the time of the user's history when that span appeared" [LM 4/44]; "all things stored in the docuverse are typed within it, by means of explicit assignments" [LM 4/45] — whose role is to classify the link's relationship rather than to be visible content at one of the link's ends. Each vitality predicate asks whether the link's endpoints are observable; Θ is not an endpoint, so its vitality is not part of either predicate. L9 explains why ghost references are *admissible* in type slots (the type hierarchy can be populated forward in time), but the exclusion of Θ from vitality rests on Θ's semantic role, not on L9's permission.

*Empty-endset cases.* Slotwise vitality is satisfied vacuously by F = ∅'s left branch when F is empty, and symmetrically for G. So a link (∅, ∅, Θ) — a pure type annotation — is *slotwise vital* in every document but is *not* bilaterally vital in any (both strict conjuncts F ≠ ∅, G ≠ ∅ fail); a link with exactly one empty content endset is slotwise vital iff its non-empty side projects, and is never bilaterally vital. This is the asymmetry the split records: slotwise vitality treats the empty endset as "no visibility claim to make on that side" and lets the predicate pass; bilateral vitality treats it as "no end to project," failing the literal-Nelson reading. The corresponding identities: for any endset e with coverage(e) = ∅, π(e, d) = locate(e, d) = ∅ in every d; for a link a with coverage(Σ.L(a).s) = ∅, a ∉ discover_s(A) for any A; and discover_s(∅) = ∅ in every state. The substantive analysis below concerns links with at least one non-empty content endset and non-empty query sets for discovery, so subsequent formulas omit empty-side conditions.

Nelson states the vitality condition as: "Links between bytes can survive deletions, insertions and rearrangements, **if anything is left at each end**" [LM 4/43]. Nelson's formulation presupposes something at each end to begin with — "if anything is left" implies there was something to leave. Bilateral vitality captures Nelson's reading literally — both ends are required to be non-empty, and each must project. Slotwise vitality captures the strictly weaker form usable in proofs that must also admit annotation-style links (one or both content endsets empty by construction) without case splits. The permanent existence of the endset spans in Σ.L is not in question; what is in question is whether those spans project to anything visible.


## The Frame of Link Permanence

Before analysing what arrangement changes do to projection, we establish what they *cannot* do to the link itself.

**Corollary (ArrangementLinkFrame, from L12, ASN-0043).** Link immutability is guaranteed directly by the foundation: for every state transition Σ → Σ', `(A a ∈ dom(Σ.L) :: a ∈ dom(Σ'.L) ∧ Σ'.L(a) = Σ.L(a))`. We do not assign this a new SV label because it introduces no new content beyond L12 — it is L12 read for its survivability content. The implication is worth making explicit nonetheless: no arrangement change — no insertion, deletion, rearrangement, or version creation — can alter a link's endsets, its address, or its existence. The link store Σ.L is entirely outside the reach of arrangement operations.

*Consequence for coverage:*

`(A Σ → Σ', a ∈ dom(Σ.L), s ∈ {from, to, type} :: coverage(Σ'.L(a).s) = coverage(Σ.L(a).s))`

Endset coverage is frozen at creation. This is the pillar on which survivability rests.

*Consequence for directionality:* The from/to/type slot assignment is a structural property of the link value (L6, SlotDistinction). Since L12 preserves the entire link value, the directional assignment — which endset is "from" and which is "to" — is permanent. No operation in the system can swap, reassign, or modify the endset ordering. This is not merely a convention; it is a consequence of the link value's immutability.


## Survivability Under Arrangement Changes

With coverage frozen and arrangements mutable, the survivability question reduces to: how do arrangement changes affect π(e, d) = coverage(e) ∩ ran(M(d))?

Since coverage(e) is invariant, the question is entirely about ran(M(d)) — the set of I-addresses currently referenced by document d. We examine each kind of arrangement change.


### Extension Preserves and May Enlarge

Two elementary transitions extend a document's arrangement by adding new V→I mappings while preserving all existing ones. *Content-subspace extension* K.μ⁺ (ArrangementExtension, ASN-0047) adds mappings of the form v ↦ i where v has subspace(v) = s_C and i ∈ dom(Σ.C); the new I-addresses are content addresses. *Link-subspace extension* K.μ⁺_L (LinkSubspaceExtension, ASN-0047) adds exactly one mapping v_ℓ ↦ ℓ where subspace(v_ℓ) = s_L and ℓ ∈ dom(Σ.L); the new I-address is a link address. Both transitions satisfy `ran(M'(d)) ⊇ ran(M(d))`, and the survivability argument for π is identical in both cases — it depends only on monotonic enlargement of ran(M(d)), not on which subspace the new V-positions inhabit. We therefore state one claim covering both transitions.

**SV2 (ExtensionMonotonicity).** (We write π_Σ(e, d) when the state at which projection is evaluated matters; the subscript selects the state whose arrangement M(d) is used.)

`(A Σ →_{K.μ⁺/K.μ⁺_L} Σ', e, d :: π_Σ(e, d) ⊆ π_{Σ'}(e, d) ∧ locate_Σ(e, d) ⊆ locate_{Σ'}(e, d))`

Vitality is monotonically preserved: if an endset was vital in d before extension, it remains vital afterward. Extension can only *enlarge* the projection — introducing I-addresses that were in coverage(e) but not previously in ran(M(d)). It cannot remove any. The resolution conjunct says the same on the V-side: every pre-existing locate-position survives, and additional V-positions introduced by the extension may enter the locate set when their I-addresses lie in coverage(e).

*Projection.* π_{Σ'}(e, d) = coverage(e) ∩ ran(M'(d)). For a fixed endset value e (a set of spans), coverage(e) is determined by e alone and is independent of state; when e is the slot value of a link in dom(L), L12 (LinkImmutability, ASN-0043) is what fixes that value across the transition. Combining state-independence of coverage with ran(M'(d)) ⊇ ran(M(d)) (K.μ⁺/K.μ⁺_L effect — both transitions extend dom(M(d)) while preserving existing V↦I mappings, so the post-state range contains the pre-state range), we have coverage(e) ∩ ran(M'(d)) ⊇ coverage(e) ∩ ran(M(d)) = π_Σ(e, d). ∎

*Resolution.* Let v ∈ locate_Σ(e, d). Then v ∈ dom(M(d)) and M(d)(v) ∈ coverage(e). Both K.μ⁺ and K.μ⁺_L preserve existing mappings (dom(M(d)) ⊆ dom(M'(d)) with M'(d)(v) = M(d)(v) for all v ∈ dom(M(d))). So v ∈ dom(M'(d)) and M'(d)(v) = M(d)(v) ∈ coverage(e), giving v ∈ locate_{Σ'}(e, d). New V-positions in dom(M'(d)) \ dom(M(d)) may additionally enter the locate set when their I-addresses lie in coverage(e). ∎

*Distinct architectural roles.* The two transitions enlarge different parts of ran(M(d)) and so contribute to different parts of π(e, d). For an endset e whose coverage lies entirely in dom(Σ.C) (the typical content endset, with spans over content I-addresses), only K.μ⁺ can strictly enlarge π(e, d) — the new I-address introduced by K.μ⁺_L is a link address in dom(Σ.L), disjoint from coverage(e). Conversely, for an endset whose coverage contains link addresses — permitted by L4 (EndsetGenerality) and the reflexive-addressing case of L13 (ReflexiveAddressing) — K.μ⁺_L can strictly enlarge π(e, d) while K.μ⁺ cannot reach those coverage members at all. The unified SV2 statement remains correct for both cases (the inclusion is reflexive when neither extension touches coverage(e)); the strictness of the inclusion depends on which subspace the endset references. We defer the detailed analysis of link-referencing endsets and reflexive addressing to the Link Subspace ASN; SV2 here captures the monotonicity that both transitions share without committing to a particular subspace.


### Contraction May Reduce

Arrangement contraction (K.μ⁻, ArrangementContraction) removes V→I mappings from M(d). Therefore ran(M'(d)) ⊆ ran(M(d)), and:

**SV3 (ContractionReduction).**

`(A Σ →_{K.μ⁻} Σ', e, d :: π_{Σ'}(e, d) ⊆ π_Σ(e, d) ∧ locate_{Σ'}(e, d) ⊆ locate_Σ(e, d))`

*Projection.* π_{Σ'}(e, d) = coverage(e) ∩ ran(M'(d)). For a fixed endset value e (a set of spans), coverage(e) is determined by e alone and is independent of state; when e is the slot value of a link in dom(L), L12 (LinkImmutability, ASN-0043) is what fixes that value across the transition. Combining state-independence of coverage with ran(M'(d)) ⊆ ran(M(d)) (K.μ⁻ restricts the domain while preserving values), we have coverage(e) ∩ ran(M'(d)) ⊆ coverage(e) ∩ ran(M(d)) = π_Σ(e, d). ∎

Contraction can only *shrink* the projection. If the contraction removes all V-positions whose I-addresses are in coverage(e), then π_{Σ'}(e, d) = ∅ and the endset loses vitality in d. This is the mechanism by which editing can degrade a link's utility in a specific document.

The vitality loss condition is:

`π_Σ(e, d) ≠ ∅ ∧ π_{Σ'}(e, d) = ∅`

which requires: `(A a : a ∈ coverage(e) ∩ ran(M(d)) : a ∉ ran(M'(d)))` — every I-address that the endset shared with d's arrangement must be removed by the contraction.

Nelson's survivability condition — "if anything is left at each end" — is precisely the negation of this: as long as at least one I-address from the endset remains in d's arrangement, the endset survives in d.

*Resolution.* Let v ∈ locate_{Σ'}(e, d). Then v ∈ dom(M'(d)) and M'(d)(v) ∈ coverage(e). Since K.μ⁻ restricts the domain (dom(M'(d)) ⊂ dom(M(d))) while preserving values (M'(d)(v) = M(d)(v) for all v ∈ dom(M'(d))), we have v ∈ dom(M(d)) and M(d)(v) = M'(d)(v) ∈ coverage(e), giving v ∈ locate_Σ(e, d). ∎


### Contraction Is Document-Local

**SV4 (ArrangementIsolation).**

`(A Σ →_{K.μ⁺/K.μ⁺_L/K.μ⁻/K.μ~} Σ', e, d, d' : d ≠ d' :: π_{Σ'}(e, d') = π_Σ(e, d'))`

Arrangement operations on document d do not alter any other document's arrangement (frame conditions of K.μ⁺, K.μ⁺_L, K.μ⁻, K.μ~: `(A d' : d' ≠ d : M'(d') = M(d'))`). Therefore π_{Σ'}(e, d') = coverage(e) ∩ ran(M'(d')) = coverage(e) ∩ ran(M(d')) = π_Σ(e, d').

*For resolution:* `locate_{Σ'}(e, d') = locate_Σ(e, d')`. Since M'(d') = M(d') (frame), locate_{Σ'}(e, d') = {v ∈ dom(M'(d')) : M'(d')(v) ∈ coverage(e)} = {v ∈ dom(M(d')) : M(d')(v) ∈ coverage(e)} = locate_Σ(e, d'). ∎

This is a crucial survivability guarantee: one user's editing of their document cannot affect the projection of any endset in any other user's document. If Alice links to a passage in Bob's document, and Bob deletes that passage, the link's projection in *Alice's* document is unaffected. Only the projection in *Bob's* document changes.

The link itself, being in Σ.L, is untouched by either party's edits. What changes is only the observable view through a specific document's arrangement.


### Reordering Preserves Projection, Changes Resolution

Arrangement reordering (K.μ~, ArrangementReordering) is a bijection on V-positions that preserves the multiset of I-addresses: ran(M'(d)) = ran(M(d)). Therefore:

**SV5 (ReorderingProjectionInvariance).**

`(A Σ →_{K.μ~} Σ', e, d :: π_{Σ'}(e, d) = π_Σ(e, d))`

*Proof of the π-invariance claim.* *Framing.* SV5 is read as a *composite-endpoint* equality: π is evaluated at the pre-state Σ and the post-state Σ' that bracket the full K.μ~ composite, and the equality is justified directly by K.μ~'s bijection equation (ASN-0047), `M'(d) ∘ ψ = M(d)` on `dom(M(d))` for the reordering bijection ψ — applying the substitution `v' = ψ(v)` to `ran(M'(d)) = {M'(d)(v') : v' ∈ dom(M'(d))}` together with K.μ~-FIX (`dom(M'(d)) = dom(M(d))`, ASN-0047) and the bijection equation yields `ran(M'(d)) = {M'(d)(ψ(v)) : v ∈ dom(M(d))} = {M(d)(v) : v ∈ dom(M(d))} = ran(M(d))`. The composite decomposition `K.μ~ = K.μ⁻ + K.μ⁺` (also fixed by ASN-0047) is consulted only when explaining why per-step π is *not* invariant; the Composite-level scope of SV5 subsection below carries that explanation. The proof below works at the composite endpoints throughout. The π-equality unfolds directly from the definition of π and K.μ~'s effect on its two inputs:

`π_{Σ'}(e, d) = coverage(e) ∩ ran(Σ'.M(d)) = coverage(e) ∩ ran(Σ.M(d)) = π_Σ(e, d)`

The first and third equalities are the definition of π (π(e, d) = coverage(e) ∩ ran(M(d))). Here e is the endset value being projected (a set of spans, bound at link creation); coverage(e) depends only on e and is state-independent in this proof — L12 (LinkImmutability, ASN-0043) holds the slot value e fixed across every transition for any e drawn from a link in dom(L), so the symbol e refers to the same endset value at Σ and Σ' and we do not re-tag it with a state subscript. The only state-dependent input to π is therefore ran(M(d)). The middle equality is range invariance read *at the composite endpoints*, derived directly from K.μ~'s bijection equation (ASN-0047): the bijection equation states `M'(d) ∘ ψ = M(d)` on `dom(M(d))` for the reordering bijection ψ, and K.μ~-FIX (ASN-0047) records `dom(M'(d)) = dom(M(d))` with ψ acting as a bijection on this common domain. Applying the substitution `v' = ψ(v)` to `ran(M'(d)) = {M'(d)(v') : v' ∈ dom(M'(d))}` and using the bijection equation yields `ran(M'(d)) = {M'(d)(ψ(v)) : v ∈ dom(M(d))} = {M(d)(v) : v ∈ dom(M(d))} = ran(M(d))`. (Mechanistically, each I-address removed by the K.μ⁻ internal stage of K.μ~ is restored by the K.μ⁺ internal stage at its new V-position, so the I-range is preserved at the composite's endpoints; the bijection equation is the formal statement of that recovery.) Per-step ran is not invariant — it shrinks at the internal K.μ⁻ stage and recovers at the internal K.μ⁺ stage, as the Composite-level scope subsection below records — so the equality stated here is the composite-endpoint reading, not a pointwise claim across the composite's elementary stages. The proof uses set equality only; M(d)'s non-injectivity (S5) means a single I-address may appear at multiple V-positions, but the set ran(M(d)) collapses such multiplicities and is the only input the projection definition consults. Both arguments to the set intersection therefore coincide at Σ and Σ', so the intersections are equal. ∎

Rearrangement cannot change which I-addresses are in the projection. The endset references exactly the same content before and after. What changes is *where* that content appears. Let ψ be the reordering bijection from K.μ~ (so that M'(d)(ψ(v)) = M(d)(v) for all v ∈ dom(M(d))). By K.μ~-FIX (ASN-0047), ψ acts on a *fixed* V-position arena — dom(M'(d)) = dom(M(d)) — and acts *within* each subspace partition, so for every v ∈ dom(M(d)), subspace(ψ(v)) = subspace(v); only the V↦I assignment is permuted, not the V-position domain or its subspace structure. The locate transformation is the second part of SV5's content, separately labelled so that downstream consumers (and SV13(e) within this note) can cite it without re-deriving:

**SV5b (ReorderingLocateTransformation).** Under the same K.μ~ hypothesis as SV5, with ψ the K.μ~ reordering bijection on dom(M(d)):

`(A Σ →_{K.μ~} Σ', e, d :: locate_{Σ'}(e, d) = {ψ(v) : v ∈ locate_Σ(e, d)})`

*Proof.* v' ∈ locate_{Σ'}(e, d) iff v' ∈ dom(M'(d)) and M'(d)(v') ∈ coverage(e). By K.μ~-FIX, dom(M'(d)) = dom(M(d)) and ψ is a bijection on this common domain (subspace-respecting), so every v' ∈ dom(M'(d)) equals ψ(v) for a unique v ∈ dom(M(d)), and M'(d)(ψ(v)) = M(d)(v). So M'(d)(v') ∈ coverage(e) iff M(d)(v) ∈ coverage(e) iff v ∈ locate_Σ(e, d). ∎

*Composite-level scope of SV5.* K.μ~ is a *distinguished composite* in ASN-0047, not an elementary transition: when dom_C(M(d)) ≠ ∅ — where `dom_C(M(d)) = V_{s_C}(d) = {v ∈ dom(M(d)) : subspace(v) = s_C}` is the content-subspace portion of M(d)'s domain — it expands into two consecutive elementary steps K.μ⁻ + K.μ⁺, each satisfying its own precondition at the respective intermediate state. Between Σ and Σ', therefore, there is an intermediate state Σ_int in which K.μ⁻ has executed but K.μ⁺ has not. At Σ_int we have `ran(M_int(d)) ⊊ ran(M(d))`, so π_{Σ_int}(e, d) ⊆ π_Σ(e, d) by SV3 and the inclusion may be strict (the K.μ⁻ stage of K.μ~ removes the source V-position(s) of the permutation before the K.μ⁺ stage rewrites the target with the moved I-address). The K.μ⁺ stage then satisfies SV2 against Σ_int, restoring ran(M'(d)) = ran(M(d)) and recovering π_{Σ'}(e, d) = π_Σ(e, d). SV5's stated equality is therefore the *composite-level* π-invariance at K.μ~'s endpoints (Σ and Σ' bracketing the full composite); per-step π is not claimed to be invariant — it shrinks at the K.μ⁻ midpoint and recovers at the K.μ⁺ endpoint. Composite-level discovery (SV7) is similarly recovered at endpoints, since both K.μ⁻ and K.μ⁺ are L-frame and L-frame composes; the intermediate state is L-frame-equivalent throughout.

In general, locate_{Σ'}(e, d) ≠ locate_Σ(e, d) as sets. *Witness:* let dom(M(d)) = {v₁, v₂} with M(d) = {v₁ ↦ a₁, v₂ ↦ a₂}, and let coverage(e) = {a₁} (so locate_Σ(e, d) = {v₁}). The swap ψ(v₁) = v₂, ψ(v₂) = v₁ gives M'(d) = {v₁ ↦ a₂, v₂ ↦ a₁}, so locate_{Σ'}(e, d) = {v₂} ≠ {v₁}. The locate set changes whenever ψ maps a V-position inside the locate set to one outside it, or vice versa.

This is the precise sense in which links "track content, not location." The strap-between-bytes metaphor (Nelson: "A Xanadu link is not between points, but between spans of data. Thus we may visualize it as a strap between bytes" [LM 4/42]) expresses this property: rearranging the beads on the string doesn't alter which beads the strap holds, only where they sit.


### Content Allocation and Coverage Stability

Content allocation (K.α, ContentAllocation) creates a new I-address a ∉ dom(Σ.C). Its frame holds M constant: `(A d :: M'(d) = M(d))`. So π and locate are trivially unchanged by K.α itself.

The deeper question is: could a newly allocated I-address fall within the coverage of an existing endset? If so, a subsequent K.μ⁺ mapping a V-position to this address would enlarge the endset's projection — the endset would appear to absorb new content never part of the original link.

The answer depends on the allocation regime and the address hierarchy. We establish what is provable and identify where the answer is level-dependent.

**SV6 (CrossOriginExclusion).** For a span (s, ℓ) where s is element-level (zeros(s) = 3), the span's action point lies strictly within the element field (k > p₃, where k = actionPoint(ℓ) and p₃ is the position of the third zero component in s), and an address b with zeros(b) = 3 and origin(b) ≠ origin(s):

`b ∉ ⟦(s, ℓ)⟧`

*Precondition.*
- `s, b ∈ T`
- `(s, ℓ)` is T12-well-formed (T12, SpanWellDefinedness, ASN-0034: `Pos(ℓ) ∧ actionPoint(ℓ) ≤ #s`)
- `zeros(s) = 3 ∧ zeros(b) = 3`
- `s, b` are T4-valid
- `origin(b) ≠ origin(s)`
- `k > p₃`, where `k = actionPoint(ℓ)` and `p₃` is the position of the third zero component in s

*Note on framing — "in an existing endset" and "newly allocated".* The formal claim is structural: the precondition list above contains no endset-membership or allocation requirement, and the conclusion `b ∉ ⟦(s, ℓ)⟧` holds for any T4-valid element-level b with `origin(b) ≠ origin(s)`, regardless of whether b ∈ dom(Σ.C) and regardless of whether (s, ℓ) appears in any link's endset. The earlier draft's qualifiers "in an existing endset" and "newly allocated" named the typical application context — a span inhabiting an endset of a link in dom(L), and K.α producing a fresh I-address whose origin differs from an existing span's start (as used in the CrossDocumentDecoupling witness below) — not preconditions of the claim. The T12-well-formedness precondition is stated explicitly above because the structural claim does not presuppose endset membership; in the typical application context, T12-well-formedness is supplied automatically by L3 (NEndsetStructure, ASN-0043) for endset members.

Equivalently, the last condition `k > p₃` says the leading k − 1 components of s contain all three field separators: `|{i : 1 ≤ i ≤ k−1 ∧ sᵢ = 0}| = 3`. This places the action point within the element field — beyond all three field separators.

T4-validity is the conjunction of the conditions in T4 (HierarchicalParsing, ASN-0034); it is required so that the projections N(s), U(s), D(s), N(b), U(b), D(b) are well-defined, and consequently `origin(s) = N(s).0.U(s).0.D(s)` and `origin(b) = N(b).0.U(b).0.D(b)` are well-defined (per the origin definition in ASN-0036, which presupposes T4-valid element-level arguments). The element-level depth condition `zeros(s) = zeros(b) = 3` places each address in the element field per T4b (UniqueParse, ASN-0034). L4 (EndsetGenerality, ASN-0043) permits non-element-level span starts, but the origin-based exclusion stated by SV6 applies only when the start is a T4-valid element-level tumbler.

*Proof.* Let k be the action point of ℓ, with k > p₃ as stated. By TumblerAdd, components before k are copied from s, and (s ⊕ ℓ)ₖ = sₖ + ℓₖ, so s and s ⊕ ℓ agree on positions 1 through k−1. Consider any t with s ≤ t < s ⊕ ℓ.

The proof proceeds in two named sub-claims: (i) extracts the algebraic consequences of *assuming* a first divergence at j < k (without yet deriving a contradiction); (ii) lifts (i) to the two structural conclusions (#t ≥ k and prefix agreement on positions 1..k−1) by combining it with span containment and a t = s vs t ≠ s case-split.

*Sub-claim (i) — early-divergence shape.* Suppose the T1 sub-configuration of (s, t) is T1(i) — i.e., t and s diverge at some position rather than one being a proper prefix of the other — with first-divergence witness position j satisfying j < k. Then #t ≥ j and tⱼ > sⱼ are direct consequences of T1(i)'s witness clause.

*Proof of (i).* T1(i)'s witness clause states that the divergence position satisfies j ≤ min(#s, #t), so #t ≥ j follows immediately (and #s ≥ j follows analogously, though span well-formedness `actionPoint(ℓ) ≤ #s` with j < k ≤ #s gives the same bound on s). T1(i) further fixes the lex-order direction at the witness: t > s iff tⱼ > sⱼ at the first divergence position. Under the SV6 hypothesis s ≤ t with t ≠ s (T1(i) presupposes divergence, hence t ≠ s), T1(i) yields tⱼ > sⱼ. □

*Sub-claim (ii) — no early divergence and prefix agreement.* For every t with s ≤ t < s ⊕ ℓ, both (a) #t ≥ k and (b) t agrees with s on positions 1 through k−1 hold.

*Proof of (ii).* We first show that the early-divergence hypothesis of (i) is incompatible with t < s ⊕ ℓ — i.e., t cannot first diverge from s at any j < k. Suppose for contradiction that the first divergence position is some j with j < k. By (i), #t ≥ j and tⱼ > sⱼ. By TumblerAdd, (s ⊕ ℓ)ⱼ = sⱼ for j < k. Since t agrees with s on positions 1..j−1 (first divergence at j) and s ⊕ ℓ agrees with s on positions 1..k−1 (TumblerAdd with k as action point), and j−1 < k−1, t agrees with s ⊕ ℓ on positions 1..j−1. So the first divergence of t and s ⊕ ℓ is at position j with tⱼ > sⱼ = (s ⊕ ℓ)ⱼ. Since k = actionPoint(ℓ) ≤ #ℓ (ActionPoint codomain bound) and `#(s ⊕ ℓ) = #ℓ` (TA0 result-length), j < k gives j < #(s ⊕ ℓ), so position j is within range for s ⊕ ℓ as well — T1(i) applies symmetrically. By T1(i), t > s ⊕ ℓ — contradicting t < s ⊕ ℓ. Hence t cannot first diverge from s at any j < k.

We now lift the no-early-divergence fact to (a) and (b). T1 (LexicographicOrder, ASN-0034) admits two cases for any pair of tumblers t, s with t ≠ s: (T1(i)) t and s diverge at some position j ≤ min(#s, #t), with t > s iff tⱼ > sⱼ at the first such j; or (T1(ii)) one is a proper prefix of the other, with the shorter being < the longer in lex order. Under the SV6 hypothesis s ≤ t, the T1(ii) sub-configuration where t is a proper prefix of s is excluded — it would give t < s by T1(ii) directly — so the only T1(ii) configuration compatible with s ≤ t is `s ≺ t` (s a proper prefix of t, hence #t > #s). Together with the boundary case t = s (where T1 is vacuous, equality holds component-by-component), the three exhaustive and mutually exclusive sub-cases for any t with s ≤ t are: (1) t = s; (2) s ≺ t (T1(ii) with s the shorter); (3) t and s diverge at some position (T1(i)). (The sub-case numerals (1), (2), (3) are local to SV6's proof; the SV11 lift schema below uses Greek letters (α), (β), (α_2), (β_2) for a different decomposition family, and the two label sets are disjoint by construction.) We discharge (a) and (b) sub-case by sub-case.

(a) *#t ≥ k.*
- *Sub-case (1) — t = s.* #t = #s ≥ k (span well-formedness via T12, ASN-0034: `actionPoint(ℓ) ≤ #s` gives k ≤ #s).
- *Sub-case (2) — s ≺ t.* By the definition of proper prefix, #t > #s ≥ k, hence #t ≥ k + 1 > k.
- *Sub-case (3) — divergence at some j.* Let j be the first divergence position; T1(i) gives j ≤ min(#s, #t), and the no-early-divergence fact gives j ≥ k. Hence #t ≥ j ≥ k.

(b) *t agrees with s on positions 1 through k − 1.*
- *Sub-case (1) — t = s.* Trivially, t agrees with s on every position of s, in particular on positions 1 through k − 1.
- *Sub-case (2) — s ≺ t.* By the definition of proper prefix, t agrees with s component-by-component on positions 1 through #s. Since #s ≥ k > k − 1, t agrees with s on positions 1 through k − 1.
- *Sub-case (3) — divergence at some j.* The first divergence position j satisfies j ≥ k by the no-early-divergence fact, so positions 1 through j − 1 (which includes positions 1 through k − 1, since k − 1 ≤ j − 1) all witness agreement.

In all three sub-cases, both (a) and (b) hold. □

Since k > p₃, the first k−1 positions of t include all three field-separator positions of s — call them p₁, p₂, p₃ (the positions where sᵢ = 0). Because t agrees with s on positions 1 through k−1, we have t_{p₁} = t_{p₂} = t_{p₃} = 0, so t has at least three zero components, all located within positions 1 through k−1: zeros(t) ≥ 3, with at least three zeros at positions p₁, p₂, p₃.

*Restricting to element-level t.* We work toward the contrapositive at b — `origin(b) ≠ origin(s) ⟹ b ∉ ⟦(s, ℓ)⟧` — by establishing the stronger general fact that *every* element-level t ∈ ⟦(s, ℓ)⟧ has origin(t) = origin(s); the contrapositive on b then follows immediately as the special case where b is element-level (per the SV6 precondition `zeros(b) = 3`). The stronger generality is stronger than what SV6 strictly requires, but it falls naturally out of the field-decomposition argument that follows, which is uniform in t rather than specialised to b. For element-level t — those with zeros(t) = 3 — the inequality is tight. The three zeros at p₁, p₂, p₃ already account for all zero components of t, so t has *exactly* three zeros and they sit at exactly the positions p₁, p₂, p₃. In particular, no zero component lies at any position j with k ≤ j ≤ #t — every component beyond position k − 1 is nonzero.

*T4-validity of t.* The origin function `origin(t) = N(t).0.U(t).0.D(t)` (ASN-0036, S7) presupposes that t is T4-valid — no adjacent zeros, t₁ ≠ 0, t_#t ≠ 0 (T4, ASN-0034). (Remark on scope: ASN-0036's S7 states `origin` for `a ∈ dom(Σ.C)`, but the structural definition `origin = N.0.U.0.D` rests on T4b's projections `N, U, D` (UniqueParse, ASN-0034), which are defined for every T4-valid element-level tumbler regardless of allocation status. S7's `dom(Σ.C)` scoping is a downstream specialisation, not a constraint on the structural function; we apply `origin` to T4-valid element-level t ∈ ⟦(s, ℓ)⟧ via T4b directly.) We verify each conjunct from properties established above:
- *t₁ ≠ 0.* Position 1 lies in [1, k − 1] (since k > p₃ ≥ 6, so k ≥ 7 ≥ 2). (`p₃ ≥ 6` follows from T4-validity of s: `s₁ ≠ 0` gives `p₁ ≥ 2`, no adjacent zeros gives `p₂ ≥ 4` and `p₃ ≥ 6`.) Conclusion (b) gives t₁ = s₁, and s is T4-valid, so t₁ = s₁ ≠ 0.
- *t_#t ≠ 0.* By conclusion (a), #t ≥ k, so position #t lies in [k, #t] — the element-field range. Every component in that range is nonzero (since the three zeros of element-level t are confined to p₁, p₂, p₃ ≤ k − 1), so t_#t ≠ 0.
- *No adjacent zeros.* Positions 1 through k − 1 inherit s's no-adjacent-zeros property since t agrees with s on that range (conclusion (b)) and s is T4-valid. Positions k through #t are all nonzero (every component beyond position k − 1 is in the element field, where the three zeros of element-level t cannot sit), so no pair of adjacent positions in [k, #t] is doubly zero. The only remaining boundary is the pair (k − 1, k). The precondition k > p₃ admits exactly two sub-cases:
  - *Case (i) — k = p₃ + 1.* Position k − 1 = p₃, so t_{k−1} = s_{k−1} = 0 (the third field separator, inherited from s by conclusion (b)). Position k lies in the element field; by the element-level zero-confinement above (the three zeros of t sit exactly at p₁, p₂, p₃ ≤ k − 1), t_k ≠ 0. The pair (k − 1, k) is therefore (0, nonzero), not doubly zero.
  - *Case (ii) — k > p₃ + 1.* Since p₁ < p₂ < p₃ < k − 1 in case (ii), position k − 1 is not among the three zero positions of t; by the element-level zero-confinement (the three zeros of t sit exactly at p₁, p₂, p₃), t_{k−1} ≠ 0. With t_{k−1} nonzero, the pair (k − 1, k) cannot be doubly zero regardless of t_k.

  In either case, position pair (k − 1, k) is not doubly zero, so no adjacent zeros occur anywhere in t.

So t is T4-valid, and origin(t) is well-defined (via T4b's projections, per the remark above). The field decomposition of t — the partition of its components by the three field-separator positions p₁, p₂, p₃ — matches the field decomposition of s component-by-component up to position p₃. The first three fields (node, user, document) of t are identical to those of s, so origin(t) = N(t).0.U(t).0.D(t) = N(s).0.U(s).0.D(s) = origin(s) (per the structural origin definition, with N, U, D supplied by T4b, ASN-0034; cf. ASN-0036, S7).

Since b is element-level by the SV6 precondition (`zeros(b) = 3`, supplied directly by the precondition rather than by appeal to S7b — S7b's `(A a ∈ dom(Σ.C) :: zeros(a) = 3)` characterises content-store entries specifically, but SV6 makes no `b ∈ dom(Σ.C)` assumption and the conclusion holds for any T4-valid element-level b), and every element-level t ∈ ⟦(s, ℓ)⟧ has origin(t) = origin(s), the contrapositive gives: any element-level b with origin(b) ≠ origin(s) satisfies b ∉ ⟦(s, ℓ)⟧. ∎

*Note.* T5 gives the weaker result origin(s) ≼ t for every t in the interval, but this prefix containment does not force separator positions to align — the sandwich argument above establishes the stronger claim.

*Note on scope — what k ≤ p₃ permits.* The precondition k > p₃ is not a technical artifact of the proof; it tracks a structural feature of the tumbler field layout that the proof relies on. When k ≤ p₃, the action-point lies at or before the third field separator, so reach = s ⊕ ℓ can advance a component within s's document-prefix — placing reach in a different prefix-field configuration from s, with the interval [s, reach) covering tumblers whose own document-prefix components differ from those of s. Such tumblers have different origin values, and SV6's exclusion does not apply to them. This is by design: Nelson's docuverse admits spans whose endpoints lie at server, account, or document field positions, with the interval implicit between them — "There is no choice as to what lies between; this is implicit in the choice of first and last point." [LM 4/25] — so that a span "may range in possible size from one byte to the whole docuverse on the whole network" [LM 4/24]. A k ≤ p₃ span is the formal vehicle for broader-level coverage: the same span machinery, applied with an action-point inside the document-prefix region rather than within the element field, yields the cross-document, cross-account, or cross-node reach that the design contemplates. The boundary at k = p₃ — between element-field action-points (covered by SV6) and document-prefix-or-earlier action-points (broader-level spanning) — is *structurally consequential but not nominally designated* in Nelson's writing. It emerges from the field structure (four fields separated by three major dividers, with the third divider at position p₃ between Document and Element [LM 4/26, LM 4/28]) rather than being introduced by Nelson as a named action-point boundary; the boundary is a consequence of the field layout, not a stipulation about where action-points may sit. Detailed treatment of broader-level spans is deferred to ASN-0034's allocator and address-hierarchy machinery.

This property is robust — it depends only on the structural separation of document-level prefixes, not on any allocation discipline.

*Two readings of "origin".* Throughout this ASN, the term "origin" refers to the structural origin function `origin(t) = N(t).0.U(t).0.D(t)` defined on T4-valid element-level tumblers (ASN-0036, S7), which projects from a tumbler's components alone and has no dependence on which allocator emitted the tumbler. SV6's exclusion `b ∉ ⟦(s, ℓ)⟧` depends only on this structural reading — the formal precondition is `origin(b) ≠ origin(s)` on T4-valid tumblers, with no appeal to allocator identity. T10a.4 (T4PreservationUnderDiscipline, ASN-0034) and S7d (DocumentAllocationDiscipline, ASN-0036) together couple structural origin to allocator identity: every address produced by a T10a-conforming allocator inherits a T4-valid prefix structure, and every document is allocated under a unique T10a allocator. Under these foundation invariants the two readings — *addresses with the same structural origin field* (a T4-projection fact) and *addresses produced by the same T10a allocator* (a T10a-discipline fact) — coincide on dom(Σ.C) ∪ dom(Σ.L). The "Same-origin coverage growth" subsection below traces growth mechanisms whose explanatory content uses the allocator reading (TA5 sibling allocation, T10a child-spawning); SV6's *formal* exclusion uses the structural reading alone, which is why SV6's framing note records that the exclusion holds for any T4-valid element-level b regardless of allocation status.

**Same-origin coverage growth.** Under the same document prefix, two mechanisms can place a new I-address within an existing endset span's denotation. *Scope.* We make no formal SV claim about same-origin coverage growth in this ASN. The analysis below is descriptive: it identifies the mechanisms (sequential overshoot, child-depth entry) by which TA5 and T10a allocations can enter existing endset coverage under a shared document prefix, but the precise allocator-discipline conditions that determine *which* same-origin allocations enter *which* spans are deferred to the allocator-discipline treatment in ASN-0034. The descriptive content here motivates the SV6 formal exclusion at element-level depth from cross-origin allocations and clarifies why endset coverage stability is *architectural*, not definitional.

*Sequential overshoot.* If a span's reach extends beyond the current allocation maximum — i.e., the span references addresses not yet allocated — future sibling allocations (TA5(c)) will enter the span as they advance through the ordinal sequence. This is the mechanism by which type endsets referencing ghost addresses (L9, TypeGhostPermission) acquire content: a link whose type endset spans a range in the type hierarchy will match future type addresses as they are allocated within that range.

*Child-depth entry.* The allocator discipline (T10a) permits child-spawning — inc(t, k') with k' > 0 — to create addresses at greater tumbler depth. By the prefix rule (T1 case (ii)), a child-depth address c produced by inc(t, 1) satisfies t < c < t+1, because t is a proper prefix of c (case (ii) gives t < c) and c and t+1 diverge at the position where c has a value less than (t+1)'s (case (i) gives c < t+1). If an endset span contains t and has reach ≥ t+1, the child-depth address c falls within the span. Crucially, when k' = 1, the result has zeros(c) = zeros(t) — the appended component is nonzero (set to 1 by TA5(d)), so no new field separator is introduced, and c remains a valid element-level tumbler (T4 preserved).

*Counterexample to a universal exclusion claim.* Suppose a document D allocates element-level content at ordinals a₁ < a₂ < ... < aₙ, all of the same tumbler length. A link is created with an endset span (a₁, ℓ) where the reach a₁ ⊕ ℓ = aₙ + 1 (one ordinal step beyond the last allocation). If the allocator later spawns a child via inc(aₙ, 1) = c, then c is element-level (zeros(c) = 3) and has origin(c) = origin(a₁) = D. We verify the sandwich aₙ < c < aₙ + 1 = a₁ ⊕ ℓ by explicit divergence positions. Let n = #aₙ. By TA5(d), inc(aₙ, 1) appends a single component 1 at position n + 1, so c agrees with aₙ on positions 1 through n and has c_{n+1} = 1, with #c = n + 1. Since aₙ is a proper prefix of c (c extends aₙ by one position), T1(ii) gives aₙ < c. For the upper inequality, aₙ + 1 = shift(aₙ, 1) is the next sibling under aₙ's prefix: by OrdinalShiftBase (ASN-0058), since aₙ is T4-valid and element-level its last component aₙ_n is nonzero, so the action point of + 1 sits at position n, and (aₙ + 1)_n = aₙ_n + 1 with the prefix on positions 1..n−1 copied from aₙ; #(aₙ + 1) = n. The two tumblers c and aₙ + 1 first diverge at position n: positions 1..n−1 agree (both copy aₙ's prefix), and c_n = aₙ_n < aₙ_n + 1 = (aₙ + 1)_n. T1(i) — both have a component at position n, prior positions agree, and c_n < (aₙ + 1)_n — gives c < aₙ + 1. So c ∈ ⟦(a₁, ℓ)⟧ — the newly allocated address falls within the existing endset's coverage.

**The architectural resolution.** Nelson's design distinguishes these levels explicitly. At the byte level within a document, content allocation is sequential and append-only — new bytes get the next ordinal position in the Istream: "Instead, suppose we create an append-only storage system. User makes changes, the changes difflessly into the storage system, filed, as it were, chronologically" [LM 2/14]. The "strap between bytes" is effectively closed to future allocations as an architectural consequence of this sequential discipline. Gregory's implementation in udanax-green operates byte-level text allocation in two regimes (via `findisatoinsertmolecule`, `granf2.c:165-167`): the *steady-state regime* uses sibling increment (`tumblerincrement(..., rightshift=0, 1, ...)`) to produce strictly monotonic same-length addresses that cannot enter a tight span over previously allocated content; and a *first-insertion regime* — when content is first written into a freshly created document with no prior allocations — uses two child-depth increments (rightshift=2 then rightshift=1) to descend into the element field and seed the first ordinal position. Once the first-insertion bootstrap is complete, all subsequent text allocation under that document prefix is sibling increment, and the steady-state closure property applies. The first-insertion case does not contradict the closure: it occurs before any span over that prefix could exist, since the link's endset spans presuppose addresses that the link can reference, and those addresses are themselves the product of the bootstrap.

At broader address levels — documents, accounts, servers — Nelson explicitly designs for coverage growth: "A span that contains nothing today may at a later time contain a million documents" [LM 4/25]. Links to accounts and nodes find "any of the documents under it" [LM 4/23], including documents not yet created. This is not a deficiency but a feature: ghost elements and hierarchical spanning are fundamental to the design. The intended discipline is *owner-gated append-only growth* within the prefix region — new documents under an account, new accounts under a node — governed by the baptism rule of the responsible allocator, so that broader-level span coverage grows monotonically as new entries are baptised under existing prefixes.

*Scope — broader-level spans are admitted but not formally characterised here.* This ASN makes no SV claim about broader-level spans (spans with action point at or before p₃, reaching across document, account, or node prefixes). Three reasons: (1) the survivability analysis below applies symmetrically — coverage is invariant, projection is determined by ran(M(d)) ∩ coverage(e), and the SV2/SV3/SV4/SV5/SV11 forms carry through unchanged for any endset — so no new claim is required to extend the analysis to broader spans. (2) The cross-origin exclusion SV6 explicitly does *not* hold at broader levels by design: a broader span over a document prefix admits future child-document allocations under that prefix, so the exclusion structure that closes element-level spans against cross-origin growth is replaced by an opt-in growth structure governed by the allocator. The allocator-discipline conditions for *which* prefix-level allocations land within *which* broader spans are the proper subject of ASN-0034's address-hierarchy treatment, not this ASN. (3) udanax-green does *not* implement broader-level spans: the span value carries only stream and width tumblers (no level or action-point field), and the address representation uses only mantissa[0] (identity/type) and mantissa[1] (character offset), with mantissa[2+] declared but dead in the allocator and span machinery. The udanax codebase therefore offers no implementation-side evidence for broader-level span survivability beyond the SV6 element-level treatment given here; a future implementation that activates broader-level spans would require the survivability analysis to be re-stated against the prefix-region allocator discipline of that implementation. Subject to these scope limits, the projection and discovery machinery developed here applies term-for-term to any endset (L4, EndsetGenerality), broader-level or element-level alike.

The survivability implication: **endset coverage stability is architectural, not definitional.** The coverage *set* is fixed forever (L12, ASN-0043). What varies is whether that fixed set intersects the growing set of allocated I-addresses — and this intersection can only grow (S1, StoreMonotonicity), never shrink. At the byte level, the intersection is typically closed at creation because sequential allocation ensures new addresses fall beyond existing spans; at broader levels, the intersection is open by design, enabling links that discover future content.


## Link Discovery

We have established what happens to a link's *resolution* under state changes. The other half of survivability concerns *discovery*: finding which links relate to given content.

**Definition — Link Discovery.** For any state Σ, set of I-addresses A ⊆ T, and endset slot s ∈ {from, to, type}, define:

`discover_s(A) in Σ = {a ∈ dom(Σ.L) : coverage(Σ.L(a).s) ∩ A ≠ ∅}`

This is the set of links whose endset at slot s shares at least one I-address with A. Note that partial overlap suffices — a single shared I-address is enough to discover the link. The "in Σ" qualifier is the consistent reading used by SV7/SV8/SV9/SV13(d)/SV14 below; the state dependence enters through the link store Σ.L on the RHS.

In practice, the query set A is derived from a document's arrangement: a reader examines some V-region of document d, the system converts those V-positions to I-addresses via M(d), and then searches for links whose endsets intersect those I-addresses. But the discovery function itself is defined purely in I-space, independent of any particular document.

**Definition — Discovery Through a Document.** When the query set A is taken to be the I-addresses currently provided by document d's arrangement, define the *document-derived discovery set*:

`discover_through_s(d) in Σ = discover_s(ran(Σ.M(d))) in Σ`

— the set of links whose slot-s endset shares at least one I-address with d's current arrangement. This is the document-derived specialisation of discover_s. The argument is the *current* ran(M(d)), so unlike discover_s(A) on a fixed A — which is permanent by SV8 below — discover_through_s(d) varies with M(d): K.μ⁺/K.μ⁺_L can enlarge it (SV2 applied to coverage(e) for each link e), and K.μ⁻ can shrink it (the link's contributing I-addresses may leave ran(M(d))).

*Domain of A.* The query domain is the full I-space T, not the content subspace alone. L4 (EndsetGenerality, ASN-0043) admits any I-address in any endset, and L13 (ReflexiveAddressing, ASN-0043) explicitly licenses link addresses as endset members; correspondingly K.μ⁺_L (LinkSubspaceExtension, ASN-0047) places link addresses into ran(M(d)), so an A derived from a document's arrangement may legitimately contain link addresses when the arrangement spans the link subspace. Restricting A to dom(Σ.C) would exclude the reflexive-addressing case by definition rather than by argument, and would render discover_s partial on inputs the rest of the schema admits. The wider domain leaves SV7–SV9 and the K.μ⁺ transclusion corollary stated below unchanged — each is established for arbitrary fixed A — and is consistent with every concrete A used in this note (each is a subset of dom(Σ.C) ⊆ T).

We observe that discover_s is defined purely as a function of an I-address set — it is parameterised by I-addresses, not by document-V-region pairs. So identical I-address sets trivially yield identical discovery results. The interesting consequence is not this definitional fact but the *discovery invariance under L-frame transitions* it entails — and, as a corollary, the *transclusion discovery guarantee* that follows from instantiating that invariance to K.μ⁺.

**SV7 (DiscoveryInvarianceUnderLFrame).** For every transition Σ → Σ' that holds L in frame — every L-frame elementary transition (K.α, K.δ, K.μ⁺, K.μ⁺_L, K.μ⁻, K.ρ) and the distinguished composite K.μ~ — and for every fixed set of I-addresses A:

`discover_s(A) in Σ' = discover_s(A) in Σ`

*Proof.* Each L-frame transition holds dom(L') = dom(L) and L'(a) = L(a) for all a ∈ dom(L). For the elementaries this is direct (per-transition effect, ASN-0047); for the distinguished composite K.μ~ it follows because both constituents K.μ⁻ and K.μ⁺ hold L in frame, so their sequential composition does. Therefore coverage(Σ'.L(a).s) = coverage(Σ.L(a).s) for every a ∈ dom(Σ.L), and dom(Σ'.L) = dom(Σ.L). Both inputs to discover_s — coverage and dom(L) — are identical in Σ and Σ', so the discovery sets are equal. ∎

The claim is an equality, not merely monotonicity: no L-frame transition introduces new discovery relationships and none removes any. The discovery mechanism itself — discover_s operating on coverage and dom(L) — is coupling-free under L-frame: the link store is untouched, and the I-address inputs to coverage are fixed by L12. The only elementary transition that can change discover_s is K.λ, which adds a new link to dom(L) and yields the strict inclusion recorded in SV9. (K.μ~ is a distinguished composite of K.μ⁻ + K.μ⁺, both L-frame, hence L-frame itself; the elementary/composite distinction does not affect the L-frame-induced invariance.)

*Corollary (TransclusionCouplingAbsence).* When K.μ⁺ extends M(d₂) with a mapping v ↦ a where a ∈ ran(M(d₁)), the link discoverability through a in d₂ requires no *link-store* coupling step beyond K.μ⁺ itself. (A valid composite extension may still require K.ρ to satisfy J1★ — that step modifies R only and leaves L and M untouched, so it does not participate in the discovery argument; the K.ρ obligation is recorded again in the closing parenthetical below.) The argument distinguishes two different address-set arguments to discover_s, the *fixed-A* invariance carried by SV7 and the *document-derived* set that changes with the arrangement:

- *Fixed-A invariance (SV7).* For every fixed I-address set A, discover_s(A) evaluated in Σ' equals discover_s(A) evaluated in Σ. In particular, discover_s({a}) in Σ' = discover_s({a}) in Σ — call this set L_a, the set of links whose slot-s endset covers a. L_a was already determined in Σ; K.μ⁺ does not alter it.

- *Document-derived set change.* The address set d₂ contributes to discovery is A_{Σ}(d₂) = ran(Σ.M(d₂)) in Σ and A_{Σ'}(d₂) = ran(Σ'.M(d₂)) = A_{Σ}(d₂) ∪ {a} in Σ'. These are *different* sets — the transition extends d₂'s contributed addresses by {a}. Discovery through d₂ in Σ' is discover_s(A_{Σ'}(d₂)) = discover_s(A_{Σ}(d₂) ∪ {a}), which by set-theoretic distribution of discover_s over union equals discover_s(A_{Σ}(d₂)) ∪ discover_s({a}) = discover_s(A_{Σ}(d₂)) ∪ L_a.

The inheritance is therefore: d₂ in Σ' discovers everything it already discovered in Σ, plus exactly L_a — the link set that was already discoverable through a in Σ (e.g., via d₁, which had a in its arrangement). No link-store operation participates; the new discovery membership is supplied entirely by the K.μ⁺ extension of d₂'s arrangement together with the pre-existing coverage of L_a. The same reasoning applies to forking (J4): the new version's arrangement extension by K.μ⁺ provides the shared addresses, and discovery through the new version inherits L_a for each shared a without explicit link propagation. (A valid composite transition containing K.μ⁺ may additionally require K.ρ to satisfy J1★ (ExtensionRecordsProvenanceContent, ASN-0047), but K.ρ modifies R only — it does not alter L or M, so the discovery argument above is unaffected; SV7 covers K.ρ directly as one of the L-frame transitions.)

**SV8 (DiscoveryPermanence).** For any fixed set of I-addresses A:

`(A Σ → Σ', a ∈ discover_s(A) in Σ :: a ∈ discover_s(A) in Σ')`

Once a link is discoverable through a set of I-addresses, it remains discoverable through that set in all subsequent states.

Proof: a ∈ discover_s(A) means coverage(Σ.L(a).s) ∩ A ≠ ∅. By L12, a ∈ dom(Σ'.L) and Σ'.L(a) = Σ.L(a). So coverage(Σ'.L(a).s) = coverage(Σ.L(a).s), and the intersection with A is unchanged. ∎

*Caveat — document-derived discovery is not permanent.* SV8 quantifies over a fixed I-address set A; the document-derived specialisation discover_through_s(d) = discover_s(ran(M(d))) is not preserved across arrangement edits, because its argument ran(M(d)) is not fixed. Formally: under a K.μ⁻ transition Σ → Σ' on d, `discover_through_s(d) in Σ' ⊆ discover_through_s(d) in Σ` (SV3 applied to coverage(Σ.L(a).s) for each link a), and the inclusion is strict whenever the contraction removes the last V-position contributing an I-address in some link's endset coverage. A link a that exits discover_through_s(d) in this way remains in discover_s(A) for the unchanged A (by SV8 on the fixed I-address set whose membership SV8 records), and remains in discover_through_s(d') for any other document d' whose current arrangement still maps a contributing I-address.

**SV9 (DiscoveryMonotonicity).**

`(A Σ → Σ' :: discover_s(A) in Σ ⊆ discover_s(A) in Σ')`

for any fixed A. *Proof.* Take any a ∈ discover_s(A) in Σ: a ∈ dom(Σ.L) and coverage(Σ.L(a).s) ∩ A ≠ ∅. Two L-invariants from ASN-0043 together place a in discover_s(A) in Σ':

- *Entry preservation [L12].* L12 (LinkImmutability, ASN-0043) gives Σ'.L(a) = Σ.L(a) entry-by-entry for every a ∈ dom(Σ.L) ∩ dom(Σ'.L), so coverage(Σ'.L(a).s) = coverage(Σ.L(a).s) and its intersection with A is unchanged.

- *Domain non-shrinking [L12a].* L12a (LinkStoreMonotonicity, ASN-0043) gives dom(Σ'.L) ⊇ dom(Σ.L), so a ∈ dom(Σ.L) carries over to a ∈ dom(Σ'.L) — the existing link address is retained.

Both clauses of `a ∈ discover_s(A) in Σ'` membership are therefore met, giving the inclusion. The inclusion may be strict: L12a admits dom(Σ'.L) ⊋ dom(Σ.L), and any newly allocated link a_new ∈ dom(Σ'.L) \ dom(Σ.L) whose endset at slot s shares an I-address with A also enters discover_s(A) in Σ'. The proof routes through the two ASN-0043 L-invariants alone, without appeal to per-transition L'-equalities in ASN-0047. The discoverable set is therefore monotonically non-decreasing in the link population. ∎

**SV14 (DocumentDerivedDiscoverySurvivability).** The document-derived specialisation `discover_through_s(d) = discover_s(ran(M(d)))` inherits both the projection-style survivability of SV2–SV4 per-link under M-modifying transitions and SV9's link-allocation monotonicity under K.λ — which holds M in frame, so ran(M(d)) is fixed across the step and the document-derived discovery reduces to a fixed-A application of SV9 — with the non-permanence already noted as the caveat above promoted to a labelled witness clause:

(a) *Monotonicity under extension.* `(A Σ →_{K.μ⁺/K.μ⁺_L} Σ', d, s :: discover_through_s(d) in Σ ⊆ discover_through_s(d) in Σ')`. *Proof.* K.μ⁺/K.μ⁺_L satisfy `ran(Σ.M(d)) ⊆ ran(Σ'.M(d))` (per the extension effect, ASN-0047). For every a ∈ discover_through_s(d) in Σ, coverage(Σ.L(a).s) ∩ ran(Σ.M(d)) ≠ ∅, so by L12 (coverage invariance) coverage(Σ'.L(a).s) ∩ ran(Σ'.M(d)) ⊇ coverage(Σ.L(a).s) ∩ ran(Σ.M(d)) ≠ ∅, giving a ∈ discover_through_s(d) in Σ'. ∎

(b) *Reduction under contraction.* `(A Σ →_{K.μ⁻} Σ', d, s :: discover_through_s(d) in Σ' ⊆ discover_through_s(d) in Σ)`. *Proof.* K.μ⁻ satisfies `ran(Σ'.M(d)) ⊆ ran(Σ.M(d))` (per the contraction effect, ASN-0047). For every a ∈ discover_through_s(d) in Σ', coverage(Σ'.L(a).s) ∩ ran(Σ'.M(d)) ≠ ∅, so by L12 coverage(Σ.L(a).s) ∩ ran(Σ.M(d)) ⊇ coverage(Σ'.L(a).s) ∩ ran(Σ'.M(d)) ≠ ∅, giving a ∈ discover_through_s(d) in Σ. ∎

(c) *Cross-document isolation.* `(A Σ →_{K.μ⁺/K.μ⁺_L/K.μ⁻/K.μ~} Σ', d, d', s : d ≠ d' :: discover_through_s(d') in Σ' = discover_through_s(d') in Σ)`. *Proof.* All four transitions hold M(d') in frame for d' ≠ d (per their frame conditions, ASN-0047); together with L12 (coverage invariance) the two inputs to discover_s — coverage and ran(M(d')) — are unchanged across Σ → Σ', so the discovery set evaluated against ran(M(d')) is identical in both states. ∎

(d) *Non-permanence witness — strict shrinkage under contraction.* The inclusion (b) is strict when the contraction removes the last V-position whose I-address is in some link's endset coverage: `(E Σ →_{K.μ⁻} Σ', d, s, a ∈ dom(Σ.L) :: a ∈ discover_through_s(d) in Σ ∧ a ∉ discover_through_s(d) in Σ')`. *Witness.* We extend the Worked Example. Before the arrangement-edit composite, K.λ (LinkAllocation, ASN-0047) allocates a fresh link a' with `Σ.L(a') = (F', ∅, Θ')` carrying `F' = {(a₃, ℓ_a')}` for `ℓ_a' = a₄ ⊖ a₃`. D0 (ASN-0053) applies to the pair (a₃, a₄) because (i) a₃ < a₄ — successive ordinal siblings; (ii) a₃ and a₄ have equal tumbler length and share their prefix through position #a₃ − 1, so they diverge first at the last position #a₃ = #a₄, with a₃_#a₃ < a₄_#a₄ = a₃_#a₃ + 1 (the divergence-at-position-≤-#a₃ condition required by D0). The resulting ℓ_a' is a tumbler with action point #a₃, last component a₄_#a₄ − a₃_#a₃ = 1, and zeros elsewhere. The span (a₃, ℓ_a') is T12-well-formed (ASN-0034, SpanWellDefinedness): a₃ ∈ T by construction; ℓ_a' ∈ T as a well-formed tumbler; `Pos(ℓ_a')` holds since the last component is 1 > 0; and `actionPoint(ℓ_a') = #a₃ ≤ #a₃ = #s` satisfies the action-point bound with equality. Reach `a₃ ⊕ ℓ_a' = a₄` (D1 inverse) and coverage `⟦(a₃, ℓ_a')⟧ = {t : a₃ ≤ t < a₄}`, which contains a₃. The type slot is any non-empty `Θ' = {(τ', ℓ_τ')}` satisfying L3. (L4 admits the from-slot span and the empty to-slot; the empty-endset case is treated elsewhere in this note.) Set s = from. The Worked Example's after-removing-a₃ composite proceeds in two transition stages — Stage 1, K.μ~ (a distinguished composite per ASN-0047, expanding into K.μ⁻ + K.μ⁺), and Stage 2, K.μ⁻; instantiate SV14(d)'s `Σ →_{K.μ⁻} Σ'` existential with the elementary K.μ⁻ step `Σ_reord →_{K.μ⁻} Σ'`, where Σ_reord is the post-K.μ~ state (full domain, reordered) with `M_reord(d) = {v₁↦a₁, v₂↦a₂, v₃↦a₄, v₄↦a₅, v₅↦a₃}` — distinct from any SV5-style elementary-stage intermediate state inside K.μ~, which has contracted domain — and Σ' has `M'(d) = {v₁↦a₁, v₂↦a₂, v₃↦a₄, v₄↦a₅}` (D-SEQ-admissible because v₅ is the maximum V-position in Σ_reord; the K.μ~ step preceding it is preparation that brings a₃ to v₅). Pre-state Σ_reord: ran(Σ_reord.M(d)) = ran(Σ.M(d)) = {a₁, a₂, a₃, a₄, a₅} (K.μ~ preserves the I-range per SV5; K.λ ran before K.μ~ and is M-frame), so a₃ ∈ coverage(F') ∩ ran(Σ_reord.M(d)) and a' ∈ discover_through_from(d) in Σ_reord. Post-state Σ': ran(Σ'.M(d)) = {a₁, a₂, a₄, a₅}; under the sibling ordering a₁ < a₂ < a₃ < a₄ < a₅, the elements a₁, a₂ lie strictly below a₃ and the elements a₄, a₅ lie at or beyond the reach a₄, so none lies in the half-open interval [a₃, a₄) = coverage(F') ∩ {a₁, a₂, a₄, a₅}; the intersection is empty, and a' ∉ discover_through_from(d) in Σ' (L12 carries coverage(F') through unchanged across the K.μ⁻ step). The exit is local to d: a' remains in discover_from({a₃}) (the fixed I-address set is unchanged, SV8), and remains in discover_through_from(d'') for any d'' ≠ d whose arrangement still maps a₃. ∎

(e) *Monotonicity under link allocation.* `(A Σ →_{K.λ} Σ', d, s :: discover_through_s(d) in Σ ⊆ discover_through_s(d) in Σ')`. *Proof.* K.λ holds M in frame (per its frame condition, ASN-0047), so ran(Σ'.M(d)) = ran(Σ.M(d)). Therefore discover_through_s(d) in Σ' = discover_s(ran(Σ'.M(d))) in Σ' = discover_s(ran(Σ.M(d))) in Σ', and applying SV9 (DiscoveryMonotonicity) to the fixed I-address set A = ran(Σ.M(d)) gives discover_s(A) in Σ ⊆ discover_s(A) in Σ', i.e., discover_through_s(d) in Σ ⊆ discover_through_s(d) in Σ'. The inclusion is strict precisely when the freshly allocated link a_new ∈ dom(Σ'.L) \ dom(Σ.L) satisfies coverage(Σ'.L(a_new).s) ∩ ran(Σ.M(d)) ≠ ∅; otherwise dom(L) grows without enlarging the discovery set through d. ∎

The caveat above (preceding SV9) records the same non-permanence informally; SV14(b) and SV14(d) supply the formal claim and witness that downstream consumers can cite. SV14(a)–(c) are direct corollaries of SV2–SV4 read per-link, and SV14(e) is the corresponding K.λ corollary of SV9 read on the fixed I-address set ran(M(d)); together they package every elementary transition's effect on the document-derived discovery query that downstream link-discovery policy notes will use most often.


## The Discovery-Resolution Distinction

We have now defined two independent operations — discovery and resolution — and we observe that they answer fundamentally different questions:

- **Discovery** asks: "which links relate to this content?" It operates on I-address intersection (coverage(e) ∩ A ≠ ∅), is independent of any particular document's arrangement, and is permanent (SV8).

- **Resolution** asks: "where in document d are this link's endpoints visible?" It operates on I-to-V conversion through d's current arrangement, depends entirely on M(d), and changes as M(d) changes.

**SV10 (DiscoveryProjectionIndependence).** A link may be discoverable through a set of I-addresses A yet have only partial projection in a particular document — the projection covers a proper subset of the endset's full coverage:

`(E Σ, a, d, s, V ⊆ dom(M(d)) :: a ∈ discover_s({M(d)(v) : v ∈ V}) ∧ π(Σ.L(a).s, d) ⊊ coverage(Σ.L(a).s))`

Note that discovery through d entails non-empty projection in d: if a ∈ discover_s({M(d)(v) : v ∈ V}), then coverage(Σ.L(a).s) ∩ {M(d)(v) : v ∈ V} ≠ ∅, and since {M(d)(v) : v ∈ V} ⊆ ran(M(d)), we have π(Σ.L(a).s, d) ⊇ coverage(Σ.L(a).s) ∩ {M(d)(v) : v ∈ V} ≠ ∅. So within the discovering document, resolution is guaranteed non-empty.

This arises naturally. Suppose a link's from-endset covers I-addresses {i₁, i₂, i₃}. Document d's arrangement contains only i₂. Discovery succeeds (non-empty intersection). But the projection in d returns only the singleton {i₂} (proper subset of the endset's coverage), and resolution of the from-endset in d returns only the V-positions corresponding to i₂ — the other two I-addresses have no V-positions in d.

*Concrete witness.* We exhibit a state Σ, link a, document d, slot s = from, and V ⊆ dom(Σ.M(d)) satisfying the existential. Fix the content subspace identifier s_C = 1 throughout — this is the value that the K.α amendment (ASN-0047) requires the first element-field component of every content-store allocation to carry (`fields(a).E₁ = s_C`), and is correspondingly the first component of every depth-2 V-position in the content subspace. Take origin O = 1.0.1.0.1 and three element-level sibling tumblers i₁ = O.0.1.1, i₂ = O.0.1.2, i₃ = O.0.1.3 — each T4-valid with zeros(i_k) = 3 (the three zeros sit at positions 2, 4, 6, all in the prefix), origin(i_k) = N(i_k).0.U(i_k).0.D(i_k) = 1.0.1.0.1 = O, and element field E(i_k) = [1, k] of length 2. Of these three tumblers, only i₂ will be allocated by K.α in the chain below; i₁ and i₃ remain well-defined T4-valid tumblers in T but are *not* placed into dom(Σ.C). This is essential for J0 (AllocationRequiresPlacement, ASN-0047) compliance — J0 obligates placement in some M(d) *only* for newly allocated content addresses, so allocating i₁ or i₃ without also placing them via K.μ⁺ would falsify the composite against J0. For the allocated i₂, S7c (ASN-0036) requires #E(i₂) ≥ 2 — satisfied as #E(i₂) = 2 — and the K.α amendment requires E(i₂)₁ = 1 = s_C; the unallocated i₁ and i₃ satisfy the same #E = 2 size and leading-component value by construction but bear no dom(Σ.C) obligation. Let s_span = i₁ and ℓ_span = 0.0.0.0.0.0.0.3 (action point k = 8, the first nonzero component, with k = 8 > 6 = p₃ so the action point lies strictly within the element field). *T12 well-formedness of (i₁, ℓ_span):* T12 (SpanWellDefinedness, ASN-0034) requires four conditions, each discharged here for the reader's audit. (i) `i₁ ∈ T` — holds by construction (i₁ = 1.0.1.0.1.0.1.1 is a length-8 sequence of natural numbers, satisfying T0). (ii) `ℓ_span ∈ T` — holds by construction (ℓ_span is a length-8 sequence of natural numbers). (iii) `Pos(ℓ_span)` — the last component is 3 > 0 (and is the only nonzero component), so Pos holds. (iv) `actionPoint(ℓ_span) ≤ #i₁` — actionPoint(ℓ_span) = 8 (the position of the first nonzero component) and #i₁ = 8, satisfying the bound with equality (T12 admits equality, since the condition is `≤`). Then s_span ⊕ ℓ_span: positions 1–7 are copied from s_span (yielding 1.0.1.0.1.0.1), and position 8 advances by 3 (1 + 3 = 4), so the reach is s_span ⊕ ℓ_span = 1.0.1.0.1.0.1.4 = O.0.1.4. The span coverage `⟦(i₁, ℓ_span)⟧ = {t : i₁ ≤ t < O.0.1.4}` contains the element-level tumblers i₁, i₂, i₃ — each agrees with i₁ on positions 1–7 and has position-8 value in {1, 2, 3} ⊆ [1, 4) — independently of allocation status, since `⟦(s, ℓ)⟧` is a subset of T determined by the tumbler structure of (s, ℓ) and is well-formed under T12 (just discharged above) regardless of whether s ∈ dom(Σ.C). Take v₁ = [s_C, 1] — the depth-2 D-MIN V-position in d's content subspace: #v₁ = 2 (satisfying S8a's depth-≥-2 requirement for V-positions in dom(M(d))), every component is strictly positive (S8a positivity), and v₁ = [s_C, 1, ..., 1] of depth m_C = 2 is the unique minimum of V_{s_C}(d) (D-MIN, ASN-0047). The state Σ is reachable from the empty state by a *standard composite chain* of elementary transitions whose existence we now make explicit (omitted, this would be the "single K.μ⁺ step" misreading that conflates the final extension with the full witness): the node entity at 1 is supplied by InitialState (we fix the bootstrap-node system parameter `n₀ = 1` throughout this ASN's witnesses; `E₀ = {n₀}` with `IsNode(n₀)` by ASN-0047's InitialState, so node 1 inhabits E from Σ₀ onward without any K.δ node-allocation step); K.δ then allocates the account entity at 1.0.1 under the now-existing node n₀ = 1 (K.δ ¬IsNode case), and the document d itself by a further K.δ step under the now-existing account (per-level K.δ steps under ASN-0047, each discharging its prefix-existence precondition against the entities already in E); K.α allocates the single element-level content address i₂ under d's prefix with fields(i₂).E₁ = s_C (per the K.α amendment) — i₁ and i₃ remain unallocated tumblers in T per the J0 discussion above, since the chain places only i₂; K.λ allocates the link at the link-subspace address `a = 1.0.1.0.1.0.s_L.1` — under the same node/account/document prefix `1.0.1.0.1` as the i_k, with the element field beginning with the link-subspace identifier `s_L` per the K.λ amendment (ASN-0047) and terminal component `1` ensuring T4-validity (zeros at positions 2, 4, 6 only; a₁ = 1 ≠ 0; a_#a = 1 ≠ 0; no adjacent zeros), with `a ∉ dom(Σ.L)` in the pre-state (freshness) — carrying the standard triple `Σ.L(a) = (F, G, Θ)` where `F = {(i₁, ℓ_span)}` is the content span constructed above, `G = ∅` (the to-endset is empty; L4 (EndsetGenerality, ASN-0043) admits empty endsets, and the ASN's "Empty-endset cases" discussion explicitly treats one-sided links of the form `(F, ∅, Θ)`), and `Θ = {(τ, ℓ_τ)}` for any single non-empty type span (the specific T4-valid `τ` and the non-zero `ℓ_τ` are immaterial to the projection computation, which reads only F; their existence is what L3's requirement `|Σ.L(a)| ≥ 3 ∧ Σ.L(a).e₃ ≠ ∅` demands of the type slot). This link value satisfies L0–L14: L3 holds because the triple has exactly three slots with `Θ ≠ ∅`; L4 admits both `(i₁, ℓ_span)` as a content span and `(τ, ℓ_τ)` as a type span; L0/L14 hold because `fields(a).E₁ = s_L` places a in the link-subspace partition disjoint from dom(Σ.C); the remaining L-properties carry through by direct inspection of the allocation. K.μ⁺ extends M(d) from V_{s_C}(d) = ∅ by adding v₁ ↦ i₂ (the value i₂ ∈ dom(Σ.C) satisfying K.μ⁺'s referential-integrity precondition, v₁ admissible as D-MIN by D-CTG with the empty prior content-subspace); and K.ρ records the provenance mapping required to satisfy J1★ (ExtensionRecordsProvenanceContent, ASN-0047) — the K.μ⁺ extension of a content-subspace mapping must be accompanied by a K.ρ step that records v₁'s I-value in R for J1★ to hold in the composite post-state. The per-step preconditions are pairwise independent up to the prefix ordering required by K.δ (account-allocation before document-allocation, with the node n₀ = 1 supplied by InitialState rather than by K.δ) and the referential-integrity ordering required by K.μ⁺ (K.α before K.μ⁺); D-CTG and S8-depth hold trivially for the singleton V_{s_C}(d) = {v₁} at the post-state. With a, F, G, Θ as constructed, coverage(F) ⊇ {i₁, i₂, i₃} ∋ i₂. Then with V = {v₁} and A = {Σ.M(d)(v₁)} = {i₂}:

- *Discovery succeeds:* coverage(F) ∩ A = {i₂} ≠ ∅, so a ∈ discover_from(A).
- *Projection is proper:* π(F, d) = coverage(F) ∩ ran(Σ.M(d)) = coverage(F) ∩ {i₂} = {i₂}, but coverage(F) ⊇ {i₁, i₂, i₃} ⊋ {i₂}, so π(F, d) ⊊ coverage(F).

The link is discoverable through d via the shared address i₂, yet resolves to only that one I-address — i₁ and i₃ remain in coverage(F) but are absent from ran(Σ.M(d)). ∎

The cross-document case is starker: a link discovered through document d₁ (which shares I-addresses with the endset) may have empty resolution in a different document d₂ whose arrangement contains none of the endset's I-addresses. Discovery and resolution operate through independent documents; discovery through one does not entail resolution in another. We state this formally.

**Corollary (CrossDocumentDecoupling, from SV10).** Discovery in one document and empty resolution in another are simultaneously realisable:

`(E Σ, a, d₁, d₂, s, A :: d₁ ≠ d₂ ∧ a ∈ discover_s(A) ∧ A ⊆ ran(Σ.M(d₁)) ∧ π(Σ.L(a).s, d₂) = ∅)`

*Witness.* Extend the SV10 witness. Σ already contains link a with F = {(i₁, ℓ_span)} and coverage(F) ⊇ {i₁, i₂, i₃}; let d₁ = d with Σ.M(d₁) = {v₁ ↦ i₂}, so the SV10 discovery clause supplies A = {i₂} ⊆ ran(Σ.M(d₁)) and a ∈ discover_from(A). We now reach, by a chain of elementary transitions enabled in Σ, a successor state Σ⁺ in which the existential's remaining clauses (d₂ ≠ d₁, π(Σ⁺.L(a).from, d₂) = ∅) are witnessed; the existential is over states reachable from Σ, so exhibiting Σ⁺ suffices.

*Setup precondition (inherited from SV10).* The SV10 base state Σ presupposes that d₁ ∈ E_doc with origin(d₁) = O = 1.0.1.0.1; for d₁'s address to be well-formed under K.δ, the prefix entities — a node entity at 1 and an account entity at 1.0.1 (under K.δ's per-level preconditions, ASN-0047) — must already inhabit E. We discharge the node requirement by identifying the bootstrap node `n₀` with address `1`: by InitialState (ASN-0047), `E₀ = {n₀}` with `IsNode(n₀)`, and the choice of n₀ is a system parameter (not a state transition); we fix this parameter at `n₀ = 1` throughout this ASN's witnesses, so the node entity at 1 inhabits E from Σ₀ onward without any K.δ node-allocation step. The account entity at 1.0.1 is then allocated by K.δ under the now-existing node n₀ = 1 (K.δ ¬IsNode case, ASN-0047), and d₁ = 1.0.1.0.1 by a further K.δ step under the now-existing account. We assume these two K.δ allocations have taken place prior to Σ (equivalently, the SV10 witness chain is implicitly preceded by K.δ steps that allocate the account at 1.0.1 and the document d₁ itself; the node 1 is supplied by InitialState, not by K.δ). Under this base state, Step 1 below allocates a *sibling* document d₂ = 1.0.1.0.2 under the *same* account 1.0.1 — the node n₀ = 1 and the account 1.0.1 are already in E, so K.δ's prefix-existence preconditions are discharged by Σ's setup and only the document-level admissibility (T4-validity, freshness) is checked in Step 1.

- *Step 1 — K.δ allocates d₂ under a node/account prefix yielding origin(d₂) ≠ O.* The SV10 origin used was O = 1.0.1.0.1. Pick any node/account/document prefix `d₂ = 1.0.1.0.2` — agreeing with O on the node and account fields (positions 1–4: 1.0.1.0) but differing at the document component (position 5: 2 vs 1), so origin(d₂) = 1.0.1.0.2 ≠ O. K.δ (EntityCreation, ASN-0047) — applied with k = 0 from t = d₁ (sibling case under K.δ case (ii), ASN-0047), which by TA5(c) preserves zeros so `IsDocument(d₁) ⇒ IsDocument(d₂)` — allocates d₂ ∈ E_doc \ dom(Σ.M) and seeds Σ.M(d₂) = ∅ per ASN-0047's K.δ effect-on-M clause for IsDocument(e) allocations. The document address d₂ is T4-valid by construction, and the prefix-entity precondition — that the node entity at 1 and the account entity at 1.0.1 inhabit E at the Σ-state where this K.δ fires — is discharged by P1 (EntityPermanence, ASN-0047) together with the setup precondition: the setup precondition places node 1 in E from Σ₀ (by InitialState) and the account 1.0.1 in E by an earlier K.δ step, and P1's `E ⊆ E'` invariant carries both entries forward through every intervening transition of the SV10 witness chain (K.δ on d₁, K.α on i₂, K.λ on a, K.μ⁺ on v₁ ↦ i₂, K.ρ — none of which can remove entries from E), so both remain in E at Σ. Freshness — `d₂ ∉ dom(Σ.M)` — holds because the SV10 chain placed only d₁ in dom(M), so the sibling-document address d₂ = 1.0.1.0.2 lies outside it. K.δ's preconditions are therefore satisfied.
- *Step 2 — K.α allocates j under d₂'s prefix with fields(j).E₁ = s_C.* Apply K.α (ContentAllocation, ASN-0047) to allocate a fresh element-level address j with prefix d₂ and element field beginning at s_C: take j = 1.0.1.0.2.0.1.1 — T4-valid (zeros at positions 2, 4, 6 only; j₁ = 1 ≠ 0; j_#j = 1 ≠ 0; no adjacent zeros), with origin(j) = N(j).0.U(j).0.D(j) = 1.0.1.0.2 = origin(d₂) ≠ O, and with fields(j).E₁ = j₇ = 1 = s_C (per the K.α amendment, ASN-0047). The post-state has j ∈ dom(Σ.C).
- *Step 3 — K.μ⁺ places v₁ ↦ j in M(d₂), composed with K.ρ for J1★.* Apply K.μ⁺ (ArrangementExtension, ASN-0047) to add the mapping v₁ ↦ j to M(d₂). The precondition v₁ ∈ V_{s_C}(d₂) is satisfied (v₁ = [s_C, 1] is the D-MIN of the content subspace, available because M(d₂) = ∅ pre-step — the `v₁ = [s_C, 1]` here is the D-MIN position of d₂'s content subspace, distinct from but co-named with d₁'s D-MIN; V-positions are document-local, so the symbol `v₁` from the SV10 witness for d₁ and the `v₁` introduced here for d₂ inhabit disjoint per-document V-arenas and refer to different V-positions despite the shared coordinate tuple), and the referential-integrity precondition j ∈ dom(Σ.C) holds by Step 2. Compose with K.ρ (ProvenanceRecording, ASN-0047) recording the provenance of the v₁ ↦ j mapping into R; this is required by ValidCompositeExtended for J1★ (ExtensionRecordsProvenanceContent, ASN-0047) to hold in the post-state, since K.μ⁺ alone would extend M(d₂) without an accompanying R-entry. K.ρ modifies R only — it preserves L and M — so the SV6 application below, which depends only on origin(j) ≠ O and the post-state Σ⁺.M(d₂), is unaffected. Post-state Σ⁺ satisfies Σ⁺.M(d₂) = {v₁ ↦ j} and the J1★ coupling.

*SV6 precondition verification.* SV6 (CrossOriginExclusion) requires six preconditions on (s, ℓ, b); for (s, ℓ, b) := (i₁, ℓ_span, j) the discharge is: (i) `i₁, j ∈ T` by construction (both length-8 ℕ-sequences). (ii) `(i₁, ℓ_span)` is T12-well-formed by the T12 verification in the SV10 witness above. (iii) `zeros(i₁) = 3` (zeros at positions 2, 4, 6) and `zeros(j) = 3` (zeros at positions 2, 4, 6) — both element-level. (iv) `i₁` is T4-valid (components 1, 0, 1, 0, 1, 0, 1, 1: no adjacent zeros, i₁_1 = 1 ≠ 0, i₁_8 = 1 ≠ 0); `j` is T4-valid (verified at Step 2 above: zeros at positions 2, 4, 6 only; j₁ = 1 ≠ 0; j_#j = 1 ≠ 0; no adjacent zeros). (v) `origin(j) = 1.0.1.0.2 ≠ 1.0.1.0.1 = origin(i₁)` — the document fields differ at position 5 (2 vs 1). (vi) `k = actionPoint(ℓ_span) = 8` and `p₃ = 6` (position of the third zero component in i₁, at position 6), so `k > p₃` ✓. All six preconditions hold; SV6 applies. By SV6, j ∉ ⟦(i₁, ℓ_span)⟧. So coverage(F) ∩ {j} = ∅ and π(Σ⁺.L(a).from, d₂) = coverage(F) ∩ ran(Σ⁺.M(d₂)) = coverage(F) ∩ {j} = ∅. The link-store-frame transitions (K.δ, K.α, K.μ⁺, K.ρ) preserve Σ.L by L12, so the discovery clause and coverage(F) are unchanged in Σ⁺, and the SV10-supplied a ∈ discover_from(A) with A ⊆ ran(Σ⁺.M(d₁)) carries forward. ∎

This asymmetry is not a deficiency. It reflects a genuine conceptual distinction: the link *exists* and *relates to* certain content (discovery); the *visibility* of that relationship depends on which document you are looking through (resolution).


## Partial Survival

When contraction removes some but not all of an endset's I-addresses from a document's arrangement, the endset survives with reduced projection. We now characterize the structure of this partial survival.

We distinguish two related concepts that the decomposition produces: *decomposition terms* — the individual set intersections that appear in the union formula — and *maximal fragments* — the maximal contiguous ordinal subsequences that the union as a whole presents in each mapping block. The two coincide when each span contributes a disjoint contiguous region; they diverge when adjacent or overlapping spans coalesce.

**Definition — Decomposition Term.** For each pair (j, k) with 1 ≤ j ≤ m and 1 ≤ k ≤ p, the *(j, k)-decomposition term* is the set `⟦(sⱼ, ℓⱼ)⟧ ∩ I(β_k)`. The number of decomposition terms is exactly m · p (possibly with empty terms).

**Definition — Maximal Endset Fragment.** For an endset e and document d, let B = {β₁, ..., β_p} be the maximally merged block decomposition (C1a, ASN-0058) of the restriction M(d)|_{V_{s_C}(d)}. A *maximal fragment* of e in d is a maximal contiguous subsequence of I-addresses within a single mapping block's ordinal sequence taken within the *text-subspace projection* π_text(e, d) — fragments are by construction confined to the content subspace s_C, since B partitions only the content-subspace V-positions and I(β_k) ⊆ ran_text(M(d)). The link-subspace contribution to π(e, d) is deferred to the Link Subspace ASN (see the parenthetical after SV11). Formally, F = {a_k + j : j₁ ≤ j ≤ j₂} ⊆ π_text(e, d) ∩ I(β_k) for some block β_k = (v_k, a_k, n_k), where F is maximal with respect to extending j₁ downward or j₂ upward within π_text(e, d) ∩ I(β_k). That is, either j₁ = 0 or a_k + (j₁ - 1) ∉ π_text(e, d), and either j₂ = n_k - 1 or a_k + (j₂ + 1) ∉ π_text(e, d).

A decomposition term ⟦(sⱼ, ℓⱼ)⟧ ∩ I(β_k) is itself contiguous in the ordinal sequence of I(β_k) (by the convexity argument below) but need not be maximal: two terms with the same k may overlap or be adjacent, in which case their union forms a single maximal fragment.

**SV11 (PartialSurvivalDecomposition).** Let e = {(s₁, ℓ₁), ..., (s_m, ℓ_m)} be an endset, and let B = {β₁, ..., β_p} be the maximally merged block decomposition of the restriction M(d)|_{V_{s_C}(d)} — the content-subspace portion of d's arrangement. This restriction satisfies C1a's conditions: functionality from S2, finiteness from S8-fin, and fixed depth from S8-depth within subspace s_C. The text-subspace projection π_text(e, d) (Endset Projection section) admits the block-indexed expansion `ran_text(M(d)) = ⋃_k I(β_k)` under B — the equality is derived from B's V-coverage and the per-block mapping rule: B1 (ASN-0058, Coverage) gives that every `v ∈ dom(M(d)|_{V_{s_C}(d)})` lies in `V(β_k)` for a unique k, and within each block β_k = (v_k, a_k, n_k) the per-block mapping rule (B3, ASN-0058) sets `M(d)(v) = a_k + (v − v_k) ∈ I(β_k)`; collecting over all v in the restriction's domain yields `ran_text(M(d)) = {M(d)(v) : v ∈ dom(M(d)|_{V_{s_C}(d)})} = ⋃_k {M(d)(v) : v ∈ V(β_k)} = ⋃_k I(β_k)`, so the I-extents of B's blocks are precisely the content-subspace I-addresses. Two count claims hold simultaneously:

(a) *Decomposition-term cover — exactly m · p terms.*

`π_text(e, d) = (∪ j, k : 1 ≤ j ≤ m ∧ 1 ≤ k ≤ p : ⟦(sⱼ, ℓⱼ)⟧ ∩ I(β_k))`

The union is over *exactly* m · p decomposition terms (one per (span, block) pair), some possibly empty. Here "exactly m · p decomposition terms" counts (span, block)-indexed positions in the Cartesian product, not distinct subsets of π_text(e, d); two terms may coincide as sets.

(b) *Maximal-fragment count — at most m · p fragments.* The same set π_text(e, d) is also the disjoint union (within each block) of its maximal ordinal-contiguous fragments, totalling *at most* m · p of them across all blocks. The inequality is strict whenever (a) some decomposition term `⟦(sⱼ, ℓⱼ)⟧ ∩ I(β_k)` is empty, or (b) two non-empty decomposition terms within a single block are ordinally adjacent or overlap, coalescing into a single maximal fragment. The bound m · p is attained iff every (j, k) pair yields a non-empty decomposition term *and* these terms are pairwise non-adjacent and non-overlapping within each block. *Reading of "non-adjacent" and "non-overlapping" within a block.* For two non-empty terms `T₁, T₂` within a single block β_k, each occupying an ordinal-offset range `[i₁, j₁]` and `[i₂, j₂]` respectively (contiguous by the S0-convexity argument below), say WLOG `j₁ < i₂` after ordering: *non-overlapping* means the two offset ranges are disjoint as sets, i.e., `j₁ < i₂` (no common offset); *non-adjacent* means the offset gap between them is at least one, i.e., `j₁ + 1 < i₂` (equivalently `i₂ − j₁ ≥ 2`). The non-adjacency condition strictly strengthens non-overlap: two terms can be disjoint but ordinal-adjacent (`i₂ = j₁ + 1`), in which case they coalesce into a single maximal fragment by mechanism (b). The biconditional's "pairwise non-adjacent and non-overlapping" therefore demands gap ≥ 1 between every pair of distinct non-empty terms within each block (equivalently, every pair has at least one unused ordinal offset between them). *Proof of the biconditional.* (⇒) Suppose the maximal-fragment count equals m · p. The fragment count is bounded above by the non-empty-term count via the following surjection. Each non-empty term `⟦(sⱼ, ℓⱼ)⟧ ∩ I(β_k)` is contiguous within its block's ordinal sequence by the S0-convexity argument below, so it lies entirely inside a single maximal fragment of β_k; the map `Φ : (non-empty term) ↦ (containing maximal fragment in its block)` is therefore well-defined. Φ is surjective: every maximal fragment F is, by construction, the union of the non-empty terms it contains (each fragment's ordinal-contiguous range coincides with the union of the terms' ranges that produced it, and a fragment without contributing non-empty terms would not appear in `π_text(e, d) ∩ I(β_k)` at all), so F is the image under Φ of at least one non-empty term. Surjectivity of Φ from the (finite) set of non-empty terms onto the (finite) set of maximal fragments gives `|non-empty terms| ≥ |maximal fragments|` (each codomain element is hit at least once, so the domain has at least as many elements as the codomain), so `|fragments| ≤ |non-empty terms| ≤ m · p` (the second inequality is the total-term count). Equality with m · p in the fragment count therefore forces equality at every link of this chain — (i) the non-empty-term count to equal m · p — every (j, k) yields a non-empty term, ruling out condition (a) — and (ii) each non-empty term to be itself a maximal fragment (Φ must be a bijection at attainment), ruling out condition (b)'s adjacency or overlap of distinct non-empty terms within a block (any adjacency *or overlap* would coalesce two terms into one fragment under Φ, dropping the count below m · p; the overlap half holds because two overlapping non-empty terms within a single block — each contiguous in I(β_k)'s ordinal sequence by the S0-convexity argument — share at least one ordinal position, so their union is itself ordinal-contiguous in I(β_k) and forms a single maximal fragment). (⇐) Conversely, suppose every (j, k) yields a non-empty term and no two non-empty terms within any single block are ordinally adjacent or overlap. Within each block β_k, the m terms (one per span) are pairwise non-adjacent and non-overlapping, and each is contiguous (by the S0-convexity argument below), so no two coalesce: the maximal fragments inside β_k are exactly the m non-empty terms restricted to β_k. Summing across the p blocks gives exactly m · p maximal fragments. ∎

*Empty-arrangement boundary case (p = 0).* When `M(d)|_{V_{s_C}(d)} = ∅` — the document has no content-subspace mappings — the maximally merged block decomposition B is empty, so p = 0. The decomposition-term cover (a) becomes a vacuous union over the empty index set `{(j, k) : 1 ≤ j ≤ m ∧ 1 ≤ k ≤ 0}`, equal to ∅, matching `π_text(e, d) = coverage(e) ∩ ran_text(M(d)) = coverage(e) ∩ ∅ = ∅`; the maximal-fragment count (b) is correspondingly 0, with `m · p = 0` as the trivial upper bound and exact attainment by the empty collection of fragments. SV11 is therefore well-formed at p = 0 with both counts equal to 0; the attainment biconditional holds vacuously (no (j, k) pairs to check, no within-block non-adjacency requirement to satisfy).

*Attainment witness coverage — roadmap.* Five regions of `(m, p)`-parameter space admit attainment witnesses; two structural-failure regions admit none. The table below summarises witness type and the structural condition each region depends on; the detailed proofs follow below in the order listed.

| Region | Witness type | Base witness | Iteration | Structural condition |
|--------|-------------|-------------|-----------|---------------------|
| `(m ≥ 1, p = 1)` | single-block sibling | 5-sibling β = (v₁, a₁, 5); m = 2 case explicit | extend to arbitrary m by adding sibling pairs | injective single block, vacuous within-block constraint at p = 1 |
| `(m = 1, p ≥ 2)` | single-span, multi-block | Worked Example (p = 2); W(1, 3) explicit | direct per-p chain from Σ₀ for p ≥ 4 (offset-1 excision schedule) | single-span vacuous (b); single span covers every block |
| `(m ≥ 2, p = 2)` | overlapping I-extents, two blocks | W(2, 2) standalone; W(3, 2) explicit | (α_2) lift `W(m, 2) → W(m + 1, 2)` from W(3, 2) for m ≥ 4 | `min_k n_k ≥ 2m − 1` (non-injective small-block threshold) |
| `(m = 2, p ≥ 3)` | overlapping I-extents, p blocks | W(2, 3) explicit | (β_2) lift `W(2, p) → W(2, p + 1)` from W(2, 3) for p ≥ 4 | `min_k n_k ≥ 2m − 1 = 3` (tight) |
| `(m ≥ 3, p ≥ 3)` | nested overlapping I-extents | W(3, 3), W(4, 3) explicit | (α) and (β) lifts from W(3, 3) for arbitrary (m ≥ 3, p ≥ 3) | `min_k n_k ≥ 2m − 1` (tight at m's value) |
| *Disjoint-pair non-attainment* | — | — | — | any pair of blocks with disjoint I-extents at `m ≥ 2, p ≥ 2` excludes attainment |
| *Small-block non-attainment* | — | — | — | overlapping I-extents but `min_k n_k < 2m − 1` at `m ≥ 2, p ≥ 2` excludes attainment |

The five attainment regions exhaust `(m ≥ 1, p ≥ 1)` together with the `p = 0` boundary case (`m · p = 0`, vacuous attainment); the two non-attainment regions exhaust the complementary structural-failure space at `(m ≥ 2, p ≥ 2)`. Witnesses follow.

*Attainment witness — p = 1 case.* The m · p bound is realised concretely *for p = 1*. Reusing the Worked Example's initial state — `M(d) = {v₁ ↦ a₁, v₂ ↦ a₂, v₃ ↦ a₃, v₄ ↦ a₄, v₅ ↦ a₅}` with five sibling addresses of equal tumbler length sharing one origin, forming a single maximally merged block β = (v₁, a₁, 5) with I-extent I(β) = {a₁, a₂, a₃, a₄, a₅} (so p = 1) — take the two-span endset `e = {(s₁, ℓ₁), (s₂, ℓ₂)}` with s₁ = a₁, ℓ₁ = a₂ ⊖ a₁ (well-defined by D0; reach a₂ by D1 inverse, coverage `⟦(a₁, ℓ₁)⟧ = {t : a₁ ≤ t < a₂}`) and s₂ = a₄, ℓ₂ = a₅ ⊖ a₄ (well-defined by D0; reach a₅, coverage `⟦(a₄, ℓ₂)⟧ = {t : a₄ ≤ t < a₅}`). Then m = 2 and the m · p = 2 decomposition terms are:

- ⟦(s₁, ℓ₁)⟧ ∩ I(β) = {a₁}
- ⟦(s₂, ℓ₂)⟧ ∩ I(β) = {a₄}

Both terms are non-empty, ruling out mechanism (a). Within β their ordinal positions are a₁ (offset 0) and a₄ (offset 3), separated by a₂, a₃ at offsets 1, 2 — non-adjacent (the gap between offsets 0 and 3 has width 2 ≥ 1) and non-overlapping (disjoint singletons), ruling out mechanism (b). The maximal-fragment count equals the non-empty-term count equals m · p = 2; both bounds are saturated simultaneously. ✓ An *alternative* (more parsimonious) parametric witness at arbitrary m with p = 1 takes m spans of the form `(a_{2j-1}, a_{2j} ⊖ a_{2j-1})` over 2m − 1 sibling addresses in a single block β = (v₁, a₁, 2m − 1); each span's coverage is the singleton `{a_{2j-1}}` at offset `2j − 2` in β, the m offsets `{0, 2, ..., 2m − 2}` form a step-2 arithmetic progression with gap-width-1 non-adjacency (the intervening offsets `1, 3, ..., 2m − 3` carry the non-endpoint siblings `a₂, a₄, ..., a_{2m − 2}`), and the maximal-fragment count equals the non-empty-term count equals m · p = m. This is a *separate* witness from the m = 2 worked example above rather than its extension — the worked example uses 5 siblings with gap-width 2 between offsets 0 and 3, while the parametric form's m = 2 instance uses 3 siblings (spans `(a₁, a₂ ⊖ a₁)` and `(a₃, a₄ ⊖ a₃)`) with gap-width 1 between offsets 0 and 2; both witnesses saturate at m = 2, p = 1, but neither is the other's extension.

*Foundation citation chain for unallocated siblings (cited below as "the unallocated-sibling chain").* The existence of T4-valid same-length siblings past any allocated address `a` — used repeatedly in the multi-block witnesses below (and again in the Worked Example's three-span variant and the W(m, p) shape specifications) for span well-formedness when the span's reach lies past `dom(Σ.C)` — is supplied by the following foundation-citation chain, recorded once here and cited by reference in subsequent witnesses. (i) T0(a) (UnboundedComponentValues, ASN-0034) instantiated at the last-component position `i = #a` and bound `M = a_{#a}` guarantees a same-length tumbler in T whose last component exceeds `a_{#a}`. (ii) OrdinalShiftBase (ASN-0058) identifies a concrete witness `a + 1 = shift(a, 1)` as the next ordinal sibling under `a`'s prefix; for a T4-valid `a` with terminal component nonzero (which T4's conjunct `a_{#a} ≠ 0` supplies), TA5-SigValid (ASN-0034) gives `sig(a) = #a`, so `a + 1` is exactly `inc(a, 0)` acting at the terminal component. (iii) TA5a (IncrementPreservesT4, ASN-0034) with `k = 0` preserves T4-validity unconditionally on T4-valid inputs, so `a + 1` inherits `a`'s T4-validity; iterating delivers `a + 2, a + 3, ...` as further T4-valid same-length siblings. These siblings inhabit T regardless of whether they are placed into `dom(Σ.C)` by a K.α step — span well-formedness via T12 (SpanWellDefinedness, ASN-0034) is a property of the tumbler structure, not of allocation status, and L4 (EndsetGenerality, ASN-0043) admits endset spans over any T-inhabiting tumbler. Subsequent witnesses cite this chain when introducing an unallocated sibling past `dom(Σ.C)` without re-deriving each step; "T0(a), unallocated-sibling chain" and "T0(a) again" in subsequent paragraphs refer to this chain.

*Attainment witnesses — multi-block (p ≥ 2).* The bound is also realised at p ≥ 2 in two further configurations. (i) *(m = 1, p ≥ 2) — single span, multiple blocks.* The Worked Example below, at its post-removal state `M'(d) = {v₁ ↦ a₁, v₂ ↦ a₂, v₃ ↦ a₄, v₄ ↦ a₅}`, has m = 1 (the single span (a₂, a₅ ⊖ a₂)) and p = 2 (blocks β₁ = (v₁, a₁, 2), β₂ = (v₃, a₄, 2)); both decomposition terms `{a₂}` and `{a₄}` are non-empty, and with m = 1 the within-block pairwise non-adjacency requirement of mechanism (b) is vacuous, yielding 2 = m · p maximal fragments. Attainment at (m = 1, p ≥ 2) is governed only by mechanism (a) — every span-block pair must be non-empty — which is satisfiable whenever the single span's coverage intersects every block's I-extent. *Concrete (m = 1, p = 3) witness.* Take seven T4-valid sibling tumblers `a₁ < a₂ < ... < a₇` allocated by K.α steps at one origin and one tumbler length (per the same J0-compliant allocation discipline used in the Worked Example), plus an unallocated T4-valid sibling `a₈` retained in T for span well-formedness (T0(a), UnboundedComponentValues, ASN-0034). Begin with M(d) mapping `v₁..v₇ ↦ a₁..a₇` sequentially in subspace s_C, then apply two excision composites paralleling the Worked Example's K.μ~ + K.μ⁻ pattern — first removing a₃ from ran(M(d)) (a K.μ~ step shifts a₃ to the maximum V-position followed by a K.μ⁻ step contracting it), then removing a₅ from ran(M(d)) by the same pattern — yielding post-state `M'(d) = {v₁ ↦ a₁, v₂ ↦ a₂, v₃ ↦ a₄, v₄ ↦ a₆, v₅ ↦ a₇}`. The maximally merged block decomposition (C1a, ASN-0058) has p = 3 blocks: β₁ = (v₁, a₁, 2) with I-extent {a₁, a₂}, β₂ = (v₃, a₄, 1) with I-extent {a₄}, β₃ = (v₄, a₆, 2) with I-extent {a₆, a₇}. Both inter-block boundaries fail M7 (BlockMergeCondition, ASN-0058): at v₂ → v₃, V-adjacency holds but I-adjacency requires `a₂ + 1 = a₃ = a₄`, false; at v₃ → v₄, V-adjacency holds but I-adjacency requires `a₄ + 1 = a₅ = a₆`, false. M12's split rule therefore forces both block boundaries. Take the single span `(a₁, a₈ ⊖ a₁)` with coverage `[a₁, a₈) = {a₁, ..., a₇}` (D0-well-defined since a₁ and a₈ share origin and tumbler length). The m · p = 3 decomposition terms are `{a₁, a₂}`, `{a₄}`, `{a₆, a₇}` — each a contiguous ordinal subsequence of its block by the S0-convexity argument below, each non-empty (mechanism (a) avoided since the single span's coverage `{a₁..a₇}` ⊇ every block's I-extent), and with m = 1 the within-block non-adjacency requirement (mechanism (b)) is vacuous (no within-block pair to check). Each non-empty term is itself a maximal fragment within its block, so the maximal-fragment count equals m · p = 3. ✓ *Generalisation to (m = 1, p ≥ 4).* For every p ≥ 4, the witness W(1, p) is constructed *directly from Σ₀* by a fresh ValidCompositeExtended chain paralleling W(1, 3) rather than by iteration from the (m = 1, p = 3) base; the iterative-lift framing of an earlier draft required an interior (non-boundary) I-address within a block of size ≥ 3 (a size-2 or size-1 block has no interior position and admits no further excision), and the (m = 1, p = 3) base witness above happens to have only size-2 and size-1 blocks, so it cannot serve as the iteration's starting point. The per-p direct construction circumvents this: allocate 2p + 1 T4-valid siblings a₁..a_{2p+1} (each by K.α at the next ordinal position past the previous, T4-validity preserved by TA5a), plus one unallocated sibling a_{2p+2} retained in T by the unallocated-sibling chain above for span well-formedness, place them sequentially in M(d) by K.μ⁺ steps (with K.ρ for J1★), and then apply p − 1 excision composites — each a K.μ~ + K.μ⁻ pair removing one chosen I-address from ran(M(d)) — to produce a p-block decomposition. Each excision targets an *interior* (non-boundary) I-address of a *currently-targeted* block of size ≥ 3, so that the K.μ⁻ split (forced by M12 since the removed position breaks I-adjacency between the resulting halves) produces two non-empty halves. *Concrete (m = 1, p = 4) construction (offset-1 schedule):* allocate a₁..a₉, place sequentially in M(d), then excise a₂ (offset 1 of the initial size-9 block, splitting it into a size-1 left piece {a₁} and a size-7 right piece {a₃..a₉}), excise a₄ (offset 1 of the resulting size-7 block, splitting it into {a₃} and {a₅..a₉} — yielding total sizes 1, 1, 5), and excise a₆ (offset 1 of the resulting size-5 block, splitting it into {a₅} and {a₇..a₉} — yielding total sizes 1, 1, 1, 3, four blocks). The single span `(a₁, a_{2p+2} ⊖ a₁)` covers `[a₁, a_{2p+2}) = {a₁..a_{2p+1}}` (D0-well-defined since a₁ and a_{2p+2} share origin and tumbler length), intersects every block's I-extent non-emptily (mechanism (a) satisfied), and mechanism (b) is vacuous at m = 1; the m · p = p decomposition terms are exactly the p non-empty block-intersection terms, each itself a maximal fragment. The same *offset-1 schedule* generalises uniformly to every p ≥ 4 and maintains the size-≥3 invariant throughout: at each step i (1 ≤ i ≤ p − 1), excise the offset-1 position of the unique current block of size ≥ 3. Tracing through, starting from sizes [2p + 1], the offset-1 excision of the size-(2p + 1 − 2(i − 1)) main block splits it into a size-1 left piece (the singleton at offset 0) and a size-(2p + 1 − 2i) right piece, yielding block sizes [1, 1, ..., 1, 2p + 1 − 2i] after step i (i size-1 pieces and one size-(2p + 1 − 2i) right-tail block, which is the unique block of size ≥ 2 at that step). The size-≥3 invariant required before step i — i.e., that the main block be at least size 3 before being excised — holds iff 2p + 1 − 2(i − 1) ≥ 3, i.e., i ≤ p. The bound i ≤ p ensures the invariant holds for every excision step i = 1, ..., p − 1; the construction therefore terminates after p − 1 excisions yielding p blocks. After step p − 1 we have p blocks with sizes [1, 1, ..., 1, 3] — exactly p blocks, no further excision required. The (m = 1, p = 4) construction above is the offset-1 schedule's base case at p = 4 (sizes 1, 1, 1, 3 after three excisions of nine siblings), and the schedule extends to every p ≥ 5 by the same recipe. The construction is *direct from Σ₀ per p* in the sense that each W(1, p) is built fresh from Σ₀ rather than by iteration from a lower-p witness's state; W(1, 2) from the Worked Example and W(1, 3) from the explicit witness above are the small-p base cases, and every W(1, p ≥ 4) is produced de novo by the offset-1 schedule. (ii) *(m ≥ 2, p ≥ 2) — non-injective sharing with sufficiently large blocks.* Let `a₁ < a₂ < ... < a₁₀` be ten T4-valid sibling addresses in subspace s_C, all sharing one origin and one tumbler length, and let `v₁ < v₂ < ... < v₁₅` be sequential V-positions in subspace s_C at the subspace's common depth m_C — V-side ordinal siblings under OrdinalShiftBase, so `v_{k+1} = v_k + 1` (the V-side application of `+` of the Notation section) for 1 ≤ k ≤ 14, with every v_k a length-m_C tumbler sharing its first m_C − 1 components. Under the depth-2 convention these read concretely as `v_k = [s_C, k]`; the witness uses only the sequential-sibling property, not a specific m_C. Take the arrangement

`M(d)|_{V_{s_C}(d)} = {v_i ↦ a_i : 1 ≤ i ≤ 10} ∪ {v_{10+i} ↦ a_{5+i} : 1 ≤ i ≤ 5}`

— a non-injective mapping admitted by S5 (UnrestrictedSharing, ASN-0036), with v₁₁..v₁₅ re-mapping to a₆..a₁₀ already mapped by v₆..v₁₀. The maximally merged block decomposition (C1a, ASN-0058) of this restriction has p = 2 blocks: β₁ = (v₁, a₁, 10) with I-extent {a₁, ..., a₁₀}, and β₂ = (v₁₁, a₆, 5) with I-extent {a₆, ..., a₁₀}. The boundary between them satisfies V-adjacency (v₁₀ and v₁₁ are consecutive V-positions) but fails I-adjacency: M7 (BlockMergeCondition, ASN-0058) would require a₁₀ + 1 = a₆, which is false (a₆ ≠ a₁₁, where a₁₁ denotes the next ordinal sibling beyond a₁₀); M12's split rule therefore forces the block boundary at v₁₀ → v₁₁, and both blocks are individually maximal. The two blocks have overlapping I-extents: β₂'s extent {a₆..a₁₀} is a subset of β₁'s {a₁..a₁₀}, exhibiting within-document sharing under S5. Now take the two-span endset e = {(s₁, ℓ₁), (s₂, ℓ₂)} with s₁ = a₁, ℓ₁ = a₈ ⊖ a₁ (well-defined by D0 since a₁ and a₈ share origin and tumbler length; coverage `⟦(a₁, ℓ₁)⟧ = {t : a₁ ≤ t < a₈}`, containing the allocated addresses {a₁, ..., a₇}) and s₂ = a₉, ℓ₂ = a₁₁ ⊖ a₉ (well-defined since a₉ and a₁₁ share origin and tumbler length, with a₁₁ a T4-valid sibling tumbler regardless of allocation status — the same L4/T12 admissibility used by the three-span variant of the Worked Example below; coverage `⟦(a₉, ℓ₂)⟧ = {t : a₉ ≤ t < a₁₁}`, containing the allocated addresses {a₉, a₁₀}). The m · p = 4 decomposition terms:

- ⟦(s₁, ℓ₁)⟧ ∩ I(β₁) = {a₁, a₂, a₃, a₄, a₅, a₆, a₇} — ordinal offsets 0..6 in β₁ (contiguous by S0 convexity)
- ⟦(s₂, ℓ₂)⟧ ∩ I(β₁) = {a₉, a₁₀} — ordinal offsets 8, 9 in β₁ (contiguous)
- ⟦(s₁, ℓ₁)⟧ ∩ I(β₂) = {a₆, a₇} — ordinal offsets 0, 1 in β₂ (contiguous)
- ⟦(s₂, ℓ₂)⟧ ∩ I(β₂) = {a₉, a₁₀} — ordinal offsets 3, 4 in β₂ (contiguous)

All four terms are non-empty, ruling out mechanism (a). Within β₁ the two terms occupy ordinal offset ranges [0, 6] and [8, 9] — separated by offset 7 (the position of a₈ in β₁), hence non-adjacent (the gap has width 1 ≥ 1) and non-overlapping. Within β₂ the two terms occupy offset ranges [0, 1] and [3, 4] — separated by offset 2 (the position of a₈ in β₂), hence non-adjacent and non-overlapping. Mechanism (b) is ruled out in both blocks. The maximal-fragment count equals the non-empty-term count equals m · p = 4. ✓ The construction satisfies `min_k n_k = 5 ≥ 2m − 1 = 3`, the necessary block-size condition derived in the non-injective small-block case below; it shows the m · p bound is realised at (m, p) = (2, 2) when an overlapping block decomposition admits sufficiently large blocks. We label this witness `W(2, 2)` for catalogue completeness; W(2, 2) is a corner case witnessed standalone here, and the (α_2) lift below starts at W(3, 2) by choice of base — the (α_2) recipe applies at m = 2 as well, but the explicit single-element-span W(3, 2) given immediately below in the three-span variant subsection serves as the lift family's base.

*Three-span variant — (m = 3, p = 2).* The same arrangement supports an (m = 3, p = 2) witness saturating m · p = 6. Retain the ten allocated sibling addresses `a₁ < ... < a₁₀` and the fifteen V-positions `v₁ < ... < v₁₅` with the non-injective M(d)|_{V_{s_C}(d)} above, so the maximally merged block decomposition is unchanged at p = 2 with β₁ = (v₁, a₁, 10) and β₂ = (v₁₁, a₆, 5). Take three single-element spans `e′ = {(a₆, ℓ_a), (a₈, ℓ_b), (a₁₀, ℓ_c)}` with `ℓ_a = a₇ ⊖ a₆` (reach a₇, coverage `[a₆, a₇) = {a₆}`), `ℓ_b = a₉ ⊖ a₈` (reach a₉, coverage `[a₈, a₉) = {a₈}`), and `ℓ_c = a₁₁ ⊖ a₁₀` (reach a₁₁, coverage `[a₁₀, a₁₁) = {a₁₀}`); each ℓ is well-defined by D0 since the span endpoints share origin and tumbler length, and a₁₁ is the same T4-valid unallocated sibling used elsewhere in the note (span well-formedness via T12 does not require allocation of the reach). The m · p = 6 decomposition terms:

- ⟦(a₆, ℓ_a)⟧ ∩ I(β₁) = {a₆} — ordinal offset 5 in β₁
- ⟦(a₈, ℓ_b)⟧ ∩ I(β₁) = {a₈} — ordinal offset 7 in β₁
- ⟦(a₁₀, ℓ_c)⟧ ∩ I(β₁) = {a₁₀} — ordinal offset 9 in β₁
- ⟦(a₆, ℓ_a)⟧ ∩ I(β₂) = {a₆} — ordinal offset 0 in β₂
- ⟦(a₈, ℓ_b)⟧ ∩ I(β₂) = {a₈} — ordinal offset 2 in β₂
- ⟦(a₁₀, ℓ_c)⟧ ∩ I(β₂) = {a₁₀} — ordinal offset 4 in β₂

All six terms are non-empty, ruling out mechanism (a). Within β₁ the three terms sit at ordinal offsets {5, 7, 9} — pairwise separated by offset gaps of width 1 (offsets 6 and 8, the positions of a₇ and a₉ in β₁), hence pairwise non-adjacent and (as singletons at distinct offsets) non-overlapping. Within β₂ the three terms sit at ordinal offsets {0, 2, 4} — pairwise separated by offset gaps of width 1 (offsets 1 and 3, the positions of a₇ and a₉ in β₂), hence pairwise non-adjacent and non-overlapping. Mechanism (b) is ruled out in both blocks. The maximal-fragment count equals the non-empty-term count equals m · p = 6. ✓ The construction satisfies the block-size threshold `min_k n_k = 5 ≥ 2m − 1 = 5` with equality at m = 3 — β₂ is tight at the small-block barrier, realising attainment at the structural boundary established by the non-injective small-block case below. This witness serves as the explicit base case `W(3, 2)` for the *p = 2 boundary shape* defined in the inductive lift schema below, from which all `(m ≥ 4, p = 2)` witnesses are produced by iterated application of the extended boundary lift `(α_2): W(m, 2) → W(m + 1, 2)` — that lift is the parameter-change recipe of (α) applied to the (m, p = 2) shape and is verified in full at the same level of rigor as the (α) and (β) lifts for the (m ≥ 3, p ≥ 3) regime.

*Three-block witness — (m = 2, p = 3).* The (m, p) = (2, 2) construction generalises to higher p by nesting additional blocks within the existing ones. We exhibit a concrete (m = 2, p = 3) witness saturating m · p = 6. Take eleven sibling tumblers `a₁ < a₂ < ... < a₁₁` at the common content-subspace depth — T4-valid, same origin, same tumbler length, sequential under OrdinalShiftBase — with `a₁..a₁₀` allocated by K.α steps (paralleling the (m ≥ 2, p = 2) witness above) and `a₁₁` an unallocated T4-valid sibling tumbler retained in T for span well-formedness (the same L4/T12 admissibility used by the three-span variant of the Worked Example below). Take V-positions `v₁ < v₂ < ... < v₁₈` sequential under OrdinalShiftBase at the content-subspace common depth m_C, with arrangement

`M(d)|_{V_{s_C}(d)} = {v_i ↦ a_i : 1 ≤ i ≤ 10} ∪ {v_{10+i} ↦ a_{5+i} : 1 ≤ i ≤ 5} ∪ {v_{15+i} ↦ a_{7+i} : 1 ≤ i ≤ 3}`

— i.e., v₁..v₁₀ ↦ a₁..a₁₀ (the base block), v₁₁..v₁₅ ↦ a₆..a₁₀ (a first re-mapping covering the tail half of the base), and v₁₆..v₁₈ ↦ a₈..a₁₀ (a second re-mapping nested further into the tail). This is a non-injective arrangement admitted by S5 (UnrestrictedSharing, ASN-0036): a₈, a₉, a₁₀ are each shared across all three block ranges, and a₆, a₇ across the first two. The maximally merged block decomposition (C1a, ASN-0058) has p = 3 blocks:

- β₁ = (v₁, a₁, 10) with I-extent {a₁, ..., a₁₀}
- β₂ = (v₁₁, a₆, 5) with I-extent {a₆, ..., a₁₀}
- β₃ = (v₁₆, a₈, 3) with I-extent {a₈, ..., a₁₀}

Both inter-block boundaries fail M7 (BlockMergeCondition, ASN-0058): at v₁₀ → v₁₁, V-adjacency holds (v₁₁ is the V-position immediately after v₁₀), but the I-adjacency requirement a₁₀ + 1 = a₁₁ ≠ a₆ is violated; at v₁₅ → v₁₆, V-adjacency holds, but a₁₀ + 1 = a₁₁ ≠ a₈. M12's split rule therefore forces both block boundaries. Within each block, M7 holds internally (sequential V-positions paired with ordinal-incremented I-images), so each block is itself maximal. The three blocks pairwise overlap in I-extent: β₃ ⊆ β₂ ⊆ β₁, so β₁ ∩ β₂ = β₂, β₂ ∩ β₃ = β₃, and β₁ ∩ β₃ = β₃ — all non-empty. The block-size threshold `min_k n_k = 3 ≥ 2m − 1 = 3` is satisfied with equality (β₃ is tight at the small-block barrier).

Take the two-span endset `e = {(a₁, ℓ₁), (a₁₀, ℓ₂)}` with `ℓ₁ = a₉ ⊖ a₁` (well-defined by D0; reach a₉; coverage `⟦(a₁, ℓ₁)⟧ = [a₁, a₉)`, intersecting the allocated addresses at `{a₁, ..., a₈}`) and `ℓ₂ = a₁₁ ⊖ a₁₀` (well-defined by D0 since a₁₀ and a₁₁ share origin and tumbler length, with a₁₁ a T4-valid sibling tumbler regardless of allocation status; reach a₁₁; coverage `⟦(a₁₀, ℓ₂)⟧ = [a₁₀, a₁₁) = {a₁₀}`). The m · p = 6 decomposition terms:

- ⟦(a₁, ℓ₁)⟧ ∩ I(β₁) = {a₁, ..., a₈} — ordinal offsets 0..7 in β₁
- ⟦(a₁, ℓ₁)⟧ ∩ I(β₂) = {a₆, a₇, a₈} — ordinal offsets 0..2 in β₂
- ⟦(a₁, ℓ₁)⟧ ∩ I(β₃) = {a₈} — ordinal offset 0 in β₃
- ⟦(a₁₀, ℓ₂)⟧ ∩ I(β₁) = {a₁₀} — ordinal offset 9 in β₁
- ⟦(a₁₀, ℓ₂)⟧ ∩ I(β₂) = {a₁₀} — ordinal offset 4 in β₂
- ⟦(a₁₀, ℓ₂)⟧ ∩ I(β₃) = {a₁₀} — ordinal offset 2 in β₃

All six terms are non-empty, ruling out mechanism (a). Within each block, the two terms (span 1's contribution at the "front" offsets, span 2's at the "tail" offset) are separated by an unmapped offset corresponding to a₉ — span 1's coverage ends just before a₉, and span 2's coverage starts at a₁₀, so the offset of a₉ within each block sits between them as a width-1 gap: in β₁ at offset 8, in β₂ at offset 3, in β₃ at offset 1. Each gap of width 1 satisfies the within-block non-adjacency requirement, and the two contributions within each block are pairwise non-overlapping by span-coverage disjointness (`⟦(a₁, ℓ₁)⟧ ∩ ⟦(a₁₀, ℓ₂)⟧ = [a₁, a₉) ∩ [a₁₀, a₁₁) = ∅`). Mechanism (b) is ruled out in every block. The maximal-fragment count equals the non-empty-term count equals m · p = 6. ✓

*Generalisation to (m = 2, p ≥ 4).* The (m = 2, p = 3) witness above serves as the explicit base case `W(2, 3)` for the *m = 2 boundary shape* defined in the inductive lift schema below, from which all `(m = 2, p ≥ 4)` witnesses are produced by iterated application of the extended boundary lift `(β_2): W(2, p) → W(2, p + 1)`. That lift is the parameter-change recipe of (β) applied to the (m = 2, p) shape — add one further nested block with I-extent identical to the smallest existing block (`{a₈, a₉, a₁₀}` of size 3) and a fresh V-extent (admitted by S5 as within-document I-address sharing across blocks), leaving the two spans and the allocated/unallocated siblings unchanged — and is verified in full at the same level of rigor as the (α) and (β) lifts for the (m ≥ 3, p ≥ 3) regime.

*Three-block witness — (m = 3, p = 3).* The (m, p) = (2, 3) nesting construction generalises to higher m with the same p by enlarging the smallest block to `min_k n_k ≥ 2m − 1`. We exhibit a concrete (m = 3, p = 3) witness saturating m · p = 9. Take ten sibling tumblers `a₁ < a₂ < ... < a₁₀` at the common content-subspace depth — T4-valid, same origin, same tumbler length, sequential under OrdinalShiftBase — with `a₁..a₉` allocated by K.α steps (paralleling the (m, p) = (2, 3) witness above) and `a₁₀` an unallocated T4-valid sibling retained in T for span well-formedness (the same L4/T12 admissibility used by the three-span variant of the Worked Example below). Take V-positions `v₁ < v₂ < ... < v₂₁` sequential under OrdinalShiftBase at the content-subspace common depth m_C, with arrangement

`M(d)|_{V_{s_C}(d)} = {v_i ↦ a_i : 1 ≤ i ≤ 9} ∪ {v_{9+i} ↦ a_{2+i} : 1 ≤ i ≤ 7} ∪ {v_{16+i} ↦ a_{4+i} : 1 ≤ i ≤ 5}`

— i.e., v₁..v₉ ↦ a₁..a₉ (the base block, size 9), v₁₀..v₁₆ ↦ a₃..a₉ (a first re-mapping covering a₃ through a₉, size 7), and v₁₇..v₂₁ ↦ a₅..a₉ (a second re-mapping nested further into the tail, size 5). This is a non-injective arrangement admitted by S5 (UnrestrictedSharing, ASN-0036): a₃..a₉ are shared across the first two block ranges, and a₅..a₉ across all three. The maximally merged block decomposition (C1a, ASN-0058) has p = 3 blocks:

- β₁ = (v₁, a₁, 9) with I-extent {a₁, ..., a₉}
- β₂ = (v₁₀, a₃, 7) with I-extent {a₃, ..., a₉}
- β₃ = (v₁₇, a₅, 5) with I-extent {a₅, ..., a₉}

Both inter-block boundaries fail M7 (BlockMergeCondition, ASN-0058): at v₉ → v₁₀, V-adjacency holds (v₁₀ is the V-position immediately after v₉), but I-adjacency requires a₉ + 1 = a₁₀ ≠ a₃; at v₁₆ → v₁₇, V-adjacency holds, but a₉ + 1 = a₁₀ ≠ a₅. M12's split rule therefore forces both block boundaries. Within each block, M7 holds internally (sequential V-positions paired with ordinal-incremented I-images), so each block is itself maximal. The three blocks pairwise overlap in I-extent: β₃ ⊆ β₂ ⊆ β₁, so β₁ ∩ β₂ = β₂, β₂ ∩ β₃ = β₃, and β₁ ∩ β₃ = β₃ — all non-empty. The block-size threshold `min_k n_k = 5 ≥ 2m − 1 = 5` is satisfied with equality at m = 3 (β₃ is tight at the small-block barrier).

Take the three-span endset `e = {(a₅, ℓ₁), (a₇, ℓ₂), (a₉, ℓ₃)}` with `ℓ₁ = a₆ ⊖ a₅` (reach a₆; coverage `⟦(a₅, ℓ₁)⟧ = [a₅, a₆) = {a₅}`), `ℓ₂ = a₈ ⊖ a₇` (reach a₈; coverage `⟦(a₇, ℓ₂)⟧ = [a₇, a₈) = {a₇}`), and `ℓ₃ = a₁₀ ⊖ a₉` (reach a₁₀; coverage `⟦(a₉, ℓ₃)⟧ = [a₉, a₁₀) = {a₉}`); each ℓ is well-defined by D0 (same origin and tumbler length), and a₁₀ is the same T4-valid unallocated sibling used elsewhere in the note (span well-formedness via T12 does not require allocation of the reach). The m · p = 9 decomposition terms:

- ⟦(a₅, ℓ₁)⟧ ∩ I(β₁) = {a₅} — ordinal offset 4 in β₁
- ⟦(a₇, ℓ₂)⟧ ∩ I(β₁) = {a₇} — ordinal offset 6 in β₁
- ⟦(a₉, ℓ₃)⟧ ∩ I(β₁) = {a₉} — ordinal offset 8 in β₁
- ⟦(a₅, ℓ₁)⟧ ∩ I(β₂) = {a₅} — ordinal offset 2 in β₂
- ⟦(a₇, ℓ₂)⟧ ∩ I(β₂) = {a₇} — ordinal offset 4 in β₂
- ⟦(a₉, ℓ₃)⟧ ∩ I(β₂) = {a₉} — ordinal offset 6 in β₂
- ⟦(a₅, ℓ₁)⟧ ∩ I(β₃) = {a₅} — ordinal offset 0 in β₃
- ⟦(a₇, ℓ₂)⟧ ∩ I(β₃) = {a₇} — ordinal offset 2 in β₃
- ⟦(a₉, ℓ₃)⟧ ∩ I(β₃) = {a₉} — ordinal offset 4 in β₃

All nine terms are non-empty, ruling out mechanism (a). Within β₁ the three single-element terms sit at ordinal offsets {4, 6, 8} — pairwise separated by gap-width 1 (the offsets carrying a₆ at 5 and a₈ at 7, which lie outside the three single-element spans), hence pairwise non-adjacent and (as singletons at distinct offsets) non-overlapping. Within β₂ the three terms sit at offsets {2, 4, 6} — pairwise separated by gap-width 1 (offsets 3 and 5, carrying a₆ and a₈), hence pairwise non-adjacent and non-overlapping. Within β₃ the three terms sit at offsets {0, 2, 4} — pairwise separated by gap-width 1 (offsets 1 and 3, carrying a₆ and a₈), hence pairwise non-adjacent and non-overlapping. Mechanism (b) is ruled out in every block. The maximal-fragment count equals the non-empty-term count equals m · p = 9. ✓

*Higher witness — (m = 4, p = 3).* The (m = 3, p = 3) construction generalises directly to higher m by enlarging the nested blocks to size ≥ 2m − 1. We exhibit a concrete (m = 4, p = 3) witness saturating m · p = 12. Take twelve sibling tumblers `a₁ < a₂ < ... < a₁₂` at the common content-subspace depth — T4-valid, same origin, same tumbler length, sequential under OrdinalShiftBase — with `a₁..a₁₁` allocated by K.α steps (paralleling the (m, p) = (3, 3) witness above) and `a₁₂` an unallocated T4-valid sibling retained in T for span well-formedness (the same L4/T12 admissibility used elsewhere in the note). Take V-positions `v₁ < v₂ < ... < v₂₇` sequential under OrdinalShiftBase at the content-subspace common depth m_C, with arrangement

`M(d)|_{V_{s_C}(d)} = {v_i ↦ a_i : 1 ≤ i ≤ 11} ∪ {v_{11+i} ↦ a_{2+i} : 1 ≤ i ≤ 9} ∪ {v_{20+i} ↦ a_{4+i} : 1 ≤ i ≤ 7}`

— i.e., v₁..v₁₁ ↦ a₁..a₁₁ (the base block, size 11), v₁₂..v₂₀ ↦ a₃..a₁₁ (a first re-mapping covering a₃ through a₁₁, size 9), and v₂₁..v₂₇ ↦ a₅..a₁₁ (a second re-mapping nested further into the tail, size 7). This is a non-injective arrangement admitted by S5 (UnrestrictedSharing, ASN-0036): a₃..a₁₁ are shared across the first two block ranges, and a₅..a₁₁ across all three. The maximally merged block decomposition (C1a, ASN-0058) has p = 3 blocks:

- β₁ = (v₁, a₁, 11) with I-extent {a₁, ..., a₁₁}
- β₂ = (v₁₂, a₃, 9) with I-extent {a₃, ..., a₁₁}
- β₃ = (v₂₁, a₅, 7) with I-extent {a₅, ..., a₁₁}

Both inter-block boundaries fail M7 (BlockMergeCondition, ASN-0058): at v₁₁ → v₁₂, V-adjacency holds (v₁₂ is the V-position immediately after v₁₁), but I-adjacency requires a₁₁ + 1 = a₁₂ ≠ a₃; at v₂₀ → v₂₁, V-adjacency holds, but a₁₁ + 1 = a₁₂ ≠ a₅. M12's split rule therefore forces both block boundaries. Within each block, M7 holds internally (sequential V-positions paired with ordinal-incremented I-images), so each block is itself maximal. The three blocks pairwise overlap in I-extent: β₃ ⊆ β₂ ⊆ β₁, so β₁ ∩ β₂ = β₂, β₂ ∩ β₃ = β₃, and β₁ ∩ β₃ = β₃ — all non-empty. The block-size threshold `min_k n_k = 7 ≥ 2m − 1 = 7` is satisfied with equality at m = 4 (β₃ is tight at the small-block barrier).

Take the four-span endset `e = {(a₅, ℓ₁), (a₇, ℓ₂), (a₉, ℓ₃), (a₁₁, ℓ₄)}` with `ℓ₁ = a₆ ⊖ a₅` (reach a₆; coverage `⟦(a₅, ℓ₁)⟧ = [a₅, a₆) = {a₅}`), `ℓ₂ = a₈ ⊖ a₇` (reach a₈; coverage `[a₇, a₈) = {a₇}`), `ℓ₃ = a₁₀ ⊖ a₉` (reach a₁₀; coverage `[a₉, a₁₀) = {a₉}`), and `ℓ₄ = a₁₂ ⊖ a₁₁` (reach a₁₂; coverage `[a₁₁, a₁₂) = {a₁₁}`); each ℓ is well-defined by D0 (same origin and tumbler length), and a₁₂ supplies span well-formedness for the fourth span without requiring allocation (span well-formedness via T12 does not require allocation of the reach). The m · p = 12 decomposition terms:

- ⟦(a₅, ℓ₁)⟧ ∩ I(β₁) = {a₅} — ordinal offset 4 in β₁
- ⟦(a₇, ℓ₂)⟧ ∩ I(β₁) = {a₇} — ordinal offset 6 in β₁
- ⟦(a₉, ℓ₃)⟧ ∩ I(β₁) = {a₉} — ordinal offset 8 in β₁
- ⟦(a₁₁, ℓ₄)⟧ ∩ I(β₁) = {a₁₁} — ordinal offset 10 in β₁
- ⟦(a₅, ℓ₁)⟧ ∩ I(β₂) = {a₅} — ordinal offset 2 in β₂
- ⟦(a₇, ℓ₂)⟧ ∩ I(β₂) = {a₇} — ordinal offset 4 in β₂
- ⟦(a₉, ℓ₃)⟧ ∩ I(β₂) = {a₉} — ordinal offset 6 in β₂
- ⟦(a₁₁, ℓ₄)⟧ ∩ I(β₂) = {a₁₁} — ordinal offset 8 in β₂
- ⟦(a₅, ℓ₁)⟧ ∩ I(β₃) = {a₅} — ordinal offset 0 in β₃
- ⟦(a₇, ℓ₂)⟧ ∩ I(β₃) = {a₇} — ordinal offset 2 in β₃
- ⟦(a₉, ℓ₃)⟧ ∩ I(β₃) = {a₉} — ordinal offset 4 in β₃
- ⟦(a₁₁, ℓ₄)⟧ ∩ I(β₃) = {a₁₁} — ordinal offset 6 in β₃

All twelve terms are non-empty, ruling out mechanism (a). Within β₁ the four single-element terms sit at ordinal offsets {4, 6, 8, 10} — pairwise separated by gap-width 1 (the intervening offsets 5, 7, 9 carry a₆, a₈, a₁₀, none of which carry the spans), hence pairwise non-adjacent and (as singletons at distinct offsets) non-overlapping. Within β₂ the four terms sit at offsets {2, 4, 6, 8} — pairwise separated by gap-width 1 (offsets 3, 5, 7 carrying a₆, a₈, a₁₀), hence pairwise non-adjacent and non-overlapping. Within β₃ the four terms sit at offsets {0, 2, 4, 6} — pairwise separated by gap-width 1 (offsets 1, 3, 5 carrying a₆, a₈, a₁₀), hence pairwise non-adjacent and non-overlapping. Mechanism (b) is ruled out in every block. The maximal-fragment count equals the non-empty-term count equals m · p = 12. ✓

*Inductive lift schema for (m ≥ 3, p ≥ 3) attainment.* The explicit (m = 3, p = 3) and (m = 4, p = 3) witnesses above are instances of a uniform construction family `W(m, p)` from which all (m ≥ 3, p ≥ 3) witnesses are produced by two lift schemata applied finitely many times. Each lift is a *witness-construction* lemma — exhibiting a state, an arrangement, and an endset that saturate the new (m, p) — rather than describing an elementary transition from the existing state; the lifted state is reached from Σ₀ by an independent ValidCompositeExtended chain paralleling the chains used for the explicit witnesses above (K.δ for the document and prefix entities, K.α for each allocated I-address, K.μ⁺ for V→I mappings, K.ρ where required by J1★), with only the parameter values (number of allocated siblings, number of V-positions, span endpoints) differing between (m, p) and (m + 1, p) or (m, p + 1).

*Reading the lift recipes below.* Each lift recipe — (α), (β), (α_2), (β_2) — describes the *target* witness (W(m + 1, p) for (α) and (α_2), W(m, p + 1) for (β) and (β_2)) by stating its *parameter delta* from the source witness W(m, p). The phrasings "extend each block's I-extent by 2 at the tail" and "V-side extensions follow the established arrangement pattern (each block's V-extent grows at the tail by two V-positions immediately after its previous end, under S8a/D-CTG)" appearing in the recipes below are *target-state parameter descriptions relative to the source-state parameters as a convenient diff*; they do *not* describe an in-place state modification of the source W(m, p)'s state. The lifted state is constructed *de novo from Σ₀* by an independent ValidCompositeExtended chain following the framing-note template above, with the target witness's parameters substituted: each I-address allocated by a fresh K.α step at the next ordinal position past the previous (T0(a), unallocated-sibling chain; T4-validity preserved by TA5a); each V-position added by a fresh K.μ⁺ step from D-MIN upward at the established common depth m_C in subspace s_C, ordered after previously-added V-positions per D-CTG/D-MIN; spans constructed from the target-witness siblings; K.ρ steps included where required by J1★. In particular, the V-arrangement of the target witness is laid out de novo as a single contiguous V-sequence from D-MIN to the maximum (satisfying D-SEQ globally on V_{s_C}(d) in the target state), with each block's V-extent ordered immediately after the prior block's V-extent in that target sequence; no V-positions are inserted between the V-extents of pre-existing source-state blocks (an operation D-SEQ does not admit on the source state). The recipe verifications that follow each lift — block boundaries, span offsets, threshold satisfaction, non-adjacency and non-overlap of decomposition terms — are properties of the target state's parameters, well-defined irrespective of the prose's "extend"/"grow" wording.

*Lemma (LiftSchema).* For W(m, p) satisfying the SV11 attainment biconditional with block sizes (n_1, ..., n_p) and m single-element spans (each span's coverage a singleton `{a_{2j+3}}` deposited at a step-2 arithmetic-progression offset in each block, with `min_k n_k ≥ 2m − 1` tight), each of the four lift recipes produces a target witness W(m', p') satisfying the same attainment biconditional under a single parameter delta:
- *(α)* at m ≥ 3, p ≥ 3: produces W(m + 1, p) by allocating two further T4-valid siblings (`a_{2m+4}` and `a_{2m+5}`), extending each block's I-extent by 2 at the tail (so β₁ grows from size `2m + 3` to `2m + 5`, and so on), and adding one new single-element span `(a_{2m+5}, a_{2m+6} ⊖ a_{2m+5})`.
- *(β)* at m ≥ 3, p ≥ 3: produces W(m, p + 1) by adding one further block β_{p+1} with fresh V-extent and I-extent identical to β_p (within-document I-address sharing under S5), leaving siblings and spans unchanged.
- *(α_2)* at m ≥ 3, p = 2: same parameter delta as (α) but applied to the W(m, 2) shape with two blocks (β₁ at size `2m + 4`, β₂ at size `2m − 1`); produces W(m + 1, 2).
- *(β_2)* at m = 2, p ≥ 3: same parameter delta as (β) but applied to the W(2, p) shape; produces W(2, p + 1).

Preservation of the biconditional follows in each case because the parameter delta extends the existing step-2 arithmetic-progression of span-offsets by one further span-offset (in the (α)/(α_2) cases) or replicates the target block's span-offset structure into a fresh block under S5 (in the (β)/(β_2) cases), so every (j', k') pair in the target witness yields a singleton non-empty term at a distinct offset gap-width-1 from its neighbours within each block, the block-size threshold `min_k n_k ≥ 2m' − 1` is satisfied at the lifted m', and pairwise block overlap is preserved by the maintained nesting `β_p ⊆ β_{p−1} ⊆ ... ⊆ β₂ ⊆ β₁`. The lift schemata's per-recipe verifications below discharge each clause of this lemma explicitly.

*Witness shape `W(m, p)`.* For m ≥ 3, p ≥ 3, the construction is characterised by:

- *Siblings.* Allocated T4-valid siblings `a₁ < a₂ < ... < a_{2m+3}` at one tumbler length and one origin (well-formedness at each new ordinal position supplied by T0(a), UnboundedComponentValues, ASN-0034), and one further unallocated T4-valid sibling `a_{2m+4}` retained in T by T0(a) for span well-formedness.
- *Blocks.* p blocks `β₁, β₂, β₃, β₄, ..., β_p` of the form:
  - β₁: I-extent `{a₁, ..., a_{2m+3}}`, size `2m + 3`
  - β₂: I-extent `{a₃, ..., a_{2m+3}}`, size `2m + 1`
  - β_k for `3 ≤ k ≤ p`: I-extent `{a₅, ..., a_{2m+3}}`, size `2m − 1`
  
  Each block has a distinct fresh V-extent in the arrangement; β₃, β₄, ..., β_p share a common I-extent but inhabit distinct V-arenas (admitted by S5, UnrestrictedSharing, as within-document I-address sharing across blocks). The inter-block boundary v_{|β_k|-end} → v_{|β_{k+1}|-start} fails I-adjacency at every k: β₁ → β₂ requires `a_{2m+3} + 1 = a_{2m+4} = a₃` (false for m ≥ 3), β₂ → β₃ requires `a_{2m+4} = a₅` (false for m ≥ 3), β_k → β_{k+1} for k ≥ 3 requires `a_{2m+4} = a₅` (false for m ≥ 3); M12 (CanonicalUniqueness, ASN-0058) forces each split.
- *Spans.* m single-element spans `e = {(a_{2j+3}, a_{2j+4} ⊖ a_{2j+3}) : j = 1, ..., m}` with coverages `{a_{2j+3}}` for j = 1, ..., m. Each `ℓ_j = a_{2j+4} ⊖ a_{2j+3}` is D0-well-defined (a_{2j+3} and a_{2j+4} share origin and tumbler length, divergence at position `#a_{2j+3}`).

*Saturation verification for `W(m, p)`.* Each span deposits a single-element term in every block at an offset determined by the block's base:
- In β₁ (base `a₁`): span j contributes `{a_{2j+3}}` at offset `2j + 2`, so the m span-offsets in β₁ are `{4, 6, ..., 2m + 2}`.
- In β₂ (base `a₃`): span j contributes `{a_{2j+3}}` at offset `2j`, so the m span-offsets in β₂ are `{2, 4, ..., 2m}`.
- In β_k for k ≥ 3 (base `a₅`): span j contributes `{a_{2j+3}}` at offset `2j − 2`, so the m span-offsets in β_k are `{0, 2, ..., 2m − 2}`.

In every block the m span-offsets form an arithmetic progression of step 2, pairwise separated by gap-width 1 (the intervening odd offsets carry the even-indexed siblings a₆, a₈, ..., a_{2m+2}, none of which are span endpoints). All m · p decomposition terms are non-empty (each a singleton) and pairwise non-adjacent within each block. Pairwise block overlap holds by nested I-extents: β_p ⊆ β_{p−1} ⊆ ... ⊆ β₃ ⊆ β₂ ⊆ β₁, with β₃ = β₄ = ... = β_p in I-extent. The smallest block size `n_p = 2m − 1` is tight at the threshold derived in the non-injective small-block case below.

*(α) Lift `W(m, p) → W(m + 1, p)`.* Parameter changes:
- Allocate `a_{2m+4}` (formerly the unallocated reach) and a new T4-valid sibling `a_{2m+5}` via K.α — both supplied by T0(a) at the established tumbler length.
- Retain `a_{2m+6}` unallocated in T as the new reach (T0(a) again).
- Extend each block's I-extent by 2 at the tail, mapping fresh V-positions to `a_{2m+4}` and `a_{2m+5}`: β₁ now `{a₁..a_{2m+5}}` size `2m + 5`, β₂ now `{a₃..a_{2m+5}}` size `2m + 3`, β_k for k ≥ 3 now `{a₅..a_{2m+5}}` size `2m + 1`. V-side extensions follow the established arrangement pattern (each block's V-extent grows at the tail by two V-positions immediately after its previous end, under S8a/D-CTG); I-side re-mappings are admitted by S5 (the new I-addresses are shared across all nested blocks just as a₅..a_{2m+3} were shared in the original W(m, p)).
- Add one new single-element span `(a_{2m+5}, a_{2m+6} ⊖ a_{2m+5})` with coverage `{a_{2m+5}}` — the (m + 1)-th odd-indexed sibling endpoint, paralleling the existing m spans.

In the lifted witness, the new span occupies offset `2m + 4` in β₁ (two ordinal positions past the previous-largest span offset `2m + 2`), offset `2m + 2` in β₂, and offset `2m` in β_k for k ≥ 3. The new offsets continue the same even-arithmetic-progression structure of step 2; pairwise gap-width-1 non-adjacency continues with `a_{2m+4}` filling the intervening odd offset between the previous-largest span and the new span. The smallest block is now `n_p = 2m + 1 = 2(m + 1) − 1`, satisfying the threshold at m + 1. All (m + 1) · p decomposition terms remain non-empty singletons and pairwise non-adjacent within each block; pairwise block overlap is preserved by the maintained nesting (each block extended at the same tail by the same two addresses). The lifted witness saturates `(m + 1) · p`.

*(β) Lift `W(m, p) → W(m, p + 1)`.* Parameter changes:
- Add one further nested block β_{p+1} with a fresh V-extent (V-positions v_{V+1}, ..., v_{V+(2m−1)} where V is the previous V-domain size, extending the V-arena past W(m, p)'s end) and I-extent identical to β_p: `{a₅, ..., a_{2m+3}}` of size `2m − 1` (a within-document copy of β_p's I-extent, admitted by S5 as within-document I-address sharing across blocks).
- All other parameters unchanged: same siblings a₁..a_{2m+4}, same blocks β₁..β_p, same m spans.

The inter-block boundary v_V → v_{V+1} between β_p and β_{p+1} satisfies V-adjacency (β_{p+1}'s V-extent immediately follows β_p's in the V-arena) and fails I-adjacency: β_p's last I-image is `a_{2m+3}`, β_{p+1}'s first I-image is `a₅` (same as β_p's first, since I-extents coincide), and I-adjacency would require `a_{2m+3} + 1 = a_{2m+4} = a₅`, false for m ≥ 3. M12 splits β_p and β_{p+1} into distinct blocks despite the I-extent coincidence. Each span deposits the same single-element term in β_{p+1} as in β_p (same I-extent, same span coverages), so the m · (p + 1) decomposition terms are all non-empty singletons with the same within-block offset structure as β_p (offsets `{0, 2, ..., 2m − 2}`, pairwise gap-width-1 non-adjacent). The smallest block size remains `n_p = 2m − 1`, satisfying the threshold. Pairwise block overlap holds for every (β_k, β_{p+1}) pair by nested I-extents (β_{p+1} ⊆ β_p ⊆ β_k, with the first inclusion reflexive for k = p and proper for k < p). The lifted witness saturates `m · (p + 1)`.

*Boundary witness shapes for the p = 2 and m = 2 regions.* The W(m, p) family above is defined uniformly for m ≥ 3, p ≥ 3. The two boundary regions (m ≥ 3, p = 2) and (m = 2, p ≥ 3) admit analogous shape specifications with the same lift recipe and the same SV11 attainment-condition preservation, differing only in the parameter values fixed at the boundary. The remaining single point (m = 2, p = 2) is the corner case W(2, 2) defined explicitly in the multi-block (p ≥ 2) attainment witness above; it is not the base of any lift family — (α_2) below starts at W(3, 2) and (β_2) below starts at W(2, 3) — and so admits no shape specification under the lift schema.

- *Witness shape `W(m, 2)` for m ≥ 3.* Allocated T4-valid siblings `a₁ < a₂ < ... < a_{2m+4}` at one tumbler length and one origin (T0(a), ASN-0034), and one further unallocated T4-valid sibling `a_{2m+5}` retained in T for span well-formedness. Two blocks:
  - β₁: I-extent `{a₁, ..., a_{2m+4}}`, size `2m + 4`
  - β₂: I-extent `{a₆, ..., a_{2m+4}}`, size `2m − 1`
  
  Each block has a distinct fresh V-extent in the arrangement; β₂'s I-extent is the strict tail subset of β₁'s I-extent admitted by S5 (UnrestrictedSharing) as within-document I-address sharing across blocks. The inter-block boundary fails I-adjacency: β₁ → β₂ requires `a_{2m+4} + 1 = a_{2m+5} = a₆`, false for m ≥ 3; M12 forces the split. *Spans:* m single-element spans `e = {(a_{2k+4}, a_{2k+5} ⊖ a_{2k+4}) : k = 1, ..., m}` with coverages `{a_{2k+4}}`; each ℓ_k is D0-well-defined. *Saturation verification:* span k contributes `{a_{2k+4}}` at offset `2k + 3` in β₁ (base a₁) and at offset `2k − 2` in β₂ (base a₆), so the m span-offsets are `{5, 7, ..., 2m + 3}` in β₁ and `{0, 2, ..., 2m − 2}` in β₂. In both blocks the m span-offsets form an arithmetic progression of step 2; the intervening offset between consecutive span offsets carries the non-span-endpoint sibling a_{2k+5} for k = 1..m−1 (a₇ between a₆ and a₈, a₉ between a₈ and a₁₀, etc.) — none of which are span endpoints — supplying pairwise gap-width-1 non-adjacency. So all m · 2 = 2m terms are non-empty singletons and pairwise gap-width-1 non-adjacent within each block. Pairwise block overlap holds by `β₂ ⊆ β₁`. The smallest block size `n_2 = 2m − 1` is tight at the threshold. The explicit (m = 3, p = 2) three-span variant above is the W(3, 2) instance.

- *Witness shape `W(2, p)` for p ≥ 3.* Allocated T4-valid siblings `a₁ < a₂ < ... < a₁₀` at one tumbler length and one origin (T0(a)), and one further unallocated T4-valid sibling `a₁₁` retained in T for span well-formedness. p blocks:
  - β₁: I-extent `{a₁, ..., a₁₀}`, size 10
  - β₂: I-extent `{a₆, ..., a₁₀}`, size 5
  - β_k for `3 ≤ k ≤ p`: I-extent `{a₈, a₉, a₁₀}`, size 3
  
  Each block has a distinct fresh V-extent; β₃, β₄, ..., β_p share a common I-extent `{a₈, a₉, a₁₀}` but inhabit distinct V-arenas (admitted by S5 as within-document I-address sharing). The inter-block boundaries fail I-adjacency at every k: β₁ → β₂ requires `a₁₀ + 1 = a₁₁ = a₆` (false), β₂ → β₃ requires `a₁₁ = a₈` (false), β_k → β_{k+1} for k ≥ 3 requires `a₁₁ = a₈` (false); M12 forces each split. *Spans:* two spans `e = {(a₁, a₉ ⊖ a₁), (a₁₀, a₁₁ ⊖ a₁₀)}` with coverages `[a₁, a₉) = {a₁..a₈}` and `[a₁₀, a₁₁) = {a₁₀}` (each ℓ is D0-well-defined). *Saturation verification:* in each block, span 1's contribution sits at the leading offsets (a₁..a₈ where present in the block's I-extent) and span 2's contribution sits at the trailing single offset (a₁₀); the gap at a₉ (carried by the within-block offset between the two contributions) supplies pairwise non-adjacency by gap-width 1 (verified in detail for β₁, β₂, β₃ in the (m = 2, p = 3) witness above; the same offset structure holds for β_k at k ≥ 4 by identical I-extent). All 2 · p decomposition terms are non-empty and pairwise gap-width-1 non-adjacent within each block; the two contributions are non-overlapping by span-coverage disjointness (`[a₁, a₉) ∩ [a₁₀, a₁₁) = ∅`). Pairwise block overlap holds by nested I-extents (β_p ⊆ ... ⊆ β₃ ⊆ β₂ ⊆ β₁, with β₃ = β₄ = ... = β_p in I-extent). The smallest block size remains `n_p = 3 = 2m − 1` at m = 2, tight at the threshold. The explicit (m = 2, p = 3) three-block witness above is the W(2, 3) instance.

*(α_2) Boundary lift `W(m, 2) → W(m + 1, 2)` for m ≥ 3.* Parameter changes:
- Allocate `a_{2m+5}` (formerly the unallocated reach) and a new T4-valid sibling `a_{2m+6}` via K.α (both supplied by T0(a) at the established tumbler length).
- Retain `a_{2m+7}` unallocated in T as the new reach (T0(a) again).
- Extend each block's I-extent by 2 at the tail: β₁ now `{a₁..a_{2m+6}}` size `2m + 6 = 2(m+1) + 4`, β₂ now `{a₆..a_{2m+6}}` size `2m + 1 = 2(m+1) − 1`. V-side extensions follow the standing arrangement pattern (each block's V-extent grows at the tail by two V-positions, under S8a/D-CTG); I-side re-mappings are admitted by S5 (the new I-addresses are shared across both blocks just as a₆..a_{2m+4} were shared in the source W(m, 2)).
- Add one new single-element span `(a_{2m+6}, a_{2m+7} ⊖ a_{2m+6})` with coverage `{a_{2m+6}}` — the (m + 1)-th even-indexed-in-β₂ sibling endpoint, paralleling the existing m spans.

In the lifted witness, the new span occupies offset `2m + 5` in β₁ (two ordinal positions past the previous-largest span offset `2m + 3`) and offset `2m` in β₂ (two ordinal positions past the previous-largest β₂-offset `2m − 2`). The new offsets continue the same step-2 arithmetic-progression structure; pairwise gap-width-1 non-adjacency continues with the newly-allocated `a_{2m+5}` (the formerly-unallocated reach, now allocated as a non-span-endpoint sibling) filling the single intervening offset between the previous-largest span and the new span in both blocks. The smallest block is now `n_2 = 2m + 1 = 2(m + 1) − 1`, satisfying the threshold at m + 1. All `(m + 1) · 2` decomposition terms remain non-empty singletons and pairwise non-adjacent within each block; pairwise block overlap is preserved by the maintained nesting `β₂ ⊆ β₁`. The lifted witness saturates `(m + 1) · 2`. The (α_2) lift is the (α) recipe instantiated at p = 2 with the appropriate base-shape relabelling — same per-step structural verification, same SV11 attainment-condition preservation.

*(β_2) Boundary lift `W(2, p) → W(2, p + 1)` for p ≥ 3.* Parameter changes:
- Add one further nested block β_{p+1} with a fresh V-extent (V-positions extending the V-arena past W(2, p)'s end by 3 positions) and I-extent identical to β_p: `{a₈, a₉, a₁₀}` of size 3 (a within-document copy of β_p's I-extent, admitted by S5 as within-document I-address sharing across blocks).
- All other parameters unchanged: same siblings a₁..a₁₁, same blocks β₁..β_p, same two spans.

The inter-block boundary v_V → v_{V+1} between β_p and β_{p+1} satisfies V-adjacency and fails I-adjacency (β_p's last I-image is `a₁₀`, β_{p+1}'s first I-image is `a₈`, requiring `a₁₀ + 1 = a₁₁ = a₈`, false); M12 splits β_p and β_{p+1} into distinct blocks despite the I-extent coincidence. Each span deposits the same contribution in β_{p+1} as in β_p (same I-extent, same span coverages), so the 2 · (p + 1) decomposition terms are all non-empty with the same within-block structure as β_p (span 1 at offset 0 covering a₈, span 2 at offset 2 covering a₁₀, pairwise gap-width-1 non-adjacent at offset 1 carrying a₉). The smallest block size remains `n_{p+1} = 3 = 2m − 1` at m = 2, satisfying the threshold. Pairwise block overlap holds for every (β_k, β_{p+1}) pair by nested I-extents (β_{p+1} ⊆ β_p ⊆ β_k for k ≤ p). The lifted witness saturates `2 · (p + 1)`. The (β_2) lift is the (β) recipe instantiated at m = 2 with the appropriate base-shape relabelling — same per-step structural verification, same SV11 attainment-condition preservation.

*Iteration of the lifts is by induction.* Each of the four lift recipes — (α), (β), (α_2), (β_2) — produces a single-step witness in the same parameter family as its source, with the per-step verifications stated explicitly above (block boundary failure under M12, span-offset arithmetic progression in each block, threshold satisfaction `min_k n_k ≥ 2(m + 1) − 1` or unchanged at m = 2, and pairwise non-adjacency and non-overlap of decomposition terms). Iteration to arbitrary parameter values is by ordinary induction on the lifted parameter — `m` for the (α) and (α_2) families, `p` for the (β) and (β_2) families — with the base case the explicit witness named at the start of each lift family (W(3, 3) for (α), W(3, 3) for (β), W(3, 2) for (α_2), W(2, 3) for (β_2)) and the step the verified single-step lift. The induction's content reduces to "the lifted witness preserves all four SV11 attainment conditions" — itself the verification supplied in the lift body above — so the inductive step is the lift body, applied with the parameter incremented by one. The "same per-step structural verification" remark closing each (α_2) and (β_2) body therefore reads as "the lift's verification carries from the source-parameter target to every subsequent target by repeating the step on the previous target's parameters," not as a proof-by-similarity over the iteration. The Coverage paragraph below records the concrete iteration count required to reach an arbitrary target (m*, p*) from each base.

*Coverage of (m ≥ 3, p ≥ 3) and the (m ≥ 3, p = 2) and (m = 2, p ≥ 3) boundary regions.* Starting from the explicit W(3, 3) and W(4, 3) witnesses above and applying (α) and (β) finitely many times reaches every (m ≥ 3, p ≥ 3): for any target (m*, p*) with m* ≥ 3 and p* ≥ 3, apply (β) `p* − 3` times then (α) `m* − 3` times starting from W(3, 3) (a canonical order; the alternative order is equivalent — see commutativity verification below). *Commutativity verification.* The (α) and (β) recipes commute when applied to any W(m, p) with `m ≥ 3, p ≥ 3` — i.e., (α)∘(β) and (β)∘(α) both yield W(m + 1, p + 1) with identical sibling sets, identical block I-extents, identical V-extents (up to renaming of fresh V-positions inside the target document), and identical spans. *Proof.* Both compositions start from W(m, p) with siblings `a_1, ..., a_{2m+3}` allocated and `a_{2m+4}` unallocated, blocks β_1..β_p of W(m, p) shape (β_1 size `2m + 3`, β_2 size `2m + 1`, β_k size `2m − 1` for `k ≥ 3`), and m spans. *(α) then (β):* (α) allocates `a_{2m+4}, a_{2m+5}`, leaves `a_{2m+6}` unallocated, extends every existing block's I-extent by 2 at the tail (β_1 → `{a_1..a_{2m+5}}`, β_2 → `{a_3..a_{2m+5}}`, β_k → `{a_5..a_{2m+5}}` for `k ≥ 3`), and adds the (m + 1)-th span `(a_{2m+5}, a_{2m+6} ⊖ a_{2m+5})`. The intermediate state is W(m + 1, p). Applying (β) at this state introduces β_{p+1} with I-extent identical to β_p — `{a_5..a_{2m+5}}` of size `2m + 1 = 2(m+1) − 1` — and leaves all other parameters unchanged. *(β) then (α):* (β) introduces β_{p+1} with I-extent identical to β_p — `{a_5..a_{2m+3}}` of size `2m − 1` — and leaves siblings, blocks β_1..β_p, and spans unchanged. The intermediate state is W(m, p + 1). Applying (α) at this state allocates `a_{2m+4}, a_{2m+5}`, leaves `a_{2m+6}` unallocated, extends *every* existing block's I-extent by 2 at the tail (including the newly-added β_{p+1}, which grows from `{a_5..a_{2m+3}}` to `{a_5..a_{2m+5}}`), and adds the (m + 1)-th span. *Endpoint comparison.* Both endpoints have siblings `a_1, ..., a_{2m+5}` allocated and `a_{2m+6}` unallocated; blocks `β_1 = {a_1..a_{2m+5}}` (size `2m + 5 = 2(m+1) + 3`), `β_2 = {a_3..a_{2m+5}}` (size `2m + 3 = 2(m+1) + 1`), `β_k = {a_5..a_{2m+5}}` for `3 ≤ k ≤ p + 1` (size `2m + 1 = 2(m+1) − 1`); and `m + 1` spans with the same single-element coverages. The V-extents of pre-existing blocks are unchanged by either composition (each lift extends V-extents at the tail of the source state, never inserts mid-V-arena), and the fresh V-extent introduced for β_{p+1} is placed at the V-arena's current tail at the moment of the (β) step — either at the W(m, p) tail (in (β) ∘ (α): freshly introduced after the (α) V-extensions) or at the W(m + 1, p) tail (in (α) ∘ (β): freshly introduced after the (α) V-extensions of β_1..β_p extended in size). In both cases the new V-extent for β_{p+1} comes *after* all V-extents of β_1..β_p in the target state's V-arena. The two endpoints are therefore identical W(m + 1, p + 1) witnesses (up to renaming of internal fresh V-position labels, which carries no SV11-attainment content). Commutativity follows. ∎ Consequently, "(β) `p* − 3` times then (α) `m* − 3` times" and "(α) `m* − 3` times then (β) `p* − 3` times" reach the same target W(m*, p*); the canonical order is named for definiteness, not because the other order would fail. The (m ≥ 5, p = 3) region is reached by iterated (α) starting from W(4, 3) (or by one extra (α) from W(3, 3) followed by further (α)'s); the (m ≥ 3, p ≥ 4) region is reached by iterated (β) starting from W(3, 3) or W(4, 3). The boundary regions are covered by iterated boundary lifts: starting from W(3, 2) (the explicit (m = 3, p = 2) three-span witness above) and applying (α_2) `m* − 3` times reaches every (m*, 2) with m* ≥ 3; starting from W(2, 3) (the explicit (m = 2, p = 3) three-block witness above) and applying (β_2) `p* − 3` times reaches every (2, p*) with p* ≥ 3. All four lift schemata — (α), (β), (α_2), (β_2) — preserve all four SV11 attainment conditions — every decomposition term non-empty, within-block non-adjacency by gap-width 1, within-block non-overlap by single-element-at-distinct-offsets (or span-coverage disjointness in the (m = 2, p ≥ 3) case), and pairwise block overlap by maintained nesting — so the m · p bound is realised at every (m ≥ 3, p ≥ 3) and at every (m ≥ 3, p = 2) and every (m = 2, p ≥ 3). The structural-failure characterisation below (disjoint-pair case and non-injective small-block case) is not affected: those cases rule out attainment regardless of p.

*Multi-block (p ≥ 2) non-attainment — two structural cases.* The configurations not covered by the witnesses above admit a structural non-attainment argument. The m · p bound is *not* attained at (m, p) with m ≥ 2 and p ≥ 2 in either of two cases: (i) *some* pair of blocks has disjoint I-extents, regardless of block sizes (this subsumes pairwise-disjoint configurations and any p ≥ 3 configuration containing at least one disjoint pair); (ii) the block I-extents overlap (S5) but the smallest block is too small — `min_k n_k < 2m − 1`. We treat the cases in turn.

*Disjoint-pair case (the generic injective configuration, and any p ≥ 3 configuration with a disjoint pair).* Suppose some pair of blocks β_{k₁}, β_{k₂} has disjoint I-extents — either because all p blocks are pairwise disjoint, or because among p ≥ 3 blocks at least one pair fails to overlap while others may. Order the pair so β_{k₁}'s first ordinal element is T-less than β_{k₂}'s first ordinal element. By S0 (Convexity), each span (s_j, ℓ_j) has coverage `⟦(s_j, ℓ_j)⟧ = [s_j, s_j ⊕ ℓ_j)`, a half-open interval in T. The argument that follows depends on the *T-relation* between the two blocks' I-extents — two ordinal-sibling sequences, disjoint as sets, can either be T-linearly separated (one block's elements all T-less than the other's) or T-interleaved (some element of one block sits T-between two elements of the other). We treat the two sub-cases.

(a) *T-linear separation — β_{k₁}'s last ordinal element T-precedes β_{k₂}'s first ordinal element.* Let e_max denote β_{k₁}'s last ordinal element and f_min denote β_{k₂}'s first ordinal element; assume e_max < f_min in T. For span j to deposit non-empty terms in both blocks, its coverage [s_j, s_j ⊕ ℓ_j) must intersect both. Pick e ∈ β_{k₁} ∩ coverage(span j) and f ∈ β_{k₂} ∩ coverage(span j); since e ∈ β_{k₁} we have e ≤ e_max, and since f ∈ β_{k₂} we have f ≥ f_min, so e ≤ e_max < f_min ≤ f. By S0 convexity, [e, f] ⊆ coverage(span j), and in particular e_max ∈ coverage(span j) and f_min ∈ coverage(span j). So span j's contribution to β_{k₁} is a *T-suffix* of β_{k₁}'s ordinal sequence including e_max, and its contribution to β_{k₂} is a *T-prefix* including f_min. For *every* span j ∈ {1, ..., m} to contribute non-empty to both blocks (m ≥ 2), all m β_{k₁}-contributions share the element e_max — hence pairwise overlap at e_max and coalesce into a single maximal fragment within β_{k₁}, violating the within-block non-overlap requirement of the attainment biconditional.

(b) *T-interleaving — β_{k₂}'s elements nest between two ordinal-adjacent β_{k₁}-elements.* If T-linear separation fails, some β_{k₂}-element f sits T-between two β_{k₁}-elements. We establish that β_{k₂} ⊂ (e_i, e_{i+1}) for some ordinal-adjacent pair e_i, e_{i+1} ∈ β_{k₁} (with e_{i+1} = e_i + 1 in β_{k₁}'s ordinal sequence) by a T1-based structural lemma, then apply a pigeonhole argument on the boundary set {e_i, e_{i+1}}.

*Four-case structural lemma — confinement of β_{k₂} to a single T-window between ordinal-adjacent β_{k₁}-elements.* β_{k₁} is an ordinal-sibling sequence: its elements e_{k₁,1}, ..., e_{k₁,n_{k₁}} share a common prefix q_{k₁} on positions 1..#e − 1 (where #e is the common tumbler length of β_{k₁}-elements), have the same length #e, and differ only at position #e (where they take consecutive ordinal values v, v + 1, ..., v + n_{k₁} − 1 under TA5(c)). For any T4-valid tumbler y with y ∉ β_{k₁}, the T1 comparison of y against β_{k₁}-elements partitions into four configurations indexed by length-matching and prefix-matching:

- *(I) #y = #e and y agrees with q_{k₁} on positions 1..#e − 1 (same length, matching prefix).* By T1(i), comparison of y with each e ∈ β_{k₁} is decided at position #e. Since β_{k₁}'s last-component values v, ..., v + n_{k₁} − 1 are consecutive and y ∉ β_{k₁} forces y_#e ∉ {v, ..., v + n_{k₁} − 1}, either y_#e < v (so y < every β_{k₁}-element by T1(i)) or y_#e > v + n_{k₁} − 1 (so y > every β_{k₁}-element). *T-linear separation.*

- *(II) #y = #e but y diverges from q_{k₁} at some position p ≤ #e − 1 (same length, non-matching prefix).* All β_{k₁}-elements share q_{k₁} on positions 1..#e − 1, so y diverges from every β_{k₁}-element at the same position p with the same direction (y_p < q_{k₁,p} or y_p > q_{k₁,p}). By T1(i), y is uniformly T-less than every β_{k₁}-element or uniformly T-greater. *T-linear separation.*

- *(III) #y ≠ #e and a prefix relationship holds (different length, prefix relationship).* Two sub-cases:
  - *(IIIa)* y is a proper prefix of some β_{k₁}-element e_{k₁,j}. Since e_{k₁,j} extends q_{k₁} by one position (at position #e), and the shared prefix q_{k₁} is itself a prefix of every β_{k₁}-element, y must be a prefix of q_{k₁}, hence a proper prefix of every β_{k₁}-element. By T1(ii), y < every β_{k₁}-element. *T-linear separation.*
  - *(IIIb)* Some β_{k₁}-element e_{k₁,j*} is a proper prefix of y (so #y > #e and y agrees with e_{k₁,j*} on positions 1..#e). By T1(ii), y > e_{k₁,j*}. To compare y with e_{k₁,j*+1} (when j* < n_{k₁}): they share q_{k₁} on positions 1..#e − 1, and at position #e the value y_#e = (e_{k₁,j*})_#e (inherited from y's agreement with e_{k₁,j*}) while (e_{k₁,j*+1})_#e = (e_{k₁,j*})_#e + 1 (ordinal-sibling increment). Hence y_#e < (e_{k₁,j*+1})_#e at the first position where they differ, and by T1(i) y < e_{k₁,j*+1}. Combined: e_{k₁,j*} < y < e_{k₁,j*+1} — *y sits strictly between ordinal-adjacent β_{k₁}-elements at positions j* and j*+1*. When j* = n_{k₁}, the comparison with e_{k₁,j*+1} is vacuous and y > e_{n_{k₁}} alone holds — *T-linear separation past the last element.*

- *(IV) #y ≠ #e and no prefix relationship holds (different length, non-prefix).* Then y diverges from q_{k₁} at some position p ≤ min(#y, #e − 1) (a position in the overlap of y's range 1..#y and q_{k₁}'s range 1..#e − 1). All β_{k₁}-elements share q_{k₁} on positions 1..#e − 1 ⊇ 1..p, so y diverges from every β_{k₁}-element at position p uniformly. By T1(i), y is T-less than every β_{k₁}-element or T-greater than every β_{k₁}-element. *T-linear separation.*

*Case routing — exhaustiveness check at the (III)/(IV) boundary.* The four cases partition every T4-valid y ∉ β_{k₁} by two independent binary tests — `#y = #e` vs `#y ≠ #e` (length-matching) and *some prefix relationship between y and a β_{k₁}-element holds* vs *none holds* (prefix-matching). Cases (I)/(II) cover the length-matching branch (with (I) the prefix-matching sub-branch — agreement on q_{k₁} — and (II) the non-prefix-matching sub-branch — divergence on q_{k₁}). Cases (III)/(IV) cover the length-different branch, with (III) sub-divided by which side is the shorter (`(IIIa)` is `#y < #e` with y a proper prefix of some β_{k₁}-element, `(IIIb)` is `#y > #e` with some β_{k₁}-element a proper prefix of y). The boundary case worth tracing explicitly is `#y < #e` with y *not* a proper prefix of any β_{k₁}-element — this is excluded from (IIIa) by the non-prefix hypothesis and from (IIIb) by the length constraint `#y < #e`, so it routes to (IV): since y is not a prefix of any β_{k₁}-element (and in particular not of q_{k₁}, which would otherwise extend each β_{k₁}-element by one position), and since all β_{k₁}-elements share q_{k₁} on positions 1..#e − 1, y must diverge from q_{k₁} at some position `p ≤ #y < #e` — placing y in (IV)'s "non-prefix" regime with divergence position `p ≤ min(#y, #e − 1) = #y` (since `#y ≤ #e − 1`). The complementary `#y > #e` non-prefix sub-case also routes to (IV) by the same divergence analysis at position `p ≤ #e − 1 < #y`. The four cases therefore partition the y-space exhaustively.

The T-interleaving sub-case rules out T-linear separation, so cases (I), (II), (IIIa), the j* = n_{k₁} branch of (IIIb), and (IV) are all excluded. The only remaining configuration is the j* < n_{k₁} branch of (IIIb): every T-interleaved tumbler y must have some β_{k₁}-element e_{k₁,j*} as a proper prefix with j* < n_{k₁}, placing y strictly between ordinal-adjacent e_{k₁,j*} and e_{k₁,j*+1}.

*Uniformity of the bracket across β_{k₂}.* β_{k₂} is itself an ordinal-sibling sequence: its elements f₁, ..., f_{n_{k₂}} share a common prefix q_{k₂} on positions 1..#f − 1, have the same length #f, and differ only at position #f. The four-case lemma applied to each f ∈ β_{k₂} forces #f > #e (sub-case (IIIb)) and the first #e positions of f to equal some β_{k₁}-element e_{k₁,j*}. Because the first #e positions of every f ∈ β_{k₂} are themselves a prefix of q_{k₂} (positions 1..#e ⊆ 1..#f − 1 since #f > #e), they are shared across β_{k₂}; hence the same index j* serves for every f. Uniformity of j* across β_{k₂} forces an exhaustive dichotomy on the j* < n_{k₁} vs j* = n_{k₁} sub-case split of (IIIb): either every f ∈ β_{k₂} has j* < n_{k₁} (the interior-window branch — all of β_{k₂} confined to a single window (e_{k₁,j*}, e_{k₁,j*+1}), the case-(b) T-interleaving regime), or every f has j* = n_{k₁} (the past-the-last-element branch — all of β_{k₂} T-greater than every β_{k₁}-element, which is itself T-linear separation, subsumed by case (a) and contradicting the case-(b) hypothesis); mixed configurations where some f have j* < n_{k₁} and others j* = n_{k₁} are impossible by uniformity. Under the (b) hypothesis, the j* = n_{k₁} option is excluded, leaving j* < n_{k₁} uniformly, so β_{k₂} ⊂ (e_{k₁,j*}, e_{k₁,j*+1}). Setting i = j*, β_{k₂} is confined to the single T-window (e_i, e_{i+1}) bounded by ordinal-adjacent β_{k₁}-elements with e_{i+1} = e_i + 1.

*Boundary intersection.* For span j to contribute non-empty to β_{k₂}, its coverage must intersect (e_i, e_{i+1}); by S0 convexity, span j's coverage `[s_j, s_j ⊕ ℓ_j)` is a T-interval. To intersect both β_{k₂} (entirely inside (e_i, e_{i+1})) and β_{k₁} (whose only elements T-adjacent to (e_i, e_{i+1}) are e_i and e_{i+1}), the coverage must include at least one of {e_i, e_{i+1}}: if `s_j > e_i` and `s_j ⊕ ℓ_j ≤ e_{i+1}`, the coverage sits strictly inside (e_i, e_{i+1}) and meets β_{k₁} in the empty set; otherwise `s_j ≤ e_i` (placing e_i ∈ coverage when e_i ≥ s_j) or `s_j ⊕ ℓ_j > e_{i+1}` (placing e_{i+1} ∈ coverage).

*Pigeonhole on the boundary set {e_i, e_{i+1}}.* For m ≥ 2 spans each contributing non-empty to both blocks, let B_j = (span j's β_{k₁}-contribution) ∩ {e_i, e_{i+1}} denote the boundary elements included in span j's β_{k₁}-contribution. Each B_j is a non-empty subset of {e_i, e_{i+1}} (so B_j ∈ {{e_i}, {e_{i+1}}, {e_i, e_{i+1}}}). Two cases dispatch every (m, configuration of B_j's):

- *(m = 2.)* B₁ and B₂ are two non-empty subsets of the 2-element set {e_i, e_{i+1}}. Within such a 2-element set, any two non-empty subsets either *share an element* or *exhaust the set as the two distinct singletons* — there is no third pattern. (i) *Shared boundary element* (B₁ ∩ B₂ ≠ ∅, covering uniformly the patterns B₁ = B₂ = {e_i}, B₁ = B₂ = {e_{i+1}}, B₁ = B₂ = {e_i, e_{i+1}}, and any asymmetric inclusion such as B₁ = {e_i, e_{i+1}} ∧ B₂ = {e_i}): the shared boundary element places the two β_{k₁}-contributions in pairwise overlap at that element, coalescing into a single maximal fragment within β_{k₁}. (ii) *Disjoint singletons* (B₁ ∩ B₂ = ∅, forcing B₁ = {e_i} and B₂ = {e_{i+1}} or symmetric): by S0 convexity each β_{k₁}-contribution is contiguous in β_{k₁}'s ordinal sequence, so the two contributions sit at ordinal-adjacent positions (e_i at offset h, e_{i+1} at offset h + 1 in β_{k₁}'s sequence, with e_{i+1} = e_i + 1 differing by one ordinal step), coalescing by mechanism (b) of the SV11 biconditional (within-block ordinal-adjacency of two non-empty terms). Either way attainment fails.

- *(m ≥ 3.)* By pigeonhole on the 2-element boundary set {e_i, e_{i+1}} (every B_j is non-empty and contained in {e_i, e_{i+1}}, so each B_j contains at least one boundary element), at least two of the m sets B₁, ..., B_m share a common boundary element of {e_i, e_{i+1}}; the corresponding two β_{k₁}-contributions overlap at that shared element, coalescing into a single maximal fragment — attainment fails.

In both cases, the within-block non-overlap-and-non-adjacency requirement fails.

In both sub-cases, attainment fails. The argument depends only on the two-block pair (β_{k₁}, β_{k₂}); the presence of additional blocks (p ≥ 3) does not rescue attainment — once one disjoint pair forces coalescence inside β_{k₁}, the m · p fragment count is unreachable regardless of what the remaining p − 2 blocks contribute. Per-block fragment counts are independently capped at m by the attainment biconditional, so a within-block deficit in β_{k₁} cannot be offset by other blocks; the total fragment count is therefore strictly less than m · p. Consequently, attainment at (m ≥ 2, p ≥ 3) requires *all* pairs of blocks to have overlapping I-extents; any single disjoint pair kills it.

*Non-injective small-block case (overlapping I-extents, S5 sharing, `min_k n_k < 2m − 1`).* When two blocks β_{k₁}, β_{k₂} have overlapping I-extents — admitted under S5 (UnrestrictedSharing) and exhibited by the two-span Worked Example below with β₂'s extent {a₂, a₃} ⊆ β₁'s extent {a₁..a₅} — the within-block non-adjacency requirement carries an independent block-size barrier. Within a block β_k with ordinal sequence length n_k, m pairwise non-adjacent singletons need at least 2m − 1 ordinal positions to fit (m occupied positions plus m − 1 gap positions). If `min_k n_k < 2m − 1`, no arrangement of m within-block contributions can satisfy non-adjacency in the smallest block, regardless of the spans. The two-span Worked Example below has n_{β₂} = 2 < 2·2 − 1 = 3, so attainment at (m = 2, p = 2) is already excluded by the block-size constraint alone — independently of the suffix-overlap argument of the disjoint-pair case. The complementary non-injective large-block case (`min_k n_k ≥ 2m − 1`) is *not* excluded by this argument; the multi-block overlap witness above realises attainment in that case, so the block-size threshold is the structural dividing line between non-injective non-attainment and non-injective attainment.

*Conclusion.* The witnessed attainment scope spans five configurations: (i) `(m ≥ 1, p = 1)` via the single-block sibling construction; (ii) `(m = 1, p ≥ 2)` via the Worked Example's post-removal state below for p = 2, the explicit `(m = 1, p = 3)` three-block witness above for p = 3, and direct per-p constructions from Σ₀ for every `(m = 1, p ≥ 4)` (each parallels W(1, 3): allocate 2p + 1 siblings, place sequentially, apply p − 1 K.μ~ + K.μ⁻ excisions targeting interior I-addresses of currently-targeted blocks of size ≥ 3 — single span whose coverage intersects every block's I-extent non-emptily, satisfying mechanism (a); mechanism (b) is vacuous at m = 1); (iii) `(m ≥ 2, p = 2)` with the two blocks having overlapping I-extents and `min_k n_k ≥ 2m − 1` — the (m = 2, p = 2) two-span witness W(2, 2) above (a corner case witnessed standalone; no lift family starts from W(2, 2), since (α_2) starts at W(3, 2) and (β_2) starts at W(2, 3)) and the (m = 3, p = 2) three-span witness W(3, 2) on the same arrangement (β₂ tight at the small-block threshold `min_k n_k = 5 = 2m − 1`), with all `(m ≥ 4, p = 2)` reached by iterated application of the boundary lift (α_2) starting from W(3, 2); (iv) `(m = 2, p ≥ 3)` via the explicit (m = 2, p = 3) three-block witness W(2, 3) above, with all `(m = 2, p ≥ 4)` reached by iterated application of the boundary lift (β_2) starting from W(2, 3); and (v) `(m ≥ 3, p ≥ 3)` via the (m = 3, p = 3) and (m = 4, p = 3) base witnesses above plus the inductive lift schema covering all (m ≥ 3, p ≥ 3). The general `(m ≥ 3, p ≥ 3)` regime is therefore witnessed at (m, p) = (3, 3) and (m, p) = (4, 3) directly, with higher-(m, p) attainment supplied by finitely many applications of the inductive lifts (α) `W(m, p) → W(m + 1, p)` and (β) `W(m, p) → W(m, p + 1)` from those base witnesses; the boundary regions are witnessed at W(3, 2) and W(2, 3) directly, with higher-(m, 2) and (2, p) attainment supplied by the parallel boundary lifts (α_2) and (β_2). The M7/M12 realisability constraints place no obstruction in any of the five configurations. Non-attainment is structural in two complementary configurations at `(m ≥ 2, p ≥ 2)`: the *disjoint-pair* case (any single disjoint pair of blocks forces coalescence inside one block of the pair — either *suffix coalescence at the shared boundary element* in the T-linear sub-case, or *boundary-element overlap or ordinal-adjacency* in the T-interleaving sub-case where one block's elements nest between two ordinal-adjacent elements of the other; either sub-case kills attainment regardless of the remaining p − 2 blocks at p ≥ 3) and the *non-injective small-block* case (`min_k n_k < 2m − 1`; the block-size barrier rules out within-block non-adjacency). The SV11 biconditional ("attainment ⇔ every (j, k) yields a non-empty term and within each block the m terms are pairwise non-adjacent and non-overlapping") remains valid as the characterisation of attainment; whether its antecedent is jointly satisfiable by a concrete (B, e) is decided by the case analysis above — yes in the five witnessed regions, no in the two structural-failure regions.

(The full projection π(e, d) = coverage(e) ∩ ran(M(d)) may additionally include I-addresses reached through link-subspace V-positions. K.μ⁺_L (LinkSubspaceExtension, ASN-0047) creates link-subspace V-positions `v_ℓ ↦ ℓ` where `subspace(v_ℓ) = s_L`, so π_text(e, d) ⊆ π(e, d) in general. The link-subspace contribution to projection — including links whose endsets reference other link addresses (L13, ReflexiveAddressing) — is deferred to the Link Subspace ASN.)

*Derivation of the formula.* The endset's coverage decomposes as `coverage(e) = ⋃_{j=1}^{m} ⟦(sⱼ, ℓⱼ)⟧` (L3/L4, ASN-0043: the endset is a set of spans and its coverage is the union of their denotations). The text-subspace range decomposes as `ran_text(M(d)) = ⋃_{k=1}^{p} I(β_k)` (B's V-coverage and the per-block mapping rule, ASN-0058: C1a lifts M11/M12's existence-and-uniqueness of the maximally merged decomposition to the restriction `M(d)|_{V_{s_C}(d)}`; B1 then places each v in the restriction's domain in a unique V(β_k), and within each block β_k = (v_k, a_k, n_k) the per-block mapping rule B3 sets `M(d)(v) = a_k + (v − v_k) ∈ I(β_k)`, so the I-extents of B's blocks exhaust the content-subspace range). Substituting both into π_text(e, d) = coverage(e) ∩ ran_text(M(d)) and distributing intersection over union (set algebra, applied twice):

`π_text(e, d) = (⋃_j ⟦(sⱼ, ℓⱼ)⟧) ∩ (⋃_k I(β_k)) = ⋃_j ⋃_k (⟦(sⱼ, ℓⱼ)⟧ ∩ I(β_k)) = ⋃_{j,k} (⟦(sⱼ, ℓⱼ)⟧ ∩ I(β_k))`

— which is the formula stated above. The remainder of this section establishes the structural properties of the m · p decomposition terms: each is contiguous within its block's ordinal sequence (S0 convexity, below), the terms may overlap or coalesce into fewer maximal fragments (fragment-count discussion), and the collection forms a cover rather than a partition under non-injective arrangements.

Consider each decomposition term ⟦(sⱼ, ℓⱼ)⟧ ∩ I(β_k). The span ⟦(sⱼ, ℓⱼ)⟧ is convex by S0 (Convexity). The set I(β_k) = {a_k + j : 0 ≤ j < n_k} is not itself convex in T — child-depth tumblers create gaps between consecutive ordinal increments — but we do not need it to be. For ordinal indices j₁ < j₂ < j₃ with a_k + j₁ and a_k + j₃ both in ⟦(sⱼ, ℓⱼ)⟧, we have a_k + j₁ < a_k + j₂ < a_k + j₃ (by M1 (OrderPreservation, ASN-0058)), so by the convexity of the span (S0), a_k + j₂ ∈ ⟦(sⱼ, ℓⱼ)⟧. Hence each decomposition term is contiguous within the ordinal sequence of I(β_k): if its first and last elements have ordinal offsets j₁ and j₂, every intermediate a_k + j with j₁ ≤ j ≤ j₂ also lies in the term.

*Decomposition terms versus maximal fragments.* The number of decomposition terms is m · p exactly — one per (span, block) pair, including empty terms. Within a single block β_k, the m terms `⟦(s₁, ℓ₁)⟧ ∩ I(β_k), ..., ⟦(s_m, ℓ_m)⟧ ∩ I(β_k)` are each contiguous in the ordinal sequence of I(β_k), but they may overlap or be adjacent; their union π_text(e, d) ∩ I(β_k) may therefore consist of fewer maximal contiguous regions than there are non-empty terms. A maximal fragment is one such maximal contiguous region. Hence the number of maximal fragments within a single block is at most the number of non-empty decomposition terms in that block, which is at most m. Across p blocks, the number of maximal fragments is bounded by m · p — the same upper bound as for decomposition terms, attained when every non-empty term is itself a maximal fragment (when the spans contribute pairwise non-adjacent regions within each block). Each maximal fragment is compactly described by its first element and count: (a_k + j₁, j₂ − j₁ + 1).

When M(d) is non-injective — within-document sharing (S5, UnrestrictedSharing) — two blocks may have overlapping I-extents, so maximal fragments from distinct blocks may share I-addresses. The fragment collection is therefore a *cover* of π_text(e, d), not necessarily a partition; summing fragment widths may overcount distinct I-addresses. The set-union formula `π_text(e, d) = ⋃_{j,k} (⟦(sⱼ, ℓⱼ)⟧ ∩ I(β_k))` remains correct (set union is idempotent), and it does not depend on the term/fragment distinction.

We note a distinction between maximal fragments (or decomposition terms — both are contiguous ordinal subsequences) and span denotations. A maximal fragment is a finite set of I-addresses {a_k + j₁, ..., a_k + j₂} produced by ordinal increment within an actually-allocated block. The span denotation ⟦(s, ℓ)⟧ = {t ∈ T : s ≤ t < s ⊕ ℓ} includes all tumblers in the half-open interval, including child-depth tumblers between consecutive ordinal increments — a child c produced by inc(a, 1) satisfies a < c < a + 1, so c ∈ ⟦(a, ℓ)⟧ for any span (a, ℓ) whose reach satisfies a ⊕ ℓ > a + 1, but c is not necessarily in ran(M(d)). The exact characterisation of π_text(e, d) is the union of its maximal fragments (equivalently, the union of its decomposition terms), not a union of span denotations. If one needs to connect projections to the span algebra of ASN-0053, the correct relationship is *covering*: for each maximal fragment with first element a_k + j₁ and last element a_k + j₂, a level-uniform span (a_k + j₁, ℓ') with reach a_k + (j₂ + 1) satisfies ⟦(a_k + j₁, ℓ')⟧ ⊇ fragment (since ordinal increment preserves tumbler length by TA5(c)). Such covering span-sets are normalizable within each tumbler-depth group (S8, NormalizationExistence).

The significance: **partial survival is well-structured.** The surviving portion of an endset in a given document decomposes into finitely many maximal fragments — at most m · p of them, where m is the endset's span count and p is the block count — each compactly described by a start address and count within a mapping block's ordinal sequence. Convexity (S0) ensures contiguity within each block, preventing degeneration into arbitrary subsets of I-addresses. The m · p bound moves with the state: a composite edit that splits an existing block (e.g., K.μ~ + K.μ⁻ excising interior content) raises p and therefore raises the bound; the same bound applies at the post-edit state under its post-edit p.


## Worked Example

We verify the key definitions against a specific scenario with explicit tumbler values.

*Setup.* Consider a document d with five I-addresses a₁ < a₂ < a₃ < a₄ < a₅ in the text subspace, allocated sequentially by sibling increment. All five share the same origin and tumbler length. The document's initial arrangement maps five V-positions in order:

`M(d) = {v₁ ↦ a₁, v₂ ↦ a₂, v₃ ↦ a₃, v₄ ↦ a₄, v₅ ↦ a₅}`

where v₁ < v₂ < v₃ < v₄ < v₅. Concretely, under the standing convention that V-positions in the content subspace inhabit depth m_C = 2 with leading component s_C (S8a/S8-depth, ASN-0036), and that v₁ is the D-MIN position `[s_C, 1, ..., 1]` of depth m_C while subsequent positions follow under OrdinalShiftBase (`v_{k+1} = shift(v_k, 1)` per ASN-0058), the five V-positions take the concrete values `v_k = [s_C, k]` for k = 1, ..., 5. Each is a depth-2 tumbler in the content subspace, satisfying S8a's componentwise positivity (s_C ≥ 1 and 1 ≤ k ≤ 5 are positive), zeros(v_k) = 0, and #v_k = 2 ≥ 2; D-MIN at the initial arrangement names v₁ = [s_C, 1] specifically. The sequential `shift(v_k, 1)` recurrence makes V-side checks (D-CTG admissibility of insertions, D-SEQ admissibility of contractions) directly verifiable against the explicit values. This is a single mapping block β = (v₁, a₁, 5) in ASN-0058's notation.

A link at address b is created with from-endset F = {(a₂, ℓ)}, where ℓ = a₅ ⊖ a₂ (well-defined by D0, since a₂ < a₅ and both have the same length). The reach is a₂ ⊕ ℓ = a₅ (by D1). So coverage(F) = {t : a₂ ≤ t < a₅}. Among the allocated I-addresses, this interval contains exactly a₂, a₃, a₄. We assume throughout this worked example that Σ contains only the single link b — `dom(Σ.L) = {b}` — so the discovery equalities `discover_s(A) = {b}` below read as equalities rather than as singletons-of-membership; in a state with additional links, the analogous claims would read `b ∈ discover_s(A)` with the equalities relaxed accordingly.

*Initial state — projection, resolution, discovery.*

- π(F, d) = coverage(F) ∩ ran(M(d)) = {a₂, a₃, a₄}
- locate(F, d) = {v ∈ dom(M(d)) : M(d)(v) ∈ coverage(F)} = {v₂, v₃, v₄}
- discover_from({a₃}) = {b}, since coverage(F) ∩ {a₃} = {a₃} ≠ ∅ and `dom(Σ.L) = {b}`

The from-endset is vital in d: π(F, d) ≠ ∅. Both π and locate are determined entirely by coverage(F) and the current M(d) (NoStaleResolutionState architectural remark).

*After removing a₃.* The net effect of removing a₃ from ran(M(d)) while satisfying D-CTG requires a composite of elementary transitions, because K.μ⁻ alone cannot remove an interior V-position — by D-SEQ, valid contractions remove from the maximum end of V_S(d) only. We label the two stages by their natural-language effect (rearrange, then contract); the underlying elementary chain is K.μ⁻ + K.μ⁺ + K.μ⁻, since K.μ~ is itself the distinguished K.μ⁻ + K.μ⁺ composite (ASN-0047). Throughout this section we write K.μ~ for the reordering composite and reserve K.μ⁻ for the trailing contraction.

*State-naming convention used below.* The composite produces four distinguishable states between Σ (pre-Step-1) and Σ' (post-Step-2), and the SV14(d) witness instantiation in §SV14 cross-references several of them. To prevent confusion when the prose says "the intermediate state" or "the post-K.μ~ state," we fix the following names up-front and use them consistently:

| Name | Stage | dom(M(d)) | Arrangement |
|------|-------|-----------|-------------|
| Σ | pre-Step-1 (initial) | {v₁, v₂, v₃, v₄, v₅} | {v₁ ↦ a₁, v₂ ↦ a₂, v₃ ↦ a₃, v₄ ↦ a₄, v₅ ↦ a₅} |
| Σ_int_kμ~ | inside K.μ~ (post-K.μ⁻, pre-K.μ⁺) — Step 1's *SV5-style elementary-stage intermediate* | {v₁, v₂} | {v₁ ↦ a₁, v₂ ↦ a₂} (upward tail {v₃, v₄, v₅} removed) |
| Σ_reord | post-K.μ~ (full domain, reordered) — Step 1's output state | {v₁, v₂, v₃, v₄, v₅} | {v₁ ↦ a₁, v₂ ↦ a₂, v₃ ↦ a₄, v₄ ↦ a₅, v₅ ↦ a₃} |
| Σ' | post-Step-2 (final, after trailing K.μ⁻) | {v₁, v₂, v₃, v₄} | {v₁ ↦ a₁, v₂ ↦ a₂, v₃ ↦ a₄, v₄ ↦ a₅} |

The SV14(d) witness instantiation in §SV14 (D-SEQ-admissible because v₅ is the maximum V-position in Σ_reord) reads the elementary K.μ⁻ step `Σ_reord →_{K.μ⁻} Σ'` — not the SV5-style internal step `Σ →_{K.μ⁻} Σ_int_kμ~` inside K.μ~, which acts on a different domain and produces a different state. The prose distinguishes the two by name throughout: "intermediate" refers to Σ_int_kμ~ specifically, "post-K.μ~" refers to Σ_reord.

*Step 1 — K.μ~ (the distinguished K.μ⁻ + K.μ⁺ composite, ASN-0047) rearranges d so that a₃ occupies the maximum V-position.* Apply the bijection ψ on dom(M(d)) = {v₁, v₂, v₃, v₄, v₅} given by ψ(v₃) = v₅, ψ(v₄) = v₃, ψ(v₅) = v₄, ψ(v_i) = v_i for i ∈ {1, 2} (a cyclic shift of the three rightmost positions sending a₃ to v₅). *Composite admissibility.* ASN-0047 reads K.μ~ as the *existence* of some K.μ⁻ + K.μ⁺ decomposition realising the reordering; we exhibit one such decomposition to discharge enabling at each admissibility check below, without claiming canonical choice — multiple decompositions may yield the same K.μ~ effect. (This clarification applies to every admissibility check in the worked example below; we do not repeat it.) *Derivation of the K.μ⁻ cut.* The bijection ψ alters the V↦I assignment at exactly the V-positions {v₃, v₄, v₅} (where ψ moves) and fixes the assignment at {v₁, v₂} (where ψ is the identity). The K.μ⁻ stage must therefore remove at least this altered-assignment set {v₃, v₄, v₅}, and by D-SEQ (ASN-0047) the removal set must be an upward tail of V_{s_C}(d) = {v₁, v₂, v₃, v₄, v₅}. The minimal upward tail containing {v₃, v₄, v₅} is the tail at cut n' = 2 — namely {v₃, v₄, v₅} itself, since the largest index k such that ψ acts as identity on {v₁, ..., v_k} is k = 2. The K.μ⁻ stage of K.μ~ therefore removes the upward tail {v₃, v₄, v₅} at cut n' = 2, which is D-SEQ-admissible per ASN-0047 (K.μ⁻ removes from the maximum end of V_S(d) only, same admissibility check applied to Step 2's contraction of v₅ below). The subsequent K.μ⁺ stage re-introduces v₃, v₄, v₅ with V↦I assignments v₃ ↦ a₄, v₄ ↦ a₅, v₅ ↦ a₃, satisfying D-CTG (each new V-position's depth and prefix match the content-subspace common shape inherited from {v₁, v₂}) and D-MIN at each successive step (the post-K.μ⁻ V_{s_C}(d) = {v₁, v₂} has max v₂, so v₃ extends the upward tail at the immediate next position, and v₄, v₅ follow analogously). *J1★ across the K.μ~ composite.* J1★ (ExtensionRecordsProvenanceContent, ASN-0047) requires that each new mapping `M'(d)(v) = a` introduced by a K.μ⁺ step in the composite be accompanied by `(a, d) ∈ R'`. Here the K.μ⁺ stage of K.μ~ re-introduces the I-addresses {a₃, a₄, a₅} — each already in `ran(Σ.M(d))` prior to the K.μ⁻ stage, with R-entries `(a₃, d), (a₄, d), (a₅, d) ∈ R` supplied by the earlier K.μ⁺ + K.ρ pairings that established the pre-Step-1 arrangement. P2 (ProvenancePermanence, ASN-0047) holds R monotonically across the K.μ⁻ stage of K.μ~ (which modifies M only, not R), so all three R-entries survive into the post-K.μ⁻ intermediate state and remain in R when the K.μ⁺ stage reads them. J1★ is therefore satisfied across the K.μ~ composite without fresh K.ρ steps inside the composite, because the re-added I-addresses are precisely those already R-recorded by prior placements. The composite K.μ~ is therefore enabled at the pre-Step-1 state, and Step 1's effect is exhibited as a single transition with the post-composite arrangement displayed below. We name the post-K.μ~ state `M_reord(d)` — "reordered" — reserving the term "intermediate" for the SV5-style elementary-stage state internal to K.μ~ (post-K.μ⁻, pre-K.μ⁺), which has contracted domain and is a different state from the one named here. The post-K.μ~ state is:

`M_reord(d) = {v₁ ↦ a₁, v₂ ↦ a₂, v₃ ↦ a₄, v₄ ↦ a₅, v₅ ↦ a₃}`

At this point, dom(M_reord(d)) = dom(M(d)) (reordering preserves the domain) and ran(M_reord(d)) = ran(M(d)) = {a₁, a₂, a₃, a₄, a₅} (SV5: reordering preserves the I-range, so π(F, d) = {a₂, a₃, a₄} is unchanged at this step; only locate has changed — locate_{M_reord}(F, d) = {ψ(v₂), ψ(v₃), ψ(v₄)} = {v₂, v₅, v₃}).

*Step 2 — K.μ⁻ removes v₅.* By D-SEQ, v₅ is now the maximum V-position and is admissible for contraction. The post-contraction state is:

`M'(d) = {v₁ ↦ a₁, v₂ ↦ a₂, v₃ ↦ a₄, v₄ ↦ a₅}`

with dom(M'(d)) = {v₁, v₂, v₃, v₄} and ran(M'(d)) = {a₁, a₂, a₄, a₅}. Reading projection, location, and discovery at this final state:

- π(F, d) = coverage(F) ∩ ran(M'(d)) = {a₂, a₄} — reduced (SV3)
- locate(F, d) = {v₂, v₃} — since M'(d)(v₂) = a₂ and M'(d)(v₃) = a₄, both in coverage(F)
- discover_from({a₃}) = {b} — unchanged, because coverage(F) is invariant (L12, ASN-0043), a₃ ∈ coverage(F) regardless of M(d) (SV8), and `dom(Σ.L) = {b}` is preserved by the L-frame transitions of this worked example

The endset remains vital but with reduced projection. The removal of a₃ from ran(M(d)) has split the endset's visible region into two maximal fragments. To see the decomposition of SV11: the post-removal arrangement has two mapping blocks — β₁ = (v₁, a₁, 2) covering {v₁, v₂} with I-extent {a₁, a₂}, and β₂ = (v₃, a₄, 2) covering {v₃, v₄} with I-extent {a₄, a₅}. With m = 1 (the single span (a₂, ℓ)) and p = 2 (the two blocks), there are m · p = 2 decomposition terms:

- ⟦(a₂, ℓ)⟧ ∩ I(β₁) = {t : a₂ ≤ t < a₅} ∩ {a₁, a₂} = {a₂}
- ⟦(a₂, ℓ)⟧ ∩ I(β₂) = {t : a₂ ≤ t < a₅} ∩ {a₄, a₅} = {a₄}

Each non-empty decomposition term coincides with a maximal fragment in this case, since each is a single-element contiguous ordinal subsequence and no two terms within the same block overlap. The fragment count equals the non-empty term count, equals m · p = 2. Together: π(F, d) = {a₂} ∪ {a₄} = {a₂, a₄}. ✓

Discovery through d still works for queries including a₂ or a₄. But discovery through the specific I-address set {a₃} — while still returning b (SV8) — no longer corresponds to anything visible in d, since a₃ ∉ ran(M'(d)). This illustrates the discovery-projection independence (SV10) and, derivatively via the locate definition, the discovery-resolution distinction of this section: the link is discoverable through a₃, but a₃ ∉ π(F, d) so the projection is partial, and correspondingly resolution of the from-endset in d yields no V-position for a₃.

*Reordering that changes locate.* From the post-removal state `M'(d) = {v₁ ↦ a₁, v₂ ↦ a₂, v₃ ↦ a₄, v₄ ↦ a₅}` with locate(F, d) = {v₂, v₃} (F = {(a₂, ℓ)}, coverage(F) = {t : a₂ ≤ t < a₅}), apply a K.μ~ step whose reordering bijection ψ swaps v₁ and v₂ (and fixes v₃, v₄), so the post-state arrangement is:

`M'''(d) = {v₁ ↦ a₂, v₂ ↦ a₁, v₃ ↦ a₄, v₄ ↦ a₅}`

*Composite admissibility.* The K.μ⁻ stage of K.μ~ must remove an upward tail of V_{s_C}(d) = {v₁, v₂, v₃, v₄} (D-SEQ, ASN-0047). The minimal removal set covering ψ's altered V↦I assignments is {v₁, v₂}, which is *not* an upward tail of {v₁, v₂, v₃, v₄}. The composite is realised instead by the full V_{s_C}(d)-removal (the upward tail at cut n' = 0, admitted by D-SEQ as the "removal of all positions in V_S(d)" boundary case), followed by a K.μ⁺ stage that re-introduces v₁, v₂, v₃, v₄ with the post-swap V↦I assignments v₁ ↦ a₂, v₂ ↦ a₁, v₃ ↦ a₄, v₄ ↦ a₅ — each successive K.μ⁺ step satisfying D-CTG and D-MIN as in Step 1's admissibility check. K.μ~ is therefore enabled at the pre-swap state.

Reading at this final state:

- π(F, d) = coverage(F) ∩ ran(M'''(d)) = coverage(F) ∩ {a₁, a₂, a₄, a₅} = {a₂, a₄} — unchanged from M'(d) (SV5: reordering preserves the I-range)
- locate(F, d) = {v ∈ dom(M'''(d)) : M'''(d)(v) ∈ coverage(F)} = {v₁, v₃} — since M'''(d)(v₁) = a₂ ∈ coverage(F) and M'''(d)(v₃) = a₄ ∈ coverage(F), and M'''(d)(v₂) = a₁ ∉ coverage(F)

The locate *set* strictly changed: {v₂, v₃} pre-swap, {v₁, v₃} post-swap. The two sets differ — v₂ exited locate (its newly assigned I-address a₁ is outside coverage(F)) and v₁ entered locate (its newly assigned I-address a₂ is inside coverage(F)) — because ψ crossed the locate boundary: ψ(v₁) = v₂ takes v₁ ∉ locate_{M'(d)} to v₂ ∈ locate_{M'(d)}, and symmetrically ψ(v₂) = v₁ moves the locate-membership the other way. Per the SV5 formal relationship `locate_{Σ'}(F, d) = {ψ(v) : v ∈ locate_Σ(F, d)}`, we verify: {ψ(v₂), ψ(v₃)} = {v₁, v₃} = locate(F, d) at M'''(d). ✓

This subsection exhibits the substantive content of SV5: the *I-addresses* in the projection are invariant under K.μ~, but the *V-positions* through which the reader observes those addresses are reshuffled by ψ. A reader navigating d after this K.μ~ step would find the from-endset's content at V-positions v₁ and v₃ — different from the V-positions v₂ and v₃ where they were found before — even though the underlying I-addresses {a₂, a₄} reached are unchanged. (A within-locate-set swap — e.g., swapping v₂ and v₃, both inside the pre-swap locate set — would preserve the locate set under SV5b as an automorphism of the locate set, demonstrating π-invariance without the locate-set strict change; we omit such a subcase as it adds no SV5 content beyond what this subsection demonstrates.)

*Two-span, non-injective scenario.* The preceding subcases used a single span (m = 1) and an injective arrangement. We now exercise SV11 with m = 2 spans and a non-injective Σ.M(d) to expose the cover-not-partition behaviour and the m · p decomposition bound.

Re-take the initial five-address content store with a₁ < a₂ < a₃ < a₄ < a₅ all sharing one origin and one tumbler length. Extend the document's arrangement with two additional V-positions v₆, v₇ that share I-addresses with v₂ and v₃ (within-document sharing, S5):

`M(d) = {v₁ ↦ a₁, v₂ ↦ a₂, v₃ ↦ a₃, v₄ ↦ a₄, v₅ ↦ a₅, v₆ ↦ a₂, v₇ ↦ a₃}`

with v₁ < v₂ < v₃ < v₄ < v₅ < v₆ < v₇. The maximally merged block decomposition of this restriction has p = 2 blocks: β₁ = (v₁, a₁, 5) covering v₁..v₅ with I-extent {a₁, a₂, a₃, a₄, a₅}, and β₂ = (v₆, a₂, 2) covering v₆..v₇ with I-extent {a₂, a₃}. We verify the M7 merge condition for β₁ and β₂ fails. *V-adjacency holds:* v₆ = shift(v₁, 5), i.e., v₆ is the V-position immediately after β₁'s last covered position v₅, so the V-side of M7 is satisfied. *I-adjacency fails:* M7 would require a₂ = a₁ + 5, but a₁ + 5 = a₅ + 1 ≠ a₂ (a₂ falls strictly inside the a₁..a₅ sequence, not past it; here `+ n` denotes the OrdinalShiftBase numeric shift on I-side tumblers, matching the convention used elsewhere in this section). With one of the two adjacency conjuncts violated, M7 cannot succeed and M12's split rule forces the block boundary at v₅ → v₆. The two blocks share I-addresses {a₂, a₃} — this is the non-injective signature.

Define a two-span endset `e = {(s₁, ℓ₁), (s₂, ℓ₂)}` with s₁ = a₁, ℓ₁ = a₃ ⊖ a₁ (reach a₃) and s₂ = a₃, ℓ₂ = a₅ ⊖ a₃ (reach a₅). Then `coverage(e) = ⟦(a₁, ℓ₁)⟧ ∪ ⟦(a₃, ℓ₂)⟧` and among the allocated I-addresses this contains {a₁, a₂, a₃, a₄}. The two spans abut at a₃: the first contains a₁, a₂ (interval `[a₁, a₃)`), the second contains a₃, a₄ (interval `[a₃, a₅)`).

*SV11 decomposition.* With m = 2 and p = 2 there are m · p = 4 decomposition terms:

- ⟦(a₁, ℓ₁)⟧ ∩ I(β₁) = {a₁, a₂} (span 1 within block 1)
- ⟦(a₃, ℓ₂)⟧ ∩ I(β₁) = {a₃, a₄} (span 2 within block 1)
- ⟦(a₁, ℓ₁)⟧ ∩ I(β₂) = {a₂} (span 1 within block 2; β₂'s extent is {a₂, a₃}, intersected with `[a₁, a₃)`)
- ⟦(a₃, ℓ₂)⟧ ∩ I(β₂) = {a₃} (span 2 within block 2)

Union: `π_text(e, d) = {a₁, a₂} ∪ {a₃, a₄} ∪ {a₂} ∪ {a₃} = {a₁, a₂, a₃, a₄}`. ✓

*Cover, not partition.* Summed term widths: 2 + 2 + 1 + 1 = 6, but |π_text(e, d)| = 4. The addresses a₂ and a₃ each occur in two terms — once per block — because the non-injective arrangement places these I-addresses in both β₁ and β₂. The fragments are not disjoint as sets; the set-union formula remains correct because set union is idempotent.

*Maximal fragments.* Within β₁, the two terms {a₁, a₂} and {a₃, a₄} are adjacent in the ordinal sequence of I(β₁) (which is a₁ < a₂ < a₃ < a₄ < a₅), so they coalesce into a single maximal fragment {a₁, a₂, a₃, a₄}. Within β₂, the terms {a₂} and {a₃} are adjacent in the ordinal sequence of I(β₂) (a₂ < a₃) and coalesce into {a₂, a₃}. By the Maximal Endset Fragment definition (Partial Survival section), each fragment is a maximal contiguous subsequence *within a single mapping block's ordinal sequence*, so the same I-addresses {a₂, a₃} that appear in both β₁'s and β₂'s I-extents are counted once per block they belong to — yielding two distinct fragments rather than one merged across blocks. The fragment count is 2 — strictly less than the non-empty-term count (4) and the m · p upper bound (4) — because adjacency within blocks merges term-level contiguous regions, while non-injective sharing introduces no new fragments beyond those each block independently contributes. *Attribution of the 4 → 2 gap (witness-specific).* In this witness, the 4 → 2 gap arises entirely from mechanism (b) within each block: each of the two blocks coalesces its 2 non-empty terms into 1 maximal fragment (2 → 1 per block, summing to 4 → 2 across blocks). Non-injective cross-block sharing — the appearance of {a₂, a₃} in both β₁ and β₂ — affects |π_text(e, d)| via union idempotence (duplicate occurrences collapse in the union) but contributes no within-block coalescence: the fragment count is a per-block sum of maximal-contiguous-region counts, computed independently in each block from that block's own non-empty terms; cross-block I-address sharing affects |π_text| but not the count of maximal contiguous regions inside any single block. The cross-block sharing's quantitative footprint is the term-cardinality inflation, captured by the structural identity `Σ_{j,k} |term_{j,k}| − |π_text(e, d)| = Σ_{a ∈ π_text(e, d)} (s_a · m_a − 1)`, where m_a is the number of blocks whose I-extent contains a and s_a is the number of spans whose coverage contains a — each I-address a ∈ π_text appears in exactly s_a · m_a decomposition terms (one per (covering span, containing block) pair), summing to `Σ_{j,k} |term_{j,k}| = Σ_{a ∈ π_text} s_a · m_a` and yielding the inflation by subtracting `|π_text| = Σ_{a ∈ π_text} 1`. In this witness s_a = 1 for every a ∈ π_text (the two spans (a₁, ℓ₁) and (a₃, ℓ₂) abut at a₃ but do not overlap, so each I-address in π_text is covered by exactly one span), so the identity reduces to `Σ_{a ∈ π_text} (m_a − 1)` = (1−1) + (2−1) + (2−1) + (1−1) = 2 = 6 − 4, matching the inflation `Σ_{j,k} |term_{j,k}| − |π_text| = 6 − 4`. *Scope of the witness-specific separation.* The clean independence here — mechanism (b) acting per-block on within-block fragment count, non-injective sharing acting on term-cardinality inflation — is a feature of *this* (B, e), not a general separation theorem. The witness arranges the two attribution components on disjoint variables (spans pairwise disjoint over π_text and blocks with overlapping but non-coalescing-via-spans I-extents), so they do not interact. A different (B, e) — e.g., one whose spans overlap at I-addresses that also appear in multiple blocks — would not isolate mechanism (b) from non-injective sharing as cleanly; the present paragraph's attribution applies to this witness only. *The structural identity itself is general:* `Σ_{j,k} |term_{j,k}| − |π_text(e, d)| = Σ_{a ∈ π_text(e, d)} (s_a · m_a − 1)` holds for every (B, e) by the same counting argument (each a ∈ π_text appears in exactly s_a · m_a decomposition terms — one per (covering span, containing block) pair — by definition of term, regardless of any incidental disjointness of the witness's spans or coalescence of its blocks). Only the *clean separation* between mechanism (b) and non-injective sharing is witness-specific. The structural identity captures the *term-cardinality inflation* `Σ_{j,k} |term_{j,k}| − |π_text(e, d)|` — a separate quantity from the *term-vs-fragment count gap* `m · p − (fragment count)`, which is governed by mechanisms (a) and (b) of the SV11 biconditional. The two quantities coincide numerically here (both equal 2) by coincidence of this witness's parameters, not as a general identity: a single-span (m = 1) endset whose covered I-address sits in two non-injectively shared blocks (p = 2, each block of size 1 holding the same I-address; M7 fails because I-adjacency requires the next sibling, not a self-loop) yields inflation `Σ|term| − |π_text| = 2 − 1 = 1` while the gap `m · p − (fragment count) = 2 − 2 = 0` (each block independently contributes one maximal fragment) — the two quantities diverge whenever s_a · m_a ≠ 1 + (fragment-contribution of a per block), and the structural identity decomposes the inflation but not the gap.

*Resolution.* `locate(e, d) = {v ∈ dom(M(d)) : M(d)(v) ∈ coverage(e)} = {v₁, v₂, v₃, v₄, v₆, v₇}` — both occurrences of a₂ (at v₂ and v₆) and both occurrences of a₃ (at v₃ and v₇) enter the locate set, while v₅ ↦ a₅ does not (a₅ ∉ coverage(e)). Thus |locate(e, d)| = 6 > 4 = |π(e, d)|, exhibiting the inequality `|locate(e, d)| ≥ |π(e, d)|` from the Endset Projection section under within-document sharing.

*Three-span variant exhibiting mechanism (a).* The scenario above realises strictness via mechanism (b) only (coalescence within blocks). We now extend the endset to three spans so that mechanism (a) — some decomposition term `⟦(sⱼ, ℓⱼ)⟧ ∩ I(β_k)` empty — appears as well. Let a₆ and a₇ be two further T4-valid sibling tumblers past a₅ (with a₅ + 1 = a₆ and a₆ + 1 = a₇ in the ordinal sequence at the same tumbler length, sharing origin and tumbler length with a₁..a₅), *not* placed into dom(Σ.C) — no K.α step allocates them, paralleling the J0 (AllocationRequiresPlacement, ASN-0047) discipline used in the SV10 witness above, where sibling tumblers not destined for an M(d) mapping are left as well-defined tumblers in T rather than allocated by K.α and left orphaned (K.α without an accompanying K.μ⁺ for the freshly allocated address would falsify the composite against J0). Σ.C, M(d), and B = {β₁, β₂} are therefore all unchanged from the two-span case: dom(Σ.C) is still {a₁, ..., a₅}, ran_text(M(d)) is still {a₁, ..., a₅}, the maximally merged block decomposition B = {β₁, β₂} is unchanged (M11/M12 evaluate on M(d), not on Σ.C), and the I-extents are still I(β₁) = {a₁, ..., a₅} and I(β₂) = {a₂, a₃}. Define a third span (s₃, ℓ₃) = (a₆, a₈ ⊖ a₆) with reach a₈ — the next ordinal sibling beyond a₇; D0 is satisfied since a₆ and a₈ share origin and tumbler length, and neither a₆ nor a₈ need be allocated for the span to be well-formed: L4 (EndsetGenerality, ASN-0043) admits span endpoints regardless of allocation status, and T12 (SpanWellDefinedness, ASN-0034) discharges via its four preconditions on the span (s₃, ℓ₃) = (a₆, a₈ ⊖ a₆): (i) `a₆ ∈ T` holds by construction (a₆ is introduced above as a T4-valid sibling past a₅); (ii) `a₈ ⊖ a₆ ∈ T` holds by TA2 (WellDefinedSubtraction, ASN-0034), since a₆ ≤ a₈ as same-length siblings sharing origin; (iii) `Pos(a₈ ⊖ a₆)` holds because the difference at position #a₆ is 2 > 0 (a₈ is two ordinal positions past a₆, via a₇); (iv) `actionPoint(a₈ ⊖ a₆) ≤ #a₆` holds because a₆ and a₈ diverge only at their last component, so the action point of their difference equals #a₆. Then `⟦(s₃, ℓ₃)⟧ = [a₆, a₈)` in the ordinal sequence, an interval strictly past a₅ and therefore disjoint from both I(β₁) and I(β₂); the SV11 decomposition below depends only on the span denotations (determined by tumbler arithmetic under T12) and the block I-extents (determined by M(d), unchanged here), so the unallocated status of a₆, a₇, a₈ does not affect any decomposition term.

The extended endset e′ = {(s₁, ℓ₁), (s₂, ℓ₂), (s₃, ℓ₃)} has m = 3 spans and p = 2 blocks, so m · p = 6 decomposition terms:

- ⟦(s₁, ℓ₁)⟧ ∩ I(β₁) = {a₁, a₂}
- ⟦(s₂, ℓ₂)⟧ ∩ I(β₁) = {a₃, a₄}
- ⟦(s₃, ℓ₃)⟧ ∩ I(β₁) = ∅       *(empty — mechanism (a))*
- ⟦(s₁, ℓ₁)⟧ ∩ I(β₂) = {a₂}
- ⟦(s₂, ℓ₂)⟧ ∩ I(β₂) = {a₃}
- ⟦(s₃, ℓ₃)⟧ ∩ I(β₂) = ∅       *(empty — mechanism (a))*

The union remains `π_text(e′, d) = {a₁, a₂, a₃, a₄}` — the third span contributes nothing because its coverage interval is disjoint from every block's I-extent. The maximal fragment count is still 2 (the within-block coalescences from the two-span case persist unchanged), strictly below the m · p = 6 ceiling. The strictness gap (6 − 2 = 4) is now jointly attributable: 2 terms vanish by mechanism (a) (the (3, 1) and (3, 2) terms), and 2 further units of slack come from mechanism (b) (each block coalesces its 2 non-empty terms into 1 fragment). This variant exhibits both mechanisms simultaneously and confirms SV11's biconditional in its sharpest reading — either source of strictness suffices on its own; here both are present.

*Standalone SV6 verification with explicit tumbler arithmetic.* This subsection verifies SV6 with explicit tumbler arithmetic on a *separate* set of tumblers from the worked example's a₁..a₅; the values introduced here (s, ℓ, t, b) do not relate to a₁..a₅ above. The SV10 witness elsewhere in this note carries the SV6 application in a context contiguous with that witness's tumblers; the present subsection is a tumbler-arithmetic check selected for clarity of the cross-origin exclusion, not a continuation of the worked example. *Subspace note.* SV6's statement and proof are *structural* — they rest on the four-field tumbler decomposition (T4, ASN-0034) and the origin function `origin(t) = N(t).0.U(t).0.D(t)` (ASN-0036, S7), which act on T4-valid element-level tumblers regardless of subspace. The *first element-field* component of an I-address — position E₁ in the element-field decomposition, which for a T4-valid element-level tumbler with separators at p₁ < p₂ < p₃ sits at position p₃ + 1 (the first component after the third field separator) — selects the subspace (content vs. link, per the K.α / K.λ amendments of ASN-0047 which require `fields(a).E₁ = s_C` for content allocations and `fields(a).E₁ = s_L` for link allocations). The first component of the tumbler proper (position 1) is the *node* identifier, not the subspace selector — these two positions are distinct and the field-separator-and-element-field layout that SV6 reasons about begins at the node field, the same in every subspace. The example below uses tumblers whose first element-field component E₁ = 1; under the parameter choice s_C = 1 fixed throughout this ASN's witnesses, this places each tumbler in the content subspace, consistently with the SV10 witness — but no part of the SV6 verification depends on the subspace choice. The same arithmetic with E₁ = s_L (link subspace) would produce an analogous cross-origin exclusion within the link subspace; SV6 is subspace-agnostic. Let s = 1.0.1.0.1.0.1.2.3 — nine components; the zeros at positions 2, 4, 6 are field separators, so p₃ = 6. Let ℓ = 0.0.0.0.0.0.0.0.5 — action point k = 9 (the first nonzero component), and k = 9 > 6 = p₃. By TumblerAdd, positions 1 through 8 are copied from s, and position 9 advances: reach = s ⊕ ℓ = 1.0.1.0.1.0.1.2.8. We verify the sandwich: reach agrees with s on positions 1 through 8, confirming that the three field separators (positions 2, 4, 6) are preserved.

Consider t = 1.0.1.0.1.0.1.2.5. We have s ≤ t (agree on positions 1–8; at position 9, t₉ = 5 > 3 = s₉) and t < reach (agree on positions 1–8; at position 9, t₉ = 5 < 8 = reach₉). So t ∈ ⟦(s, ℓ)⟧. The field separators of t are at positions 2, 4, 6 — matching s — so origin(t) = 1.0.1.0.1 = origin(s). ✓

Now consider b = 1.0.1.0.2.0.1.2.5, a different-origin address with origin(b) = 1.0.1.0.2. We compare b with reach = 1.0.1.0.1.0.1.2.8: they agree on positions 1–4; at position 5, b₅ = 2 > 1 = reach₅. By T1(i), b > reach, so b ∉ ⟦(s, ℓ)⟧. The SV6 precondition k > p₃ ensures that the element-field action point cannot advance the document-prefix components: reach differs from s only at positions ≥ k = 9 > 6 = p₃, so no different-origin address can slip between s and reach. ✓


## Content Fidelity

The preceding analysis addresses the *extent* of what survives — how many I-addresses remain in the projection. We now address the *identity* of what survives: is the content at those addresses the same as when the link was created?

**Corollary (ContentFidelity, from S0, ASN-0036).** Content fidelity is guaranteed directly by the foundation: for every a ∈ dom(Σ.C) and every state transition Σ → Σ', a ∈ dom(Σ'.C) and Σ'.C(a) = Σ.C(a). We do not assign this a new SV label because it introduces no new content beyond S0 — it is S0 read for its survivability content. Applied to endset I-addresses: for any link a ∈ dom(Σ.L) created at state Σ_k, and any later state Σ_j with j ≥ k, `(A i : i ∈ coverage(Σ.L(a).s) ∩ dom(Σ_k.C) : Σ_j.C(i) = Σ_k.C(i))` for every endset slot s. The survivability implication merits emphasis: whatever portion of the endset remains visible in a document's arrangement, the content at those I-addresses is *exactly* what was there when the link was created. No edit, no revision, no amount of rearrangement can alter the content the link references. The surviving fragment may be smaller than the original endset, but each byte in the fragment is identical to the original.

The guarantee is the strongest possible short of cryptographic verification: the system's fundamental architecture makes it impossible to change content at an I-address through any defined operation.

The guarantee is architectural rather than cryptographic — there is no hash or signature that a client can verify independently. The guarantee rests on the structural property that the content store is append-only (S1, StoreMonotonicity) and values are frozen at allocation (S0). Nelson explicitly acknowledges this is contractual trust, not mathematical proof of non-tampering [LM 5/17–18].


## Weakest Precondition Analysis

The forward claims SV2–SV5 each have a natural reading as a *weakest precondition*. Given an elementary transition K and a postcondition R, write wp(K, R) for the weakest predicate on the *pre-state Σ and the transition's parameters* such that R holds in every reachable post-state. Each elementary transition in K is parameterised by what it adds or removes (an inserted V↦I mapping, a set of removed V-positions, an allocated address, a reordering bijection), and the wp expression names those parameters explicitly so that the predicate is evaluated entirely in the pre-state. The wp form makes precise what an editor must guarantee *before* acting in order not to falsify R afterward. The forward claims SV2–SV5 give us each wp by direct rearrangement; we are recasting, not introducing new content. The "vitality loss condition" already developed in the SV3 discussion is essentially the wp of K.μ⁻ for non-vitality.

We take the postcondition R = "endset e remains vital in d after the transition", formally `π_{Σ'}(e, d) ≠ ∅`, and read off wp for each elementary transition. We restrict to non-empty endsets, as the empty case is degenerate (the section above).

- **wp(K.μ⁻ removing V_rm ⊆ dom(Σ.M(d)), π(e, d) ≠ ∅) = `(E v : v ∈ dom(Σ.M(d)) \ V_rm : Σ.M(d)(v) ∈ coverage(e))`** — *under the domain-of-applicability precondition that V_rm is D-SEQ-admissible:* for every subspace S, V_rm ∩ V_S(d) is either an upward tail `{[S, 1, ..., 1, k] : n'_S < k ≤ n_S}` of V_S(d) for some 0 ≤ n'_S ≤ n_S, or empty (D-SEQ, ASN-0047). Arbitrary V_rm that violate D-SEQ are not valid K.μ⁻ parameters in the first place — the transition is not enabled — and the wp expression is meaningful only on the admissible parameter space. So K.μ⁻ removes a non-empty set V_rm of V-positions from the maximum end of V_S(d) per subspace, as enforced by D-SEQ, and vitality is preserved iff some V-position *not* in V_rm carries an I-address in coverage(e). The negation — the *vitality-loss* condition — is `(A v : v ∈ dom(Σ.M(d)) \ V_rm :: Σ.M(d)(v) ∉ coverage(e))` together with `(E v : v ∈ V_rm : Σ.M(d)(v) ∈ coverage(e))` (so the endset was vital pre-transition but every contributing V-position was removed). Both conjuncts are pre-state predicates over Σ and the D-SEQ-admissible removal parameter V_rm. This is Nelson's "if anything is left at each end" condition in formal dress. The wp form applies uniformly to content-subspace and link-subspace contractions: K.μ⁻ acts per-subspace on dom(M(d)) with D-SEQ enforcing upward-tail removal in each V_S(d) independently, and the coverage predicate `Σ.M(d)(v) ∈ coverage(e)` is well-formed for any endset whose coverage may include link addresses (admitted by L4, EndsetGenerality, with the reflexive-addressing case isolated by L13, ReflexiveAddressing). So V_rm ⊆ V_{s_L}(d) contracting a link-subspace tail can falsify vitality of an endset whose coverage references those link addresses by the same condition stated above for content-subspace tails — the formula is subspace-agnostic.

- **wp(K.μ⁺ adding extension Δ = M'(d) ↾ (dom(M'(d)) \ dom(Σ.M(d))) with new I-address set I_new = ran(Δ) ⊆ dom(Σ.C), π(e, d) ≠ ∅) = `π(e, d) ≠ ∅ ∨ coverage(e) ∩ I_new ≠ ∅`** where Δ is the set of new V↦I mappings introduced by the transition (K.μ⁺ in ASN-0047 admits any extension `dom(M'(d)) ⊃ dom(M(d))` with value preservation at existing positions, so Δ may contain one or many new mappings; the precondition `(A v ∈ dom(Δ) : Δ(v) ∈ dom(Σ.C))` is required by K.μ⁺'s referential integrity). Extension preserves vitality if it already held in the pre-state *or* if at least one new I-address lies in coverage(e). The single-mapping specialisation Δ = {(v_new, i_new)} yields I_new = {i_new} and the formula reduces to `π(e, d) ≠ ∅ ∨ i_new ∈ coverage(e)`. For the typical case of an endset already vital before the extension, the first disjunct is satisfied and the wp reduces to the pre-state vitality regardless of the size of Δ.

- **wp(K.μ⁺_L adding v_ℓ ↦ ℓ, π(e, d) ≠ ∅) = `π(e, d) ≠ ∅ ∨ ℓ ∈ coverage(e)`** where ℓ is the link address added by the transition. For endsets whose coverage lies entirely in dom(Σ.C) (the typical content endset), the second disjunct is unreachable and the wp reduces to pre-state vitality.

- **wp(K.μ~ under bijection ψ, π(e, d) ≠ ∅) = `π(e, d) ≠ ∅`.** Reordering preserves ran(M(d)) (SV5), so the precondition is identical to the postcondition and does not depend on ψ.

- **wp(K.α, π(e, d) ≠ ∅) = `π(e, d) ≠ ∅`** and **wp(K.δ, π(e, d) ≠ ∅) = `π(e, d) ≠ ∅`** (for any d carried over by the frame) and **wp(K.ρ, π(e, d) ≠ ∅) = `π(e, d) ≠ ∅`** and **wp(K.λ, π(e, d) ≠ ∅) = `π(e, d) ≠ ∅`** for every pre-existing endset e. Each of these transitions preserves M in its frame, so π is unchanged for every endset that existed prior to the transition.

The aggregate observation: only K.μ⁻ can falsify vitality, and only by a specific characterised action — every pre-state V-position contributing to coverage(e) being among the set V_rm removed by the contraction. Every other elementary transition either trivially preserves vitality (M-frame: K.α, K.δ, K.λ, K.ρ) or can only enlarge the projection (K.μ⁺, K.μ⁺_L). The distinguished composite K.μ~ preserves vitality at composite endpoints (its K.μ⁻ stage may shrink π and the K.μ⁺ stage restores it), so its composite-level wp is also `π(e, d) ≠ ∅`. The wp framework therefore localises the *single* operation that places vitality at risk and gives the *exact* pre-state condition under which the risk materialises.

For discovery, the corresponding wp values follow the same pattern with discover_s in place of π. Because discover_s depends only on coverage(e) and dom(Σ.L), every transition that holds L in frame preserves discover_s pointwise:

- **wp(K, a ∈ discover_s(A)) = `a ∈ discover_s(A)`** for every L-frame elementary K ∈ {K.α, K.δ, K.μ⁺, K.μ⁺_L, K.μ⁻, K.ρ} and for the distinguished composite K.μ~ (L-frame as a composite of L-frame elementaries), for every fixed A. Discovery is invariant under arrangement and content operations.

- **wp(K.λ allocating a_new with L_new(a_new) = (F_new, G_new, Θ_new), a ∈ discover_s(A)) = `a ∈ discover_s(A) ∨ (a = a_new ∧ coverage(L_new.s) ∩ A ≠ ∅)`** where a_new is the newly allocated link address and L_new is the corresponding endset tuple. K.λ can extend dom(L) by exactly one entry, so a previously-non-discoverable a becomes discoverable only when it is the newly allocated link and the new link's endset shares an I-address with A.

The discovery wp formalises SV7–SV9: invariance under L-frame transitions, monotonic growth under K.λ, permanence (SV8) as wp instance for any K. Nothing new is established beyond the forward claims; the reformulation makes the *direction of dependency* explicit — for a postcondition concerning resolution or discovery, we can read off the corresponding pre-state requirement directly from the transition's parameters and frame.


## The Complete Guarantee

We can now synthesize the survivability guarantee into a single coherent statement.

**SV13 (SurvivabilityTheorem).** For a link a ∈ dom(Σ.L) with Σ.L(a) = (F, G, Θ), and for any state transition Σ → Σ':

(a) *The link persists:* a ∈ dom(Σ'.L) and Σ'.L(a) = (F, G, Θ). [L12]

(b) *Endset coverage is invariant:* coverage(F), coverage(G), coverage(Θ) are the same in Σ' as in Σ. [L12, ASN-0043]

(c) *Content at endset addresses is unchanged:* for every I-address i in any endset's coverage, Σ'.C(i) = Σ.C(i) when i ∈ dom(Σ.C). [S0, ASN-0036]

(d) *Discovery is permanent:* if a ∈ discover_s(A) in Σ for some fixed A, then a ∈ discover_s(A) in Σ'. [SV8]

(e) *Projection and resolution are arrangement-dependent:*
- Extension of M(d) — whether K.μ⁺ (content subspace) or K.μ⁺_L (link subspace) — can only enlarge π(e, d) and locate(e, d). [SV2]
- Contraction of M(d) can only shrink π(e, d) and locate(e, d). [SV3]
- Reordering of M(d) — via the *distinguished composite* K.μ~, which expands into a K.μ⁻ + K.μ⁺ pair under ASN-0047, not an elementary transition — has two related but distinct consequences, which we record as separate sub-bullets:
  - *Composite-endpoint π-invariance, with per-step variability.* K.μ~ preserves π(e, d) exactly *at the composite endpoints* (pre-state Σ and post-state Σ' bracketing the full K.μ~ composite): the I-addresses present in the projection are unchanged from Σ to Σ'. The per-step picture is genuinely different — across the composite's elementary stages, per-step π is *not* invariant: it shrinks at the K.μ⁻ internal stage (which removes V-positions from dom(M(d)), reducing ran(M(d)) by the dropped I-addresses) and recovers at the K.μ⁺ internal stage (which re-introduces those V-positions, restoring ran(M(d))). The SV5 composite-level scope subsection in this note carries the per-stage account of the intermediate state, which applies here term-for-term. The composite-endpoint claim above does *not* assert per-step invariance; it asserts only that the K.μ⁻ shrinkage and the K.μ⁺ recovery cancel exactly at the composite's endpoints. [SV5 for π-invariance at endpoints; SV5's composite-level scope note for the per-step account]
  - *Locate-set transformation via the bijection ψ.* The locate *set* may change across K.μ~, and the transformation is fully characterised by `locate_{Σ'}(e, d) = {ψ(v) : v ∈ locate_Σ(e, d)}` where ψ is the reordering bijection from K.μ~. By K.μ~-FIX (ASN-0047), ψ acts on a fixed domain — dom(M'(d)) = dom(M(d)) — so the V-position arena is invariant and only the V↦I assignment is permuted. The locate set changes whenever ψ maps a V-position inside locate_Σ(e, d) to one outside it, or vice versa; otherwise the {ψ(v) : v ∈ locate_Σ(e, d)} expression reduces to an automorphism of the locate set. [SV5b for the locate transformation]
- Changes to M(d) cannot affect locate(e, d') for d' ≠ d. [SV4]
- K.α, K.δ, K.ρ, and K.λ — the M-frame transitions, distinct from K.μ⁺ and K.μ⁺_L which modify M at their targeted document d (both covered under Extension above; the *K.μ⁺_L parallel* note below additionally elaborates the per-document parallel with K.δ for the dom(M)-extension aspect) — all preserve M-values in their frame, so locate(e, d) is unchanged for every endset e and every pre-existing document d ∈ dom(Σ.M) carried over the transition. K.δ additionally introduces a new document d_new with Σ'.M(d_new) = ∅ (ASN-0047); the locate function on the empty arrangement is empty by definition, so `locate_{Σ'}(e, d_new) = ∅` at Σ' for every endset e, and the new-document slot is well-defined immediately at Σ' without any state-priming step. *K.μ⁺_L parallel.* Both K.δ and K.μ⁺_L extend a domain while preserving existing M-values, differing only in which domain they extend: K.δ enlarges dom(M) by allocating a new document d_new (handled above), while K.μ⁺_L enlarges dom(M(d)) by adding a fresh V-position v_ℓ in the link subspace under an existing d, preserving the prior V↦I assignments on dom(Σ.M(d)). The consequences for locate diverge only because of what each extension places: K.δ seeds an *empty* arrangement at d_new (so `locate_{Σ'}(e, d_new) = ∅` trivially, as already stated); K.μ⁺_L places a single new mapping `v_ℓ ↦ ℓ` at d, so `locate_{Σ'}(e, d)` can strictly grow over `locate_Σ(e, d)` if ℓ ∈ coverage(e), which is the SV2-extension content already stated in the bullet covering K.μ⁺/K.μ⁺_L. K.λ additionally creates a new link, and the locate and discover_s sets for its *new* endsets are evaluated against the unchanged M for the first time — see SV9 for the resulting monotonic growth of discover_s and SV7 for invariance under every transition *except* K.λ.

  *Corollary (NewLinkEvaluationDefinedness).* For a link a_new allocated by K.λ at Σ → Σ' with Σ'.L(a_new) = (F_new, G_new, Θ_new), every slot s ∈ {from, to, type} and every document d ∈ dom(Σ'.M) yield well-defined values `locate(Σ'.L(a_new).s, d)` and `discover_s(A)` (with a_new admissible to enter) immediately at Σ' — the former by evaluation against the K.λ-unchanged M(d) and the new endset's coverage, the latter by evaluation against the K.λ-extended dom(L). No additional state-priming step is required to read the new link's projection, location, or discoverability; the read is well-defined as soon as K.λ commits.

  *Proof.* The two evaluation forms each decompose into four definedness obligations, each discharged by a one-line citation.

  - *`Σ'.L(a_new)` is defined at Σ'.* K.λ's effect (LinkAllocation, ASN-0047) is exactly `Σ'.L = Σ.L ⊕ {a_new ↦ (F_new, G_new, Θ_new)}`, so a_new ∈ dom(Σ'.L) and Σ'.L(a_new) = (F_new, G_new, Θ_new) immediately at Σ'.
  - *`.s` is well-defined for s ∈ {from, to, type}.* L3 (NEndsetStructure, ASN-0043) requires `|Σ'.L(a_new)| ≥ 3`, and the K.λ-amendment of ASN-0047 records the value as a standard triple `(F_new, G_new, Θ_new)`, so the slot projection `.from`, `.to`, `.type` is defined as the first, second, and third tuple component respectively.
  - *`coverage(Σ'.L(a_new).s)` is a well-defined subset of T.* L4 (EndsetGenerality, ASN-0043) makes each slot value a set of spans over T, and `coverage(e) = ⋃_{(s, ℓ) ∈ e} ⟦(s, ℓ)⟧` is well-defined under T12 (SpanWellDefinedness, ASN-0034) for every span; the union is well-defined as a set operation.
  - *`Σ'.M(d)` is well-defined for d ∈ dom(Σ'.M).* K.λ holds M in frame (per K.λ's per-document frame in ASN-0047: `(A d' :: Σ'.M(d') = Σ.M(d'))`), so `Σ'.M(d) = Σ.M(d)` for every d ∈ dom(Σ.M) = dom(Σ'.M), and `Σ'.M(d)` is well-defined at every d in the post-state arrangement domain.

  These four supply the well-definedness of `locate(Σ'.L(a_new).s, d) = {v ∈ dom(Σ'.M(d)) : Σ'.M(d)(v) ∈ coverage(Σ'.L(a_new).s)}` immediately at Σ'. For `discover_s(A)` with a_new admissible to enter: `discover_s(A) in Σ' = {a ∈ dom(Σ'.L) : coverage(Σ'.L(a).s) ∩ A ≠ ∅}` is well-defined because dom(Σ'.L) is well-defined (K.λ extends dom(L) by exactly one entry), the coverage of every slot value is well-defined by L4/T12 as above, and set membership against the fixed I-address set A is decidable; whether a_new ∈ discover_s(A) is then determined by `coverage(F_new).s ∩ A ≠ ∅` per the discover_s definition. No state-priming step (between K.λ committing and the evaluation read) is referenced by any of the four citations, so the read is well-defined at the immediate post-state. ∎

(f) *Cross-origin coverage exclusion:* new allocations from a different origin cannot enter existing endset spans when the span start is element-level and the span's action point `k` satisfies `k > p₃` (strictly beyond the third field separator — i.e., the leading `k − 1` components of the span start contain all three field separators, placing the action point structurally within the element field rather than within the node/account/document prefix). The exclusion is scoped to *element-level allocations* — addresses b with zeros(b) = 3 (per the SV6 precondition) — and to *element-level spans* — those with k > p₃. Broader-level spans (k ≤ p₃, with the action point at or before the third field separator) are admitted by L4 (EndsetGenerality, ASN-0043) but are not closed by this clause; their cross-prefix coverage behaviour is determined by the allocator discipline of the responsible address-hierarchy level and is deferred to ASN-0034. [SV6]

*Remark (same-origin coverage growth).* Same-origin coverage growth depends on the allocation regime. At the byte level, sequential sibling allocation closes existing spans whose coverage is fully allocated (tight spans) to future sibling allocations; spans whose reach extends beyond the current allocation maximum remain open to sequential overshoot, and child-depth allocation (TA5(d) with k' > 0) can enter any span containing the parent address. At broader address levels, coverage growth is open by design. See the detailed analysis in the "Content Allocation and Coverage Stability" section.

(g) *Partial survival is well-structured:* the surviving text-subspace projection in any document is the union of *exactly* m · p decomposition terms `⟦(sⱼ, ℓⱼ)⟧ ∩ I(β_k)` (at the post-transition state's block count p; some possibly empty), equal as a set to the union of *at most* m · p maximal ordinal-contiguous fragments within mapping blocks (the same post-transition p). The count inequality is strict whenever (a) some decomposition term is empty or (b) two non-empty terms within a single block are ordinally adjacent or overlap, coalescing into a single maximal fragment. The two-span non-injective worked example below exhibits mechanism (b) — 4 non-empty terms coalescing to 2 maximal fragments — and its three-span extension additionally exhibits mechanism (a), with two empty decomposition terms contributed by a span whose reach lies in an interval disjoint from both block I-extents. The collection is a cover — not necessarily a partition — due to non-injective arrangements. [SV11]

*Caveat — m · p is state-dependent, not a system-level fragment-count invariant.* The bound m · p applies at each state under that state's own values of m and p, not as a fixed quantity across the lifetime of the link or document. The span count m is frozen at link creation by L12 (LinkImmutability), but the block count p is a property of the *current* arrangement M(d): composite edits that fragment an existing block — e.g., a K.μ~ + K.μ⁻ sequence excising interior content, which can replace a single block by two — raise p, and the m · p ceiling at the new state rises accordingly. The same applies in reverse: a composite that merges adjacent blocks lowers p and lowers the ceiling. Two consequences worth recording: (i) one cannot quote a fragment-count upper bound for a link in isolation; the bound must be quoted against the document state at which it is read. (ii) the per-state bound never claims that *some* fragment count survives across transitions — only that whatever count is realised lies under m · p evaluated at the *post*-transition state.

(h) *Bilateral vitality survives extension and reordering; only contraction can falsify it.* For a link with `F ≠ ∅`, `G ≠ ∅`, `π_Σ(F, d) ≠ ∅`, `π_Σ(G, d) ≠ ∅` at Σ (the bilateral-vitality predicate read at Σ, per the Endset Projection section's definition), the elementary-transition behaviour follows by slot-wise application of SV2–SV5:

- *K.μ⁺ / K.μ⁺_L extension.* Both content-subspace and link-subspace extension preserve `π(F, d) ≠ ∅ ∧ π(G, d) ≠ ∅` by SV2 applied slot-by-slot; the extension cannot remove any I-address from either projection. Bilateral vitality survives.
- *K.μ~ reordering.* The distinguished composite preserves `π(F, d) ≠ ∅ ∧ π(G, d) ≠ ∅` at composite endpoints by SV5 applied slot-by-slot — the I-range is unchanged, so both projections are unchanged as sets, and both vitality conjuncts carry through. (Per the SV5 composite-level scope note, per-step π may shrink at the K.μ⁻ internal stage and recover at the K.μ⁺ internal stage; bilateral vitality is read at the composite endpoints, where both sides recover.) Bilateral vitality survives.
- *K.μ⁻ contraction.* The only elementary transition that can falsify bilateral vitality. The wp form is the conjunction of two K.μ⁻-vitality wp conditions read per side: `(E v : v ∈ dom(Σ.M(d)) \ V_rm : Σ.M(d)(v) ∈ coverage(F))` *and* the symmetric clause for coverage(G), evaluated against the same D-SEQ-admissible removal V_rm. Bilateral vitality is lost exactly when at least one side's conjunct fails — when the contraction removes the last V-position whose I-address sits in coverage(F), or the last whose I-address sits in coverage(G), or both. Loss is one-sided sufficient: a single side's failure suffices, by the conjunction structure of bilateral vitality.
- *M-frame transitions K.α, K.δ, K.ρ, K.λ.* Each holds M(d) in frame at every pre-existing d, so both projections are unchanged for every endset existing at the pre-state, and bilateral vitality of every pre-existing link is preserved. K.λ additionally creates a new link, whose own bilateral vitality at Σ' is evaluated against the K.λ-unchanged M(d) and the new link's coverage — well-defined by NewLinkEvaluationDefinedness.

The corollary follows directly from SV2/SV4/SV5 (per-slot positive monotonicity and isolation) and SV3 (per-slot reduction); no additional foundation property is invoked. Downstream consumers — link-semantics ASNs and link-discovery policy notes that work with the literal-Nelson reading "anything left at each end" demanding both ends non-empty *and* both projecting — read this clause directly rather than re-deriving the slot-wise conjunction from SV2–SV5. [SV2, SV3, SV4, SV5 read per slot; BilateralVitality predicate from the Endset Projection section]

(i) *System-level discovery structure (beyond per-link permanence).* Clauses (a)–(h) above scope SV13 as a *per-link, per-transition* survivability guarantee; the system-level discovery picture is supplied by four further sub-clauses that complement (d)'s per-link permanence. Each sub-clause is independently citeable.

  - (i₁) *Invariance under L-frame transitions.* `discover_s(A) in Σ' = discover_s(A) in Σ` for every transition that holds the link store in frame — every L-frame elementary transition (K.α, K.δ, K.μ⁺, K.μ⁺_L, K.μ⁻, K.ρ) and the distinguished composite K.μ~ — so no resolution or content-allocation operation alters the discoverability of any existing link relative to any fixed I-address set A. The equality is two-sided: no L-frame transition introduces new discovery relationships and none removes any. [SV7]

  - (i₂) *Monotonic growth under K.λ.* The discoverable set is non-decreasing as the link store grows, with `discover_s(A) in Σ ⊆ discover_s(A) in Σ'` for any transition; strict growth occurs exactly when a freshly allocated link's slot-s endset shares an I-address with A. [SV9]

  - (i₃) *Discovery does not entail full projection.* A link discoverable through a query set A may project to only a subset of its coverage in the discovering document; the cross-document realisation — discovery succeeding in one document while resolution is empty in another — is exhibited by CrossDocumentDecoupling and is a structural feature of the discovery/resolution distinction rather than a defect. The discovery-projection independence applies even within a single document when the endset's coverage extends beyond `ran(Σ.M(d))`. [SV10, CrossDocumentDecoupling]

  - (i₄) *Document-derived discovery non-permanence.* `discover_through_s(d) = discover_s(ran(Σ.M(d)))` is *not permanent* — it inherits the per-link projection structure of clauses (e) above on its state-dependent argument ran(M(d)) rather than the fixed-A permanence of (d). Specifically: monotonic under K.μ⁺/K.μ⁺_L extension of d (links discoverable through d's pre-extension addresses remain discoverable, and new V-positions may add to the discoverable set); reduced under K.μ⁻ contraction of d (strict when contraction removes the last V-position contributing a discovered I-address — the link exits discover_through_s(d) for d while remaining in discover_s(A) for the unchanged A); isolated across d ≠ d' under arrangement transitions on d; and monotonic under K.λ via SV9 applied to the K.λ-fixed ran(M(d)). The per-link permanence of (d) and the document-derived non-permanence here are compatible because the two scopes have different quantifications: (d) fixes A and tracks the link's discoverability through that fixed A across transitions, while document-derived discovery tracks the link's discoverability through d's *current* arrangement, whose I-content varies under arrangement transitions. [SV14]

The survivability guarantee is therefore: the link, its endsets, and the content at its endset addresses are all permanent. What varies is the *visibility* of the endset content through each document's arrangement — and this variation is precisely characterised by the projection and resolution functions, which respond only to the arrangement of the specific document being queried and are immune to changes elsewhere.

Nelson's "strap between bytes" is exactly right. The strap (the link's endsets) is permanent, fastened to permanent bytes (I-addresses with immutable content). What moves is the string the bytes sit on — the document's Vstream arrangement. The strap follows the bytes, not the string.


## Properties Introduced

*Withdrawn labels.* SV0, SV1, and SV12 were used in earlier drafts and have been withdrawn during revision; their content was not deleted but relocated — SV0 and SV1 both into the NoStaleResolutionState architectural remark (their schema-inspection content is not a transition-induced survivability claim, so no SV number is attached), and SV12 into SV7 (DiscoveryInvarianceUnderLFrame) when transclusion-coupling absence was demoted from a standalone property to a corollary of L-frame invariance. The labels are not reused; the surviving SV labels retain their historical numbering so that external citations (consultations, reviews, downstream ASNs) remain stable.

| Label | Statement | Status |
|-------|-----------|--------|
| π(e, d) | Endset projection: `coverage(e) ∩ ran(M(d))` | introduced |
| locate(e, d) | Endset location: `{v ∈ dom(M(d)) : M(d)(v) ∈ coverage(e)}` | introduced |
| Resolution | Resolution of endset e in document d is the function `locate(e, d)`; "resolution" throughout the note refers to this function (distinct from "projection" which refers to π(e, d)) | introduced |
| Vitality | Endset e is vital in d when `π(e, d) ≠ ∅` | introduced |
| SlotwiseVitality | Link is slotwise vital in d when each non-empty content endset is vital in d (disjunctive form: `F = ∅ ∨ π(F, d) ≠ ∅` and symmetrically for G) — passes vacuously on empty sides | introduced |
| BilateralVitality | Link is bilaterally vital in d when both content endsets are non-empty *and* each projects non-emptily (`F ≠ ∅ ∧ π(F, d) ≠ ∅` and symmetrically for G) — strict form, Nelson's "anything left at each end" read literally | introduced |
| discover_s(A) | Link discovery: `{a ∈ dom(L) : coverage(L(a).s) ∩ A ≠ ∅}` | introduced |
| Decomposition term | `⟦(sⱼ, ℓⱼ)⟧ ∩ I(β_k)` — one per (span, block) pair, contiguous in I(β_k)'s ordinal sequence | introduced |
| Maximal fragment | maximal contiguous ordinal subsequence within a single mapping block's I-extent | introduced |
| NoStaleResolutionState | Architectural remark (not SV-labelled): no state component external to (coverage(e), current M(d)) participates in resolution; caching of historical V-positions is structurally precluded by the link-store and state-schema signatures | introduced |
| ArrangementLinkFrame | Corollary of L12 (ASN-0043): arrangement changes preserve L entirely | cited |
| TransclusionCouplingAbsence | Corollary of SV7: K.μ⁺ extending M(d₂) with v ↦ a (a ∈ ran(M(d₁))) inherits the link set L_a discoverable through a without any link-store coupling step | introduced |
| SV2 | ExtensionMonotonicity: K.μ⁺/K.μ⁺_L can only enlarge π(e, d) and locate(e, d) | introduced |
| SV3 | ContractionReduction: K.μ⁻ can only shrink π(e, d) and locate(e, d) | introduced |
| SV4 | ArrangementIsolation: arrangement changes to M(d) do not affect π(e, d') or locate(e, d') for d' ≠ d | introduced |
| SV5 | ReorderingProjectionInvariance: K.μ~ preserves π(e, d) exactly | introduced |
| SV5b | ReorderingLocateTransformation: under K.μ~, `locate_{Σ'}(e, d) = {ψ(v) : v ∈ locate_Σ(e, d)}` where ψ is the K.μ~ reordering bijection — the locate set transforms by ψ rather than being preserved as a set | introduced |
| SV6 | CrossOriginExclusion: allocations from a different document prefix cannot enter existing endset spans (within element field) | introduced |
| SV7 | DiscoveryInvarianceUnderLFrame: `discover_s(A) in Σ' = discover_s(A) in Σ` for every L-frame elementary transition (K.α, K.δ, K.μ⁺, K.μ⁺_L, K.μ⁻, K.ρ) and for the distinguished composite K.μ~ — discovery is invariant, not merely monotonic; transclusion-coupling absence is the corollary instance under K.μ⁺ | introduced |
| SV8 | DiscoveryPermanence: once discoverable through A, always discoverable | introduced |
| SV9 | DiscoveryMonotonicity: the discoverable set is non-decreasing as links are created | introduced |
| SV10 | DiscoveryProjectionIndependence: discovery in d does not entail full projection in d; the projection may be a proper subset of the endset's coverage when only some of its I-addresses are in ran(M(d)). The witness exhibits the projection inequality directly (π ⊊ coverage); the resolution-side asymmetry — locate's V-set is shaped by M(d), and missing I-addresses produce missing V-positions — follows by the locate definition rather than by a separate clause | introduced |
| SV11 | PartialSurvivalDecomposition: the text-subspace projection equals the union of *exactly* m·p decomposition terms (one per span-block pair, some possibly empty), equivalently the union of *at most* m·p maximal ordinal-contiguous fragments (strict whenever some term is empty or non-empty adjacent/overlapping terms coalesce within a block); the bound m·p is attained iff every (j,k) yields a non-empty term and these terms are pairwise non-adjacent and non-overlapping within each block; the collection is a cover, not necessarily a partition, under non-injective arrangements | introduced |
| SV14 | DocumentDerivedDiscoverySurvivability: discover_through_s(d) is monotonic under K.μ⁺/K.μ⁺_L (a), reduced under K.μ⁻ (b), isolated across documents under K.μ⁺/K.μ⁺_L/K.μ⁻/K.μ~ (c), not permanent — strict shrinkage realised when contraction removes the last V-position carrying a contributing I-address (d), and monotonic under K.λ via SV9 with M held in frame (e) | introduced |
| ContentFidelity | Corollary of S0 (ASN-0036): content at endset I-addresses is immutable | cited |
| CrossDocumentDecoupling | Corollary of SV10: discovery in one document and empty resolution in another are simultaneously realisable (witness extends the SV10 base state with a sibling document and a different-origin I-address) | introduced |
| SV13 | SurvivabilityTheorem: synthesis of the complete guarantee — per-link, per-transition clauses (a)–(h) covering link permanence (a), coverage invariance (b), content fidelity (c), per-link discovery permanence (d), arrangement-dependent projection/resolution (e), cross-origin exclusion (f), partial-survival decomposition (g), and bilateral-vitality preservation (h); plus system-level clause (i) covering L-frame discovery invariance (SV7), monotonic discovery growth under link allocation (SV9), discovery/projection independence (SV10, CrossDocumentDecoupling), and document-derived discovery non-permanence with inherited per-link projection structure (SV14) | introduced |
| NewLinkEvaluationDefinedness | Corollary of SV13(e): for a link a_new allocated by K.λ, every slot s and every d ∈ dom(Σ'.M) yield well-defined `locate(Σ'.L(a_new).s, d)` and `discover_s(A)` immediately at Σ' without any state-priming step | introduced |
| wp(K, R) | Weakest precondition reformulation of SV2–SV5: K.μ⁻ is the unique elementary transition whose wp for `π(e, d) ≠ ∅` is non-trivial; all other transitions preserve vitality definitionally | introduced |


## Open Questions

- What must the system guarantee about resolution when the same I-address appears at multiple V-positions within a single document through within-document sharing?
- Must the system provide a mechanism to transition a dormant link (vital in no document) back to vitality, and if so, what operation achieves this?
- What must the system guarantee about the ordering of fragments in a partially surviving endset — is there a canonical ordering that all implementations must respect?
- When two independent links share overlapping endset coverage, what invariants govern their independent partial survival under the same contraction?
- Must the system guarantee an upper bound on the number of fragments that a single endset can produce in any given document?
- What must the system guarantee about discovery latency — must newly created links be discoverable immediately, or is eventual consistency permitted?
- Under what conditions must bilateral vitality be preserved across a fork (version creation), given that the fork copies only a subset of the source's arrangement?
- What must the system guarantee about the relationship between a link's home document and the documents where its endsets are vital — can these be entirely disjoint?
