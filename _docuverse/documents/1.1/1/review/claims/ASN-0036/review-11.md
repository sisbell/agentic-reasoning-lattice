I read the foundation statements, then the ASN as a system — tracing each precondition chain from caller to callee, checking that terms hold one meaning across the note and the per-claim contracts, and walking the proofs of S5, S7, S8, D-CTG-depth, D-SEQ, and ValidInsertionPosition. Most chains close cleanly: S8's body now grounds depth-preservation in shift's frame (not S8-depth), the worked example's origins/runs check out, and the contiguity cone (D-CTG → D-CTG-depth → D-MIN → D-SEQ → ValidInsertionPosition) is internally consistent. One divergence does not close.

### S7 well-definedness chain to T10a.4 is type-confused in the note, while the per-claim contracts route it correctly
**Class**: REVISE
**Foundation**: T10a.4 (T4PreservationUnderDiscipline) — *"For every allocator `A` in the tree and every `t ∈ dom(A)`, `t` satisfies T4"*; instantiation requires `A ∈ 𝒯` and `t ∈ dom(A)` (two memberships of distinct types). S7a, S7.
**ASN**: The note's "Structural attribution" S7a paragraph: *"every `a ∈ dom(Σ.C)` satisfies `a ∈ dom(A_element) ⊆ 𝒯` for a T10a-conforming element allocator `A_element` … This membership is the premise that, in S7's well-definedness step, licenses T10a.4's preservation invariant to be instantiated at `a`."* And the note's S7 proof, Well-definedness: *"instantiating it at `A := A_element` and `t := a` — legitimate precisely because S7a places `a ∈ dom(A_element) ⊆ 𝒯` — yields that `a` is T4-valid."*
**Issue**: `dom(A_element)` is a set of tumblers (addresses); `𝒯` is the set of allocators. So `dom(A_element) ⊆ 𝒯` is a category error — it asserts every address in the allocator's domain *is itself an allocator* — and the chain `a ∈ dom(A_element) ⊆ 𝒯` concludes `a ∈ 𝒯` (the address `a` is an allocator), which is type-incoherent. Worse, the form supplies neither premise T10a.4's instantiation actually needs: it never establishes `A_element ∈ 𝒯` (the allocator is a tree node), and it mis-types `a ∈ dom(A_element)` as a transitive link to `𝒯`. The accompanying singular "This membership is the premise" and "instantiated at `a`" compound the error — two memberships of distinct types are required, instantiating at `A := A_element, t := a`. The precondition chain from S7 (caller) to the foundation claim T10a.4 (callee) is therefore broken *in the note*. The per-claim S7a and S7 contracts have already corrected this to *"two memberships of distinct types — `A_element ∈ 𝒯` … and `a ∈ dom(A_element)`"* and *"S7a places `A_element ∈ 𝒯` and `a ∈ dom(A_element)`, the two memberships of distinct types the instantiation requires."* The note is stale relative to its own contracts — a reconciliation that landed on the claim files but not on the note (consistent with the note being the lone modified S7-bearing artifact).
**What needs resolving**: In the note's S7a paragraph and S7 Well-definedness step, replace `a ∈ dom(A_element) ⊆ 𝒯` with the two separate memberships `A_element ∈ 𝒯` and `a ∈ dom(A_element)`, and adjust "This membership is the premise … instantiated at `a`" to "These two memberships are the premises … instantiated at `A := A_element, t := a`," matching the already-corrected per-claim S7a/S7 contracts so the note's instantiation of T10a.4 is type-coherent.

VERDICT: REVISE