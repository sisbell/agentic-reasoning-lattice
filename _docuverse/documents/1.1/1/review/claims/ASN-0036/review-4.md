I read the foundation contracts and then ASN-0036 as a system, tracing the operator groundings and precondition chains that cross claim boundaries (S0→S1→S3; S7a→T10a.4→T4→T4b; OrdShiftHom→OrdinalShift→TumblerAdd→TA0; S8's succ/chain machinery against TS2/TS3/TS4; D-CTG→D-CTG-depth→D-SEQ; ValidInsertionPosition→D-MIN).

The core mathematics is sound. The two-stream separation (S0/S1/S3), the GlobalUniqueness lifting in S4/S7, the T4-validity chain into S7's well-definedness, and the injective-acyclic-partial-function argument behind S8's partition all hold, and the worked example is internally consistent (zeros counts, origin truncation, and the two-run break at the transclusion/append boundary all check out). The shift operator is correctly kept off the I-address subspace question (the open question rightly defers alignment to the operations layer), and the i=0-vs-i≥1 boundary against TS3's `n₁ ≥ 1` precondition is handled by the `shift(t,0):=t` convention throughout.

The findings below concern dependency grounding and narrative/contract synchronization, not the underlying proofs.

### S8 Depends omits the OrdinalShift operator it is defined in terms of
**Class**: REVISE
**Foundation**: OrdinalShift (and OrdinalDisplacement), ASN-0034
**ASN**: S8 (CorrespondenceRunPartition). The per-claim *Depends:* lists only `S2, S3, S8a, S8-depth, S8-fin, OrdShiftHom, TS2, TS3, TS4, T1`. But S8's own *Definition* uses the shift operator directly: "a *correspondence run* is a triple (v, a, n) … such that for every k … `shift(v, k) ∈ dom(M(d))`, `M(d)(shift(v, k)) = shift(a, k)`," and `succ(v) = shift(v, 1)`. Meanwhile the Properties Introduced table states S8 is "theorem from … TS4, **OrdinalShift, OrdinalDisplacement** (ASN-0034)."
**Issue**: The `shift` operator appearing in S8's run definition and lockstep identity is grounded by OrdinalShift, which is absent from S8's Depends. OrdShiftHom/TS2/TS3/TS4 are *consumers* of shift, not its defining claim, so they do not supply the operator. The sibling shift-using claims follow the opposite convention: OrdShiftHom and ValidInsertionPosition both cite OrdinalShift directly ("supplies the shift operator and its expansion"). So S8 is the lone shift-using claim that leaves the operator ungrounded in its Depends, and its Depends contradicts the Properties Introduced table by exactly `{OrdinalShift, OrdinalDisplacement}`.
**What needs resolving**: Add OrdinalShift to S8's *Depends:* (supplying the `shift` operator used in the run definition, the lockstep successor, and the displacement identity), and reconcile OrdinalDisplacement between the table and the contract — either cite it in Depends or drop it from the table row, per whether S8 references `δ` directly (it does not appear to; it reaches `δ` only through OrdinalShift).

### S8 over-attributes to S8-depth; the dependency is non-load-bearing and the Depends prose is defensive
**Class**: OBSERVE
**Foundation**: S8-depth (FixedDepthVPositions), this ASN; the depth-preservation frame `#shift(v,1)=#v` (ultimately TA0)
**ASN**: S8. Precondition: "Every `v ∈ dom(M(d))` is a well-formed V-position at a fixed depth `m` (S8a, S8-depth)." Depends note: "[S8-depth] does *not* license the succ-confinement step's per-position depth equality … Likewise it does *not* license the injectivity step's appeal to TS2 …," with a parallel "grounded unconditionally in the frame rather than in S8-depth's domain-restricted quantifier" clause in the OrdShiftHom entry.
**Issue**: Walking S8's proof, no step uses S8-depth's subspace-wide depth uniformity: chain depth-uniformity follows from shift's frame (`#shift(v,1)=#v`, transitively along the chain), injectivity derives `#u=#u'` locally from `shift(u,1)=shift(u',1)`, and the partition closes via OrdShiftHom(a)'s "union over subspaces." The Depends note itself disclaims S8-depth at both load-bearing sites — that disclaimer is a use-site inventory accreted across cycles. Separately, the Precondition phrase "at a fixed depth `m`" reads as a single global depth, which is false for a `dom(M(d))` spanning text and link subspaces (the proof's own "each chain lying in a single subspace … union over subspaces" handles multiple depths); the proof's local `m = #v` is the correct reading.
**What needs resolving**: Establish whether S8-depth is actually load-bearing for S8; if not, demote it from the Depends and replace the disclaiming inventory with a one-line statement of its role (or drop it). Re-phrase the Precondition so "fixed depth `m`" is per-subspace (consistent with the multi-subspace partition), not a single global depth.

### Narrative proofs of D-SEQ and S5 lag their per-claim formal contracts
**Class**: OBSERVE
**Foundation**: n/a (internal consistency)
**ASN**: (i) Main-body D-SEQ, Step 1 *Case m ≥ 3*: "all positions in V_1(d) share components 2 through m − 1 … Every position is therefore [1, 1, …, 1, k]" — the leading component is asserted as 1 without the `subspace(v)=v₁=1` step that the per-claim D-SEQ adds ("every position has `v₁ = 1` — the subspace identifier sits at component 1"). (ii) Main-body S5 within-document construction ends at "the within-document sharing multiplicity is `|{v : …}| = N + 1 > N`," whereas the postcondition quantifies global multiplicity `|{(d,v) : …}|`; the per-claim S5 supplies the bridge "Since `d` is the only document, this within-document count equals the global multiplicity."
**Issue**: The narrative (exposition) proofs and the per-claim formal contracts are both present in the document and have diverged. The omitted steps are individually trivial (definitional / single-document), but the narrative versions therefore present a slightly incomplete derivation relative to the authoritative contracts.
**What needs resolving**: Sync the narrative D-SEQ Case m ≥ 3 (state `v₁ = 1` from the V_1(d) definition) and narrative S5 within-document conclusion (note the within-document count equals the global multiplicity for the single-document witness) with their per-claim formal-contract versions.

VERDICT: REVISE