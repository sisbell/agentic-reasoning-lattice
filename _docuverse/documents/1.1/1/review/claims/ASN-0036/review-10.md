I read the foundation statements, then the ASN as a system: the two-component state (Σ.C, Σ.M), the content-store invariants (S0→S1, S3, S4←GlobalUniqueness), the attribution chain (S7b/S7a/S7d→S7 through T4/T4a/T4b/T10a.4/GlobalUniqueness), the V-position layer (S8a/S8-depth/OrdShiftHom→S8), and the contiguity stack (D-CTG→D-CTG-depth→D-SEQ→ValidInsertionPosition). I traced precondition chains across claim boundaries and checked the worked example against every invariant it claims to exercise.

Most of the system holds together. S0→S1 is clean; S4 and S7's uniqueness both route GlobalUniqueness correctly off distinct allocation events; the S8 chain-decomposition (succ injective+acyclic+finite ⇒ disjoint paths) is sound, the k=0/i≥1 TS3 split is handled, and the worked example's two-run d₂ decomposition checks out (lockstep breaks exactly at the transclusion/append boundary). D-CTG-depth's infinite-intermediate contradiction and D-SEQ's assembly are correct. I did not re-open the S8-depth/OrdShiftHom-frame question — the body already routes depth through OrdShiftHom's frame, matching the prior reviser resolution.

What I found is a note-vs-claim-file divergence concentrated in two claims whose claim files were evidently revised without updating the narrative proofs — and in one case the stale narrative version is actually wrong.

### S7a / S7 narrative state the T10a.4 instantiation premise with a type error
**Class**: REVISE
**Foundation**: T10a (AllocatorDiscipline) — `𝒯` is "the set of allocators"; `dom(A) = {tₙ : n ≥ 0} ⊆ T` is a set of addresses (tumblers). T10a.4 (T4PreservationUnderDiscipline) — quantifies "for every allocator `A` in the tree and every `t ∈ dom(A)`."
**ASN**: Note body of S7a: *"every `a ∈ dom(Σ.C)` satisfies `a ∈ dom(A_element) ⊆ 𝒯` for a T10a-conforming element allocator `A_element` … This membership is the premise that … licenses T10a.4's preservation invariant to be instantiated at `a`."* And the S7 well-definedness proof: *"instantiating it at `A := A_element` and `t := a` — legitimate precisely because S7a places `a ∈ dom(A_element) ⊆ 𝒯` — yields that `a` is T4-valid."*
**Issue**: The chained membership `a ∈ dom(A_element) ⊆ 𝒯` asserts `dom(A_element) ⊆ 𝒯` — i.e. that the address domain (a set of tumblers in `T`) is a subset of the allocator tree (a set of allocators). That is a type error; addresses are not allocators. Read as a chain it concludes `a ∈ 𝒯` (the address is a node of the tree), which is both false and useless for the instantiation. T10a.4's instantiation at `A := A_element, t := a` requires **two memberships of distinct types** — `A_element ∈ 𝒯` (allocator-in-tree, supplying the `A` binding) and `a ∈ dom(A_element)` (address-in-domain, supplying the `t` binding) — but the narrative collapses them into one malformed expression and calls it "This membership" (singular). The claim-file versions of S7a and S7 already state this correctly: *"two memberships of distinct types — `A_element ∈ 𝒯` … and `a ∈ dom(A_element)`"* / *"S7a places `A_element ∈ 𝒯` and `a ∈ dom(A_element)`, the two memberships of distinct types the instantiation requires."* So the note carries a stale, incorrect formulation of a load-bearing well-definedness step.
**What needs resolving**: In the note body of S7a and S7, replace `a ∈ dom(A_element) ⊆ 𝒯` with the two distinct-type memberships `A_element ∈ 𝒯` and `a ∈ dom(A_element)` (matching the claim-file text), so the T10a.4 instantiation at `A := A_element, t := a` is licensed by the premises it actually requires.

### Note-body proofs of OrdShiftHom(b) and S7 strict-positivity drift from their claim files
**Class**: OBSERVE
**Foundation**: OrdinalShift (postcondition `shift(v,n)_{#v} = v_{#v} + n ≥ 1`); T4b (projections return sequences over `ℕ⁺`) vs T0 + NAT-zero (`tᵢ ≠ 0 ⇔ tᵢ > 0`).
**ASN**: OrdShiftHom part (b): note body says *"at `i = m`, `rₘ = vₘ + n ≥ 1 + 1 > 0`"* (fresh arithmetic from `vₘ ≥ 1`, `n ≥ 1`), whereas the claim file says *"we do not re-derive the bound: OrdinalShift already exports the component lower bound `shift(v, n)_{#v} = v_{#v} + n ≥ 1` … which at `#v = m` reads `rₘ = vₘ + n ≥ 1`."* S7 well-definedness strict positivity: note body discharges it *"by T0's carrier ℕ … under which `tᵢ ≠ 0 ⇔ tᵢ > 0`"*, whereas the claim file takes it *"directly from T4b's own postcondition, under which every defined projection returns a sequence over `ℕ⁺`."*
**Issue**: Both renderings of each step are individually sound, but they justify the same step two different ways — a consistency/staleness symptom of the same note/claim-file divergence as the REVISE above (the claim files were revised to cite the foundation postconditions; the note proofs were not). Separately, the claim-file OrdShiftHom(b) wraps its (correct) step in defensive meta-prose — *"We are looking for the lower bound … without any fresh arithmetic"* and *"we do not re-derive the bound"* — which narrates the proof strategy rather than the proof, the kind of justification-about-justification the precise reader has to skip past.
**What needs resolving**: Reconcile the note-body proofs of OrdShiftHom(b) and S7 strict-positivity with their claim-file counterparts so a single justification stands for each step (preferably the foundation-citation form: OrdinalShift's exported `≥ 1` bound, and T4b's `ℕ⁺` postcondition); and trim the claim-file OrdShiftHom(b)'s defensive meta-prose to state the action-point bound plainly.

VERDICT: REVISE