I read the foundation statements, then the ASN as a whole, tracing each claim through both its narrative proof and its later formal-contract restatement. The core architecture is sound: the two-component strand, the append-only-log invariants (S0/S1), referential integrity (S3), origin-based identity (S4/S7), the correspondence-run partition (S8), and the text-subspace contiguity chain (D-CTG → D-CTG-depth → D-MIN → D-SEQ → ValidInsertionPosition) all hold up under case-walking. The worked example is internally consistent (run boundaries fall exactly where lockstep breaks).

The findings concentrate on a systemic issue a per-claim check cannot see: **each claim appears twice** — once as a narrative proof in the body, once as a formal-contract proof at the bottom — and for two claims the two copies have diverged, with the body copy carrying the *unsound* version that the formal contract already corrected. A per-claim pipeline that validates the formal contract will never notice that the narrative proof of the same claim is broken.

### S8 narrative proof justifies depth preservation circularly via S8-depth
**Class**: REVISE
**Foundation**: S8-depth (FixedDepthVPositions); OrdShiftHom frame (ultimately TA0)
**ASN**: Body proof of S8, "succ stays within a subspace" step: *"by OrdShiftHom (b) `shift(v, 1)` again satisfies S8a; **by S8-depth it shares `v`'s depth `m`**."* The formal-contract proof of the same claim replaces this with: *"its depth is fixed by OrdShiftHom's frame, `#shift(v, 1) = #v = m` … an unconditional property of shift **that does not presuppose `shift(v, 1) ∈ dom(M(d))`**, so we may invoke it here, before the lockstep link is known to land back in the domain."*
**Issue**: S8-depth is `(A d,u,w : u ∈ dom(M(d)) ∧ w ∈ dom(M(d)) ∧ subspace(u)=subspace(w) : #u=#w)`. To conclude `#shift(v,1) = #v` from it you must instantiate `w := shift(v,1)` and discharge `shift(v,1) ∈ dom(M(d))` — which is exactly the not-yet-established lockstep-link condition at this point in the proof. The body invokes S8-depth unconditionally (it even covers the case where `succ(v)` is *undefined*, i.e. `shift(v,1) ∉ dom(M(d))`, where S8-depth simply does not apply). The conclusion is true, but via shift's frame, not S8-depth. The formal contract recognizes this and routes through the frame; the body still carries the circular justification.
**What needs resolving**: Rewrite the body's depth step to derive `#shift(v,1) = #v` from shift's frame property (OrdShiftHom / TA0), matching the formal-contract proof; do not cite S8-depth there. While reconciling, settle whether S8 depends on S8-depth at all — the only depth-sensitive steps (this one and the injectivity step) are both grounded in shift's frame in the formal contract, so S8-depth's listed role in S8 may be vestigial.

### S7 narrative proof instantiates T10a.4 without discharging its domain-membership premise
**Class**: REVISE
**Foundation**: T10a.4 (T4PreservationUnderDiscipline) — precondition `t ∈ dom(A)` for an allocator `A` in a T10a-conforming tree; S7a (DocumentScopedAllocation)
**ASN**: Body proof of S7, Well-definedness: *"By S7b (element-level I-addresses), `zeros(a) = 3`, and **by T10a.4 …, `a` is T4-valid**; hence T4's field-decomposition machinery applies to `a`."* The formal-contract proof instead writes: *"We obtain T4-validity from the allocation discipline. By S7a …, `a ∈ dom(A_element) ⊆ 𝒯` … instantiating it at `A := A_element` and `t := a` — legitimate precisely because S7a places `a ∈ dom(A_element)` — yields that `a` is T4-valid."*
**Issue**: T10a.4 yields T4-validity only for addresses in some allocator's domain. The body jumps from `zeros(a) = 3` (S7b) to "`a` is T4-valid by T10a.4" without the membership `a ∈ dom(A_element)` that licenses the instantiation; `zeros(a) = 3` alone fixes separator count but not the validity premise. The formal contract supplies the missing S7a step. Same lagging pattern appears in the S5 body proof, whose within-document construction concludes `|{v : …}| = N+1` but never bridges to the global multiplicity `|{(d,v) : …}|` that the postcondition is stated over (the formal contract adds "Since `d` is the only document, this within-document count equals the global multiplicity").
**What needs resolving**: Bring the S7 (and S5) body proofs up to the rigor of their formal contracts — discharge T10a.4's `a ∈ dom(A_element)` premise via S7a in S7's Well-definedness, and bridge within-document to global multiplicity in S5. More broadly, run a sync pass: the narrative proofs are stale relative to the tightened contract proofs.

### Defensive "does-not-license / does-not-depend" essays accumulating in Depends slots
**Class**: OBSERVE
**Foundation**: n/a (structural-slot prose)
**ASN**: S8's Depends entries. The S8-depth entry: *"It does *not* license the succ-confinement step's per-position depth equality … Likewise it does *not* license the injectivity step's appeal to TS2 …"* The OrdinalShift entry: *"OrdShiftHom, TS2, TS3, and TS4 are *consumers* of shift, not its defining claim … S8 … does *not* depend on OrdinalDisplacement directly."* The OrdShiftHom entry similarly litigates what its frame does and does not presuppose.
**Issue**: A Depends entry should state what a dependency *supplies*. These entries instead spend multiple sentences arguing what each dependency does *not* license and why a sibling claim is *not* depended on — exactly the reviser-drift pattern (use-site inventory / negative-dependency essay in a structural slot). The precise reader must skip past the litigation to recover the actual dependency. This compounds across cycles. (The content is a symptom of the S8-depth confusion in finding 1: the essay exists to pre-empt the very circularity the body proof still contains.)
**What needs resolving**: Reduce each Depends entry to what the dependency supplies to S8. Move any genuinely needed disambiguation (e.g. "depth preservation comes from shift's frame, not S8-depth's quantifier") into the proof body as a one-clause justification at the step, and drop dependencies S8 does not actually consume rather than annotating their non-use.

### D-MIN's "Why this must be posited rather than proved" section is meta-commentary on the axiom's status
**Class**: OBSERVE
**Foundation**: n/a
**ASN**: D-MIN formal contract, the "**Why this must be posited rather than proved**" paragraph and surrounding framing ("It is tempting to read left-anchoring off the constraints already in hand …", "We supply it as a design decision …").
**Issue**: The concrete witness `{[1,5],[1,6],[1,7]}` and the one-line independence statement (it satisfies D-CTG/S8a/S8-fin/S8-depth but has `min = [1,5] ≠ [1,1]`) are genuinely useful — they tell a downstream consumer not to attempt to derive D-MIN. That part is a concrete example, not meta-prose, and should stay. But the surrounding essay — explaining *why* the axiom is needed and narrating the temptation to prove it — is prose about the axiom's epistemic status rather than its content, and it is long enough that the reader works to extract the witness.
**What needs resolving**: Keep the witness and a single sentence asserting non-derivability (independent of D-CTG, S8a, S8-fin); trim the "it is tempting / we supply it as a design decision" framing narration around it.

VERDICT: REVISE