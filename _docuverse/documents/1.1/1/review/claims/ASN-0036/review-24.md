I read the foundation statements (ASN-0034) and then ASN-0036 as a system, tracing the precondition chains and definition usages across claim boundaries. I checked the two declined findings are genuinely resolved (the S8 depth step now routes through OrdShiftHom's frame, not S8-depth; S5 is fully defined with S0 in its Depends) and did not re-raise either.

The substantive proofs walk their cases correctly. S3's induction splits inherited (S1) vs new/redirected (AX-2) and the two cases exhaust `dom(Σ'.M(d))`. D-CTG-depth's least-disagreement-index contradiction is sound (the witness `w` is between `u` and `x` for all large `n`, infinitely many distinct, against S8-fin). D-SEQ's four steps and assembly correctly yield `{[1,…,1,k] : 1 ≤ k ≤ n}`. S8's chain decomposition is sound: `succ` injective (TS2 at the locally-derived common depth, not S8-depth), acyclic (TS4 + T1 irreflexivity), the displacement induction handles `i=0` by convention and `i≥1` by TS3, and `shift(a,k)` membership comes from S3 (not from OrdShiftHom, which correctly is never applied to the non-zero-free I-addresses). S7's well-definedness correctly insists on T4-validity (via S7a→T10a.4) rather than the bare zero count, and the `zeros(origin(a))=2` / four-conjunct T4-validity derivation is correct though microscopically verbose. The S7a/S7b/S7d → GlobalUniqueness lift (event-distinctness to address-distinctness) is sound, as is the consistency between S7a's element-allocator and S7b's `zeros=3` (the model requires `spawnParam=2`, which is compatible, and the proof uses each posit independently). S5's witnesses correctly model only S0–S3. Frame conditions are present and respected.

Two findings.

### subspace claim defines the v₁ projection without grounding it in T0
**Class**: REVISE
**Foundation**: T0 (CarrierSetDefinition, ASN-0034) — component projection `i ↦ aᵢ` and length operator `#·`
**ASN**: subspace (VPositionSubspaceIdentifier) — the entire claim: *"For any tumbler `v` of depth `#v ≥ 1`, define: `subspace(v) = v₁` extracting the subspace identifier as the first component of a V-position."* There is no *Formal Contract*, *Definition*, or *Depends* block.
**Issue**: subspace is a **derived** definition — `subspace(v) = v₁` consumes T0's component projection (`v₁` for `v ∈ T`) and T0's length operator (`#v ≥ 1`) — yet it cites no foundation at all. It is referenced as a first-class dependency by V-sub, S8-depth, D-CTG, D-CTG-depth, D-SEQ, OrdShiftHom, and ValidInsertionPosition, each writing "subspace (VPositionSubspaceIdentifier) — supplies the projection `subspace(v) = v₁`" to ground *their* use of `v₁`. The grounding chain therefore terminates at subspace, where `v₁` is introduced with no edge to T0. A consumer (formalization tool included) tracing where `v₁` is defined hits an unsupported symbol. This is unlike the sibling signature definitions: Σ.C names "`T` is the set of tumblers (ASN-0034)" in prose, and Strand uses only this-ASN symbols (`Σ.C`, `Σ.M`); subspace alone consumes a foundation symbol while citing nothing. Every other claim in the ASN that reads a component cites T0 for the projection.
**What needs resolving**: Give subspace a *Depends* entry citing T0 (CarrierSetDefinition) for the component projection `v₁` and the length operator `#·` its definition consumes, so the `v₁` symbol is grounded at its definition site rather than left dangling for the seven downstream consumers that ground their `v₁` usage on it.

### Posit claims duplicate their full non-derivability essay inside the Axiom structural slot
**Class**: OBSERVE
**Foundation**: N/A
**ASN**: D-CTG (VContiguity) and S8a (ArrangementDomainRestriction), *Axiom* slots. E.g., S8a's *Axiom* slot, after the (correct, clear) subset statement: *"…It is not derived from any other claim — in particular not from the content-store invariants S0, S1, S2, S3, which govern the evolution of `C` alone, nor from the transition axioms AX-1 (which fixes only the empty base arrangement) and AX-2 (which constrains the targets of new V-mappings, never the shape of the keys); none of these constrains the shape of a V-position. OrdShiftHom part (b) and S8 consume the restriction as a load-bearing precondition… so a consumer, a formalization tool included, must treat it as an implementation obligation…"* D-CTG's *Axiom* carries the parallel enumeration.
**Issue**: The Axiom slot should state what the posit asserts. In these two claims the slot, after the formal statement, re-runs the full non-derivability argument (enumerating which other claims fail to entail it) plus methodological commentary ("must treat it as an implementation obligation", "a formalization tool included"). This is the "new prose around an axiom [that] explains why the axiom is needed rather than what it says" pattern, and the same material already appears at length in each claim's gloss — so the structural slot duplicates it. The formal statements are sound, so this does not affect correctness (hence OBSERVE), but the slot discipline degrades as this prose compounds across cycles. (S8-fin's "why not `|·|`" remark and D-MIN's concrete counterexample are *substantive* — well-formedness justification and an independence witness respectively — and are not part of this finding.)
**What needs resolving**: Trim the D-CTG and S8a Axiom slots to the posited statement plus a brief self-contained marker ("posit, accepted within this ASN without proof"); the S0–S3 / AX-1 / AX-2 non-derivability enumeration and the implementation-obligation framing already live in the gloss and need not be repeated in the structural slot.

VERDICT: REVISE