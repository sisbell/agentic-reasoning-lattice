I read the foundation set, then ASN-0036 end to end, tracing the precondition chains that cross claim boundaries (S0→S1, S7a/S7b/S7d/T10a.4/GlobalUniqueness→S7) and the shift-algebra chain that S8 and the D-* family ride on (OrdShiftHom→S8-depth/TS2/TS3/TS4→S8→D-CTG-depth→D-SEQ→ValidInsertionPosition).

The shift machinery is sound as a system. OrdShiftHom's `m ≥ 2` precondition is met at every call site (all arrangement keys satisfy S8a's `#t ≥ 2`); S8's succ-graph argument correctly grounds the per-position depth equality in shift's frame (`#shift(v,1)=#v`) rather than S8-depth's two-endpoint quantifier, and grounds the TS2 appeal locally from `shift(u,1)=shift(u',1)`; the `shift(t,0):=t` convention cleanly bridges the `k=0` boundary that TS3's `n₁≥1` precondition excludes. D-CTG-depth's infinite-intermediate construction, D-SEQ's four-step assembly, and the worked example all check out (I verified the tumbler arithmetic: `1.0.1.0.1.0.1.3` parses to `origin = 1.0.1.0.1 = d₁`, the run-A/run-B break at the transclusion boundary is real).

The substantive problem is a systemic one: the **main-body narrative proofs trail the (corrected) formal-contract proofs of the same claims**. The formal contracts have been tightened; the narrative `*Proof.*` blocks still carry the pre-tightening shortcuts — and in S7 those shortcuts are demonstrably unsound, because the formal contract itself spends paragraphs explaining *why* the shortcut is invalid. Two distinct gaps below.

### S7 narrative well-definedness invokes T10a.4 without its domain-membership premise
**Class**: REVISE
**Foundation**: T10a.4 (T4PreservationUnderDiscipline) — postcondition is "for every allocator `A ∈ 𝒯` and every `t ∈ dom(A)`, `t` is T4-valid"; requires `a ∈ dom(A)` to instantiate.
**ASN**: S7 main-body proof, Well-definedness: *"By S7b (element-level I-addresses), `zeros(a) = 3`, and by T10a.4 (T4PreservationUnderDiscipline, ASN-0034), `a` is T4-valid; hence T4's field-decomposition machinery applies to `a`."*
**Issue**: T10a.4 is keyed on allocator-domain membership, not on zero-count. The cited premises (S7b gives `zeros(a)=3`; T10a.4) do not connect — nothing here places `a` in any allocator's domain, so T10a.4 cannot be instantiated at `a`. The missing premise is S7a's `a ∈ dom(A_element) ⊆ 𝒯`. This is not a terseness gap: the S7 formal contract explicitly proves the step is non-trivial — *"`zeros(a) = 3` alone fixes the number of separators but says nothing about their adjacency or the boundary components, which are precisely the constraints T4 imposes"* — and routes through S7a to supply the membership. The narrative omits that route entirely, leaving an unjustified leap from `zeros=3` to T4-validity.
**What needs resolving**: The main-body S7 well-definedness step must establish `a ∈ dom(A_element) ⊆ 𝒯` from S7a *before* instantiating T10a.4 at `a`, matching the formal-contract derivation (S7a membership → T10a.4 → T4-validity), with S7b supplying `zeros(a)=3` only afterward to fix the element level.

### S7 narrative uniqueness misattributes "distinct tumblers" to S7d, skipping GlobalUniqueness
**Class**: REVISE
**Foundation**: S7d (DocumentAllocationDiscipline) — states "distinct documents arise from distinct allocation *events*"; GlobalUniqueness — supplies the event→address injectivity (distinct events ⇒ distinct addresses).
**ASN**: S7 main-body proof, Uniqueness across documents: *"By S7d's postcondition, distinct documents have distinct document-level tumblers. By T3 (CanonicalRepresentation, ASN-0034), this distinctness is decidable by component-wise comparison. Therefore, for any `a₁, a₂ ∈ dom(Σ.C)` allocated under distinct documents: `origin(a₁) ≠ origin(a₂)`."*
**Issue**: S7d's postcondition is distinctness of *allocation events*, not of *tumblers*. The narrative asserts S7d directly yields distinct document-level tumblers — a conclusion S7d does not state. The passage from distinct events to distinct addresses is exactly GlobalUniqueness, which the narrative never invokes. The S7 formal contract identifies this precise gap: *"The second fact separates distinct documents only at the level of events; what it does not by itself supply is the passage from distinct events to distinct addresses — for two distinct allocation events could, a priori, still emit the same tumbler value. That passage is exactly GlobalUniqueness."* The narrative both skips a load-bearing step and misreads its cited dependency.
**What needs resolving**: The main-body S7 uniqueness step must lift S7d's event-distinctness to tumbler-distinctness via GlobalUniqueness (whose T10a precondition is discharged by S7d's "arising from an allocation event under T10a"), exactly as the formal contract does, rather than attributing tumbler-distinctness to S7d. S7's narrative `Depends`/citation set should then carry GlobalUniqueness in the uniqueness role.

### ValidInsertionPosition proof cites enumeration labels (b), (c), (d) that resolve to nothing
**Class**: OBSERVE
**Foundation**: N/A (internal labeling).
**ASN**: ValidInsertionPosition proof (both main-body and formal-contract copies): *"…at `j = 0` the position is `v = min(V_1(d)) = [1, ..., 1]` by D-MIN. **This is (d)**. Every component is then `≥ 1` … so `zeros(v) = 0` with componentwise positivity **(b)**, and OrdShiftHom (a) fixes `v₁ = 1` … giving exactly `N + 1` positions **(c)**."*
**Issue**: The parenthetical tags `(b)`, `(c)`, `(d)` reference an enumerated postcondition list that does not exist in the claim — neither the Definition nor the Postconditions slot is enumerated `(a)`–`(d)`. ("OrdShiftHom (a)" is a legitimate reference to OrdShiftHom's part (a); the others are vestigial.) This is reviser drift: tags left behind from a draft whose postconditions were itemized and later folded into prose. The proof's logic is sound; the labels are debris a reader must work around.
**What needs resolving**: Either remove the dangling `(b)/(c)/(d)` tags or re-introduce the matching enumerated postcondition list (form, positivity, count) they were meant to index; apply to both the main-body and formal-contract copies so they stay in sync.

VERDICT: REVISE