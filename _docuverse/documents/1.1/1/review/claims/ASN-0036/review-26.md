I read the foundation set (ASN-0034 tumbler algebra, the NAT-* stratum, and the allocator discipline) and then ASN-0036 as a system. The two-stream separation holds consistently: I-addresses (`dom(Σ.C)`, element-level, `zeros = 3` per S7b) and V-positions (`dom(Σ.M(d))`, zero-free depth ≥ 2 per S8a) live in distinct address spaces, and `M : V → I` bridges them without ever conflating the two zero-counts. The transition-invariant chain `AX-1 → S3` (via S1 for inherited references, AX-2 for new/redirected) walks both cases exhaustively; the posit/theorem boundary (D-CTG, D-MIN, S8a, S8-depth, S8-fin posited; D-CTG-depth, D-SEQ, S8, S7 derived) is drawn cleanly, and the non-derivability arguments (D-MIN's floating `{[1,5],[1,6],[1,7]}` block) check out. S7's attribution chain (S7a→T10a.4 for T4-validity, S7b for `zeros=3`, S7d→GlobalUniqueness for cross-document distinctness) is logically sound, as is S8's run partition (injective acyclic `succ` ⇒ disjoint chains, with depth grounded in shift's frame per the prior cycle's fix). The two previously declined findings (S8 circular depth; S0/S5 reference) are correctly resolved in this substrate and I did not re-raise them.

The defects I found are grounding-consistency gaps in the contiguity/insertion cluster: several derived claims write foundation symbols in their proofs but omit the defining claims from Depends, against the strict "cite the definer wherever the symbol is written" convention the ASN itself articulates (in D-CTG-depth's and S8-fin's own NAT-closure entries) and applies elsewhere.

### D-SEQ omits NAT-order and NAT-closure despite using both throughout its proof
**Class**: REVISE
**Foundation**: NAT-order (NatStrictTotalOrder, ASN-0034); NAT-closure (NatArithmeticClosureAndIdentity, ASN-0034)
**ASN**: D-SEQ (SequentialPositions). Its Depends list is exactly "S8-depth, S8a, D-CTG-depth, D-MIN, T1, T0, D-CTG, S8-fin, subspace" — no NAT-* entry at all. Yet Step 3 writes "its leading m − 1 components are all **1 ∈ ℕ**" (the component value fed to T0's comprehension), and the proof reasons over ℕ-order throughout: "Let **k₁ < k₂**", "For any k ∈ ℕ with **k₁ < k < k₂**", "the last component k satisfies **k > k₁ ≥ 1**", "every integer with **1 ≤ k ≤ n** is attained", "Let **n = max(k-values)**", "n ≥ 1".
**Issue**: The constant `1` written as a component value is grounded only by NAT-closure (`1 ∈ ℕ`); the ASN states this convention explicitly — D-CTG-depth's own NAT-closure entry says "T0 ... grounds it through T0's own dependence on NAT-closure rather than re-exporting it ... so this consumer — writing 1 as a first-class component value — grounds it here directly," and D-CTG-depth, performing the identical `[1,…,1,k]` construction, does cite NAT-closure. The k-value comparisons and `max(k-values)` are ℕ-order facts, not tumbler-order facts, so the cited T1 does not supply them (T1, like T0 with `1`, consumes NAT-order internally without re-exporting it for arbitrary naturals). D-SEQ writes both families of symbols and cites neither definer, leaving them ungrounded in its contract — a tool resolving symbols from Depends finds `1 ∈ ℕ` and `<`/`≤`/`max` on ℕ unsupported.
**What needs resolving**: Add NAT-closure (for the component constant `1 ∈ ℕ` and `n ≥ 1`) and NAT-order (for the k-value `<`/`≤`/`>` comparisons and the well-definedness of `max(k-values)` over a finite ℕ-set) to D-SEQ's Depends, matching D-CTG-depth's parallel construction.

### zeros's definer T4 (and NAT-card for |∅|=0) is mis-grounded in the positivity→zeros=0 step across three claims
**Class**: REVISE
**Foundation**: T4 (HierarchicalParsing, ASN-0034); NAT-card (NatFiniteSetCardinality, ASN-0034)
**ASN**: The "componentwise positivity ⟹ `zeros(·)=0`" derivation recurs in OrdShiftHom (b), D-CTG-depth, D-SEQ, and ValidInsertionPosition. OrdShiftHom grounds it fully — its Depends cite T4 ("supplies the symbol `zeros` and its zero-count definition ... part (b) unfolds ... back as `zeros(r) = 0`") and NAT-card ("supplies the empty-set cardinality `|∅| = 0` ... for the closing step of part (b)"). The other three diverge: D-CTG-depth's proof writes "(A i : 1 ≤ i ≤ #w : wᵢ > 0), **which is zeros(w) = 0**" but lists T4 only under *Forward References* (justified there solely by the post-proof ordinal commentary, "zero is a field separator") and omits NAT-card; D-SEQ writes "so **zeros(w) = 0**" with neither T4 nor NAT-card in Depends; ValidInsertionPosition writes "so **zeros(v) = 0** with componentwise positivity" and cites NAT-card but not T4.
**Issue**: Each of the three derived claims writes `zeros(·)` load-bearing in its proof/postcondition, so by the ASN's own convention (a symbol's defining claim goes in Depends wherever the symbol is written — followed by S8a, D-CTG, S7b, S7d, OrdShiftHom for exactly this symbol) T4 must be in Depends, not Forward References. "Forward References" signals "named but not depended upon," so a consumer loading only Depends finds `zeros` ungrounded in D-CTG-depth and D-SEQ. The `|∅| = 0` cardinality step that turns "no zero components" into `zeros = 0` likewise needs NAT-card (cited by OrdShiftHom) but is absent in both D-CTG-depth and D-SEQ.
**What needs resolving**: Add T4 to Depends for D-CTG-depth (a distinct entry for the proof's `zeros(w)` usage, kept separate from the existing commentary Forward Reference), D-SEQ, and ValidInsertionPosition; add NAT-card to D-CTG-depth and D-SEQ for the `|∅| = 0` step — bringing the three into line with OrdShiftHom's grounding of the identical derivation.

### S7's NAT-* Depends entries are paragraph-length use-site inventories
**Class**: OBSERVE
**Foundation**: N/A
**ASN**: S7 (StructuralAttribution), Depends. The entries for NAT-order, NAT-addcompat, NAT-closure, and NAT-sub each run a full paragraph re-narrating where in the Well-definedness proof the foundation is consumed — e.g. NAT-order's entry walks "the separation #N(a)+1 < ... the four-case walk ... the bound 1 = #U(a)+1 ≥ 2 ... closes the contradiction by forcing 1 < 1," and NAT-addcompat's entry re-derives the length-climb and separator-ordering inline.
**Issue**: These are the use-site-inventory pattern the precise reader must work around: the proof body already performs each step, and the Depends entry restates the same derivation a second time, burying the grounded fact (which foundation supplies which operator) under a re-narration of the proof. It is reference material so it does not break the argument, but it is the "use-site inventory in a structural slot" the review flags as degrading signal — and the repetition is itself a place a later cycle can introduce drift between the two narrations.
**What needs resolving**: Compress the S7 NAT-* Depends entries to the grounded fact (operator supplied + the single role it plays), letting the proof body carry the step-by-step consumption rather than duplicating it in Depends.

VERDICT: REVISE