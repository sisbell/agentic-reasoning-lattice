Reading ASN-0036 as a system, the per-claim formal contracts (the blocks after the `---`) read as sound: S8's chain-decomposition is correctly grounded in shift's frame rather than S8-depth (the previously-declined finding's text is genuinely gone), the contiguity chain D-CTG → D-CTG-depth → D-SEQ is acyclic and each construction now grounds `w ∈ T`, and S7's two-stage T4-validity/zeros=3 separation is clean. The defects I find are cross-section: the narrative prose (note body) has drifted out of sync with the corrected per-claim contracts, and in one place the stale narrace formulation is a genuine type error.

### S7a/S7 narrative asserts type-confused `a ∈ dom(A_element) ⊆ 𝒯`, supplying the wrong premise for T10a.4
**Class**: REVISE
**Foundation**: T10a.4 (T4PreservationUnderDiscipline), instantiated at `A := A_element, t := a`; the typing it relies on is from T10a (`𝒯` is a set of *allocators*, `dom(A)` is a set of *addresses* ⊆ T)
**ASN**: Narrative S7a: *"Consequently every `a ∈ dom(Σ.C)` satisfies `a ∈ dom(A_element) ⊆ 𝒯` ... This membership is the premise that ... licenses T10a.4's preservation invariant to be instantiated at `a`."* Narrative S7 well-definedness: *"instantiating it at `A := A_element` and `t := a` — legitimate precisely because S7a places `a ∈ dom(A_element) ⊆ 𝒯` — yields that `a` is T4-valid."*
**Issue**: The chain `a ∈ dom(A_element) ⊆ 𝒯` asserts `dom(A_element) ⊆ 𝒯` — that the allocator's domain (a set of tumbler addresses) is a subset of the allocator tree (a set of allocators). Addresses are not allocators; this is a type error (it conflates T with 𝒯). Transitively it yields `a ∈ 𝒯` (the address is a tree node), which is the wrong premise: T10a.4 quantifies "for every allocator `A ∈ 𝒯` and every `t ∈ dom(A)`," so its instantiation at `A := A_element, t := a` needs the two distinct-type memberships `A_element ∈ 𝒯` and `a ∈ dom(A_element)` — *not* `a ∈ 𝒯`. The singular "This membership is the premise" reinforces the conflation. The per-claim contracts for S7a and S7 already carry the corrected form ("two memberships of distinct types — `A_element ∈ 𝒯` ... and `a ∈ dom(A_element)`"); the narrative still carries the superseded single chain, so the same claim reads two incompatible ways across the document.
**What needs resolving**: Replace the narrative's `a ∈ dom(A_element) ⊆ 𝒯` (in both S7a and S7's well-definedness step) with the two distinct-type memberships `A_element ∈ 𝒯` and `a ∈ dom(A_element)`, matching the per-claim contracts, so the premise licensing the T10a.4 instantiation is stated coherently.

### S1 narrative grounds fresh-address uniqueness in "T9 and T10," conflicting with the per-claim's GlobalUniqueness
**Class**: REVISE
**Foundation**: GlobalUniqueness (consolidates same-allocator, sibling, and cross-level distinctness); cf. T9 (ForwardAllocation), T10 (PartitionIndependence)
**ASN**: Narrative S1: *"New entries may be added — each at a fresh address guaranteed unique by T9 and T10 (ASN-0034) — but no existing entry may be modified or removed."* Per-claim S1: *"... each at a fresh address guaranteed unique by GlobalUniqueness (ASN-0034) ..."*
**Issue**: Two copies of the same sentence cite different foundations for the same fact. T9 supplies only per-allocator forward ordering and T10 only non-nesting-prefix distinctness; neither, nor their conjunction, establishes that two outputs of the *same* allocator's sibling stream are distinct — exactly what GlobalUniqueness consolidates (its proof draws on T10a.6/T10a.7). So "guaranteed unique by T9 and T10" under-grounds the uniqueness claim, and disagrees with both the per-claim contract and S4/S7 (which both cite GlobalUniqueness). S1's proof itself uses only S0, so this is explanatory, but the document should name one grounding consistently.
**What needs resolving**: Update the narrative S1 to cite GlobalUniqueness (ASN-0034) for fresh-address uniqueness, matching the per-claim contract and the S4/S7 citations.

### Narrative D-CTG-depth and D-SEQ invoke D-CTG at a constructed `w` without grounding `w ∈ T`
**Class**: OBSERVE
**Foundation**: T0 (CarrierSetDefinition) comprehension clause; D-CTG, whose inner universal ranges over `v ∈ T`
**ASN**: Narrative D-CTG-depth: *"define w of length m by: ... Then w has depth m (it has m components by construction) ... D-CTG requires w ∈ V_1(d)."* Narrative D-SEQ Step 3: *"the tuple w = [1, 1, …, 1, k] satisfies subspace(w) = 1, #w = m ... By D-CTG (VContiguity), w ∈ V_1(d)."*
**Issue**: Both narrative proofs construct `w` componentwise and immediately instantiate D-CTG's inner universal (which ranges over `v ∈ T`) at `w` without first establishing `w ∈ T`. The per-claim contracts for both claims insert the missing step ("by T0's comprehension clause ..., w ∈ T ... the prerequisite for instantiating D-CTG's inner v ∈ T quantifier at w"); the narrative versions omit it, leaving the instantiation formally ungrounded — sound, since `w` is patently a tumbler, but unjustified in the prose. Same note-lags-the-contract pattern as the two findings above.
**What needs resolving**: Add the `w ∈ T` grounding (via T0's comprehension clause) to the narrative D-CTG-depth and D-SEQ proofs before each D-CTG instantiation, matching the per-claim contracts.

VERDICT: REVISE