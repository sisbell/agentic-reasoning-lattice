I read the foundation layer and then the full ASN — both the note narrative and the per-claim formal contracts — tracing each cross-claim precondition chain. The per-claim contracts (the authoritative S7, S7a, S8, S5, D-CTG-depth, D-SEQ, ValidInsertionPosition, etc.) check out: the S8 combinatorial argument (succ injective + acyclic + finite ⇒ disjoint chains, with depth grounded in shift's frame), the D-CTG-depth infinite-intermediate contradiction, and the three-state worked example all walk their cases soundly. The defects I found are systematic divergences where the **note narrative carries earlier, less-complete proof text than the revised per-claim contracts** — and in two places the note's version is not merely terser but logically broken.

### Note S7 proof carries two unjustified inferences that the per-claim S7/S7a already repair
**Class**: REVISE
**Foundation**: T10a.4 (T4PreservationUnderDiscipline); GlobalUniqueness; S7d; S7a
**ASN**: Note-narrative S7 proof (under "## Structural attribution").
(a) *Well-definedness:* "By S7b (element-level I-addresses), `zeros(a) = 3`, and by T10a.4 (T4PreservationUnderDiscipline, ASN-0034), `a` is T4-valid".
(b) *Uniqueness across documents:* "By S7d's postcondition, distinct documents have distinct document-level tumblers."
**Issue**:
(a) T10a.4's conclusion is "for every allocator `A` in the tree and every `t ∈ dom(A)`, `t` satisfies T4." To conclude `a` is T4-valid you must discharge the premise `a ∈ dom(A)` for some allocator `A` in the conforming tree. The note's S7 names no allocator and never asserts `a`'s membership; the note's S7a states only that content is "allocated under the tumbler prefix of the document," never `a ∈ dom(A_element) ⊆ 𝒯`. The premise is missing. The per-claim S7 inserts exactly this ("Before decomposing `a`… By S7a… `a ∈ dom(A_element)`… instantiating [T10a.4] at `A := A_element` and `t := a`"), and the per-claim S7a supplies the membership the note's S7a lacks.
(b) S7d's postcondition — in *both* the note and claim renderings — is "Distinct documents arise from distinct allocation **events**," not distinct tumblers. Two distinct events could a priori emit the same tumbler value; the passage to distinct addresses is GlobalUniqueness, not S7d. The note attributes tumbler-distinctness to "S7d's postcondition," overstating what S7d gives. The per-claim S7 explicitly flags this ("what it does not by itself supply is the passage from distinct events to distinct addresses… That passage is exactly GlobalUniqueness") and routes through GlobalUniqueness; the note omits the bridge entirely.
**What needs resolving**: Port the per-claim S7 reasoning into the note: in Well-definedness, establish `a ∈ dom(A_element) ⊆ 𝒯` (via S7a) before instantiating T10a.4, and add that membership statement to the note's S7a; in Uniqueness, derive document-tumbler distinctness from S7d's event-distinctness via GlobalUniqueness rather than attributing it to S7d's postcondition.

### Note narrative out of sync with the revised per-claim contracts (S8 depth step, S5 multiplicity bridge)
**Class**: REVISE
**Foundation**: OrdShiftHom frame (ultimately TA0); S8-depth; S5 postcondition
**ASN**:
- S8 note proof, "succ stays within a subspace" step: "by OrdShiftHom (b) `shift(v, 1)` again satisfies S8a; **by S8-depth it shares `v`'s depth `m`**." The per-claim S8 proof of the same step reads instead: "its depth is fixed by OrdShiftHom's frame, `#shift(v, 1) = #v = m` … an unconditional property of `shift` that does not presuppose `shift(v, 1) ∈ dom(M(d))`."
- S5 note proof, within-document construction, ends at "The within-document sharing multiplicity is `|{v : …}| = N + 1 > N`." The per-claim S5 adds: "Since `d` is the only document, this within-document count equals the global multiplicity `|{(d, v) : …}| = N + 1`" — the quantity the S5 postcondition actually bounds.
**Issue**: The per-claim formal contracts were revised but the note narrative was not synced, so the two copies disagree. For S8: instantiating S8-depth at `w := shift(v,1)` to get `#shift(v,1) = #v` requires discharging `shift(v,1) ∈ dom(M(d))` — the not-yet-established lockstep condition at that point in the proof (the step even covers the `succ(v)` undefined case, where `shift(v,1) ∉ dom(M(d))` and S8-depth does not apply). The conclusion is true, but via shift's frame, not S8-depth, so the note's justification is circular. This is the same defect a prior cycle resolved — but **the fix landed in the claim file (`S8.md`, unmodified in git, carrying the OrdShiftHom-frame text) and did not propagate to the note (`ASN-0036-strand-model.md`, modified in git, still circular).** I am aware this overlaps a previously-declined finding; the new evidence is that the decline's stated rationale — the circular text "no longer exists in the substrate" — holds only for the claim contract, while the note still reads "by S8-depth it shares `v`'s depth `m`." For S5: the note proves a bound on `|{v : …}|` while the postcondition quantifies `|{(d, v) : …}|`; with one document these coincide, but the note leaves the identification implicit.
**What needs resolving**: Rewrite the note's S8 depth step to ground `#shift(v,1) = #v` in OrdShiftHom's frame (not S8-depth), matching `S8.md`; complete the note's S5 within-document construction with the within-document-to-global multiplicity identification; and audit the note for further proof text left stale relative to the revised per-claim contracts (e.g. the note's D-SEQ Step 1 `m ≥ 3` case leaves `v₁ = 1` implicit where the claim states it).

VERDICT: REVISE