I read the foundation layer (ASN-0034 tumbler algebra) and then ASN-0036 as a system, tracing the cross-claim precondition chains. The major chains are sound:

- **S3** correctly joins AX-1 (base), S1 (inherited references), and AX-2 (new/redirected references); the inductive dichotomy is exhaustive and the guarded application of `Σ.M(d)(v)` respects the strict partial-function reading.
- **S7**'s `origin` well-definedness chain (S7a → T10a.4 gives T4-validity; S7b gives `zeros(a)=3`; T4b projections land in their domains; T0 comprehension builds `origin(a)∈T` with `zeros=2`; the four T4-valid conjuncts are discharged) is internally consistent, including the `k'=2` element-spawn implicit in the `zeros(a)=3` requirement meeting T10a's `zeros≤2` guard exactly.
- **S8**'s succ-confinement now grounds `#shift(v,1)=#v` in OrdShiftHom's frame (TA0), not in S8-depth's domain-restricted quantifier — the prior declined finding's fix is present in the body. The injectivity step likewise derives the common depth locally rather than from S8-depth.
- **D-CTG-depth → D-SEQ → ValidInsertionPosition** is sound: the m≥3 / m=2 dispatch is clean, the infinite-witness contradiction against S8-fin is valid, and `N` is correctly grounded as D-SEQ's `n` via NAT-card on the initial segment.
- **S5** is a genuine S0–S3-fragment model; it confirms S0's reference to S5 (the other declined finding) is valid.

The S7d common-allocator reroute (most recent commit) is sound — `d₁≠d₂` in a common allocator forces distinct indices `m≠n` hence distinct events, with no appeal to output-injectivity. But the reroute left an adjacent assertion that still needs the avoided lemma:

### S7d's single-valuedness clause reintroduces the T10a.7/T10a.6 dependence the reroute disavows, and is unconsumed
**Class**: REVISE
**Foundation**: T10a.6 (DomainDisjointness), T10a.7 (EnumerationInjectivity) — neither in S7d's Depends (which lists T4, T10a, TA5)
**ASN**: S7d (DocumentAllocationDiscipline). Body: *"The map `ev` is total and single-valued: each document's document-level address is produced by exactly one event of the discipline — every address of `𝒯` is either an allocator's base, established by one child-spawning event, or the `n`-th output of one allocator, produced by one `inc(·, 0)` step (T10a)."* Axiom: *"the map `ev` ... is total and single-valued — every address of `𝒯` is produced by exactly one event ... (T10a) — and injective ..."* Reroute disavowal, same claim: *"T10a.7 (EnumerationInjectivity)'s output-injectivity `n ↦ tₙ` ... we have no need of it — index-distinctness alone separates the two events."*
**Issue**: The single-valuedness clause "every address of 𝒯 is produced by **exactly one** event" is not one of the two facts S7 consumes — S7's Uniqueness step takes only (i) each document's tumbler arises from an allocation event, and (ii) distinct documents arise from distinct events (the injectivity). For the lift to GlobalUniqueness, ev needs only totality + injectivity; single-valuedness is never used. Worse, validating "exactly one event" requires precisely the uniqueness facts the reroute set out to avoid: within an allocator, no address appears at two indices (T10a.7's output-injectivity — the very `tₘ ≠ tₙ` the prose says it has "no need of"); across allocators, no address lies in two domains (T10a.6). The bare "(T10a)" citation papers over this — and S7a's precedent (which lists T10a *and* T10a.4 separately) shows this ASN's convention is to cite the specific T10a.* sub-claim used. So the clause is either a gratuitous "trace" of a posited property (essay-prose justifying why the posit holds) or an under-cited derivation; the reroute fixed the load-bearing injectivity path but this residue still leans on the avoided lemma. This is reviser drift: an adjacent assertion left standing after the path it shared was rerouted.
**What needs resolving**: Drop the unconsumed single-valuedness "trace" ("every address produced by exactly one event ... (T10a)") so the claim asserts only what S7 consumes (totality + injectivity), keeping the reroute's "no need of T10a.7" honest; or, if ev's single-valuedness is to be claimed, cite T10a.6 and T10a.7 in Depends and remove the disavowal of T10a.7.

A cross-claim prose pattern, logged for the record:

### Duplicated posit-justification boilerplate across D-CTG, S8a, S8-fin
**Class**: OBSERVE
**Foundation**: N/A
**ASN**: D-CTG (*"We posit it rather than derive it; the point of naming it is methodological. ... so a consumer, a formalization tool included, must treat it as an implementation obligation on permitted arrangements rather than as a theorem to discharge."*), S8a (*"We posit it rather than derive it; the point of naming it is methodological. ... so a consumer, a formalization tool included, must treat it as an implementation obligation ... rather than ... automatically satisfied by the existing axioms."*), S8-fin (*"We posit it rather than derive it; the point of naming it is methodological — several downstream proofs need an explicit, citable finiteness premise"*).
**Issue**: Three posits carry near-verbatim "we posit rather than derive / the point of naming it is methodological / a formalization tool included, must treat it as an implementation obligation" essay paragraphs. Each also restates, at length, that S0–S3/AX-1/AX-2 do not constrain the posited property. This is the "new prose around an axiom explains why the axiom is needed rather than what it says" pattern, replicated. It is visible only when reading the posits together as a set. (D-MIN's non-derivability paragraph is *not* in scope here — its `{[1,5],[1,6],[1,7]}` independence witness is a concrete example, which the brief excludes from meta-prose.)
**What needs resolving**: Consider compressing the repeated "posit, not theorem / implementation-obligation / formalization-tool" justification to a single terse line per posit (e.g. "Design posit; not entailed by S0–S3, AX-1, AX-2."), retaining only claim-specific content (D-MIN's witness, S8-fin's bijection-vs-`|·|` reasoning, the per-posit non-derivability fact). Stylistic; no soundness impact.

VERDICT: REVISE