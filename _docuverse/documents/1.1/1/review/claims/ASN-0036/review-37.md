I read the foundation statements and then the full ASN as a system, tracing the precondition chains that cross claim boundaries: the AX-1/AX-2 → S3 transition-grounding chain; the S8a/S8-depth/OrdShiftHom well-formedness propagation into S8's correspondence-run partition; the D-CTG → D-CTG-depth/D-MIN → D-SEQ contiguity chain and its shared use of S8-fin's enumerating bijection; the NAT-induction → D-PRED/D-INJ/D-MIN/D-SEQ induction scaffolding; and the S7a/S7b/S7d → S7 attribution chain into GlobalUniqueness.

The cross-claim reasoning is sound. Spot checks I verified as consistent: S3's inherited/new-redirected case split is exhaustive and each disjunct is discharged by the correct axiom (S1 vs AX-2); S8's `succ` depth-preservation is grounded in shift's frame (not circularly in S8-depth), and its TS2/TS3/TS4 invocations discharge their length/`n≥1` preconditions; OrdShiftHom's `m≥2` precondition is load-bearing for part (a) and available at every use site; D-INJ's image count genuinely does not assume `P ≤ n`, so the `P:=N+1, n:=N` pigeonhole in D-CTG-depth closes; the S7 length/separator-position arithmetic (`p`, the two zero positions, `#U(a)+1≥2` non-adjacency) is correct; S7d's event-injectivity split on `same_allocator` correctly stops at events and hands distinctness to GlobalUniqueness; D-MIN (T1/tumbler order) and D-SEQ (NAT-order/ℕ) use the correct order in their dual least/greatest-element inductions. The five declined findings were all checked against the current text and are correctly stale/absent (D-SEQ's third trichotomy case reads correctly with `h`/NAT-order/maximizes; D-CTG-depth's Depends lists neither D-PRED nor NAT-cancel; D-SEQ uses the direct P(N) induction not T0(a)/D-INJ; S8's depth step is frame-grounded; S5 is fully defined). No soundness, coverage, or grounding gaps found.

The two observations below concern noise, not correctness.

### S8-depth trailing Depends prose argues non-dependencies at essay length
**Class**: OBSERVE
**Foundation**: N/A
**ASN**: S8-depth (FixedDepthVPositions), the paragraph appended after the Formal Contract's Depends list: *"OrdinalShift and OrdShiftHom are *not* dependencies of the formal posit... S8a is excluded by the very same test, and it is worth saying why it does not earn a place the test would otherwise seem to grant it... to list it among the dependencies would assert a topological order the logic does not have, as though S8a had to be established before S8-depth could even be written down..."*
**Issue**: A full paragraph of meta-reasoning about the citation structure — defending why three claims (OrdinalShift, OrdShiftHom, S8a) are *not* dependencies — sits appended to the Depends slot. The underlying claims are accurate (the posit's statement `#u = #w` does contribute no S8a symbol; `T`/`dom`/`subspace`/`#` are the only symbols, grounded elsewhere), so this is correct-but-noisy: essay content in a structural slot that the precise reader must parse to extract the actual (short) dependency structure. This is the clearest concentration of "defense of absences" in the ASN.
**What needs resolving**: Reduce to a one-line note (e.g., "OrdinalShift/OrdShiftHom/S8a appear in the commentary but contribute no symbol to `#u = #w`; they are not dependencies of this posit") rather than a paragraph-length justification. No change to the dependency set itself.

### Near-verbatim "non-derivability" litany repeated across S8a, D-CTG, S8-depth
**Class**: OBSERVE
**Foundation**: N/A
**ASN**: The same boilerplate — *"...not from the content-stream invariants S0, S1, S2, S3, which govern the evolution of C alone, nor from the transition axioms AX-1 (which fixes only the empty base arrangement) and AX-2 (which constrains the targets of newly established V-mappings, never [the betweenness of / the depth of / the shape of] the keys)"* — appears near-verbatim in S8a (ArrangementDomainRestriction), D-CTG (VContiguity), and S8-depth (FixedDepthVPositions), each differing only in the claim-specific tail clause.
**Issue**: The litany is generic to the whole family of arrangement-shape posits (it lists the same four invariants and two axioms and asserts none constrains the keys). Repeating it three times near-verbatim is cross-claim redundancy that the "read as a system" reader notices. Each posit's genuinely claim-specific content is only the short tail ("never the betweenness of the keys" / "never the depth of the keys"). This is defensible as self-containment, hence OBSERVE, but it is the kind of defensive-justification repetition that compounds across the ASN.
**What needs resolving**: Consider collapsing each occurrence to the claim-specific tail plus a pointer to a single shared statement of the generic non-derivability fact, rather than restating the full "S0/S1/S2/S3 govern C; AX-1 empty base; AX-2 targets" chain in each posit. No correctness change.

VERDICT: OBSERVE