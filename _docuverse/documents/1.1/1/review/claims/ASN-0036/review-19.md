I read the foundation statements, then traced ASN-0036 as a system: the content/arrangement invariants (S0–S5), the structural-attribution chain (S7 ← S7a/S7b/S7d, with GlobalUniqueness and T10a.4 from ASN-0034), the arrangement-structure chain (S8 ← OrdShiftHom, with TS2/TS3/TS4/T1), and the text-subspace contiguity chain (V-sub → D-CTG → D-CTG-depth/D-MIN → D-SEQ → ValidInsertionPosition).

Most cross-claim seams hold. The S8 depth-handling matches the previously-declined finding's resolution — the body now routes `#shift(v,1)=#v` through OrdShiftHom's frame (TA0), not S8-depth, and the injectivity step derives `#u=#u'` locally; I did not re-raise that. The D-CTG → D-CTG-depth → D-SEQ precondition chain is unbroken: D-CTG's `zeros(v)=0 ∧ #v=#u` guard matches what D-CTG-depth's constructed witness `w` and D-SEQ's `[1,…,1,k]` witness supply, and the `m≥3`/`m=2` scope split is consistent across the two claims. The S8 succ-orbit partition (injective + acyclic + finite ⟹ disjoint paths) is sound, and the `i=0` vs `i≥1` case split correctly stays inside TS3's `n₁≥1` precondition. S7's origin construction (T0 comprehension → `zeros=2` via NAT-card → four-conjunct T4-validity) walks its cases. The defects below are in the counting machinery and one quantifier scope.

### Cardinality |·| applied outside NAT-card's domain
**Class**: REVISE
**Foundation**: NAT-card (NatFiniteSetCardinality) — `|·|` is posited only on `S ⊆ {j ∈ ℕ : 1 ≤ j ≤ n}`, characterised by a strictly increasing `f : {1,…,k} → ℕ` with `image f = S`.
**ASN**: ValidInsertionPosition Definition: `N = |V_1(d)|`, and its NAT-card Depends entry: *"transported across D-SEQ's bijection from that segment onto V_1(d), this fixes `N = |V_1(d)| = n`."* Also S5 Postcondition: `… |{(d, v) : v ∈ dom(Σ.M(d)) ∧ Σ.M(d)(v) = a}| > N`.
**Issue**: `|·|` is NAT-card's operator and is defined only for subsets of an ℕ-initial-segment. `V_1(d) ⊆ T` is a set of tumblers and `{(d,v) : …}` is a set of pairs; neither is a subset of `{j : 1 ≤ j ≤ n}`, so `|V_1(d)|` and `|{(d,v):…}|` lie outside NAT-card's domain (no `f : {1,…,k} → ℕ` can have a tumbler-set or pair-set as its image, so the "unique `k`" has no witness). ValidInsertionPosition closes the gap by "transporting `|·|` across a bijection" `{1,…,n} → V_1(d)`, but cardinality-invariance-under-bijection is not a foundation axiom — NAT-card furnishes only `|{1,…,n}| = n`. S5 has no bijection bridge at all; it asserts `|pair-set| = N+1` by informal counting. The discipline is respected everywhere else (`zeros(t) = |{i : 1 ≤ i ≤ #t ∧ tᵢ = 0}|` in S8a/S7/S7d/T4 keeps `|·|` on an `{1,…,#t}`-subset), so these two sites are inconsistent with the rest of the ASN.
**What needs resolving**: Express each count over a set that already lies in ℕ. For ValidInsertionPosition, count the last-component set `{k ∈ ℕ : [1,…,1,k] ∈ V_1(d)}` — a subset of ℕ, equal to `{1,…,n}` by D-SEQ, on which NAT-card gives `n` directly — or take `N := n` (D-SEQ's `n`) outright, dropping `|V_1(d)|` and the bijection-transport language. For S5, restate the multiplicity as the existence of `N+1` pairwise-distinct pairs (or count the explicit index set `{1,…,N+1} ⊆ ℕ`) rather than `|{(d,v):…}|`.

### S7a mis-scopes the element-allocator quantifier
**Class**: REVISE
**Foundation**: T10a.6 (DomainDisjointness) — for distinct allocators `X ≠ Y`, `dom(X) ∩ dom(Y) = ∅`.
**ASN**: S7a: *"Consequently, for a T10a-conforming element allocator `A_element`, every `a ∈ dom(Σ.C)` satisfies two memberships of distinct types — `A_element ∈ 𝒯` … and `a ∈ dom(A_element)`, the address lying in that node's domain …"*
**Issue**: The quantifier order as written binds `A_element` before `∀a`, i.e. `∃A_element ∀a : a ∈ dom(A_element)` — one element allocator whose domain contains every stored address. This is false: when content is allocated under more than one document, `dom(A_element)` (one document's element addresses) cannot contain all of `dom(Σ.C)`, and by T10a.6 distinct documents' element allocators have disjoint domains, so no single `A_element` holds two documents' addresses. The intended statement is `∀a ∃A_element` — for each `a`, the element allocator of `a`'s home document. S7's own Precondition states it correctly per-address ("for the document owner's T10a-conforming element allocator"), so the defect is confined to S7a's prose, but S7a is an exported claim and its statement is unsound as written.
**What needs resolving**: Re-scope so the element allocator is bound per address: for every `a ∈ dom(Σ.C)` there exists a T10a-conforming element allocator `A_element` (the allocator of `a`'s home document) with `A_element ∈ 𝒯` and `a ∈ dom(A_element)`.

### Non-empty worked example placed under ValidFirstInsertionPosition
**Class**: OBSERVE
**Foundation**: N/A
**ASN**: ValidFirstInsertionPosition's "Valid insertion position examples" block, whose first half — *"Non-empty case (binary predicate). Let subspace S = 1 and suppose V₁(d) = {[1, 1], [1, 2], [1, 3]} … That gives N + 1 = 4 positions."* — illustrates the binary `ValidInsertionPosition` predicate, defined in the sibling claim.
**Issue**: ValidFirstInsertionPosition defines the empty-case ternary predicate, but the worked example leads with the non-empty binary case, which belongs to ValidInsertionPosition. The example is correct and the binary/ternary contrast is pedagogically useful, but a reader seeking the binary predicate's illustration finds it under the wrong claim.
**What needs resolving**: Move the non-empty example to ValidInsertionPosition (retaining the empty-case example here), or state explicitly that the block exists to contrast both predicates. Placement only; no content change.

VERDICT: REVISE