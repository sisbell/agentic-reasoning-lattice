I read the foundation statements, then the ASN as a system, tracing each precondition chain across claim boundaries and checking every set-membership claim against the definitions in ASN-0034.

The two-component state model (Σ.C, Σ.M) and the S0–S5 invariant block are internally coherent; S1's and S5's proofs are sound, and S5's two witness constructions are genuine models of S0–S3. The S8 correspondence-run partition is a clean combinatorial argument (injective, acyclic, finite partial function ⇒ disjoint maximal chains), with the displacement-identity induction correctly handling the i=0 boundary via the `shift(t,0):=t` convention and TS3 only for amounts ≥ 1. The D-CTG-depth contradiction and the D-SEQ assembly walk their cases; the D-MIN non-derivability witness `{[1,5],[1,6],[1,7]}` genuinely satisfies D-CTG ∧ S8a ∧ S8-fin while violating D-MIN. The worked example is consistent at Σ₀–Σ₃ (zero-counts, origins, run boundaries all check). The previously-declined S8/S8-depth depth-justification has in fact been routed through OrdShiftHom's frame in the current body, so I do not re-surface it.

The findings below concern the S7 attribution chain's grounding and one lemma's dependency list.

### S7a/S7 conflate the allocator tree 𝒯 with the address domain dom(A_element)
**Class**: REVISE
**Foundation**: T10a (defines `𝒯` = *the set of allocators*); T10a.4 (instantiation requires `A ∈ 𝒯` and `t ∈ dom(A)`); AllocatedSet/GlobalUniqueness use `A ∈ 𝒯` for allocators, `a ∈ dom(A)` for tumblers
**ASN**: S7a — "every `a ∈ dom(Σ.C)` satisfies `a ∈ dom(A_element) ⊆ 𝒯`"; and S7 Well-definedness — "instantiating it at `A := A_element` and `t := a` — legitimate precisely because S7a places `a ∈ dom(A_element) ⊆ 𝒯`"
**Issue**: `𝒯` is the set of *allocators* (T10a: "the set of allocators induced by T10a"); `dom(A_element)` is a set of *tumbler addresses*. The written `dom(A_element) ⊆ 𝒯` therefore asserts *tumblers ⊆ allocators*, a type mismatch. What T10a.4's instantiation actually needs are two distinct memberships of distinct types: `A_element ∈ 𝒯` (the element allocator is a node of the tree) and `a ∈ dom(A_element)` (the address lies in its domain). Both hold — S7a's own prose establishes them ("A_element — a node of the global allocator tree 𝒯" and `a ∈ dom(A_element)`) — but the compressed chain `a ∈ dom(A_element) ⊆ 𝒯` mis-encodes the allocator-membership as a subset relation on the address domain, violating the definitions in scope at the exact point the proof claims to be "legitimate."
**What needs resolving**: State the licensing premise type-correctly as the conjunction `A_element ∈ 𝒯 ∧ a ∈ dom(A_element)` (not `dom(A_element) ⊆ 𝒯`) at both S7a and S7's Well-definedness step, so the T10a.4 instantiation `A := A_element, t := a` is discharged by memberships of the right types.

### S7 grounds strict positivity of field components in T0's carrier rather than in T4b / NAT-zero
**Class**: REVISE
**Foundation**: NAT-zero (`(A n ∈ ℕ :: 0 < n ∨ 0 = n)` — the actual source of `n ≠ 0 ⇔ n > 0` on ℕ); T4b (UniqueParse: each projection "returns a nonempty finite sequence over `ℕ⁺` … every component is strictly positive")
**ASN**: S7 Well-definedness — "each a finite sequence of natural numbers whose non-separator components are strictly positive — strict positivity discharged not by T4 but by T0's carrier ℕ (ASN-0034), under which `tᵢ ≠ 0 ⇔ tᵢ > 0`."
**Issue**: T0 only declares that components are ℕ-valued; it supplies no order property. The equivalence `tᵢ ≠ 0 ⇔ tᵢ > 0` is NAT-zero's content (`0 < n ∨ 0 = n`), and NAT-zero does not appear in S7's Depends — so the inference as attributed is ungrounded in the listed dependencies. Worse, the strengthening is redundant: T4b — which *is* in S7's Depends and is already invoked one clause earlier for the projections — directly postconditions that `N(a), U(a), D(a)` are `ℕ⁺`-valued (strictly positive). The clause re-derives, via a flawed route, a fact a cited foundation already hands over. This is the "new prose explaining why a foundation is needed rather than what it says" pattern.
**What needs resolving**: Ground the strict positivity of `N(a), U(a), D(a)` in T4b's `ℕ⁺` postcondition (already in scope), dropping the T0-carrier detour; or, if the `≠0 ⇔ >0` step is kept, attribute it to NAT-zero and add NAT-zero to S7's Depends.

### OrdShiftHom part (b) re-derives `rₘ > 0` with NAT arithmetic absent from its Depends
**Class**: REVISE
**Foundation**: OrdinalShift (postcondition `shift(v, n)_{#v} = v_{#v} + n ≥ 1`, already in OrdShiftHom's Depends); NAT-addcompat / NAT-addbound, NAT-closure, NAT-order, NAT-zero (the arithmetic the explicit step actually consumes)
**ASN**: OrdShiftHom Part (b) — "at `i = m`, `rₘ = vₘ + n ≥ 1 + 1 > 0`. Every component of `r` is positive, so `zeros(r) = 0`". OrdShiftHom's Depends lists only OrdinalShift, OrdinalDisplacement, TumblerAdd, TA0, S8a.
**Issue**: The conclusion `rₘ > 0` (needed for `zeros(r) = 0`) is reached by the inequality `vₘ + n ≥ 1 + 1 > 0`, which uses additive monotonicity and ℕ positivity/order — NAT-addcompat (or NAT-addbound), NAT-closure, NAT-order, NAT-zero — none of which is in the Depends list. The foundation claims of ASN-0034 meticulously cite exactly these NAT-* axioms for the same kind of step (OrdinalShift itself lists NAT-addcompat/NAT-order/NAT-closure for `vₘ + n ≥ 1`), so the omission is a genuine gap by the spec's own standard. Note the lemma is re-proving a fact it already has: OrdinalShift (cited) exports `shift(v,n)_{#v} = v_{#v} + n ≥ 1` as a postcondition.
**What needs resolving**: Either discharge `rₘ ≠ 0` by citing OrdinalShift's `shift(v,n)_{#v} ≥ 1` postcondition (eliminating the open arithmetic), or add the NAT-* foundations the explicit `vₘ + n ≥ 1 + 1 > 0` derivation consumes to OrdShiftHom's Depends.

VERDICT: REVISE