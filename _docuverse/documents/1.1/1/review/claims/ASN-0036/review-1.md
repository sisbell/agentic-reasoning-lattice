I read the foundation contracts and then ASN-0036 as a system, tracing the precondition chains across S0–S8, the contiguity lemmas (D-CTG, D-CTG-depth, D-MIN, D-SEQ), the shift homomorphism (OrdShiftHom), and the run-partition theorem (S8).

The core architecture is sound. The two-component split (Σ.C immutable / Σ.M mutable), S0→S1→S3 referential-integrity chain, S4/GlobalUniqueness origin identity, and S7's attribution chain all hold together. S8's correspondence-run partition is a genuinely careful proof: the lockstep-successor `succ` is shown injective (via TS2 at common depth), acyclic (via TS4 + T1 irreflexivity), and the shift-convention `shift(t,0):=t` is correctly fenced off from TS3's `n ≥ 1` precondition in the induction. OrdShiftHom is applied only to the V-side (where S8a holds), and the I-side images are placed in `dom(C)` via S3 rather than via OrdShiftHom — the correct routing. D-CTG-depth's infinite-intermediate contradiction is valid, and the worked example is internally consistent across Σ₀–Σ₃.

The serious problem is between two sibling claims in the contiguity cluster: **D-MIN is classified as a design requirement everywhere except in its own per-claim contract, where it is given a circular "derivation" from D-CTG.** A minor reviser-drift residue (orphaned proof labels, an implicit T10a instantiation, a loose uniqueness citation) rounds out the findings. One small phrasing nit: D-CTG-depth's Preconditions assert "m ≥ 3 (non-triviality bound, per the Preconditions)" — a self-reference inside the Preconditions slot; it should just state m ≥ 3 as the claim's scope.

### D-MIN carries a circular derivation; it is a design axiom, not a theorem
**Class**: REVISE
**Foundation**: T1 (LexicographicOrder), and the sibling design constraints D-CTG / S8-fin / S8a within this ASN
**ASN**: D-MIN per-claim contract, Step 3: *"By the contiguity constraint, V_1(d) is a single block with no internal gaps, and a document's text is laid down starting at the origin of its subspace... the smallest position the subspace admits is [1, 1, ..., 1], and that is where the block begins. A non-empty block contains its starting position, so [1, 1, ..., 1] ∈ V_1(d)."*
**Issue**: The Properties table and main body classify D-MIN as a "design requirement / design constraint," but the per-claim contract attaches a 4-step proof ending in ∎ that purports to *derive* `min(V_1(d)) = [1,…,1]` from contiguity plus positivity. Step 2 only establishes that `[1,…,1]` is a universal *lower bound* (`[1,…,1] ≤ v` for all `v`); Step 3 then asserts membership `[1,…,1] ∈ V_1(d)` by claiming "that is where the block begins" — which is exactly the conclusion D-MIN states. The step "a document's text is laid down starting at the origin of its subspace" smuggles in the thesis. The derivation is in fact impossible from the cited premises: the set `V_1(d) = {[1,5],[1,6],[1,7]}` satisfies D-CTG (contiguous — `[1,6]` is the only intermediate and it is present), S8a (all components positive, depth 2), S8-fin (finite), and S8-depth (common depth 2), yet `min = [1,5] ≠ [1,1]`. So D-MIN is logically independent of D-CTG/S8a/S8-fin and cannot be a theorem over them. Separately, the Depends list names only S8-depth, while the proof body invokes the contiguity constraint (D-CTG) and the finiteness constraint (S8-fin).
**What needs resolving**: Drop the 4-step derivation and present D-MIN purely as a design requirement (consistent with the table and the main-body treatment, and with how D-SEQ/ValidInsertionPosition already consume it as a premise). If D-MIN is instead to remain derived, it must cite the actual left-anchoring premise that forces the least element to the all-ones tuple (a strand- or operations-layer "block starts at origin" invariant) — and acknowledge that D-CTG + S8a + S8-fin alone do not entail it, exhibiting why the contiguous/positive/finite witness `{[1,5],[1,6],[1,7]}` is excluded. Either way, fix the Depends list to match whatever premises the final text uses.

### Orphaned proof labels (b), (c), (d) in ValidInsertionPosition
**Class**: OBSERVE
**Foundation**: N/A (internal labeling)
**ASN**: ValidInsertionPosition proof: *"...at j = 0 the position is v = min(V_1(d)) = [1, ..., 1] by D-MIN. **This is (d)**. Every component is then ≥ 1 ... so zeros(v) = 0 with **componentwise positivity (b)**, and OrdShiftHom (a) fixes v₁ = 1 ... giving exactly N + 1 positions **(c)**."*
**Issue**: The parentheticals "(d)", "(b)", "(c)" reference lettered clauses that do not exist — the Definition and Postconditions of ValidInsertionPosition are unlabeled prose. (`OrdShiftHom (a)` is a valid reference to OrdShiftHom's part (a), but (b)/(c)/(d) resolve to nothing.) This is reviser-drift residue from a prior version whose predicate had lettered conditions; a reader following the proof has to skip past dangling tags.
**What needs resolving**: Either label the Postcondition clauses (a)–(d) so the references resolve, or replace the parenthetical tags with plain prose ("…so v is zero-free and componentwise positive," etc.).

### S7 well-definedness invokes T10a.4 without isolating the instantiation premise
**Class**: OBSERVE
**Foundation**: T10a.4 (T4PreservationUnderDiscipline) — "for every allocator A in the tree and every t ∈ dom(A), t satisfies T4"
**ASN**: S7 proof, Well-definedness: *"By S7b (element-level I-addresses), zeros(a) = 3, and by T10a.4 (T4PreservationUnderDiscipline, ASN-0034), a is T4-valid."*
**Issue**: T10a.4 yields T4-validity only when instantiated at `a ∈ dom(A)` for a T10a-conforming allocator A. S7b supplies `zeros(a) = 3` (which alone does not give T4-validity — it says nothing about adjacency or boundary nonzero), and the inference relies on `a` being an output of the T10a allocator tree. The strand model asserts T10a conformance at the *document* level (S7d) and "allocation under the document's prefix" (S7a), and S4's proof invokes "a system conforming to T10a," but no labeled claim crisply states that *element-address* allocation is part of the T10a allocator tree — the premise that licenses the T10a.4 instantiation at `a`.
**What needs resolving**: State, at the S7 (or S7a/S7b) site, that every `a ∈ dom(Σ.C)` is produced by a T10a-conforming allocator within the global allocator tree (paralleling S7d's document-level assertion), so that T10a.4 instantiates at `a` to yield T4-validity. If this is already intended to follow from S7a + S4's system-conformance framing, make the chain `a ∈ dom(A_element) ⊆ 𝒯` explicit rather than implicit.

### S1 attributes uniqueness to T9 and T10 rather than GlobalUniqueness
**Class**: OBSERVE
**Foundation**: T9 (ForwardAllocation) gives per-allocator forward ordering; T10 (PartitionIndependence) gives cross-non-nesting-prefix distinctness; GlobalUniqueness packages the full result
**ASN**: S1 prose: *"New entries may be added — each at a fresh address guaranteed **unique by T9 and T10** (ASN-0034) — but no existing entry may be modified or removed."* (S1's Forward References note T9/T10 are "not used in S1's proof.")
**Issue**: The packaged "no two distinct allocation events produce the same address" guarantee is GlobalUniqueness, which S4 correctly cites for exactly this purpose. T9 (ordering) and T10 (non-nesting distinctness) are ingredients, not the uniqueness result itself; citing them here for "unique" is imprecise and inconsistent with S4's citation of the same fact. Non-load-bearing (explicitly a forward reference), so soundness is unaffected.
**What needs resolving**: Align the citation with S4 — attribute fresh-address uniqueness to GlobalUniqueness (optionally noting T9/T10 as the underlying ordering/distinctness ingredients), so the same guarantee is cited the same way across the ASN.

VERDICT: REVISE