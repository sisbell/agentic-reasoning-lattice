I read the foundation statements, then traced ASN-0036 as a system: every precondition chain that crosses a claim boundary, every shared term, every case split.

**Cross-claim soundness checks (all intact):**

- **Displacement chain (D-CTG → D-CTG-depth → D-MIN → D-SEQ → ValidInsertionPosition).** The shared `m` (common text-subspace depth from S8-depth) is consistent across all four. D-CTG-depth's intermediate witness `w` is verified `∈ T` (T0 comprehension), zero-free, same-depth, and strictly between `u` and `x` regardless of the unbounded last-but-one component, so the T0(a)/S8-fin contradiction lands. D-SEQ's `m=2` / `m≥3` split is exhaustive and the `m≥3` branch correctly fuses D-CTG-depth's shared prefix with D-MIN's all-ones minimum. The `j = m−1` boundary of D-CTG-depth's construction works (the `wᵢ=1` clause goes empty, the divergence witnesses still land at the right positions).
- **Attribution chain (S7b → S7a → S7 with T10a.4 / GlobalUniqueness).** T4-validity is correctly sourced from S7a→T10a.4 (not from S7b's bare `zeros=3`), and `zeros≥2` from S7b, jointly placing `a ∈ dom(E)` for T4b's projections. The event→address lift in Uniqueness correctly routes S7d's event-distinctness through GlobalUniqueness. No S7a/S7b cycle.
- **Run partition (S8).** `succ` is shown a partial function (out-degree ≤1) and injective (TS2 at the locally-derived common depth) and acyclic (TS4 + T1 irreflexivity); the chain decomposition, displacement-identity induction (convention at `k=0`, TS3 for `k≥1`, on both `v` and image `a`), and partition/uniqueness all hold. The previously-declined depth-step circularity is genuinely resolved — the body now grounds `#shift(v,1)=#v` in OrdShiftHom's frame.
- **S5** witnesses are valid models of S0–S3 (vacuous S0/S1 over an empty transition relation; S2/S3 discharged per construction; T3 distinctness). **OrdShiftHom**, **TS2/TS3/TS4** preconditions all met at use sites. No vestigial dependencies (S8-depth's framing role in S8 is a settled/declined matter).

I found no correctness defects. Two observations on prose that the precise reader must work around:

### S7 no-two-zeros-adjacent: four-case walk re-derives a one-line gap fact
**Class**: OBSERVE
**Foundation**: T4 (HierarchicalParsing) field-segment constraint; the separator gap `p₂ − p₁ = #U(a) + 1` is already computed in S7's own zero-count step via NAT-addassoc + NAT-sub left-telescoping.
**ASN**: S7 Well-definedness, the no-adjacent-zeros conjunct: *"each of `i` and `i + 1` independently equals one of the two members `#N(a) + 1` or `X` — a four-case domain, three of whose assignments we must eliminate before the fourth survives"* — followed by per-case irreflexivity / trichotomy eliminations and the final gap contradiction.
**Issue**: By the time this conjunct is discharged, S7 has already pinned the zero-index set to exactly `{p₁, p₂}` with `p₁ < p₂` and `p₂ − p₁ = #U(a) + 1 ≥ 2`. Non-adjacency then follows in one step: if `origin(a)ᵢ = origin(a)ᵢ₊₁ = 0` then `{i, i+1} ⊆ {p₁, p₂}`, and `i < i+1` with `p₁ < p₂` forces `i = p₁, i+1 = p₂`, whence `1 = p₂ − p₁ ≥ 2`. The three eliminated sub-cases (order-reversed `i=X, i+1=p₁`; the two coincident `i=i+1` assignments) are excluded by the `p₁ < p₂` and `i < i+1` facts already in hand. The walk is correct but is a redundant case enumeration of a fact the gap bound gives directly.
**What needs resolving**: Discharge the no-adjacent-zeros conjunct directly from the established separator gap `p₂ − p₁ = #U(a) + 1 ≥ 2` (two consecutive indices differ by `1 < 2`), replacing the four-sub-case enumeration with the order-forced single case.

### S7 Depends entries are use-site inventories; well-definedness is a single monolithic derivation
**Class**: OBSERVE
**Foundation**: N/A (structural slots in an editable claim)
**ASN**: S7's Depends list — e.g., the NAT-order entry recounts every `<`-comparison in the body (*"the separation … the four-case walk … the bound `1 = #U(a) + 1 ≥ 2` … closes the contradiction by forcing `1 < 1`"*), and the NAT-addcompat / NAT-sub / NAT-addassoc entries similarly replay the proof's instantiations site-by-site — together with the Well-definedness section, which is one ~1,500-word paragraph carrying the `zeros(origin(a)) = 2` + T4-validity derivation with inline NAT-* citations at every step.
**Issue**: This is the "use-site inventory" pattern the review discipline flags as degrading the argument: the Depends slot re-narrates the proof's derivation steps rather than naming what each foundation supplies, so a reader reconstructing the dependency graph must read the entire proof twice. The well-definedness paragraph compounds this — the load-bearing claim (origin(a) is the document-level prefix, T4-valid, `zeros=2`) is buried under exhaustive arithmetic bookkeeping that a downstream consumer does not need to re-walk. The argument is sound; the density is the cost.
**What needs resolving**: Reduce the Depends entries to what each foundation contributes (operator/clause supplied) and let the proof body carry the use sites; consider breaking the Well-definedness derivation into the named sub-results it establishes (length `p ≥ 1`; the two separator positions and their `≥ 2` gap; the four T4-valid conjuncts) so the structure is legible without skipping past the inventory. Stylistic — no soundness change.

VERDICT: OBSERVE