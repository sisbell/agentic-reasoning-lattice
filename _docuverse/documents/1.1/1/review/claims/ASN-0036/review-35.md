I read the foundation statements (ASN-0034's tumbler/NAT-* group) and then ASN-0036 as a whole, tracing the cross-claim precondition chains: the two-stream invariants (S0–S5), the structural-attribution chain (S7 → S7a/S7b/S7d → T10a.4/GlobalUniqueness), the V-position posits (S8a/S8-depth/S8-fin) and the S8 correspondence-run partition, and the sequential-position derivation (D-MIN/D-SEQ/D-CTG/D-CTG-depth/D-INJ/D-PRED) built on the locally-posited NAT-induction.

The mathematical content is sound. I verified the load-bearing chains specifically: S3's induction case-split exactly matches AX-2's range (inherited via S1+J0, new/redirected via AX-2); S7's well-definedness correctly discharges T4-validity (S7a→T10a.4) before invoking T4b's partial projections, and the `zeros(origin(a))=2`/T4-validity computation (separator distinctness via NAT-addassoc re-association + NAT-sub telescoping, the four-case adjacency walk) is rigorous; D-CTG-depth's witness `w` stays `< x` independent of the unbounded `j+1` component (comparison decided at `j`), so the pigeonhole against S8-fin via D-INJ closes; the S8 succ-confinement depth step correctly grounds `#shift(v,1)=#v` in shift's frame (TA0) rather than S8-depth's domain-restricted quantifier, and the injectivity step derives `#u=#u'` locally for TS2. The element-vs-document allocator layering (zeros=3 element addresses spawned via `k'=2` from zeros=2 document addresses, versions child-spawned into distinct allocators) is consistent with T10a/TA5(d). Edge cases (singleton V_1(d), m=2, empty arrangement) hold. I did not re-raise the five previously-declined findings; the current artifact matches the reviser's rationales (D-SEQ's third trichotomy case reads with `h`/NAT-order/maximizes; D-CTG-depth's Depends omit D-PRED/NAT-cancel; S5 is fully defined).

The findings below are structural, not mathematical.

### Σ.C (ContentStore) has no Formal Contract block, unlike its sibling Σ.M(d)
**Class**: REVISE
**Foundation**: T0 (CarrierSetDefinition, ASN-0034); Σ.M(d) (Arrangement)
**ASN**: Σ.C (ContentStore) — the entire claim is prose: "The *content store* `Σ.C : T ⇀ Val`: a partial function mapping Istream addresses to content values. `T` is the set of tumblers (ASN-0034); `Val`, its codomain, is an unspecified nonempty set … The domain `dom(Σ.C)` is the set of I-addresses at which content has been stored." There is no `*Formal Contract:*`, no `*Definition:*` slot, and no `*Depends:*`.
**Issue**: Σ.C is the only state-component declaration in the ASN without a Formal Contract. Its exact sibling Σ.M(d) (Arrangement) carries a full `*Formal Contract:*` with a `*Definition:*` (`Σ.M(d) : T ⇀ T`) and a `*Depends:*` citing T0 for `T`; the other definitional claims (Strand, V-sub, subspace) all have Definition slots. Yet Σ.C is cited as a grounding `*Depends:*` entry by at least ten claims (AX-2, S0, S1, S3, S4, S5, S7, S7a, S7b, S8), each of which says "Σ.C (ContentStore) — supplies the content store `Σ.C`, its domain `dom(Σ.C)`, …" or "its value codomain `Val`." Σ.C is in fact the sole grounding site for `Val` (S5's `w ∈ Val` is explicitly charged here). Under the ASN's own strict direct-citation discipline — which grounds even same-ASN symbols at their defining sites and obsessively distinguishes direct from transitive dependence everywhere else — a defining claim that grounds `T` and introduces `Val` but exposes no structured Definition or Depends is an inconsistency a downstream consumer (a formalization tool reading structured slots) hits: it cannot extract Σ.C's type `Σ.C : T ⇀ Val` or its dependence on T0 from any contract slot. Secondarily, Σ.C's prose attributes the arrangement type to S2 ("the arrangement declaration `Σ.M(d) : T ⇀ T` (S2)") when the declaration's defining site is Σ.M(d) (Arrangement); S2 only reads single-valuedness off that type.
**What needs resolving**: Give Σ.C a Formal Contract paralleling Σ.M(d): a `*Definition:*` slot stating `Σ.C : T ⇀ Val` with `dom(Σ.C)`/`Val` named (Val a primitive opaque codomain introduced here), and a `*Depends:*` citing T0 (CarrierSetDefinition, ASN-0034) for the carrier `T` at both domain and codomain-factor positions. Correct the analogy citation to point at Σ.M(d) (Arrangement) as the arrangement-type declaration site rather than S2.

### Strand (StrandDefinition) Definition has no Depends grounding its component symbols
**Class**: OBSERVE
**Foundation**: Σ.C (ContentStore); Σ.M(d) (Arrangement); T0 (CarrierSetDefinition, ASN-0034)
**ASN**: Strand (StrandDefinition), Formal Contract — has a `*Definition:*` ("The *strand* is the pair `(Σ.C, Σ.M)`, where `Σ.C : T ⇀ Val` … and `Σ.M` maps each document `d` to its arrangement `Σ.M(d) : T ⇀ T` …") but no `*Depends:*` block.
**Issue**: Strand's Definition restates the types of both components and uses `T` and `Val`, but grounds none of them via a Depends, where the ASN's discipline grounds same-ASN symbols at their defining sites (V-sub cites Σ.M(d) and subspace; subspace cites T0). This is a milder instance of the Σ.C gap (Strand at least has a Definition slot). Sound for a human reader; the symbols are recoverable from prose.
**What needs resolving**: Add a `*Depends:*` to Strand citing Σ.C (ContentStore) and Σ.M(d) (Arrangement) as the two component declarations it pairs, with T0 for `T` if grounding the carrier directly.

### S8-depth dependency-exclusion paragraph is use-site inventory in a structural slot
**Class**: OBSERVE
**Foundation**: S8-depth (FixedDepthVPositions)
**ASN**: S8-depth, the ~250-word paragraph after the Depends list: "OrdinalShift and OrdShiftHom are *not* dependencies of the formal posit … S8a is excluded by the very same test, and it is worth saying why it does not earn a place the test would otherwise seem to grant it … to list it among the dependencies would assert a topological order the logic does not have …"
**Issue**: This is the "posit framing sprawl" recurring across cycles (logged OBSERVE in review-34). The paragraph explains why three claims are *not* dependencies — a defensive use-site inventory about dependency bookkeeping that the precise reader must read past to reach the axiom's content. The same pattern recurs in many Depends entries as transitive-route disclaimers ("grounded here directly, not through the transitive routes D-MIN → NAT-induction → NAT-zero or D-MIN → S8-fin → NAT-zero"). It carries a real distinction (symbolic dependence vs. semantic co-ordination), so it is framing, not a soundness defect — but it is essay content in a structural slot.
**What needs resolving**: Condense the non-dependency reasoning to a single sentence or move it to a commentary remark outside the Depends apparatus; the semantic co-ordination of S8a with shift-preservation is already recorded in the shift-preservation commentary above the Depends.

VERDICT: REVISE