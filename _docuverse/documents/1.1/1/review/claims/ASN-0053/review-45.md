Reading the foundation statements against all five claims in sequence.

**S0 (Convexity).** The proof derives mixed transitivity steps (`start(σ) ≤ p` composed with `p ≤ q` giving `start(σ) ≤ q`, and `q ≤ r` composed with `r < reach(σ)` giving `q < reach(σ)`) by explicit four-case and two-case splits on the `≤` abbreviation, reducing each to T1's strict transitivity (c) or substitution of equals. The claim that T1 does *not* export these mixed steps is correct — T1's only transitivity export is `(A a,b,c ∈ T : a < b ∧ b < c : a < c)`. Both bound derivations are complete. The carrier conjunct `q ∈ T` is explicit in the preconditions. Proof is sound.

**WF (WellFormedSpanFromEndpoints).** The key discharge chain: (1) `s ≠ r` from T1 trichotomy disjointness; (2) T1's definition of `s < r` gives a witness `k` satisfying the minimality universal; (3) `#s = #r` eliminates T1 case (ii) — the proof walks both sub-cases of `#s + 1 ≤ #s` (strict and equality), each reaching `#s < #s` via NAT-addcompat's successor inequality and NAT-order's transitivity or substitution, refuted by irreflexivity; (4) T1 case (i) holds, giving `k ≤ #s` and `sₖ ≠ rₖ`, so `k = divergence(s, r)` by Divergence's uniqueness clause; (5) `divergence(s, r) = k ≤ #s` discharges D1's remaining precondition; (6) Divergence symmetry carries case (i) from `(s, r)` to `(r, s)`; ZPD's Relationship-to-Divergence then gives `zpd(r, s) = k`; TumblerSub's positive-branch postconditions supply `r ⊖ s ∈ T`, `Pos(r ⊖ s)`, and `actionPoint(r ⊖ s) = k ≤ #s = #(r ⊖ s)` (the last from TumblerSub's length postcondition with `#r = #s`). T12's preconditions are all met. Level-uniformity follows directly from TumblerSub's length postcondition and `#r = #s`. Proof is sound.

**S6 (LevelConstraint).** A single application of TumblerAdd's result-length postcondition `#(a ⊕ w) = #w` at `(a, w) = (s, ℓ)` under the well-formedness hypotheses gives `#reach(σ) = #ℓ`; level-uniformity `#ℓ = #s` closes the chain. All four of TumblerAdd's preconditions are listed in S6's preconditions. Proof is sound.

**S2 (EmptyDistinction).** Direct corollary of T12's postcondition (b), `s ∈ span(s, ℓ)`, under the same four well-formedness preconditions T12 assumes. No gap.

**S11 (DifferenceBound).** Reach membership (`reach(α), reach(β) ∈ T`) is secured first via TumblerAdd's carrier postcondition instantiated at each well-formed span. The boundary characterization is correct: `start(β) ∈ ⟦β⟧ ⊆ ⟦α⟧` gives `start(α) ≤ start(β) < reach(α)`; the reach bound is by contradiction — assuming `reach(β) > reach(α)`, the three membership conditions for `reach(α) ∈ ⟦β⟧` are met (carrier established above; `start(β) < reach(α)` from the start derivation; `reach(α) < reach(β)` by assumption), yielding `reach(α) ∈ ⟦α⟧` and hence `reach(α) < reach(α)`, refuted by T1 irreflexivity. The three-way partition of `⟦α⟧` by T1 totality is exhaustive and pairwise disjoint. Construction of `λ`: WF's preconditions (`start(α), start(β) ∈ T` as span starts; `start(α) < start(β)`; `#start(α) = #start(β)` from `level_compat`) are all discharged. Construction of `ρ`: `reach(β), reach(α) ∈ T` from the upfront carrier derivation; `reach(β) < reach(α)` from the current case; `#reach(β) = #reach(α)` derived via S6 applied to each level-uniform span (`#reach(σ) = #start(σ)`) combined with `level_compat`. The tightness argument picks `t ∈ ⟦β⟧` (non-empty by S2), verifies `start(α) ∈ ⟦λ⟧ ⊆ ⟦γ⟧` and `reach(β) ∈ ⟦ρ⟧ ⊆ ⟦γ⟧` (both by reflexivity of `≤` and the strict boundary inequalities of the neither-coincides case), discharges S0's bracketing precondition `start(α) ≤ t ≤ reach(β)` by the same case-split technique S0 itself uses for mixed steps, concludes `t ∈ ⟦γ⟧`, and derives the contradiction `t ∉ ⟦λ⟧ ∪ ⟦ρ⟧` from `t ≥ start(β) = reach(λ)` and `t < reach(β) = start(ρ)`. Every case in the count (0, 1, 2) follows from whether the respective interval is empty. Proof is sound.

VERDICT: CONVERGED