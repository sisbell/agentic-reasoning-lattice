I read the foundation contracts, then the ASN as a system — tracing each span operation's precondition chain back to T12/TumblerAdd/TumblerSub and the displacement claims D0–D2, and checking that "span," "level-uniform," and "reach" keep one meaning across S0–S11d, WF, WR, and SC. The algebra is, in the main, carefully built and has clearly been hardened over many cycles: the intersection/merge/split/difference proofs walk their cases, S9's uniqueness case-split is exhaustive (the equal-start/equal-reach configuration is correctly excluded via TA-LC), and S7's finite-vs-infinite obstruction is grounded in T0(b). The findings below concern a term that does not hold one meaning across the document, plus accreted meta-prose.

### "level-uniform span" precondition omits the well-formedness it silently requires
**Class**: REVISE
**Foundation**: S6 (LevelConstraint); T12 (SpanWellDefinedness); TumblerAdd / TA0 (reach defined).
**ASN**: S6 defines the term with length only — "A span σ = (s, ℓ) is *level-uniform* when level_compat(s, ℓ), i.e., #s = #ℓ." But S6's own postcondition chain is explicitly conditioned on well-formedness: "for a *well-formed* level-uniform span, start, width, and reach all share one tumbler length … Drop a precondition — say Pos(ℓ) — and s ⊕ ℓ need not be defined." The downstream callers state their preconditions *without* "well-formed": S1 — "α and β are level-uniform spans"; S4 — "σ = (s, ℓ) is a level-uniform span"; S8 — "component spans are level-uniform and mutually level-compatible." Yet each proof invokes reach(α)/reach(σ) (requires s ⊕ ℓ defined, i.e. Pos(ℓ), actionPoint(ℓ) ≤ #s) and S6's reach-length conclusion (requires well-formedness). By contrast S3 ("well-formed level-uniform spans") and S6 itself ("well-formed level-uniform span") carry the qualifier.
**Issue**: One term, two meanings. If a "span" is well-formed by convention (the intro's "by TA-strict every span is non-empty" and S2's "every *well-formed* span") then "well-formed" in S3/S6 is redundant; if it is not — and S2's contract treats well-formedness as a *precondition* on (s,ℓ), implying non-well-formed pairs exist — then S1/S4/S8 admit a level-uniform-but-not-well-formed input (e.g. ℓ with Zero(ℓ), #ℓ = #s) for which reach is undefined, S6 does not apply, and the proof has no foundation. A downstream consumer reading S1's stated precondition literally can supply such a pair.
**What needs resolving**: Fix the term to one meaning — either state once that every span is well-formed (and drop the redundant "well-formed" qualifier where it appears), or add "well-formed" to the S1, S4, S8 (and any other bare "level-uniform span") preconditions so the chain to S6/T12 is unbroken at the stated boundary.

### reach(σ) ∈ T discharged explicitly only in S11; relied on silently elsewhere
**Class**: OBSERVE
**Foundation**: TA0 / TumblerAdd (a ⊕ w ∈ T).
**ASN**: S11's revised proof adds an entire carrier-membership preamble — "we must locate the reach tumblers in the carrier T … TumblerAdd's carrier postcondition a ⊕ w ∈ T then yields reach(σ) … ∈ T … applied once at α and once at β" — because S11 genuinely tests reach(α) for membership in ⟦β⟧ = {t ∈ T : …}. But S1 (r' = min(reach(α), reach(β)) fed to WF as r), S3 (γ = (s, r ⊖ s), r = max(reach…)), S4 (d' = reach(σ) ⊖ p as a TumblerSub minuend), S8 (emit (s, r ⊖ s), r a reach), and S11c (γ' = (reach(β), reach(α) ⊖ reach(β))) all feed reach values into WF/TumblerSub without any reach ∈ T step.
**Issue**: The fact (reach(σ) = start ⊕ width ∈ T by TA0) is sound everywhere, so this is not a correctness gap — but the rigor is presented inconsistently. The elaborate S11 discharge reads as relocated prior-finding content; the others rely on an unstated global convention. A precise reader cannot tell whether reach ∈ T is assumed-everywhere or proved-where-needed.
**What needs resolving**: (OBSERVE — no revision required.) Either state the convention "reach(σ) ∈ T by TA0" once globally and trim S11's bespoke discharge, or apply the same one-line discharge uniformly.

### Defensive use-site inventory in S6 and S11 Depends entries
**Class**: OBSERVE
**Foundation**: TumblerAdd (result-length identity).
**ASN**: S6's TumblerAdd Depends entry closes with "This is the sole source of the addition result-length: the in-scope foundations supply only the subtraction length (TumblerSub…) and the round-trip identity (D1…), neither of which yields #(s ⊕ ℓ) = #ℓ …". S11's TumblerAdd Depends entry runs to a paragraph of consumption-tracking — "this membership is consumed twice. It is needed already in the boundary characterization … and again in the ρ-construction … (without this step they would not be known to lie in T) …".
**Issue**: This is meta-prose around an axiom dependency — it argues *why the dependency is needed and that no alternative supplies it* rather than stating *what the cited postcondition says*. It is the reviser-drift pattern the rubric names ("new prose around an axiom explains why the axiom is needed rather than what it says"); a reader must skip past the justification to recover the actual cited fact.
**What needs resolving**: (OBSERVE — no revision required.) Reduce these Depends entries to the postcondition cited and its instantiation site.

VERDICT: REVISE