Working through the ASN systematically: S0 (Convexity), WF (WellFormedSpanFromEndpoints), S6 (LevelConstraint), S2 (EmptyDistinction), S11 (DifferenceBound).

**S0** — The four-case unfolding of start(σ) ≤ q and the two-case unfolding of q < reach(σ) are both complete. The derivation does not cite ≤-transitivity as a T1 export, correctly notes T1's transitivity postcondition (c) covers only strict < chains, and fills the mixed ≤-< step by expanding the abbreviation. q ∈ T is listed explicitly as a precondition; no silent assumption. The informal Gregory Q11 citation is in the body of the argument but functions as supporting illustration, not a proof step. Proof is sound.

**WF** — T1 case (ii) elimination is fully cased: #s + 1 ≤ #s splits on the ≤ abbreviation into #s + 1 < #s (chained with NAT-addcompat's successor inequality via NAT-order transitivity to get #s < #s, refuted by irreflexivity) and #s + 1 = #s (substituted into the same successor inequality, same refutation). Case (i) then identifies k = divergence(s, r) ≤ #s via Divergence's uniqueness clause. Divergence symmetry carries the case (i) witness to the pair (r, s), and ZPD's Relationship-to-Divergence at (r, s) certifies zpd(r, s) = k is defined, licensing TumblerSub's positive branch and its exported Pos(r ⊖ s) and actionPoint(r ⊖ s) = k postconditions. T12's four preconditions are all discharged; D1's five preconditions are all discharged (the length inequality #s ≤ #r holds by equality). Level-uniformity follows from TumblerSub's length postcondition #(r ⊖ s) = #s (sub-case α, since #r = #s). Proof is sound.

**S6** — Direct application of TumblerAdd's result-length identity #(a ⊕ w) = #w at (a, w) = (s, ℓ) under TumblerAdd's preconditions, which are exactly S6's preconditions. The chain #reach(σ) = #ℓ = #s closes with level-uniformity. Sole dependency (TumblerAdd) is cited. Proof is sound.

**S2** — Invokes T12's postcondition (b), s ∈ span(s, ℓ), whose preconditions are exactly S2's preconditions. The set equality ⟦s, ℓ⟧ = span(s, ℓ) is definitional. Proof is sound.

**S11** — reach(α), reach(β) ∈ T are placed at the outset via TumblerAdd's carrier postcondition, which is needed both in the boundary characterization (to test reach(α) ∈ ⟦β⟧) and in the ρ-construction (to discharge WF's carrier preconditions for endpoints that are computed sums, not primitive start tumblers). The boundary characterization derives start(α) ≤ start(β) from S2's concrete postcondition start(β) ∈ ⟦β⟧ (S2 is cited), and reach(β) ≤ reach(α) by contradiction (reach(α) ∈ ⟦β⟧ ⊆ ⟦α⟧ would give reach(α) < reach(α)). The (L)/(M)/(R) partition is exhaustive by T1's totality, and membership in each region is correctly equated with ⟦β⟧ inclusion/exclusion. WF's preconditions are discharged for λ (start(α), start(β) ∈ T immediately; level_compat is the hypothesis) and ρ (reach(α), reach(β) ∈ T from TumblerAdd; #reach(β) = #reach(α) via S6 applied to each span then composed with level_compat). The tightness argument: start(α) ∈ ⟦λ⟧ ⊆ ⟦γ⟧ and reach(β) ∈ ⟦ρ⟧ ⊆ ⟦γ⟧ are verified; the bracketing start(α) ≤ t ≤ reach(β) is established by weakening (t < reach(β) to t ≤ reach(β)) and a case-split on the abbreviation for start(β) ≤ t (mirroring S0's own ≤-composition technique, explicitly noted); S0 then forces t ∈ ⟦γ⟧; t ∉ ⟦λ⟧ (since t ≥ start(β) = reach(λ)) and t ∉ ⟦ρ⟧ (since t < reach(β) = start(ρ)) complete the contradiction. All dependency citations are present and consumed. Proof is sound.

VERDICT: CONVERGED