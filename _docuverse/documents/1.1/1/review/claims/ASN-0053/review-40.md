**Audit trace**

**S0 (Convexity).** The lower-bound case analysis distributes start(σ) ≤ p with p ≤ q into four cases, each reducing to T1(c) or substitution of equals. The upper-bound case analysis distributes q ≤ r with r < reach(σ) into two cases. All cases are explicit, the abbreviation unfolding is correct, and the conclusion folds back to q ∈ ⟦σ⟧. T1 is the sole dependency, invoked only through postcondition (c) and the abbreviation — no undeclared ≤-transitivity. Sound.

**WF (WellFormedSpanFromEndpoints).** The T1 case-(ii) elimination is tight: #s = #r forces #s + 1 ≤ #s, which the proof splits into a strict sub-case (NAT-addcompat + NAT-order transitivity chain to #s < #s) and an equality sub-case (indiscernibility rewrites #s < #s + 1 to #s < #s), both refuted by NAT-order irreflexivity. The case-(i) witness k = divergence(s, r) ≤ #s then discharges D1's fifth precondition. TumblerSub's positive branch exports Pos(r ⊖ s) directly from zpd(r, s) being defined; actionPoint(r ⊖ s) = k ≤ #s = #(r ⊖ s) satisfies T12's actionPoint precondition. D1's preconditions (five of them) are all discharged, and D1 closes reach(γ) = r. Level-uniformity follows from #(r ⊖ s) = #s. Sound.

**S6 (LevelConstraint).** TumblerAdd's result-length identity #(s ⊕ ℓ) = #ℓ is earned under TumblerAdd's four preconditions, all of which S6's precondition block supplies. Level-uniformity (#ℓ = #s) closes the chain to #reach(σ) = #s. The note distinguishing what TumblerSub and D1 export versus what TumblerAdd exports is accurate and prevents over-citation. Sound.

**S2 (EmptyDistinction).** A single-step application of T12(b) under the same precondition block that T12 assumes. The witness is s itself. The empty-case framing (zero-width would require Pos(ℓ) to fail, which well-formedness forbids) is accurate. Sound.

**S11 (DifferenceBound).** The reach-carrier step (TumblerAdd at each σ ∈ {α, β}) is explicit and consumed twice — in the boundary derivation and in ρ's WF discharge. The boundary characterization is correct: start(β) ∈ ⟦β⟧ ⊆ ⟦α⟧ delivers start(α) ≤ start(β); the reach-bound contradiction at t = reach(α) requires reach(α) ∈ T and reach(α) < reach(β), both in hand. The three-sub-range partition follows from T1's totality with no gap; the classification of (L), (M), (R) members relative to ⟦β⟧ is individually verified. The λ construction (endpoints are primitive starts, T ∈ T immediately; level_compat is the direct precondition) and the ρ construction (endpoints are computed sums requiring the pre-established carrier facts; #reach(β) = #reach(α) via S6 + level_compat chain) both satisfy WF's three preconditions. The tightness argument picks t ∈ ⟦β⟧ (S2), brackets it between start(α) ∈ ⟦γ⟧ and reach(β) ∈ ⟦γ⟧, discharges S0's precondition start(α) ≤ t ≤ reach(β) via a case-split on the abbreviation for the mixed step, invokes S0 to force t ∈ ⟦γ⟧, then exhibits t ∉ ⟦λ⟧ (t ≥ reach(λ) = start(β)) and t ∉ ⟦ρ⟧ (t < start(ρ) = reach(β)) — a genuine contradiction. All five claims are internally consistent, all precondition chains are unbroken across claim boundaries, and no case is skipped.

VERDICT: CONVERGED