# Cone Review — ASN-0034/T10a.8 (cycle 5)

*2026-04-18 05:18*

Reading through the ASN carefully against previous findings, focusing on what's verifiable from the visible text and not already captured.

### T10a-N's Depends asserts a false cross-reference to T10a.3 and T10a.5 convention application
**Foundation**: The per-step convention TA5 establishes — citing NAT-addcompat explicitly for strict-successor (`n < n + 1`) and left order-compatibility (`p ≤ n ⟹ m + p ≤ m + n`) — is applied in T10a.8's standalone and its summary listing inside T10a's Formal Contract (both cite NAT-addcompat).
**ASN**: T10a-N's Depends clause under NAT-addcompat: "chaining sharpens TA5(d)'s equation `#a₂ = #a₁ + k'` to the strict length inequality `#a₂ > #a₁` — **matches the per-step convention T10a.3, T10a.5, and T10a.8 apply** for the structurally identical strict-successor plus left order-compatibility inference". But checking the current state: T10a.3's summary Depends inside T10a's Formal Contract lists only "T10a.1 (uniform sibling length) and TA5 (TA5(d): #inc(t, k') = #t + k' for k' > 0)" — no NAT-addcompat. T10a.5's summary Depends lists "T10a, T10a.1, T10a.3, T10a.4, T4, TA5, TA5-SigValid, T3, Prefix" — no NAT-addcompat. Only T10a.8 among the three cited companions actually applies the convention in its Depends.
**Issue**: T10a-N's present-tense assertion ("apply") claims a shared convention across T10a.3, T10a.5, and T10a.8 that only T10a.8 currently satisfies. A reader following T10a-N's cross-reference into T10a.3 or T10a.5 expecting structurally identical NAT-addcompat citations finds none. The claim pre-announces a convention that two of the three named companions have not yet been updated to follow (as separately flagged in cycles 3/4 for those properties). This is a distinct gap from the cycles 3/4 findings about T10a.3 and T10a.5 themselves — it is a claim-versus-reality mismatch within T10a-N's own Depends attribution.
**What needs resolving**: Either T10a-N's Depends attribution must be aligned with the current state of its companions (cite only T10a.8 as the exemplar, or qualify the convention as in-progress), or the attribution must be consistent with T10a.3 and T10a.5 once their own Depends are updated to explicitly cite NAT-addcompat — so that every cross-reference T10a-N makes terminates at a matching citation in the referenced property.
