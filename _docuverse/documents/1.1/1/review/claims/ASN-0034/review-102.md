# Cone Review — ASN-0034/T10a-N (cycle 7)

*2026-04-16 21:02*

### T10 T0 citation limited to well-ordering while proof also uses ℕ-order and carrier operators
**Foundation**: N/A (internal)
**ASN**: T10 (PartitionIndependence), *Formal Contract* — `Depends` paragraph for T0: "T0 (CarrierSetDefinition) — the least-element construction `k = min{j : 1 ≤ j ≤ ℓ ∧ p₁ⱼ ≠ p₂ⱼ}` invokes T0's well-ordering of ℕ, which makes `min(S)` well-defined for every nonempty `S ⊆ ℕ` and so licenses naming the least divergence index."
**Issue**: The T0 entry itemizes only well-ordering, but the T10 proof invokes T0 repeatedly elsewhere: (i) length `#p₁`, `#p₂`, `#a`, `#b` and component projection `p₁ᵢ`, `p₂ᵢ`, `aₖ`, `bₖ` — the same carrier operators that Prefix, T1, and TA5 explicitly attribute to T0 ("the definition uses length `#p` … and component projection `pᵢ` … which T0 introduces"); (ii) the non-strict ℕ-ordering in the length split "Case 1: `m ≤ n`" / "Case 2: `m > n`" and the conversion `m ≤ n ⟹ ℓ = m` (symmetric in Case 2), which invokes T0's `≤` on ℕ and its interaction with `min`; (iii) ℕ-order transitivity at the chain "`k ≤ ℓ ≤ min(m, n)`" and the subsequent "`k ≤ m`" / "`k ≤ n`" steps that license indexing into each prefix's agreement range. Under the per-step citation convention the document enforces — T1 enumerates irreflexivity/trichotomy/transitivity of `<` on ℕ, TA5 enumerates successor/discreteness/order-compatibility/`≤`-unfolding, Prefix enumerates length/index-range/`≤`-unfolding — T10's T0 paragraph is narrower than the proof's actual T0 usage.
**What needs resolving**: Extend T10's T0 entry to cite the additional ℕ-level and carrier-level appeals (length and component-projection operators; non-strict order `≤` for the `m ≤ n` / `m > n` split and the `ℓ = min(m, n)` identification; `≤`-transitivity for the chain `k ≤ ℓ ≤ min(m, n)` that feeds the `k ≤ m` and `k ≤ n` index bounds), or justify why T10 may leave these implicit while T1/TA5/Prefix cite the analogous steps explicitly.

### T10a-N states `#t₁ = #t₀` via TA5(c) but never consumes it
**Foundation**: N/A (internal)
**ASN**: T10a-N (AllocatorDisciplineNecessity), proof — "By TA5(c), `#t₁ = #t₀`. By TA5(d), since `k > 0` at the second step, `#t₂ = #t₁ + k`…"
**Issue**: The fact `#t₁ = #t₀` is stated as the opening length step but no subsequent step depends on it. The chain that reaches the Prefix conditions uses only `#t₂ = #t₁ + k` (TA5(d)), agreement of `t₂` with `t₁` on positions `1..#t₁` (TA5(b) for `k > 0`), and the T0 arithmetic/order facts that convert `k > 0` into `#t₁ ≤ #t₂`. The Depends paragraph for TA5 mirrors this: it itemizes TA5(c) as "gives `#t₁ = #t₀`" but never ties that conclusion to a downstream step, so the citation is dangling under the per-step convention (which requires each cited subclause to discharge an identified proof step). Either `#t₁ = #t₀` is load-bearing somewhere and the load is currently invisible, or the citation is vestigial from an earlier proof shape.
**What needs resolving**: Either tie `#t₁ = #t₀` to a step that consumes it (for example, if `t₁`'s length being equal to `t₀`'s is needed to argue `t₁` itself is a legitimate sibling output or to justify that the allocator's base and first output share a level), or remove the TA5(c) citation and the accompanying sentence so the Depends entry for TA5 names only the subclauses (TA5(b), TA5(d)) the argument actually uses.

### T10a-N carries a residual "k = 1" gloss after the argument was generalized
**Foundation**: N/A (internal)
**ASN**: T10a-N (AllocatorDisciplineNecessity), proof — "The argument is parametric in `k`: no step below depends on any specific value beyond `k > 0`, and **the witness `k = 1` is merely the minimal such value**."
**Issue**: The construction and every subsequent step now use the symbolic `k > 0` throughout — TA5(d) is instantiated as `#t₂ = #t₁ + k`, the T0 discreteness/order-compatibility chain derives `#t₁ ≤ #t₂` generically, and TA5(b) for `k > 0` is applied without fixing a value. No step of the argument instantiates `k = 1`, and no later sentence refers back to `k = 1` as a witness; the concluding paragraph likewise quantifies over "every `k > 0`". The clause "the witness `k = 1` is merely the minimal such value" therefore names a witness the proof does not actually use. Under the precision convention the document otherwise observes, an unused witness named inside the parametricity note is misleading — a reader looking for where `k = 1` is consumed will find no corresponding step.
**What needs resolving**: Either remove the "`k = 1` is merely the minimal such value" gloss so the parametricity sentence reads as "the argument is parametric in `k > 0`", or restore a concrete instantiation step (e.g., a trailing remark that specializing `k = 1` recovers the minimal witness) so the gloss has a referent in the proof.
