# Regional Review — ASN-0034/TumblerAdd (cycle 4)

*2026-04-20 13:50*

Looking over the ASN as a system, checking for issues not already flagged in Previous Findings.

### TumblerAdd's NAT-sub Depends bundles the two inverse forms that the axiom body stipulates must be independently cited
**Foundation**: NAT-sub (NatPartialSubtraction) — states right-inverse characterisation `(m − n) + n = m` and left-inverse characterisation `n + (m − n) = m` as independent clauses, with the accompanying rationale: "Both inverse forms are stated because citing either one without commutativity of addition would otherwise be tacit."
**ASN**: TumblerAdd's result-length argument: "the collapses `(k - 1) + 1 = k` and `k + (n - k) = n` are NAT-sub's right- and left-inverse characterisations." TumblerAdd's Depends entry for NAT-sub: "conditional closure of `k - 1` and `n - k`; inverse characterisations collapse the result-length identity."
**Issue**: The proof prose distinguishes the two forms correctly — `(k − 1) + 1 = k` is the right-inverse `(m − n) + n = m` at `m = k, n = 1`, and `k + (n − k) = n` is the left-inverse `n + (m − n) = m` at `m = n, n = k`. NAT-sub's body argues these two are independent precisely because the ASN withholds commutativity of addition on ℕ. TumblerAdd's Depends, however, cites them bundled as "inverse characterisations," mirroring the imprecision flagged in Previous Findings about NAT-cancel's summand-absorption citation ("summand absorption rules out..." without naming the symmetric form). A reader following TumblerAdd's Depends into NAT-sub cannot recover the form-to-site pairing without re-parsing the proof, and the bundled citation silently reconstructs the form-fusion that NAT-sub's body takes care to rule out.
**What needs resolving**: Either refine TumblerAdd's NAT-sub Depends entry to name each inverse form with its collapse site (right-inverse at `(k − 1) + 1 = k`, left-inverse at `k + (n − k) = n`), or rewrite the result-length argument so only one form is invoked (with the other step re-routed through content already named).

### T1 trichotomy proof names the first divergence position `k` via NAT-wellorder before case-splitting on whether any divergence exists
**Foundation**: NAT-wellorder (NatWellOrdering) — least-element principle applies only to nonempty subsets of ℕ.
**ASN**: T1 part (b), trichotomy setup: "Define the *first divergence position* `k` as the least positive integer at which `a` and `b` disagree — either because `aₖ ≠ bₖ` at some position present in both (`k ≤ m ∧ k ≤ n`), or because one tumbler is exhausted at position `k` while the other continues (`k = m + 1 ≤ n`, or `k = n + 1 ≤ m`). The least-element claim rests on NAT-wellorder applied to the nonempty subset of mismatched positions. Three cases are exhaustive." Then immediately: "*Case 1: no divergence exists.* Then `aᵢ = bᵢ` for all `i` with `1 ≤ i ≤ m ∧ i ≤ n`, and the two exhaustion-shape divergences are absent too..."
**Issue**: The setup paragraph names `k` as a definite object via NAT-wellorder's least-element principle, then "Three cases are exhaustive" positions Cases 1–3 as specialisations of that named `k`. But Case 1 is explicitly the case where the mismatch set is *empty* — the very precondition NAT-wellorder requires to deliver a least element. In Case 1, `k` is not a well-defined object, and the case closes via `¬(m + 1 ≤ n)`, `¬(n + 1 ≤ m)`, and the agreement hypothesis — content that does not reference `k` at all. The presentation introduces `k` as universally available across the case analysis when its existence is actually conditional on Cases 2 and 3. The "applied to the *nonempty* subset" qualifier signals the conditionality in passing but does not separate the definition of `k` from the case-split that determines whether it exists.
**What needs resolving**: Either restructure the trichotomy so the case split on "divergence exists / does not exist" precedes the definition of `k`, with `k` introduced only in the branches where the mismatch set is nonempty, or rewrite the setup so `k` is described as a potential witness whose existence is itself part of the case analysis — not a definite object the three cases merely specialise.
