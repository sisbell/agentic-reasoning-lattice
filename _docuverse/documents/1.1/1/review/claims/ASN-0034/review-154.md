# Cone Review — ASN-0034/T1 (cycle 7)

*2026-04-17 11:52*

### Uncounted NAT-order transitivity sites at Case 2's `k'<k` case-(ii) rebuttal (both sub-cases)

**Foundation**: NAT-order (NatStrictTotalOrder).

**ASN**: T1 (LexicographicOrder), part (b) Trichotomy, Case 2's `k' < k` reverse-witness rebuttal via case (ii): *"case (ii) requires `k' = n + 1` (or `m + 1`), but `k' < k` combined with the case hypothesis's conjunct `k ≤ n` (respectively `k ≤ m`) gives `k' < n` (respectively `k' < m`), contradicting both."* The NAT-addcompat enumeration for sites 4 and 5 explicitly confirms the derivation path: *"the strictly sharper inequality produced by substituting the sub-case's `k' = n + 1` into the agreement bound `k' < k` combined with the Case 2 hypothesis's conjunct `k ≤ n`... which yields `k' < n`"* — i.e., transitivity of `k' < k` with `k ≤ n` is performed first, substitution of `k' = n + 1` second.

**Issue**: The step "`k' < k` combined with `k ≤ n` yields `k' < n`" is a strict-with-non-strict transitivity of `<` on ℕ — structurally identical to the enumerated NAT-order transitivity site 2 (part (c) Case `k₂ < k₁` under case-(i) of `a < b`, which composes `k₂ < k₁` with `k₁ ≤ m` to yield `k₂ < m` via the same strict/equality disjunct unpacking). The mirrored sub-case performs the parallel composition `k' < k` with `k ≤ m` to yield `k' < m`. Neither composition appears in the six-site NAT-order transitivity enumeration, which lists only sites in part (c) and Case 3 of part (b). Under the declared per-instance convention and the stated citation policy that "each proof cites only the ℕ facts it actually uses," the two sub-cases contribute two distinct NAT-order transitivity sites at distinct symbols (`k' < n` vs `k' < m`), neither of which is counted. The enumeration's omission is asymmetric with its own treatment of the analogous part (c) site 2 and with its per-instance splitting of Case 3's branches and Case 2's other rebuttal sub-cases.

**What needs resolving**: Either enumerate the two Case 2 `k'<k` case-(ii) transitivity sites — raising NAT-order's transitivity count from six to eight and the preamble total from twenty to twenty-two, with per-instance splitting consistent with the rest of the enumeration — or restructure the proof step so the contradiction at each sub-case routes through an already-enumerated NAT-order site (for instance, substituting `k' = n + 1` first to rewrite the agreement bound `k' < k` as `n + 1 < k` and composing with `k ≤ n` under an already-counted transitivity site, or refactoring to use `k' ≤ n` directly from minimality plus the Case 2 conjunct without a separate strict composition). Whichever route is chosen, the enumeration must attribute every transitivity consumption the proof text executes.
