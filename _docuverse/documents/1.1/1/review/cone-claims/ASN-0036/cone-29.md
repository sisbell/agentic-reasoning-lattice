Reading through the foundation statements, then each claim in the ASN, then the cross-claim dependency structure.

**Design posits.** D-CTG, S8-depth, S8-fin, S8a, and Σ.M(d) are correctly framed as axioms or design constraints. S8a's equivalence derivation (`zeros(t) = 0 ⟺ (A i : 1 ≤ i ≤ #t : tᵢ > 0)`) via NAT-card's empty-set characterization and NAT-zero's `0 < n ∨ 0 = n` is sound. The depends for each proved step are accurate.

**OrdShiftHom parts (a) and (b).** Part (a): `m ≥ 2` puts position 1 strictly below the action point `m`, so TumblerAdd's copy rule gives `r₁ = v₁`, and `subspace(r) = r₁ = v₁ = subspace(v)`. Part (b): copied prefix components `rᵢ = vᵢ ≥ 1` (from S8a on `v`); action-point component `rₘ = vₘ + n ≥ 1` (OrdinalShift's exported lower bound); `#r = m ≥ 2` (TA0). Every component strictly positive, so `zeros(r) = 0`. Both parts are correctly proved. The instance `[1,3,5] ⊕ [0,0,2] = [1,3,7]` is correctly computed.

**D-CTG-depth first postcondition.** The contradiction argument is sound. Given two positions `u < x` in `V_1(d)` disagreeing at some interior component, NAT-wellorder supplies the first disagreement `j` with `2 ≤ j ≤ m−1`; T1(i) at `k = j` (case (ii) fails since `m+1 ≤ m` is impossible for equal-depth tumblers) gives `uⱼ < xⱼ`. The witness `w` (components: `uᵢ` for `i ≤ j`; `n` at `j+1`; `1` thereafter) is in T by T0 comprehension, has `subspace(w) = 1` (since `j ≥ 2` puts position 1 in the copied prefix), satisfies `u < w` (T1(i) at `k = j+1`, since `n > uⱼ₊₁`) and `w < x` (T1(i) at `k = j`, since `wⱼ = uⱼ < xⱼ`), and has `zeros(w) = 0` (all components ≥ 1 via S8a on `u` and the choice `n ≥ 1`). D-CTG forces each `w ∈ V_1(d)`. T0(a) iterates arbitrarily large values of `n`, T3 makes each witness distinct, contradicting S8-fin. The edge case `j = m−1` is handled: the clause `wᵢ = 1` for `j+2 ≤ i ≤ m` is empty, `w < x` is decided by T1(i) at `k = m−1`, and `n` is free to grow without bound.

**D-CTG-depth second postcondition (reduction).** Forward direction: if the guard-admitted intermediate `v` disagrees with `u` at some `j ∈ {2,...,m−1}`, then `v₁ = u₁ = 1` (D-CTG's `subspace(v) = 1` and `u ∈ V_1(d)`) excludes component 1, NAT-wellorder gives the least such `j ≥ 2`, T1(i) yields `uⱼ < vⱼ`, the first postcondition gives `xⱼ = uⱼ`, so `xⱼ < vⱼ`, making `x < v` by T1(i) — contradicting `v < x`. Hence `v` shares the prefix; T1(i) at `k = m` collapses `u < v < x` to `uₘ < vₘ < xₘ`. Converse: prefix-sharing plus `uₘ < vₘ < xₘ` lifts directly to `u < v < x` by T1(i) at `k = m` (two separate applications, once for `u < v` and once for `v < x`). The equivalence is established in both directions. The note that the prefix conjunct is load-bearing is correct: a `v` with `v₂ ≠ u₂` can satisfy `uₘ < vₘ < xₘ` while lying outside `(u, x)` when `u₂ = x₂` forces `x < v`.

---

### OrdShiftHom precondition mixes formal and informal domain language
**Class**: OBSERVE
**Foundation**: S8a (ArrangementDomainRestriction); OrdShiftHom formal contract
**ASN**: OrdShiftHom, Preconditions: *"`v` is a V-position with `#v = m ≥ 2`; `n ≥ 1`. For part (b), additionally `v` satisfies S8a (`zeros(v) = 0` and `vᵢ ≥ 1` for every `i`)."*
**Issue**: "V-position" in this specification carries a technical reading — element of `dom(Σ.M(d))` for some `d`. Under that reading, S8a holds automatically for any such element (by S8a's domain constraint `dom(Σ.M(d)) ⊆ {t ∈ T : zeros(t) = 0 ∧ #t ≥ 2}`), making the "additionally v satisfies S8a" clause for part (b) redundant. The proof's structure reveals a different intent: part (a) genuinely needs only `v ∈ T` with `#v = m ≥ 2`, and part (b) adds S8a as a real constraint — which only makes sense if "V-position" is being used informally to mean "tumbler with V-position shape" (depth ≥ 2), not "element of the arrangement domain." The two readings give inconsistent interpretations of what the "additionally" contributes.
**What needs resolving**: Replace the informal "V-position" label in the precondition with the explicit structural condition `v ∈ T` with `#v = m ≥ 2`, so that "additionally v satisfies S8a" for part (b) is unambiguously a genuine additional constraint rather than a repetition of what domain membership already implies. Alternatively, annotate that "V-position" is used informally here to mean the structural shape only.

VERDICT: OBSERVE