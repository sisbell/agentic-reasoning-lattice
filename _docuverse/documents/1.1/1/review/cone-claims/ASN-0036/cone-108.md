## Audit

**Σ.M(d):** Definitional. Type declaration `T ⇀ T` depends on T0 for both ends of the partial-function type. No symbols appear in the formal statement beyond T0's carrier. Clean.

**subspace:** Depends solely on T0 for the component projection `v₁` and the length operator `#·`. The depth guard `1 ≤ #v` is the `j = 1` instance of T0's component-projection domain condition `{j ∈ ℕ : 1 ≤ j ≤ #v}` — not a fresh introduction of `≤`, but a reading of T0's projection-applicability condition. T0's nonemptiness postcondition `(A a ∈ T :: 1 ≤ #a)` is an exported axiom clause, and the guard is that postcondition instantiated at `a := v`. T0's dependence on NAT-order grounds `≤` transitively; `subspace` need not re-cite NAT-order for a condition it reads off an exported T0 axiom rather than introduces for its own purposes. V-sub's statement that "T0 does not export the literal constant `1`" is about `1` as a value name (the text-subspace identifier), not about `≤` as part of T0's projection semantics — these are distinct uses, and the claims are not inconsistent. The totality argument (T0's nonemptiness discharges the guard for every `v ∈ T`) is correct.

**S8-depth:** Formal statement uses only `dom(Σ.M(d))` (grounded at Σ.M(d)), `subspace(·)` (grounded at subspace), and `#·` (grounded at T0). Explicit exclusions of S8a and OrdShiftHom from the Depends list are sound: neither symbol appears in `#u = #w`. The grounding gap for non-text subspaces is correctly documented as a posit obligation, not a derived result. The note that S8 consumes S8-depth as the subspace-wide common depth (not per-link via OrdShiftHom) is consistent with the formally posited quantifier range.

**S8-fin:** The bijection formulation is correctly motivated by the out-of-scope issue with `|·|` on tumbler sets. Each symbol in the formal statement is accounted for: NAT-carrier for `ℕ`, NAT-zero for the base-state witness `n = 0`, NAT-closure for the lower bound `1`, NAT-order for `<` and `≤` in the injectivity clause and index domain, T0 for the codomain carrier, Σ.M(d) for the arrangement domain. The base-state uniqueness argument (`n = 0` is the only witness when the codomain is empty) is sound: any `n ≥ 1` gives a non-empty domain from which no total function maps into `∅`. The n=0 empty-arrangement check is correctly grounded at NAT-zero. No `|·|` operator appears.

**V-sub:** Definition `V_S(d) = {v ∈ dom(Σ.M(d)) : subspace(v) = S}` depends on Σ.M(d) for the arrangement domain, subspace for the predicate, and NAT-closure for the literal `1` in the text-subspace specialization `V_1(d)`. The argument that neither `subspace` nor T0 exports `1` as a named constant (in contrast to `≤` as a projection-applicability relation) is consistent with the system's dependency conventions. Disjointness and coverage follow from `subspace` being single-valued; these are correct consequences of the definition without needing to be listed as separate postconditions.

**D-MIN:** The claim's three dependencies for the right-hand side `[1,1,...,1]` are correctly traced: S8-depth supplies the common depth `m` (well-defined when V_1(d) ≠ ∅ because any two elements share a subspace and S8-depth applies), T0's comprehension axiom at `p = m`, `r ≡ 1` supplies the witness in T (existence; uniqueness by T0's extensionality), and NAT-closure supplies `1 ∈ ℕ` as the component value. The min-existence argument ("fold the binary minimum across finitely many elements, well-defined by T1's totality and order-independent by T1's transitivity") is sound: the fold over a finite non-empty totally ordered set produces a unique minimum that belongs to the set, with transitivity ensuring the fold is commutative and associative. V_1(d) is finite because V_1(d) ⊆ dom(Σ.M(d)) (by V-sub's definition) and dom(Σ.M(d)) is finite (by S8-fin); the "subset of a finite set is finite" step follows from S8-fin's bijection by restriction and is standard. The non-derivability witness {[1,5],[1,6],[1,7]} is consistent with the described depth-guard on D-CTG's betweenness quantifier. The statement that `min(V_1(d))` restricted to fixed-depth m-tuples equals standard lex order on integer m-tuples is correct: T1's case (ii) (prefix extension) never fires between same-depth tumblers, so only case (i) (component divergence) applies, yielding standard lex.

VERDICT: CONVERGED