## Audit Narrative

**Structural scan.** I traced every dependency cited in each claim's Depends list against the actual proof steps, checked the foundation statements for any symbol used without grounding, and verified all universal case analyses for completeness.

**Posits (S8a, D-CTG, S8-depth, S8-fin, NAT-induction).** All five are correctly labeled as design posits or foundation posits, with explicit statements that they are not derived and with accurate explanations of why derivation fails. NAT-induction's placement note ("stated here only because ASN-0036 is where its absence first bites") is accurate and its independence from the NAT-* order-and-addition axioms is correctly asserted as a classical fact.

**D-PRED.** The induction on H = {n : n=0 ∨ ∃i: i+1=n} is correctly structured: 0 ∈ H by the left disjunct; closure under successor because k+1 witnesses the right disjunct for k+1 regardless of k's membership (a key move the descent argument cannot make). The exclusion of j=0 via `0 < 0+1 = 1 ≤ j ⟹ 0 < j` with the two-case split on `≤`-definition (pure `<`-transitivity when `1 < j`, indiscernibility of `=` when `1 = j`) is correctly assembled from NAT-addcompat, NAT-closure, and NAT-order. Depends list matches.

**D-INJ.** Base P=1: singleton image enumerated by the vacuously-strict map f.1=h.1; NAT-card value clause at q:=1 applies. Step P→P+1: least μ from NAT-wellorder; ρ's injectivity in the same-branch upper case uses NAT-cancel (correct); surjectivity's below-k₀ sub-case uses NAT-discrete and successor reflection (correctly decomposed into the equality sub-case via NAT-cancel and the strict sub-case via NAT-addcompat + irreflexivity); surjectivity's above-k₀ sub-case draws predecessor i from D-PRED, then places i in the successor branch via NAT-discrete and the same successor reflection. The prepend construction's strictly-increasing verification (seam, beyond-seam, spanning-seam cases) is correct, with the spanning-seam mixed chain closed by the two-case split on `≤`-definition. Depends list matches and no D-PRED/NAT-cancel from the declined stale finding remains.

**D-CTG-depth.** The ordering-fix by T1 trichotomy, the first-disagreement j by NAT-wellorder, the prefix agreement chain (interior placement via NAT-discrete then minimality, component 1 via shared subspace), and the k=j pinning (k<j gives uₖ=xₖ against clause (i)'s uₖ<xₖ; k>j gives uⱼ=xⱼ against the disagreement) are all correctly argued. The witness construction covers every component with an ℕ-valued value, T0 comprehension grounds w∈T. Zero-freeness zeros(w)=0 is derived in three cases: u-copied components from S8a's positivity Consequence, new component nₖ>uⱼ₊₁>0 by NAT-order transitivity, tail components =1>0 by NAT-closure's Consequence. D-CTG applies with all guards met. The finiteness contradiction reads N from S8-fin, applies T0(a) exactly N+1 times (a finite iteration of fixed length, not a choice principle), produces N+1 pairwise-distinct witnesses in V_1(d)⊆dom(M(d)), pulls back through f's surjectivity to N+1 distinct indices in {1,...,N} (injectivity of k↦rₖ from f's single-valuedness plus distinctness of w^(k)), then D-INJ at P:=N+1, n:=N gives cardinality N+1 while NAT-card's upper bound gives ≤N, the mixed chain closing to N<N against irreflexivity. Depends list correctly excludes D-PRED and NAT-cancel as direct entries (they are transitive through D-INJ) and correctly describes NAT-discrete's direct use at (m,n):=(i,m) for interior range placement.

**Formal contracts.** Precondition and postcondition fields are consistent with the proofs. The declined finding (D-CTG-depth falsely listing D-PRED/NAT-cancel as direct deps with ρ-construction language) is confirmed absent from the current artifact. No stale residue detected.

VERDICT: CONVERGED