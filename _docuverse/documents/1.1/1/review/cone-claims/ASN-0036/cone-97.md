## Audit

I read the ASN claim by claim, tracking every symbol to its grounding, every proof step to its cited foundation, and every precondition chain from caller to callee.

**Foundation layer (NAT-*, T0, T1, T3, T4).** All foundation statements are internally consistent. The dependency ordering is correct: NAT-carrier → NAT-zero, NAT-order → NAT-closure, NAT-addcompat, NAT-discrete, NAT-wellorder, NAT-cancel → T0 → T1, T3, T4, NAT-card. No circularity.

**Definitions (Σ.M(d), subspace, V-sub, S8a, D-CTG, S8-depth, S8-fin).** All posits and definitions are well-formed. Σ.M(d) types the arrangement as T ⇀ T and exports only the domain symbol; all downstream claims ground against it. S8a's `zeros(t) = 0` unfolding via NAT-card's k=0 case and NAT-zero's 0 < n ∨ 0 = n is correct. S8-depth's exclusion of S8a from its Depends list is correctly reasoned — `dom(Σ.M(d))` is grounded at Σ.M(d), and `#` at T0; S8a contributes no symbol to `#u = #w`. S8-fin's bijection formulation for finiteness is the correct choice given NAT-card's scope restriction to subsets of initial ℕ segments.

**NAT-induction.** The ω·2 counterexample argument correctly shows that the NAT-* group (well-ordering, discreteness, ordered cancellative addition) does not entail generation from 0. The posit is correctly stated and its Depends list (NAT-carrier, NAT-zero, NAT-closure) is accurate.

**D-PRED.** The H = {n : n = 0 ∨ ∃i. i+1 = n} induction is sound. Base: 0 ∈ H by left disjunct. Closure: k+1 ∈ H with witness i=k (k+1 ∈ ℕ by NAT-closure). H = ℕ by NAT-induction. Exclusion of j=0 at guard j ≥ 1: 0 < 0+1 = 1 (NAT-addcompat + NAT-closure left identity), chain with 1 ≤ j via NAT-order mixed transitivity gives 0 < j, irreflexivity gives j ≠ 0. The observation that well-ordering does not underwrite this — the ω·2 model being the witness — is correctly stated and explains why NAT-induction is the load-bearing axiom. Only existence is exported; no uniqueness claimed. ✓

**D-INJ.** Induction on P. Base P=1: image {h.1}, vacuously strictly increasing length-1 enumeration, NAT-card value clause reads cardinality 1. Step P→P+1: μ = min S by NAT-wellorder; unique k₀ by injectivity; ρ defined piecewise. Injectivity of ρ: both-below-k₀ by identity; straddling by chain a < k₀ ≤ b < b+1 (NAT-addcompat + NAT-order transitivity) giving a < b+1 hence a ≠ b+1; both-above-k₀ by NAT-cancel right cancellation (NAT-addcompat gives only a+1 ≤ b+1, insufficient alone). Surjectivity of ρ onto {1,...,P+1}\{k₀}: below-k₀ sub-case uses NAT-discrete to descend j < P+1 to j+1 ≤ P+1, then NAT-cancel to j ≤ P; above-k₀ sub-case uses D-PRED to extract predecessor i, NAT-discrete + NAT-cancel to place i in the successor branch's range. k₀ is missed by successor-inequality argument (NAT-addcompat). Prepend construction: g.1 = μ, g.(r+1) = g′.r. Image = {μ} ∪ S′ = S. Strict increase: seam uses least-element minimality + trichotomy; beyond-seam from g′; spanning-seam by mixed transitivity a < b ∧ b ≤ c ⟹ a < c. NAT-card value clause at q=P+1 reads cardinality P+1. ✓

**D-CTG-depth.** The proof by contradiction is well-structured. WLOG u < x is justified by symmetry of the interior disagreement set and the fact that the witness construction anchors on the smaller element. First disagreement j by NAT-wellorder. Pinning the T1 witness to k=j: clause (ii) impossible (m < m+1 by NAT-addcompat, trichotomy); k < j gives agreement by subspace value at k=1 or interior minimality at k≥2 (NAT-discrete + NAT-order transitivity to place k in interior range, then j-minimality); k > j gives agreement at j by T1's prefix clause, contradicting j being a disagreement point. Witness w: components ℕ-valued at all positions (u's components from S8a positivity Consequence; n > uⱼ₊₁ > 0 by NAT-order transitivity; 1 > 0 from NAT-closure Consequence); T0 comprehension applies at length m ≥ 1. u < w: agreement through j, w_{j+1} = n > u_{j+1}, T1 clause (i) at k=j+1 ≤ m. w < x: agreement through j-1 (component 1 by subspace; components 2..j-1 by j-minimality via NAT-discrete), w_j = u_j < x_j (from k=j pinning), T1 clause (i) at k=j, position condition j < m from j+1 ≤ m via NAT-addcompat + NAT-order transitivity. zeros(w) = 0: each component positive (S8a for first j, transitivity for j+1, NAT-closure Consequence for tail), NAT-card empty-set characterization converts component-positivity to zeros = 0. D-CTG gives w ∈ V_1(d). Finiteness step: N from S8-fin; N+1 applications of T0(a) (feeding each output's (j+1)-th component as next bound) give n₁ < ... < n_{N+1}; N+1 distinct positions w⁽ᵏ⁾ in dom(Σ.M(d)) (distinct by T3 on j+1 component); surjectivity gives rₖ ∈ {1,...,N}; k ↦ rₖ injective (f single-valued); D-INJ at P=N+1, n=N gives |{rₖ}| = N+1; NAT-card upper bound gives ≤ N; N+1 ≤ N chains with N < N+1 (NAT-addcompat) via NAT-order transitivity to N < N; irreflexivity closes. ✓

All Depends lists match the symbols actually consumed in the respective formal contracts. No grounding gaps, no silent preconditions, no unwalked cases.

VERDICT: CONVERGED