The foundation statements are consistent and well-formed. The dependency chain from NAT-carrier through NAT-order, NAT-closure, NAT-wellorder, NAT-addcompat, NAT-discrete, NAT-card, NAT-zero, T0, T0(a), T1, T3, T4 is clean. S8a, S8-fin, S8-depth, V-sub, subspace, and D-CTG are all coherent posits with the correct Depends entries.

**D-CTG-depth proof audit.** The contradiction structure is sound. The disagreement set `{i : 2 ≤ i ≤ m−1 ∧ uᵢ ≠ xᵢ}` is a non-empty subset of ℕ, so NAT-wellorder yields a least element j. The T1 witness k is correctly pinned to j (k < j contradicts uₖ = xₖ from minimality; k > j contradicts j being a disagreement). The direction uⱼ < xⱼ follows from T1 clause (i) (clause (ii) is impossible since m+1 ≤ m is false by NAT-addcompat). The w construction covers the j = m−1 edge case correctly (the clause wᵢ = 1 for j+2 ≤ i ≤ m is vacuously empty; wₘ = n is the sole modified component). Both u < w (witness k = j+1, agreement on 1…j, wⱼ₊₁ = n > uⱼ₊₁, j+1 ≤ m) and w < x (witness k = j, agreement on 1…j−1, wⱼ = uⱼ < xⱼ, j ≤ m) are correctly discharged through T1(i). The component positivity of w (all wᵢ ≥ 1) follows from S8a on u for i ≤ j, n > uⱼ₊₁ ≥ 1 for i = j+1, and wᵢ = 1 for i ≥ j+2. T0(a) is correctly instantiated at t = u, i = j+1, with successively increasing bounds to generate n₁ < n₂ < … yielding distinct w's by T3. The infinite set in dom(M(d)) contradicts S8-fin. One imprecision survives:

---

### D-CTG-depth formal contract misrepresents D-CTG's guard condition, and the proof body gives an inverted causal explanation for the S8a check
**Class**: OBSERVE
**Foundation**: D-CTG (VContiguity)
**ASN**: D-CTG-depth, Precondition 5 ("V_1(d) is contiguous (D-CTG): if u, x ∈ V_1(d), u < w < x, subspace(w) = 1, #w = #u, **and w satisfies S8a**, then w ∈ V_1(d)"); and proof body ("We also verify that w satisfies S8a — **necessary because D-CTG ranges over V_1(d) ⊆ dom(M(d))**")
**Issue**: D-CTG's actual inner-quantifier guard is `zeros(v) = 0` (plus v ∈ T, subspace(v) = 1, #v = #u, u < v < q). S8a is `zeros(t) = 0 ∧ #t ≥ 2`. Precondition 5 substitutes "w satisfies S8a" for D-CTG's guard, overstating it by adding the `#w ≥ 2` conjunct that D-CTG does not check. The proof body then gives a causal explanation that inverts the logic: it says S8a is "necessary because D-CTG ranges over V_1(d) ⊆ dom(M(d))," which reverses the direction — D-CTG's *conclusion* places w in V_1(d) ⊆ dom(M(d)), making S8a a *consequence* of that conclusion, not an input requirement. What actually needs to be verified before invoking D-CTG is `zeros(w) = 0`; the `#w ≥ 2` half of S8a is not checked by D-CTG at all. In the current proof's scope (m ≥ 3, so #w = m ≥ 3 ≥ 2 always), no soundness gap results — D-CTG's guard is satisfied and the contradiction holds — but the precondition and explanation misrepresent D-CTG's interface to any downstream reader.
**What needs resolving**: In precondition 5, replace "w satisfies S8a" with "zeros(w) = 0" to match D-CTG's actual guard. In the proof body, correct the causal explanation: S8a is not a guard for D-CTG; what the proof verifies before invoking D-CTG is zeros(w) = 0 (part of D-CTG's guard), and `#w ≥ 2` follows independently from #w = m ≥ 3.

VERDICT: OBSERVE