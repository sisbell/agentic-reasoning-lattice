I read the foundation contracts and then ASN-0053 as a system. The core arguments are sound: the order-composition discipline in S0 (deriving ≤-transitivity and mixed ≤-< steps from T1(c) plus the abbreviation, never citing a non-exported ≤-transitivity) is correct and reused consistently in S11's tightness step; WF correctly discharges D1's `divergence(s,r) ≤ #s` from `#s = #r` and routes TumblerSub's positive branch through ZPD's Relationship-to-Divergence; S6's length chain rests only on TumblerAdd's exported result-length identity; S2 is a one-step read of T12(b); and S11's decomposition `⟦α⟧ \ ⟦β⟧ = (L) ∪ (R)`, the boundary characterization (which correctly places `reach(α) ∈ T` before testing membership), the WF preconditions for both λ and ρ, and the convexity-based tightness contradiction all chain cleanly. The worked example checks out. Findings below.

### S11 cardinality partition: case (a) trigger `α = β` is not the boundary condition the proof establishes
**Class**: REVISE
**Foundation**: TumblerAdd (TumblerAdd) — its exported postconditions are `a ⊕ w ∈ T`, `#(a ⊕ w) = #w`, `a ⊕ w > a`, `a ⊕ w ≥ w`; it exports no left-cancellation/injectivity of `⊕`.
**ASN**: S11 *Postconditions*: "its cardinality is 0 when **α = β**, 1 when exactly one boundary coincides, and 2 when neither coincides." The proof body instead writes "(a) Both boundaries coincide (α = β): difference is empty — 0 spans."
**Issue**: The cardinality refinement is a three-way partition whose other two arms are stated in boundary-coincidence terms ("exactly one boundary coincides", "neither coincides"), but case (a) is stated as the representation-level equality `α = β`. The condition the proof actually discharges for an empty difference is *both boundaries coincide* (`start(α) = start(β) ∧ reach(α) = reach(β)`), which gives `⟦α⟧ = ⟦β⟧`. "Both boundaries coincide ⟹ α = β" requires recovering `width(α) = width(β)` from `start(α) ⊕ width(α) = start(β) ⊕ width(β)` with equal starts — i.e. left-cancellation/injectivity of `⊕`, which TumblerAdd does not export. So the three stated triggers are not demonstrably exhaustive: a both-boundaries-coincide pair that is not known to satisfy `α = β` falls under none of the three labels, yet has cardinality 0. A downstream consumer reading "cardinality 0 iff α = β" cannot verify it against the foundation.
**What needs resolving**: State case (a)'s trigger in the same boundary-coincidence vocabulary as cases (b) and (c) — e.g. "0 when both boundaries coincide" (equivalently `⟦α⟧ = ⟦β⟧`) — so the partition is manifestly exhaustive without appeal to an unestablished `⊕`-cancellation property; or, if `α = β` is intended, establish (or cite) `⊕` left-injectivity as the bridge.

### S2 — defensive type-coherence prose for a comparison no one is making (reviser drift)
**Class**: OBSERVE
**Foundation**: ActionPoint / Span preconditions — `actionPoint(ℓ) ∈ ℕ`, precondition `actionPoint(ℓ) ≤ #s`.
**ASN**: S2 prose: "This second condition is a comparison of natural numbers — actionPoint(ℓ) is the ℕ action point of the length — not of the end offset s ⊕ ℓ, which is a tumbler." Restated in *Preconditions*: "The last is a comparison of natural numbers (actionPoint(ℓ) ∈ ℕ), **not the type-incoherent comparison of the tumbler s ⊕ ℓ against #s**."
**Issue**: The precondition `actionPoint(ℓ) ≤ #s` is plainly an ℕ–ℕ comparison; nothing in the claim writes or invites `s ⊕ ℓ ≤ #s`. The text twice defends against a type error the claim never commits — the "new prose explaining why a comparison is well-typed rather than what it says" pattern, duplicated across the prose body and a structural slot. A precise reader must skip past it to follow the single-step argument.
**What needs resolving**: Drop the type-incoherence disclaimer (both occurrences); stating `actionPoint(ℓ) ≤ #s` with `actionPoint(ℓ) ∈ ℕ` already carries the typing.

### S6 — counterfactual "drop a precondition" prose imagines an excluded case (reviser drift)
**Class**: OBSERVE
**Foundation**: TumblerAdd (TumblerAdd) — preconditions `Pos(w)`, `actionPoint(w) ≤ #a`.
**ASN**: S6 prose: "Drop a precondition — say Pos(ℓ) — and s ⊕ ℓ need not be defined, so the length identity has nothing to stand on." Restated in *Preconditions*: "They are not implied by level-uniformity: a level-uniform pair with Pos(ℓ) failing has reach(σ) undefined, and the length identity below does not hold of it."
**Issue**: `Pos(ℓ)` and `actionPoint(ℓ) ≤ #s` are stated preconditions of the claim. The prose imagines the claim's own precondition failing and narrates the consequence — the "paragraph imagines a case the precondition already excludes" drift pattern — and does so twice (prose + Preconditions). It explains why the precondition is needed rather than what the claim asserts; it does not advance the length-identity argument.
**What needs resolving**: Remove the counterfactual ("drop Pos(ℓ) …") in both places; the Preconditions field already lists `Pos(ℓ)` and `actionPoint(ℓ) ≤ #s` as required, which is the operative content.

### WF / S11 — relocated "no detour" and "cited explicitly" justifications in structural slots (reviser drift)
**Class**: OBSERVE
**Foundation**: TumblerSub (TumblerSub), TumblerAdd (TumblerAdd) — TumblerSub exports `Pos(a ⊖ w)` exactly when `zpd(a,w)` is defined; TumblerAdd is the shared base under S6.
**ASN**: WF prose: "We need no detour through the divergence-point components to read off positivity …"; echoed in WF's TumblerSub *Depends* entry: "(no detour through the divergence-point components is needed)". And S11's TumblerAdd *Depends* entry: "The proof names both identities in their own right — rather than only through S6's packaged length consequence — so TumblerAdd is cited here explicitly; it is the same foundation S6 is grounded on."
**Issue**: These read as responses to earlier review questions relocated into the proof and dependency fields rather than removed — meta-commentary about *why a citation is present / why a detour is avoided*, not statements of what is used. The "no detour" remark appears twice; the "cited explicitly … same foundation S6 is grounded on" remark explains a citation-bookkeeping choice. They are noise the precise reader works around.
**What needs resolving**: State the facts directly (`Pos(r ⊖ s)` from TumblerSub's positive-branch postcondition; TumblerAdd supplies `a ⊕ w ∈ T` and `#(a ⊕ w) = #w`) and drop the justifications for why they are mentioned or not detoured through.

VERDICT: REVISE