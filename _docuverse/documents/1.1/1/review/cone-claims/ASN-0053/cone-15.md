Audit narrative follows, then findings.

**SC (SpanClassification).** The five-case split is well-structured. Exhaustiveness holds by construction — at each decision branch (reach(α) vs start(β); then start(α) vs start(β) under the WLOG; then reach(α) vs reach(β)) exactly one of <, =, > applies by T1 total order. Mutual exclusivity follows from the same trichotomy at each branch. Non-degeneracy (start(σ) < reach(σ)) is correctly grounded in T12 postcondition (b). The case (iv) containment derivation — four-way case split on the ≤-abbreviation for the lower bound, two-way case split for the upper bound — correctly avoids claiming T1 exports mixed transitivity, reducing every step to T1(c) or substitution of equals. Intersection characterization is complete.

**S6 (LevelConstraint).** Sound. TumblerAdd's result-length postcondition #(a ⊕ w) = #w, instantiated at (s, ℓ) under the well-formedness preconditions, gives #reach(σ) = #ℓ; level-uniformity closes #ℓ = #s. The claim correctly identifies that TumblerSub's length identity and D1's round-trip do not yield this result, so TumblerAdd is the unique source.

**WF (WellFormedSpanFromEndpoints).** Sound. The equal-length hypothesis #s = #r eliminates T1 case (ii) cleanly: #s + 1 ≤ #s splits by NAT-order's ≤-definition into strict and equality sub-cases; NAT-addcompat's successor inequality feeds T1 transitivity in the strict sub-case and indiscernibility-of-= in the equality sub-case; both deliver #s < #s, refuted by irreflexivity. Divergence case (i) then supplies k = divergence(s, r) ≤ #s, discharging D1's remaining precondition. TumblerSub's positive-branch postconditions (Pos, actionPoint = zpd = k) are invoked correctly; actionPoint ≤ #(r ⊖ s) = #s follows from ActionPoint's type. D1 closes reach(γ) = r.

**S11c (DifferenceOverlap).** Cases 1 and 2 are correctly set up. Element-chasing is complete in both directions for Case 1. For Case 2, the ⊆ direction correctly derives the lower bound start(β) ≤ t from the mixed chain start(β) < start(α) ≤ t by explicit case analysis (strict branch via T1(c); equality branch via substitution). The construction of witness spans γ and γ' correctly invokes WF; S6 supplies #reach(β) = #reach(α) for Case 2's length precondition; TumblerAdd's carrier postcondition supplies reach(β), reach(α) ∈ T. One gap identified below.

---

### S11c Case 2 ⊇ direction: mixed strict-nonstrict chain cited to T1 without case analysis
**Class**: REVISE
**Foundation**: T1 (LexicographicOrder) — postcondition (c): `(A a,b,c ∈ T : a < b ∧ b < c : a < c)`; abbreviation `a ≤ b ≡ a < b ∨ a = b`
**ASN**: S11c (DifferenceOverlap), Case 2 ⊇ direction — *"The Case 2 hypothesis supplies start(α) < reach(β), so the chain start(α) < reach(β) ≤ t composes transitively (T1) to start(α) < t"*
**Issue**: T1 exports only strict-strict transitivity (postcondition (c): `a < b ∧ b < c → a < c`). The cited chain `start(α) < reach(β) ≤ t` is a mixed strict-nonstrict composition, which T1 does not export. Grounding it requires expanding `reach(β) ≤ t` via the ≤-abbreviation into `reach(β) < t ∨ reach(β) = t`, then handling each branch (T1(c) in the strict branch; substitution in the equality branch) to conclude `start(α) < t`. SC's formal contract is explicit: "Neither composition is a T1 export; each is derived in the proof by case analysis on the abbreviation." The ⊆ direction of Case 2 follows this discipline precisely (it expands `start(α) ≤ t` and handles both branches). The ⊇ direction omits the case analysis in both the proof body and the Depends section, which describes the step only as "composes the Case 2 hypothesis start(α) < reach(β) with reach(β) ≤ t" without the case split.
**What needs resolving**: The ⊇ direction must supply the missing case analysis: expand `reach(β) ≤ t` into `reach(β) < t ∨ reach(β) = t`; in the strict branch apply T1(c) to `start(α) < reach(β)` and `reach(β) < t` to obtain `start(α) < t`; in the equality branch substitute `reach(β) = t` into `start(α) < reach(β)` to obtain `start(α) < t`; conclude `start(α) ≤ t` via the ≤-abbreviation. The Depends section's description of the ⊇ direction should likewise spell out the case split, matching the precision of the ⊆ direction's entry.

VERDICT: REVISE