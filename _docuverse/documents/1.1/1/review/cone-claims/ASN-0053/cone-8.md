I read each claim against the foundations and tracked every precondition chain end-to-end.

**S0 (Convexity).** The four-case unfolding of the lower bound (start(σ) ≤ p composed with p ≤ q) and the two-case unfolding of the upper bound (q ≤ r composed with r < reach(σ)) both reduce correctly to T1(c) or substitution of equals. The proof's explicit disclaimer that it is not citing a named ≤-transitivity from T1 — but deriving each step from T1(c) and the abbreviation — is accurate. No gap.

**WF (WellFormedSpanFromEndpoints).** All five of D1's preconditions are discharged before D1 is invoked. The non-trivial one — divergence(s, r) ≤ #s — is established by showing T1's case (ii) witness (k = #s + 1 ≤ #r) is refuted under #s = #r: unfolding the ≤ into its two disjuncts, the strict sub-case combines NAT-addcompat's successor inequality with NAT-order transitivity to reach #s < #s; the equality sub-case substitutes directly to the same conclusion; NAT-order irreflexivity closes both branches. This hands control to T1's case (i), identifying k = divergence(s, r) ≤ #s via Divergence's uniqueness clause. TumblerSub's positive-branch postconditions (Pos(r ⊖ s), actionPoint(r ⊖ s) = k, #(r ⊖ s) = max(#r, #s) = #s) are then assembled correctly. Level-uniformity and T12's preconditions are fully discharged before D1 is applied. No gap.

**S6 (LevelConstraint).** TumblerAdd's result-length postcondition #(a ⊕ w) = #w, instantiated at (s, ℓ) under the well-formedness preconditions, gives #reach(σ) = #ℓ; composing with the level-uniform hypothesis #ℓ = #s closes the chain. The precondition structure is correct. No gap.

**S2 (EmptyDistinction).** Directly applies T12 postcondition (b), s ∈ span(s, ℓ), to the well-formedness preconditions — which are exactly T12's own preconditions — and reads span(s, ℓ) = ⟦s, ℓ⟧ through the Definition. No gap.

**S11 (DifferenceBound).** The reach carrier facts reach(α), reach(β) ∈ T are correctly derived from TumblerAdd applied once each under σ's well-formedness. The boundary derivation is sound: start(α) ≤ start(β) from start(β) ∈ ⟦β⟧ ⊆ ⟦α⟧; reach(β) ≤ reach(α) by contradiction — assuming reach(β) > reach(α) yields reach(α) ∈ ⟦β⟧ ⊆ ⟦α⟧, forcing reach(α) < reach(α), which T1 irreflexivity refutes. The three-way partition (L)/(M)/(R) is exhaustive and mutually exclusive by T1 totality. For λ: WF's carrier preconditions are immediate (starts of well-formed spans lie in T), the order precondition is the case condition, and the length precondition is level_compat directly. For ρ: WF's carrier preconditions are the reach-membership facts already placed, the order precondition is the case condition, and the length precondition #reach(β) = #reach(α) is discharged through S6 applied to each span in turn, chained via level_compat. The tightness argument is complete: S2 supplies a witness t ∈ ⟦β⟧ with start(β) ≤ t < reach(β); both brackets start(α) ∈ ⟦λ⟧ and reach(β) ∈ ⟦ρ⟧ are verified by checking the ⟦·⟧ membership conditions explicitly; the bracketing precondition for S0 is discharged by case-splitting start(β) ≤ t (T1(c) in the strict branch, substitution in the equality branch) for the lower half, and weakening t < reach(β) for the upper half; S0 then forces t ∈ ⟦γ⟧ = ⟦λ⟧ ∪ ⟦ρ⟧, while t ≥ start(β) = reach(λ) and t < reach(β) = start(ρ) separately exclude t from each component — contradiction. No gap.

VERDICT: CONVERGED