Reading the full dependency cone from NAT-* foundations through T0/T1/T3/TumblerAdd/TumblerSub/ZPD/Divergence/ActionPoint/TA-Pos up through TA-strict/TA0/D1/D2/T12, then through WF/S6/WR and into S3/S4/S3b.

**WR (WidthRecovery).** D2 is invoked at (a, b, w) = (s, reach(σ), ℓ). All nine preconditions are discharged: carrier membership and Pos(ℓ)/actionPoint(ℓ) ≤ #s come from σ's well-formedness; s < reach(σ) from TA-strict; a ⊕ w = b from the definition of reach; #s = #reach(σ) from TA0's length postcondition and level-uniformity; divergence(s, reach(σ)) ≤ #s from the inline case analysis eliminating T1 case (ii) via equal lengths. D2's conclusion w = b ⊖ a instantiates to ℓ = reach(σ) ⊖ s = width(σ). Sound.

**WF (WellFormedSpanFromEndpoints).** The divergence bound argument is complete: T1 witnesses s < r; the equal-length hypothesis eliminates case (ii) by driving #s + 1 ≤ #s to contradiction via NAT-addcompat and NAT-order's ≤-unfolding (both the strict and equality sub-cases yield #s < #s, refuted by irreflexivity); case (i) gives k ≤ #s and sₖ ≠ rₖ, qualifying for Divergence case (i) uniqueness. TumblerSub's positive branch is entered via ZPD's Relationship-to-Divergence certifying zpd(r, s) is defined. The well-formedness postconditions — Pos(r ⊖ s), actionPoint(r ⊖ s) = k ≤ #s, #(r ⊖ s) = #s — are read off TumblerSub directly, without needing the divergence-point component values. D1 closes reach(γ) = r. Sound.

**S6 (LevelConstraint).** The single chain #reach(σ) = #(s ⊕ ℓ) = #ℓ = #s is read off TumblerAdd's length postcondition under the well-formedness preconditions, then closed by level-uniformity. The qualification that the identity fails without Pos(ℓ) is correctly documented. Sound.

**S4 (SplitPartition).** WF is invoked twice: for λ = (s, p ⊖ s) after discharging s, p ∈ T, s < p, #s = #p; and for ρ = (p, reach(σ) ⊖ p) after discharging reach(σ) ∈ T via TumblerAdd, p < reach(σ) from interiority, and #p = #reach(σ) via the TumblerAdd length chain. The three postconditions follow: (a) from WF giving reach(λ) = p and reach(ρ) = reach(σ), with T1 totality covering the converse inclusion (documented in Depends); (b) from T1 transitivity and irreflexivity; (c) directly from WF's reach postcondition for λ and from start(ρ) = p by construction. Sound.

**S3 (MergeEquivalence).** The WLOG step is valid: the formula s = min, r = max is symmetric, and the conclusion is symmetric in α and β. Under start(α) ≤ start(β), the adjacency disjunct reach(β) = start(α) is eliminated by (†)-style reasoning: reach(β) = start(α) ≤ start(β) < reach(β) chains via T1's mixed ≤-< corollary to reach(β) < reach(β), refuted by irreflexivity. The forward inclusion (⟦α⟧ ∪ ⟦β⟧ ⊆ {t : s ≤ t < r}) is immediate. The converse two-case split is sound: Case 1 (t < reach(α)) is direct; Case 2 (t ≥ reach(α)) derives r = reach(β) from r > reach(α), then combines reach(α) ≥ start(β) with t ≥ reach(α) to place t in ⟦β⟧. WF is applied to (s, r) after discharging s, r ∈ T, s < r (via TA-strict at α plus r ≥ reach(α)), and #s = #r (via S6 within each span plus level_compat across spans). Sound.

**S3b (MergeSplitInverse).** The non-emptiness facts (†) are correctly derived from TA-strict at each well-formed span. Case A and Case B are exhaustive and mutually exclusive (they partition the adjacency predicate). In each case: the merge formula is computed, the shared boundary is shown interior via (†), WR identifies the split parts as α and β by equating the TumblerSub operand with the span's reach (which the Case hypothesis provides directly). The level_compat check for S4 in Case B correctly uses symmetry of level_compat. Sound.

No gaps between claims, no broken precondition chains, no missing cases, no ungrounded operators found.

VERDICT: CONVERGED