## Audit

**WR (WidthRecovery).** D2's nine preconditions at (a, b, w) = (s, reach(σ), ℓ): carrier memberships (s ∈ T, ℓ ∈ T) come from σ's well-formedness; reach(σ) ∈ T from TA0's carrier postcondition cited in the result-length parenthetical; s < reach(σ) from TA-strict; Pos(ℓ) and actionPoint(ℓ) ≤ #s from σ's well-formedness; s ⊕ ℓ = reach(σ) by definition; #s ≤ #reach(σ) since both equal #s (TA0 gives #(s ⊕ ℓ) = #ℓ, level-uniformity closes the chain); and divergence(s, reach(σ)) ≤ #s via the T1 case-(i) witness — case (ii) eliminated by #s = #reach(σ) together with NAT-addcompat's successor inequality. All nine preconditions are in scope and accounted for. The proof is complete.

**S4 (SplitPartition).** WF is invoked at (s, p) for λ: #s = #p from level_compat, s < p from interiority. WF is invoked at (p, reach(σ)) for ρ: reach(σ) ∈ T from TumblerAdd at σ's well-formedness preconditions; #p = #reach(σ) from #p = #s (level_compat) and #reach(σ) = #ℓ = #s (TumblerAdd + level-uniformity). Parts (a), (b), (c): T1 trichotomy covers the union; irreflexivity plus ≥-unfolding covers the empty intersection; WF's reach postcondition directly delivers reach(λ) = p. No gaps.

**S3 (MergeEquivalence).** WLOG start(α) ≤ start(β) eliminates the reach(β) = start(α) adjacency disjunct via the chain reach(β) = start(α) ≤ start(β) < reach(β) — the final strict step from TA-strict at β — yielding reach(β) < reach(β) against T1 irreflexivity. The converse coverage of [s, r) is exhaustive: Case 1 (t < reach(α)) gives t ∈ ⟦α⟧; Case 2 (t ≥ reach(α)) forces r = reach(β) and uses the overlap condition reach(α) ≥ start(β) to place t ∈ ⟦β⟧. S6 correctly grounds #s = #r for the WF invocation. The proof is complete.

**WF (WellFormedSpanFromEndpoints).** The T1 case-(ii) elimination is fully discharged: #s + 1 ≤ #s unfolds into two sub-cases, each deriving #s < #s, which NAT-order irreflexivity refutes. The ZPD definedness argument carries correctly: Divergence symmetry transfers the case-(i) witness k from (s, r) to (r, s), ZPD's Relationship-to-Divergence equates zpd(r, s) = k, licensing TumblerSub's positive branch. TumblerSub then exports Pos(r ⊖ s) and actionPoint(r ⊖ s) = k ≤ #s without further component arithmetic. D1's fifth precondition divergence(s, r) ≤ #s is discharged by the divergence paragraph. Level-uniformity follows from #(r ⊖ s) = max(#r, #s) = #s. The proof is complete.

**S6 (LevelConstraint).** Straightforward: TumblerAdd's result-length identity instantiated at (s, ℓ) gives #reach(σ) = #ℓ; level-uniformity closes to #s. The precondition separation — noting that Pos(ℓ) failure leaves the identity inapplicable — is correctly placed. No gaps.

**S3b (MergeSplitInverse).** The (†) facts start(α) < reach(α) and start(β) < reach(β) are correctly sourced from TA-strict at each well-formed span. Both Case A and Case B identify s and r from S3's formula using (†), establish interiority of the shared boundary, verify level_compat(start(γ), p) from the given hypothesis, and invoke WR twice — once at α, once at β — to equate the S4 split outputs with the original spans. The left-right assignment is correctly distinguished between cases. The proof is complete.

**Cross-claim coherence.** S4 and S3 forward-reference WF and S6 (which appear later in the document); WF and S6 depend only on ASN-0034 foundations, so no circularity arises. The dependency DAG across all six claims is acyclic. Level-uniformity is propagated correctly through each operation: WF's length postcondition #(r ⊖ s) = max(#r, #s) = #s grounds level-uniformity of every constructed span.

VERDICT: CONVERGED