Reading the foundation statements and the ASN claims in order.

**SC (SpanClassification).** The exhaustiveness argument proceeds through a WLOG-plus-decision-tree. Every comparison invokes T1's trichotomy on pairs that are in T (starts by Span precondition, reaches by T12(a)), so every branch is well-defined. The WLOG assumption start(α) ≤ start(β) is valid: cases (i), (ii), (v) are stated symmetrically; (iii) and (iv) carry explicit "or symmetrically" riders. Mutual exclusivity is implicit in the tree: each node has exactly one outcome by T1's trichotomy, and at each leaf only one case applies. The adjacency (case ii) boundary-point argument is precise: reach(α) = start(β) lies in ⟦β⟧ (start(β) ≤ reach(α) with reach(α) < reach(β) by non-degeneracy) and outside ⟦α⟧ (reach is excluded upper bound). The disjoint and overlap proofs are element-chasing under T1 transitivity and use T12(b)'s implication start(σ) < reach(σ) correctly. SC Depends (Span, T12, T1) is complete — reach carrier membership is packaged in T12 and need not pull TumblerAdd directly.

**S6 (LevelConstraint).** Preconditions exactly match TumblerAdd's four preconditions instantiated at (s, ℓ), so the result-length identity #(s ⊕ ℓ) = #ℓ applies. Level-uniformity (#ℓ = #s) closes the chain. The note that TumblerSub's length postcondition and D1's round-trip identity do not yield #(s ⊕ ℓ) = #ℓ for a general ℓ is correct. Depends is complete.

**WF (WellFormedSpanFromEndpoints).** Step 1 eliminates T1 case (ii) cleanly: #s = #r and case (ii) would force #s + 1 ≤ #s; unfolding ≤ into < ∨ = yields #s < #s in both sub-cases, refuted by NAT-order irreflexivity. Case (i) then provides k ≤ #s with sₖ ≠ rₖ; Divergence's uniqueness identifies k = divergence(s, r) ≤ #s, discharging D1's fifth precondition. ZPD's Relationship-to-Divergence applied to (r, s) in Divergence case (i) — valid because the case-(i) qualifier is symmetric — certifies zpd(r, s) = k defined, licensing TumblerSub's positive branch. TumblerSub then exports Pos(r ⊖ s), actionPoint(r ⊖ s) = k ≤ #s, and #(r ⊖ s) = #s (since #r = #s selects sub-case α with L = #s). All four of T12's preconditions are assembled. D1 applies and delivers reach(γ) = r. Level-uniformity is #width(γ) = #s = #start(γ). Depends is complete.

**S11c (DifferenceOverlap).** Case 1 element-chasing: the ⊆ direction shows t < start(β) by contradiction (t ≥ start(β) together with t < reach(α) < reach(β) gives t ∈ ⟦β⟧); the ⊇ direction uses transitivity t < start(β) < reach(α). WF at (start(α), start(β)) needs start(α) < start(β) (case hypothesis) and #start(α) = #start(β) (level_compat); both hold. Case 2 element-chasing: within ⟦α⟧, start(β) < start(α) ≤ t makes start(β) ≤ t always true, so ⟦β⟧-membership reduces to t < reach(β); the ⊆ direction drops the lower guard start(α) ≤ t (sound); the ⊇ direction recovers it via start(α) < reach(β) ≤ t. The reach-carrier discharge (reach(β), reach(α) ∈ T) via TumblerAdd's a ⊕ w ∈ T is correct. The length equality #reach(β) = #reach(α) via S6 applied to both level-uniform spans and level_compat is correct. WF at (reach(β), reach(α)) then delivers γ'. Depends is complete and correctly notes Case 1 does not need S6.

The worked examples verify at the component level: all additions and subtractions follow TumblerAdd/TumblerSub's component formulas correctly.

VERDICT: CONVERGED