**S0 (Convexity):** The proof walks four cases for the lower bound (start(σ) ≤ q from start(σ) ≤ p and p ≤ q) and two cases for the upper bound (q < reach(σ) from q ≤ r and r < reach(σ)), each reducing via T1 postcondition (c) or substitution of equals. The Axiom section correctly notes that ≤-transitivity and the mixed ≤-< step are derived here, not exported by T1. The precondition q ∈ T is explicitly stated and correctly marked as the caller's obligation. No gap.

**WF (WellFormedSpanFromEndpoints):** The case-(ii) elimination proceeds in two sub-cases (strict and equality), both reaching #s < #s via NAT-addcompat's successor inequality and NAT-order's irreflexivity. The divergence-bound paragraph correctly identifies k = divergence(s,r) ≤ #s, satisfying D1's fifth precondition. The switch from (s,r) to (r,s) for TumblerSub is covered by Divergence's symmetry feeding ZPD's Relationship-to-Divergence. TumblerSub's positive-branch postconditions (Pos, actionPoint identification, length = max(#r,#s) = #s) are correctly invoked. Level-uniformity #width(γ) = #start(γ) follows from the length postcondition. No gap.

**S6 (LevelConstraint):** The chain #reach(σ) = #(s ⊕ ℓ) = #ℓ = #s is a direct application of TumblerAdd's result-length postcondition under the stated preconditions (Pos(ℓ) and actionPoint(ℓ) ≤ #s). The Preconditions section correctly notes that level-uniformity alone does not entitle one to the length chain—both TumblerAdd preconditions are required. No gap.

**S2 (EmptyDistinction):** The proof reduces directly to T12 postcondition (b): s ∈ span(s,ℓ). The four well-formedness preconditions of T12 are the same as S2's. No gap.

**S11 (DifferenceBound):** The proof is the most involved. The reach-membership steps (reach(α), reach(β) ∈ T) are established upfront via TumblerAdd's carrier postcondition and consumed at two distinct sites: the boundary contradiction and WF's carrier preconditions for ρ. The three-way sub-range decomposition of ⟦α⟧ is exhaustive and pairwise disjoint under T1's totality. λ and ρ are well-formed via WF: for λ the carrier preconditions are immediate (start tumblers ∈ T) and the length precondition is level_compat; for ρ the carrier preconditions are the TumblerAdd carrier facts already in hand, and the length precondition #reach(β) = #reach(α) follows by chaining S6 twice with level_compat. The tightness argument: start(α) ∈ ⟦λ⟧ ⊆ ⟦γ⟧ (start(α) < start(β) = reach(λ), so start(α) < reach(λ) and start(α) ≤ start(α)); reach(β) ∈ ⟦ρ⟧ ⊆ ⟦γ⟧ (reach(β) ≤ reach(β) and reach(β) < reach(α) = reach(ρ)); the S0 bracketing discharge is walked using the same abbreviation case-split S0 itself uses; the contradiction t ∈ ⟦γ⟧ but t ∉ ⟦λ⟧ ∪ ⟦ρ⟧ is clean (t ≥ start(β) = reach(λ) rules out ⟦λ⟧; t < reach(β) = start(ρ) rules out ⟦ρ⟧). No gap.

VERDICT: CONVERGED