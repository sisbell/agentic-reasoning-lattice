Reading through the foundations and each claim in turn.

**SC (SpanClassification).** The exhaustiveness argument proceeds by a decision tree on the four boundary points under the WLOG assumption start(α) ≤ start(β): trichotomy on reach(α) vs start(β) splits into cases (i), (ii), and sharing; within sharing, trichotomy on starts then on reaches reaches the five leaves. Mutual exclusivity follows from exactly-one trichotomy at each split node — no two branches of the tree are simultaneously traversed. The disjointness/overlap characterisation is correctly derived: cases (i) and (ii) are verified by explicit element-chasing, case (iii) by exhibiting start(β) as a common member, case (iv) by showing ⟦β⟧ ⊆ ⟦α⟧ with ⟦β⟧ nonempty, and case (v) by ⟦α⟧ = ⟦β⟧. The case (iv) containment proof correctly case-splits the ≤-abbreviations for both the lower and upper bound of an arbitrary q ∈ ⟦β⟧, reducing each to T1(c) or substitution of equals. The "or symmetrically" rider correctly handles the β-larger sub-case by structural symmetry of the argument. The T1 dependency slot notes that ≤-transitivity and mixed ≤-< composition are *derived* in the proof by case analysis, not cited as T1 exports — this is precise and correct.

**S6 (LevelConstraint).** The chain #reach(σ) = #(s ⊕ ℓ) = #ℓ = #s follows directly: TumblerAdd's result-length postcondition #(a ⊕ w) = #w under the preconditions (s ∈ T, ℓ ∈ T, Pos(ℓ), actionPoint(ℓ) ≤ #s) gives the first two equalities; level-uniformity #ℓ = #s gives the third. The preconditions are exactly those discharged by S6's Preconditions block. The result is tight.

**WF (WellFormedSpanFromEndpoints).** The divergence-bound paragraph correctly eliminates T1 case (ii) from the pair (s, r): the equal-length hypothesis #s = #r makes #s + 1 ≤ #r reduce to #s + 1 ≤ #s, which NAT-addcompat's successor inequality #s < #s + 1 contradicts via NAT-order's transitivity (strict sub-case) or indiscernibility of = (equality sub-case), both refuted by irreflexivity. T1 case (i) thus holds with k ≤ #s and sₖ ≠ rₖ via exactly-one trichotomy. Divergence case (i)'s uniqueness identifies k = divergence(s, r), discharging D1's precondition divergence(s, r) ≤ #s. For TumblerSub on (r, s): r ≥ s from T1's ≤/≥ abbreviations; zpd(r, s) is defined via Divergence's symmetry carrying the case-(i) witness to the pair (r, s), then ZPD's Relationship-to-Divergence. TumblerSub then exports Pos(r ⊖ s), actionPoint(r ⊖ s) = k, and #(r ⊖ s) = #s, closing actionPoint(r ⊖ s) ≤ #s and level-uniformity. All five D1 preconditions are in hand and D1 delivers reach(γ) = r.

**S11c (DifferenceOverlap).** Case 1: the element-chasing correctly identifies ⟦α⟧ \ ⟦β⟧ = {t : start(α) ≤ t < start(β)} — the (⊆) direction derives a contradiction from t ≥ start(β) using t < reach(α) < reach(β) and T1(c); the (⊇) direction composes start(α) ≤ t < start(β) < reach(α) by T1(c). The witness γ = (start(α), start(β) ⊖ start(α)) is discharged by WF: both endpoints are span starts in T, start(α) < start(β), and level_compat gives #start(α) = #start(β). WF delivers reach(γ) = start(β). Case 2: the element-chasing derives start(β) ≤ t for t ∈ ⟦α⟧ with t < reach(β) by case-splitting start(α) ≤ t into strict (T1(c) on start(β) < start(α) < t) and equality (substitution) branches. The two-inclusion argument recovers start(α) ≤ t for the ⊇ direction by case-splitting reach(β) ≤ t the same way on start(α) < reach(β). The witness γ' = (reach(β), reach(α) ⊖ reach(β)) is discharged by WF: reach(β), reach(α) ∈ T follow from TumblerAdd's carrier postcondition applied to each well-formed span; #reach(β) = #reach(α) follows from S6 (#reach(σ) = #start(σ)) applied to both spans and combined with level_compat(start(α), start(β)). WF delivers reach(γ') = reach(α). Non-emptiness is witnessed by reach(β) in both cases.

All precondition chains are unbroken. All case analyses cover their stated domains. Every postcondition is established by the proof. The dependency lists are complete and the citations are accurate.

VERDICT: CONVERGED