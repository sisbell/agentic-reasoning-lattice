I read the four claims (SC, S6, WF, S11c) as a connected system, tracing the precondition chains from S11c down through WF, S6, SC into the ASN-0034 foundations.

The mathematics holds. SC's five-case split is exhaustive and mutually exclusive under the decision tree (WLOG start(α) ≤ start(β), then the reach(α)/start(β), start(α)/start(β), reach(α)/reach(β) comparisons), and the disjoint/overlap correspondence is established case by case. WF is a clean black box: for any (s,r) with s,r ∈ T, s < r, #s = #r it discharges D1's `divergence ≤ #s` generically (T1 case (ii) excluded by equal length), enters TumblerSub's positive branch via the Divergence-case-(i) witness through ZPD's Relationship-to-Divergence, and returns a level-uniform span with reach = r. S6's `#reach(σ) = #start(σ)` is correctly grounded in TumblerAdd's result-length postcondition under the ⊕ preconditions. S11c's two sub-cases are exactly SC case (iii)'s two orderings; the element-chases are sound (the mixed/strict compositions are correctly derived by case-splitting the `≤`-clause rather than cited as T1 exports), WF's carrier preconditions for the reach-valued endpoints of γ' are discharged via TumblerAdd, and both worked examples compute correctly. I found no broken precondition chain, no missing case, no term whose meaning shifts.

What I did find is recurring defensive/meta-prose — the reviser-drift pattern the brief calls out — concentrated in slots that should carry only statements.

### S11c Axiom slot carries use-site essay and a refactor-residue parenthetical
**Class**: OBSERVE
**Foundation**: Span / SC (denotation convention ⟦σ⟧ = {t : start(σ) ≤ t < reach(σ)})
**ASN**: S11c *Axiom*: "For any span σ, ⟦σ⟧ = {t : start(σ) ≤ t < reach(σ)} — reach(σ) is the exclusive upper bound… **This half-open convention is the external fact the witness constructions take as given: it is what lets us read ⟦γ⟧ = … and ⟦γ'⟧ = … directly off the constructed endpoints and match each to ⟦α⟧ \ ⟦β⟧. (The discharge of WF's carrier and length preconditions for these witnesses is derived, not stipulated, and is carried out in the Case 2 proof body.)**"
**Issue**: Only the first sentence states the convention; the remainder is use-site explanation ("it is what lets us read…") plus a defensive parenthetical reassuring the reader where the discharge lives. The parenthetical is residue from the recent "demote Axiom discharge proof into body" edit — it explains what is *not* in the slot rather than asserting the convention. This is the "prose around an axiom explains why it is needed rather than what it says" pattern, and it makes the reader skip past meta-commentary to reach the load-bearing line.
**What needs resolving**: Reduce the Axiom slot to the convention itself (the half-open denotation with reach exclusive); drop the use-site sentence and the discharge-location parenthetical (the body already carries the discharge).

### "Level-uniformity is not enough / sole source" point repeated across four slots
**Class**: OBSERVE
**Foundation**: TumblerAdd (result-length identity `#(a ⊕ w) = #w`)
**ASN**: The same precondition-necessity argument appears in S6 body ("**Drop a precondition — say Pos(ℓ) — and s ⊕ ℓ need not be defined, so the length identity has nothing to stand on.**"), S6 *Preconditions* ("**They are not implied by level-uniformity: a level-uniform pair with Pos(ℓ) failing has reach(σ) undefined…**"), S6 *Depends* ("**This is the sole source of the addition result-length: the in-scope foundations supply only the subtraction length (TumblerSub…) and the round-trip identity (D1…), neither of which yields #(s ⊕ ℓ) = #ℓ…**"), and again in S11c Case 2 ("**the reach equality being S6's packaging of TumblerAdd's result-length identity … not a consequence of #width(σ) = #start(σ) by itself**").
**Issue**: The single substantive claim — that #reach needs TumblerAdd's postcondition under its ⊕ preconditions, which level-uniformity alone does not supply — is correct and load-bearing once. Stated four times it becomes noise: the Depends version is a use-site inventory of what *other* foundations do not provide; the body and Preconditions versions are counterfactuals imagining `Pos(ℓ)` failing, a case the "well-formed span" precondition already excludes. These compound across cycles.
**What needs resolving**: Keep the necessity statement in one slot (the S6 body is the natural home). Trim the Preconditions echo, the Depends "sole source / what others don't supply" inventory, and the S11c Case 2 parenthetical to bare cross-references, removing the counterfactuals about excluded preconditions.

VERDICT: OBSERVE