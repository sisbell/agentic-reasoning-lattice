# Review of ASN-0129

I checked the technical core against the foundations: V-AUD's audit lifts are AM-conformant and finite by the cited bounds; V-IDX's vacuity argument is correct (R-C1 forces `[K_R]` into every constructible registry and S3 gives it `behaviors = ∅`, so no behavior family is universally attached); PC3's cross-view reconstructions type-check (`⋃(L_K, addrs_F)` is PC2a over a tuple-valued base with a V-TUP body, and it is V-AUD's union); PD0's polarity rules are sound — I verified the ∃/∀ and count rules against grow-only domain growth, witness persistence (L12/L12a via B2 with RP-b), and the `count(L_K) ≤ 3` example; PD2's exception list correctly names all three cross-type routes (retraction through `nullified`, BH4's home-wide chain footprint, `targets_keyed`'s cross-type join); PC6's converse is honestly relativized with its costs stated, and the Observe_K normalization matches ASN-0086's contract exactly (`F̂ ⊆ coverage(F)` unfolds to the per-tuple conjunction as claimed); C-reach's demotion to conjecture is properly grounded, with the three counts against the old FO argument each correct. I also recomputed the four-state trace end to end — gate verdicts, frontier addresses `chain_d(0..3)`, C3 at Σ₄ via the sibling antichain, both view sequences (⊥,⊤,⊥,⊥ active; ⊥,⊤,⊥,⊤ default), and the `ever_res` lock — and it is right. The remaining findings are accretion findings under this note's anti-bloat mode.

## REVISE

### Issue 1: `is_doc`'s grounding is stated twice, with mutual cross-pointers between V-DOC and QD-audit
**ASN-0129, V-DOC and QD-audit**: V-DOC: "document-residence, the emit surface's own home-validation test (I1/I5, ASN-0128; performed per branch structure, QD-audit) exposed as a read … `dom(Σ.M)` is not a QD base, the admission and the restriction grounded together at QD-audit." QD-audit: "document-residence `d ∈ dom(Σ.M)` is the emit surface's own home validation — performed on every `idem = ⊤` miss (I1: home validation is branch-local, a hit never reads `d`), every admitted `idem = ⊥` call (I5), and every `Nullify_Binary` call (P0, both branches, S3 — all ASN-0128) … V-DOC admits exactly that test, as `is_doc`."
**Problem**: The same justification — the atom is the emit surface's own home-validation test, performed per branch — is written in both places, each deferring to the other ("performed per branch structure, QD-audit" / "V-DOC admits exactly that test"). This is the accretion signature the note's review mode flags: two paragraphs in different sections saying the same thing in different words, joined by mutual deferral. The reader processes the grounding twice and must check the two statements agree. (The membership-only restriction additionally recurs at PC6's base list, Structural-reads-only, and FP — those repetitions are slot-inherent and fine; the duplicated *justification* is the problem.)
**Required**: One site owns the grounding. Either V-DOC states the atom's definition and the membership-only restriction bare, with QD-audit carrying the full emit-surface justification (and QD-audit's sentence reducing to "the document store contributes the membership atom `is_doc` (V-DOC), not a base" before the document-census rationale); or V-DOC keeps the full grounding and QD-audit points to it. Delete the surviving cross-pointer in the other direction.

### Issue 2: the self-emit proof obligation is written out in full twice (QD-audit and Open Question 6), inside an over-defended exclusion passage
**ASN-0129, QD-audit and Open Question 6**: QD-audit: "a proof would be an invariance argument over every atom denotation, handling those routes in particular — BH4's `age` is defined from the very quantity `f_d^Σ` (its home-wide footprint recorded at FP), and the link-domain data of which `a_emit(Σ, d)` is a function enters term position through the reflected `L_dom` (QD-refl) — … the obligation is recorded alongside Open Question 6's." Open Question 6: "whose proof must additionally handle the frontier-derived routes the vocabulary does admit (BH4's `age`, the reflected `L_dom`)."
**Problem**: The proof-route detail (frontier-derived routes via BH4's `age` and the reflected `L_dom`) appears in full in QD-audit and again in OQ6 — duplicated content across sections deferring to the same location, the pattern the review mode names. The passage also carries a defensive tail — "The exclusion costs a gating discipline nothing: the self-emit disjunct licenses the one call whose target is the tuple its own emission deposits, a check only the emitting surface — which knows its frontier — can make…" — which justifies the non-admission's acceptability rather than specifying what is and isn't admitted; this reads as accretion from the demotion cycle (the recent revision that moved self-emit to conjecture status), prior-finding content relocated rather than reduced.
**Required**: State the exclusion's grammar fact and the conjecture status once in QD-audit, with a bare pointer to OQ6; OQ6 (where C-reach's obligation already lives) owns the proof-route detail. Compress the defensive tail to its one load-bearing sentence — the gate states the residence clause; the self-emit check belongs to the emitting surface, which performs it (S3) — and drop the "costs nothing" framing.

## OUT_OF_SCOPE

### Topic 1: completeness of V-PRIM's finite-set fragment
V-PRIM ships membership, set equality, emptiness, and `elems`, but no binary union, intersection, or difference on set-valued terms. Intersection is recoverable as a filter (`{x ∈ S₁ : x ∈ S₂}`), and indexed `⋃` over a filtered domain recovers many unions, but the union of two ghost-containing enumeration results (e.g., two `targets_of` values) has no evident spelling. The note's relativization paragraph already classifies such gaps as deliberate vocabulary omissions, closable by admission.
**Why out of scope**: This is a future admission decision on the vocabulary axis the note itself names (the ℕ-multiplication category), not an error in any stated claim — nothing in the note asserts binary set union is expressible.

### Topic 2: extending PD0's grow-only class to audit-view atom results
`targets_of(x, audit)` is semantically grow-only (a union over the growing `L_K`, by the same argument PD0 gives for audit `M_K`), but the grow-only class as defined admits only the bases, audit `M_K`, and filters — so existentials over audit enumeration-atom results aren't certified ⊤-stable even where they are. PD0 is sound as written; this is incompleteness of the syntactic classification.
**Why out of scope**: Open Question 5 already owns the completeness/decidability of the dynamics classification; sharpening the grow-only class is work under that question, not a defect in PD0's stated rules.

VERDICT: REVISE
