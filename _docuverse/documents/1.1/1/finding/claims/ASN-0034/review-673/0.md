### NAT-card body: defensive aside around k=0 case
**Class**: OBSERVE
**Foundation**: NAT-card (NatFiniteSetCardinality)
**ASN**: Body prose — "(at `k = 0` the domain `{j ∈ ℕ : 1 ≤ j ≤ 0}` is empty, `f` is the empty function, vacuously strictly increasing with image `∅`, forcing `S = ∅` and `|∅| = 0` without recourse to a convention on empty lists)"
**Issue**: The clause "without recourse to a convention on empty lists" explains what the axiom was designed to avoid, not what it establishes. The preceding trace of the k=0 case does useful work; the phrase appended to it is a defensive aside that a precise reader must parse past to follow the content.

---