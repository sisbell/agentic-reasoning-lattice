### Z defined but unused within this ASN
**Class**: OBSERVE
**Foundation**: TA-Pos (PositiveTumbler)
**ASN**: TA-Pos *Definition:* clause — `**Z** = {t ∈ T : Zero(t)}`
**Issue**: The set Z is introduced in TA-Pos's prose, its formal contract *Definition:* clause, and its formal contract *Consequence:* clause, but it appears nowhere else in the ASN — no subsequent Depends annotation, no proof step, no postcondition cites it. Every other name introduced in this ASN (Pos, Zero, actionPoint, ⊕) is consumed by at least one downstream claim. Z is a pure export with no internal role.

---

The remainder of the ASN is sound. Every named operator (⊕, actionPoint, Pos, Zero) is grounded by a defining claim. Every proof walks its cases:

- **T1 trichotomy**: the three-way split (no divergence / α-divergence / β-γ divergence) is exhaustive, each branch establishes exactly one relation, and the "no reverse witness" arguments are complete for all three witness positions (k′ < k, k′ = k, k′ > k).
- **T1 transitivity**: all six k₁-vs-k₂ sub-cases (k₁ < k₂ in two sub-cases by T1 type; k₂ < k₁ with the T1(ii) branch ruled out; k₁ = k₂ in four sub-cases (i,i), (ii,i), (i,ii) contradiction, (ii,ii) contradiction) discharge correctly.
- **TumblerAdd dominance**: the three-branch case analysis (∃j < k with aⱼ > 0; all zero below k with aₖ > 0; all zero at and below k) is exhaustive and each branch lands in r > w or r = w as required for r ≥ w.
- **ActionPoint zeros-below**: the contradiction proof correctly assembles all four S-membership conditions and discharges both disjuncts of actionPoint(w) ≤ i.
- **NAT-sub strict monotonicity and strict positivity**: all branches of the trichotomy dispatches reach contradictions except the desired conclusion.
- **TA-MTO biconditional**: the forward direction covers all three component regions; the converse recovers agreement at positions < k by prefix-copy and at k by right cancellation (wₖ is the common right addend).

All Depends lists are accurate against the proof steps that consume them. No ungrounded symbols, no missing edge cases, no circular dependencies.

VERDICT: OBSERVE