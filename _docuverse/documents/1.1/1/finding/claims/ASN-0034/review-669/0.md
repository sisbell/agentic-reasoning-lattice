### ActionPoint postcondition 2: well-typedness of the contradiction supposition
**Class**: OBSERVE
**Foundation**: T0 (CarrierSetDefinition) — component projection `i ↦ aᵢ : {j ∈ ℕ : 1 ≤ j ≤ #a} → ℕ` is total on the index domain, partial outside it.
**ASN**: ActionPoint derivation, postcondition 2 block — `"Suppose otherwise, for contradiction: then wᵢ ≠ 0, and to conclude i ∈ S we must also establish…i ≤ #w"`
**Issue**: The proof writes `wᵢ ≠ 0` before `i ≤ #w` has been established in the proof text. T0's projection is only defined on the index domain `{j ∈ ℕ : 1 ≤ j ≤ #w}`, so the supposition `wᵢ ≠ 0` presupposes `i ≤ #w`. That bound is available from postcondition 1 (actionPoint(w) ≤ #w, proved immediately above) together with the hypothesis `i < actionPoint(w)`, via NAT-order transitivity — but the proof derives `i ≤ #w` only inside the contradiction branch, as part of verifying `i ∈ S`, leaving the supposition's well-typedness implicit. At this document's level of explicitness, a reader must supply the step.

---

**Uniqueness proof, postcondition 1, postcondition 3:** All correct. The four-case analysis for uniqueness is exhaustive. Postcondition 1 (1 ≤ actionPoint(w) ≤ #w) follows directly from actionPoint(w) ∈ S. Postcondition 3 (1 ≤ w\_actionPoint(w)) chains: membership → w\_actionPoint(w) ≠ 0 → NAT-zero disjunction → NAT-discrete at (0, w\_actionPoint(w)) → left-identity rewrite 0+1=1. Every citation in the *Depends* list is used and no dependency appears to be missing.

VERDICT: OBSERVE