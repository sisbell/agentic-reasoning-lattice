### NAT-card preamble in T4 body restates the axiom rather than using it
**Class**: REVISE
**Foundation**: T4 (HierarchicalParsing) — opening of T4 body
**ASN**:
> "NAT-card axiomatizes the cardinality operator `|·|` as a well-defined total function on subsets of every initial segment `{1, 2, …, n} ⊆ ℕ` (`n ∈ ℕ`), with codomain ℕ and with `|S|` characterised as the length of the unique strictly increasing enumeration of `S`; here at T4, where we use both operators, we distinguish `|·|` from T0's tumbler-length `#· : T → ℕ`, which acts on sequences rather than sets."

**Issue**: This is the reviser-drift pattern the review prompt names: "new prose around an axiom explains why the axiom is needed rather than what it says." NAT-card is a declared dep; restating its signature, codomain, and characterisation in the body before any reasoning step uses it is preview-of-cited-content, not reasoning. The actual application step appears two sentences later — `{i : 1 ≤ i ≤ #t ∧ tᵢ = 0}` is a subset of `{1, …, #t} ⊆ ℕ`, so NAT-card applies at `n = #t` and `zeros(t) ∈ ℕ` — and that step names the dep and discharges the side condition by itself. The only locally useful content is the disambiguation between `|·|` and `#·`, and even that fits in a single line at the application site.
**What needs resolving**: Drop the NAT-card preamble. If the `|·|` vs `#·` distinction is worth a sentence, keep that sentence; remove the preview of NAT-card's contract.