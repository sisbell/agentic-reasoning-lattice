### Mixed transitivity invoked without case elaboration
**Class**: OBSERVE
**Foundation**: NAT-order (NatStrictTotalOrder)
**ASN**: TumblerAdd strict-advancement prose — *"NAT-order composes these into `aₖ + wₖ > aₖ`"*; T1 Case 3 — *"hence `m < n` via NAT-addcompat's `m < m + 1`"*
**Issue**: Both sites combine `a < b` with `b ≤ c` to conclude `a < c` (mixed transitivity). The `≤`-definition must be unfolded into `b < c ∨ b = c`, then each branch discharged — the `<` branch via NAT-order's `<`-transitivity axiom, the `=` branch via indiscernibility. The text collapses this to "NAT-order composes" or "via NAT-addcompat's `m < m + 1`" without the case split.

---