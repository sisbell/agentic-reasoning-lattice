### T4 cites T4a for segment count it does not derive
**Class**: REVISE
**Foundation**: n/a (internal — T4 ↔ T4a)
**ASN**: T4 body: "The *field segments* of `t` are the maximal contiguous sub-sequences of field-component positions, delimited by the field separators; how many such segments arise for a given `t`, and whether each is non-empty, is a derived structural fact (proved downstream in T4a) rather than a stipulation here." T4a's *Consequence* covers only the biconditional "field-segment constraint ⟺ every field segment non-empty"; the count `k + 1` appears in T4a's body — "The `k + 1` field segments of `t` are the index ranges `(s_i, s_{i+1})` for `i = 0, 1, …, k`" — as an asserted decomposition, not as a derived Consequence.
**Issue**: T4 advertises that T4a proves both "how many segments arise" and "whether each is non-empty". T4a's stated *Consequence* delivers only the latter (and only as a biconditional, not unconditionally). The count `k + 1` is used as setup machinery and never lifted to a Consequence with its own derivation walking the maximality argument. A downstream consumer reading T4 expects a citable count fact in T4a and finds none.
**What needs resolving**: Either lift the segment count to a stated Consequence in T4a (with the maximality-of-contiguous-sub-sequences argument walked from T4's definition and the enumeration `s_1 < … < s_k`), or revise T4's pointer to claim only what T4a actually delivers (the non-emptiness biconditional). The current cross-claim citation overstates T4a's payload.

VERDICT: REVISE