### Antisymmetry of `≤` consumed by TA5-SigValid is not a NAT-order Consequence
**Class**: OBSERVE
**Foundation**: NAT-order (NatStrictTotalOrder); TA5-SigValid (SigOnValidAddresses)
**ASN**: TA5-SigValid's combining step — "Combining the two through antisymmetry of `≤` — supplied by NAT-order via exactly-one trichotomy, which eliminates the three disjoint-pair cases `m < n ∧ n < m`, `m < n ∧ m = n`, and `m = n ∧ n < m` from the four-way distribution of the conjoined disjunctions — yields `sig(t) = #t`."
**Issue**: NAT-order's contract advertises `≤`-transitivity as a Consequence (with full derivation in the body) but not `≤`-antisymmetry, although both are derivable from the same exactly-one trichotomy clauses. TA5-SigValid combines `sig(t) ≥ #t` and `sig(t) ≤ #t` through antisymmetry, sketching the four-way case distribution inline in a single clause. The derivation goes through — the wording "supplied by NAT-order via exactly-one trichotomy" plus the case-elimination remark is enough scaffolding for a reader to reconstruct it — so the claim is sound as written. The observation is purely about contract organization: any future claim that needs `≤`-antisymmetry will repeat the same one-line derivation, since the property is not packaged at the source.
**What needs resolving**: n/a (OBSERVE).

VERDICT: OBSERVE