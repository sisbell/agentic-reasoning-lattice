### S8-fin's Axiom text uses AX-1's content but AX-1 is absent from Depends
**Class**: REVISE
**Foundation**: S8-fin (FiniteArrangement); AX-1 (not shown in given content, referenced by name); NAT-zero (NatZeroMinimum)
**ASN**: S8-fin, *Axiom* bullet: *"— holding at every state reachable from the base state `Σ₀` by the transition relation. At the base state itself, where **AX-1 fixes `dom(Σ₀.M(d)) = ∅`**, the witness is necessarily `n = 0` — no `n ≥ 1` admits a total map into the empty codomain — so the existential's range `n ∈ ℕ` is discharged there by `0 ∈ ℕ`, grounded from NAT-zero."*
**Issue**: The claim that `n = 0` is the *necessary* witness at Σ₀ rests on `dom(Σ₀.M(d)) = ∅`, which this very sentence attributes to AX-1. The Depends section then cites NAT-zero specifically *because* of this base-state argument ("supplies `0 ∈ ℕ`, the membership the unique base-state witness `n = 0` inhabits. At `Σ₀`, where AX-1 fixes `dom(Σ₀.M(d)) = ∅` …"), but AX-1 itself — the source of the fact the whole argument turns on — has no Depends entry. This breaks the ASN's own direct-citation discipline, applied with unusual rigor everywhere else in this document (e.g. D-MIN grounds T0, NAT-closure, and NAT-zero directly rather than transitively, precisely to avoid this kind of gap; Σ.M(d)'s own commentary explicitly attributes `dom(Σ₀.M(d)) = ∅` to AX-1, distinct from Σ.M(d)'s own type-only content, confirming AX-1 — not Σ.M(d) — is the source claim). A downstream reader following the citation chain for "why is `n=0` forced at Σ₀" hits a fact with no grounding entry.
**What needs resolving**: Add AX-1 as a Depends entry for S8-fin, supplying `dom(Σ₀.M(d)) = ∅`, on the same standard the ASN applies to every other fact used within a formal Axiom/Definition bullet.

The remaining claims (V-sub, S8-depth, NAT-induction, subspace, Σ.M(d)) each have Depends lists that exactly match the symbols their formal statements use, with commentary-only citations (OrdinalShift/OrdShiftHom/S8a for S8-depth, NAT-wellorder for D-MIN) correctly excluded per the ASN's own stated exclusion tests.

VERDICT: REVISE