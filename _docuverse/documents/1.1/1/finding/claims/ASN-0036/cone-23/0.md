### S7a depends block does not cover T4b's domain preconditions for U(a) and D(a)
**Class**: REVISE
**Foundation**: T4b (UniqueParse) — postconditions block: "Outside the T4-valid subdomain, none of the projections is assigned a value; consumers must carry T4-validity as a precondition." Also: `dom(U) = {t ∈ dom(N) : zeros(t) ≥ 1}`, `dom(D) = {t ∈ dom(N) : zeros(t) ≥ 2}`.
**ASN**: S7a (DocumentScopedAllocation) — "for every `a ∈ dom(Σ.C)`, the document-level prefix of `a` — the tumbler `N(a).0.U(a).0.D(a)` obtained by truncating the element field, where `N(a)`, `U(a)`, `D(a)` are the partial projections supplied by T4b" — and S7a's depends block, which cites T4b and T10a but neither T10a.4 nor S7b.
**Issue**: S7a's statement universally applies T4b's partial projections U and D to every `a ∈ dom(Σ.C)`. For these projections to be defined, two conditions must hold for every such `a`:

1. **T4-validity** — required by dom(N) (the base domain of all four projections). This is established by T10a.4 (T4PreservationUnderDiscipline), not by T10a alone. T10a.4 is a separately formalized foundation statement with its own proof and depends chain; citing T10a does not automatically deliver T10a.4's content for free.
2. **zeros(a) ≥ 2** — required for `a ∈ dom(D)`. This is established by S7b (which axiomatizes zeros(a) = 3 for all `a ∈ dom(Σ.C)`), not by T10a or T10a.4 alone (T10a.4 establishes T4-validity, which means only zeros ≤ 3 with the field-segment constraint, not zeros ≥ 2).

T4b's postconditions contain an explicit warning to this effect. S7a is a declared consumer of T4b but does not carry the stated precondition. A reader of S7a's depends block in isolation cannot verify that U(a) and D(a) are defined on the stated domain. The precondition chain from S7a's declaration to T4b's domain requirements is broken.

**What needs resolving**: S7a's depends block must be extended to include T10a.4 (establishing T4-validity for all `a ∈ dom(Σ.C)` via the element allocator memberships T10a grounds) and S7b (establishing zeros(a) = 3, which places `a` in dom(U) and dom(D) as well as dom(E)). Together these close the two domain conditions T4b requires of any consumer that applies U or D.

---

VERDICT: REVISE