### S7 over-elaborates in structural slots: Depends use-site inventories and a vacuous four-case adjacency walk
**Class**: OBSERVE
**Foundation**: NAT-order, NAT-addassoc, NAT-sub, NAT-addcompat, T0, T4, T4a, T4b (all ASN-0034) — re-derived inside S7's own Depends entries
**ASN**: S7 (StructuralAttribution). Depends entries re-prove the body: the NAT-order entry runs several hundred words re-walking the no-adjacent-zeros contradiction ("the bound `1 = #U(a) + 1 ≥ 2`, i.e. `2 ≤ 1`, that — unfolded against `2 > 1` … into the sub-cases `2 < 1` … and `2 = 1` … closes the contradiction by forcing `1 < 1`"); the NAT-addassoc, NAT-sub, NAT-addcompat entries each restate instantiation sites and re-walk the re-association/telescoping. Separately, the body's no-adjacent-zeros step enumerates a "four-case domain" of assignments of `(i, i+1)` to the two separators `{#N(a)+1, X}`, of which **two are vacuous** (`i = #N(a)+1 = i+1` and `i = X = i+1`, both requiring `i = i+1`), and the surviving case is closed by a two-way single-valuedness-of-subtraction argument.
**Issue**: A Depends entry's job is to name what the foundation supplies (symbol/axiom + one-clause role); these instead re-execute the proof inside the dependency list, forcing the reader to verify the same argument twice. This is the named noise pattern (use-site inventories in a structural slot). The four-case walk is the same over-elaboration in the body: the Formal Contract derives non-adjacency in one line — "their difference `#U(a) + 1 ≥ 2` … keeps the two separators at least two indices apart, hence never at consecutive positions `i`, `i + 1`" — and explicitly flags that "the full four-case adjacency contradiction" lives in the body. So the body carries a padded, partly-vacuous enumeration of what the Contract states cleanly from `p₂ − p₁ = #U(a)+1 ≥ 2 > 1`. Neither defect touches correctness — the proof is sound — but aggregate sprawl in the system's most-cited attribution claim degrades reviewability across cycles.
**What needs resolving**: Reduce S7's Depends entries to what each foundation supplies plus a one-clause role, removing the embedded re-derivations (the proof body is where instantiation detail belongs). In the body, replace the four-case adjacency enumeration with the difference-`≥ 2` argument the Formal Contract already uses; the two `i = i+1` cases are vacuous and the single-valuedness-of-subtraction detour is unnecessary once the separator gap `#U(a)+1 ≥ 2` is in hand.

VERDICT: OBSERVE