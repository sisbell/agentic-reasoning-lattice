### NAT-induction Forward References omits D-SEQ
**Class**: OBSERVE
**Effective class**: REVISE (classifier override)
**Classifier rationale**: NAT-induction's Forward References record is an explicit, maintained structural slot whose stated purpose is to list all downstream consumers, so omitting the direct, independent D-SEQ consumer makes that record factually incomplete (wrong) rather than merely unpolished.
**Foundation**: NAT-induction (NatInduction)
**ASN**: NAT-induction — Forward References list (D-PRED, D-INJ, D-MIN); D-SEQ (SequentialPositions) Assembly, which directly cites NAT-induction ("The recursion is the from-`1` specialization of NAT-induction's (NatInduction) generation-from-`0` principle") and lists it in its own Depends.
**Issue**: NAT-induction maintains a Forward References record of its downstream consumers, each with a description of how the consumer uses it. D-SEQ is a direct, independent consumer — its Assembly runs its own greatest-element induction over S8-fin's enumeration via NAT-induction, not transitively through D-MIN — yet D-SEQ is absent from the list, while its "dual" D-MIN (least-element induction) is present. The record is therefore incomplete: a reader tracing NAT-induction's forward edges would miss the NAT-induction → D-SEQ edge. This is non-load-bearing (D-SEQ's backward edge in its own Depends is present, so the substrate dependency graph is complete), but the consumer record on NAT-induction is inconsistent with its actual consumer set.
**What needs resolving**: Add D-SEQ to NAT-induction's Forward References, with an entry describing its use — the from-`1` specialization applied to the greatest-element predicate P(N) over the length of S8-fin's enumerating bijection, the dual of D-MIN's least-element induction.

VERDICT: REVISE