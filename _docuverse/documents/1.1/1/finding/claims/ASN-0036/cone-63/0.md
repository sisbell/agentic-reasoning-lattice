### OrdShiftHom Formal Contract Depends missing T4 and NAT-card for the part (b) conclusion zeros(r)=0
**Class**: REVISE
**Foundation**: T4 (HierarchicalParsing), NAT-card (NatFiniteSetCardinality)
**ASN**: OrdShiftHom (OrdinalShiftPreservation), part (b) proof: *"Every component of r is therefore at least 1, hence positive, so zeros(r) = 0 and (A i : 1 ≤ i ≤ #r : rᵢ > 0)"*; Formal Contract Depends entry: *"S8a (ArrangementDomainRestriction) — supplies the predicate definition (zeros(t)=0, #t≥2, all components ≥ 1) consumed as the part (b) hypothesis and proved to hold on shift(v,n)."*
**Issue**: Part (b) establishes zeros(r) = 0 by the chain: all rᵢ ≥ 1 (proved) → {i : rᵢ = 0} = ∅ → zeros(r) = |∅| = 0. The symbol zeros is defined in T4 (zeros(t) = |{i : 1 ≤ i ≤ #t ∧ tᵢ = 0}|); the empty-set cardinality |∅| = 0 comes from NAT-card's k=0 case. Neither T4 nor NAT-card appears in OrdShiftHom's Formal Contract Depends. S8a is listed, but S8a's Formal Contract axiom states only "dom(Σ.M(d)) ⊆ {t ∈ T : zeros(t) = 0 ∧ #t ≥ 2}" — it uses zeros as a predicate argument without exporting the biconditional zeros(t)=0 ⟺ (A i : tᵢ > 0) as a formal postcondition. The derivation of that biconditional sits in S8a's body prose, which is not the formal exchange boundary. The ASN convention is that the symbol's defining claim must appear directly in Depends when the symbol is used: S8a cites T4 because zeros appears in S8a's own axiom; by the same token OrdShiftHom, which concludes zeros(r)=0 in part (b)'s final step, should cite T4 directly. S8 is not affected: S8 consumes "satisfies S8a" as an opaque predicate from OrdShiftHom and never writes zeros explicitly in its own argument.
**What needs resolving**: Add T4 (HierarchicalParsing, ASN-0034) to OrdShiftHom's Formal Contract Depends, noting it supplies the zeros definition consumed when concluding zeros(r) = 0 in part (b). Add NAT-card (NatFiniteSetCardinality, ASN-0034) noting it supplies |∅| = 0 for the empty-index-set step. Alternatively, S8a's Formal Contract can export the biconditional zeros(t)=0 ⟺ (A i : 1 ≤ i ≤ #t : tᵢ > 0) as a formal postcondition, which OrdShiftHom can then cite directly without requiring T4 and NAT-card in its own Depends.

VERDICT: REVISE