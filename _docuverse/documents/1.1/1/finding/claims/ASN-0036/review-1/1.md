### Orphaned proof labels (b), (c), (d) in ValidInsertionPosition
**Class**: OBSERVE
**Foundation**: N/A (internal labeling)
**ASN**: ValidInsertionPosition proof: *"...at j = 0 the position is v = min(V_1(d)) = [1, ..., 1] by D-MIN. **This is (d)**. Every component is then ≥ 1 ... so zeros(v) = 0 with **componentwise positivity (b)**, and OrdShiftHom (a) fixes v₁ = 1 ... giving exactly N + 1 positions **(c)**."*
**Issue**: The parentheticals "(d)", "(b)", "(c)" reference lettered clauses that do not exist — the Definition and Postconditions of ValidInsertionPosition are unlabeled prose. (`OrdShiftHom (a)` is a valid reference to OrdShiftHom's part (a), but (b)/(c)/(d) resolve to nothing.) This is reviser-drift residue from a prior version whose predicate had lettered conditions; a reader following the proof has to skip past dangling tags.
**What needs resolving**: Either label the Postcondition clauses (a)–(d) so the references resolve, or replace the parenthetical tags with plain prose ("…so v is zero-free and componentwise positive," etc.).