### S7 Well-definedness proves "no adjacent zeros" via a four-case reductio that duplicates the Postconditions' one-line separation argument
**Class**: OBSERVE
**Foundation**: T4 (HierarchicalParsing) field-segment constraint — ASN-0034
**ASN**: S7 (StructuralAttribution), Well-definedness. The *"No two zeros adjacent"* step supposes `origin(a)ᵢ = origin(a)ᵢ₊₁ = 0`, locates `i, i+1` among the two zero positions `{#N(a)+1, X}`, walks four assignments eliminating three, and derives `1 = #U(a)+1 ≥ 2` in the survivor. The Postconditions section establishes the identical conjunct in one line — *"their difference `#U(a) + 1 ≥ 2` … keeps the two separators at least two indices apart, hence never at consecutive positions"* — and explicitly states the four-case walk is *"not repeated here."*
**Issue**: At the point of the four-case reductio in Well-definedness, the separation `X − (#N(a)+1) = #U(a) + 1 ≥ 2` has already been established (it is used immediately above to count the two zeros as distinct). Non-adjacency follows directly: two zeros separated by `≥ 2` cannot occupy consecutive positions. The four-case reductio re-derives this same fact at roughly four times the length, and the Postconditions section confirms the author knows the one-line argument suffices (it uses exactly that argument). The verbose version is a redundant sub-proof a reader must wade through in an already very large proof.
**What needs resolving**: Replace the four-case adjacency reductio in Well-definedness with the separation argument (two zeros differ by `#U(a) + 1 ≥ 2`, hence are never at consecutive indices), matching the Postconditions section.

VERDICT: OBSERVE