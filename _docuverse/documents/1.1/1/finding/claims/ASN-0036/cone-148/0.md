### D-CTG-depth Formal Contract Depends entries for NAT-addcompat, NAT-zero, and NAT-order attribute D-MIN's induction proof steps to D-CTG-depth
**Class**: REVISE
**Foundation**: NAT-addcompat (NatAdditionOrderAndSuccessor); NAT-zero (NatZeroMinimum); NAT-order (NatStrictTotalOrder); D-MIN (VMinimumPosition)
**ASN**: D-CTG-depth Formal Contract Depends — NAT-addcompat entry opening: *"the inference rule consumed in the ⊇ direction of the induction step's segment identity {j : 1 ≤ j ≤ N + 1} = {j : 1 ≤ j ≤ N} ∪ {N + 1}, on which the Q⁻ = ∅ ⇒ Q = {N + 1} branch of the existence proof turns"*; NAT-zero entry: *"seeds the lower bound 1 ≤ N + 1 of the singleton element N + 1 in the ⊇ direction of the induction step's segment identity"*, closing with *"not through the transitive routes D-MIN → NAT-induction → NAT-zero…"*; NAT-order entry opening: *"all consumed as first-class inference steps in the induction step's segment identity {j : 1 ≤ j ≤ N + 1} = {j : 1 ≤ j ≤ N} ∪ {N + 1}"*, closing with *"D-MIN consumes the same axiom clauses in the same direct way and is cited on the same standard"*.
**Issue**: D-CTG-depth's proof is a contradiction argument — it finds a first disagreement j via NAT-wellorder, constructs N+1 witnesses via T0(a), forces them into dom(M(d)) via D-CTG, then reaches N+1 ≤ N via D-INJ + NAT-card. It contains no induction, no segment identity {j : 1 ≤ j ≤ N+1} = {j : 1 ≤ j ≤ N} ∪ {N+1}, no Q⁻ = ∅ branch, no singleton element N+1, and no existence proof — all constructs that live in D-MIN's least-element induction. The three Depends entries are near-verbatim copies of D-MIN's entries (including the self-referencing phrase "D-MIN consumes the same axiom clauses"). D-CTG-depth's actual uses of these axioms — NAT-addcompat at j for j < m in the w < x branch, at m to rule out T1 clause (ii), at N for the closing N < N+1 ≤ N → N < N step; NAT-zero and NAT-addcompat's right-order compatibility for 1 ≤ N+1 to discharge D-INJ's P ≥ 1 guard at P := N+1; NAT-order's transitivity for 0 < u_{j+1} < n → 0 < n in the zeros check, its trichotomy for fixing u < x WLOG, and its irreflexivity for the final ¬(N < N) — are described nowhere in the Depends section. A formal verifier reading the Depends entries would search for a segment-identity induction that does not exist in D-CTG-depth's proof and would fail to verify the actual inference steps the claim makes.
**What needs resolving**: Replace the NAT-addcompat, NAT-zero, and NAT-order Depends entries with descriptions of D-CTG-depth's actual uses. NAT-addcompat: strict successor at n := j (j < j+1, leading to j < m for the w < x branch's T1(i) position condition), at n := m (m < m+1, to exclude T1 clause (ii) for the shared-depth pair u, x), at n := N (N < N+1, for the closing contradiction), and its right-order compatibility at p := 0, n := N, m := 1 (delivering 0+1 ≤ N+1 toward 1 ≤ N+1 to discharge D-INJ's P ≥ 1 guard). NAT-zero: its clause 0 < N ∨ 0 = N at n := N, read as 0 ≤ N, seeding the 1 ≤ N+1 chain that discharges D-INJ's guard (the same chain that appears in D-MIN and D-SEQ for the singleton lower bound, but grounded here for the D-INJ instantiation). NAT-order: its trichotomy for fixing u < x without loss of generality, its pure <-transitivity for 0 < u_{j+1} < n → 0 < n in the zeros(w) = 0 check and for the mixed chains j < j+1 ≤ m → j < m and N < N+1 ≤ N → N < N via the ≤-definition split, and its irreflexivity ¬(N < N) for the final contradiction.

VERDICT: REVISE