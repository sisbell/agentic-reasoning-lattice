### S7 and S7a note proofs state the T10a.4 instantiation precondition with a type error
**Class**: REVISE
**Foundation**: T10a.4 (T4PreservationUnderDiscipline) — postcondition quantified over `A ∈ 𝒯, t ∈ dom(A)`; T10a (𝒯 = "the set of allocators"); S7a (DocumentScopedAllocation)
**ASN**: Note narrative — "## Structural attribution", S7 well-definedness: *"instantiating it at `A := A_element` and `t := a` — legitimate precisely because S7a places `a ∈ dom(A_element) ⊆ 𝒯` — yields that `a` is T4-valid"*. And the S7a definition body: *"every `a ∈ dom(Σ.C)` satisfies `a ∈ dom(A_element) ⊆ 𝒯` for a T10a-conforming element allocator `A_element` … This membership is the premise that … licenses T10a.4's preservation invariant to be instantiated at `a`."*
**Issue**: `𝒯` is the set of **allocators** (T10a: "the set of allocators induced by T10a"); `dom(A_element)` is a set of **addresses** (tumblers). The chain `a ∈ dom(A_element) ⊆ 𝒯` asserts `dom(A_element) ⊆ 𝒯` — that addresses are allocators — and concludes `a ∈ 𝒯`. That is a type error. T10a.4's instantiation needs two memberships of distinct types: `A_element ∈ 𝒯` (allocator-in-tree, for `A := A_element`) and `a ∈ dom(A_element)` (address-in-domain, for `t := a`). The note's compression supplies the second but replaces the first with the type-incorrect `dom(A_element) ⊆ 𝒯`, so it never establishes the allocator membership `A_element ∈ 𝒯` that the `A`-instantiation actually requires — the precondition chain into T10a.4 is broken at the note. The per-claim S7 and S7a files already carry the corrected formulation (*"`A_element ∈ 𝒯` and `a ∈ dom(A_element)`, the two memberships of distinct types the instantiation requires"*; *"These two memberships are the premises…"*), and S7's formal-contract preconditions list `A_element ∈ 𝒯 ∧ a ∈ dom(A_element)`. The note copy is stale: it still has the single buggy chain and the singular "This membership."
**What needs resolving**: Reconcile the note's S7 proof and S7a definition with the claim files — replace `a ∈ dom(A_element) ⊆ 𝒯` with the two distinct-type memberships `A_element ∈ 𝒯` and `a ∈ dom(A_element)`, and "This membership" with "These two memberships," so the T10a.4 instantiation precondition is type-correct and actually establishes `A_element ∈ 𝒯`.

**Routing**: emitted as comment.observe (non-blocking) — fix locus not an editable claim in this ASN.
