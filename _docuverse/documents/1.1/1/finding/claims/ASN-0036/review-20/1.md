### S7 Depends rationales re-derive the proof body as use-site inventory
**Class**: OBSERVE
**Foundation**: S7 (StructuralAttribution) — Depends slot
**ASN**: Several S7 Depends entries restate the proof's case analysis rather than naming what the foundation supplies. E.g., the NAT-order entry: *"supplies the strict total order `<` on ℕ … used throughout Well-definedness to compare positions: the separation `#N(a) + 1 < ((#N(a) + 1) + #U(a)) + 1` of the two zero positions — established not from the positivity of their difference alone but by NAT-order's `≤`-definition and transitivity chaining … — the magnitude `#U(a) + 1 ≥ 2 > 0` of their difference; the four-case walk in the no-two-zeros-adjacent step … its two equal-assignment cases (`i = i + 1`) closed by irreflexivity … its order-reversed case … closed by the exactly-one trichotomy clause … and the bound `1 = #U(a) + 1 ≥ 2` … that … closes the contradiction by forcing `1 < 1` …"* The NAT-addcompat and NAT-sub entries are similarly paragraph-length re-narrations of the same four-case adjacency walk that the proof body already performs.
**Issue**: These are use-site inventories in a structural slot — to learn that NAT-order *supplies the strict order on ℕ* the reader must skip past a full restatement of the well-definedness case analysis already given in the body. The duplication is exactly what the precise reader must work around; it adds surface without advancing the argument, and (per sprawl-blinds-review) compounds the difficulty of auditing the genuinely load-bearing claim. The math is sound; this is about reviewability, not correctness.
**What needs resolving**: Collapse the S7 Depends rationales to what each foundation *supplies* (the operator/axiom and the one site that consumes it), letting the proof body carry the case-by-case derivation rather than restating it in the Depends slot.

VERDICT: REVISE