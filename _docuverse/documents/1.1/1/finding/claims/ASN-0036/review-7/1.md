### S7 narrative proof instantiates T10a.4 without discharging its domain-membership premise
**Class**: REVISE
**Foundation**: T10a.4 (T4PreservationUnderDiscipline) — precondition `t ∈ dom(A)` for an allocator `A` in a T10a-conforming tree; S7a (DocumentScopedAllocation)
**ASN**: Body proof of S7, Well-definedness: *"By S7b (element-level I-addresses), `zeros(a) = 3`, and **by T10a.4 …, `a` is T4-valid**; hence T4's field-decomposition machinery applies to `a`."* The formal-contract proof instead writes: *"We obtain T4-validity from the allocation discipline. By S7a …, `a ∈ dom(A_element) ⊆ 𝒯` … instantiating it at `A := A_element` and `t := a` — legitimate precisely because S7a places `a ∈ dom(A_element)` — yields that `a` is T4-valid."*
**Issue**: T10a.4 yields T4-validity only for addresses in some allocator's domain. The body jumps from `zeros(a) = 3` (S7b) to "`a` is T4-valid by T10a.4" without the membership `a ∈ dom(A_element)` that licenses the instantiation; `zeros(a) = 3` alone fixes separator count but not the validity premise. The formal contract supplies the missing S7a step. Same lagging pattern appears in the S5 body proof, whose within-document construction concludes `|{v : …}| = N+1` but never bridges to the global multiplicity `|{(d,v) : …}|` that the postcondition is stated over (the formal contract adds "Since `d` is the only document, this within-document count equals the global multiplicity").
**What needs resolving**: Bring the S7 (and S5) body proofs up to the rigor of their formal contracts — discharge T10a.4's `a ∈ dom(A_element)` premise via S7a in S7's Well-definedness, and bridge within-document to global multiplicity in S5. More broadly, run a sync pass: the narrative proofs are stale relative to the tightened contract proofs.

**Routing**: emitted as comment.observe (non-blocking) — fix locus not an editable claim in this ASN.
