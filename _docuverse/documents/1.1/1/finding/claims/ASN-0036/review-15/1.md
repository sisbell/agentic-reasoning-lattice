### D-CTG summary-table dependency gloss disagrees with its formal contract
**Class**: OBSERVE
**Effective class**: REVISE (classifier override)
**Classifier rationale**: Without aligning the gloss, the summary table states a dependency set (S8a, S8-depth) that the claim's formal contract and statement do not actually use while omitting ones they do (T4, T0, S8-fin), so the artifact asserts something untrue about D-CTG's grounding.
**Foundation**: T4 (zeros), T0 (length/component), T1 (order), S8-fin (finiteness) — what D-CTG's statement actually consumes.
**ASN**: The Properties Introduced table row reads *"D-CTG | … | design; uses S8a, S8-depth, T1 (ASN-0034)"*, but the per-claim formal contract lists *Depends:* `S8-fin, T4, T1, T0`. The two lists share only T1: the table names S8a and S8-depth (neither appears in the contract), and omits T4, T0, S8-fin (all in the contract).
**Issue**: D-CTG's statement uses `zeros(v) = 0` directly (T4), `#v = #u` (T0), `<` (T1), and the "single unbroken block" remark (S8-fin); it does not reference the S8a predicate or S8-depth's uniformity. The formal contract is the accurate one; the table gloss mis-attributes dependencies a graph-builder reading the summary would pick up wrong.
**What needs resolving**: Align the table's derivation gloss for D-CTG with the formal-contract Depends (`S8-fin, T4, T1, T0`), or drop the S8a/S8-depth attributions that the claim's statement does not use.

**Routing**: emitted as comment.observe (non-blocking) — fix locus not an editable claim in this ASN.
