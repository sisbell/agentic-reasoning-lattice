### S4 part (c): two different proofs of `reach(λ) = p`, one reading as a correction of the other
**Class**: OBSERVE
**Foundation**: D1 (DisplacementRoundTrip), WF (WellFormedSpanFromEndpoints).
**ASN**: S4 (SplitPartition), part (c). Narrative: "Since #s = #p (level compatibility) and s < p, the divergence is of type (i) … so D1's preconditions … are met and D1 gives s ⊕ (p ⊖ s) = p. So reach(λ) = s ⊕ d = p = start(ρ)." Standalone: "The construction of λ above already invoked WF, whose postcondition reads reach(λ) = s ⊕ (p ⊖ s) = p. Read at the endpoints (s, p), this is exactly the identity we need: it delivers reach(λ) = p directly, with no further argument — **in particular no re-derivation of the divergence precondition**."
**Issue**: Both proofs are sound, but they take different routes (narrative re-derives D1's divergence precondition; standalone reads it off WF's postcondition, which already absorbed that derivation). The standalone's "no re-derivation of the divergence precondition" phrasing reads as a pointed correction of the narrative — leftover residue of a revision that updated one site and not the other. Within a single ASN the same proof step should not be argued two different ways.
**What needs resolving**: n/a (OBSERVE) — collapse to one route (the WF-postcondition route is shorter and avoids re-deriving the divergence type).

A general note, not a finding: the *Depends* fields throughout the standalone contracts (notably S6's TumblerAdd entry, S11's TumblerAdd entry, S3/S8's TumblerAdd entries) carry multi-sentence use-site inventories that restate the proof body inside the dependency slot. This matches the verbosity of the imported foundation statements, so I treat it as house style rather than flag each instance — but it is the kind of prose that compounds, and S6/S11 are at the heavy end.

VERDICT: REVISE