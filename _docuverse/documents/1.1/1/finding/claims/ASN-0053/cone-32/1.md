### S2 — defensive type-coherence prose for a comparison no one is making (reviser drift)
**Class**: OBSERVE
**Foundation**: ActionPoint / Span preconditions — `actionPoint(ℓ) ∈ ℕ`, precondition `actionPoint(ℓ) ≤ #s`.
**ASN**: S2 prose: "This second condition is a comparison of natural numbers — actionPoint(ℓ) is the ℕ action point of the length — not of the end offset s ⊕ ℓ, which is a tumbler." Restated in *Preconditions*: "The last is a comparison of natural numbers (actionPoint(ℓ) ∈ ℕ), **not the type-incoherent comparison of the tumbler s ⊕ ℓ against #s**."
**Issue**: The precondition `actionPoint(ℓ) ≤ #s` is plainly an ℕ–ℕ comparison; nothing in the claim writes or invites `s ⊕ ℓ ≤ #s`. The text twice defends against a type error the claim never commits — the "new prose explaining why a comparison is well-typed rather than what it says" pattern, duplicated across the prose body and a structural slot. A precise reader must skip past it to follow the single-step argument.
**What needs resolving**: Drop the type-incoherence disclaimer (both occurrences); stating `actionPoint(ℓ) ≤ #s` with `actionPoint(ℓ) ∈ ℕ` already carries the typing.