### S6 — counterfactual "drop a precondition" prose imagines an excluded case (reviser drift)
**Class**: OBSERVE
**Foundation**: TumblerAdd (TumblerAdd) — preconditions `Pos(w)`, `actionPoint(w) ≤ #a`.
**ASN**: S6 prose: "Drop a precondition — say Pos(ℓ) — and s ⊕ ℓ need not be defined, so the length identity has nothing to stand on." Restated in *Preconditions*: "They are not implied by level-uniformity: a level-uniform pair with Pos(ℓ) failing has reach(σ) undefined, and the length identity below does not hold of it."
**Issue**: `Pos(ℓ)` and `actionPoint(ℓ) ≤ #s` are stated preconditions of the claim. The prose imagines the claim's own precondition failing and narrates the consequence — the "paragraph imagines a case the precondition already excludes" drift pattern — and does so twice (prose + Preconditions). It explains why the precondition is needed rather than what the claim asserts; it does not advance the length-identity argument.
**What needs resolving**: Remove the counterfactual ("drop Pos(ℓ) …") in both places; the Preconditions field already lists `Pos(ℓ)` and `actionPoint(ℓ) ≤ #s` as required, which is the operative content.