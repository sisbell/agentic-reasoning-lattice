### S11c Axiom slot carries use-site essay and a refactor-residue parenthetical
**Class**: OBSERVE
**Foundation**: Span / SC (denotation convention ⟦σ⟧ = {t : start(σ) ≤ t < reach(σ)})
**ASN**: S11c *Axiom*: "For any span σ, ⟦σ⟧ = {t : start(σ) ≤ t < reach(σ)} — reach(σ) is the exclusive upper bound… **This half-open convention is the external fact the witness constructions take as given: it is what lets us read ⟦γ⟧ = … and ⟦γ'⟧ = … directly off the constructed endpoints and match each to ⟦α⟧ \ ⟦β⟧. (The discharge of WF's carrier and length preconditions for these witnesses is derived, not stipulated, and is carried out in the Case 2 proof body.)**"
**Issue**: Only the first sentence states the convention; the remainder is use-site explanation ("it is what lets us read…") plus a defensive parenthetical reassuring the reader where the discharge lives. The parenthetical is residue from the recent "demote Axiom discharge proof into body" edit — it explains what is *not* in the slot rather than asserting the convention. This is the "prose around an axiom explains why it is needed rather than what it says" pattern, and it makes the reader skip past meta-commentary to reach the load-bearing line.
**What needs resolving**: Reduce the Axiom slot to the convention itself (the half-open denotation with reach exclusive); drop the use-site sentence and the discharge-location parenthetical (the body already carries the discharge).