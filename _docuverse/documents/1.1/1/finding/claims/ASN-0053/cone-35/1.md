### "Level-uniformity is not enough / sole source" point repeated across four slots
**Class**: OBSERVE
**Foundation**: TumblerAdd (result-length identity `#(a ⊕ w) = #w`)
**ASN**: The same precondition-necessity argument appears in S6 body ("**Drop a precondition — say Pos(ℓ) — and s ⊕ ℓ need not be defined, so the length identity has nothing to stand on.**"), S6 *Preconditions* ("**They are not implied by level-uniformity: a level-uniform pair with Pos(ℓ) failing has reach(σ) undefined…**"), S6 *Depends* ("**This is the sole source of the addition result-length: the in-scope foundations supply only the subtraction length (TumblerSub…) and the round-trip identity (D1…), neither of which yields #(s ⊕ ℓ) = #ℓ…**"), and again in S11c Case 2 ("**the reach equality being S6's packaging of TumblerAdd's result-length identity … not a consequence of #width(σ) = #start(σ) by itself**").
**Issue**: The single substantive claim — that #reach needs TumblerAdd's postcondition under its ⊕ preconditions, which level-uniformity alone does not supply — is correct and load-bearing once. Stated four times it becomes noise: the Depends version is a use-site inventory of what *other* foundations do not provide; the body and Preconditions versions are counterfactuals imagining `Pos(ℓ)` failing, a case the "well-formed span" precondition already excludes. These compound across cycles.
**What needs resolving**: Keep the necessity statement in one slot (the S6 body is the natural home). Trim the Preconditions echo, the Depends "sole source / what others don't supply" inventory, and the S11c Case 2 parenthetical to bare cross-references, removing the counterfactuals about excluded preconditions.

VERDICT: OBSERVE