### D0 listed as "cited" in Properties Introduced but never invoked
**Class**: OBSERVE
**Effective class**: REVISE (classifier override)
**Classifier rationale**: The Properties Introduced table claims D0 is "cited" but no proof in the ASN invokes it, making the table factually inaccurate about which foundations are actually consumed.
**Foundation**: D0 (DisplacementWellDefined, ASN-0034)
**ASN**: Properties Introduced table — row "D0 | Displacement well-definedness: a < b and divergence(a, b) ≤ #a (DisplacementWellDefined, ASN-0034) | cited". The "## The reach function" prose grounds round-tripping in "TumblerSub ... WF and WR below discharge the conditions," and the actual proofs that need displacement facts use D1 (WF, S5), D2 (WR), and TumblerSub directly.
**Issue**: No proof in the ASN invokes D0; its well-definedness results (b ⊖ a ∈ T, Pos, actionPoint = divergence index) are obtained instead through TumblerSub inside WF. Marking it "cited" overstates the dependency relative to D1/D2, which are genuinely consumed.
**What needs resolving**: N/A (OBSERVE) — the table entry is harmless but inaccurate; D0 is imported, not cited by any proof.

VERDICT: REVISE

**Routing**: emitted as comment.observe (non-blocking) — non-actionable (What-needs-resolving N/A/absent).
