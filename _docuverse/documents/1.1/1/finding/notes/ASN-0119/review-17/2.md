### Issue 3: The Claims Introduced table reproduces body proofs in its cells
**ASN-0119, "Claims Introduced"**: The S3★ row carries a full derivation — "for a text position v, M'(d)(v) = M(d)(π⁻¹(v)) with π⁻¹(v) again a text position, so pre-state S3★ gives M(d)(π⁻¹(v)) ∈ dom(C); link positions are frame-fixed (ASN-0084 R-NS)... What is invariant is that π maps each subspace onto itself, not the image filed at any individual key."

**Problem**: This is the body's S3★ argument restated near-verbatim in a structural slot meant for claim statements; the clarifying sentence "what is invariant is that π maps each subspace onto itself, not the image filed at any individual key" appears a third time (the body already says "What `π` preserves is that it maps each subspace onto itself"). A summary table should name the claim and its source, not re-prove it.

**Required**: Reduce the S3★ row (and any other cell carrying a multi-sentence proof) to the claim statement plus its provenance; leave the derivation in the body once.