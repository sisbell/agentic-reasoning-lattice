### Issue 1: `is_doc`'s grounding is stated twice, with mutual cross-pointers between V-DOC and QD-audit
**ASN-0129, V-DOC and QD-audit**: V-DOC: "document-residence, the emit surface's own home-validation test (I1/I5, ASN-0128; performed per branch structure, QD-audit) exposed as a read … `dom(Σ.M)` is not a QD base, the admission and the restriction grounded together at QD-audit." QD-audit: "document-residence `d ∈ dom(Σ.M)` is the emit surface's own home validation — performed on every `idem = ⊤` miss (I1: home validation is branch-local, a hit never reads `d`), every admitted `idem = ⊥` call (I5), and every `Nullify_Binary` call (P0, both branches, S3 — all ASN-0128) … V-DOC admits exactly that test, as `is_doc`."
**Problem**: The same justification — the atom is the emit surface's own home-validation test, performed per branch — is written in both places, each deferring to the other ("performed per branch structure, QD-audit" / "V-DOC admits exactly that test"). This is the accretion signature the note's review mode flags: two paragraphs in different sections saying the same thing in different words, joined by mutual deferral. The reader processes the grounding twice and must check the two statements agree. (The membership-only restriction additionally recurs at PC6's base list, Structural-reads-only, and FP — those repetitions are slot-inherent and fine; the duplicated *justification* is the problem.)
**Required**: One site owns the grounding. Either V-DOC states the atom's definition and the membership-only restriction bare, with QD-audit carrying the full emit-surface justification (and QD-audit's sentence reducing to "the document store contributes the membership atom `is_doc` (V-DOC), not a base" before the document-census rationale); or V-DOC keeps the full grounding and QD-audit points to it. Delete the surviving cross-pointer in the other direction.