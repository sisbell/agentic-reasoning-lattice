### Topic 2: evaluation atomicity against concurrent writers
**Why out of scope**: PC4 fixes determinism of two evaluators at the *same* Σ; whether an implementation provides snapshot isolation for an evaluation racing `→_sh` steps is scheduler territory the note explicitly fences ("fire atomicity" in the not-covered list). If protocols need a stated guarantee, it belongs in the protocol-layer ASN, not here.

VERDICT: REVISE