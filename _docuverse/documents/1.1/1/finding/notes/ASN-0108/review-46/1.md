### Issue 2 (anti-bloat): meta-prose around the key naming, and a repeated qualifier
**ASN-0108, "The Enumeration Order" (Gregory's reading)**:
> "Because the key is this fixed selection from the immutable endset — not the link's currently-matched endpoint — we name it for the selection rather than the match."

**Problem**: The preceding two sentences already establish (a) the key is the least covered I-address (a fixed endset selection) and (b) why the matched-endpoint alternative is rejected (not state-stable). This sentence adds only a rationale for the *name*, restating (a)/(b) as a naming decision. It is skippable meta-prose — the reasoning is complete without it.

**ASN-0108, W9b**: the multiplicity qualifier is stated three-plus times in one claim — "*read as a count with multiplicity, never a set*", "*one event per link placed, not one per transition*", "*the event term counted with multiplicity over links*". One statement (with the per-link LP18 example) carries the point; the repetitions are padding.

**Required**: Delete the naming sentence; state the multiplicity qualifier once.