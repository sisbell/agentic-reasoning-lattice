### Topic 1: Full (non-sliced) content/link domain disjointness
**Why out of scope**: L14 and L0b establish disjointness only over `dom(Σ.C)|_{s_C}`, not all of `dom(Σ.C)`. This gap is correctly identified in the ASN's own Open Questions ("Should a content-side invariant fix a global content-subspace constant…"). Closing it requires a new content-side invariant fixing a global content subspace — a future ASN, not an error here. The scoped guarantee is sound as stated.

VERDICT: REVISE