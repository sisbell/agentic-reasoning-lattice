### Topic 1: Whether content-side disjointness should extend beyond the `s_C`-resident slice
**Why out of scope**: The ASN deliberately scopes L14/L14a/L0a disjointness to `dom(Σ.C)|_{s_C}` and flags the global-constant question in Open Questions. Promoting it to an unconditional content-subspace invariant is new territory, not an error here.

VERDICT: REVISE