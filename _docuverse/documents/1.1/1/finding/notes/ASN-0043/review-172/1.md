### Topic 1: Global content-subspace residence
L14, L14a, L1d(b), and L11b all condition disjointness on the `s_C`-resident hypothesis (L0a), leaving content outside `s_C` outside the disjointness guarantee. This is correctly scoped — the ASN flags it in Open Question 1 ("Should a content-side invariant fix a global content-subspace constant…"). Establishing residence as a system invariant rather than a per-state hypothesis is future-ASN territory, not an error here.

**Why out of scope**: The conditional framing is deliberate and acknowledged; promoting it to an unconditional invariant requires a new content-side axiom, which belongs in a content-model revision, not this link note.

VERDICT: REVISE