### Topic 1: Concurrent fork semantics, snapshot vs living forks, transcludent source forking
The ASN's Open Questions list covers concurrent modification beyond SequentialTransitionAxiom, the snapshot/living fork distinction, forking when `M(d_src)` references foreign-origin I-addresses, and version-space presentation as a coherent collection.

**Why out of scope**: These topics depend on protocol layers (concurrency model beyond sequential atomicity, semantic version graph, transclusion forking semantics, version-space presentation) not present in ASN-0047's transition model. Correctly flagged as forward topics.

VERDICT: REVISE