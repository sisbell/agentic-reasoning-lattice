### Issue 1: TA5(b) citation missing in E0's #E preservation arguments
**ASN-0076, E0 (EditLink as Composite)**: Two sites use this incomplete citation:

1. Depth-bound paragraph (successor step): "TA5(c) gives `#t' = #t` (total tumbler length preserved) and confines the modification to position sig(t)."
2. Supersession-step verification: "The depth bound `#E(ℓ_sup) ≥ 2` follows from TA5(c) inherited from `#E(ℓ_new) ≥ 2`."

**Problem**: TA5(c) (ASN-0034) specifies only `#t' = #t` and `t'_{sig(t)} = t_{sig(t)} + 1` for `k = 0`. The clause "modification confined to position sig(t)" — that positions other than sig(t) are unchanged — is supplied by TA5(b), not TA5(c). The subsequent step "no other position is touched" rests on this confinement, and the field-decomposition preservation argument (`#E(t') = #E(t)` because zero positions are identical between `t` and `t'`) depends on it. Without TA5(b), one cannot exclude a hypothetical inc(·,0) variant where some other position changes — TA5(c) alone is consistent with such a variant. The proof's logical conclusion is correct (TA5(b) holds and supplies the confinement), but the citation chain is incomplete at both sites.

**Required**: Add TA5(b) to the citation alongside TA5(c) at both sites, or restructure to attribute length preservation to TA5(c) and modification confinement to TA5(b) separately.

VERDICT: REVISE