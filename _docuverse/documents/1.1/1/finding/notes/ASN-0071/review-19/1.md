### Topic 1: Historical-containment (`R`-based) query as an operation
The reconciliation section correctly defers "what documents EVER contained this" to a separate mechanism over `R`, and lists it as an Open Question. That operation belongs in a future ASN, not here.

VERDICT: REVISE