### Topic 1: The effective-owner identity `ω_{Σ'}(d) = ω_Σ(A)` and the E↔B coupling invariant
**Why out of scope**: The ASN correctly identifies that `ω` is defined over ASN-0042's registry `B`, absent from the `(C,L,E,M,R)` state model, and that no foundation couples the entity-set document chain to the registry sibling stream. It declines to assert the `ω`-valued statement and defers it (and the coupling invariant) to a registry-carrying ASN. This deferral is the right call — the coupling is genuinely new territory, not a defect here.

VERDICT: REVISE