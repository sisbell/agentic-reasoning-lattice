### Topic 1: The effective-owner equality `ω_{Σ'}(d) = ω_Σ(A)`
**Why out of scope**: CND.own correctly declines to assert this over the registry-free state `(C,L,E,M,R)` and defers it to a registry-carrying ASN. This deferral is sound and properly handled, not an error here.