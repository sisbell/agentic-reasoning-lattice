### Topic 1: V-presentation of returned endsets in a querying document's coordinates
**Why out of scope**: The ASN explicitly defers the contract for re-presenting an endset in `d`'s V-space (the lossy projection when `d` arranges only part of the coverage) to the first Open Question. This is new territory for a later ASN, not a defect here.