### Topic 1: Contract for presenting returned endsets back in a querying document's V-coordinates
The ASN explicitly defers the lossy I→V presentation of returned endsets (closing prose; Open Question 1). This is correctly held out as future work, not an error here.