### Issue 1: D-CTG★ preservation asserted "read off the set" without the interior-component argument
**ASN-0087, Invariant Preservation → Per-State Invariants (D-SEQ★ paragraph)**: "The same explicit set discharges D-CTG★ directly: the exhibited set is a contiguous segment under T1 at the fixed depth, which is exactly the D-CTG★ contiguity conjunct — read off the set itself." And in the table row for D-CTG★: "discharged directly by the D-SEQ★ set computation below, which exhibits a contiguous initial segment."

**Problem**: D-CTG★ (ASN-0047) quantifies over the *full* depth-`m_L`, subspace-`s_L` slice — every positive-component tuple `z` with `v_lo ≤ z ≤ v_hi` must be a member. Exhibiting the post-state set `{[s_L, 1, …, 1, k] : 1 ≤ k ≤ n_L+1}` as "a contiguous segment" only shows contiguity in the *last component*. For `m_L = 2` (the depth MAKELINK itself always commits, per M-DepthConv) the slice is exactly `{[s_L, k]}` and the claim is trivially complete — but D-CTG★ must be preserved at *every* reachable pre-state, and the ASN's own Effect/S8a prose admits `m_L(d) ≠ 2` is reachable ("else the existing `m_L(d)`", since K.μ⁺_L's `ValidFirstLinkPosition` admits any `m ≥ 2` from a non-MAKELINK placer). At `m_L ≥ 3` the non-trivial step — that any slice tuple with an interior component `> 1` exceeds the max and so cannot lie strictly between two members — is exactly what makes contiguity hold, and it is not shown. "Read off the set itself" is the checkmark this review exists to flag.

**Required**: Either (a) supply the one-line interior-component argument (any `z` in the slice with `z_j > 1` for some `2 ≤ j ≤ m_L−1` satisfies `z > [s_L, 1, …, 1, n_L+1]`, so the only between-tuples are `[s_L, 1, …, 1, k]` with `1 ≤ k ≤ n_L+1`, all members), or (b) argue from the contiguous pre-state — `V_{s_L}(d)` is D-CTG★ at `Σ`, MAKELINK adds only the immediate canonical successor at the top, and no slice tuple lies strictly between old-max and new-max — which discharges D-CTG★ for all `m_L` without re-deriving the full slice.

VERDICT: REVISE