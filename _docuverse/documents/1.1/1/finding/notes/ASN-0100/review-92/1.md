### Issue 2: §Position Constraints — non-advancing recap
**ASN-0100, §Position Constraints**: "Which regions are empty at a given position — and when K.μ⁻ is consequently omitted — is already fixed by (INS.μ⁻-fires) and §Effect Three; the append (`j = N`) and empty-document cases are exactly those with `Right = ∅`."

**Problem**: With the `review-mode.anti-bloat` classifier active: this sentence restates facts already established by (INS.μ⁻-fires), §Effect Three, and the append/empty worked examples, deferring to two prior locations without advancing the argument. The substantive claim of the section (universal region forms handle edge cases via vacuous quantifiers, the Q6/APPEND point) stands on its own; this back-reference sentence is recap.

**Required**: Delete the recap sentence; the surrounding substantive claims do not depend on it.