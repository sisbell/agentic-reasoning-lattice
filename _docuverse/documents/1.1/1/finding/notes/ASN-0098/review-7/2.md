### Topic 1: Reverse-discovery primitive invariants
Determining, for a given V-position, the set of links projecting to it is a separate operation requiring its own state structure (a back-pointer index). Belongs in a future ASN.