### Topic 1: Link-canonical companion wp case
**Why out of scope**: The ASN explicitly flags this case (every span canonical with `s = [d_s, 0, s_L, k_s]` under the `n'_{s_C} = 0, n'_{s_L} > 0` retention pattern) as OUT_OF_SCOPE within LP12b, with rationale that LP-Fin Corollary at `X = s_L` gives F-candidates inhabiting `dom(L)`-eligible territory, so the structural argument inverts. Appropriate deferral to future work.