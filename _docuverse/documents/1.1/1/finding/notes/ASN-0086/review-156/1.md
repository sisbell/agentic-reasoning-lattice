### Topic 1: Higher-arity typed relations (`|Σ.L(a)| > 3`)
**Why out of scope**: The note explicitly restricts to standard-triple links and lists multi-arity projection as an Open Question. Defining `L_K^{(n)}` is new territory, not a defect here.