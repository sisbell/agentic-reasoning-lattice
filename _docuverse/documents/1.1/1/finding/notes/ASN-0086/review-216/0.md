### Issue 1: FreshLinkKeyDisjointness non-publication rationale stated twice
**ASN-0086, Sub-lemma — FreshLinkKeyDisjointness** and **R0 (closing paragraph)**:

The sub-lemma opens: "L14 (DualPrimitive) and L14a (NonTranscludability) of ASN-0043 are *not* published by ASN-0093's K.λ contract, so this note carries them across each K.λ-step itself, via this sub-lemma."

R0's final paragraph then re-states the same justification: "(with L14/L14a at the fresh key supplied by the FreshLinkKeyDisjointness sub-lemma, since ASN-0093's K.λ contract does not itself publish them)."

**Problem**: The "ASN-0093's K.λ contract does not publish L14/L14a, so this note carries them" rationale appears in two places — once as the sub-lemma's reason-for-existence, once again parenthetically at R0's invocation site. This is the "two paragraphs say the same thing in different words" pattern, with the lemma's raison d'être restated where the lemma is merely *used*. The sub-lemma's *content* is load-bearing (R0 genuinely needs L14/L14a at the fresh key); only the meta-justification is duplicated. At the R0 use-site the parenthetical re-explanation interrupts the chain one must follow to confirm Σ' satisfies the full invariant catalog.

**Required**: Keep the non-publication rationale at the sub-lemma (its natural home, where it scopes the lemma). At R0's invocation, drop the "since ASN-0093's K.λ contract does not itself publish them" clause and cite the sub-lemma by name only — e.g., "with L14/L14a at the fresh key supplied by FreshLinkKeyDisjointness."