# Channel Assignment — ASN-0077 review-38

**Date:** 2026-05-28 11:37

## Issue 1: O0(b)/O0a take on an ungrounded vocabulary-completeness assumption that foundations already discharge
Reason: The fix is internal — it asks whether ASN-0047's Allocator-hierarchy definition (`origin(ℓ) = d` for every output of `A_L(d)`) plus L1c and SubAllocatorAxiom (e) already deliver the dom(L) correspondence and totality without the frame-vocabulary-exhaustiveness apparatus. That is a logical restructuring against foundation claims already cited in the ASN; no design-intent or implementation evidence is required to decide whether L1c + SubAllocatorAxiom subsume the K.λ-event closure.

## Issue 2: The frame-vocabulary exhaustiveness derivation is restated three times nearly verbatim
Reason: The fix is internal — collapse the triplicated closure prose to a single statement (O0a) and have O0(b)/O0(c) cite it. Pure deduplication derivable from the ASN's own content.
