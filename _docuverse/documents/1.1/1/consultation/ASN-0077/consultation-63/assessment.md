# Channel Assignment — ASN-0077 review-63

**Date:** 2026-06-04 13:30

## Issue 1: Worked example re-derives foundation K.μ~ admissibility
Reason: Internal fix. The required change — citing K.μ~'s ASN-0047 admissibility instead of re-discharging its invariant package — is derivable from the ASN's own existing citations (O14, K.μ~ in ASN-0047); the load-bearing fact (`{d₁}` vs `{d₃}` incomparability) is already present in the text.

## Issue 2: Worked example over-verifies near-trivial claims
Reason: Internal fix. Deciding which demonstrations are load-bearing (multi-origin, O13, O14, one wp) versus trivial corollary recaps is a pruning judgment fully determined by the claims already stated in the ASN; no design intent or implementation evidence is required.
