# Channel Assignment — ASN-0077 review-75

**Date:** 2026-06-04 14:59

## Issue 1: Proof-plan narration inside the O2 derivation
Reason: Pure prose deletion of a planning sentence; the M-int/S3★ steps already execute the discharge, so the fix is internal to the existing derivation.

## Issue 2: Non-consumer inventory in O3
Reason: Trimming a non-consumer enumeration and a "supplied but not consulted" parenthetical; the positive claim ("computed from the address value alone") is already present, so the fix is internal.

## Issue 3: Exclusions duplicate the Open Questions
Reason: Editorial deduplication between two sections of the same note; deciding where the boundary statement lives requires only the ASN's own content, so the fix is internal.
