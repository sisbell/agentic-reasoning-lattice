# Channel Assignment — ASN-0036 review-172

**Date:** 2026-05-29 05:27

## Issue 1: `#v ≥ 2` / `m ≥ 2` is attributed to S8a in multiple places, but S8a disclaims it
Reason: Internal citation contradiction resolvable from the ASN alone — the domain-restriction axiom and S8a's own text already fix where the `#v ≥ 2` bound originates; the fix is choosing to widen S8a or reroute the five citations, both of which are derivable from existing content.

## Issue 2: S8a's routing note is use-site meta-prose
Reason: The note is editorial prose internal to the ASN; removing it (and optionally restating the bound in the postcondition) requires no design intent or implementation evidence.
