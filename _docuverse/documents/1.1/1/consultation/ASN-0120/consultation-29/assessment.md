# Channel Assignment — ASN-0120 review-29

**Date:** 2026-06-11 06:10

## Issue 1: The operation's contract is not consolidated into the claims — the seating effect, the `m = 2` convention, and `enabled` exist only in prose
Reason: All three missing pieces — the seating effect with both `v_a` branches, the `m = 2` convention, and the `enabled` expansion — are already fully stated and derived in the ASN's own prose; the fix is purely editorial consolidation into the claims table (a new operation-definition claim or folding into ML0/ML9/ML10). No design-intent or implementation evidence is needed.
