# Channel Assignment — ASN-0120 review-10

**Date:** 2026-06-09 11:27

## Issue 1: ML6 defends against a case its own precondition excludes
Reason: Pure prose surgery — the fix removes a negative L9 rebuttal and retains the L8 address-matching content already present in the ASN. No design intent or implementation evidence is needed.

## Issue 2: Defended omission in the wp `enabled` predicate
Reason: Internal — the well-formed spec-set definition already in the ASN establishes that sources are allocated; the fix only restates that as an argument-type property and drops the negative-space defense.

## Issue 3: Use-site inventory and non-use justifications around the resolution proof
Reason: Internal — the `#E = 2` derivation and S3★ containment step are self-contained in the ASN; the fix drops the inventory and the lemma-declination parenthetical without needing external input.

## Issue 4: Editorializing inside the implementation notes
Reason: Internal — the CREATELINK factual behavior (empty type sporgl passing the guards) is already recorded and retained; only the evaluative closing sentences are removed, requiring no fresh evidence from Gregory.
