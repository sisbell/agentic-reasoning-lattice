# Channel Assignment — ASN-0120 review-23

**Date:** 2026-06-11 05:24

## Issue 1: The ρ = ∅ boundary is settled twice, with twin deferrals to the same Open Question
Reason: Pure structural deduplication — both passages state facts already derived in the ASN (recovery equation forces `e_j = ∅`, L3-legality, ML9 inertness), and the Nelson LM 4/48 slot-convention material is already quoted in the document. The fix is consolidation and re-citation, requiring no new design intent or implementation evidence.

## Issue 2: The "equivalently" gloss on the ordinal-displacement precondition is false as stated
Reason: A mathematical error fixable from the ASN's own machinery — the review supplies the counterexample and the correct equivalence (`#ℓ_j = #u_j ∧ actionPoint(ℓ_j) = #u_j`), and the normative `wf` clause is already correct in the ASN. Internal.

## Issue 3: ML7's parenthetical hedges against the invariant it just proved
Reason: The fix is derivable from the ASN's own content — L12 already carries the unconditional claim, the EDITLINK scope exclusion already exists, and the review gives an acceptable replacement formulation (link retirement as a model extension outside the present transition vocabulary). No design-intent ruling on link deletion is needed to delete or recast a hedge.

## Issue 4: ML4's paragraph states the no-coupling fact three times in two sentences
Reason: Sentence-level deduplication with no semantic change — the formal non-coupling statement and its consequence are both already present in the ASN; the fix only removes repetition. Internal.
