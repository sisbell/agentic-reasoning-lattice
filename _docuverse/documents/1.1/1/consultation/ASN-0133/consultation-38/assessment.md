# Channel Assignment — ASN-0133 review-38

**Date:** 2026-06-14 12:01

## Issue 1: Worked composition re-asserts the heterogeneous-PL result for a case the example excludes
Reason: Purely editorial deletion — the required fix is to end the claim at "(Q1)" and drop a counterfactual clause that defers to Q0's own worked illustration. The single-view registry's `quiescent_R ∈ PL` already rests on Q0 directly within the ASN, and the heterogeneous result being deferred to is material Q0 already owns and demonstrates. Nothing about design intent or implementation behavior is at stake.

## Issue 2: H-SFAIR's regime-form derivation is wrapped in use-site narration
Reason: Purely editorial — the substantive regime-form derivation (Q-EXT caps real fires at one, consequent unsatisfiable, implication collapses to "no `(ρ, x)` trigger-true at infinitely many indices") is already present and correct in the text; the fix only strips the "in exactly one role" / "is the content Q6 consumes" framing and lets Q6 cite the regime form where it uses it. No external evidence is needed.
