# Channel Assignment — ASN-0133 review-49

**Date:** 2026-06-14 16:58

## Issue 1: "regime (i)" is defined three non-equivalent ways, and the proof establishes only the strongest
Reason: Internal — the review supplies the reconciling definition (footprint-relevant state eventually constant), and every concept it relies on is already deployed in the note: FP/footprint reads, the content (K.α) invisibility to triggers via V-DOC/QD-audit, the constant-tail argument, and the turn-fairness condition from H-SFAIR. Rewriting the proof and constructing the turn-fair footprint-perturbing example are recombinations of present content, requiring neither design intent nor implementation evidence.

## Issue 2: Q3 carries a scope-justifying aside about the *other* half of at-most-once
Reason: Internal — pure deletion of a digressive aside; whether to relocate the SF/extinction split-of-labor to Q-EXT is an editorial choice within the note's own structure.

## Issue 3: "Worked composition" states the internal/external divergence split three times
Reason: Internal — pure deduplication of three restatements of an argument (type-isolation closes internal divergence, unbounded flagging survives) the note already makes; no external input needed to choose which sentence to keep.

## Issue 4: the H-W dismissal digresses into stutter-unavailability
Reason: Internal — pure compression to the core sentence the review itself supplies, using facts already in the note (V-DOC content-invisibility, the per-σ vs. registry-level `|W(σ)|` distinction).
