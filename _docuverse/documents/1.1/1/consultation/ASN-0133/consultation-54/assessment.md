# Channel Assignment — ASN-0133 review-54

**Date:** 2026-06-14 19:25

## Issue 1: Regime (ii) attributes the structural fire bound to "all-SF," which Q5a's own text refutes
Reason: Internal — the corrective text is already in the ASN. Q5a explicitly states "SF alone does not bound the count" and supplies the counterexample, so the fix is to align the regime (ii) and package-list phrasing with Q5a's own attribution (full package: all-SF + extinction discipline + bounded growth). No design intent or implementation evidence is at stake; this is a consistency edit against the note's existing claims.

## Issue 2: Residual meta-prose — deferred-layer inventories and re-explained concepts
Reason: Internal — all three sub-edits are deletions of decorative or redundant prose ((a) the scheduler three-name list, (b) the naive-merge restatement already given verbatim in Q0, (c) the actor enumeration). What stays load-bearing ("any fair discipline," "non-registry actors emit through the same surface") is already present, so nothing turns on design intent or implementation behavior.
