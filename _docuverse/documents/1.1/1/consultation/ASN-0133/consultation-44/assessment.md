# Channel Assignment — ASN-0133 review-44

**Date:** 2026-06-14 14:39

## Issue 1: H-W collapses to perpetual quiescence, so its apparatus and "implies quiescence directly" are near-vacuous
Reason: Internal. The collapse is provable from the note's own definitions alone — `W(σ)` (the trigger-true triple set), the reflexivity of `→_sh*` (the environment-stutter freeze), and Q5's per-σ injection — so stating the equivalence and compressing the apparatus needs neither design intent nor implementation evidence.

## Issue 2: The worked example's registry-side summary drops the bounded-direct-comment-traffic condition
Reason: Internal. The correct condition is already written verbatim in the note's own *Bound* paragraph ("and any direct environment comment traffic"); the fix only propagates that clause into the *Quiescence* summary sentence — a pure consistency repair grounded in `L_cmt`'s stated growth equation.

## Issue 3: Meta-prose accretion (anti-bloat)
Reason: Internal. Deleting the flourish, the defensive aside, the reader-instruction, and the duplicate registry-side restatement is editorial cleanup against the note's own `review-mode.anti-bloat` discipline; no external knowledge is involved.
