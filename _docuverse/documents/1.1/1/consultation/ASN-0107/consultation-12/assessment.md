# Channel Assignment — ASN-0107 review-12

**Date:** 2026-06-08 10:36

## Issue 1: A2 states its conclusion twice and carries a defensive parenthetical
Reason: Purely editorial — collapsing a duplicated bound and deleting a parenthetical that re-explains the slot/subspace distinction already fixed by the `sat` definition and ASN-0047. No design intent or implementation evidence is at stake.

## Issue 2: Claims-table entries are essays in a structural slot
Reason: Internal — condensing table cells to one line each is a presentation fix; the qualifications already live in the body, so no external channel is needed.

## Issue 3: `sat` section repeats "no part need be constrained"
Reason: Internal — consolidating restated totality/legitimacy claims and dropping the defensive Nelson-quote justification is editorial; the `T`/`∅`/`num=0` facts are all already established in the section.

## Issue 4: R1 is over-provisioned with "load-bearing because" prose and a redundant closing
Reason: Internal — removing the duplicated opening/closing and the per-proviso "load-bearing because" excursions is a structural trim; R2 and R5 already carry the displaced reasoning, and the "no per-link delete" fact is stated elsewhere in the ASN.

## Issue 5: The D2 reordering biconditional is a tautology presented as a result
Reason: Internal — the biconditional is the definitional expansion of `Qᵢ(Σ')=Qᵢ(Σ)`, so deleting it and leading with the sufficient condition plus the content-sharing counterexample is derivable from the section's own content.
