# Channel Assignment — ASN-0107 review-33

**Date:** 2026-06-08 13:13

## Issue 1: "Insertion shifts the following positions" contradicts the note's own extension semantics
Reason: Internal fix. The conflict is resolved entirely by the note's own apparatus — K.μ⁺ preserves prior images (LP9/ASN-0047), only K.μ~ moves a fixed position's image (D2's reordering clause), and the note already states a mid-stream insert decomposes into extension plus reordering. Re-attribution to reordering follows from definitions already present.

## Issue 2: R2's definition of `D` overstates under intra-document content sharing
Reason: Internal fix. R1's (P-uniq) clause already gives the correct treatment for shared I-addresses; redefining `D` as the addresses with no surviving consulted preimage in slot `i` mirrors that existing pattern, so the correction is derivable from the ASN's own content.
