# Channel Assignment — ASN-0107 review-17

**Date:** 2026-06-08 11:14

## Issue 1: A1 conflates the two anchorings under one conditional header
Reason: Internal. The split is purely a restructuring of existing content — the existence half is a corollary of E3 (already present) and the discovery half rests on the no-incoming-links premise (already stated in A1). No design intent or implementation evidence is needed.

## Issue 2: Reconciliation recap paragraph does not advance reasoning
Reason: Internal. The required action is deletion/compression to the single load-bearing sentence already implied by E1–E4 and D1–D2; no new design or evidence claim is being introduced.

## Issue 3: A2 defensive aside imagines a move the definition already excludes
Reason: Internal. The `Qᵢ(Σ)` resolution definition in the ASN structurally forecloses passing `T` under discovery anchoring; removing the rebuttal prose requires only the ASN's own definition.

## Issue 4: Reordering necessity/sufficiency stated twice
Reason: Internal. The deduplication keeps the principle in D2 and strips the worked example's re-derivation — both passages already exist in the ASN and the K.μ~ bijection / shared-image facts are cited from ASN-0047/M13.

## Issue 5: Implementation mechanics in an abstract-guarantee slot
Reason: Internal. Dropping the enumeration sentence leaves the abstract invariant P1 (`[sat] ∈ {0,1}`) intact; the cut is editorial and needs no implementation evidence to justify since P1 itself carries the guarantee.
