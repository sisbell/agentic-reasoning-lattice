# Channel Assignment — ASN-0107 review-7

**Date:** 2026-06-07 22:23

## Issue 1: D2 reordering states an incorrect necessity ("only when π fixes Wᵢ setwise")
Reason: Fix is internal. The counterexample, the resolution definition `Qᵢ(Σ)`, and the content-sharing premise (M13/S5, already cited in R1) are all present in the ASN; restating sufficiency or giving the exact image-equality characterization is pure logic over those definitions.

## Issue 2: A2's `Q₂ = Q₃ = T` special case is incompatible with the discovery anchoring it is stated under
Reason: Fix is internal. The discovery-resolution definition and Nelson's "widen-to-universal-span" design note (LM 4/38, already quoted in §State) are in-ASN; correcting A2 to let `W₂, W₃` range over whole subspaces — which resolve to address sets, not `T` — is a definitional adjustment derivable from material already present.
