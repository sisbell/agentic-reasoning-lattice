# Revision Categorization — ASN-0084 review-13

**Date:** 2026-04-10 12:13



## Issue 1: PermutationDisplacement domain does not cover R-DISP's exterior case
Category: INTERNAL
Reason: The fix is fully derivable from the ASN's own content — the permutation π is already defined on all of dom(M(d)) with π(v) = v for exterior positions, so extending Δ(v) = ord(π(v)) − ord(v) to all of dom(M(d)) is a straightforward notational correction requiring no external evidence.
