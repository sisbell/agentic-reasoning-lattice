# Channel Assignment — ASN-0115 review-28

**Date:** 2026-06-09 23:08

## Issue 1: Implementation-hazard aside in R10 imagines a case the operation's carrier already excludes
Reason: Pure deletion of a self-labeled non-claim. The `act`/`item` definitions already resolve every position through `Σ.M(d)`, and S3★ already fixes the subspace dispatch, so the excluded misuse path and its discharge are both internal to the ASN — no design intent or implementation evidence is needed to remove the aside.

## Issue 2: R9's normative claim box carries a forward-reference to an Open Question and a redundant restatement
Reason: Editorial trim of redundancy within the claim box. The kind-asymmetry invariant is already stated two clauses earlier in the same box, and OQ1 already records the deferred inline-provenance question — both the "Co-assembly thus…" restatement and the OQ1 forward-reference are removable from the ASN's own content alone.
