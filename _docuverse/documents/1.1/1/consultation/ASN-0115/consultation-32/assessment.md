# Channel Assignment — ASN-0115 review-32

**Date:** 2026-06-10 00:02

## Issue 1: R8's "TransclusionRevelation" claims an output-level disclosure the operation does not provide
Reason: The contradiction is wholly internal — R8's "revelation/made manifest/ascertainably one content" prose conflicts with R0 (co-delivery is mere concatenation), R1 (content items carry values, not addresses), S4 (value-equality ≠ address-identity), and R9 (content origin is not output-recoverable), all already present and treated as correct in the ASN; the "returns material, not location" intent backing R1 is itself already Nelson-grounded within the ASN (4/67, 4/63, 4/68), so aligning R8 with that settled content needs no new consultation.

## Issue 2: The inline Confinement lemma re-proves a foundation result (ASN-0058 C0a) without relating to it
Reason: The fix is a cross-reference within the spec corpus — citing ASN-0058 C0a (whose exact statement and `V_{u₁}(d_s) ≠ ∅` precondition the review already supplies, and which the ASN already cites for the depth discipline) and reframing the inline lemma as its extension to the `V_S(d) = ∅` case; this is a formal relating of two ASN results, requiring neither design intent (Nelson) nor implementation evidence (Gregory).
