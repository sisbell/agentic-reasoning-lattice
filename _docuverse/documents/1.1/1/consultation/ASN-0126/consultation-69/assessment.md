# Channel Assignment — ASN-0126 review-69

**Date:** 2026-06-09 21:45

## Issue 1: "Single-source" forward-references the note's entire core machinery; the projection bridge serves four sites but is stated after two of them
Reason: Purely expository restructuring — hoisting the projection bridge to a named lemma before its first use and reordering the shape catalog / `→_sh` / bridge ahead of Single-source (or splitting Single-source). Every construct involved (the bridge, `→_sh`/`K.λ_sh`, the shapes, the R-Scope frame argument) is already present and proven in the note; no design intent or implementation evidence is at stake, only the order in which existing material is presented.
