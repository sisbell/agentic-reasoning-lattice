# Channel Assignment — ASN-0126 review-41

**Date:** 2026-06-09 10:50

## Issue 1: `[r]` notation is ill-typed and used before definition
Reason: Pure notational fix — the `[·]` convention and the span/tumbler distinction are both defined within the note; rewriting `[r]` as `{r}` and relocating the definition is derivable from the ASN alone.

## Issue 2: domain-discharge ordering explained three times
Reason: De-duplication of the note's own meta-prose; stating the partiality argument once at `K.λ_sh` and citing it downstream requires only internal editing.

## Issue 3: Single-source retraction prose imagines an excluded case and defers downstream
Reason: Cutting the unregistered-R digression and consolidating repeated `|F| = 1` statements is internal; the gate's precondition (i) already covers the excluded case and OQ4 already holds the deferred content.

## Issue 4: gate-vs-landing separation explained redundantly
Reason: Removing the two intermediate explanatory restatements while keeping the wp statement and the Worked illustration demonstration is a structural edit derivable from the note's existing content.
