# Channel Assignment — ASN-0094 review-56

**Date:** 2026-05-24 02:52

## Issue 1: NAT-sub appendix's predecessor existence cited but not derived
Reason: The gap is purely about logical derivation from the listed NAT axioms plus Peano supplements. The fix is either deriving predecessor existence from NAT-wellorder + (Peano-zero-least) via strong induction on `P(m) := m = 0 ∨ ∃m'. m'+1 = m`, or adding (Peano-pred) as a third Peano-core clause — both are internal to the appendix's own discipline.

## Issue 2: Per-shape uniformity downgrade leaves a load-bearing gap unaddressed
Reason: This is an internal coherence issue between Sh5(a)'s downgrade-to-aspiration and Sh5(b)'s/the catalog rows' continuing "inherit" language. The framework has already chosen the downgrade; the fix is either sharpening to a procedural recipe or revising consumer-side prose to match — both are internal META-discipline edits.

## Issue 3: AllocatedAddressAntichain's Sub-case 3b worked example exhibits a configuration forbidden by R0a-Cor2
Reason: The lemma is correct and the inconsistency is purely pedagogical — the example's `#E(a') = 3` violates R0a-Cor2's `#E = 2` strengthening. The fix is either constructing a Sub-case 3b example consistent with R0a-Cor2 (or noting that R0a-Cor2 closes 3b vacuously by length-counting) or prepending a clear L1b-vs-R0a-Cor2 caveat — both derivable from the lemma and R0a-Cor2 already in scope.

## Issue 4: Sh5(b)'s exhaustiveness claim is universally framed but only checked per-row
Reason: This is a framing inconsistency between Sh5(b)'s universal "no implicit residual" phrasing and its actual META commitment (per-row audit checklist). The fix is to reframe Sh5(b)'s prose to match its committed META status — internal prose edit, no external evidence needed.
