# Channel Assignment — ASN-0094 review-70

**Date:** 2026-05-24 20:15

```
## Issue 1: Missing "approved_by" walkthrough promised by Resolution catalog row
Reason: The fix is purely internal — either add the promised walkthrough exercising existing Resolution-shape templates at a new K registration, or rewrite the catalog row's parenthetical. No external evidence needed; the Resolution shape, base templates, and standalone-admissibility argument are all present in the ASN.
```

```
## Issue 2: Undefined "Sh5(b) per-row review checklist (steps 0–3)"
Reason: Internal editorial issue. The checklist must be defined within Sh5(b) using prose already present (citation convention, signature derivation rule), or the references rewritten to point to what Sh5(b) actually contains. No external channels needed.
```

```
## Issue 3: Per-K discipline (FDD, SHCD) registration scope ambiguous across ~-classes
Reason: The ASN's own definitions force the fix: per-class shape constancy combined with `L_K = L_{K'}` when `K ~ K'` entails that mixed per-K registration within a ~-class would let unprotected `Emit_{K'}` calls bypass the discipline gate while contributing to the same active slice, breaking the preservation theorems. Per-~-class consistency is the only viable resolution, derivable from the ASN's own logic.
```

```
## Issue 4: "Sh5(b) is a hand-followed convention" referenced as a labeled section that does not exist
Reason: Pure documentation issue — either label the relevant Sh5(b) content with the cross-reference name or remove the reference. No external knowledge required.
```

```
## Issue 5: Prose duplication on "per-shape uniformity" across catalog rows
Reason: Editorial consolidation only — move the convention's full statement into Sh5(a) and replace each catalog row's restatement with a brief deferral. No external input needed.
```

```
## Issue 6: Sh4 contract has explicitly expository paragraph not consumed by any proof
Reason: Editorial cleanup — the paragraph self-declares non-consumption. Remove it; AllocatedAddressAntichain is independently stated and citable if needed elsewhere. No external channels required.
```

```
## Issue 7: LinkAddressNotPrefixOfEmit proof contains meta-subsections commenting on proof structure
Reason: Editorial restructuring of the proof body — collapse meta-subsections (Case-symmetry, Dependence audit) into terse symmetry citations and either remove or relocate the worked-example illustration. The proof's logical content is unchanged; no external input needed.
```

```
## Issue 8: Empty-baseline assumption stated as required for Sh4/FDD/SHCD but not surfaced as a scope boundary
Reason: The fix is an editorial decision — add a scope-boundary item to Open Questions reflecting what the Initial-State Baseline section already states. The substantive content (empty-baseline required for Sh4/FDD/SHCD; vacuous-baseline relaxation for Sh0–Sh3) is already in the ASN; surfacing it as a tagged scope boundary uses existing material.
```
