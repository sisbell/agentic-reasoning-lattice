# Channel Assignment — ASN-0094 review-9

**Date:** 2026-05-19 22:34

## Issue 1: Sh3 proof violates "no proof by similarly" standard
Reason: Pure proof restatement using content already present in Sh2. The structure (Case A monotone preservation, Case B Sh-conf clause discharge) is fully derivable from the ASN's own Sh2 proof with mechanical F→G substitution.

## Issue 2: Direct cross-ASN references to ASN-0093 (non-foundation)
Reason: Reframing existing references through ASN-0086's already-cited substrate-conforming-layer interface. The interface and its catalog (a) are present in the ASN's foundation citations; no new content needed.

## Issue 3: Sh-conf "iff" formulation potentially misleading
Reason: Rewording for clarity. The full conjunction is already written explicitly in the wp_eff formulation below the "iff" line; the fix is to surface it at the headline statement.

## Issue 4: Retraction-shape enforcement of unit-depth discipline not exhibited
Reason: Connection is derivable from definitions present in the ASN — canonical-slot form (defined here) combined with Retraction's `c_G = 1` (in the catalog) directly forces single unit-depth spans matching ASN-0086's unit-depth retraction discipline.

## Issue 5: SharedDepthOneAllocator lemma has no downstream consumer
Reason: Decision about lemma status is determined by inspecting the ASN's own proofs for citations. The Scope section's content-side scaffolding already supplies the load-bearing facts via ASN-0086's interface, so demotion or deletion is internally justified.

## Issue 6: Properties Introduced table contains internal-history annotation
Reason: Pure cleanup — strip a parenthetical with no semantic content. Trivially internal.

## Issue 7: `subspace_I(a) = E(a).1` connection implicit at AllocatedAddressAntichain Step 3.3
Reason: Bridge follows from T4b's E-field structure and T7 as cited from ASN-0034/ASN-0043 (already in the foundation set). The equivalence is derivable from the foundation references the ASN already invokes.
