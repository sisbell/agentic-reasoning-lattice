# Channel Assignment — ASN-0087 review-70

**Date:** 2026-06-04 03:22

## Issue 1: `dom(Σ'.M) = dom(Σ.M)` equality re-derived in four places
Reason: This is a pure prose-deduplication fix — consolidate the equality derivation to one site and have the other three cite it, and drop a cross-foundation provenance clause. All the relevant facts (M1's inclusion, K.λ's frame, K.μ⁺_L's effect, `dom(M) = E_doc`) are already present in the ASN; no design intent or implementation evidence is needed.
