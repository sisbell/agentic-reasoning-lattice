# Channel Assignment — ASN-0087 review-66

**Date:** 2026-06-04 02:57

## Issue 1: M1 dual-attribution prose is citation bookkeeping, not reasoning
Reason: Pure prose-reduction task. The single load-bearing fact (`dom(M) = E_doc` from ASN-0047's M1) is already present in the ASN; the fix only trims the citation-convention meta-prose and the duplicate attribution in wp Case 2. Derivable from the ASN's own content.

## Issue 2: L1c discharge conflates `A_L(d)` with L1c's chain-from-`d`
Reason: The clean discharge (option a) relies on a fact ASN-0087 already cites — in *Atomicity*, "K.λ is an atomic substrate operation that ASN-0093/ASN-0047 establish to preserve the per-state invariants on reachable states." Since L1c is a per-state link-store invariant, "ℓ enters `dom(L)` via K.λ, which preserves L1c, so L1c holds at Σ'" is derivable from reasoning already present. No external channel needed.
