# Channel Assignment — ASN-0087 review-65

**Date:** 2026-06-04 02:48

## Issue 1: Inconsistent foundation attribution for M1 (ArrangementMonotonicity)
Reason: Internal — the fix is a citation-consistency edit. The ASN already names both foundations (ASN-0047 carries the `dom(M) = E_doc` identity, ASN-0093 the bare inclusion); reconciling them into one canonical citation requires only the note's own cross-references, no design intent or implementation evidence.

## Issue 2: The v_ℓ positioning rule is restated in full across multiple sections
Reason: Internal — pure anti-bloat deduplication. Collapsing the Effect/M-Effect verbatim restatement into one statement plus a pointer is an editorial move fully derivable from the ASN's existing content.

## Issue 3: Reflexive worked variant does not flag that it exercises non-standard authoring
Reason: Internal — the ASN already proves (M-FreshExcl, M-Reflexive) that standard authoring excludes the reflexive route, so the required one-sentence flag is derivable from content already present.
