# Channel Assignment — ASN-0134 review-53

**Date:** 2026-06-14 19:07

## Issue 1: Use-site forward-pointers pre-announcing where a claim gets consumed
Reason: Internal — this is a structural edit (deleting upstream "W1 will classify this" / "§4 leans on this" pre-announcements and leaving the downstream citation direction intact). No design intent or implementation evidence bears on where the note announces its own consumption; the fix is pure reasoning-flow housekeeping derivable from the ASN's own structure.

## Issue 2: V2's "global, not per-home / writer-side vs reader-side" point is stated twice before it is used
Reason: Internal — the scope/duality assertion already exists in the note; the fix is to state it once (trim it from V2's body or from the following paragraph). This is redundancy removal, decidable entirely from the note's own text, with no dependence on Nelson's intent or Gregory's code.

## Issue 3: H3(b)'s commutation attributes a case hypothesis to the wrong premise
Reason: Internal — the corrected attribution (case (b)'s distinctness is its own case hypothesis `d'_new ≠ d_new`; §4's conditional instead produces the *colliding* same-account pair routed to the `≺`-comparable account-tier branch) is already fully stated in §4 and H3. The fix is a logical-attribution correction the reviewer has already pinned down from the note's own content.
