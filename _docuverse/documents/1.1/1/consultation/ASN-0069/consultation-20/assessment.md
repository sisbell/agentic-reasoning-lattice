# Channel Assignment — ASN-0069 review-20

**Date:** 2026-05-25 18:27

## Issue 1: V6 incorrectly listed as "vacuous" in the empty-source case
Reason: Internal fix. The ASN's own worked example (link-only vignette) already articulates the correct substantive justification — V6 holds via K.δ's `M'(d_new) = ∅` initialization. Reclassification is mechanical reconciliation between two sections of the same ASN; no external evidence or design intent needed.

## Issue 2: V5 miscited for entity preservation in empty-source vignette
Reason: Internal fix. V5's statement (`M'(d_src) = M(d_src)`) and the correct basis (K.δ's E-frame `E¹ = E ∪ {d_new}` plus P1 EntityPermanence from ASN-0047) are both already established within the ASN and its cited foundation. The fix is replacing one citation with the correct one.
