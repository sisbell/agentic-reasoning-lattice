# Channel Assignment — ASN-0069 review-53

**Date:** 2026-06-02 14:45

## Issue 1: Citations to ASN-0047/0034 claims under invented or non-existent names
Reason: The fix is internal — it is a citation-name reconciliation against the foundation ASNs (0034/0036/0047) themselves, which the reviewer has already enumerated (correct names: `SubAllocatorBundle`/K.δ activation effect, `NodeBaptism`, `K.δ-ID.zeros-0/1`, `K.δ-ID.parent-0/1`, `T4-valid`, `Document`/`Node`/`Element`, `ContentSubspaceRestriction`, `S3★`). Resolving it requires neither Nelson's design intent nor Gregory's implementation evidence — only matching handles to the foundation's actual claim names, with one branch (whether `SubAllocatorAxiom`/`NodeUniqueAllocation` named a real guarantee or a gap) resolvable by inspecting ASN-0047's claim set.
