# Channel Assignment — ASN-0069 review-65

**Date:** 2026-06-02 22:45

## Issue 1: Use-site inventory enumerating downstream consumers of `d_op`
Reason: Pure editorial trim — replacing a label manifest with the operative rule. The rule itself ("content-inheritance/correspondence claims use `d_op`, identity/ancestry/isolation use `d_src`") is already stated in the same sentence; no design intent or implementation evidence is needed.

## Issue 2: Use-site inventory in V5a justification
Reason: Editorial trim — dropping a consumer list (V10(b), V11) while keeping the self-standing generality claim. The lemma's per-document/per-sequence framing over the arrangement-modifying vocabulary is already present in the ASN; no external channel needed.

## Issue 3: Forward-deferral paragraph after V1/V2
Reason: Editorial collapse of stacked forward pointers into one sentence. The substantive content (V1/V2 are identity/ancestry only) is already in the ASN, and the deferred topics are all developed later in the same document; derivable internally.

## Issue 4: V4-strengthens-J4 stated twice
Reason: De-duplication — reducing a pre-proving forward reference to a bare pointer, keeping the full φ-identity deviation account at V4. Both statements already exist in the ASN; resolving the redundancy requires only the ASN's own text.
