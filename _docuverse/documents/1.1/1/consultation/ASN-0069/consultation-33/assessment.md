# Channel Assignment — ASN-0069 review-33

**Date:** 2026-05-27 13:49

## Issue 1: V11a's recovery sub-claim is over-narrow and undischarged
Reason: The fix is internal — derivable from the ASN's own content. V1 already defines both fork sub-cases (`inc(·, 1)` and `inc(d_prev, 0)`), V2's derivation already handles both via TA5(b)/(c)/(d), and the choice between scoping V11 to first-fork chains versus generalizing the recovery wording is an authorial decision about V11's intended scope — not a question of design intent or implementation evidence.
