# Channel Assignment — ASN-0069 review-77

**Date:** 2026-06-03 00:16

## Issue 1: Transitive correspondence across the version (sibling) stream is left underived, while the structurally identical chain-fork case (V11) is fully derived
Reason: The fix reuses machinery already present in the ASN — V8 adjacent correspondence, V1's `inc(·,0)` subsequent-fork sub-case, and the per-step-unedited premise — composed exactly as V11 does for chains; Nelson's intercomparison intent is already established by the cited LM 2/20 quote, so no channel is needed.

## Issue 2: V5a re-derives a transition-vocabulary frame theorem that exceeds what the fork operation needs
Reason: Reducing V5a to its two consumed corollaries via one-line composition of ASN-0047's per-transition frame conditions uses frames the ASN already quotes (K.δ, K.μ⁺, K.ρ); this is an internal scoping/editorial fix.

## Issue 3: Meta-prose around claim statements that justifies framing rather than advancing reasoning
Reason: Pure editorial deletion of rationale and exhaustiveness asides whose object-level content is already carried by V11's formula and V5a's clauses; derivable from the ASN alone.
