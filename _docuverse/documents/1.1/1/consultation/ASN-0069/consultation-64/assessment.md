# Channel Assignment — ASN-0069 review-64

**Date:** 2026-06-02 22:37

## Issue 1: V11a carries subsequent-fork machinery (j ≥ 1) that V11's chain premise excludes
Reason: The fix is internal — it reconciles V11a's `1 + j` characterization against V11's own first-fork premise, both stated within the ASN. Choosing between restricting to value `1` or detaching as an independent lemma needs only the ASN's existing V1/V11 definitions.

## Issue 2: The R' set-equality verification is stated three times
Reason: Pure deduplication of a derivation the ASN already gives; consolidating to one location and citing it elsewhere requires no external input.

## Issue 3: V5a wraps the lemma in use-site inventory and meta-commentary
Reason: Deleting downstream-consumer enumeration and "not a property of the fork operation" editorializing is an internal trim; the lemma content and corollaries are unchanged.

## Issue 4: V11's two "Remark on premise scope" paragraphs defend cases the premise already excludes
Reason: Collapsing defensive prose about out-of-scope edits is internal — the premise scoping and the V5a Cor. 2 / anchoring discharges are already in the ASN.

## Issue 5: V0 "Composite structure" clause is a forward/backward cross-reference essay
Reason: Removing the "is what permits V4 and V5" inventory and V11-analogue sentence while keeping the uninterrupted-sequence requirement is purely an internal editorial cut.

## Issue 6: Dependency Audit contains self-referential citation-practice prose
Reason: Reducing to the ASN-0040-removal finding is internal; the dependency conclusion and the V11a length by-product note are already established in the ASN.

## Issue 7: V9 is derived from a coupling constraint rather than the step that discharges it
Reason: Re-pointing the derivation at the K.ρ × n cumulative effect (already verified in "The Fork Composite") and noting J1★ is discharged by V9 is an internal reordering using machinery the ASN already contains.
