# Channel Assignment — ASN-0075 review-60

**Date:** 2026-06-03 10:03

## Issue 1: P4★/P4a boundary-availability caveat restated at every use site
Reason: Pure exposition fix — D-BOUND already establishes boundary availability once; deleting the repeated caveats and the D-DISJ preview is mechanical and derivable from the ASN's own structure. No channel needed.

## Issue 2: "Documents with no shared content" edge case is a pure back-reference
Reason: Editorial deduplication — the entry only relabels D-DISJ's hypothesis and points back to existing text; reducing it to a one-liner needs nothing beyond the ASN itself.

## Issue 3: D-NEED meta-bridge to D-DISCR
Reason: Removing reviser meta-prose; the substantive scope point is already stated by the adjacent sentence, so the fix is internal.

## Issue 4: Redundant "content-subspace" qualifier on R-projections
Reason: The ASN already establishes `R ⊆ dom(C) × E_doc` (P7) and `dom(C)` is wholly `s_C`, so dropping the qualifier follows from content already present. No channel needed.
