# Channel Assignment — ASN-0075 review-27

**Date:** 2026-06-03 00:25

## Issue 1: Claims table overstates D-EXH's domain
Reason: Internal fix — the lemma body already restricts to composite-boundary states and D-BOUND makes this the contract; the table just needs to be brought into agreement with text already present in the ASN.

## Issue 2: `subspace_I` misattributed to a nonexistent ASN-0036 claim
Reason: Internal fix — a pure citation correction. The Foundation Recap already cites the SubspaceConventionAxiom (ASN-0047) for the `s_C=1, s_L=2` convention, so the correct home for `subspace_I` is derivable from the ASN itself.

## Issue 3: Two foundation citations name claims that do not exist under those names
Reason: Internal fix — both underlying facts are already used correctly; only the foundation labels need correcting to `SubAllocatorBundle` and `J0 (AllocationPlacementCoupling)`, derivable from the ASN's own usage.

## Issue 4: D-SUBSP asserts a "structural necessity" without the chain it elsewhere supplies
Reason: Internal fix — the required chain (CL-OWN, L14, S3★ content clause) uses only claims already cited and spelled out in D-EXH; assembling it for the link case is derivable from the ASN's existing content.
