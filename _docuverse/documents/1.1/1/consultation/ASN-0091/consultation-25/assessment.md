# Channel Assignment — ASN-0091 review-25

**Date:** 2026-05-26 21:04

## Issue 1: R-SP hypothesis fails in unified state when link subspace is populated
Reason: The fix is derivable from the ASN's own content — the worked examples already verify each load-bearing invariant directly without R-SP, so the repair is to extract that per-invariant discharge into the abstract section. No designer intent or implementation evidence is needed; the logical structure of which ASN-0036 invariants survive supersession and which depend on legacy S3 is already laid out in the ASN.

## Issue 2: S8★ content-subspace clause incorrectly cites "R-SP's discharge of ASN-0036's S8"
Reason: Pure internal consistency repair — the worked example already supplies the operative argument ("direct application of ASN-0036's S8 to the restricted arrangement"), and the ASN itself states R-SP's S8 is not load-bearing. Removing the incorrect parenthetical is derivable from the ASN alone.

## Issue 3: RE-trans single-step claim's "origin(a)'s arrangement unchanged" fails for a case the ASN admits
Reason: Pure logical conditioning fix. The multi-step RE-trans★ already correctly splits into (i)+(ii) unconditional and (iii) conditional; the same split structure transfers to the single-step claim. No channels needed.

## Issue 4: RE-proj single-step formula's domain is not the quantifier's range
Reason: Type-correctness fix using the π̂ pattern already established in the composition section. The uniform formulation is derivable from the ASN's own multi-step treatment; no external input required.

## Issue 5: "RE-sub is the one consequence" is internally outdated
Reason: Pure cross-reference alignment — the ASN itself introduces RE-ext as a second REARRANGE_K-specific consequence later in the same document. The fix is to align the framing with content already present.
