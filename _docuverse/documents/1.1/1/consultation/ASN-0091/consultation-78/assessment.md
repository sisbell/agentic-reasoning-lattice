# Channel Assignment — ASN-0091 review-78

**Date:** 2026-06-04 03:37

## Issue 1: `d` denotes two different documents — overloaded in the Transclusion section and the claims catalog
Reason: Pure notational disambiguation — rename the transclusion target to `d_view` and reserve `d` for the rearrangement target. The fix is entirely internal: it rewrites symbols and collapses the distinguishing paragraph, requiring no design intent or implementation evidence beyond what the ASN already states.
