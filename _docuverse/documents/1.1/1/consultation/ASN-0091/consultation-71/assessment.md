# Channel Assignment — ASN-0091 review-71

**Date:** 2026-06-04 02:42

## Issue 1: Empty-case discharge is a use-site inventory of downstream claim labels
Reason: The fix is purely editorial — collapse a label roll-call into one statement using facts (empty bijection, R-PRE(iv)/CS2 exclusion) already present in the ASN. No design intent or implementation evidence required.

## Issue 2: Identity-case carries a blanket downstream-coverage sentence
Reason: Deleting a restatement sentence once `Σ' = Σ` is established is internal to the ASN's own reasoning; no external channel is needed.

## Issue 3: Standalone S2 derivation duplicates RA-adm
Reason: Whether S2 is already covered by RA-adm (a per-state foundation invariant) is settled by the ASN's own definitions; the decision to remove or reframe is derivable from internal content alone.
