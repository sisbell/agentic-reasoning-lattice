# Channel Assignment — ASN-0091 review-6

**Date:** 2026-05-26 15:23

## Issue 1: Copy-paste error in 4-cut worked example
Reason: The fix is a mechanical correction using addresses already defined in the 4-cut example setup (`a₁`–`a₄`). The run-extension conditions follow from the chain relationships already stated in the ASN.

## Issue 2: Notational conflation in π non-uniqueness paragraph
Reason: The fix is a notational substitution — replacing `π⁻¹(a)` with `Σ.M(d)⁻¹(a)` for the pre-image of an I-address under the arrangement. The intended meaning is unambiguous from context within the ASN.

## Issue 3: Misleading bullet in "What Rearrangement Is Not"
Reason: The corrected wording must align with RE-proj, which is already stated precisely in the ASN. The fix is internal rewording for consistency with the formal claim.

## Issue 4: Imprecise "unrelated to either" in coalescence witness
Reason: The exclusion set for `c` is determined by what makes the run-cardinality arithmetic in the witness work out (already stated as "cardinality 3" pre-state and "cardinality 2" post-state). The required exclusions derive from the ASN's own run-extension definition.

## Issue 5: Coalescence direction not formally labeled
Reason: The choice of formalization (add RE-coal vs. restate RE-frag as bidirectional non-monotonicity) is editorial. Both witnesses (fragmentation and coalescence) are already exhibited in the ASN; the fix is a labeling/claims-table decision derivable from existing content.
