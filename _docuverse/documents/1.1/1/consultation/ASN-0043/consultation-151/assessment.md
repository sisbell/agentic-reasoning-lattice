# Channel Assignment — ASN-0043 review-151

**Date:** 2026-05-30 23:54

## Issue 1: L1d's siting paragraph is a use-site inventory plus document-ordering justification
Reason: Pure editorial deletion — the siting sentence advances no reasoning and the fix (delete it, state the two parts directly) is fully internal to the ASN. No design intent or implementation evidence is at stake.

## Issue 2: L0b enumerates its own downstream consumers
Reason: Pure editorial deletion of a use-site inventory; the load-bearing content (T4-validity makes projections well-defined) already precedes it. Derivable from the ASN alone.

## Issue 3: The same Gregory evidence is restated verbatim in two sections
Reason: Deduplication of evidence already present and verified in both sections — the fix relocates the existing citation to L6 and trims the StandardTriple to the arity-3 fact. No new implementation evidence is required since both passages cite the same already-confirmed constants.
