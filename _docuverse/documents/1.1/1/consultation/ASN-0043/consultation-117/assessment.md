# Channel Assignment — ASN-0043 review-117

**Date:** 2026-05-30 17:53

## Issue 1: L7's "Structurally." gloss restates the L7 claim in different words
Reason: Pure deletion of a redundant restatement; the fix removes a paragraph that duplicates the L7 statement, derivable from the ASN's own text without design intent or implementation evidence.

## Issue 2: FSP's closing L11a paragraph addresses a property outside the lemma's stated conclusion
Reason: Editorial removal of defensive prose; L11a's derivation already lives in its own section, so the fix is internal — relocating or dropping a pointer requires no external channel.

## Issue 3: L9 "Selection of d'" justifies the construction rather than performing it
Reason: The fix collapses motivational prose into the one-line construction already present (pick `d ∈ dom(Σ.M)`, T4-valid by DocVal); all needed facts are in the ASN, so no channel is required.
