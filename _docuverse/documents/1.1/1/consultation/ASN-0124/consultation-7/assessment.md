# Channel Assignment — ASN-0124 review-7

**Date:** 2026-06-13 08:53

## Issue 1: FD-VDYN(d)'s absorption claim carries no in-note support
Reason: Internal. The required witness is a reachable state built entirely from the note's own apparatus — insertion composites arranging two shared addresses (the FD-LOSSY/FD-NEUT(c) construction pattern), a K.μ~ swap (validity conditions established in the worked illustration), and FD-LOCAL to see `d_x` is unaffected; the swing law `image_C(W, d_q, Σ') = image_C(π⁻¹(W), d_q, Σ)` is already stated in FD-VDYN(d). No design intent or implementation evidence is needed, and the review explicitly excludes rebuilding ASN-0127's machinery.

## Issue 2: FD-ASKER's answer-equality half forward-references FD-V for its entire justification
Reason: Internal. A pure relocation of existing text — the answer-equality consequence is a one-line corollary of `finddocs_V ≡ finddocs ∘ resolve` (FD-V), already in the note; the transclusion identity stays put. No external input required.

## Issue 3: the per-candidate-arrangement-filter requirement is stated in two sections
Reason: Internal. De-duplication only — state the filter conclusion once in the historical-companion paragraph and have deviation #1 cite it. Both passages already exist in the note; nothing new is needed.
