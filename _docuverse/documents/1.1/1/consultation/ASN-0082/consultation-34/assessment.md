# Channel Assignment — ASN-0082 review-34

**Date:** 2026-05-15 12:47

## Issue 1: D-SEQ-post forward citation violates stated lemma ordering
Reason: Internal proof-ordering issue. The ASN already contains the inline cardinality argument (|L ∪ Q₃| = N − c) that establishes finiteness without S8-fin-post, so the fix is derivable from the ASN alone.

## Issue 2: I3-VD proof citation incomplete for non-S subspaces
Reason: Internal citation completeness issue. Both I3-X (forward direction) and I3-CX (reverse direction) are already stated in the ASN; the fix is to cite both to establish the biconditional.

## Issue 3: Statement Registry definition of ordinal-level conflates two conditions
Reason: Internal definitional alignment issue. The body already states the definition correctly (actionPoint(ℓ) = m with level-uniformity as separate context); the fix is to update the registry entry to match.
