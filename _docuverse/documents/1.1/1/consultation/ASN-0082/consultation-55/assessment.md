# Channel Assignment — ASN-0082 review-55

**Date:** 2026-05-30 10:26

## Issue 1: Reviser drift — wp commentary imagines cases the carrier invariant already excludes
Reason: Internal fix. The discharge is restated positively from invariants already present in the ASN (S8a gives `vₘ ≥ 1` and `p₂ ≥ 1`; `n ≥ 1` and `v ∈ R` are stated preconditions). No design intent or implementation evidence is needed.

## Issue 2: Use-site inventory in the Subspace scoping axiom
Reason: Internal fix. Deleting the downstream-lemma inventory is a pure editorial removal; the per-lemma `At S = 1` headers already carry the scope. No channel input required.

## Issue 3: Prose justifying lemma ordering plus a second inventory
Reason: Internal fix. Replacing the ordering-justification and re-listing with one substantive sentence is editorial; the dependency discipline is visible in each lemma's own citations. No channel input required.
