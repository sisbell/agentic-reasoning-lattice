# Channel Assignment — ASN-0110 review-13

**Date:** 2026-06-08 01:40

## Issue 1: Open Question 4 is already answered by RE-anon
Reason: Fully internal. RE-anon already establishes the lower bound `max_i |Eᵢ|` and the L11b-driven undeterminability of the exact count; whether OQ4 duplicates that or names a genuinely-open residual is a judgment over claims already present in the ASN. No design-intent or implementation evidence is needed to remove or sharpen the question.

## Issue 2: RE-surv mis-types the composite K.μ~ as a single-step transition
Reason: Fully internal. The review itself quotes A1a's distinction (single-step `Σ → Σ'` for atomic edits, `Σ →* Σ'` for the composite K.μ~), and the ASN already invokes A1a; the fix is a typing correction mirroring that quoted distinction, derivable without consulting either channel.
