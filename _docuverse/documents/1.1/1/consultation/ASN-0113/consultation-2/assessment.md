# Channel Assignment — ASN-0113 review-2

**Date:** 2026-06-05 00:07

## Issue 1: W19's justification contradicts the substrate it relies on
Reason: The fix is internal — the note must drop the false "forking, never mutation" parenthetical and ground permanence in W18 (no transition ⟹ state unchanged ⟹ `V_S(d)` fixed). The conflicting transitions K.μ⁻/K.μ~ are already in ASN-0047, the note's own cited substrate, so no design intent or fresh implementation evidence is needed.

## Issue 2: Open Question 2 has already been answered normatively by the body
Reason: The fix is internal — W7 and W14 already decide omission-of-empty-subspace and zero-as-absent, and W3/S2 already establish no zero-extent span exists. Deleting or recasting the open question requires only the note's own normative claims.

## Issue 3: W12's reachability construction elides required coupling steps
Reason: The fix is internal — K.α, K.μ⁺, and the J0 coupling are all part of ASN-0047, the foundation the note already builds on, so restating the per-position step as a coupled K.α + K.μ⁺ pair needs only the existing substrate, not design intent or implementation evidence.
