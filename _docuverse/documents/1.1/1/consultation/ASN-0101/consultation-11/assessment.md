# Channel Assignment — ASN-0101 review-11

**Date:** 2026-05-27 17:21

## Issue 1: K.σ missing from foundation vocabulary list
Reason: Internal consistency fix — D10 already lists the correct extended vocabulary including K.σ, so the opening list need only be aligned with D10's own statement. No external evidence required.

## Issue 2: Notation overload between span width and link addresses
Reason: Editorial/notational fix — the ASN already introduces `ℓ_σ` in D11; applying it consistently from D0 onward is internal to the document. No external consultation needed.

## Issue 3: D11 wp not verified against the worked examples
Reason: All data needed for the wp verification (pre-state projections, regions Λ/X/Ρ, post-state cardinalities) is already computed in the worked examples; the wp computation is mechanical from D11's own formulas. Internally derivable.

## Issue 4: D10 out of numerical order in Claims Introduced table
Reason: Pure formatting fix to the claims table. No external consultation needed.

## Issue 5: D7 statement uses informal pre-state quantification
Reason: Formalization fix — D7's justification already uses the precise form `a ∈ ran(M(d))` at Σ; the statement need only be rewritten to match. Internal to the ASN.
