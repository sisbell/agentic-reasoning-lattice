# Channel Assignment — ASN-0101 review-29

**Date:** 2026-06-03 16:33

## Issue 1: D8 omits ActivatedEmission from its invariant catalogue while claiming exhaustiveness
Reason: Derivable from the ASN alone. D8's frame already establishes `E' = E` and that DEL performs no entity-allocation event; the review itself supplies the trivial pointwise discharge, so adding the ActivatedEmission clause requires no design intent or implementation evidence.
