# Channel Assignment — ASN-0051 review-35

**Date:** 2026-05-16 01:17

## Issue 1: K.ρ omission in J1★-required witness composites
Reason: Fix is internal — J1★ is defined in ASN-0047 (already cited), and SV7's TransclusionCouplingAbsence corollary already demonstrates the exact acknowledgment pattern to mirror. Mechanical addition of K.ρ step or parenthetical.

## Issue 2: SV6 worked example silently uses non-conforming s_8
Reason: Fix is internal — SV6 is explicitly a structural claim about origins and field separators, and the SV10 witness already declares s_C = 1 as a model for the convention. Adding a one-line clarification or subspace declaration is derivable from existing content.

## Issue 3: SV10 main witness elides multi-step reachability from Σ₀
Reason: Fix is internal — all required operations (K.δ, K.α, K.μ⁺, K.ρ, K.λ) and their preconditions are defined in ASN-0047 (already cited) and used elsewhere in this ASN. Tracing or summarizing the chain requires no external input.

## Issue 4: SV13(e) "M-frame transitions" listing is potentially misleading
Reason: Fix is internal — K.δ's seeding behavior (introducing M(d_new) = ∅ for a fresh document) is specified in ASN-0047 (already cited) and used in the SV10 corollary's Step 1. Adding the clarifying clause requires only existing references.

## Issue 5: Same-origin coverage growth — counterexample missing tumbler-level verification
Reason: Fix is internal — the divergence-position argument uses T1(i) and TA5(d) from ASN-0034 (already cited), and SV6's worked example in this ASN already exhibits the required level of detail as a model. Mechanical expansion of the proof sketch.
