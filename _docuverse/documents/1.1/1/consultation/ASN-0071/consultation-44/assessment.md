# Channel Assignment — ASN-0071 review-44

**Date:** 2026-06-03 09:28

## Issue 1: Read/find inverse stated twice
Reason: Pure deletion of a duplicate restatement; the intro already carries the framing, so the fix is internal to the ASN.

## Issue 2: "Source-anchored" paragraph duplicates F-LOC and gestures out of scope
Reason: F-LOC already states the locality content and the distributed-node clause is self-declared out of scope; deletion is derivable from the ASN alone.

## Issue 3: Currency prose pre-answers a deferred Open Question
Reason: The fix is to cut a claim the ASN itself defers as an Open Question, keeping the bare "does not consult `R`" — entirely internal.

## Issue 4: P1 forward-preservation reassurance in `find`'s precondition
Reason: Dropping a temporal aside that overlaps the Currency section while keeping the single-state `wp-defined` statement; derivable from the ASN's own structure.
