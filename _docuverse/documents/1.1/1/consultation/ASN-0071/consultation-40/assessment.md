# Channel Assignment — ASN-0071 review-40

**Date:** 2026-06-03 09:04

## Issue 1: "What this verifies" is a use-site inventory wrapped in meta-prose
Reason: Purely editorial deletion — the fix is to drop the framing sentence and the bullets that merely re-cite already-labelled trace lines, keeping only those adding new reasoning (F-CUR's contraction remark, home/transcluding recovery). Which bullets re-point versus add reasoning is fully determined by the ASN's own text.

## Issue 2: Basis-column legend explains table format rather than reasoning
Reason: Removing a format-glossary paragraph whose vocabulary is self-explanatory in context. No design intent or implementation evidence bears on whether the legend is redundant — derivable from the ASN alone.

## Issue 3: Worked scenario re-discharges ASN-0047 coupling instead of exercising the operation
Reason: The fix collapses the per-composite J0/J1★/J1'★ discharge into one sentence asserting reachability by standard ASN-0047 composites. The ASN already states the composites are standard allocate–place–record triples and that ASN-0047's invariants hold at the reached state, so the replacement text is derivable internally.
