# Channel Assignment — ASN-0071 review-77

**Date:** 2026-06-03 16:35

## Issue 1: Σ-reachability justified twice
Reason: Pure editorial deduplication — removing a redundant preamble clause that repeats the closing *Reachability.* paragraph's argument. No design intent or implementation evidence is involved; the fix is fully internal.

## Issue 2: Prose gloss restates the `iaddrs_one` formula and drifts to `find`
Reason: Deleting a restatement of the set-builder and relocating a set-valuedness remark are structural editing decisions about the ASN's own text. Both the F-FILT point and the `find` set-valuedness claim are already established in the ASN, so the fix is derivable internally.
