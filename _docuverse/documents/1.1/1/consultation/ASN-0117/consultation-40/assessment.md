# Channel Assignment — ASN-0117 review-40

**Date:** 2026-06-11 03:14

## Issue 1: undefined label "F0"
Reason: The minimal fix (dropping the label) is internal, but the review's alternative fix hinges on a fact only the evidence channel holds — whether F0 is the consultation's own designation for the cross-document frame confirmed at Q17. Gregory can settle which fix is correct; Nelson is not needed since the frame's semantics are already established via D-CD/DEL-FDOC.
Gregory question: Does the consultation evidence or knowledge base synthesis (specifically the material behind Q17) use "F0" as a named designation for the cross-document frame property, and if so, what is its exact statement and citation?

## Issue 2: within-document sharing example silently forces `a_2 = a_5`
Reason: The fix is purely editorial and derivable from the ASN itself — the base scenario, S2's functionality constraint, and the intended `a_2 = a_5` reading are all already in the text; the example only needs rewording to state the stipulation explicitly.

## Issue 3: J1★ parenthetical defends a case the predicate already excludes
Reason: The fix is internal — J1★'s trigger conjunct is quoted in the ASN and is visibly one-directional, so deleting or trimming the parenthetical requires no external authority on either design intent or implementation behavior.
