# Channel Assignment — ASN-0117 review-16

**Date:** 2026-06-09 09:33

## Issue 1: Link-survival-by-byte-anchoring restated across four sites
Reason: Pure editorial deduplication — the load-bearing argument already lives at P4 (via LP3/LP12/LP16/LP17/LP18) and the redundant restatements are internal to the note; no design intent or implementation evidence is needed to collapse them to pointers.

## Issue 2: Implementation-mechanics parenthetical in the delete-everything boundary
Reason: The fix is to delete an implementation-shape aside and keep only the abstract clause (both arrangements denote the empty partial function), which is already established in the ASN's own state model; no channel input required.
