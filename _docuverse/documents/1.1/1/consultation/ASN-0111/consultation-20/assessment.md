# Channel Assignment — ASN-0111 review-20

**Date:** 2026-06-08 11:54

## Issue 1: RL4 contradicts itself on whether the read discloses ownership
Reason: Internal fix. The contradiction is resolved entirely from the ASN's own content — L2 and the body already establish that `home(a)` is address-derived (T4 field projection) and that the read outputs endsets only; reconciling the table row and "What the read reveals" framing to attribute ownership to the key, not the read, needs no external channel.

## Issue 2: Method-narration meta-prose that advances no claim
Reason: Internal fix. Deleting a sentence that describes the note's rhetorical method requires no design intent or implementation evidence.

## Issue 3: Defensive existence-justification preamble in the invariants section
Reason: Internal fix. Collapsing the redundant justification to a clause keyed to the already-established standing precondition is purely a within-note editorial action, derivable from the ASN's existing structure.
