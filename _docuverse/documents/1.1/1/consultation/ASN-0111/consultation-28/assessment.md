# Channel Assignment — ASN-0111 review-28

**Date:** 2026-06-08 12:46

## Issue 1: Orphaned-instance worked example re-derives FOLLOWLINK machinery instead of verifying the READLINK postcondition
Reason: Internal. The fix replaces a discoverability derivation with the hypothesis "suppose `a` is orphaned" and verifies only that `readlink` reads `Σ.L` — a fact already stated in RL8. No design intent or implementation evidence is needed.

## Issue 2: Accreted disambiguation parentheticals in the from-set bullet
Reason: Internal. Removing terminology-policing clauses and a forward cross-reference to RL8 is a pure prose trim; the three I-addresses and their unarranged status are already present in the example.

## Issue 3: Motivational essay in the nesting preamble
Reason: Internal. Deleting one editorial sentence that advances no claim, while keeping the L13 reflexive-span setup RL6 uses, is derivable from the ASN's own structure.
