# Channel Assignment — ASN-0111 review-25

**Date:** 2026-06-08 12:22

## Issue 1: Redundant restatement in the home-from-key remark
Reason: Pure editorial condensation — drop two restatements, keep the L2-cited distinction (home fixed by key `a`, not value). No design intent or implementation evidence informs which words to cut; derivable from the ASN's own content.

## Issue 2: Nesting "reader's affair" prose duplicated across claim and worked example
Reason: Pure deduplication — keep the obligation prose in RL6, reduce the worked instance to the concrete `a' ∈ coverage(...)` check. The non-recursion claim is already proven in the ASN; trimming is internal.
