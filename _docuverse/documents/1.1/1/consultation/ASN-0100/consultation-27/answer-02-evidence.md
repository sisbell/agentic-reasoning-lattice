## Question 2 [evidence]

> What does the ActivatedEmission invariant in ASN-0047's ExtendedReachableStateInvariants quantify over and constrain — does it range over the entity set E, or over content/link allocator emissions (dom(C)/dom(L)) that INSERT's K.α firings extend?

## KB Synthesis

Let me check memory and the project files for ASN-0047 context.

Looking at the project files to find ASN-0047.

Looking at the docuverse paths and ASN-0047 documents.
<function_calls>
<invoke name="mcp__claude-code__read_file">
<parameter name="path">/Users/shane/Documents/github/claude/xanadu-spec/memory/MEMORY.md</parameter>
</invoke>
</function_calls>
<function_calls>
<invoke name="mcp__claude-code__glob">
<parameter name="pattern">_docuverse/documents/**/*ASN-0047*</parameter>
<parameter name="cwd">/Users/shane/Documents/github/claude/xanadu-spec</parameter>
</invoke>
</function_calls>
<function_calls>
<invoke name="mcp__claude-code__glob">
<parameter name="pattern">_docuverse/documents/**/*asn*47*</parameter>
<parameter name="cwd">/Users/shane/Documents/github/claude/xanadu-spec</parameter>
</invoke>
</function_calls>
<function_calls>
<invoke name="mcp__claude-code__bash">
<parameter name="command">find /Users/shane/Documents/github/claude/xanadu-spec/_docuverse -name "*.md" -o -name "*.dfy" -o -name "*.txt" | xargs grep -l -i "asn-0047\|asn0047" 2>/dev/null | head -20</parameter>
</invoke>
</function_calls>
<function_calls>
<invoke name="mcp__claude-code__bash">
<parameter name="command">find /Users/shane/Documents/github/claude/xanadu-spec/_docuverse -name "*.md" -o -name "*.dfy" -o -name "*.txt" | xargs grep -l -i "ActivatedEmission\|ExtendedReachableState" 2>/dev/null | head -20</parameter>
</invoke>
</function_calls>
<function_calls>
<invoke name="mcp__claude-code__bash">
<parameter name="command">find /Users/shane/Documents/github/claude/xanadu-spec -name "*.md" -o -name "*.dfy" -o -name "*.txt" | xargs grep -l -i "ActivatedEmission\|ExtendedReachableState" 2>/dev/null | head -20</parameter>
</invoke>
</function_calls>
<function_calls>
<invoke name="mcp__claude-code__bash">
<parameter name="command">find /Users/shane/Documents/github/claude/xanadu-spec -name "*.md" | xargs grep -l "ASN-0047" 2>/dev/null | head -10</parameter>
</invoke>
</function_calls>
<function_calls>
<invoke name="mcp__claude-code__bash">
<parameter name="command">find /Users/shane/Documents/github/claude/xanadu-spec -name "*.md" | xargs grep -l "47" 2>/dev/null | head -20</parameter>
</invoke>
</function_calls>
<function_calls>
<invoke name="mcp__claude-code__bash">
<parameter name="command">ls /Users/shane/Documents/github/claude/xanadu-spec/_docuverse/documents/</parameter>
</invoke>
</function_calls>
<function_calls>
<invoke name="mcp__claude-code__bash">
<parameter name="command">ls /Users/shane/Documents/github/claude/xanadu-spec/_docuverse/documents/1.1/</parameter>
</invoke>
</function_calls>
<function_calls>
<invoke name="mcp__claude-code__bash">
<parameter name="command">ls /Users/shane/Documents/github/claude/xanadu-spec/_docuverse/documents/1.1/1/</parameter>
</invoke>
</function_calls>
<function_calls>
<invoke name="mcp__claude-code__bash">
<parameter name="command">find /Users/shane/Documents/github/claude/xanadu-spec/_docuverse -name "ASN-0047" -type d 2>/dev/null; find /Users/shane/Documents/github/claude/xanadu-spec/_docuverse -name "*0047*" 2>/dev/null</parameter>
</invoke>
</function_calls>
<function_calls>
<invoke name="mcp__claude-code__bash">
<parameter name="command">ls /Users/shane/Documents/github/claude/xanadu-spec/_docuverse/documents/1.1/1/ | head -20</parameter>
</invoke>
</function_calls>
<function_calls>
<invoke name="mcp__claude-code__bash">
<parameter name="command">ls /Users/shane/Documents/github/claude/xanadu-spec/_docuverse/documents/1.1/1/ | grep -i "spec\|asn\|proposal"</parameter>
</invoke>
</function_calls>
<function_calls>
<invoke name="mcp__claude-code__bash">
<parameter name="command">find /Users/shane/Documents/github/claude/xanadu-spec/_docuverse -type d | head -30</parameter>
</invoke>
</function_calls>
<function_calls>
<invoke name="mcp__claude-code__bash">
<parameter name="command">find /Users/shane/Documents/github/claude/xanadu-spec/_docuverse -type d | grep -i "spec\|proposal\|asn" | head -30</parameter>
</invoke>
</function_calls>
<function_calls>
<invoke name="mcp__claude-code__bash">
<parameter name="command">ls /Users/shane/Documents/github/claude/xanadu-spec/_docuverse/documents/1.1/1/</parameter>
</invoke>
</function_calls>
<function_calls>
<invoke name="mcp__claude-code__bash">
<parameter name="command">ls /Users/shane/Documents/github/claude/xanadu-spec/_docuverse/documents/1.1/1/ | cat</parameter>
</invoke>
</function_calls>
<function_calls>
<invoke name="mcp__claude-code__bash">
<parameter name="command">find /Users/shane/Documents/github/claude/xanadu-spec -type d -name "consultation" | head -10</parameter>
</invoke>
</function_calls>
<function_calls>
<invoke name="mcp__claude-code__bash">
<parameter name="command">ls /Users/shane/Documents/github/claude/xanadu-spec/_docuverse/documents/1.1/1/consultation/</parameter>
</invoke>
</function_calls>
<function_calls>
<invoke name="mcp__claude-code__bash">
<parameter name="command">ls /Users/shane/Documents/github/claude/xanadu-spec/_docuverse/documents/1.1/1/consultation/ | head -30</parameter>
</invoke>
</function_calls>
<function_calls>
<invoke name="mcp__claude-code__bash">
<parameter name="command">ls /Users/shane/Documents/github/claude/xanadu-spec/_docuverse/documents/1.1/1/consultation/ | grep "47"</parameter>
</invoke>
</function_calls>

ASN-0047 doesn't appear to be in the docuverse yet. Let me search more broadly.
<function_calls>
<invoke name="mcp__claude-code__bash">
<parameter name="command">grep -r "ASN-0047\|asn-0047" /Users/shane/Documents/github/claude/xanadu-spec/ 2>/dev/null | head -20</parameter>
</invoke>
</function_calls>
<function_calls>
<invoke name="mcp__claude-code__bash">
<parameter name="command">grep -r "ActivatedEmission\|ExtendedReachable" /Users/shane/Documents/github/claude/xanadu-spec/ 2>/dev/null | head -20</parameter>
</invoke>
</function_calls>

ASN-0047 and the terms `ActivatedEmission` and `ExtendedReachableStateInvariants` do not exist anywhere in this repository. This question cannot be answered from the KB.

---

**The question is not answerable from the KB.**

ASN-0047 does not exist in this repository, and the terms `ActivatedEmission`, `ExtendedReachableStateInvariants`, and the quantification structure you describe (`E`, `dom(C)/dom(L)`, `K.α firings`) appear in no KB entry, document, spec file, or consultation record under `_docuverse/`.

The KB entries closest to the concepts embedded in your question are:

- **[ST-INSERT]** — covers how INSERT's call to `inserttextingranf` allocates fresh I-addresses (the content allocator `C` analog) and how `createorglingranf` for links allocates link orgls (the `L` analog), both drawing from the same monotonically-incrementing granfilade.
- **[INV-MONOTONIC]** — the I-address allocation invariant: both text and link orgls extend a shared monotonic sequence; `dom(C)` and `dom(L)` grow only via INSERT/CREATELINK firings.
- **[SS-ADDRESS-ALLOC]** / **[ST-ADDRESS-ALLOC]** — allocation postconditions for the entity set `E` (documents, versions).

If ASN-0047 is a draft spec being written elsewhere and uses these terms, you would need to provide that document's text directly — the KB has no record of it.

---

## Code Exploration

---

## Answer: ASN-0047's ActivatedEmission Cannot Be Located

The specific document and its formal invariants do not exist in any file I can access in this repository.

**What I searched:**
- `findings/` — 80+ files, none contain "ActivatedEmission", "ASN-0047", "ExtendedReachableStateInvariants", or "K.α"
- `knowledge-base/kb-formal.md` (587KB), `kb-synthesis.md`, all 74 `analyzed/` files — no matches
- The entire `udanax-test-harness/` tree — zero hits for any of those terms

**What "0047" refers to here:**

Finding 0047 (`findings/0047-docispan-granularity-per-span.md`) is about DOCISPAN per-span granularity. Its analyzed form (`knowledge-base/analyzed/0047.md`) defines four entries: `SS-DOCISPAN`, `ST-INSERT`, `ST-COPY`, `INV-SPANF-GROWTH`. None of these use the notation "ActivatedEmission", "entity set E", "dom(C)/dom(L)", or "K.α firings".

**What the code actually shows about INSERT's allocator emissions:**

The closest behavioral analog in the code:

- `do1.c:71` — `inserttextingranf(taskptr, granf, &hint, textset, &ispanset)` — allocates fresh I-addresses (the content "emission")
- `do1.c:92` — `docopy(...)` then calls `insertspanf(..., DOCISPAN)` — registers that emission in the spanfilade
- `spanf1.c:48` — one `insertnd` call per `typeispan`, not per byte — the granularity invariant (`INV-SPANF-GROWTH`)

The relevant formal statement that *is* in the KB (`kb-synthesis.md:5169`):

> Frame condition for INSERT on the identity layer: `forall doc d != target, forall v in d :: d.mapping(v) is unchanged`. Within the target: `forall v in target where v mapped to I before INSERT :: exists v' in target' where target'.mapping(v') == I`. No I-address is lost.

**Conclusion:**

The terms "ActivatedEmission", "ExtendedReachableStateInvariants", and "K.α firings" appear in neither the C source, nor the knowledge base, nor the findings. If "ASN-0047" refers to an **Annotated Specification Note** in the parent `xanadu-spec` channel (outside this test harness directory), I cannot read it — the parent directory was permission-denied during traversal. The question cannot be answered from what is accessible here.

To answer it properly: locate the ASN-0047 document in the broader `xanadu-spec` repository and read the `ExtendedReachableStateInvariants` section directly.
