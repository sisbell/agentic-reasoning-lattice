# Channel Assignment — ASN-0100 review-46

**Date:** 2026-06-04 14:40

## Issue 1: The "non-tight alternative" in the worked example imagines a case the example's own construction excludes
Reason: Internal fix. The review supplies the structural argument (K.α subsequent-emission rule and chain monotonicity place `a_{new0}, a_{new1}` above `a_5`) and the concrete replacement span; the math is fully derivable from the ASN's own allocation discipline and tumbler order — no design intent or implementation evidence is at stake.

## Issue 2: §Cross-document independence applies single-step LP4/LP5 to the composite without chaining
Reason: Internal fix. The ASN already contains the correctly-chained argument in §Coverage and link discoverability; the resolution is to delete the under-rigorous one-shot citation and forward-point (or inline the same chaining). No external channel needed.

## Issue 3: Presentation meta-prose around the non-import principle
Reason: Internal fix. Purely editorial — drop the presentation-justifying clauses while keeping the object-level statement of which frozen-store frames fail and which invariants are re-derived. Nothing to consult.
