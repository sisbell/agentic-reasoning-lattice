# Channel Assignment — ASN-0125 review-15

**Date:** 2026-06-13 12:19

## Issue 1: EL11(b) closes with a use-site inventory
Reason: The fix is a pure deletion of redundant prose — drop the call-site enumeration and the "which every use here supplies" clause — with no change to any claim's content. The load-bearing point (the `Observe`-identification needs `y ∈ dom(Σ.L)`, the bare comprehension does not) is already stated and is fully carried by the following sentence, so the fix is internal to the ASN with neither design intent nor implementation evidence at issue.
