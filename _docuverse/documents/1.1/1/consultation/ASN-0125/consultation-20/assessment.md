# Channel Assignment — ASN-0125 review-20

**Date:** 2026-06-13 14:41

## Issue 1: A motivational sentence in `editlink`'s definition reasons about a case `DC` excludes
Reason: Internal fix. The slippage is corrected using facts already in the ASN — R6a (retraction's nullifying effect is permanent, ASN-0086), L12 (original persists), and DC's text constraining only `ℓ'` not `a`. Both offered remedies (drop the R6a speculation, or state the `a ∈ L_R`-admitted case precisely and defer to the existing Open Question on retraction supersession) draw solely on present content; no design intent or implementation evidence is at issue.

## Issue 2: The "home ≠ named principal" scope caveat is restated three to four times
Reason: Internal fix. Pure deduplication — the caveat (home-to-named-owner resolution is an ASN-0042 overlay, not a function of `Σ`) is already stated and self-contained; consolidating to EL8(b) and replacing the EL3/table restatements with citations needs no external channel.

## Issue 3: Df-LAY's disambiguation paragraph defends the definition against itself
Reason: Internal fix. Restructuring the definition so "bare K.λ" means standalone original-creation at first mention, with editlink's internal `[K_sup]`-carrying `K.λ` admitted there, uses only content already present in Df-LAY and EL1; it is an editorial reorganization, not a question of intent or evidence.
