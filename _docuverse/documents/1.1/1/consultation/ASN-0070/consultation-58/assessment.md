# Channel Assignment — ASN-0070 review-58

**Date:** 2026-06-03 01:45

## Issue 1: Empty result component mis-attributed to the vacuous-subspace convention
Reason: Internal — the ASN already defines both the main V-restricted denotation (`⟦⟨⟩⟧_V = ∅`) and the separate Vacuous-subspace convention, and Configuration 1 supplies `m_{s_L}(d) = 2` defined. Distinguishing the two empty-source cases and re-citing requires only the note's own definitions.

## Issue 2: Insertion mechanics narrated in the Setting are unused by the query
Reason: Internal — `follow` reads the current `M(d)`, and the note itself identifies the only consumed fact (common depth `m_S(d)` exists when the subspace is non-empty, undefined otherwise). Removing the pinning/re-pinning narration is a scope-trimming edit derivable from the ASN's own reasoning.
