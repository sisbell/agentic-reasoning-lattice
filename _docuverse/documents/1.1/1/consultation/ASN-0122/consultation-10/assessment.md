# Channel Assignment — ASN-0122 review-10

**Date:** 2026-06-13 10:25

## Issue 1: Implementation-cache musing inside an abstract consequence
Reason: Neither channel — the fix is a pure deletion of redundant prose whose content is already carried by the two preceding memorylessness sentences and whose forward-looking cache/index question is already held open in Open Question 4. No design intent or implementation evidence is needed to remove a sentence the ASN itself shows to be redundant.

## Issue 2: Interpretive paragraph restating X4's own corollaries
Reason: Neither channel — the paragraph re-derives the Compositionality and "No invention, no silent loss" corollaries plus X5, all already proved in the ASN; the single novel point (correspondence is pointwise, so a window has no context to disturb) is stated in the ASN's own results. The reduction and the dropped X8 forward pointer are derivable internally.

## Issue 3: Over-attribution in the chain-partition proof
Reason: Neither channel — the correction is internal proof bookkeeping: successor-uniqueness follows because `succ` is a total (single-valued) function defined in the ASN, and predecessor-uniqueness rests on the already-cited TS2 (per coordinate, with `#u` equal across feet by the already-cited S8-depth). All facts are present in the ASN and its referenced foundations.
