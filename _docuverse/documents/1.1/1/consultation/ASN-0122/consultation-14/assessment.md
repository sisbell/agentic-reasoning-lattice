# Channel Assignment — ASN-0122 review-14

**Date:** 2026-06-13 11:25

## Issue 1: X12's closing sentence restates the R4 bullet
Reason: Pure redundancy cut — the duplication is between two passages already in the ASN (the R4 bullet and the post-Frame sentence), and the only non-duplicated content ("exactly as for span-sets") is itself an internal cross-reference the spec already makes. No design-intent or implementation evidence bears on whether one of two identical assertions should be deleted.

## Issue 2: Residual self-referential / defensive framing
Reason: Pure reframing — the substantive content (the D-SEQ★-density argument legitimizing the count; the two premises X6(b) needs) is already present and unchanged; the fix only strips defensive/process framing and leads with the positive claim, with the Required section supplying the exact rewrites. Nothing about design intent or the implementation is in question.
