## Question 1 [theory]

> When asked which spans of two documents correspond, what exactly must the system return — a list of paired spans, and what does each pair assert about the two documents?

I have strong curated coverage of this (the `correspondence` concept and the `operations` file both quote the SHOWRELATIONOF2VERSIONS return spec at 4/70). But since this is a precise specification question, let me verify against the actual page.

The relevant command, SHOWRELATIONOF2VERSIONS, is at 4/70 (raw page ~237). Let me read it and its surrounding context.

`★ Insight ─────────────────────────────────────`
The curated quote "a list of ordered pairs of the spans of the two spec-sets that correspond" is attributed to 4/70, the same page-range as BEBE. Worth confirming the exact command signature and whether the page adds nuance about what a "pair" contains.
`─────────────────────────────────────────────────`
