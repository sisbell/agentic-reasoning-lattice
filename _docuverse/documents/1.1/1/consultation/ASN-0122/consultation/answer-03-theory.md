## Question 3 [theory]

> What ordering must the returned list of corresponding pairs follow, and is that ordering defined by position in the first document, the second, or both?

I'll start by examining what the curated sources say, then verify against the original page since this is a precise question about the SHOWRELATIONOF2VERSIONS return value.

The curated sources consistently quote one sentence from 4/70:

> "This returns a list of ordered pairs of the spans of the two spec-sets that correspond." (4/70)

This appears in the `correspondence` concept, the `correspondence` design-intent note, and the `operations` reference. Let me read the original page to see whether the surrounding context specifies the *list's* sort order, since the curated extract is just this single sentence.

The TOC maps 4/70 to raw page 237. Let me read it and its neighbors.
