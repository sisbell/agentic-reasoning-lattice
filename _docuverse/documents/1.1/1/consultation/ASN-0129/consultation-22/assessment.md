# Channel Assignment — ASN-0129 review-22

**Date:** 2026-06-11 22:46

## Issue 1: PC2's binder guard leaves the else-branch untyped, and the worked example leaves it uninstantiated
Reason: The fix is internal. The note's own commitments force the term reading: PC6's converse claims syntax-directed evaluation denotes exactly PL, and a binder-guard node with a state-reading else-branch is squarely inside the evaluation class (syntax-fixed control, same Σ and view per PC2's existing provision), so the constant-only reading would break the ceiling's exactness by the note's own argument; instantiating `head_live` with `else ⊥` is a local edit.

## Issue 2: PC6's parity assessment surveys an incomplete ℕ fragment
Reason: The fix is internal. The ASN already contains everything needed — `age`'s definition and home-wide footprint (FP), the binder route into ℕ position (PC2), and the registry-pinned ceiling — and the required edit is either an explicit BH4-free scoping (matching the existing registry-pinning move) or an extension of the survey to age-bearing terms, plus mirroring the route in OQ6 the way the self-emit entry already does; no design intent or implementation evidence bears on which terms the note's own grammar admits.

## Issue 3: Duplicated deliberateness/deferral prose across PC6a, C-reach, and Open Question 4
Reason: The fix is internal. This is pure editorial deduplication — consolidating the grammar fact at PC6a, the conjecture status at C-reach, and stripping OQ4's framing prose — with no semantic content at stake that either channel could inform.
