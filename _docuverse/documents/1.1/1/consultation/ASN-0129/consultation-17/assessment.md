# Channel Assignment — ASN-0129 review-17

**Date:** 2026-06-11 21:17

## Issue 1: Self-emit inexpressibility asserted at theorem strength the note elsewhere refuses
Reason: The fix is internal — it is a calibration repair against the note's own standard (PC6's fence, C-reach's epistemic status): demote the extensional "never" to the vocabulary-level grammar fact plus a recorded conjecture-grade obligation. The vocabulary facts cited (no `home` atom, no address arithmetic in V-PRIM, prefix-test failure on sub-document homes) are already established in the ASN, and the review confirms the design conclusion survives the demotion unchanged, so neither design intent nor implementation evidence is required.

## Issue 2: PC6's parity candidate argues against a fragment smaller than the one V-PRIM ships
Reason: The fix is internal — V-PRIM's actual admitted fragment (term-to-term `=`/`≤`, term-level `+`, ℕ literals) is defined in the ASN itself, and the required change is to restate the fragment correctly and reassess the parity candidate against it while keeping the existing hedge. This is a self-consistency correction between two of the note's own sections, needing no external authority.
