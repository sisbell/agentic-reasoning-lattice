# Channel Assignment — ASN-0129 review-34

**Date:** 2026-06-14 21:50

## Issue 1: "This note's own additions are six" — forward-reference inventory
Reason: Internal. The fix is a pure deletion of a redundant catalog whose every item is already fenced at its point of introduction (V-AUD/V-TUP/V-PRIM/V-DOC); nothing about design intent or implementation is in question.

## Issue 2: COD inhabitation / producer inventory — decorative completeness
Reason: Internal. The judgment that no proof depends on codomain inhabitation is checkable against the note's own proofs, and the fix keeps the `Codom` set and `Map_fin` definition while dropping the decorative realization/intro-elim annotations — no external evidence needed.

## Issue 3: V-IDX — vacuity case over-expansion plus survivor inventory
Reason: Internal. The one-sentence core (R mandatory with `behaviors = ∅` via S3/R-C1) and the instance-wise well-formedness rule are already present in the passage; compression and the single retained `targets_keyed(s)[K]` route are restructuring of existing content.

## Issue 4: Depends-line rationale duplicates PC4
Reason: Internal. The trim relocates rationale already carried by PC4 and the "Structural reads only" paragraph; the replacement text is fully specified and derivable from the note.

## Issue 5: "⊥ is a verdict" restated across sections
Reason: Internal. Deduplication against PC2's existing statement of the ⊥-verdict semantics; the replacement clause is given and requires no design-intent or implementation input.
