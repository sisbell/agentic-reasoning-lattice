# Channel Assignment — ASN-0112 review-15

**Date:** 2026-06-08 08:39

## Issue 1: Reach biconditional stated twice in succession, wrapped in distinctness meta-commentary
Reason: Pure editorial deduplication. Whether the level-uniform arithmetic fact (`σ_d` level-uniform iff `#origin_d ≥ #reach_d`) is load-bearing is decidable from the ASN's own downstream uses (V6, V17, the wp factoring), so no external channel is needed.

## Issue 2: V17 closing sentence is a defensive non-dependency that defers to V2
Reason: Straight deletion of a redundant back-reference; V17's own D0-based derivation already stands within the ASN, so the fix is internal.

## Issue 3: Authorization digression in the precondition section is essay content in a structural slot
Reason: Condensation to one sentence by cutting restatement and the BERT/session detail. The material to remove is already present and the ASN itself flags it as unexpressible in the model, so the fix is derivable internally.
