# Channel Assignment — ASN-0114 review-13

**Date:** 2026-06-09 22:14

## Issue 1: F5's premises are stated three different ways, and one of them names a fact the derivation never uses
Reason: The fix is internal — the review already pins the correct premise set to {F1, L12 (composed by LP13)} by inspecting the `∎` derivation present in the ASN, and the task is purely to make the opener and wrap-up match that derivation. No design intent or implementation evidence bears on which facts the existing proof actually invokes; F1, L12, LP13, and F4 are all defined within the note, so the attribution can be corrected from the ASN's own content alone.
