# Channel Assignment — ASN-0114 review-7

**Date:** 2026-06-09 20:54

## Issue 1: Open Question 3 is already answered by F7
Reason: The contradiction is visible within the note itself — F7 already supplies, as a mandatory claim, the very invariant the open question presents as unresolved. Both the removal and the suggested reword (toward the serialization/protocol-boundary residue that F7, an abstract-`Σ` statement, does not cover — already evidenced in-note by the `sporgl.c:93` → `putrequestfailed` collapse) draw only on material already present. Internal.

## Issue 2: Use-site inventory in the "Status of the result" paragraph
Reason: Pure editorial deletion of a downstream-consumer inventory; the well-definedness fact and its F3 justification already stand in the same sentence without the `(F5, F6, F8)` list. Internal.

## Issue 3: F5's "load-bearing" paragraph restates one point three times with defensive framing
Reason: Editorial compression of an already-correct distinction the note establishes (L12 ⟹ coverage-permanence; content-identity addressing is what upgrades "same coverage" to "same material," which F5 does not formally claim); the review itself supplies the target one-sentence form. Internal.

## Issue 4: F2's proof excludes the singleton but not the empty result
Reason: A one-line proof patch assembled entirely from lemmas the note already cites — disconnectedness gives `p, r ∈ coverage(eᵢ)`, so `coverage(eᵢ) ≠ ∅`, hence by F1 `coverage(R) ≠ ∅`, forcing `R ≠ ⟨⟩` via ASN-0053 S2. Internal.
