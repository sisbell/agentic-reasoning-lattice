# Channel Assignment — ASN-0047 review-210

**Date:** 2026-06-01 04:12

## Issue 1: Triplicated child-spawn-premise discharge across K.δ case (ii) k=2 sub-cases A/B/C
Reason: Pure structural consolidation — the three sub-cases and their distinct premise sources are already fully present in the ASN; collapsing them into one parameterised discharge with a three-row dispatch is derivable from existing content alone.

## Issue 2: NodeRegistryBootstrap / registry-tracking prose explains why the axiom is needed, not what the system guarantees
Reason: The fix choice between (a) a Σ-only lemma and (b) declaring node baptism a boundary input turns on whether node creation was *intended* as external protocol-level infrastructure (Nelson) and on whether udanax-green actually maintains a node registry distinct from docuverse state (Gregory).
Nelson question: Was node creation intended to sit outside the docuverse transition model — a protocol/boundary act establishing servers — rather than a state transition over the docuverse's own components?
Gregory question: Does udanax-green allocate node addresses through a mechanism distinct from the granfilade/document state, and if so, what registry or structure tracks allocated node addresses?

## Issue 3: "Content-store invariance under arrangement mutation" restated three times
Reason: Pure editorial deduplication — the corollary is a one-line consequence of P0 plus the arrangement frames already stated in the ASN; deriving once and cross-referencing requires no external input.
