# Channel Assignment — ASN-0047 review-162

**Date:** 2026-05-31 19:34

## Issue 1: Axiom annotated with rationale for its own absence elsewhere
Reason: Purely editorial deletion — the object-level fact (content depth governed by `ValidFirstInsertionPosition`, may change after clearance) is already carried by K.μ⁺'s precondition; removing the justification paragraph is internal to the ASN.

## Issue 2: Reviser drift — discharging a case the operation excludes
Reason: Internal fix — K.μ⁺_L's effect adds a single mapping by construction, and the substantive obligation `v_ℓ ∉ dom(M(d))` is already verified in parts (a)/(b); dropping the excluded-case sentence requires no external evidence.

## Issue 3: Use-site inventory in a notation slot
Reason: Internal editorial trim — the `⊂`/`⊃` proper-subset convention is fully specified by the preceding clause, and the strictness requirement is already restated at the K.μ⁺/K.μ⁻ effect clauses; removing the consumer inventory is derivable from the ASN alone.

## Issue 4: First-emission/subsequent-emission freshness discharge restated verbatim across five sites
Reason: Internal refactoring — the first/subsequent split (FirstEmission for the seed, GlobalUniqueness thereafter, L14 cross-subspace) is a single lemma already fully present in the ASN; consolidating it and citing by name needs no design or implementation input.

## Issue 5: Defensive premise-cataloguing and role-distinction prose
Reason: Internal editorial fix — the premises (T10a.7, chain-advancement uniqueness, P1, per-`(t,k')` axiom) are already cited at the proof steps that consume them; removing the standalone catalogue and defensive gloss is derivable from the ASN's own proof structure.
