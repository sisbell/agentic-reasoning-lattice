# Channel Assignment — ASN-0047 review-365

**Date:** 2026-06-02 12:23

## Issue 1: The K.μ~ shape-invariant discharge is a cross-section deferral loop with duplicated derivation
Reason: Purely editorial — the fix consolidates a duplicated S8-fin/D-SEQ★ derivation to a single site, derivable entirely from the ASN's own structure with no design-intent or implementation input needed.

## Issue 2: ValidComposite★ lists the named composite K.μ~ as an atomic transition
Reason: Internal consistency fix — K.μ~ is already defined as a named composite within the ASN, so separating it from the atomic enumeration is derivable from the ASN's own definitions.

## Issue 3: S4 verification paragraph carries defensive "what it is NOT" prose plus a use-site inventory
Reason: Pure prose deletion — removing navigational meta-prose; the S4 content-store discharge and the separate matrix rows already exist in the ASN.

## Issue 4: SubAllocatorBundle opens with an inherited-facts inventory
Reason: Editorial compression of an inherited-fact catalog already established by the *Inherited from foundation* table; the load-bearing delta and its consuming lemmas are all present in the ASN.
