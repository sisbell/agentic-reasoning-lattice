# Channel Assignment — ASN-0047 review-237

**Date:** 2026-06-01 11:03

## Issue 1: S3★-aux is both a K.μ~ admissibility hypothesis and an unconditionally-derived invariant
Reason: Internal. The tension is resolvable from the ASN's own structure — the D-SEQ★ derivation list and the Class (a) matrix both already fix S3★-aux's status (it plays no role in the derivation and is proved unconditionally for K.μ~), so the fix is to reclassify it as a derived consequence or a pre-state hypothesis without external input.

## Issue 2: Four near-duplicate K.μ~ paragraphs in the Class (a) discharge prose
Reason: Internal. Pure deduplication of identical "admissibility (i) stipulates; full-clearance decomposition realises" prose, with the per-property delta (S8-fin's independent discharge) already present in the text.

## Issue 3: "Link store and extended system state" section is a use-site inventory plus forward reference
Reason: Internal. The required deletion keeps only the empty-endset reading of L3, which the ASN itself already marks as the sole non-inherited local content; the inventory and preservation pre-announcement duplicate the matrix/P3 and the Inherited-from-foundation table.

## Issue 4: Defensive/scope meta-prose in clause (i) and Coupling-and-isolation
Reason: Internal. Removing forward references, the "not part of clause (i)" disclaimer (contingent on Issue 1), and the "clarification on scope" preamble is editorial cleanup against the ASN's existing definitions, requiring no design intent or implementation evidence.
