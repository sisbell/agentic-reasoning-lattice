# Channel Assignment — ASN-0047 review-221

**Date:** 2026-06-01 06:08

## Issue 1: `shift(t, 0) := t` reinvented from ASN-0034 instead of citing the foundation that already establishes it
Reason: Internal fix — the correct citation target (ASN-0036's S8 convention) is already referenced throughout this ASN as the foundation S8★ extends; swapping the parenthetical to cite it requires no design intent or implementation evidence.

## Issue 2: Implementation-anomaly essay sentence embedded mid-derivation in J1'★
Reason: Internal fix — the anomaly sentence is already attributed to Gregory and its content is not in dispute; removing or relocating it out of the wp derivation is purely editorial and needs no new channel input.

## Issue 3: The allocated-empty vs. default-empty `M(d) = ∅` distinction is explained in three separate slots
Reason: Internal fix — consolidating three restatements of a distinction the ASN already defines (the Notational convention) into a single home with cross-references is deduplication derivable from the ASN's own content.
