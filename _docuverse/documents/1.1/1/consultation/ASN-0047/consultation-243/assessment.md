# Channel Assignment — ASN-0047 review-243

**Date:** 2026-06-01 12:02

## Issue 1: Worked example mis-attributes the source of the `k₀` / `n'_{s_C}` cut-point notation
Reason: This is a citation/cross-reference error fully resolvable within the ASN — the `k₀` cut-point notation is defined in the *Decomposition of K.μ~* section, and the *Elementary transitions* replacement sentence is quoted in full, so correcting the back-reference (or adding the notation where claimed) is internal.

## Issue 2: Repeated exposition of the per-state / composite-boundary distinction
Reason: Pure editorial consolidation of redundant prose; all five restatements are present in the ASN and the distinction's content is already fixed, so collapsing to one statement with internal references needs no external input.

## Issue 3: `P4` is referenced as a superseded property but never given a definition
Reason: The unscoped bound `Contains(Σ) ⊆ R` and its content-scoped refinement P4★ are both stated in the ASN, so either labeling P4 with a one-line statement or dropping the label is derivable from existing content without design intent or implementation evidence.
