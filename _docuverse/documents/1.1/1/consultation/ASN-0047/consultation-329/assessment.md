# Channel Assignment — ASN-0047 review-329

**Date:** 2026-06-02 04:44

## Issue 1: Triplicated "scope GlobalUniqueness to the node-rooted subtree" argument
Reason: Pure deduplication — the subtree-scoping fact already lives in NodeRootedForest; consolidating three inline restatements into a single named citation requires no design intent or implementation evidence, only the ASN's own structure.

## Issue 2: K.δ case (ii) k=1 and k=2 restate the at-most-once discharge verbatim
Reason: Internal parameterization — collapsing two identical sentences into one statement over `k' ∈ {1, 2}` while retaining the k-specific conjuncts is a prose refactor derivable entirely from the existing K.δ case (ii) content.

## Issue 3: Defensive "robustness" paragraph imagining an internal step ordering
Reason: Internal deletion — the substantive P4a discharge via J1'★'s endpoint conjunct is already complete; removing the redundant robustness sentence needs no external input.

## Issue 4: Default-value convention stated twice
Reason: Internal deduplication — dropping the inline pre-statement in favor of the formal "Notational convention" paragraph is a single-site consolidation derivable from the ASN alone.
