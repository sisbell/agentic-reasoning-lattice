# Channel Assignment — ASN-0047 review-283

**Date:** 2026-06-01 20:08

## Issue 1: Same downstream location deferred to from multiple sections (forward-reference accretion)
Reason: Pure organizational fix — extract the `M'(d)|_{dom_L} = M(d)|_{dom_L}` + CL-UNIQ result as a named lemma and cite it; no design intent or implementation evidence is at stake, only prose placement already fully present in the ASN.

## Issue 2: Defensive meta-prose in the Notation section
Reason: Internal editorial fix — the correspondence is already derived in full at S3★; removing the disclaimer or stating signatures plainly requires only the ASN's own content.

## Issue 3: K.μ⁺ amendment enumerates a sibling transition's obligations
Reason: Internal fix — deleting the stray K.μ⁺_L sentence relies only on K.μ⁺'s own frame argument, which already closes the link-subspace obligation within the ASN.

## Issue 4: `dom(M(d)) ≠ ∅` is "derived" in K.μ⁻ but checked as a precondition in the worked example
Reason: Internal consistency fix — choosing the derived-vs-precondition framing follows from K.μ⁻'s own constructive-precondition logic already stated in the ASN; no external channel determines it.
