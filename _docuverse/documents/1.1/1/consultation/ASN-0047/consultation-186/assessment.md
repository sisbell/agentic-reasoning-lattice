# Channel Assignment — ASN-0047 review-186

**Date:** 2026-05-31 23:42

```
## Issue 1: M-total "Typing note" is a use-site survival inventory
Reason: The fix is a pure prose-collapse: the identity (†), the "dom(M) ↦ E_doc" reading, and the M2 exception are all already stated in the ASN. No design intent or implementation evidence is needed to drop the per-result "Survives" restatements.

## Issue 2: K.μ⁺_L precondition restates a discharge it doesn't need
Reason: The fix relocates an already-present derivation — the CL-UNIQ-at-post-state argument lives in the verification matrix prose, and the unused equivalence can simply be dropped or moved. All material is internal to the ASN; no channels needed.
```
