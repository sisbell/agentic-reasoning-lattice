# Channel Assignment — ASN-0047 review-136

**Date:** 2026-05-19 22:46

```
## Issue 1: K.μ~ dependency chain Step (A) implicit dependency on content-side subspace preservation
Reason: Pure proof-structure issue. The fix requires restructuring the dependency chain so admissibility (i) + L14 directly derives subspace preservation, or restating Step (A)'s consumed conditions. Derivable from the ASN's own content (K.μ⁺ amendment, L14, admissibility clauses); no design intent or implementation evidence needed.
```

```
## Issue 2: K.μ~ "preservation" framing in verification matrix is tautological for S3★
Reason: Internal presentation issue about matrix cell labels. The distinction between "stipulated by admissibility (i)" and "preserved via decomposition" is a proof-presentation refinement traceable from K.μ~'s own definition. No external channels needed.
```

```
## Issue 3: Missing worked example for K.δ k=0
Reason: Constructive task — build a concrete trace exercising FrontierEquivalence + T10a.6 dispatch against a chosen operand (e.g., second sibling document or second version). All structural ingredients (FrontierEquivalence lemma, T10a.6, K.δ-ID identities, Sub-allocator names) are already in the ASN; the example is derivable directly.
```

```
## Issue 4: Missing worked examples for two-step and three-step replacement composites
Reason: Constructive task — trace the two-step (prior-provenance) and three-step (first-time transcluded) forms against concrete tumblers. The replacement enumeration in *Elementary transitions* already specifies the precondition/discharge structure for each form; the worked examples are derivable from the ASN's own coupling definitions (J1★, J1'★) and P2's persistence of R.
```

```
## Issue 5: K.δ-ID identities framed as new properties in the table rather than as derived consequences
Reason: Pure table-organization issue. The ASN already states K.δ-ID identities are derived from TA5(c)/(d) + T4b's parent projection; the fix is presentational (separate subsection or per-row derivation tag). No external channels needed.
```
