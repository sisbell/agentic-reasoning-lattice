# Channel Assignment — ASN-0047 review-195

**Date:** 2026-06-01 01:33

## Issue 1: K.μ~ admissibility clause (i) under-specifies the invariant set its own derivations consume
Reason: Internal fix — the required invariant set (adding S8-fin and D-SEQ★, or stating "full per-state package") is determined entirely by what K.μ~-FIX, Necessity, and Sufficiency already consume within the ASN. No design intent or implementation evidence is at issue.

## Issue 2: Non-advancing musing on an unused decomposition in S8★
Reason: Internal fix — deleting a speculative clause that the existence-postcondition proof never consumes is derivable from the ASN's own discharge structure.

## Issue 3: K.δ "Subsumption" paragraph carries a use-site inventory and forward-defer in a specification slot
Reason: Internal fix — the operative statement (K.δ on a document enters e into E_doc with M'(e) = ∅) is already present in the adjacent frame split; reducing the paragraph is a local editorial rewrite.
