# Channel Assignment — ASN-0047 review-125

**Date:** 2026-05-19 18:36

## Issue 1: Incorrect logical relationship for L14a supersession
Reason: Pure editorial fix to a table cell. The body text already uses correct "supersede" wording, and S3★ + CL-OWN permitting link-subspace V→I mappings (which L14a forbids) is fully documented in the ASN — the supersession relationship is internally derivable.

## Issue 2: K.μ⁻ admissible contraction shape proof glosses depth/inner-1s preservation
Reason: The missing step is S8-depth preservation under restriction, which is already documented in the Class (a) verification matrix's K.μ⁻ row ("restriction preserves all three" for S8a/S8-depth/S8-fin). The fix consists of citing this existing preservation argument inline at the K.μ⁻ admissible contraction shape proof.

## Issue 3: Worked example "Step 2: K.α" freshness discharge for cross-document case
Reason: The Cross-document disjointness chain lemma is stated in the ASN with Case A explicitly covering prefix-comparable pairs (e₁ ≺ e₂); the fix is correctly citing Case A's length-comparison argument at the divergence position, which is fully derivable from the lemma's existing statement and the worked example's tumbler values.

## Issue 4: Implicit step in P7a proof — content-subspace V-position derivation
Reason: The temporal positioning fix is internal — the K.μ⁺ amendment's content-subspace restriction is defined in this ASN, and the composite structure (K.α before K.μ⁺ with J0 satisfied at the composite endpoint) is captured in ValidComposite★. The clarification about when v is created and which mechanism forces subspace(v) = s_C is derivable from these existing definitions.
