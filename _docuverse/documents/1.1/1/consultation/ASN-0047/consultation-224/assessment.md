# Channel Assignment — ASN-0047 review-224

**Date:** 2026-06-01 06:33

## Issue 1: S8★ contradicts itself about S8 condition (c)
Reason: Internal fix — the ASN already establishes that the content-subspace route applies ASN-0036's S8 directly (range in dom(C), S3 holds, so uniqueness (c) is inherited) while the link-subspace route uses the trivial length-1 decomposition (omitting (c)); scoping the (c)-drop to the link subspace only is derivable from the existing text.

## Issue 2: Defensive consistency-restatement in the K.μ~ decomposition (Step B.3)
Reason: Internal fix — purely editorial deletion/compression; B.1 and B.2 already establish S3★(Σ') and the composite frame fixes dom(C')/dom(L'), so no external evidence is needed.

## Issue 3: Open question references the superseded non-star invariants
Reason: Internal fix — the ASN itself supersedes D-CTG/D-MIN/D-SEQ with the starred forms, so reframing the open question against D-CTG★/D-MIN★/D-SEQ★ requires only the ASN's own content.
