# Channel Assignment — ASN-0047 review-198

**Date:** 2026-06-01 02:05

## Issue 1: Essay-length cells contradict the matrix's stated "navigational index" purpose
Reason: Purely editorial restructuring — reduce K.μ~ cells to one-line pointers and relocate derivation text to the existing per-invariant prose. The fix is derivable from the ASN's own content and convention.

## Issue 2: Meta-prose deciding which rows receive prose
Reason: Deletion of self-referential prose-allocation commentary; no design intent or implementation evidence bears on whether to remove a sentence about the document's own structure.

## Issue 3: Single-home navigation prose in the K.δ catalogue
Reason: Trim a navigation-justifying clause to the bare forward pointer; entirely internal editorial fix with no semantic content at stake.

## Issue 4: Deferral redundancy around S4 and the distinctness corollaries
Reason: Remove one side of a reciprocal pointer; the scoping fact (S4 is content-only, entity/link distinctness handled in corollaries) is already present in the ASN and needs no external input.

## Issue 5: The identity dom(M) = E_doc is load-bearing but not enrolled as a tracked invariant
Reason: The choice between enrolling (†) as a per-state invariant versus declaring it a definitional consequence of K.δ's effect and the default-value convention is derivable from the ASN's own K.δ catalogue and Bridging lemma — no design intent or implementation evidence is required.
