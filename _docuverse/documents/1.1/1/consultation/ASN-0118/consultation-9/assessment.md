# Channel Assignment — ASN-0118 review-9

**Date:** 2026-06-08 22:38

## Issue 1: Provenance dichotomy misses "range-new but already recorded"
Reason: The third branch (range-new yet already in `Σ.R` via prior contraction + P2 permanence) is built entirely from machinery the ASN already cites — K.μ⁻ removal-from-range, P2 provenance permanence, and the J1★/J1'★ couplings — so restating J1★'s effect as a membership requirement satisfiable by P2 is derivable from the ASN's own content. No design-intent or implementation evidence is needed.

## Issue 2: Ordinal-level required as input but no consumer in the derivation
Reason: Whether ordinal-level is formally load-bearing is answerable internally (content-residence + S8-depth already supply single-subspace and single-depth), but deciding between *dropping* it and keeping it as a *normalizing convention* turns on whether the implementation actually constrains or normalizes COPY's input spans — Gregory's evidence.
Gregory question: When udanax-green processes a COPY (transclusion) spec, does it require or normalize the source span's action point to sit at the deepest component (ordinal-level), or does it accept spans whose action point is above the last component and resolve them anyway?
