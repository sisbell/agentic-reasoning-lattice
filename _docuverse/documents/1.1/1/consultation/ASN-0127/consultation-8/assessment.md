# Channel Assignment — ASN-0127 review-8

**Date:** 2026-06-10 01:34

## Issue 1: The query-region symbol collides with the provenance component of Σ, and is named inconsistently
Reason: This is a purely notational symbol-collision fix internal to the note — renaming the query region from `R` to `W` (already used for the identical concept in the discovery section) to avoid clashing with the provenance component of `Σ = (C, L, E, M, R)`. It depends only on the note's own symbol bindings, not on design intent or implementation behavior.
