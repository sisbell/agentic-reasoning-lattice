# Channel Assignment — ASN-0099 review-25

**Date:** 2026-05-27 01:40

## Issue 1: Reused name `d_c` for two different hypothetical documents
Reason: Pure renaming issue. The fix is a mechanical disambiguation between the F10 version-extension document and Query 10's sibling document, derivable from the ASN's own structure.

## Issue 2: F4 weakening-direction discharge has subtle self-reference that the framing only partially resolves
Reason: Restructuring an argument that already exists in the ASN. The realizability content is present; the request is presentational reordering to lead with realizability rather than meta-level framing. Internal.

## Issue 3: A1's closed-world reading is grounded interpretively rather than directly
Reason: The ASN already cites both Nelson (design intent) and Gregory (implementation evidence) in A1's derivation. The reviewer asks for a presentational choice between (a) citing LP12a and arguing from frame-absence, or (b) acknowledging the convention as interpretive. Both options use content already in the ASN.

## Issue 4: F2/F3 verification in the worked example tests the spec against itself rather than against an implementation
Reason: Purely a reframing of verification language to make the implementation/spec obligation distinction explicit. The substance is already in the ASN; only phrasing needs adjustment.

## Issue 5: F9★ is in the claims table but never explicitly verified in the worked example
Reason: Either add a K.μ-only multi-step verification (derivable from operations already exercised in the ASN) or note the F9★/F9★-cor relationship explicitly. Internal.

## Issue 6: F1's status in the claims table is "introduced" but it functions as a definition
Reason: Pure table-labeling consistency fix. The redundant entries for `matches(a, I, Σ)` and F1 can be reconciled by inspection of the ASN's own usage.
