# Channel Assignment — ASN-0108 review-58

**Date:** 2026-06-13 13:34

## Issue 1: "least-covered-I-address key" mislabels what the key actually reads, and omits the tiebreaker it requires
Reason: Internal. The divergence between the note's a-priori type-including slice and udanax-green's matched-slot key is already acknowledged in the note, so this is not a re-consultation of Gregory's evidence. The relabeling ("tumbler" not "I-address"; type-classifier address when from/to empty) follows from ASN-0043 facts the note already cites — the `Endset = 𝒫_fin(Span)` definition, L3 constraining only `e₃`, and L9's ghost-permitted type addresses outside `dom(Σ.C)` — and the mandatory address tiebreaker is stated in the note itself, so folding it into the name/definition is an internal-consistency edit. The reviewer frames it as precision repair to illustrative prose, not a proof gap.

## Issue 2: Gregory's-key permanence asserted three ways in one bullet (anti-bloat)
Reason: Internal. Pure anti-bloat — sentence (b) restates (a)'s conclusion with no intervening step, so dropping it requires only the note's own argument structure; permanence remains carried by (a) and reused by name in W5/W8.
