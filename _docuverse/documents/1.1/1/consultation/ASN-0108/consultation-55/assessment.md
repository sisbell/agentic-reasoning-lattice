# Channel Assignment — ASN-0108 review-55

**Date:** 2026-06-13 12:13

## Issue 1: The content-position foil key is multi-valued under the adopted discoverability reading
Reason: The fix is internal. The content-position key is an explicit foil that *neither* channel adopts — udanax-green "emphatically does not" key on position, and it is not the design intent — so neither Nelson nor Gregory has standing to define it. The note already resolves the identical multiplicity for Gregory's key by selecting the *least* element (and cites the `onlinklist` dedup as that resolution); the author need only apply the parallel rule (least matched V-position in `d_q`) or scope the foil's general claims to unique-endpoint links, both supplied by the review and derivable from the note's existing "least"-selection template.

## Issue 2: W8 draws the computability-vs-stability distinction twice
Reason: The fix is internal — a pure editorial deduplication of the computability-vs-comparison-movement distinction between W8's preamble and body, requiring no design intent or implementation evidence. The definitions of computability and value-totality already stand on their own; dropping or parenthesizing the preamble's redundant cross-reference to W5's triad is a self-contained prose edit.
