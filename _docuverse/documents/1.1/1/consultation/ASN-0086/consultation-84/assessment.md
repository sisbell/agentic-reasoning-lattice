# Channel Assignment — ASN-0086 review-84

**Date:** 2026-05-31 15:27

## Issue 1: Allocator-depth apparatus re-derives ASN-0093 structure for a worked-sketch aside
Reason: The fix is internal — it removes apparatus the review confirms is consumed only by the worked-sketch structural witness, replacing it with a direct ASN-0093 FirstEmission/anchor citation already present in the note. No design intent or implementation evidence is at stake; the only judgment is whether the distinction is load-bearing downstream, which is answerable from the ASN's own claim-dependency structure.

## Issue 2: substrate-conforming layer Definition re-lists ~40 invariants no proof consumes item-by-item
Reason: The fix is internal — R7a's own proof discharges preservation wholesale and singles out catalog (b)'s ChainMembershipForOrigin as the load-bearing item, so the exhaustive list can be collapsed to the blanket reference using only material already in the note.

## Issue 3: "single derivation site" announcement and dual forward references are document-ordering meta-prose
Reason: The fix is internal — deleting a sentence that describes document structure and rephrasing two upstream citations requires no external input; the lemma's content is unchanged.

## Issue 4: Consequences typology key is document-structure meta-prose
Reason: The fix is internal — removing the consumer-list sentence while keeping the tag legend is a pure prose deletion derivable from the ASN alone.

## Issue 5: R7a carries defensive meta-prose re-arguing its own precondition
Reason: The fix is internal — both passages re-argue a precondition the claim already states by hypothesis; compressing them to the single fact the proof uses (conforming ⟹ L12/L12a/L-fin/S0/S1) is a self-contained editorial change.

## Issue 6: Duplicated import of the same ASN-0093 facts
Reason: The fix is internal — consolidating the L0 and SC-NEQ imports to one site and having R4 and FreshLinkKeyDisjointness cite it is a restructuring of material already present, requiring no design intent or implementation evidence.
