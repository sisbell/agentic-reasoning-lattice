# Channel Assignment — ASN-0086 review-182

**Date:** 2026-06-01 11:33

## Issue 1: Implementation aside between a definition and R7a advances no part of R7a's reasoning
Reason: The fix is purely editorial — relocating or marking a non-load-bearing implementation aside. The ASN already states R7a holds for arbitrary `m ≥ 1` and that the `m = 1` observation feeds no premise, so the decision to drop or demote it is internal.

## Issue 2: The relational-layer reduction corollary front-loads an R7a-applicability argument its core claim disclaims needing
Reason: The fix reorganizes the corollary's proof, moving the R7a-applicability setup to the single "composite extensions" sentence it serves. The proof itself already identifies which sentence consumes Paragraph 1, so the restructuring is derivable from the ASN's own text.
