# Channel Assignment — ASN-0086 review-131

**Date:** 2026-06-01 00:55

## Issue 1: WP Case 2 "Direct K.λ callers" paragraph elaborates regimes the note's operations exclude
Reason: The fix deletes out-of-vocabulary material and replaces it with a one-sentence scoping note; the disciplines relied on (unit-depth, sole-R-producer) are already defined in the note, so this is purely internal editing.

## Issue 2: R6b's "audit slice, not active subset" point is restated four-plus times
Reason: Deduplication of prose the note already contains in multiple places — consolidating to one canonical statement with pointers is fully derivable from the ASN's own text.

## Issue 3: R0's "conjunct-by-conjunct" invariant discharge omits L5 and L6
Reason: The review supplies the invariant names (L5 EndsetSetSemantics, L6 SlotDistinction) and grants they are trivially preserved; the added sentence follows from the emitted value's standard-triple shape `(F, G, K)` and the `Link` type, requiring no external evidence.

## Issue 4: R7a Decomposition Example illustrates a case no operation in this note produces
Reason: The fix deletes or compresses a worked appendix; the reduction corollary establishing the n=1 collapse is already in the note, so the scope decision is internal.
