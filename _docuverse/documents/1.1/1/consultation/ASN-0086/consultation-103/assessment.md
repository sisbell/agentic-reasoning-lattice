# Channel Assignment — ASN-0086 review-103

**Date:** 2026-05-31 20:01

## Issue 1: The P1-necessity argument (fresh emitter / L12a) is stated three times
Reason: Pure consolidation of a duplicated derivation already present in the ASN; no design intent or implementation evidence is needed to pick the canonical home and replace the others with citations.

## Issue 2: The crafted-span witness `{(d, δ(1, #d))}` is exhibited three times
Reason: The example is already introduced and named ("regime-(ii) crafted-span witness") within the ASN; collapsing to a single introduction plus back-references is purely internal editing.

## Issue 3: The active/audit distinction is framed twice in identical terms
Reason: Both framings are in the ASN and say the same thing; dropping the section-opener restatement requires only the ASN's own content.

## Issue 4: Nullify's P0/P1/P2 gating taxonomy is re-explained inside the wp
Reason: The taxonomy is fully established in Definition — Nullify and the per-conjunct necessity argument; deleting the redundant closing paragraph is derivable from the ASN alone.

## Issue 5: Definition — Nullified restates its own A_rel restriction rationale twice
Reason: The restriction and the Convention RetractionDirectionality citation both already exist in the ASN; merging the two statements is internal de-duplication.

## Issue 6: R6a precondition contains a redundant conjunct
Reason: `nullified(Σ)`'s set-builder restriction `a ∈ A_rel^Σ` is defined within the ASN, so the binder's redundancy is provable from the definition alone.
