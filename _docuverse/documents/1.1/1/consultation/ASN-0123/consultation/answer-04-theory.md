## Question 4 [theory]

> How must successive forks extend the ancestry encoded in identity, so that a version of a version still carries the full chain of descent in its own name?

I'll answer this question about how forking extends ancestry through the tumbler address. The curated sources cover this well, but let me verify the key mechanism pages on forking and the Document field subdivision, since the question is precisely about address structure.

`★ Insight ─────────────────────────────────────`
The question hinges on a structural property of forking: new digits are added *to the right, under* the parent, and existing addresses *never change*. That combination is what makes the parent's full address survive as a *prefix* of the child's — so descent is cumulative, not overwritten.
`─────────────────────────────────────────────────`

Let me look at the original pages for the forking mechanism and the Document-field subdivision.


name: Read
input:
  file_path: page-187.png
system<system-reminder>Warning: the file you are trying to read (page-187.png) does not exist. Verify you are using the correct absolute path. If the file does in fact exist, try again with a different (e.g. absolute) path.</system-reminder>
