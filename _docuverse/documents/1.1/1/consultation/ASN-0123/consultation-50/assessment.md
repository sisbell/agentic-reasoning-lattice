# Channel Assignment — ASN-0123 review-50

**Date:** 2026-06-13 22:29

## Issue 1: nextd's B6-validity justification misattributes `zeros(pfx(π)) = 1` to O1a
Reason: Pure citation correction internal to the ASN — O1a's `≤ 1` ceiling and the account-tier hypothesis (with Account-entity occupancy from PS incumbency) are both already present in the note; the fix only re-attributes the equality to its real source. No design intent or implementation evidence bears on which axiom supplies a bound.

## Issue 2: the node-tier-exclusion rationale is restated across five sites, with V0 carrying a worked-out excluded case
Reason: Prose consolidation derivable from the ASN alone — P-tier already fixes the domain restriction and V-WF/V9 already discharge the load-bearing obligations; trimming V0's redundant excluded-case computation to a P-tier citation is an editorial move requiring no external facts.
