# Channel Assignment — ASN-0123 review-38

**Date:** 2026-06-13 17:39

## Issue 1: `nextv`'s B2-non-citation footnote is reviewer-defense, not construction, and echoes in three further sites
Reason: Internal — the fix is pure deletion of prose that justifies a *non-citation*. The replacement derivation (VN-B1 + S0 case split; GlobalUniqueness in V0) is already proved and stated within the ASN, so nothing about ASN-0040's B2 content, Nelson's intent, or the implementation is consulted.

## Issue 2: V0's count argument analyzes a case P-tier excludes, duplicating the P-tier comment
Reason: Internal — deduplication of an argument already present at P-tier. The domain restriction, the two in-domain branches, and their one-K.δ-each counts are all fixed by the ASN's own preconditions and Effect clause; no design intent or implementation evidence is in question.

## Issue 3: V9 preamble editorializes around the (load-bearing) O5(ii) derivation
Reason: Internal — the load-bearing O5(i)/(ii) derivation stays verbatim and the fix only strips proof-method framing around it. The technical content (stream form `[pfx(π),0,k]`, Z-mono maximality, ASN-0047 document-allocator structure) is already cited in-ASN, so neither channel is needed.
