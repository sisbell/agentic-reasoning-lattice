# Channel Assignment — ASN-0123 review-16

**Date:** 2026-06-13 02:20

## Issue 1: The cross-owner branch — the ASN's most novel postconditions — is never exercised against a concrete instance
Reason: The required fix is a pure instantiation of results already fully derived in the ASN — V9 severance, V9/V8 ownership, V0/V1 single-mint and zero footprint, V9w dual provenance — over the namespace apparatus already defined (the `(account, 2)` document-creation namespace, depth-2 separator giving `v = 1.2.0.k`). Constructing the worked instance requires only plugging the reviewer's supplied tumblers into proved theorems; no design intent or implementation fact is in question (the cross-owner meaning [LM 2/32–2/40] and the udanax-green correspondence in deviation 4 are already recorded), so the fix is internal.
