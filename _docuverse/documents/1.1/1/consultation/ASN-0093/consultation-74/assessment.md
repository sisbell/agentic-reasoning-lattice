# Channel Assignment — ASN-0093 review-74

**Date:** 2026-05-31 13:16

## Issue 1: Inconsistent naming of the document-prefix projection for links (`home` vs `origin`)
Reason: Pure internal-consistency fix — the note already uses `origin(·)` everywhere including its own L1a restatement, so replacing the lone `home(ℓ)` is derivable from the ASN's own terminology without any external channel.

## Issue 2: SD matrix row restates "covers both allocation transitions" in both cells
Reason: Pure editorial deduplication — the K.α cell already states the single argument covers both transitions, so trimming the K.λ cell to the bare pointer is derivable from the ASN's own text.
