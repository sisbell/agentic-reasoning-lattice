# Channel Assignment — ASN-0093 review-61

**Date:** 2026-05-31 11:07

## Issue 1: Base case misclassifies M1 as a domain-quantified invariant
Reason: Internal fix. M1's definition `(A Σ → Σ' :: dom(M) ⊆ dom(M'))` is stated in the ASN itself, as are C0/L12's transition-quantified forms; the misclassification and its correction are derivable from the note's own text.

## Issue 2: Anchor-exclusion paragraph cites C1 for a `#E ≥ 2` claim that C1 does not make
Reason: Internal fix. Both C1 (`zeros(a) = 3`) and C1b (`#E(a) ≥ 2`) are defined within the ASN; the correct citation is determined entirely by those definitions.
