# Channel Assignment — ASN-0093 review-19

**Date:** 2026-05-31 04:00

## Issue 1: C1b cites a nonexistent foundation claim (ASN-0036 S7c)
Reason: Internal. The review already establishes ASN-0036 has no S7c; the invariant is sound and discharged at K.α by its `#E(a) ≥ 2` precondition. The fix is a provenance relabel (parallel to L0's C-clause), derivable from the ASN alone.

## Issue 2: SubAllocatorAxiom.Exists buries its content under proof-discipline meta-prose
Reason: Internal. Reducing the axiom to its object-level statement plus a one-clause M1 corollary is a restructuring of material already present; M1 is independently proved from frame conditions within the ASN.

## Issue 3: "Earlier-draft note" is relocated revision history
Reason: Internal. Deleting revision-history narration and dropping "Replaces the former..." phrasing requires no external input; the derived lemmas already stand on their own where defined.

## Issue 4: Use-site inventories attached to definitions and per-chain disciplines
Reason: Internal. Removing "Consumed at / Consumed in" bookkeeping leaves each discipline's statement plus its ASN-0040 source intact — pure deletion, derivable from the ASN.

## Issue 5: Operation preconditions re-prove the freshness lemmas in full
Reason: Internal. Collapsing the inlined derivations to one-line citations of FirstEmissionFreshness and the matrix entries is deduplication of content already proved in those locations.

## Issue 6: "Design rationale for retaining M" is essay content in a state-definition slot
Reason: Internal. The substantive content (M partial, `dom(M)` = registered documents, `M(d)=∅`) is already specified above; collapsing the rationale to one sentence or relocating it is an editorial move requiring no external channel.
