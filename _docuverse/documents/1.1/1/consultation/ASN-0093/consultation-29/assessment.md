# Channel Assignment — ASN-0093 review-29

**Date:** 2026-05-31 05:58

## Issue 1: Non-circularity justification embedded in a proof
Reason: Pure deletion of a defensive proof-ordering sentence; the per-use citations of FirstEmission's structural form already carry the reasoning. Derivable from the ASN alone.

## Issue 2: Proof-hygiene meta-prose in the induction framing
Reason: Strip generic proof-hygiene reassurances, keep the substantive two-group split. Internal editorial fix needing no external channel.

## Issue 3: Properties Introduced table duplicates the discharge matrix
Reason: Reduce Source cells to origin tags and drop consumer lists; the discharge matrix already carries the per-transition reasoning. Fully internal deduplication.

## Issue 4: Dual-form provision with consumer enumeration
Reason: Collapse to the single (T10 anchor-incomparability) form the proofs actually consume and drop the consumer-choice clause. The note itself shows which form FirstEmissionFreshness and K.α/K.λ freshness use, so this is internal.

## Issue 5: `ValidAddress` reinvents `T4-valid`
Reason: Whether to use the foundation term `T4-valid` and drop the `ValidAddress` alias is settled by the foundation-ASN naming convention (ASN-0034/0043), verifiable from the spec corpus itself — no design-intent or implementation evidence needed.

## Issue 6: Worked-example summary inventory
Reason: Delete or compress the closing roll-call; the example body already verifies each invariant inline. Internal editorial.

## Issue 7: SequentialTransitionAxiom stated but never invoked
Reason: The axiom is already stated in the ASN and the ChainMembershipForOrigin induction over transition sequences is what consumes it; adding the citation is internal cross-referencing within the note.
