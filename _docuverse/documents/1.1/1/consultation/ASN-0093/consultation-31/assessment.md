# Channel Assignment — ASN-0093 review-31

**Date:** 2026-05-31 06:16

## Issue 1: Duplicate statement — chain-indexed properties need no per-transition discharge
Reason: Pure deduplication of two paragraphs asserting the same state-independence claim; the fix is choosing one home and deleting the other, derivable from the ASN's own structure.

## Issue 2: Dependency-inventory preamble in FirstEmissionFreshness
Reason: The preamble redundantly lists premises the four-step proof already cites inline; deleting it is an internal edit needing no external channel.

## Issue 3: Parameter-semantics paragraph duplicates the binding preconditions
Reason: The pinning formulas appear verbatim in the K.α/K.λ precondition blocks; collapsing the restatement is a self-contained editorial fix.

## Issue 4: ChainDiscipline use-site clause and narrative overlap
Reason: Removing the "used downstream" use-site clause and the triplicated first-emission exhibition is internal to the ASN's own lemma structure.

## Issue 5: Properties Introduced table carries full derivations instead of index hooks
Reason: Collapsing each Source cell to its premises/citation list is a formatting deduplication against the body proofs already present in the ASN.

## Issue 6: TA5a anchor-admissibility argument restated three times
Reason: Consolidating the identical per-step admissibility derivation into FirstEmission and citing it from C1c/L1c is internal; the chain-specific extra step (L1c's `inc(b_C(d), 0)`) is already in the ASN.

## Issue 7: "K.σ admissibility scope" restates the precondition
Reason: The subsection verbatim restates K.σ's precondition with no added constraint; deletion is purely internal.
