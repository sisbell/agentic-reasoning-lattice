# Channel Assignment — ASN-0121 review-39

**Date:** 2026-06-11 17:27

## Issue 1: FL-JUNK's preamble asserts unconditional insensitivity that the lemma's own hypothesis exists to deny
Reason: Internal fix. The corrected gloss is fully derivable from the ASN's own machinery — `nullified` is defined from other links' stored values via `L_R^Σ`, and FL-WP(c) already exhibits the exact interference route the hypothesis excludes; the revision is a one-clause qualification aligning prose with the lemma's stated hypothesis.

## Issue 2: FL-RET's non-obstruction parenthetical cites FL-JUNK, which cannot be instantiated there — and the claim itself needs care for retracted retractors
Reason: Internal fix. The retracted-retractor behavior follows from definitions already in the ASN's dependency cone — `L_R^Σ` is selected from `dom(Σ.L)` with no addressability filter, so a nullified retraction tuple keeps nullifying its targets (R6b, ASN-0086, cited by the review) — and the per-link basis for non-obstruction (others' membership conjuncts never read `a`'s status) plus the FL-WP(c) rewording are textual corrections requiring no design-intent or implementation evidence.
