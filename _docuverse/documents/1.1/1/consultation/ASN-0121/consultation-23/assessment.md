# Channel Assignment — ASN-0121 review-23

**Date:** 2026-06-09 02:45

## Issue 1: Retraction-class membership written as coverage-in-endset-class (type confusion with ASN-0086's `[·]`)
Reason: Pure notational/type-correctness fix derivable from the ASN and its cited foundation ASN-0086 — the corrected form (`coverage(Σ'.L(ℓ).e₃) = coverage(R)` or `Σ'.L(ℓ).e₃ ∈ [R]`) is exactly what ASN-0086 already defines, so no design intent or implementation evidence is needed.
