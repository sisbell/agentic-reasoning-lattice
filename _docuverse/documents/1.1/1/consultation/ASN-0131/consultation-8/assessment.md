# Channel Assignment — ASN-0131 review-8

**Date:** 2026-06-13 08:25

## Issue 1: The "full taxonomy" claims retraction is the sole pair-remover, contradicting the ASN's own deletion result
Reason: Internal precision/self-consistency fix — no channel needed. The narrower remove-clause ("via the addressable population / the `Σ.L` channel") follows from facts already cited in the ASN: `dom(Σ.L)` only grows (L12) and `nullified` only grows (R6a), so `addressable` can shrink only under retraction. The link-subspace-only `K.μ⁻` carve-out is derivable from the ASN's own RE-CWP (retaining all content positions gives `Δ = ∅`, hence answer unchanged) together with the per-subspace `K.μ⁻` semantics the review has already quoted from the cited ASN-0047 (`n'_{s_C} = n_{s_C}`, `n'_{s_L} < n_{s_L}`); both the model (`K.μ⁻[d, R]` over an arbitrary V-position retention set) and the subspace structure (`s_C`/`s_L`, already handled for `K.μ⁺_L`) are present in the note.
