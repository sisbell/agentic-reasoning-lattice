# Channel Assignment — ASN-0131 review-25

**Date:** 2026-06-13 18:55

## Issue 1: Claims-table rows carry argument, caveats, and proof-structure — not claims
Reason: Purely editorial restructuring — the grounding, distinctions, and conditionality being trimmed from RE-EDIT/RE-RET/RE-UDIST/RE-CWP are already proven in the body; reducing table rows to claim-statements moves no semantic content. The sub-point on "link-subspace-only `K.μ⁻`" is derivable from the ASN's own `W ⊆ s_C` restriction (dropped `s_L` positions are not in `W`, so the image is unchanged — the same equality-under-`W ⊆ s_C` argument the body already runs for `K.μ⁺_L`). No design intent or implementation evidence is needed.

## Issue 2: Decidability paragraph re-walks foundation span-membership and double-cites S8-fin
Reason: Purely editorial condensation — every fact (T2 interval-membership, S8-fin finiteness, L-fin, `nullified` computability per ASN-0086) is already cited in the ASN; collapsing the re-derivation to a citation-level statement and dropping the duplicate S8-fin cite adds no new content and requires no external channel.
