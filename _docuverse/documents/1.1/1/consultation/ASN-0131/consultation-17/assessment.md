# Channel Assignment — ASN-0131 review-17

**Date:** 2026-06-13 15:42

## Issue 1: Defensive-justification accretion in the worked instance's type-endset argument
Reason: Internal — the worked instance is a constructed example, and the field-agreement proof plus the facts the three asides reference (T7, Prefix/T4 from ASN-0034, coverage-vs-membership) are all already present in the ASN; deciding whether to keep the single load-bearing proof or stipulate `coverage(e₃) ∩ dom(Σ.C) = ∅` parallel to `e₂′`/`e₃′` requires no design intent or implementation evidence.

## Issue 2: Forward-reference accretion around the content-subspace restriction
Reason: Internal — the semantic payload (link-subspace V-positions resolve to an image in `dom(Σ.L)` via S3★, ASN-0047) and the content-subspace rationale (`I ⊆ dom(Σ.C)`) are both already stated in the note; removing the "just below" pointer and relocating the one sentence into OQ7 is pure restructuring of existing content.

## Issue 3: RE-EDIT states retraction as unconditional removal; RE-RET makes it conditional
Reason: Internal — the conditionality to be propagated into RE-EDIT is exactly what RE-RET and the "Under retraction" prose already establish, and the required replacement text is supplied; the edit only aligns the summary table with the body.
