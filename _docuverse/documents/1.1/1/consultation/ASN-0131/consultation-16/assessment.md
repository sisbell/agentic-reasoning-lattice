# Channel Assignment — ASN-0131 review-16

**Date:** 2026-06-13 15:14

## Issue 1: RE-RET's "unconditional" core result is falsified by retraction of a Θ-typed retraction link
Reason: Internal fix. This is a formal correction the reviewer has fully diagnosed and salvaged: the sole-bearer forward direction must be conditioned on `coverage(Θ) ∩ I = ∅` (a hypothesis the note already holds in hand), and the `b ∉ nullified(Σ')` assertion needs a citation to ASN-0086 (wp Case 2's third conjunct / R0a). Both moves are derivable from the note's own reasoning and its already-cited dependency ASN-0086 (R6a/R6b/R6c, R0a, R-Scope) — no design-intent question (the note's own settled position is that ASN-0086 does *not* furnish Θ-content disjointness, so the touching-Θ case is formally live) and no udanax-green evidence is required.

## Issue 2: Use-site inventory in "Why confine W to the content subspace?"
Reason: Internal fix. Pure anti-bloat deletion of a downstream-consumer enumeration; the substantive justification (S3★: `s_L` positions map to link addresses, so the `s_C` restriction keeps "content-image" accurate) and the OQ7 pointer already stand on their own in the paragraph.

## Issue 3: Redundant "full taxonomy" recap paragraph in Stability
Reason: Internal fix. Pure anti-bloat removal of a recap that triplicates the per-motion prose and the RE-EDIT claim row, plus collapsing the thrice-stated RE-WHOLE/OQ1 deferral to one prose mention and the claim row — both editorial, with the derivation prose and canonical claim row already carrying the content.
