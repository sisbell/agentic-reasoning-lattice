# Channel Assignment — ASN-0131 review-30

**Date:** 2026-06-13 20:42

## Issue 1: "any fresh K.λ output is addressable" is false for self-targeting retractions
Reason: The fix is a formal scoping correction fully determined by ASN-0086's already-cited `wp` Case 2 — the second conjunct and the `Nullify` self-emit permission (P-tgt) — which the note already reasons over (it invokes the third conjunct in the very passage). Restricting the lemma to non-self-targeting emissions (or invoking both conjuncts) is internal; no design intent or implementation evidence is required.

## Issue 2: The standing assumption is grounded in the wrong layer
Reason: The fix relocates the discipline onto ASN-0047's transition vocabulary and makes the `Σ.L`-framing transfer explicit using only facts the note already cites (K.μ/K.δ/K.ρ/K.α frame `Σ.L`; M2 empties arrangements over ASN-0086's K.σ∪K.α∪K.λ layer). All materials are present in the note's own stability section, so the fix is internal.

## Issue 3: Duplicated rationale (anti-bloat)
Reason: Pure editorial collapse of rationale repeated within the note (wide-retraction justification twice; content-identity invariance three times) into single statements with invocations at the use sites — derivable from the ASN alone.
