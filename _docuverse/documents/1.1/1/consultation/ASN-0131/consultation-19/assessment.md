# Channel Assignment — ASN-0131 review-19

**Date:** 2026-06-13 16:49

## Issue 1: K.λ emission is named as answer-moving but never derived
Reason: Internal. The required derivation is the population-grow mirror of the note's own retraction-emitter argument: a fresh `ℓ_new ∈ dom(Σ'.L)` is addressable in `Σ'` by the same discipline/R0a (flat-antichain) reasoning already deployed for `b`, frame `M' = M` holds the image fixed, and `touch_W` on `Σ.L(ℓ_new).eᵢ` adds `(i, eᵢ)` or leaves the answer fixed — the E-MONO/F-LAMBDA (ASN-0127) analogue. Every ingredient is already in the note or its cited foundations; this is a completeness gap in the note's own exhaustiveness argument, not a design-intent or implementation-evidence question.

## Issue 2: Speculative downstream-layer mechanics in the retraction Θ discussion
Reason: Internal. The fix is a pure anti-bloat trim — drop the `s_R`-seating "e.g." and "must furnish … placement being that layer's mechanics" speculation (which the note itself disclaims as "not ours to impose here"), keep the load-bearing wide-span observation, and state the bare conditional `coverage(Θ) ∩ dom(Σ.C) = ∅` plus the OQ6 pointer. The review has already adjudicated what is load-bearing versus speculative, so no design intent or implementation evidence is required to execute it.
