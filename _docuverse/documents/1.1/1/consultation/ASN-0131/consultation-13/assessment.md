# Channel Assignment — ASN-0131 review-13

**Date:** 2026-06-13 13:47

## Issue 1: Claims-table cells RE-EDIT and RE-RET are full essays, not claim statements
Reason: Internal — the fix compresses two claims-table cells down to their claim statements, leaving the (already-written) derivation in the prose sections. Both the full content and the target distillation are present in the ASN; no design intent or implementation evidence is in play.

## Issue 2: Defensive over-justification in the decidability paragraph
Reason: Internal — the core decidability argument (finite image via S8-fin, span-by-span membership via T2, computable `nullified`) is already in the note; the task is only to drop the meta-prose defending against the interval-union misreading. No channel needed.

## Issue 3: The link-subspace-region topic is deferred to the same future query at four sites
Reason: Internal — this is deduplication of redundant in-prose deferrals to Open Question 7, keeping one load-bearing pointer at the `W ⊆ s_C` payoff. All four sites and the open question already exist in the ASN; nothing external is consulted.

## Issue 4: The existence/discovery section restates ASN-0127's taxonomy before applying it
Reason: Internal — the recap being compressed is ASN-0127's cited taxonomy (cite, don't rebuild), and the RE-specific two-axis contribution that stays — including the designer's existence reading — is already written; the fix neither re-opens the designer's intent nor needs new implementation evidence.

## Issue 5: The symbol `R` is overloaded across adjacent stability claims
Reason: Internal — a pure notational disambiguation (rename one `R`, add a one-line note as was done for `Σ.R`); the review even supplies candidate names. Derivable from the ASN alone.
