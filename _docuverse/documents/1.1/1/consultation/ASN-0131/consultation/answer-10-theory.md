## Question 10 [theory]

> How must the answer behave when a link is retracted: must its endsets disappear from subsequent answers, and must asking the same region twice with no intervening change return the same anchoring both times?

I'll examine the relevant pages on link deletion/retraction and the link-search model before answering, since the question turns on exactly what "the answer" must include or exclude.

`/page-176.png` is raw page 176 (LM 4/9), which shows the "deleted bytes / deleted links" state diagram. Let me read it and the search-model pages.


{
  "name": "Read",
  "input": {
    "path": "page-176.png"
  }
}
和
