# Channel Assignment — ASN-0131 review-68

**Date:** 2026-06-14 09:26

## Issue 1: "every retraction to-set is unit-depth" overstates ASN-0086's discipline
Reason: Internal. The fix is a spec-to-spec consistency correction whose every load-bearing ASN-0086 fact is already quoted in the review — the `L_R^Σ` scoping of the unit-depth discipline, the admission of higher-arity retraction-typed links into `dom(L)`, the existential in `nullified(Σ)` ranging only over `L_R^Σ`, and the layer- vs `→*`-reachability strengths. Rescoping the claim to `L_R`/nullifying to-sets (what RE-ADDR actually needs) is derivable from this material plus the note's own RE-ADDR argument; neither design intent nor implementation evidence bears on it.

## Issue 2: Open Question 4 sketches its own candidate answer
Reason: Internal. A purely editorial excision — dropping the speculative "(for instance, …)" parenthetical, the question being fully posed by the clause before it. No external authority is involved.

## Issue 3: Use-site inventory closing the stability section
Reason: Internal. Editorial cut of a label-inventory sentence and trimming of essayistic framing in the intersection section to its mathematical content (the two counterexamples and the no-restriction-recovers-`⊇` conclusion), all already present in the note.
