# Channel Assignment — ASN-0131 review-96

**Date:** 2026-06-14 20:14

## Issue 1: Accreted, misattributed gloss on the R-Scope citation
Reason: Internal — the fix is a deletion. The review itself supplies what R-Scope states ("arity-independent: it holds regardless of `|Σ.L(a)|`") and diagnoses the gloss's error (it points at retractor `b`'s fixed arity-3 rather than the target's arity), and the bare citation already discharges the step. Removing the redundant parenthetical (optionally substituting "for `ℓ` of any arity") needs no design intent and no implementation evidence.

## Issue 2: Restatements within adjacent material
Reason: Internal — pure deduplication. Both occurrences of each fact are present in the ASN's own text; keeping one and dropping the echoed parenthetical requires only the note's own content, no external channel.
