# Channel Assignment — ASN-0116 review-54

**Date:** 2026-06-09 19:10

## Issue 1: Cross-section navigation and recap prose that does not advance reasoning
Reason: Purely editorial. The fix is to delete the named forward/back-reference framing and the closing recap, keeping the substantive clauses — no design intent or implementation evidence bears on which prose to cut; the reviewer has already pinpointed every target.

## Issue 2: Worked-example provenance citation names the wrong invariant
Reason: The correct invariant (P4★) and its applicability (`a_1 = M(d)(q_1)` lies in the content-subspace range of `M(d)` at a composite boundary) are already established in the ASN — the same section invokes P4★ for the analogous shifted-suffix records `(a_3, d), (a_4, d), (a_5, d)`. The fix is a citation swap derivable from the ASN's own content.
