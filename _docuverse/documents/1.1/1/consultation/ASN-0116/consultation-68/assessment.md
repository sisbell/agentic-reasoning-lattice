# Channel Assignment — ASN-0116 review-68

**Date:** 2026-06-11 00:34

## Issue 1: Categorical "not a superset" claim contradicts IP4's own case analysis
Reason: The fix is internal — IP4's own case analysis already establishes the correct conditional statement, and the revision is purely editorial (hedge or delete the categorical sentence to match the claim it introduces). No design intent or implementation evidence bears on a prose-consistency fix.

## Issue 2: J1★ discharged only for the operated document
Reason: The fix is internal — the review identifies the exact missing steps, and both are already present in the ASN's Frame: F-DOC gives `M'(d') = M(d')` for the `d' ≠ d` instances, and F-ENT gives `E'_doc = E_doc` for the quantifier domain. The revision just invokes them in the J1★ discharge.

## Issue 3: Duplicated restatements (anti-bloat)
Reason: The fix is internal — both duplications restate arguments already closed elsewhere in the ASN (the bridge's cross-subspace clause and IP6's containment-vs-emptiness point), so the revision is to replace restatements with citations. No external content is required.
