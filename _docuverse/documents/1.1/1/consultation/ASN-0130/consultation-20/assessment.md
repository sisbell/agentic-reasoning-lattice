# Channel Assignment — ASN-0130 review-20

**Date:** 2026-06-13 03:31

## Issue 1: PR1 claims permanence for a conjunct that de-registration withdraws without content change
Reason: Internal fix. Every fact the correction needs is already in the ASN — the three validation conjuncts (PR0), (iv)'s reading of the active slice `A_pdef^Σ` (PR0), de-registration's falsifying of (iv) without content change (PS2), evaluation keying on ever-registration rather than (iv)-currency (PR3), and the parallel caveat already written at PR5a's permanence paragraph. The fix is to mirror PR5a into PR1 and rescope the gloss; it deliberately preserves rather than resolves OpenQ3, so no design-intent (Nelson) or implementation-evidence (Gregory) input is required.
