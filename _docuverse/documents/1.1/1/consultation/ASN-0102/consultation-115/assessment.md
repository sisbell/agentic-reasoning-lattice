# Channel Assignment — ASN-0102 review-115

**Date:** 2026-06-08 06:26

## Issue 1: X14's second paragraph is reviser-drift around the atomicity modeling choice
Reason: The fix is purely editorial — trimming meta-prose to its kernel — and requires neither design intent nor implementation evidence. Whether `Σ'` is composite-reachable and whether the excluded orderings fail are both already settled within X14 itself; the revision only decides how much of that to retain, which is derivable from the ASN's own content.
