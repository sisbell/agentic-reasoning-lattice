# Channel Assignment — ASN-0119 review-14

**Date:** 2026-06-09 17:10

## Issue 1: LP11 is invoked by mischaracterizing its hypothesis; REARRANGE is never established as a K.μ~ transition
Reason: This is a formal proof-structure repair, fully internal. Both repair routes draw only on material already imported into the note: route (a) discharges K.μ~ admissibility (i)–(v) from facts the note already establishes (the "`V_{s_C}(d)` unchanged as a set" observation and "π maps each subspace onto itself" from the S3★ proof), with non-triviality handled as a logical caveat; route (b) inlines LP11's three-line proof from P2 plus the already-cited LP2/LP3. No design intent or implementation evidence is required to choose or execute either.

## Issue 2: Forward-reference accretion and essay content in structural slots
Reason: Pure editorial cleanup — deleting two deferral pointers and a provenance meta-line, and compressing the P7a/P7c table cells whose content already lives in the body. Nothing turns on design intent or implementation behavior.

## Issue 3: the "first position" boundary is handled only implicitly
Reason: The note's own equations already cover the empty-left-exterior case — R-EXT degenerates to an empty quantifier and π remains a bijection by P2/P3 — so the confirming line is derivable from the ASN's existing machinery without any external channel.
