# Channel Assignment — ASN-0042 review-61

**Date:** 2026-05-14 13:18

```
## Issue 1: "Structural forcing" claim in O10 over-generalized
Reason: The fix is derivable from the ASN alone — the reviewer has identified a logical gap (pre-delegation Bop calls by the O5-authorized covering ancestor can populate S(pfx(π_B), 2) before delegation) and supplied three concrete reframing options (a/b/c). The choice among them is a proof-presentation decision, and the Unilateral O10★ statement itself is unaffected since it holds in both branches.
```

```
## Issue 2: Minor — worked example state labeling
Reason: Pure notational/editorial issue internal to the ASN — either explicitly subscript each transition or add a one-line convention note at the top of the *Worked Example*. No design intent or implementation evidence is at stake.
```
