---
title: RETRIEVEENDSETS Operation
out_of_scope: link traversal (FOLLOWLINK), link discovery (FINDLINKS),
  link creation (MAKELINK), link editing (EDITLINK), content retrieval
  (RETRIEVEV), replication and inter-server protocol (BEBE)
question: What happens when a link's endsets are read directly by the
  link's address? What is returned, what does the reader learn about the
  relationship the link records, what does this reveal that following the
  link would not, and what invariants govern the endsets the read can
  return?
depends: [34, 36, 43, 47, 53, 86, 93]
---

# Inquiry: RETRIEVEENDSETS Operation
