---
title: RETRIEVEDOCVSPANSET Operation (per-subspace document extent query)
out_of_scope: content delivery (RETRIEVEV), single overall extent
  (RETRIEVEDOCVSPAN / ASN-0112), reading a caller-supplied region of
  the document, link counting (FINDNUMOFLINKSFROMTOTHREE), link
  discovery (FINDLINKSFROMTOTHREE), version comparison
  (SHOWRELATIONOF2VERSIONS), document discovery (FINDDOCSCONTAINING),
  replication and inter-server protocol (BEBE)
question: What happens when a document is asked, by its identity alone,
  for the extent of each of its subspaces — the number of characters of
  text and the number of links it contains? What does the returned
  span-set describe, what relationship must each member span bear to its
  subspace, what does reporting them together reveal about the document
  that single-subspace extent queries would not, and what invariants
  must hold across the per-subspace spans the operation may return?
depends: [34, 36, 40, 43, 45, 47, 53, 58, 93]
---

# Inquiry: RETRIEVEDOCVSPANSET Operation (per-subspace document extent query)
