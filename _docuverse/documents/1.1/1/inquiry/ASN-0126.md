---
title: Substrate Shape Framework
out_of_scope: idem semantics at emit and the relationship between
  tuples with equal (F, G, K) under idem=⊤ (deferred to operational
  successor), behavior catalog and the predicates each unlocks
  (deferred), default predicates beyond what shape-conformance
  implies (deferred), standard registrations the substrate may
  pre-ship (deferred), predicate composition (deferred to a separate
  successor), richer arity (F > 1 and N > 3 — bypassed via the link
  store directly), replication and inter-server protocol (BEBE)
question: What happens when the substrate narrows ASN-0086's typed
  relations of arity three into a concrete shape vocabulary that
  apps register against? What structural commitments must the
  framework make about F and G cardinalities so that shape-
  conformance is a well-formedness predicate decidable from the
  tuple and the registry entry alone, what relationship must the
  registry bear to the substrate's state evolution so apps can rely
  on a stable shape vocabulary across every reachable state, what
  does committing to a finite concrete shape catalog rather than an
  abstract framework reveal about the boundary between structural
  substrate vocabulary and the operational semantics layered on top,
  and what invariants must the framework preserve about registry
  permanence, shape-conformance well-formedness at every emit, and
  the structural state-independence of every registered type's
  shape, idem flag, and domain restrictions?
depends: [86]
---

# Inquiry: Substrate Shape Framework
