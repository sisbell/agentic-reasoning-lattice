---
title: FINDNUMOFLINKSFROMTOTHREE Operation
out_of_scope: link enumeration itself (FINDLINKSFROMTOTHREE /
  ASN-0121 — its four-set match semantics is settled there; this
  inquiry asks where the shared description belongs, not what it is),
  paginated link retrieval (FINDNEXTNLINKSFROMTOTHREE / ASN-0108),
  content-region endset search (RETRIEVEENDSETS), reading link
  structure (READLINK / ASN-0111, FOLLOWLINK / ASN-0114), link
  creation and editing (MAKELINK / ASN-0120, EDITLINK / ASN-0125),
  re-derivation of the existence/discovery anchoring taxonomy
  (ASN-0127's layer — cite, do not rebuild), predicate-level
  aggregation semantics (ASN-0129's layer), content delivery
  (RETRIEVEV), replication and inter-server protocol (BEBE)
question: What happens when the system is asked how many links match
  a description given as four sets — a home-set bounding where the
  links reside, a from-set bounding what their first endset
  references, a to-set bounding what their second endset references,
  and a third-set bounding their type or connector endset? What
  exactly is the unit being counted — link identities, anchorings, or
  appearances surfaced through arrangements — and what does the
  choice commit the system to when one link is reachable through
  transcluded content in many documents, or anchored to fresh content
  no arrangement yet surfaces? What relationship must the count bear
  to the enumeration its sibling operation FINDLINKSFROMTOTHREE would
  return for the same four criteria at the same moment, and where
  should the shared match-description live so that neither operation
  must be specified in terms of the other? What does a count of zero
  assert, and what relationship must any count bear to the link store
  as it stands at the moment of inquiry? What invariants must the
  operation maintain
  about consistency with enumeration, stability under content edits
  and link retraction, and what does asking for a number instead of
  the links themselves reveal about the cost and the meaning of link
  discovery?
depends: [34, 36, 43, 45, 47, 53, 58, 82, 86, 93, 98, 121, 127]
---

# Inquiry: FINDNUMOFLINKSFROMTOTHREE Operation
