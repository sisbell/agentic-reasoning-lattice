---
title: FOLLOWLINK Operation (read one endset of a link by selector)
out_of_scope: reading a link's full structure by its address (READLINK /
  ASN-0111), content-region endset search (RETRIEVEENDSETS / ASN-0110),
  resolving an endset's spec-set to V-positions of a specific document,
  link discovery (FINDLINKSFROMTOTHREE), link counting
  (FINDNUMOFLINKSFROMTOTHREE), link creation (MAKELINK), link editing
  (EDITLINK), content retrieval (RETRIEVEV), replication and inter-server
  protocol (BEBE)
question: What happens when one endset of a link is requested by giving
  the link's address together with a selector for which end? What is
  returned, what relationship must the returned spec-set bear to the
  endset the link records at that position, what does the result expose
  about the link without naming or returning the other endsets, and what
  invariants govern the spec-set the operation may return?
depends: [34, 36, 43, 47, 53, 86, 93, 98]
---

# Inquiry: FOLLOWLINK Operation (read one endset of a link by selector)
