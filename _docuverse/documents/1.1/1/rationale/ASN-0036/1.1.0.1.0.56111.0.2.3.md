Finding is stale/incorrect: the corrupted g/T1/'minimizes' text is not in D-SEQ.md (and 'minimizes over Q' never appears in D-SEQ's git history). D-SEQ's third trichotomy case already reads correctly with h, NAT-order's <-transitivity, the case h.J′ < h.(N+1), and the greatest-element 'maximizes' conclusion — matching its own NAT-order Depends entry. The quoted text lives in D-MIN.md, where it is the correct least-element argument.
