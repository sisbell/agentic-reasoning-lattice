Stale finding: D-SEQ's current proof grounds the greatest k-value by a direct induction (P(N)) over S8-fin's enumerating bijection via NAT-induction, explicitly replacing any pigeonhole/image-cardinality argument; it establishes tumbler distinctness via T1 (lexicographic order) and grounds w in T via T0's comprehension clause. T0(a), T3, and D-INJ — and the finding's quoted phrases ('admissible values drawn from T0(a)', 'unequal by T3', 'invoke D-INJ instantiated at P := N+1') — appear nowhere in the proof body across all 12 revisions, so adding them to Depends would be phantom dependencies; the existing 18-entry list already covers every claim the proof invokes.
