D-SEQ Step 3's zeros(w)=0 sub-argument already reads '...k > 0 by NAT-order's transitivity of < (0 < k₁ < k ⟹ 0 < k)', using D-SEQ's own variables k₁ and k, consistent with the Depends entry's instantiation '(0, k₁, k)'. The phrase 'instantiated at the ℕ-valued uⱼ₊₁ and n' does not appear in D-SEQ.md (and never has, per git log -S); it is D-CTG-depth.md's correct text, where uⱼ₊₁ and n are the local variables. No mismatch exists to fix.
