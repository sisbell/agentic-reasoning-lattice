The promised lemma is already present: OrdShiftHom (OrdinalShiftPreservation) is a named sub-claim immediately following S8-depth's 'as the following lemma establishes' sentence (note ASN-0036-strand-model.md lines 195-207; claim file OrdShiftHom.md), proving both (a) subspace(shift(v,n))=subspace(v) and (b) S8a-preservation from OrdinalShift's exported postconditions (incl. the action-point lower bound v_{#v}+n>=1) and S8a. Its full proof appears in the ASN and it is consumed downstream by S8 and ValidInsertionPosition, so the finding's claim that 'the argument does not appear anywhere in the ASN content' is incorrect.
