ASN-0075 is not in this inquiry's declared depends set ([34,36,43,45,47,53,58,82,86,93,98,127]) and was not supplied as a foundation, so citing it (which both of the finding's offered remedies require) would introduce an undeclared cross-note dependency the ASN methodology forbids. The six historical-companion claims do not re-derive any declared dependency: they derive locally from ASN-0047's provenance apparatus (P2, P4★, P4a, M1, SequentialTransitionAxiom), which they already cite; they are internally non-redundant and load-bearing for the note's central implementation-deviation thesis (green computes the historical finddocs_R, not the live finddocs) and for the inquiry's explicitly in-scope question about soundness against ghost/deleted I-addresses — so they are not bloat.
