The appendix's opening already documents (Peano-rec), (Peano-zero-least), (Peano-pred) as Peano-core upstream supplements that the present draft localizes to the appendix rather than promoting to the foundation, with explicit future-work text: 'A future draft may extend the foundation to enumerate (Peano-rec) explicitly and absorb the appendix into the foundation.' ASN-0094 cannot unilaterally promote these to a foundation extension ASN — that would require amending ASN-0034 (or a foundation-level NAT primitives ASN), each with its own independent consultation cycle. The present draft's choice to localize the derivations and tag them as future foundation candidates is the appropriate scope for a derived ASN that consumes NAT-card and NAT-sub at two narrow citation sites. Downstream proofs cite NAT-card and NAT-sub by name without consulting their derivations, so the appendix's load on the main framework is bounded; the appendix exists to discharge those two named primitives in finite text rather than to leave them as undefended appeals.
