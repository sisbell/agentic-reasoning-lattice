The framework's components are tightly interdependent: Sh-conf gates catalog admissions, Sh4 is consumed by the per-K disciplines (FDD strictly strengthens Sh4, SHCD is structurally exclusive with Sh4 via the idem flag), the templates' totality depends on the slot accessors derived from Sh0–Sh3, and the per-shape walkthroughs verify the per-K discipline contracts against concrete state evolutions. Splitting (a) core framework, (b) catalog, (c) per-K disciplines, (d) Nullify compatibility, (e) NAT primitives would force every consumer to reassemble the dependencies across multiple ASNs. The Nullify-compatibility material is specifically a layer-level discipline atop ASN-0086's relational layer (per Finding 5 rationale), not an ASN-0086 substrate revision. The NAT primitives appendix is already explicitly flagged as future work for foundation absorption in the appendix's opening paragraph. The length reflects substantive content density (one framework with five named components, eight worked walkthroughs, three layer-discipline contracts each with preservation theorems and gate orderings, plus the NAT derivation), not redundancy or scope creep.
