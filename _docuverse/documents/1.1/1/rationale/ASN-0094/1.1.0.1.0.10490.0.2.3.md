The finding's premise is incorrect: SharedDepthOneAllocator is not referenced anywhere in ASN-0094 (verified by exhaustive grep — zero matches for 'SharedDepthOneAllocator' or 'SharedDepth'). The scaffolding clauses in ASN-0094's Scope and Substrate Scaffolding section cite L1, L1b, T10a.4, R0a, and the named scaffolding-specific clauses by direct name, but never cite SharedDepthOneAllocator. Since the lemma is not consumed (and not referenced as foundation context either), there is nothing to relocate or clarify.
