The definitions ZeroCountDepth and AllocatorTreeDepth referenced in the finding do not appear in ASN-0094 — they exist only in the ASN-0086 foundation claims provided in the prompt header. A grep across the file for these names plus 'zero-count', 'allocator-tree', and 'child-spawn' returns zero matches in the note body. The finding's premise that these dead definitions exist at the top of Scope and Substrate Scaffolding does not hold; no edit is required.
