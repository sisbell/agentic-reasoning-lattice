Three-way split is a structural restructuring decision previously punted to the operator (per prior review-30); executing it requires creating two new ASNs and re-wiring cross-references, which exceeds the scope of per-finding prose revision. Targeted cleanup per findings 1-11 reduces the sprawl, repetition, and fragmentation symptoms inside the existing ASN.
