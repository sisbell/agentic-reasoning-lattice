Splitting the ASN into multiple ASNs is a structural restructuring, not a targeted per-finding fix. The instructions specify 'make targeted fixes per finding' and 'preserve the existing structure'. The framework's coherence relies on the components being co-located so that Sh0-Sh4 preservation theorems, the per-K layer disciplines (Sh4, FDD, SHCD), and the Sh5 META catalog all reference each other within a single argument; splitting would either duplicate the shared scaffolding across multiple ASNs or fragment cross-references that prove the disciplines are jointly compatible. An architectural decision of that magnitude requires operator scope-direction, not a unilateral reviser move. Finding noted as out of scope for revise-cycle action.
