The cited 'Scope section, SharedDepthOneAllocator (LEMMA)' does not appear in ASN-0094. Verified by Grep — the string 'SharedDepthOneAllocator' produces no matches in the file. The SharedDepthOneAllocator lemma lives in ASN-0086 (provided in the foundation block), not ASN-0094. ASN-0094's Scope section uses only the substrate-conforming-layer scaffolding interface (per-document link sub-allocator chains) and does not restate or cite SharedDepthOneAllocator. Nothing to demote or cite.
