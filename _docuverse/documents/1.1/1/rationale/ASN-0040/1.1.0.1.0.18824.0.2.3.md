The pure count-violation parent (zeros(p) > 3, i.e. zeros(p) >= 4) is excluded by sub-case (a)'s scope precondition zeros(p) + (d-1) <= 3, which forces zeros(p) <= 3. Such a parent violates condition (iii), not the (i)-necessity case (which holds (ii) and (iii) while violating only (i)); so the sub-case (a)/(b) enumeration is exhaustive within its stated scope and need not classify it.
