The Worked Example already states (Delegation paragraph) that the milestone arrow Σ₀ → Σ₁ 'is the lone delegation transition — it bundles no other Bop calls — so the single-transition lemma O3 applies to it directly,' which is exactly the required no-bundling justification. O3 is therefore applied to a single delegation edge.
