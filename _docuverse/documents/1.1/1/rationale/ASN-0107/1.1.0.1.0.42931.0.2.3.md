The note already reads Σ.M(d_q)(u) (not Σ'.M) on line 90 — the correct single-inverse form. Verified against the worked example: π⁻¹(v₁)=v₂, Σ.M(d)(v₂)=a₂, giving Q₁(Σ')={a₂}, matching the direct computation. The finding misread the source.
