The ASN does not contain a 'Definition — Coverage' block. A grep across the entire file confirms no such section exists; the ASN uses coverage(·) directly as defined in foundation ASN-0043 without restating it. The only references to 'Coverage' in the file are the in-prose claims at lines 217 and 241 noting that 'Coverage is a deterministic function of its endset argument' — these reference the function, not restate its definition.
