V6a(iii) and its full ⊆/⊇ derivation already state the relation in terms of d_op (project(a,i,d_op,Σ) ∩ V_{s_C}(d_op) = project(a,i,d_new,Σ'), with inheritance equality M'(d_new)(v) = M(d_op)(v)), reducing to the d_src reading when d_op = d_src. The d_src-based text the finding quotes is not present in the current note's part (iii); only part (ii), which is legitimately about d_src via V5, uses d_src.
