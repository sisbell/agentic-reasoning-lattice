Already addressed in a prior revision. The composite verification (sub-cases A and B) already cites V1 for Document(d_new) and parent(d_new)=parent(d_src), explicitly stating 'not re-derived here' and 'we cite it rather than re-establish it from K.δ-ID.zeros-0/1'. The quoted re-derivation text no longer appears in the file; only the legitimate precondition discharges (freshness, parent(d_src) in E via P8, T4-validity via T10a.4) remain.
