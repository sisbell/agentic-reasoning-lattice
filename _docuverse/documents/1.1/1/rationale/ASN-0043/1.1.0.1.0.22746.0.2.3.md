Already addressed in current revision. The FSP proof's L1c bullet derives both strong conjuncts (k1=2 and #ti>#s for all i) from the seed-equals-home constraint (h2: seed = T4-valid document-level home(a), so zeros(s)=2) together with zeros(a)=3 (h3): only a k'=2 step changes the zero count, so the 2->3 jump needs exactly one such step and it must be first (any earlier k in {0,1} step pushes the third zero past position #s+1, forcing home(a)!=s), after which every ti has length >= #s+2. This is precisely the argument the finding requests; the finding's quoted bare one-liner does not match the current L1c bullet.
