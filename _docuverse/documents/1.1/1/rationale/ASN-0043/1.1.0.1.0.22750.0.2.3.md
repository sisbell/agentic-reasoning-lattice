Already addressed in current revision. L1b is not a bare invariant: it grounds #E(a)>=2 three ways -- (1) the S8a parallel ('mirrors S8a's #t>=2 for V-positions'), exactly the grounding the finding suggests; (2) an explicit derivation that a link needs both a subspace identifier (E(a)1=s_L, fixed by L0) and a within-subspace ordinal, since L0 assigns the same s_L to every link so distinguishing siblings (L11a) requires the ordinal; (3) Gregory evidence that findisatoinsertmolecule under LINKATOM allocates a fixed subspace digit followed by a separately-incremented ordinal. This matches the consultation (element field is exactly subspace identifier + ordinal, depth 2).
