The finding itself notes this is 'presentation rather than correctness.' The ASN scope reflects the natural unit of the transition model — the elementary set, the per-elementary discharge analysis, and the coupling constraints form a single argument structure whose components depend on each other (K.δ discharge uses T10a; K.μ~ decomposes through K.μ⁻ + K.μ⁺; J0/J1★/J1'★ scope the composite-boundary properties). Splitting would fragment the argument rather than clarify it; the verification matrix and named subsections already give the structural decomposition that supports thorough review without partitioning across separate ASNs.
