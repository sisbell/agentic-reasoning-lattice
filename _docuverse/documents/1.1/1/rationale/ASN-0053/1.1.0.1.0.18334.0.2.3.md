The quoted restatement paragraph ('Nelson argues this is structurally guaranteed: spans are intervals on a total order...') does not exist in the current ASN. The S10 trailing paragraph is already a single-sentence Q8 grounding citation about set-theoretic semantics (byte collections vs ordered series); it grounds the proof's semantic premise rather than re-arguing the commutativity/associativity proof, matching the finding's own 'single source-citation tag if grounding is wanted' exception.
