The quoted motivating paragraph ('The level_compat precondition is what excludes the troublesome cross-level case...') does not exist in the current ASN. The WF section goes directly from header to the WF claim with no precondition-justification prose; the prefix-case exclusion appears only inside the WF proof itself, where it is load-bearing.
