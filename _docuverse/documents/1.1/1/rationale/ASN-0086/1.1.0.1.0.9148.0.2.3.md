SFD(Σ) was removed entirely from both WP expressions. The sibling-frontier discipline is now built into ASN-0093's K.λ contract, so R0a is unconditional and no SFD predicate over Σ is needed in the wp.
