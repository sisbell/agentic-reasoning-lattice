---
label_prefix: MAT
default_campaign: dulong-petit-maxwell
---

# Materials

The materials lattice: a Dulong–Petit / Maxwell-era theory-of-heat
formalization, authored under (node, user) = (1.2, 1) in this
docuverse. Notes, claims, agents, and supporting documents authored
for this lattice live under `_docuverse/documents/1.2/1/`.

Membership in this lattice is expressed as a `lattice` substrate link
from a document to this lattice doc. Configuration that scopes to the
whole lattice (label_prefix, default_campaign) lives in this doc's
frontmatter and is read by `lib/lattice/config.py`.
