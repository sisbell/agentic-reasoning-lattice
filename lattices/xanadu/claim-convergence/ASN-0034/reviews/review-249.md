# Cone Review — ASN-0034/T12 (cycle 6)

*2026-04-18 06:20*

### Definition (Span) Depends T0 entry mischaracterizes TA0's T0 citation scope
**Foundation**: TA0 (WellDefinedAddition) — TA0's Depends T0 entry (as revised): "T0 (CarrierSetDefinition, this ASN) — supplies the carrier `T` and the length operator `#`: `∈ T` appears at `a ∈ T` and `w ∈ T` in the precondition list and at `a ⊕ w ∈ T` in the first postcondition, and `#` appears at `actionPoint(w) ≤ #a` in the precondition and at `#(a ⊕ w) = #w` in the second postcondition." That is, TA0 cites T0 for four distinct usages across its precondition and postcondition lists.
**ASN**: Definition (Span), Depends entry for T0, parenthetical comparison clause: "This definition writes `T` and `#` directly in its own body, so it cites T0 directly in parallel with T1 (which cites T0 for `#a` and component projection `aₖ` on `a ∈ T`) **and TA0 (which cites T0 for `∈ T` in the postcondition `a ⊕ w ∈ T`)** rather than inheriting through either of them — both consume T0 themselves and neither re-exports T0's vocabulary as a handle the way TA0 re-exports TumblerAdd's first two postconditions".
**Issue**: The parenthetical describes TA0's T0 citation as covering only "`∈ T` in the postcondition `a ⊕ w ∈ T`" — a single usage. TA0's actual Depends T0 entry now covers four usages: `∈ T` in the precondition (`a ∈ T`, `w ∈ T`), `∈ T` in the first postcondition (`a ⊕ w ∈ T`), `#` in the precondition (`actionPoint(w) ≤ #a`), and `#` in the second postcondition (`#(a ⊕ w) = #w`). TA-strict's parallel comparison clause — "TA0 (which cites T0 for `∈ T` and `#` in its precondition and postcondition lists)" — uses the broader, accurate characterisation; Definition (Span)'s narrower characterisation is the drift. The argumentative force of the comparison ("neither re-exports T0's vocabulary as a handle") is unaffected, but a reviser auditing TA0's T0 coverage against the cross-references that describe it will find one site agreeing (TA-strict) and one site disagreeing (Definition (Span)).
**What needs resolving**: Update Definition (Span)'s parenthetical description of TA0's T0 citation to match TA0's actual scope — either by enumerating the four usages explicitly as TA0 itself does, or by aligning with TA-strict's compact "`∈ T` and `#` in its precondition and postcondition lists" formulation. Whichever form is used, the three cross-references (Definition (Span)'s, TA-strict's, and TA0's own Depends entry) should describe TA0's T0 coverage uniformly.
