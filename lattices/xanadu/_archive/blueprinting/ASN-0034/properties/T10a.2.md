**T10a.2 (NonNestingSiblingPrefixes).** Distinct siblings from the same allocator are prefix-incomparable: `tᵢ ⋠ tⱼ ∧ tⱼ ⋠ tᵢ`.

Let `tᵢ` and `tⱼ` be distinct siblings from the same allocator with `i < j`. We must show neither is a prefix of the other.

First, `tᵢ ≠ tⱼ`. By TA5(a), each application of `inc(·, 0)` produces a strictly greater tumbler under T1, so the sibling sequence is strictly increasing: `t₀ < t₁ < ... < tⱼ`. In particular `tᵢ < tⱼ`, and by T1 irreflexivity `tᵢ ≠ tⱼ`.

Second, `#tᵢ = #tⱼ` by T10a.1, since both are siblings of the same allocator.

Now suppose for contradiction that `tᵢ ≼ tⱼ`. Since `tᵢ ≠ tⱼ`, this is a proper prefix relationship, which requires `#tᵢ < #tⱼ` — contradicting `#tᵢ = #tⱼ`. The symmetric argument excludes `tⱼ ≼ tᵢ`: if `tⱼ ≼ tᵢ` with `tⱼ ≠ tᵢ`, then `#tⱼ < #tᵢ`, again contradicting `#tᵢ = #tⱼ`.

Therefore `tᵢ ⋠ tⱼ ∧ tⱼ ⋠ tᵢ` — the sibling prefixes are non-nesting, satisfying the precondition of T10. ∎

*Formal Contract:*
- *Precondition:* `tᵢ`, `tⱼ` are distinct siblings from the same allocator (`i ≠ j`).
- *Postcondition:* `tᵢ ⋠ tⱼ ∧ tⱼ ⋠ tᵢ` — neither is a prefix of the other.
