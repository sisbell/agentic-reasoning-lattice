### NAT-closure body paragraphs explain *why* the axiom matters rather than *what* it claims
**Class**: OBSERVE
**ASN**: NAT-closure (NatArithmeticClosureAndIdentity), the two body paragraphs beginning "The signature `+ : ℕ × ℕ → ℕ` carries two load-bearing commitments..." and "The pair `1 ∈ ℕ` and `0 < 1` names a second constant in ℕ..."
**Issue**: Both paragraphs sit between the headline and the Formal Contract and add no inference — the signature paragraph restates that the domain makes `+` total and the codomain closes the operation, then summarises with "Totality rules out partial addition and closure rules out sums that escape ℕ"; the constant paragraph re-narrates `1 ∈ ℕ` and `0 < 1` in prose. Neither derives a Consequence nor unfolds a definition; they explain why the listed axiom clauses are needed rather than what they claim. This matches the rubric's reviser-drift pattern ("new prose around an axiom explains why the axiom is needed rather than what it says").
**What needs resolving**: n/a (OBSERVE)