### T1 transitivity sub-case (ii, ii) — implicit sub-case split in `≤` expansion
**Class**: OBSERVE
**Foundation**: T1 (LexicographicOrder), transitivity proof, sub-case (ii, ii)
**ASN**: "From `m + 1 ≤ n` with NAT-addcompat's `m < m + 1`, we derive `m < n` (via NAT-order transitivity or substitution)"
**Issue**: `m + 1 ≤ n` expands by the `≤`-defining clause into `m + 1 < n ∨ m + 1 = n`, each resolved differently — the first by transitivity (`m < m + 1 < n`), the second by substitution (`m < m + 1 = n`). The pointer "transitivity or substitution" identifies which principle applies to which branch but does not walk the expansion. The argument is sound; the sub-cases are immediate from the available axioms. A formal verifier must reconstruct the split.

---

VERDICT: OBSERVE