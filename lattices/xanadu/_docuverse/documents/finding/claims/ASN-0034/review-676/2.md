### Prefix proper-prefix length — symmetry step elided when applying T3
**Class**: OBSERVE
**Foundation**: T3 (CanonicalRepresentation) — postcondition `a = b ⟺ #a = #b ∧ (A i : 1 ≤ i ≤ #a : aᵢ = bᵢ)`
**ASN**: Prefix (PrefixRelation), derived postcondition (proper-prefix length) — "the component condition `(∀i : 1 ≤ i ≤ #p : qᵢ = pᵢ)` covers all positions of both tumblers, so by T3 `p = q`"
**Issue**: Prefix's definition records components as `qᵢ = pᵢ` (the second operand's components equal the first's). T3's (←)-direction requires `pᵢ = qᵢ` (first operand's components equal the second's). Passing from `qᵢ = pᵢ` to `pᵢ = qᵢ` requires one application of symmetry of equality. The step is elided. The conclusion is correct; the symmetry application is not stated.

---

VERDICT: OBSERVE