### T4 formal contract uses numerals `2` and `3` without Definition entries
**Class**: REVISE
**Foundation**: NAT-closure (NatArithmeticClosureAndIdentity) — axioms `1 ∈ ℕ` and `+ : ℕ × ℕ → ℕ`
**ASN**: T4 (HierarchicalParsing) — *Definition:* block (T4-valid predicate) and *Consequence:*
**Issue**: The T4-valid predicate is defined with `zeros(t) ≤ 3`, and the Consequence states `zeros(t) ∈ {0, 1, 2, 3}`. The numerals `2` and `3` appear in these formal contract statements without a corresponding *Definition:* entry. The symbols `0` and `1` are each introduced as explicit axioms (NAT-zero and NAT-closure respectively); `2` and `3` are not defined anywhere in the formal contract. The Depends annotation for NAT-closure gestures at `2 := 1 + 1` and `3 := 2 + 1`, but a dependency annotation is not a *Definition:* entry — it explains what a dependency provides, not what the citing claim introduces. A downstream consumer reading T4's formal contract cannot locate a definition for `3` within the claim's own *Definition:* block, and the Exhaustion proof's step "NAT-discrete at `(2, zeros(t))`… promotes to `3 ≤ zeros(t)`" implicitly uses `2 + 1 = 3`, which is nowhere stated.
**What needs resolving**: Either add `2 := 1 + 1` and `3 := 2 + 1` as *Definition:* entries in T4's formal contract, or rewrite the affected statements (`zeros(t) ≤ 3`, `zeros(t) ∈ {0, 1, 2, 3}`) using the expanded forms `zeros(t) ≤ 1 + 1 + 1` and `zeros(t) ∈ {0, 1, 1+1, 1+1+1}`. Whichever form is chosen, the Exhaustion proof's intermediate steps at `m = 1` and `m = 2` must use the same grounded notation.

---

VERDICT: REVISE