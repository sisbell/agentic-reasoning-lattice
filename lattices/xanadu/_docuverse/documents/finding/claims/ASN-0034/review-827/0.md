### T10a.5 NAT-order Depends: wrong form of mixed transitivity cited
**Class**: REVISE
**Foundation**: NAT-order (NatStrictTotalOrder) — mixed transitivity derives `0 < (tₙ)_{#s+1} + 1`
**ASN**: T10a.5 (CrossAllocatorIncomparability) — Formal Contract *Depends:* section, NAT-order entry: "the mixed <-≤ transitivity `m < n ∧ n ≤ p ⟹ m < p` ... chains NAT-addcompat's strict-successor conclusion `(tₙ)_{#s+1} < (tₙ)_{#s+1} + 1` with NAT-zero's lower bound `0 ≤ (tₙ)_{#s+1}` to obtain `0 < (tₙ)_{#s+1} + 1`"
**Issue**: The annotation names the `<`-`≤` form (`m < n ∧ n ≤ p ⟹ m < p`) and describes applying it to two premises: (A) `(tₙ)_{#s+1} < (tₙ)_{#s+1} + 1` and (B) `0 ≤ (tₙ)_{#s+1}`. No assignment of `m, n, p` makes the `<`-`≤` form yield `0 < (tₙ)_{#s+1} + 1` from these two inputs in a single step — A provides the strict left arm of `<`-`≤`, but B (`0 ≤ (tₙ)_{#s+1}`) does not match the non-strict right arm `n ≤ p` at any consistent variable binding. The correct form is `≤`-`<`: `m ≤ n ∧ n < p ⟹ m < p`, instantiated at `m = 0`, `n = (tₙ)_{#s+1}`, `p = (tₙ)_{#s+1} + 1`, consuming B as the left arm and A as the right arm and delivering `0 < (tₙ)_{#s+1} + 1` directly. The conclusion is correct; the cited inference rule is not the one that establishes it. A downstream consumer tracing the Depends annotation cannot verify the step as described.
**What needs resolving**: The annotation must name the `≤`-`<` form (`m ≤ n ∧ n < p ⟹ m < p`) and identify the instantiation: `m = 0` from NAT-zero's lower bound, `n = (tₙ)_{#s+1}`, `p = (tₙ)_{#s+1} + 1` from NAT-addcompat's strict successor, yielding `0 < (tₙ)_{#s+1} + 1`.

---

VERDICT: REVISE