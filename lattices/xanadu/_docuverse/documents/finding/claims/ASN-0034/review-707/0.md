### T0 missing from TA-RC's Depends
**Class**: REVISE
**Foundation**: T0 (CarrierSetDefinition) — comprehension clause
**ASN**: TA-RC (RightCancellationFailure) — Formal Contract *Depends:* section
**Issue**: TA-RC's postcondition is `∃ a, b, w ∈ T : …`. The proof discharges this by exhibiting `a = [1,3,5]`, `b = [1,3,7]`, `w = [0,2,4]` and verifying all conditions. Every condition check cited in the Depends section (T3 for `a ≠ b`, TA0 for well-definedness preconditions, ActionPoint for `k = 2`, TumblerAdd for result computation) presupposes `a, b, w ∈ T`. That presupposition is discharged by T0's comprehension clause — "for every `p ≥ 1` and every component map `r : {j ∈ ℕ : 1 ≤ j ≤ p} → ℕ`, there exists `t ∈ T` with `#t = p` and `tᵢ = r(i)`" — instantiated at length 3 with the respective component maps. T0 is not in TA-RC's Depends. The precedent is set by TumblerAdd, which explicitly cites T0's comprehension clause for `a ⊕ w ∈ T`; the same grounding is owed here for each witness tumbler.
**What needs resolving**: T0 must be added to TA-RC's Depends, annotated that its comprehension clause is instantiated at length 3 and the component maps for `a`, `b`, and `w` respectively to establish `a, b, w ∈ T`.

---