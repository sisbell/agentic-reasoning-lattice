### NAT-zero missing NAT-carrier in Depends
**Class**: REVISE
**Foundation**: NAT-carrier (NatCarrierSet)
**ASN**: NAT-zero (NatZeroMinimum) — Formal Contract *Depends:* section
**Issue**: NAT-zero's axiom uses `ℕ` directly in `0 ∈ ℕ` and `(A n ∈ ℕ :: 0 < n ∨ 0 = n)`, and the Consequence `(A n ∈ ℕ :: ¬(n < 0))` quantifies over it. NAT-carrier is the sole provider of `ℕ` as a set. The Depends section lists only NAT-order. Every other claim that directly mentions `ℕ` — NAT-order, NAT-closure, NAT-addcompat, NAT-discrete, NAT-cancel, NAT-wellorder, T0, TA-Pos — cites NAT-carrier explicitly for precisely this reason. NAT-zero is the sole exception. Without the citation, NAT-zero's axiom symbol `ℕ` is ungrounded in its stated dependency chain; a downstream consumer reading the Depends list cannot trace `ℕ` to its source.
**What needs resolving**: NAT-carrier must be added to NAT-zero's Depends, with an annotation that it supplies `ℕ` as the underlying set in the membership clause `0 ∈ ℕ`, the axiom quantifier `(A n ∈ ℕ :: ...)`, and the Consequence quantifier `(A n ∈ ℕ :: ¬(n < 0))`.

---