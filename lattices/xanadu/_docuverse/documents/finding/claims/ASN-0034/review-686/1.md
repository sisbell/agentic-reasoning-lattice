### [ZPD: NAT-closure dependency is transitive via Divergence]
**Class**: OBSERVE
**Foundation**: ZPD (ZeroPaddedDivergence)
**ASN**: *Depends:* entry "NAT-closure (NatArithmeticClosureAndIdentity) — addition closure instantiated at `(#a, 1)` and `(#w, 1)` ... places `#a + 1` and `#b + 1` in ℕ in the postcondition"
**Issue**: ZPD's own codomain is `{1, ..., L}` with `L ∈ {#a, #w}` — no `+1` arithmetic appears in ZPD's definition or codomain. The `#a + 1` and `#w + 1` values appear only in the Relationship-to-Divergence postcondition, where they are Divergence's output values. Divergence already carries the NAT-closure dependency and establishes `divergence(a, b) ∈ ℕ` as a postcondition. ZPD's direct citation of NAT-closure is therefore transitive through Divergence (already listed) and over-inclusive.

---

VERDICT: OBSERVE