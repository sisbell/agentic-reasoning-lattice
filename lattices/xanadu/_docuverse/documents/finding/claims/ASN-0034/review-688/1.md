### T1 trichotomy Case 1: mutual exclusion argument elides the substitution
**Class**: OBSERVE
**Foundation**: T1 (LexicographicOrder), part (b), Case 1
**ASN**: "Case 1: no divergence position exists. Then… `a = b` by T3. Part (a) gives `¬(a < a)` and `¬(a > a)`."
**Issue**: The proof must discharge all four trichotomy conjuncts: `¬(a < b ∧ a = b)`, `¬(a < b ∧ b < a)`, `¬(a = b ∧ b < a)`. After establishing `a = b`, each conjunct collapses by substituting `b ↦ a`, converting every offending hypothesis into `a < a`, which irreflexivity contradicts. The text names the tool (`¬(a < a)`) but omits the substitution step that applies it. A precise reader must supply it.

---