### T6 asserts `tumbleraccounteq` is the operational form of T6 without verifying the correspondence
**Class**: OBSERVE
**Foundation**: T4 (HierarchicalParsing) — field-separator semantics
**ASN**: T6 (DecidableContainment) — final prose paragraph: "`tumbleraccounteq` is the operational form of T6"
**Issue**: T6 describes `tumbleraccounteq` as doing "prefix match with zero-as-wildcard, truncating candidate to parent length." T6's formal semantics uses zeros as structural separators parsed by T4b — a component `tᵢ = 0` marks a field boundary, not a wildcard. These are distinct: a zero in a field position would match any field value under wildcard semantics but would instead trigger a field-boundary parse under T6's semantics. On inputs where a zero appears at a non-boundary component position, the two procedures could disagree. The equivalence is asserted, not derived.

---

VERDICT: OBSERVE