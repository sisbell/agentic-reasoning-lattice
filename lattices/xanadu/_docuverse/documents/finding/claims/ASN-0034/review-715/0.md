### `≥` in T0 comprehension axiom unacknowledged in Depends annotation
**Class**: OBSERVE
**Foundation**: NAT-order (NatStrictTotalOrder) — Definition clause `(A m, n ∈ ℕ :: m ≥ n ⟺ n ≤ m)`
**ASN**: T0 (CarrierSetDefinition) — Formal Contract *Depends:* entry for NAT-order; comprehension axiom `(A p ∈ ℕ : p ≥ 1 : ...)`
**Issue**: The comprehension axiom uses `≥` as `p ≥ 1`. NAT-order's *Definition* section exports `≥` via `m ≥ n ⟺ n ≤ m`, so `≥` is grounded — NAT-order IS listed as a dependency. But the NAT-order annotation in T0's *Depends* reads only "supplies the non-strict relation `≤`…" and names two specific sites; `≥` does not appear anywhere in the annotation. A reader auditing symbol provenance in T0 finds `≥` in the formal contract and nothing in the Depends annotations pointing to its source. The grounding chain is intact; the annotation is the incomplete party.

---