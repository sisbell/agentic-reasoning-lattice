### NAT-discrete cited in ZPD but absent from ASN
**Class**: REVISE
**Foundation**: (none — the gap is a missing claim in this foundation ASN itself)
**ASN**: ZPD (ZeroPaddedDivergence) — Depends list entry and Relationship-to-Divergence prose: "NAT-discrete's forward direction `m < n ⟹ m + 1 ≤ n`, instantiated at `(#a, k)` in sub-case (β) and at `(#w, k)` in sub-case (γ), bridges the construction's strict bound `#a < k` (resp. `#w < k`) on the least disagreement position to the postcondition's `#a + 1 ≤ k` (resp. `#w + 1 ≤ k`), grounding `zpd(a, w) ≥ divergence(a, w)`"
**Issue**: The claim NAT-discrete (NatDiscreteness) is listed as a dependency of ZPD and its forward direction `m < n ⟹ m + 1 ≤ n` is explicitly invoked to close the gap between the strict bound `#a < k` (or `#w < k`) produced by the construction and the postcondition's required `#a + 1 ≤ k` (or `#w + 1 ≤ k`). NAT-discrete appears nowhere in the ASN's claim set. Since the header declares this a foundation ASN with no external ground statements, every cited dependency must be defined within the ASN. The property `m < n ⟹ m + 1 ≤ n` is not derivable from the axioms in scope: NAT-closure provides `+` and `1 ∈ ℕ` but does not assert `n < n + 1` for all `n`; NAT-wellorder gives a least element of nonempty subsets but cannot establish that nothing lies strictly between `m` and `m + 1` without the discreteness property. Without NAT-discrete, the postcondition `zpd(a, w) ≥ divergence(a, w)` in Divergence case (ii) is ungrounded.
**What needs resolving**: NAT-discrete (NatDiscreteness) must be added as a claim in this ASN, stating at minimum the forward direction `(A m, n ∈ ℕ : m < n : m + 1 ≤ n)`, with its dependency chain (NAT-carrier, NAT-order, NAT-closure, and whatever additional axiom grounds discreteness of ℕ). The ZPD postcondition can then cite it legitimately.

VERDICT: REVISE