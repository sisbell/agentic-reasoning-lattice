### OrdinalDisplacement's Depends section omits NAT-carrier
**Class**: REVISE
**Foundation**: NAT-carrier (NatCarrierSet)
**ASN**: OrdinalDisplacement — *Formal Contract* → *Depends:* (complete list)
**Issue**: OrdinalDisplacement's Preconditions state `n ∈ ℕ` and `m ∈ ℕ`, and the construction of δ(n, m) uses ℕ-valued components throughout. Every other claim in this ASN that directly mentions ℕ — NAT-order, NAT-zero, NAT-closure, NAT-wellorder, NAT-discrete, T0, TA-Pos, ActionPoint — cites NAT-carrier explicitly for precisely this reason. OrdinalDisplacement is the sole exception: NAT-carrier does not appear in its Depends section, leaving the symbol ℕ in its Preconditions ungrounded in the stated dependency chain.
**What needs resolving**: NAT-carrier must be added to OrdinalDisplacement's Depends, annotated as supplying ℕ for the Precondition membership claims `n ∈ ℕ` and `m ∈ ℕ` and for the ℕ-valued component construction.

---