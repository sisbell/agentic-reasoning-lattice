### OrdinalDisplacement cites a nonexistent named clause from NAT-order
**Class**: REVISE
**Foundation**: NAT-order (NatStrictTotalOrder)
**ASN**: OrdinalDisplacement — derivation body: "By NAT-order's disjointness clause `(A m, n ∈ ℕ : m < n : m ≠ n)` instantiated at `(0, n)`..."
**Issue**: NAT-order's Formal Contract exports no statement named "disjointness clause." Its *Consequence* section exports only the exactly-one trichotomy package — including the conjunct `¬(m < n ∧ m = n)` — and `≤`-transitivity. The cited formula `(A m, n ∈ ℕ : m < n : m ≠ n)` is a propositional consequence of `¬(m < n ∧ m = n)` (by `¬(A ∧ B) ∧ A ⟹ ¬B`), but it is not a named exported item. A formal consumer reading OrdinalDisplacement's Depends list and following the citation to NAT-order will not find this clause.
**What needs resolving**: Replace the citation with the actually-exported item: the exactly-one trichotomy's `¬(m < n ∧ m = n)` conjunct, and make the propositional step explicit.

---