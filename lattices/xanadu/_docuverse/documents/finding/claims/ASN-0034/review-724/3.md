### NAT-discrete Consequence proof description inconsistent with prose
**Class**: OBSERVE
**Effective class**: REVISE (classifier override)
**Classifier rationale**: The Formal Contract description names a specific instantiation (`n := m+1`) that does not match the substitution path described in the prose proof, making the contract a misdescription of the actual proof step rather than merely imprecise.
**Foundation**: NAT-order (NatStrictTotalOrder)
**ASN**: NAT-discrete — *Formal Contract* → *Consequence:* description: "irreflexivity `¬(n < n)` at `n := m + 1` after rewriting"
**Issue**: The prose proof reaches the contradiction by substituting `m + 1 = n` into `n < m + 1` (right-hand side) to get `n < n`, then applying irreflexivity at the outer variable `n`. The Formal Contract description says irreflexivity is instantiated "at `n := m + 1`", which describes the alternative route of substituting `n = m + 1` into `n < m + 1` (left-hand side) to get `m + 1 < m + 1`. Both paths are valid; the two descriptions are inconsistent with each other.

---

VERDICT: REVISE