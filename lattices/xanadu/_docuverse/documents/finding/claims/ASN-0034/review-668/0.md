### NAT-closure successor-positivity body: axiom-justification prose
**Class**: OBSERVE
**Foundation**: NAT-closure (NatArithmeticClosureAndIdentity) — successor-positivity clause
**ASN**: NAT-closure body — "Without the universal clause, a model could satisfy the signature, the identity laws, and `0 < 1` while still permitting `m + 1 = 0` at some `m ≥ 1`; the new clause closes that gap uniformly across ℕ."
**Issue**: This paragraph explains why the axiom was included (a counterfactual: what models would be admitted without it), not what the axiom asserts. The axiom's content — `(A n ∈ ℕ :: 0 < n + 1)` — is already stated in the formal contract; the prose body's role is to connect it to the rest of the specification, not to motivate its necessity. This is the reviser drift pattern: new prose around an axiom that explains why the axiom is needed rather than what it says.

---