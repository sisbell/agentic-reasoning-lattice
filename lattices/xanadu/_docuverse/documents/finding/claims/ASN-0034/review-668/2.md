### T0 *Depends* description for NAT-order is incomplete
**Class**: OBSERVE
**Foundation**: T0 (CarrierSetDefinition) — *Depends* section
**ASN**: T0 *Depends* — "NAT-order (NatStrictTotalOrder) — supplies the non-strict relation `≤` on ℕ appearing in the nonemptiness clause `1 ≤ #a` and in the index-domain bound `1 ≤ j ≤ #a` of the component-projection signature."
**Issue**: The description names only two sites for `≤` and is silent about: (a) `≥` in the comprehension axiom's guard `p ≥ 1`, which is NAT-order's defined companion `m ≥ n ⟺ n ≤ m`; (b) `≤` in the comprehension's component-map domain `{j ∈ ℕ : 1 ≤ j ≤ p}`; (c) `≤` in the comprehension's inner axiom `(A i : 1 ≤ i ≤ p : ...)` and in extensionality's `(A i : 1 ≤ i ≤ #a : ...)`. The dependency citation is correct; the description of what NAT-order supplies to T0 is incomplete.

---

VERDICT: OBSERVE