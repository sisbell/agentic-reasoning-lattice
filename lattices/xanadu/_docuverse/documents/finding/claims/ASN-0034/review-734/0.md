### T3 postcondition drops the `∈ ℕ` carrier on the index quantifier
**Class**: OBSERVE
**Foundation**: T0 (CarrierSetDefinition)
**ASN**: T3 (CanonicalRepresentation) — *Postcondition:* `a = b ⟺ #a = #b ∧ (A i : 1 ≤ i ≤ #a : aᵢ = bᵢ)`; and T3 body, quoting T0's extensionality clause as `(A a, b ∈ T : #a = #b ∧ (A i : 1 ≤ i ≤ #a : aᵢ = bᵢ) : a = b)`
**Issue**: T0's extensionality axiom (Formal Contract) writes the inner quantifier as `(A i ∈ ℕ : 1 ≤ i ≤ #a : aᵢ = bᵢ)` with an explicit carrier. T3's postcondition and its in-body quotation of T0 omit the `∈ ℕ`, writing `(A i : 1 ≤ i ≤ #a : aᵢ = bᵢ)`. The carrier is recoverable from context, but the inconsistency between what T0 states and what T3 quotes and exports leaves the precise reader to supply the implicit domain and weakens the claim that T3 is a direct reading of T0's extensionality.

VERDICT: OBSERVE