### Claim presentation precedes dependency grounding
**Class**: OBSERVE
**Foundation**: (n/a — internal ordering)
**ASN**: T3 (position 3), T0 (position 6); NAT-carrier (position 8) after positions 1–7; NAT-zero (position 11) after NAT-closure (position 9)
**Issue**: T3's proof invokes T0's extensionality clause and uses `#·`, `·ᵢ` before T0 appears in the document. NAT-carrier — the foundation on which every quantifier in the document ranges — appears after six claims that depend on it. NAT-zero appears after NAT-closure, which depends on it. A reader verifying sequentially cannot discharge T3 at the point it is presented, and encounters `#a`, `aᵢ` as ungrounded symbols. The depends fields are correctly stated, so the logical DAG is acyclic and sound; but the document order does not respect that DAG.

---