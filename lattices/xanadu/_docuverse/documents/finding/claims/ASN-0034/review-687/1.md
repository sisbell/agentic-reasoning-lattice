### T4b depends list cites NAT-discrete as a direct dependency, but the actual use is transitive through T4a
**Class**: OBSERVE
**Foundation**: T4b (UniqueParse), Depends section; NAT-addassoc's body for the stated convention
**ASN**: T4b Depends — NAT-discrete entry
> "NAT-discrete (NatDiscreteness) — supplies `m < n ⟹ m + 1 ≤ n`, licensing the last-segment non-emptiness inequality `s_k + 1 ≤ #t` in its native `+1` form **(from T4a's Reverse direction)** without subtractive conversion."

**Issue**: The parenthetical "from T4a's Reverse direction" is an admission that T4a does the NAT-discrete work. T4b consumes T4a's exported inequality `s_k + 1 ≤ #t` directly, without independently invoking `m < n ⟹ m + 1 ≤ n`. The stated convention — carried explicitly in NAT-addassoc's body as "naming every directly-used predecessor rather than relying on transitive reachability" — is violated: T4a is already listed as a direct dependency, and T4a itself correctly lists NAT-discrete. The entry in T4b's depends is a transitive citation masquerading as a direct one.

---

VERDICT: OBSERVE