### Divergence symmetry proof for case (ii) abbreviates the sub-case cross-check
**Class**: OBSERVE
**Foundation**: (none — internal to Divergence)
**ASN**: Divergence (Divergence) — symmetry argument, case (ii): "swapping `(a, b)` exchanges sub-cases (ii-a) and (ii-b); both select one-plus the shorter tumbler's length"
**Issue**: The stated argument is correct but the sub-case cross-check is not exhibited. For concreteness: when `#a < #b`, `divergence(a, b)` is in sub-case (ii-a) and equals `#a + 1`. For `divergence(b, a)`, the condition is still case (ii) (same length inequality `#b ≠ #a`, same component-agreement by `=`-symmetry), but now `#b > #a` places it in sub-case (ii-b) for `divergence(b, a)`, yielding `#a + 1`. The claim that the two values coincide rests on recognising that in each sub-case the result is `min(#a, #b) + 1`, and `min` is symmetric — a step the prose asserts but does not derive.

---

VERDICT: OBSERVE