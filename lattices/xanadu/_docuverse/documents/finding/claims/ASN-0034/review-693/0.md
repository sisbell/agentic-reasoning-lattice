### D1: directed inequality at divergence point is unjustified
**Class**: REVISE
**Foundation**: T1 (LexicographicOrder), Divergence (Divergence)
**ASN**: D1 (DisplacementRoundTrip) — body proof, Divergence case (i) segment: "We are in case (i): aᵢ = bᵢ for i < k, and aₖ < bₖ (by T1)."
**Issue**: Divergence case (i) supplies only aₖ ≠ bₖ — the existence of a first shared-position mismatch, not its direction. The annotation "(by T1)" asserts aₖ < bₖ without walking the argument. The required chain has four non-trivial steps: (1) T1 case (ii) is impossible for the pair (a, b) — because it would yield divergence(a, b) = #a + 1, contradicting the precondition k = divergence(a, b) ≤ #a via NAT-addcompat and NAT-order, exactly as the Divergence case (ii-a) elimination already established; (2) T1 case (i) therefore holds for some witness j with aⱼ < bⱼ, aᵢ = bᵢ for i < j, j ≤ #a, j ≤ #b; (3) NAT-order's exactly-one trichotomy converts aⱼ < bⱼ to aⱼ ≠ bⱼ, qualifying j for Divergence case (i)'s conjunction; (4) Divergence's uniqueness clause — the minimality characterization — identifies j = k, yielding aₖ < bₖ. TumblerSub walks this identical argument in full detail for the pair (w, a); D1 omits every step. Without the argument, the Depends entry "T1 — derives aₖ < bₖ from a < b at divergence point k" states a claim the proof body does not establish.
**What needs resolving**: The proof must establish aₖ < bₖ by: ruling out T1 case (ii) for (a, b) via the precondition divergence(a, b) ≤ #a; asserting T1 case (i) gives some witness j with aⱼ < bⱼ; converting aⱼ < bⱼ to aⱼ ≠ bⱼ via NAT-order exactly-one trichotomy to qualify j for Divergence case (i); and invoking Divergence's uniqueness to identify j = k.

---