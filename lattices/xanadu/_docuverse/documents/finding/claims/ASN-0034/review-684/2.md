### T1 Case 2: symmetric sub-case covered by "(resp.)" rather than spelled out
**Class**: OBSERVE
**Foundation**: T1 (LexicographicOrder) — proof of part (b), Case 2
**ASN**: *"case (ii) requires `k = n + 1` (resp. `m + 1`), contradicting `k ≤ n` (resp. `k ≤ m`)"*
**Issue**: The "no reverse witness" argument for the `aₖ < bₖ` sub-case (showing no `k'` witnesses `b < a`) is spelled out explicitly. The symmetric sub-case `bₖ < aₖ` (showing no `k'` witnesses `a < b`) is covered only by "(resp.)" notation throughout the argument. The swapped lengths and swapped roles are genuinely symmetric, but the "(resp.)" compression conflates two distinct instantiations.

---