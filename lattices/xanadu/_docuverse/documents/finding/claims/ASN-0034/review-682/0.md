### TA5-SIG: `0` untraced — NAT-zero absent from *Depends:*
**Class**: REVISE
**Foundation**: NAT-zero (NatZeroMinimum) — establishes `0 ∈ ℕ`
**ASN**: TA5-SIG (LastSignificantPosition), *Definition:* — `sig(t) = max({i : 1 ≤ i ≤ #t ∧ tᵢ ≠ 0})` and the all-zero case condition `(A i : 1 ≤ i ≤ #t : tᵢ = 0)`
**Issue**: The constant `0` appears in the definition of `sig(t)` — once in the set comprehension `tᵢ ≠ 0` and once in the all-zero condition `tᵢ = 0`. The symbol `0` denotes `0 ∈ ℕ`, introduced by NAT-zero. NAT-zero is not listed in TA5-SIG's *Depends:* section. The NAT-closure entry covers only `1 ∈ ℕ` and closure under addition — it does not supply `0 ∈ ℕ`. Every other claim that uses `0` directly (NAT-closure, NAT-addbound) cites NAT-zero explicitly for that constant. TA5-SIG's *Depends:* leaves `0` untraced.
**What needs resolving**: NAT-zero must be added to TA5-SIG's *Depends:* with a citation identifying `0 ∈ ℕ` as what it supplies for the definition.

VERDICT: REVISE