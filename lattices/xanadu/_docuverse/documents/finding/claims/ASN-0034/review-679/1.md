### T5 precondition `#p ≥ 1` is redundant
**Class**: OBSERVE
**Foundation**: T0 (CarrierSetDefinition) — nonemptiness axiom `(A a ∈ T :: 1 ≤ #a)`
**ASN**: T5 (ContiguousSubtrees), Formal Contract *Preconditions*: "`p` is a tumbler prefix with `#p ≥ 1`"
**Issue**: T0's nonemptiness axiom already guarantees `1 ≤ #a` for every `a ∈ T`, so for any `p ∈ T`, `#p ≥ 1` is not a precondition but a consequence of membership. Listing it as a precondition suggests it imposes an additional constraint when it does not.

---

VERDICT: REVISE