### T10 post-case-split bounds stated without case-local derivation
**Class**: OBSERVE
**Foundation**: —
**ASN**: T10 (PartitionIndependence) — proof paragraph beginning "Before extracting components"
**Issue**: After the two cases complete, the proof asserts `k ≤ m` to chain `k ≤ #a` and `k ≤ n` to chain `k ≤ #b`, without noting that each bound is direct in one case and requires a transitivity step in the other. In Case 1 (`ℓ = m`): `k ≤ m` holds directly from `k ≤ ℓ`, but `k ≤ n` requires `k ≤ m ≤ n`. In Case 2 (`ℓ = n`): `k ≤ n` is direct, but `k ≤ m` requires `k ≤ n` and `n < m ⟹ n ≤ m` and `≤`-transitivity. Both bounds hold; the elision is in the presentation, not the conclusion.

---

VERDICT: OBSERVE