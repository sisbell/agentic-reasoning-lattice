### T4b — T0 comprehension not cited for membership of extracted projections in T
**Class**: REVISE
**Foundation**: T0 (CarrierSetDefinition) — comprehension axiom `(A p ∈ ℕ : p ≥ 1 : (A r : {j ∈ ℕ : 1 ≤ j ≤ p} → ℕ :: (E t ∈ T :: #t = p ∧ (A i ∈ ℕ : 1 ≤ i ≤ p : tᵢ = r(i)))))`
**ASN**: T4b (UniqueParse), Derivation paragraph — "In each present case the components listed lie in ℕ⁺, so the resulting sub-sequence belongs to the all-positive subset of T claimed as the image"; Postconditions — "returns a nonempty finite sequence over ℕ⁺ — an element of T"
**Issue**: The assertion that each extracted sub-sequence is an element of `T` requires T0's comprehension axiom: any nonempty finite sequence over ℕ, given as a length `p ≥ 1` and a component map into ℕ, exists in T. That the sub-sequences are nonempty is established (T4a's non-empty segment conclusion), and that each component is in ℕ is established (T0 projection). But T membership is only licensed by T0's comprehension clause. T4b's Depends annotation for T0 lists "the Axiom's nonemptiness clause `(A a ∈ T :: 1 ≤ #a)`" and "component projection `tᵢ`" — neither is the comprehension clause. The derivation text asserts T membership without citing the axiom that grounds it.
**What needs resolving**: T4b's Depends annotation for T0 must cite the comprehension clause, explaining that it licenses membership of the extracted sub-sequences in T: each projection value is a nonempty (by T4a) finite sequence of ℕ-elements (by T0 projection), and T0's comprehension places every such sequence in T.

---

VERDICT: REVISE