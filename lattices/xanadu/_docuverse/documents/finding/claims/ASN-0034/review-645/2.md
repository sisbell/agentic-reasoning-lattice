### "k+1 field segments when zeros(t) = k" stated without field-segment constraint
**Class**: OBSERVE
**Foundation**: T4 (HierarchicalParsing) — body prose introducing field segments
**ASN**:
> "The *field segments* of `t` are the maximal contiguous sub-sequences of field-component positions — `k+1` of them when `zeros(t) = k`, separated by the field separators."

**Issue**: The "`k+1` of them" count holds only under the field-segment constraint (no adjacent zeros, `t₁ ≠ 0`, `t_{#t} ≠ 0`). Without it — e.g. `t = 1.0.0.5` has `zeros(t) = 2` and only two maximal field-component segments, not three — the count fails. The prose implicitly assumes T4-validity but the universal phrasing "`k+1` of them when `zeros(t) = k`" reads as holding for any `t`. Since the count is not load-bearing for any *Formal Contract* item, the looseness has no current downstream effect; flag for tightening.
**What needs resolving**: n/a (OBSERVE).

VERDICT: REVISE