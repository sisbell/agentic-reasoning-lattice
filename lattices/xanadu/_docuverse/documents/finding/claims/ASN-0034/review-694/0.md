### `>` on T used but not defined
**Class**: REVISE
**Foundation**: T1 (LexicographicOrder) — Abbreviations
**ASN**: TumblerAdd (TumblerAdd) — *Postconditions:* `a ⊕ w > a (T1)` and the proof step "T1 case (i) at divergence position k yields a ⊕ w > a"
**Issue**: T1's Abbreviations introduce `a ≤ b` and `a ≥ b` but not `a > b`. The postcondition `a ⊕ w > a` — with `a, a ⊕ w ∈ T` — uses `>` on the tumbler carrier. No definition of `a > b` on T appears anywhere in the ASN. The strict-advancement proof correctly establishes `a < a ⊕ w` via T1 case (i), but writes the conclusion using the unnamed converse. NAT-order defines `m > n ⟺ n < m` on ℕ; that definition does not extend to T without an explicit abbreviation in T1. A downstream consumer reading TumblerAdd's formal contract encounters a postcondition with an ungrounded symbol.
**What needs resolving**: Either add `a > b abbreviates b < a` to T1's Abbreviations (parallel to NAT-order's treatment), or restate TumblerAdd's postcondition and the corresponding concluding proof step as `a < a ⊕ w` using the already-defined `<` on T.

VERDICT: REVISE