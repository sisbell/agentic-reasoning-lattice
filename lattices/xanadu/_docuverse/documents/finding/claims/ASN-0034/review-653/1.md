### T4c Postconditions slot restates the proof rather than asserting what is established
**Class**: REVISE
**Foundation**: n/a (internal)
**ASN**: T4c Postconditions: "*Pairwise distinctness:* the four labels ... are pairwise distinct, since `0, 1, 2, 3` are pairwise distinct in ℕ — NAT-closure's distinctness axiom supplies the base link `0 < 1` directly, NAT-addcompat's strict successor inequality `n < n + 1` instantiated at `n ∈ {1, 2}` supplies `1 < 2` and `2 < 3`, and NAT-order transitivity chains these to `0 < 1 < 2 < 3`, with NAT-order's exactly-one trichotomy Consequence conjunct `¬(m < n ∧ m = n)` excluding equality within that chain — and `zeros(t)` is single-valued, so distinct zero counts induce distinct labels."
**Issue**: The Postconditions slot replays the full Injectivity proof from the body — same axiom citations, same chain construction, same closing observation. A Postconditions slot should state what is established (every T4-valid tumbler receives exactly one of the four labels), not re-derive it. The duplication is essay content in a structural slot, and it amplifies finding 1 since the axiom-choice prose is implicitly carried along.
**What needs resolving**: Postconditions should state the two clauses (exhaustion, pairwise extensional disjointness) and reference the proof above, not re-execute it.