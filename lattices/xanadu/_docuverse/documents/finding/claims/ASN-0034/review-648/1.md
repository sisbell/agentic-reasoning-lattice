### *Consequence* slot in T4 carries a dep inventory with step-level rewrite instantiation
**Class**: REVISE
**Foundation**: T4 (HierarchicalParsing) — *Formal Contract*, *Consequence* bullet
**ASN**:
> "*Consequence:* `(A t ∈ T : zeros(t) ≤ 3 : zeros(t) ∈ {0, 1, 2, 3})` — for every `t ∈ T` with `zeros(t) ≤ 3`, `zeros(t) ∈ {0, 1, 2, 3}` (equivalently `zeros(t) = 0 ∨ zeros(t) = 1 ∨ zeros(t) = 2 ∨ zeros(t) = 3`). Derived below from the bound `zeros(t) ≤ 3`, NAT-zero's `0 ≤ zeros(t)`, NAT-order's trichotomy, NAT-discrete, and NAT-closure's left additive identity `(A n ∈ ℕ :: 0 + n = n)` at `n := 1` (collapsing NAT-discrete's output `0 + 1 ≤ zeros(t)` to `1 ≤ zeros(t)` at the `m = 0` step); NAT-card's codomain secures `zeros(t) ∈ ℕ` so that trichotomy applies."

**Issue**: A *Consequence* slot states the derived statement so a downstream consumer can cite it. The trailing "Derived below from … (collapsing NAT-discrete's output `0 + 1 ≤ zeros(t)` to `1 ≤ zeros(t)` at the `m = 0` step); NAT-card's codomain secures …" reproduces the proof's dep manifest and even names the local rewrite at `m = 0` — the same use-site-narration pattern previous cycles flagged in Depends entries and body prose, now relocated into the *Consequence* slot. A consumer instantiating the Consequence does not need the parenthetical rewrite step; the body paragraph carries that work.

**What needs resolving**: Trim the *Consequence* bullet to the universal statement (and the optional disjunction equivalent). Drop the "Derived below from …" inventory and the parenthetical "(collapsing NAT-discrete's output …)". If a single locator is wanted, "Derived in the *Exhaustion* paragraph below" is enough; do not list deps or step instantiations in the slot.

VERDICT: REVISE