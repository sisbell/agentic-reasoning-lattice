### Body *Exhaustion* preamble still names every dep before the derivation begins
**Class**: REVISE
**Foundation**: T4 (HierarchicalParsing) — body, opening of the *Exhaustion* paragraph
**ASN**:
> "We derive as a Consequence — universally quantified by the bound alone — that `(A t ∈ T : zeros(t) ≤ 3 : zeros(t) ∈ {0, 1, 2, 3})`, i.e. for every `t ∈ T` with `zeros(t) ≤ 3`, `zeros(t) ∈ {0, 1, 2, 3}`. The derivation uses only `zeros(t) ≤ 3`, NAT-zero, NAT-order's exactly-one trichotomy (Consequence), NAT-discrete, and NAT-closure's left additive identity (cited at the `m = 0` step below). NAT-card's codomain secures `zeros(t) ∈ ℕ` so that trichotomy applies at `zeros(t)`."

**Issue**: The previous cycle flagged a four-dep walkthrough sentence ("DEP supplies X, used at site Y") immediately before the derivation and asked for it to be dropped. The sentence was reworded rather than removed: a five-dep "uses only …" inventory plus a separate NAT-card sentence still preview every dep about to be cited, and "(cited at the `m = 0` step below)" is a forward-pointer into the same paragraph. The case-by-case derivation that follows already names each dep at the moment of use; the preamble is preview-of-cited-content with no reasoning load.
**What needs resolving**: Remove the preamble. Open the paragraph with the actual derivation step (the trichotomy at `(zeros(t), 0)`), letting the per-step citations stand. If the `|·|` vs `#·` disambiguation needs a line, keep that; do not enumerate which deps the proof will use.