### T4a Depends — NAT-zero omitted for direct use of `0 ∈ ℕ`
**Class**: REVISE
**Foundation**: NAT-zero (NatZeroMinimum) — sole source of `0 ∈ ℕ` as a first-class axiom
**ASN**: T4a (SyntacticEquivalence), Formal Contract Depends list; proof setup — "Set `s₀ = 0` and `s_{k+1} = #t + 1` as sentinels"; Forward Conditions (ii) and (iii) — `k = 0` case branches
**Issue**: The sentinel `s₀ = 0` introduces the ℕ-element `0` directly in T4a's proof body, not mediated by any T4 export. The `k = 0` case branches likewise compare `zeros(t)` against the ℕ-element `0`. The convention throughout the ASN set is to cite the ASN that provides the specific exported claim: T4c cites NAT-zero "for the literal `0` appearing in `zeros(t) = 0 ↔ t is a node address`"; NAT-addbound cites NAT-zero for `0 ≤ m` in its proofs; NAT-sub cites NAT-zero for `(A k ∈ ℕ :: 0 < k ∨ 0 = k)`. T4a's listed Depends include NAT-closure, which itself depends on NAT-zero, but NAT-closure does not export `0 ∈ ℕ` as its own axiom — it borrows it. T4a's direct use of `0 ∈ ℕ` for the sentinel has no grounding entry in the Depends list.
**What needs resolving**: Add NAT-zero (NatZeroMinimum) to T4a's Depends list with an entry specifying that it supplies `0 ∈ ℕ` for the sentinel value `s₀ = 0` and for the `k = 0` case branches in the Forward and Reverse derivations.

---

VERDICT: REVISE