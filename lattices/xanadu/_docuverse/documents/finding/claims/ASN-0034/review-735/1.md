### Divergence body cites "NAT-order transitivity" for a mixed `≤`-`<` chain
**Class**: OBSERVE
**Effective class**: REVISE (classifier override)
**Classifier rationale**: The artifact cites "NAT-order transitivity" for a mixed ≤-< chain that the stated transitivity consequences do not directly cover, leaving a proof gap the reader must silently close with an unjustified two-case argument.
**Foundation**: NAT-order (NatStrictTotalOrder)
**ASN**: Divergence (Divergence) — body, sub-cases (ii-a) and (ii-b): "i ≤ #a entails i ≤ #b by NAT-order transitivity" and the symmetric instance
**Issue**: NAT-order's stated transitivity consequences are `<`-transitivity (`m < n ∧ n < p → m < p`) and `≤`-transitivity (`m ≤ n ∧ n ≤ p → m ≤ p`). The step actually needed is the mixed chain `i ≤ #a ∧ #a < #b → i ≤ #b`, which requires unfolding `i ≤ #a` into `i < #a ∨ i = #a` and discharging each branch with `<`-transitivity or substitution respectively. Neither stated consequence covers this directly; the citation "NAT-order transitivity" is imprecise and requires the reader to silently execute a two-case argument.

---

VERDICT: REVISE