### TumblerSub promised but absent
**Class**: REVISE
**Foundation**: (internal structural claim — this ASN)
**ASN**: Introduction prose: "Its inverse — tumbler subtraction (⊖), which recovers the displacement between two positions — is constructed below as TumblerSub." TumblerAdd Forward References: "TumblerSub (TumblerSub) — the inverse operation, constructed below in this ASN; TumblerAdd's correctness does not depend on it."
**Issue**: The ASN commits to defining TumblerSub in two independent places — the opening prose and TumblerAdd's Forward References section. Neither is a tentative mention: the prose says "constructed below," and the Forward References field is a structural slot that names content in this ASN. The ASN ends at TA-RC without introducing TumblerSub or any section that could be mistaken for it. A downstream consumer relying on the Forward References to locate TumblerSub will find nothing.
**What needs resolving**: TumblerSub must either be defined in this ASN with its own Formal Contract, or both the introductory prose reference and the Forward References entry must be removed. If TumblerSub belongs to a different ASN, the Forward References entry must be corrected to name that ASN.

---

The remaining claims are internally consistent. The result-length identity `#(a ⊕ w) = #w` is correctly derived via NAT-sub's right- and left-inverse characterisations. Strict advancement and dominance are fully case-split, including the k = 1 degenerate case (vacuously zero prefix) and the `aₖ = 0` equality branch that discharges to `r = w` via T3. T1's trichotomy proof correctly applies NAT-wellorder to the nonempty set of divergence positions and exhausts all three case shapes. The transitivity sub-cases (i,i), (ii,i), (i,ii), and (ii,ii) are each walked; sub-case (i,ii) and (ii,ii) correctly terminate in contradiction. TA-RC's counterexample computation is verified by direct substitution. No dependency is ungrounded and no circular dependency is present.

VERDICT: REVISE