# Cone Review — ASN-0034/TA1-strict (cycle 4)

*2026-04-18 02:28*

### TA1-strict's Depends omits T0 despite using length and component-projection operators throughout
**Foundation**: T0 supplies the carrier set `T`, the length operator `#·`, and component projection `·ᵢ`. Per-step citation discipline established elsewhere in this ASN explicitly lists T0 whenever these operators appear: Divergence's Depends ("T0 (CarrierSetDefinition) — the membership `a, b ∈ T` in the Definition, together with the lengths `#a`, `#b` and the component projections `aₖ`, `bₖ`, `aᵢ`, `bᵢ` appearing throughout … all come from T0's characterisation"), T1's Depends ("T0 — the definition uses length `#a` and component projection `aₖ`"), ActionPoint's Depends, TumblerAdd's Depends, and even TA0's Depends ("T0 — supplies the meaning of `∈ T` in the postcondition `a ⊕ w ∈ T`; … matching the per-step citation discipline").
**ASN**: TA1-strict's *Depends* list enumerates Divergence, T1, TA-Pos, ActionPoint, TA0, TumblerAdd, NAT-addcompat, NAT-order, NAT-cancel — but no T0. The proof itself reads `#a`, `#b`, `#w`, `#(a ⊕ w)`, `#(b ⊕ w)`, `aⱼ`, `bⱼ`, `aₖ`, `bₖ`, `wₖ`, `aᵢ`, `bᵢ` repeatedly (e.g., the case-(ii) rule-out, the Case 1 prefix-copy step "the Divergence agreement property gives `aᵢ = bᵢ`", the Case 2 step "(a ⊕ w)ⱼ = aⱼ < bⱼ = (b ⊕ w)ⱼ").
**Issue**: Per-step convention requires the operators `#·`, `·ᵢ`, and `∈ T` to be sourced where consumed. TA1-strict's preconditions even include `a ∈ T, b ∈ T, w ∈ T` — semantics carried by T0 — yet T0 is left to flow transitively through Divergence/T1/ActionPoint/TumblerAdd/TA0. Sibling properties that only re-export postconditions (TA0) still cite T0 directly. A reviser tightening T0 will lack Depends-backed visibility into TA1-strict.
**What needs resolving**: Either add T0 to TA1-strict's Depends with the role enumerated (preconditions `a, b, w ∈ T`; lengths `#a`, `#b`, `#w`, `#(a ⊕ w)`, `#(b ⊕ w)`; component projections), or document a convention (and amend Divergence, T1, TA0, ActionPoint, TumblerAdd to match) under which T0 is deemed transitively sourced through other foundation citations and need not be re-listed.

### TA1-strict's `divergence(a, b)` precondition has no `a ≠ b` source
**Foundation**: Divergence's Definition opens "For tumblers `a, b ∈ T` with `a ≠ b`, the *divergence* `divergence(a, b)` is defined…". The contract restates "For `a, b ∈ T` with `a ≠ b`: …". The function is undefined when `a = b`, so any caller writing `divergence(a, b)` must have `a ≠ b` in scope.
**ASN**: TA1-strict's *Preconditions* list `a ∈ T, b ∈ T, w ∈ T, a < b, Pos(w), actionPoint(w) ≤ #a, actionPoint(w) ≤ #b, actionPoint(w) ≥ divergence(a, b)`. The precondition `actionPoint(w) ≥ divergence(a, b)` consumes `divergence(a, b)`, but `a ≠ b` does not appear in the precondition list. The proof's opening "Let `j = divergence(a, b)`" likewise does not establish `a ≠ b` before the invocation.
**Issue**: The well-formedness of the precondition `actionPoint(w) ≥ divergence(a, b)` itself requires `a ≠ b` to discharge Divergence's Definition-level precondition. The entailment `a < b ⟹ a ≠ b` holds via T1's irreflexivity (substituting `a = b` into `a < b` yields `a < a`, contradicting `¬(a < a)`), but this discharge step is not enumerated in TA1-strict's prose or in the T1 entry of its Depends. Per the per-step convention the ASN follows for analogous precondition chains (e.g., TumblerAdd's preconditions inheriting `Pos(w)` from TA-Pos and `actionPoint(w) ≤ #a` from ActionPoint), the `a ≠ b` premise needed by Divergence at the call site should be sourced where consumed.
**What needs resolving**: Either add `a ≠ b` explicitly to TA1-strict's preconditions (and note the `a < b ⟹ a ≠ b` derivation if downstream contracts prefer the implicit form), or extend the T1 Depends entry to enumerate the irreflexivity-mediated discharge of `a ≠ b` at the divergence-invocation site, so that Divergence's Definition-level precondition is visibly satisfied at the caller.
