# Regional Review — ASN-0034/NAT-card (cycle 1)

*2026-04-24 11:20*

### Label for `0 < 1` understates the axiom
**Class**: OBSERVE
**Foundation**: n/a (foundation ASN)
**ASN**: NAT-closure, Formal Contract axiom bullet: "`0 < 1` (distinctness of the two named constants)"
**Issue**: The parenthetical label names only distinctness, but `0 < 1` is strictly stronger than `0 ≠ 1` — it orients the two constants in the strict order. A reader scanning the label alone could reasonably conclude the axiom is symmetric in `0` and `1`. The axiom itself is correct; the label is just loose.

VERDICT: OBSERVE

## Result

Regional review converged after 1 cycles.

*Elapsed: 126s*
