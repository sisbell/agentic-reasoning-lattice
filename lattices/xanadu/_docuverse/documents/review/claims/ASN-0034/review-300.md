# Cone Review — ASN-0034/TA3 (cycle 1)

*2026-04-18 15:24*

### TA3 uses `min`/`max` primitives the rest of the ASN has systematically abandoned
**Foundation**: N/A — cross-cutting consistency between TA3 and the ASN's established operator convention (TumblerSub Definition, ZPD Definition, Divergence case (i), TA2 Depends).
**ASN**: TA3 Proof and Depends. Throughout: "`j ≤ min(#a, #b)`", "zero-pad both operands to length `max(#x, #w)`", "result length `max(#x, #w)`", "`dₐ ≤ min(#a, #w) ≤ #a`", "length `max(#a, #w) = #a`", "length `max(#b, #w) ≥ max(#a, #w)`", "`max(#a, #w) < max(#b, #w)`". Also Case B: "There exists a first position `j ≤ min(#a, #b)`".
**Issue**: TumblerSub, ZPD, Divergence, and TA2 all explicitly avoid `min(·, ·)` and `max(·, ·)` as "primitive binary-minimum/maximum operator on ℕ" and replace them with NAT-order trichotomy dispatch plus conjunction forms (e.g., `k ≤ #a ∧ k ≤ #w`). TA3 uses both operators freely and does not cite NAT-order trichotomy at the length pairs `(#a, #w)`, `(#b, #w)`, `(#a, #b)` to name them. The invocation "`j ≤ min(#a, #b)`" also misreads T1 case (i), whose definition in this ASN uses the conjunction `k ≤ #a ∧ k ≤ #b`.
**What needs resolving**: Either TA3 must rephrase every `min`/`max` use as a trichotomy-dispatched sub-case selection (matching TumblerSub's and ZPD's `L = ...` dispatch and Divergence case (i)'s conjunction form) and add the corresponding NAT-order citations, or the ASN must justify treating `min`/`max` as admitted primitives and loosen the convention it imposes on neighbouring properties.

### TA3 consumes zero-padded component values without citing NAT-zero
**Foundation**: N/A — cross-cutting citation discipline established in ZPD, ActionPoint, TA-Pos, TumblerSub, and TA2 Depends, each discharging `0 ∈ ℕ` from NAT-zero wherever a zero-padded component is compared or asserted ℕ-valued.
**ASN**: TA3 Sub-case A1 ("`bᵢ = 0` for all `i > #w`"), A2 tail ("`(a ⊖ w)ᵢ = 0` from `a`'s zero-padding", "`0` if `i > #b` (from `b`'s zero-padding)"), A3 (`aᵢ = 0` for `i > #w`), and B2/B3/B4 where `w_{dₐ}`, `w_{d_b}`, `a_{d_b}` appear at indices that may exceed `#w` or `#a`. Depends lists TA2, TumblerSub, ZPD, T1, T3, TA6, NAT-sub, NAT-order — but not NAT-zero.
**Issue**: TA3 writes component equalities and inequalities (e.g., "`a_{dₐ} ≠ w_{dₐ}` but `b_{dₐ} = w_{dₐ}`" in Sub-case B3, "`(b ⊖ w)_{d_b} = b_{d_b} - w_{d_b} > 0`" in Sub-case B4) where `w_{d_b}` or `w_{dₐ}` is the padded value `0` whenever the index exceeds `#w`. These are ℕ-valued comparisons that require `0 ∈ ℕ`, which T0 does not supply on indices outside native domains and which every sibling property in this ASN discharges from NAT-zero. TA3's Depends omits NAT-zero, so the padded comparisons are consumed without an axiomatic source.
**What needs resolving**: TA3 must either add NAT-zero to its Depends with per-site accounting of the zero-padded comparisons it consumes, or restructure the argument so that every `w_i`, `a_i`, `b_i` used at an index outside the operand's native domain is routed through a cited property (e.g., TumblerSub's postcondition or ZPD's padded projections) that already discharges the NAT-zero premise.
