# Regional Review — ASN-0034/TumblerAdd (cycle 1)

*2026-04-20 13:12*

### T1's NAT-order Depends omits the `≤` definition despite using it in Case 3
**Foundation**: NAT-order (NatStrictTotalOrder) — four clauses: irreflexivity, transitivity, trichotomy, and the non-strict relation definition `m ≤ n ⟺ m < n ∨ m = n`.
**ASN**: T1's Depends entry: "NAT-order (NatStrictTotalOrder) — irreflexivity, trichotomy, and transitivity of `<` on ℕ." T1's proof, part (b) Case 3: "case (ii) would require `n + 1 ≤ m`, which with NAT-addcompat's `n < n + 1` gives `n < m` (via NAT-order transitivity in the strict disjunct, or substitution in the equality disjunct), contradicting `m < n`." T1's proof, part (c) Case `k₂ < k₁`: "But `a < b` requires `k₁ ≤ n` (directly in case (i), via `k₁ = m + 1 ≤ n` in case (ii)), and NAT-addcompat gives `n + 1 > n`, so `k₂ > n ≥ k₁` by NAT-order transitivity".
**Issue**: The "strict disjunct" / "equality disjunct" split of `n + 1 ≤ m` is precisely NAT-order's ≤-defining clause `m ≤ n ⟺ m < n ∨ m = n` being unfolded; likewise, composing `k₂ > n` with `n ≥ k₁` into `k₂ > k₁` requires unfolding `n ≥ k₁` as `n > k₁ ∨ n = k₁`. Both are uses of NAT-order's ≤ definition, yet T1's Depends enumerates only irreflexivity, trichotomy, and transitivity of `<`. This is the mirror of the cycle-2 ActionPoint finding (which over-declared trichotomy/irreflexivity): here T1 under-declares by listing no edge for the ≤ defining clause. TumblerAdd's Depends entry for NAT-order, by contrast, does name "defining clause unfolds ≤ at dichotomy and strict-promotion sites," so the two consumer claims disagree about whether the ≤ defining clause is a load-bearing NAT-order edge at all.
**What needs resolving**: Reconcile the citation ledger — either extend T1's NAT-order Depends entry to list the ≤ defining clause with its use-sites (Case 3 disjunct split; part (c) `k₂ > n ≥ k₁` composition), or rewrite the proof text so the `≤`-to-disjunction moves are discharged by content already listed (e.g., by restating the Case 3 step purely in terms of `<` without invoking `n + 1 ≤ m`).

### TumblerAdd's proof block encloses three observational statements before ∎
**ASN**: TumblerAdd's proof closes dominance with "In every case: **`a ⊕ w ≥ w`**." and then continues: "Three properties of this definition require explicit statement: **No carry propagation:** ... **Tail replacement, not tail addition:** ... **The many-to-one property:** ... This is correct and intentional: advancing to 'the beginning of the next chapter' lands at the same place regardless of where you were within the current chapter. ∎"
**Issue**: The three "explicitly stated" properties (no carry propagation, tail replacement, many-to-one) are not postconditions declared in the formal contract and are not proof obligations: the ∎-terminated proof body is for the four postconditions `a ⊕ w ∈ T`, `#(a ⊕ w) = #w`, `a ⊕ w > a`, `a ⊕ w ≥ w`. The three properties are observational characterizations of the definition itself — "the sum … is a single natural-number addition", "components after the action point come entirely from w", "distinct start positions can produce the same result" — each flagged as a property that "requires explicit statement," not one that requires discharge. Placing them inside the proof block ending in ∎ asks the reader to parse them as potentially-being-proved content when they are statements of what the operation does. This is a placement issue, not an existence issue: these are exactly the "concrete examples and statements of what the operation does or does not do" that belong in commentary slots, but appear here between the last discharged postcondition and ∎.
**What needs resolving**: Either relocate the three properties out of the proof block (e.g., as a commentary subsection following TumblerAdd's formal contract, or as prose sitting between the end of the proof ∎ and the formal contract), or restate them as formal postconditions in the contract with their own discharge steps inside the proof — so the ∎-bounded region contains only content for which the proof is responsible.
