# Cone Review — ASN-0034/TA4 (cycle 3)

*2026-04-18 13:12*

### Case 1's strict-through-addition step `aₖ + wₖ > wₖ` lacks the NAT-addcompat + NAT-cancel citations TumblerAdd's identical site requires
**Foundation**: TumblerAdd's dominance proof, inner sub-case `aₖ > 0`. That sub-case derives `aₖ + wₖ > wₖ` (equivalently `rₖ > wₖ`) via an explicitly documented four-step chain: NAT-addcompat's right order-compatibility at `n = aₖ, p = 0, m = wₖ` lifts NAT-zero's `0 ≤ aₖ` into `aₖ + wₖ ≥ 0 + wₖ`; NAT-closure's additive identity rewrites `0 + wₖ` to `wₖ`; NAT-cancel's summand absorption in the symmetric form `n + m = m ⟹ n = 0` rules out the equality disjunct (which would force `aₖ = 0`, contradicting the sub-case hypothesis via NAT-order's irreflexivity); and NAT-order's defining clause then delivers the strict `aₖ + wₖ > wₖ`. TumblerAdd's Depends annotates this chain per-axiom and observes that the mirrored route through NAT-addcompat's left order-compatibility with `aₖ ≥ 1` would require `1 + wₖ > wₖ`, which is not available from T0's exhaustive NAT-* enumeration (it lacks commutativity of addition).
**ASN**: TA4 (PartialInverse), proof Step 2, Case 1: "Then `rₖ = aₖ + wₖ > wₖ` (since `aₖ > 0`), so `rₖ ≠ wₖ` and the first divergence is at position `k`." TA4's Depends lists T0(implicit), TA-Pos, ActionPoint, TumblerAdd, TA0, T1, TumblerSub, T3, NAT-order, NAT-sub — NAT-addcompat and NAT-cancel are absent.
**Issue**: TA4's Case 1 consumes the exact strict-through-addition step TumblerAdd devotes a four-axiom chain to at its analogous site, but elides the chain behind "(since `aₖ > 0`)" and lists neither NAT-addcompat nor NAT-cancel in Depends. The per-step citation convention TumblerAdd establishes for this pattern — precisely because T0's enumeration excludes commutativity and the conclusion cannot be reached through a shorter route — is broken here while invoking the same conclusion. TA4 also consumes `rₖ > wₖ` (not merely `rₖ ≥ wₖ`) as the premise for TumblerSub's divergence-point precondition consequence and for NAT-sub's right-telescoping applicability, so the strict form is load-bearing and not a casual aside.
**What needs resolving**: TA4 must either discharge Case 1's `aₖ + wₖ > wₖ` via the same NAT-addcompat (right order-compatibility) + NAT-closure (additive identity) + NAT-cancel (summand absorption) + NAT-order chain TumblerAdd enforces, listing NAT-addcompat and NAT-cancel in Depends with per-site entries, or route the step through an already-cited lemma (e.g., re-invoking TumblerAdd's dominance postcondition `a ⊕ w ≥ w` together with a separate argument that at position `k` the dominance is strict under the case hypothesis).
