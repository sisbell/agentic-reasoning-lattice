# Cone Review — ASN-0034/ReverseInverse (cycle 7)

*2026-04-18 20:36*

### Step 3's "by Y3, `aₖ = wₖ`" inference from `yₖ = 0` is an unsourced NAT-sub + NAT-closure chain

**Foundation**: NAT-sub (NatPartialSubtraction) — the right-inverse characterisation `(m − n) + n = m` under `m ≥ n`; NAT-closure (NatArithmeticClosureAndIdentity) — the additive identity `0 + n = n`.

**ASN**: ReverseInverse, Step 3, `*y ⊕ w > w*` sub-section's `yₖ > 0` rule-out sub-proof: "If `yₖ = 0`, then by Y3, `aₖ = wₖ`." Y3 (promulgated at the close of Step 1) states `yₖ = aₖ - wₖ`.

**Issue**: From `yₖ = 0` and Y3's `yₖ = aₖ - wₖ`, we get `aₖ - wₖ = 0`. Concluding `aₖ = wₖ` from `aₖ - wₖ = 0` is not immediate — it consumes (a) NAT-sub's right-inverse characterisation `(aₖ - wₖ) + wₖ = aₖ` under the premise `aₖ ≥ wₖ` (which itself is derived from Step 1's case split and needs NAT-order's defining-clause conversion from the branch's `aₖ > wₖ ∨ aₖ = wₖ`), substituting `aₖ - wₖ = 0` to give `0 + wₖ = aₖ`, and (b) NAT-closure's additive identity `0 + wₖ = wₖ` to yield `wₖ = aₖ`. ReverseInverse's NAT-sub Depends entry enumerates only conditional closure and strict positivity (both at the Step 1 divergence branch); NAT-closure's additive identity entry enumerates the `yₖ = 0` rule-out's `(y ⊕ w)ₖ = 0 + wₖ = wₖ` step and the strict-promotion chain's right-hand-side rewrite, but not the `aₖ - wₖ = 0 ⟹ aₖ = wₖ` step inside the same sub-proof. Under the per-step citation convention this ASN enforces ("every NAT-* step must be discharged by a Depends-backed axiom citation"), the two-step inference bridging `yₖ = 0` to `aₖ = wₖ` — load-bearing for the T3 application that follows — is unsourced.

Additionally, the same consistency question arises for Y3's own statement across Step 1's two branches: in the equality branch (`aₖ = wₖ`), `y` is the zero tumbler of length `k`, so yₖ = 0 literally; Y3's `yₖ = aₖ - wₖ` then requires `aₖ - wₖ = 0` under `aₖ = wₖ`, the same right-inverse + additive-identity step that Step 3 silently re-consumes. Either Y3 must be split by branch (equality branch: `yₖ = 0`; divergence branch: `yₖ = aₖ - wₖ`), or the uniform Y3 must explicitly cite the NAT-sub + NAT-closure step that makes it hold in the equality branch.

**What needs resolving**: Either (i) expand the Step 3 "by Y3, `aₖ = wₖ`" step with explicit NAT-sub right-inverse + NAT-closure additive-identity citations (and add per-site entries to the existing NAT-sub and NAT-closure Depends blocks), and do the same for Y3's equality-branch consistency at the close of Step 1, or (ii) restate Y3 as a branch-indexed pair of properties (`yₖ = 0` in the equality branch, `yₖ = aₖ - wₖ` in the divergence branch) and rewrite Step 3's sub-proof to case-split on which Step 1 branch the hypothetical `yₖ = 0` places us in (only the equality branch is consistent with `yₖ = 0`, since the divergence branch produces `yₖ > 0` via NAT-sub strict positivity), whereupon `aₖ = wₖ` follows directly from the branch hypothesis without a NAT-sub inference.
