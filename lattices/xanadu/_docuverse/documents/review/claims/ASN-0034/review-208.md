# Cone Review — ASN-0034/TA-assoc (cycle 4)

*2026-04-17 23:06*

### TA-assoc: "both summands are positive" → `s_k > 0` uses NAT-addcompat without citation
**Foundation**: NAT-addcompat (NatAdditionOrderAndSuccessor) — supplies order-compatibility of addition on ℕ (e.g., `n ≥ p ⟹ m + n ≥ m + p`), the only axiom in T0's enumeration that lifts component-level ordering through a sum.
**ASN**: TA-assoc (AdditionAssociative), *Action point of `s`* paragraph, sub-case `k_b = k_c = k`: "`s_{k_b} = b_{k_b} + c_{k_b} > 0`, since both summands are positive action-point values." TA-assoc's Depends lists T0, TumblerAdd, TA0, TA-Pos, ActionPoint, T3, NAT-addassoc, NAT-closure, NAT-order (adding the four already flagged in previous findings) — NAT-addcompat is not cited.
**Issue**: The step from "both summands are positive" (ActionPoint supplies `b_k ≥ 1` and `c_k ≥ 1`) to the inequality `b_k + c_k > 0` is not discharged by any cited axiom. NAT-closure gives closure and additive identity, NAT-order gives trichotomy/transitivity, NAT-zero gives `0 ≤ n` — none of these lifts the per-summand positivity through `+` to strict positivity of the sum. The canonical route is NAT-addcompat's order-compatibility (e.g., `0 ≤ c_k` composed with `b_k + c_k ≥ b_k + 0 = b_k ≥ 1`), the same pattern TumblerAdd's dominance proof routes explicitly through NAT-addcompat's right order-compatibility + NAT-closure's additive identity at structurally identical sites. Without the citation the "positive action-point values" inference appeals to background monotonicity of `+` on ℕ — the very appeal NAT-addassoc's rationale warns against and TumblerAdd's per-step citation discipline otherwise avoids.
**What needs resolving**: Either cite NAT-addcompat in TA-assoc's Depends with the specific sub-step it discharges (lifting per-summand positivity through `+` to strict positivity of the sum in the `k_b = k_c` sub-case of *Action point of `s`*), or rephrase the sub-case to route `b_k + c_k > 0` through an already-cited source.
