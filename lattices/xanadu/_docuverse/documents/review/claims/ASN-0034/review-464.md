# Regional Review — ASN-0034/NAT-addbound (cycle 7)

*2026-04-24 01:07*

### Rationale "recorded as a named theorem rather than an axiom" is triple-stated across prose and both Consequence bullets
**Class**: REVISE
**Foundation**: n/a (foundation ASN)
**ASN**: NAT-addbound — the opening prose says "recorded jointly as a named theorem so that NAT-sub's right-telescoping clause `(m + n) − n = m` can discharge the implicit precondition `m + n ≥ n`, and its left-telescoping clause `(n + m) − n = m` can discharge the implicit precondition `n + m ≥ n` ... without each downstream consumer re-deriving the lifts." Then the right-dominance Consequence bullet restates: "recorded as a named theorem rather than an axiom so NAT-sub's conditional-closure precondition `m + n ≥ n` can be discharged without each downstream consumer re-deriving it." Then the left-dominance Consequence bullet restates a third time: "recorded as a named theorem rather than an axiom so NAT-sub's left-telescoping precondition `n + m ≥ n` (this clause instantiated at `(m, n) := (n, m)`) can be discharged without each downstream consumer re-deriving it."
**Issue**: The same meta-rationale — *why NAT-addbound is recorded as a Consequence rather than an Axiom, and which downstream NAT-sub precondition it exists to discharge* — appears three times in a single claim: once in prose, once per Consequence bullet. A reader walking the Formal Contract meets the justification at every Consequence bullet even though it is about the structural choice of slot (Consequence vs Axiom), not about what the stated inequality asserts. This is the same defensive-meta-prose pattern called out earlier in the document; here it has localised into redundancy within one claim rather than across claims. A reviser fixing the prose rationale could leave either Consequence bullet stale, and the reader cannot tell whether the bullets' wording is meant to differ from the prose or merely repeats it.
**What needs resolving**: State the "recorded as Consequence because downstream consumers need to cite it without re-deriving" rationale in exactly one place — either the prose or a single meta-note — and strip it from the Consequence bullets, which should state what the inequality asserts and cite the proof. If the bullets need a pointer, let them say "derived as shown in the preceding prose" (the convention the bullets already partially adopt for the proof itself) without re-entering the axiom/theorem structural argument.

### Disjointness form is structurally buried inside the exactly-one-trichotomy Consequence bullet yet textually claims to be "exported alongside" it
**Class**: REVISE
**Foundation**: n/a (foundation ASN)
**ASN**: NAT-order Consequence — the single Consequence bullet states exactly-one trichotomy, then inside the prose of that bullet says: "The disjointness form `(A m, n ∈ ℕ : m < n : m ≠ n)` is the contrapositive of `¬(m < n ∧ m = n)` and is exported alongside exactly-one trichotomy as a Consequence."
**Issue**: A downstream consumer scanning the Formal Contract's Consequence bullets finds one bullet — exactly-one trichotomy. The disjointness form `m < n ⟹ m ≠ n` is named inside that bullet's explanatory prose and asserted to be "exported alongside ... as a Consequence," but it has no Consequence bullet of its own. Either the disjointness form is a separate exported Consequence (in which case the structural convention of the rest of the document — one bullet per named fact — requires it to be its own bullet so downstream Depends citations can point at it unambiguously), or it is merely a contrapositive restatement of a conjunct of the exactly-one trichotomy (in which case "exported alongside ... as a Consequence" overstates what the bullet does and should be softened to a reading note). As written, the text claims one structural status while the formal contract realises another, and a consumer who wants to cite NAT-order for `m < n ⟹ m ≠ n` cannot tell whether the correct citation target is the exactly-one trichotomy bullet, a not-present disjointness bullet, or both.
**What needs resolving**: Pick one. Either add a dedicated `*Consequence:*` bullet for the disjointness form with its own short derivation (contrapositive of `¬(m < n ∧ m = n)`) so it can be cited independently, or demote the "exported alongside ... as a Consequence" sentence to a reading note that clarifies the form is a derivable restatement of an exactly-one-trichotomy conjunct and is not a separately exported Consequence. Either way, the structural realisation and the textual claim must agree.

### NAT-addbound's NAT-closure Depends bullet appends a non-citation clause about what the claim does with `+`
**Class**: OBSERVE
**Foundation**: n/a
**ASN**: NAT-addbound Depends, NAT-closure bullet — after citing the two identity rewrites (`0 + n = n` at `k := n`, `k + 0 = k` at `k := m`), the bullet appends: "Also underpins the binary operation `+` whose value `m + n` is here bounded below by each operand."
**Issue**: Every other Depends bullet in this ASN states what the dependency supplies and where the supplied fact is instantiated in the proof. The trailing "Also underpins the binary operation `+` whose value `m + n` is here bounded below by each operand" supplies no new citation target — the signature `+ : ℕ × ℕ → ℕ` is not invoked at a specific step in the proof; it is just the ambient commitment that `m + n` is a ℕ-element. The clause is meta-prose about what the overall claim is doing (bounding `m + n` by its operands), not about what NAT-closure contributes to the derivation. The other NAT-* foundations' bullets (NAT-zero, NAT-addcompat, NAT-order) end cleanly at their last instantiation; only NAT-closure's carries this trailing commentary.
**What needs resolving**: Drop the trailing sentence, or — if the intent is to flag that NAT-closure's signature clause (not the identities) is separately needed to type `m + n : ℕ` — cite it as a discrete instantiation in the same form as the other bullets ("... and the signature clause `+ : ℕ × ℕ → ℕ` ensures `m + n ∈ ℕ` as the subject of the inequality").

VERDICT: REVISE
