# Cone Review — ASN-0036/D-CTG-depth (cycle 8)

*2026-04-14 23:25*

### D-CTG-depth's formal preconditions for postcondition (1) omit three foundation properties that the proof critically depends on
**Foundation**: T0(a) (UnboundedComponentValues, ASN-0034) — produces intermediates in T; T3 (CanonicalRepresentation, ASN-0034) — establishes distinctness of intermediates; T1 (LexicographicOrder, ASN-0034) — provides all ordering comparisons
**ASN**: D-CTG-depth formal contract: "*Preconditions (postcondition 1):* `d ∈ D` (D-DOC, Document); `V_S(d) ⊆ dom(Σ.M(d))` (V_S(d), SubspaceVPositionSet), `dom(Σ.M(d)) ⊆ T` (Σ.M(d)), providing carrier-set membership for T1; V_S(d) non-empty; common depth m (S8-depth); m ≥ 3; D-CTG (VContiguity); S8-fin (finite arrangement)." Proof text: "applying T0(a) (UnboundedComponentValues, ASN-0034) to v₁ at position j + 1"; "by T1(i), t' > v₁"; "by T1(i), t' < v₂"; "Distinct values at position j + 1 yield distinct tumblers (by T3, CanonicalRepresentation, ASN-0034)"
**Issue**: The proof of postcondition (1) explicitly invokes T0(a) (to produce intermediates guaranteed to be in T), T3 (to establish that distinct component values yield distinct tumblers, completing the infinite-set contradiction), and T1 (for every ordering comparison that places intermediates between v₁ and v₂ and thereby triggers D-CTG). None of these three appears in postcondition (1)'s formal precondition list. T1 is obliquely acknowledged — "providing carrier-set membership for T1" explains why `dom(M(d)) ⊆ T` is needed — but carrier-set membership is a sub-requirement of T1, not T1 itself; listing the sub-requirement without the property is like listing "x ∈ ℝ" as a precondition while omitting the Cauchy-Schwarz inequality that motivated it. T0(a) and T3 are entirely absent from postcondition (1)'s preconditions, yet the proof cannot function without either: without T0(a), no intermediates can be constructed with guaranteed T-membership; without T3, the intermediates might not be distinct and the infinite-cardinality contradiction fails. The omission is internally inconsistent within D-CTG-depth's own formal contract: postcondition (2)'s precondition note explicitly names T0(a) and T3 as properties it does *not* require ("without invoking D-CTG, S8-fin, T0(a), or T3"), demonstrating that the author tracked these properties as relevant — yet never declared them where they *are* required, in postcondition (1). A consumer tracing D-CTG-depth's dependency chain from the formal contract alone would find no path to T0(a) or T3 and could not verify that the proof's intermediate-construction argument has a foundation-level anchor.
**What needs resolving**: List T0(a), T3, and T1 as explicit preconditions for postcondition (1), each with its role: T0(a) for producing T-member intermediates, T3 for establishing their distinctness, and T1 for the ordering arguments that place intermediates within the D-CTG quantifier's scope.

## Result

Cone not converged after 8 cycles.

*Elapsed: 13275s*
