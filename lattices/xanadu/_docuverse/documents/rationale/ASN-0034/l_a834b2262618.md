The finding describes a copy-paste error in T1's (γ)-impossible sub-case, but the file already contains the correct derivation: 'the first branch combines with NAT-addcompat's n < n + 1 by NAT-order's transitivity to yield n < m, and the second substitutes n + 1 = m into n < n + 1 to yield n < m'. The text the finding claims is in the file (using m < m + 1 to yield m < n) is not present in line 27 — all four parallel reverse-derivation passages in Case 3 already use the correct NAT-addcompat instantiation.
