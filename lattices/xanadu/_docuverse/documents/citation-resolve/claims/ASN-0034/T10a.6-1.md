# Citation Resolve — ASN-0034/T10a.6 — run 1

*2026-04-30T05:01:03Z*
*Model: sonnet*

## Output

T10a.7 appears in the prose ("requires the within-allocator injectivity of T10a.7") and isn't classified. It's a forward reference — T10a.6's own proof is complete; T10a.7 is named as the downstream claim that handles the injectivity piece of the consequence. All five existing depends labels remain in the prose, so no retractions.

```
CLASSIFICATIONS:
- label: T10a.7
  direction: forward
  bullet: "- T10a.7 (EnumerationInjectivity) — elaborates the witness-uniqueness consequence: supplies within-allocator index injectivity so that the enumeration indices `(i, j)` with `a = tᵢ, b = tⱼ` are single-valued once T10a.6 has fixed the witnessing allocator."

RETRACTIONS: []
```
