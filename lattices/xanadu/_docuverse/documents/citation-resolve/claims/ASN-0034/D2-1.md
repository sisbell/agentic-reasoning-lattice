# Citation Resolve — ASN-0034/D2 — run 1

*2026-04-30T04:27:19Z*
*Model: sonnet*

## Output

D0 appears as `*D0 check.*` in the worked example — a navigation label, not a proof step. D2 re-derives D0's conclusions from TumblerSub/ZPD/NAT-sub directly. All 13 currently-classified depends labels are still present in the prose; no retractions needed.

```
CLASSIFICATIONS:
- label: D0
  direction: forward
  bullet: "- D0 (DisplacementWellDefined) — named in the worked example as the precondition checkpoint; D2 re-derives its conclusions independently."

RETRACTIONS: []
```
