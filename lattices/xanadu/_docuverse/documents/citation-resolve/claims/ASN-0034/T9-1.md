# Citation Resolve — ASN-0034/T9 — run 1

*2026-04-30T05:07:13Z*
*Model: sonnet*

## Output

All five labels already classified are still present in the prose. I need to check whether any direction is wrong.

**TA5** — the formal contract bullet reads *"no direct use at T9 after the rewrite."* The claim's own prose disavows a direct dependency: TA5 feeds T10a.7 internally, but T9's proof never invokes it. Classifying it as `depends` of T9 is wrong; it is a transitive note, not a direct consumed ingredient. It fits neither `depends` nor `forward` (TA5 is not downstream of T9), so it should be retracted without reclassification.

All other directions hold:
- **T10a** supplies `dom(A)`, `same_allocator`, and the enumeration — directly consumed. → `depends` ✓  
- **T10a.6** uniquely fixes the allocator in the proof step — directly consumed. → `depends` ✓  
- **T10a.7** supplies both index uniqueness and the strict-order postcondition instantiated in the proof — directly consumed. → `depends` ✓  
- **T1** supplies the total order `<` in which the conclusion is phrased — directly consumed. → `depends` ✓

```
CLASSIFICATIONS: []

RETRACTIONS:
- label: TA5
  direction: depends
  reason: "The prose explicitly states 'no direct use at T9 after the rewrite' — TA5 is an internal ingredient of T10a.7's own proof, not consumed by T9 directly. Classifying it as a depends of T9 misrepresents the proof structure."
```
