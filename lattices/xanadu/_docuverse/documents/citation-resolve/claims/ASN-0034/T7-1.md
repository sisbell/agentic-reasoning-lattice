# Citation Resolve — ASN-0034/T7 — run 1

*2026-04-30T05:06:03Z*
*Model: sonnet*

## Output

The final paragraph references `T1` — not yet classified. The claim names T1's lexicographic order as the source of a downstream consequence (text before link addresses); T7's proof doesn't rest on T1. That's a `forward` reference.

All eleven currently-classified `depends` labels appear in the formal contract prose — no retractions.

```
CLASSIFICATIONS:
- label: T1
  direction: forward
  bullet: "- T1 (LexicographicOrder) — the ordering T1 induces places all text addresses (subspace 1) before all link addresses (subspace 2) within the same document, as a consequence of `1 < 2` at the subspace position under lexicographic order."

RETRACTIONS: []
```
