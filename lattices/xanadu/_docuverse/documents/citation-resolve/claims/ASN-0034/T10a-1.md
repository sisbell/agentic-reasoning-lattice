# Citation Resolve — ASN-0034/T10a — run 1

*2026-04-30T04:48:35Z*
*Model: sonnet*

## Output

All referenced claim labels are confirmed real. Now I have enough to produce the classifications. The claim body references exactly these external claim labels, all as `depends`:

- **T4**, **TA5**, **TA5a**, **Prefix**, **T3**, **T0**, **NAT-addcompat**, **NAT-zero**, **NAT-discrete**, **NAT-closure**, **TA5-SigValid**, **T1**, **NAT-order**

There are no forward references in the prose (the T10a.1–T10a.8 and T10a-N labels are sub-consequences of T10a itself, not citations to external claims).

```
CLASSIFICATIONS:
- label: T4
  direction: depends
  bullet: "- T4 (HierarchicalParsing) — supplies the field-segment constraint (T4-validity) and the `zeros(·)` function; T10a's axiom requires the root's base to satisfy T4 and imposes `zeros(t) ≤ 2` at child-spawning step `k' = 2`; T10a.4 establishes that every conforming allocator output satisfies T4."

- label: TA5
  direction: depends
  bullet: "- TA5 (HierarchicalIncrement) — supplies the `inc` operator, positional agreement under `k > 0` (TA5(b)), length preservation under `k = 0` (TA5(c)), strict order `inc(t, k) > t` (TA5(a)), and length increment by `k` (TA5(d)); threaded through every consequence and the necessity argument."

- label: TA5a
  direction: depends
  bullet: "- TA5a (IncrementPreservesT4) — supplies the T4-preservation envelope: unconditional under `inc(·, 0)` and `inc(·, 1)` on T4-valid inputs, conditional under `inc(·, 2)` when `zeros(t) ≤ 2`; drives T10a.4's induction step and the field-segment constraint argument in the necessity section."

- label: Prefix
  direction: depends
  bullet: "- Prefix (PrefixRelation) — supplies the prefix relation `≼` and its positional-agreement clause; used in T10a.2's collapse of sibling prefix-relation to equality and in T10a.5's closure step contradicting `x ≼ y` from a divergence position."

- label: T3
  direction: depends
  bullet: "- T3 (CanonicalRepresentation) — collapses equal-length positional agreement to identity; used in T10a.2 to rule out distinct equal-length siblings being prefix-related, and in T10a.5's base case to extract a divergence position from `tₓ ≠ tᵧ` at common length `m`."

- label: T0
  direction: depends
  bullet: "- T0 — fixes the carrier ℕ as the index domain for tumbler positions and the spawn parameter `k' ∈ {1, 2}`; underpins ℕ-typed quantification throughout."

- label: NAT-addcompat
  direction: depends
  bullet: "- NAT-addcompat (NatAdditionOrderAndSuccessor) — supplies the strict successor `n < n + 1` and left order-compatibility on `<`; used in T10a.3's length-separation chain (`#t < #t + 1 ≤ #t + k'`), T10a-N's necessity argument, and T10a.8's positivity propagation."

- label: NAT-zero
  direction: depends
  bullet: "- NAT-zero (NatZeroMinimum) — supplies `0 ≤ n` for `n ∈ ℕ`; used in T10a.3 to lift `k' > 0` to `1 ≤ k'`, and in T10a.5 and T10a.8 to ground positivity arguments."

- label: NAT-discrete
  direction: depends
  bullet: "- NAT-discrete (NatDiscreteOrder) — sharpens `n > 0` to `n ≥ 1` on ℕ; used in T10a.3 and T10a-N to obtain `1 ≤ k'` from `k' > 0`, and in T10a.8 to derive `(tₙ)_{#tₙ} ≥ 1`."

- label: NAT-closure
  direction: depends
  bullet: "- NAT-closure (NatArithmeticClosureAndIdentity) — supplies addition closure (`n + 1 ∈ ℕ`) and the literal `1 ∈ ℕ`; used in T10a.5 and T10a.8 to keep successor-positivity arguments inside ℕ."

- label: TA5-SigValid
  direction: depends
  bullet: "- TA5-SigValid — fixes `sig(t) = #t` for T4-valid tumblers; used in T10a.5 to identify which positions `inc(·, 0)` modifies and in T10a.8 to localize the zero-count argument to position `#tₙ`."

- label: T1
  direction: depends
  bullet: "- T1 (StrictTotalOrderOnTumblers) — supplies irreflexivity (T1(a)) and transitivity (T1(c)) of `<`; used in T10a.7 to derive injectivity of `n ↦ tₙ` from successor strict-positivity."

- label: NAT-order
  direction: depends
  bullet: "- NAT-order (NatStrictTotalOrder) — supplies trichotomy on ℕ; used in T10a.7 to resolve `m ≠ n` into `m < n ∨ n < m` for the injectivity argument."

RETRACTIONS: []
```
