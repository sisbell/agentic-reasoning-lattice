# Signature Resolve — ASN-0034/TA-Pos — run 1

*2026-04-30T20:28:56Z*
*Model: sonnet*

## Output

INTRODUCES:
- bullet: "- `Pos` — unary predicate on T; `Pos(t)` holds iff tumbler `t` has at least one nonzero component"
- bullet: "- `Zero` — unary predicate on T; `Zero(t)` holds iff every component of tumbler `t` equals `0`"
- bullet: "- `Z` — the set of all zero tumblers; `Z = {t ∈ T : Zero(t)}`"

REMOVES: []
