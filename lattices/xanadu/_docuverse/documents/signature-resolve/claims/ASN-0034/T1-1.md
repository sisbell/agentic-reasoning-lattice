# Signature Resolve — ASN-0034/T1 — run 1

*2026-04-30T20:20:23Z*
*Model: sonnet*

## Output

INTRODUCES:
- bullet: "- `<` — lexicographic strict total order on T; defined by component-wise ℕ comparison with the proper-prefix convention (prefix is less than any proper extension)"
- bullet: "- `≤` — non-strict companion order on T; abbreviates `a < b ∨ a = b` for tumblers"
- bullet: "- `≥` — reverse non-strict order on T; abbreviates `b ≤ a` for tumblers"
- bullet: "- `>` — reverse strict order on T; abbreviates `b < a` for tumblers"

REMOVES: []
