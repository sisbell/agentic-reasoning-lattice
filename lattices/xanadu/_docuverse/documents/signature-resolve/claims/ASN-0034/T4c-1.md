# Signature Resolve — ASN-0034/T4c — run 1

*2026-04-30T20:25:58Z*
*Model: sonnet*

## Output

INTRODUCES:
- bullet: "- `node address` — unary predicate on T; `t is a node address` iff `zeros(t) = 0` on the T4-valid subdomain"
- bullet: "- `user address` — unary predicate on T; `t is a user address` iff `zeros(t) = 1` on the T4-valid subdomain"
- bullet: "- `document address` — unary predicate on T; `t is a document address` iff `zeros(t) = 2` on the T4-valid subdomain"
- bullet: "- `element address` — unary predicate on T; `t is an element address` iff `zeros(t) = 3` on the T4-valid subdomain"

REMOVES: []
