# Signature Resolve — ASN-0034/T4 — run 1

*2026-04-30T20:24:12Z*
*Model: sonnet*

## Output

INTRODUCES:
- bullet: "- `zeros` — function `zeros : T → ℕ` returning the count of zero-valued components of a tumbler; defined as `zeros(t) = |{i : 1 ≤ i ≤ #t ∧ tᵢ = 0}|`"
- bullet: "- `2` — numeral constant in ℕ, defined as `2 := 1 + 1` via NAT-closure"
- bullet: "- `3` — numeral constant in ℕ, defined as `3 := 2 + 1` via NAT-closure"

REMOVES: []
