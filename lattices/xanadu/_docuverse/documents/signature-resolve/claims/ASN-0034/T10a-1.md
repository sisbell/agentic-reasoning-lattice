# Signature Resolve — ASN-0034/T10a — run 1

*2026-04-30T20:21:30Z*
*Model: sonnet*

## Output

INTRODUCES:
- bullet: "- `𝒯` — the carrier set of allocators; the tree induced by the spawning rule, containing the root and every allocator reachable by finite iteration of child-spawning"
- bullet: "- `parent(·)` — function mapping each non-root allocator to its parent in `𝒯`"
- bullet: "- `spawnPt(·)` — function mapping each non-root allocator to the tumbler in the parent's domain at which it was spawned"
- bullet: "- `spawnParam(·)` — function mapping each non-root allocator to the spawn parameter `k' ∈ {1, 2}` used at its creation"
- bullet: "- `dom(·)` — function mapping each allocator `A` to its output domain `{tₙ : n ≥ 0}` generated by iterated `inc(·, 0)` from `A`'s base"
- bullet: "- `same_allocator(·, ·)` — binary predicate asserting that two tumblers lie in the domain of a common allocator"
- bullet: "- `depth(·)` — function on `𝒯` satisfying `depth(root) = 0` and `depth(Y) = depth(parent(Y)) + 1`"

REMOVES: []
