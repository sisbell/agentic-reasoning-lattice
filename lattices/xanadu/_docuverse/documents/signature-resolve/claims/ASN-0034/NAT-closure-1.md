# Signature Resolve — ASN-0034/NAT-closure — run 1

*2026-04-30T20:15:38Z*
*Model: sonnet*

## Output

INTRODUCES:
- bullet: "- `+` — binary addition operation on ℕ, with signature `+ : ℕ × ℕ → ℕ`"
- bullet: "- `1` — the second named constant in ℕ, distinct from `0` in the strict order"

REMOVES: []
