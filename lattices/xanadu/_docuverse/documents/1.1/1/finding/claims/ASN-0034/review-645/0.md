### Use-site narration in T4 body prose
**Class**: REVISE
**Foundation**: T4 (HierarchicalParsing)
**ASN**: T4 body, the "purely definitional" paragraph:
> "T4 is purely definitional: it characterises T4-valid as a predicate on `T` (the four-conjunct conjunction above) without asserting which `t ∈ T` satisfy it; downstream consumers requiring T4-validity (T4b's projection domains `dom(N), dom(U), dom(D), dom(E)`; T4c's level subdomain) carry it as an explicit precondition rather than receiving it from T4."

**Issue**: The parenthetical inventories which downstream sites consume T4-validity. T4's correctness does not depend on what T4b's `dom` is or what T4c's level subdomain is, and the body cannot — without re-stating those claims — explain or justify those downstream constructs anyway. This is the same use-site-inventory pattern the prior cycle flagged in Depends entries, here relocated into the body. A reader chasing T4-validity has to skip past it to reach the next definitional clause.
**What needs resolving**: State the predicate and stop. If a remark about who carries the precondition is wanted, leave one declarative line that names the discipline ("T4-validity is a precondition at the use-site, not derived here") without enumerating which downstream claims rely on it.