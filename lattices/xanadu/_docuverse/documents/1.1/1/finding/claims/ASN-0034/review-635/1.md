### NAT-order's Formal-Contract Consequences restate the body proofs verbatim
**Class**: OBSERVE
**Foundation**: NAT-order (NatStrictTotalOrder)
**ASN**: The two *Consequence* bullets in NAT-order's Formal Contract — exactly-one trichotomy and `≤`-transitivity — each carry full derivation prose duplicating the body. Example: the body says `¬(m < n ∧ m = n)` "follows by substituting `m = n` into `m < n` via indiscernibility of `=`, rewriting to `m < m`, which irreflexivity at `n := m` rules out"; the contract bullet says `¬(m < n ∧ m = n)` "follows by substituting `m = n` into `m < n` via indiscernibility of `=`, rewriting to `m < m` against irreflexivity at `n := m`." The four-case unfolding for `≤`-transitivity is similarly reproduced word-for-word in both slots.
**Issue**: The body is the place to walk a derivation; the contract's *Consequence* slot is the place to state what is consequence-of-axiom and (where useful) point at where the derivation lives. Doubling the derivation prose forces the precise reader to scan two near-identical paragraphs to confirm they really say the same thing — and creates two surfaces to keep in sync if the derivation is later sharpened. Sound as written; flagged as a structural-slot/body duplication pattern likely to accrete across cycles.