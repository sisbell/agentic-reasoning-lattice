### Presentation order is reversed relative to the dependency DAG
**Class**: OBSERVE
**ASN**: Sections appear T0 → NAT-closure → NAT-order → NAT-zero → NAT-carrier → NAT-discrete → TA-Pos, but the DAG order is NAT-carrier → NAT-order → {NAT-zero, NAT-closure} → NAT-discrete → T0 → TA-Pos.
**Issue**: A reader walking top-to-bottom encounters `<`, `+`, `0`, and `1` before their introducing axioms. Soundness is preserved by the *Depends:* lists, but the prose-level justifications in earlier sections (e.g. NAT-closure's `0 < 1`, T0's `1 ≤ #a`) reference machinery the reader has not yet seen.

VERDICT: REVISE