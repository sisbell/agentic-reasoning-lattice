### Tautological chain in T10
**Class**: OBSERVE
**Foundation**: —
**ASN**: T10, post-case-split paragraph: "k ≤ ℓ ≤ min(m, n)"
**Issue**: ℓ is defined as min(m, n) at the opening of the proof. The chain `k ≤ ℓ ≤ min(m, n)` therefore reads `k ≤ ℓ ≤ ℓ`, collapsing to `k ≤ ℓ`. The second inequality adds nothing.

---