### Numerals 2 and 3 have no formal definition entry
**Class**: OBSERVE
**Foundation**: T4 (HierarchicalParsing)
**ASN**: Exhaustion prose and Consequence — "NAT-discrete promotes to `2 ≤ zeros(t)`", "NAT-discrete promotes to `3 ≤ zeros(t)`", and `(A t ∈ T : zeros(t) ≤ 3 : zeros(t) ∈ {0, 1, 2, 3})`
**Issue**: `2` and `3` are used as named values throughout the Exhaustion derivation and in the Consequence without any *Definition* entry introducing them. The depends note for NAT-closure acknowledges the grounding informally (`2 := 1 + 1`, `3 := 2 + 1`) but no formal definition statement appears in any claim's contract. The Consequence as stated, `zeros(t) ∈ {0, 1, 2, 3}`, is not parseable from the axioms alone without knowing that `2` abbreviates `1 + 1` and `3` abbreviates `(1 + 1) + 1`. The derivation steps that invoke NAT-discrete at `m := 1` and `m := 2` implicitly unfold to `1 + 1 ≤ zeros(t)` and `(1 + 1) + 1 ≤ zeros(t)` respectively; the shorthand absorbs the identity rewrites without tracing them.

---

VERDICT: OBSERVE