### Defensive axiom-choice prose appears four times in T4c
**Class**: REVISE
**Foundation**: n/a (internal)
**ASN**: T4c (LevelDetermination). Injectivity body: "The base link `0 < 1` is the distinctness axiom of NAT-closure, cited directly; routing it through NAT-addcompat at `n = 0` would yield only `0 < 0 + 1`, demanding a separate left-identity reduction `0 + 1 = 1` to recover `0 < 1`, which the direct citation avoids." NAT-closure depends entry: "(the alternative — instantiating NAT-addcompat at `n = 0` — would yield `0 < 0 + 1` and require a separate left-identity reduction `0 + 1 = 1`, which the direct citation avoids)." NAT-addcompat depends entry: "(the base link `0 < 1` is taken from NAT-closure's distinctness axiom directly, not from a NAT-addcompat instantiation at `n = 0`)."
**Issue**: The same defensive justification — explaining why NAT-closure's `0 < 1` is cited rather than NAT-addcompat at `n = 0` — appears in the proof body, the NAT-closure depends entry, and the NAT-addcompat depends entry. Depends entries should state what a dependency supplies that the claim uses, not narrate what is not used and why an alternative was rejected. The reader must skip this counterfactual prose to follow either the proof or the dependency surface, and the same content compounds across slots.
**What needs resolving**: The proof should cite `0 < 1` from NAT-closure once, without justifying why it isn't routed through NAT-addcompat. Depends entries should describe what is consumed, not what was rejected.