### T10a.8 Depends missing NAT-order
**Class**: REVISE
**Foundation**: NAT-order (NatStrictTotalOrder) — irreflexivity `¬(n < n)`, the `>` companion definition `m > n ⟺ n < m`
**ASN**: T10a.8 (UniformSiblingZeroCount) — *Formal Contract Depends* section; proof paragraph "NAT-closure gives … NAT-addcompat gives (tₙ)_{sig(tₙ)} < (tₙ)_{sig(tₙ)} + 1; combined with NAT-zero, (tₙ₊₁)_{sig(tₙ)} > 0."
**Issue**: The zero-set equality argument requires that the modified position `sig(tₙ)` is excluded from the zero-index set of `tₙ₊₁`, i.e., `(tₙ₊₁)_{sig(tₙ)} ≠ 0`. The proof establishes `0 < (tₙ)_{sig(tₙ)} + 1` — either via NAT-closure's successor-positivity `(A n ∈ ℕ :: 0 < n + 1)` directly, or via the chain `0 ≤ (tₙ)_{sig(tₙ)} ∧ (tₙ)_{sig(tₙ)} < (tₙ)_{sig(tₙ)} + 1` using NAT-order transitivity. Either route, the step `0 < x ⟹ x ≠ 0` requires NAT-order's irreflexivity: if `x = 0` then `0 < 0`, contradicting `¬(0 < 0)`. The `> 0` notation also invokes NAT-order's `m > n ⟺ n < m` definition. NAT-order does not appear in T10a.8's Depends, leaving this inference ungrounded in the stated dependency chain. By contrast, TA5a case `k = 0` makes the identical argument and explicitly annotates "NAT-order's irreflexivity … lifts `> 0` to `≠ 0`" in its Depends — T10a.8 performs the same logical step without citing the same source.
**What needs resolving**: NAT-order must be added to T10a.8's Depends, annotated to supply the `>` companion definition and irreflexivity `¬(n < n)`, consumed by the step `0 < (tₙ₊₁)_{sig(tₙ)} ⟹ (tₙ₊₁)_{sig(tₙ)} ≠ 0` in the zero-index-set equality argument.

VERDICT: REVISE