### T4 formal contract: `zeros(t) ≤ #t` present in prose, absent from formal contract
**Class**: OBSERVE
**Foundation**: NAT-card upper-bound axiom (`|S| ≤ n` for `S ⊆ {j ∈ ℕ : 1 ≤ j ≤ n}`)
**ASN**: T4 (HierarchicalParsing), zero-count definition: `"so NAT-card applies at n = #t and yields zeros(t) ∈ ℕ"`
**Issue**: The prose establishes `zeros(t) ∈ ℕ` with `zeros(t) ≤ #t`; the formal contract Definition records only `zeros(t) ∈ ℕ`. The bound `zeros(t) ≤ #t` is a direct consequence of NAT-card's upper-bound axiom at `n = #t` and is the fact that makes `zeros(t) ≤ 3` a non-trivial constraint. It appears in the prose but not in the formal contract.

---

VERDICT: REVISE