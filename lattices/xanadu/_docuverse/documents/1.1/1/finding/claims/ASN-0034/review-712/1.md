### T4 Depends annotation for NAT-zero omits `0 ∈ ℕ`
**Class**: REVISE
**Foundation**: NAT-zero (NatZeroMinimum)
**ASN**: T4 (HierarchicalParsing) — *Depends:* entry for NAT-zero
**Issue**: The annotation reads "NAT-zero (NatZeroMinimum) — supplies `(A n ∈ ℕ :: 0 < n ∨ 0 = n)`." NAT-zero's axiom has two parts: `0 ∈ ℕ` and the universal clause. T4 uses the literal `0` as a specific element of ℕ in the zero-count definition (`tᵢ = 0`), the T4-valid predicate (`tᵢ = 0`, `tᵢ₊₁ = 0`, `t₁ ≠ 0`, `t_{#t} ≠ 0`), and the Consequence (`zeros(t) ∈ {0, 1, 2, 3}`). Every other claim in this ASN that uses the literal `0` cites NAT-zero explicitly for `0 ∈ ℕ` (NAT-closure: "supplies `0 ∈ ℕ` for the literal `0` appearing in…"; NAT-card: "supplies `0 ∈ ℕ` for the literal `0` appearing in…"). T4 is the sole exception, leaving the literal `0` in its zero-count definition and T4-valid predicate without a traced source.
**What needs resolving**: The T4 annotation for NAT-zero must add `0 ∈ ℕ` with a citation listing the specific occurrences of the literal `0` in T4's definitions and predicate.

---