### Body-prose dependency walkthrough mirrors the removed NAT-card preamble for the other deps
**Class**: REVISE
**Foundation**: T4 (HierarchicalParsing) — body paragraph immediately before the *Exhaustion* derivation
**ASN**:
> "T0 fixes the carrier as ℕ; NAT-order supplies the strict total order `<` on ℕ and its companion `≤` (defined by `m ≤ n ⟺ m < n ∨ m = n`), which support the zero-count bound `zeros(t) ≤ 3` and the adjacency clause's index range `1 ≤ i < #t`; NAT-closure supplies `1 ∈ ℕ` together with closure of ℕ under addition, grounding the numerals `2 := 1 + 1 ∈ ℕ` and `3 := 2 + 1 ∈ ℕ` so that the bound `zeros(t) ≤ 3` compares two elements of ℕ; and NAT-zero's disjunction `(A n ∈ ℕ :: 0 < n ∨ 0 = n)` instantiated at each field-component position `tᵢ` forces strict positivity..."

**Issue**: The prior cycle flagged the NAT-card preamble for previewing what the dep supplies before any reasoning step uses it. That single-dep preamble was removed, but the same pattern now appears as a four-dep sentence (T0, NAT-order, NAT-closure, NAT-zero), each entry of the form "DEP supplies X, used at site Y in this claim." The application sites already discharge the side conditions where they appear (e.g., `0 < tᵢ` follows from `0 < tᵢ ∨ 0 = tᵢ` and `tᵢ ≠ 0` exactly where the field-segment constraint is unfolded). The walkthrough adds no reasoning the body's local steps don't do — it duplicates the Depends-slot content as prose, and is exactly the "preview of cited content" pattern. The Depends slot has been kept short for these very deps; the body prose then re-inflates them.
**What needs resolving**: Drop the dep-walkthrough sentence. If a single line is needed to fix the disambiguation between `|·|` (NAT-card) and `#·` (T0), keep that. Otherwise let the application sites in the *T4-valid predicate* and the *Exhaustion* derivation cite their deps locally, as they already do.