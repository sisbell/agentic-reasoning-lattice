### Missing NAT-carrier in *Depends:* lists of NAT-closure, NAT-zero, and T0
**Class**: REVISE
**ASN**: NAT-closure axiom asserts `+ : ℕ × ℕ → ℕ` and `1 ∈ ℕ`; *Depends:* lists only NAT-zero (for `0`) and NAT-order (for `<`). NAT-zero axiom asserts `0 ∈ ℕ` and `(A n ∈ ℕ :: 0 < n ∨ 0 = n)`; *Depends:* lists only NAT-order (for `<`). T0 axioms use `ℕ` directly in `#· : T → ℕ`, `aᵢ ∈ ℕ`, `p ∈ ℕ`, and `r : {j ∈ ℕ : 1 ≤ j ≤ p} → ℕ`; *Depends:* lists only NAT-closure (for `1`) and NAT-order (for `≤`).
**Issue**: NAT-order's *Depends:* entry establishes the convention used throughout this ASN: each cited claim is named for the *specific symbol/object it supplies* (NAT-order cites NAT-carrier as supplying `ℕ` as the underlying set, with the use sites enumerated). By that convention, every axiom that mentions `ℕ` directly — as a set, a domain, a codomain, or a quantifier range — must list NAT-carrier. NAT-closure (`ℕ × ℕ`, `ℕ`, `1 ∈ ℕ`), NAT-zero (`0 ∈ ℕ`, `(A n ∈ ℕ :: ...)`), and T0 (`aᵢ ∈ ℕ`, `r : ... → ℕ`, `p ∈ ℕ`, length codomain `ℕ`) all do, yet none cites NAT-carrier. Either the depend lists must be completed, or NAT-order's NAT-carrier dependency must be removed for consistency — but the current state has only one of three sibling axioms acknowledging the carrier it stands on.
**What needs resolving**: Reconcile the dependency convention. If the convention is fine-grained (each used object gets a citation), NAT-closure, NAT-zero, and T0 must add NAT-carrier with use-site descriptions paralleling NAT-order's entry. If the convention is transitive (NAT-carrier reached through NAT-order), then NAT-order's explicit NAT-carrier line is over-specified.