### T3 postcondition drops `∈ ℕ` type annotation on index variable `i`
**Class**: OBSERVE
**Foundation**: T0 (CarrierSetDefinition) — extensionality clause `(A a, b ∈ T : #a = #b ∧ (A i ∈ ℕ : 1 ≤ i ≤ #a : aᵢ = bᵢ) : a = b)`
**ASN**: T3 (CanonicalRepresentation) — Formal Contract *Postcondition:* `a = b ⟺ #a = #b ∧ (A i : 1 ≤ i ≤ #a : aᵢ = bᵢ)`
**Issue**: T3 reformulates T0's extensionality as a biconditional, but the exported postcondition drops the `∈ ℕ` annotation on `i` that T0 carries explicitly. T3's postcondition is the form cited by T1's proof (Case 1 for `a = b`, Cases 2–3 for `a ≠ b`). The domain of the index variable is implicit rather than stated — inconsistent with T0's own precision.

---