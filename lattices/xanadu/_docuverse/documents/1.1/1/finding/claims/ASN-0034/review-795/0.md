### TumblerSub Depends: NAT-discrete missing, needed for sub-case (ii-b) elimination
**Class**: REVISE
**Foundation**: NAT-discrete (NatDiscreteness) — forward direction `m < n ⟹ m + 1 ≤ n`
**ASN**: TumblerSub (TumblerSub) — Precondition proof, Divergence case (ii), sub-case (ii-b): "Sub-case (ii-b) is eliminated: its prefix hypothesis yields `a < w` via T1 case (ii), contradicting `w < a`."
**Issue**: Applying T1 case (ii) to conclude `a < w` requires exhibiting a witness `k` satisfying `k = #a + 1 ≤ #w`. Divergence case (ii-b) supplies `#a < #w`; the step from `#a < #w` to `#a + 1 ≤ #w` is precisely NAT-discrete's forward direction instantiated at `(#a, #w)`. Without this step, the T1(ii) witness is not fully verified. NAT-discrete does not appear in TumblerSub's Depends section, and the proof body does not make the bridge explicit. TA3-strict handles the analogous situation (eliminating T1 case (ii) under `#a = #b`) by explicitly citing NAT-addcompat and showing why the T1(ii) condition fails; TumblerSub's sub-case (ii-b) elimination needs the same level of justification for why it succeeds.
**What needs resolving**: NAT-discrete must be added to TumblerSub's Depends with an annotation that its forward direction `m < n ⟹ m + 1 ≤ n`, instantiated at `(#a, #w)`, bridges Divergence case (ii-b)'s `#a < #w` to the T1(ii) condition `#a + 1 ≤ #w`, completing the exhibition of the T1(ii) witness `k = #a + 1`.

---

**TA3-strict** — Cases A, 1 (both subcases j = d and j > d), 2, and 3 are all correctly handled. The `d_a ≤ #a` and `d_b ≤ #b` padding-zone arguments, the `≥ 1`-to-`> 0` bridges in Cases A and 3, and the trichotomy-disjointness contradiction in Case 2 are all sound. ✓

VERDICT: REVISE