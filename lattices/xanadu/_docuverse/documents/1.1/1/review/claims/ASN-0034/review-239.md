# Cone Review — ASN-0034/T10a.8 (cycle 4)

*2026-04-18 05:08*

### T10a.4 summary inside T10a Formal Contract diverges from standalone's T10a citation
**Foundation**: Previous Finding 3 (cycle 1) required T10a.4's Depends to cite T10a (AllocatorDiscipline), naming the two clauses the proof consumes — root T4-initialization (base case) and the runtime `zeros(t) ≤ 2` check at `k' = 2` (placing child-spawning inside TA5a's preservation envelope). The standalone `**T10a.4 (T4Preservation)**` section's Formal Contract now carries this: its Depends opens with "T10a (AllocatorDiscipline) — supplies two clauses consumed by the proof: (i) the root-initialization constraint... and (ii) the runtime discipline that admits child-spawning `inc(·, k')` with `k' ∈ {1, 2}` and restricts `k' = 2` to inputs `t` with `zeros(t) ≤ 2`...", followed by T4 and TA5a. This matches the citation pattern T10a.1, T10a.3, T10a.5, T10a.7, and T10a.8 use when consuming axiom clauses of T10a.
**ASN**: The summary listing for T10a.4 inside T10a's own Formal Contract Postconditions block (enumerating T10a.1–T10a.8 and T10a-N): "T10a.4 (T4 preservation): The root allocator's base address satisfies T4 (initialization constraint); since `inc(·, 0)` and `inc(·, 1)` unconditionally preserve T4 on any T4-valid input (TA5a) and `inc(·, 2)` preserves T4 whenever `zeros(t) ≤ 2` (the runtime bound the axiom enforces at `k' = 2`), every output of a conforming allocator satisfies T4 by induction on the allocator tree. Depends: T4 (HierarchicalParsing — the invariant preserved by induction), TA5a (inc(·, 0) and inc(·, 1) preserve T4 unconditionally on T4-valid inputs; inc(·, 2) preserves T4 under `zeros(t) ≤ 2`)."
**Issue**: Two listings of T10a.4's Depends now disagree. The summary inside T10a's Formal Contract lists 2 dependencies (T4, TA5a) and explicitly attributes both consumed clauses — "the initialization constraint" and "the runtime bound the axiom enforces at `k' = 2`" — to T10a in its own prose, yet omits T10a from the Depends clause; the standalone T10a.4 lists T10a alongside T4 and TA5a. This is the precise pattern Previous Finding 10 (cycle 4) flagged for T10a.8: a reader consulting the T10a Formal Contract sees a different Depends for T10a.4 than the standalone advertises, and a dependency extractor consuming the summary would miss the T10a edge that the resolved standalone carries (and that cycle 1 Finding 3 already required). The summary's prose names exactly the two T10a clauses being consumed; the Depends clause must do the same.
**What needs resolving**: The T10a.4 entry inside T10a's Formal Contract Postconditions block — specifically its Depends clause — must match the standalone T10a.4 Formal Contract: it must cite T10a (AllocatorDiscipline) with the two clauses it consumes (root T4-initialization for the base case, and the runtime `zeros(t) ≤ 2` discipline at `k' = 2` for the inductive step), so the two Depends listings for this single property are identical, mirroring the cycle-4 alignment now required of T10a.8.
