# Cone Review — ASN-0034/T1 (cycle 4)

*2026-04-17 11:20*

### NAT-order consumption at Case 3's case-(i) reverse-witness rebuttals (both branches) is uncounted

**Foundation**: NAT-order (NatStrictTotalOrder).

**ASN**: T1 (LexicographicOrder), part (b) Trichotomy, Case 3's `m < n` branch: *"No witness for `b < a` exists: case (i) would require `bⱼ < aⱼ` at some position `j` present in both tumblers (`j ≤ m ∧ j ≤ n`), but all such positions have equal components..."* and the mirrored `n < m` branch (*"the mirrored argument gives `b < a` as the unique outcome — case (ii) of any reverse witness for `a < b`..."* — which narratively addresses only the case-(ii) rebuttal but relies on the parallel case-(i) rebuttal being identical).

**Issue**: The rebuttal "case (i) would require `bⱼ < aⱼ`... but all such positions have equal components" is a NAT-order consumption at the component pair `(aⱼ, bⱼ)`: combining the Case 3 hypothesis `aⱼ = bⱼ` with a hypothetical `bⱼ < aⱼ` yields `v < v` (irreflexivity violation) or is ruled out by trichotomy's exactly-one clause once equality is fixed. The enumeration's trichotomy tally itemizes Case 3's opening step and explicitly states that "the same invocation's exactly-one clause additionally underwriting both the mutual exclusivity of the `m < n` and `n < m` branches and the case-(ii) reverse-witness rebuttals within each branch" — the exactly-one coverage is scoped to the length pair `(m, n)` and to the case-(ii) rebuttals only. The case-(i) rebuttal operates at a different pair `(aⱼ, bⱼ)` and cannot be folded into the length-pair invocation. Under the declared per-instance convention, each branch's case-(i) rebuttal instantiates NAT-order at distinct component pairs and should contribute a site per branch (paralleling how Case 2's `k' < k` case-(i) rebuttal is enumerated as a separate trichotomy site at the `(a_{k'}, b_{k'})` pair). Neither irreflexivity (which is "invoked once, at part (a) Case (i)") nor trichotomy (whose ten sites do not include Case 3 case-(i) rebuttals) absorbs these consumptions.

**What needs resolving**: Either enumerate Case 3's case-(i) rebuttal in each branch as additional NAT-order sites (with the preamble total and per-clause count raised accordingly — consistent with the per-instance split applied to addcompat's and transitivity's Case 3 branches), or restructure the rebuttal so the case-(i) failure routes through an already-counted site — for example, by citing T3's forward direction to derive `a ≠ b` first and then unifying both reverse cases under a single contradiction that appeals to the enumerated Case 3 opening invocation. The enumeration must account for every NAT-order fact the proof consumes, with the convention applied uniformly across Cases 2 and 3.
