# Cone Review — ASN-0034/TS5 (cycle 7)

*2026-04-18 14:43*

### TS4 proof body parenthetically re-derives `actionPoint(δ(n, m)) = m` from the structural form while Depends claims it is read off the exported postcondition
**Foundation**: OrdinalDisplacement exports `actionPoint(δ(n, m)) = m` as a postcondition, with the underlying derivation (positions 1..m−1 are zero, position m is `n`, and `n ≥ 1 ⟹ n ≠ 0`) discharged inside OrdinalDisplacement's own proof through NAT-addcompat's `0 < 1` anchor and NAT-order's defining clause / transitivity of `<` / irreflexivity. TS4's Depends entry for ActionPoint states: "The specific value `actionPoint(δ(n, m)) = m` is read off OrdinalDisplacement's exported postcondition rather than re-derived from ActionPoint's defining minimum-position formula inline". TS3 at the analogous site writes "`actionPoint(δ(n₁, m)) ≤ #v` is exactly the `k = m ≤ m = #v` bound discharged at the first shift via OrdinalDisplacement's exported `actionPoint(δ(n₁, m)) = m`" — naming the exported postcondition as the source.
**ASN**: TS4's second precondition check: "*Second precondition: actionPoint(δ(n, m)) ≤ #v.* By OrdinalDisplacement, the action point of δ(n, m) is m (position m is the first nonzero component, since positions 1 through m − 1 are zero and position m is n ≥ 1). Since #v = m, the precondition m ≤ m holds."
**Issue**: The parenthetical "(position m is the first nonzero component, since positions 1 through m − 1 are zero and position m is n ≥ 1)" is an inline re-derivation of `actionPoint(δ(n, m)) = m` from δ's structural form plus the `n ≥ 1 ⟹ n ≠ 0` promotion — the very promotion that OrdinalDisplacement's own proof routes through NAT-addcompat + NAT-order. TS4 neither cites those NAT-* axioms at this step nor restricts itself to the exported `actionPoint(δ(n, m)) = m` postcondition the Depends claims it is reading. The proof body and the Depends narrative are in direct tension: one performs a partial structural re-derivation, the other asserts no such re-derivation occurs.
**What needs resolving**: TS4 must either (a) replace the parenthetical with a direct citation of OrdinalDisplacement's exported `actionPoint(δ(n, m)) = m` postcondition (matching the Depends narrative and TS3's route at the analogous site), or (b) keep the inline derivation and add the NAT-* citations it implicitly invokes (`n ≥ 1 ⟹ n ≠ 0` via NAT-addcompat anchor + NAT-order, plus ActionPoint's defining minimum-position formula) in both proof body and Depends — mirroring the symmetric choice captured in the parallel "TS4 re-derives `Pos(δ(n, m))`" finding for the first precondition.
