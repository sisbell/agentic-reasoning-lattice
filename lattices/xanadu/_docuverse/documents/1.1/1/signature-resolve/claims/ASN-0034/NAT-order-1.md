# Signature Resolve — ASN-0034/NAT-order — run 1

*2026-04-30T20:15:55Z*
*Model: sonnet*

## Output

INTRODUCES:
- bullet: "- `<` — strict total order relation on ℕ; binary relation `< ⊆ ℕ × ℕ` satisfying irreflexivity, transitivity, and at-least-one trichotomy"
- bullet: "- `≤` — non-strict companion order, defined as `m ≤ n ⟺ m < n ∨ m = n`"
- bullet: "- `≥` — reverse non-strict order, defined as `m ≥ n ⟺ n ≤ m`"
- bullet: "- `>` — reverse strict order, defined as `m > n ⟺ n < m`"

REMOVES: []
