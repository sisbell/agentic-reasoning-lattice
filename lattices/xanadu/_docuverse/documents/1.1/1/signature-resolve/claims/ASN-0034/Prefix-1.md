# Signature Resolve — ASN-0034/Prefix — run 1

*2026-04-30T20:18:43Z*
*Model: sonnet*

## Output

INTRODUCES:
- bullet: "- `≼` — prefix relation on tumblers; `p ≼ q` iff `#p ≤ #q` and all components of `p` match those of `q` positionally"
- bullet: "- `≺` — proper prefix relation; `p ≺ q` iff `p ≼ q ∧ p ≠ q`"
- bullet: "- `⋠` — non-prefix relation; `p ⋠ q` iff `¬(p ≼ q)`"

REMOVES: []
