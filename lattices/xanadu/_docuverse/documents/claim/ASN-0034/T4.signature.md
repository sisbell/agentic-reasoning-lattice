- `zeros` — function `zeros : T → ℕ` returning the count of zero-valued components of a tumbler; defined as `zeros(t) = |{i : 1 ≤ i ≤ #t ∧ tᵢ = 0}|`
- `2` — numeral constant in ℕ, defined as `2 := 1 + 1` via NAT-closure
- `3` — numeral constant in ℕ, defined as `3 := 2 + 1` via NAT-closure
