In Gregory's udanax-green implementation, the function tumblerincrement(source, rightshift, bint, dest) takes a bint parameter that controls the increment amount. We know that element-level allocation uses bint=textset->length (inserttextingranf in granf2.c). Question: What bint values are used at each allocation level — servers, accounts, documents, versions, and elements? Find every call site of tumblerincrement across the codebase and report the rightshift and bint values used. We need to know whether bint=1 is the universal case at structural levels, or whether any structural-level allocation also uses bint>1.
