### NAT-sub Consequence meta-justification prose
**Class**: REVISE
**ASN**: NAT-sub (NatPartialSubtraction). Two paragraphs introduce the *Consequences*. The strict-monotonicity paragraph opens: "Strict monotonicity — ... — is exported as a *Consequence:* rather than an additional axiom clause, because its content derives from the right-inverse together with NAT-addcompat's right order compatibility and NAT-order's at-least-one trichotomy with irreflexivity. Retaining it as an axiom clause would launder that derivation through a non-minimal clause, the same concern that kept NAT-order's disjointness form `m < n ⟹ m ≠ n` from being separately exported and left it as a derivable contrapositive of the exactly-one-trichotomy Consequence's `¬(m < n ∧ m = n)` conjunct." The strict-positivity paragraph opens analogously: "Strict positivity — `m > n ⟹ m − n ≥ 1` — is exported as a *Consequence:* rather than an additional axiom clause, because its content is not purely subtractive: lifting `m − n ≠ 0` to `m − n ≥ 1` requires the discreteness fact that no natural number lies strictly between `0` and `1`, which NAT-discrete names." Both *Consequence:* bullets in the Formal Contract repeat the same self-justification ("recorded as a Consequence rather than an axiom clause so the derivation is not laundered through a non-minimal clause"; "recorded as a Consequence rather than an axiom clause because its content is not purely subtractive — lifting `m − n ≠ 0` to `m − n ≥ 1` leans on NAT-discrete's structural commitment to discreteness of ℕ").
**Issue**: This is defensive prose explaining the *classification choice* (Consequence vs axiom) rather than the mathematical content. The cross-reference to NAT-order's disjointness form is essay content — a use-site inventory of categorization decisions in another ASN. A precise reader has to skip past this meta-justification to reach the actual derivation, and then encounters the same justification again in the Formal Contract bullets. The classification of a derivable fact as Consequence is the default; restating it twice with comparative reasoning to other ASN choices is noise.
**What needs resolving**: Remove the comparative-justification sentences ("Retaining it as an axiom clause would launder…"; the cross-reference to NAT-order's disjointness form; "recorded as a Consequence rather than an axiom clause…" tails of the Consequence bullets). Lead with the derivation itself; the choice to record a derivable fact as a Consequence does not require defense in the body of the claim.

VERDICT: REVISE