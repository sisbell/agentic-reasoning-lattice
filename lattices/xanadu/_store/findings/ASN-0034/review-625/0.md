### NAT-carrier body is entirely meta-prose, not content
**Class**: REVISE
**ASN**: NAT-carrier (NatCarrierSet). Body paragraphs: "The declaration is irreducible at this level: `ℕ` is taken as a primitive — not constructed from…"; "Every Cartesian product `ℕ × ℕ` (NAT-order's `< ⊆ ℕ × ℕ`, NAT-closure's `+ : ℕ × ℕ → ℕ`)…presupposes this primitive commitment."; "No further structure on `ℕ` is asserted here. The strict order `<` is introduced by NAT-order…NAT-carrier supplies only the carrier set on which those subsequent commitments are layered."
**Issue**: All three body paragraphs match patterns flagged as reviser drift / noise: paragraph 1 explains *why* the axiom is taken as primitive rather than what `ℕ is a set` says; paragraph 2 is a verbatim use-site inventory of every place ℕ appears in sister axioms; paragraph 3 is a not-here essay listing what is *not* asserted by NAT-carrier. The contentful axiom — "ℕ is a set" — is one line, and nothing in the body advances reasoning a downstream consumer needs.
**What needs resolving**: The body should either be empty (the axiom is self-evident) or carry content about what `ℕ is a set` commits to (e.g. that quantification over `ℕ` is well-defined). Use-site inventories and not-here lists belong in cross-reference apparatus, not the body of an axiom.