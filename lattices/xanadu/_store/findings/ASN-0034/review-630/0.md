### TA-Pos omits NAT-carrier from declared depends
**Class**: REVISE
**Foundation**: TA-Pos (PositiveTumbler)
**ASN**: TA-Pos, *Depends:* lists T0, NAT-zero, NAT-order, NAT-closure — but not NAT-carrier. The Definition reads `(A t ∈ T :: Pos(t) ⟺ (E i ∈ ℕ : 1 ≤ i ≤ #t : ¬(tᵢ = 0)))` and `(A t ∈ T :: Zero(t) ⟺ (A i ∈ ℕ : 1 ≤ i ≤ #t : tᵢ = 0))`.
**Issue**: TA-Pos's own Definition references `ℕ` directly in the bounded quantifiers `(E i ∈ ℕ : ...)` and `(A i ∈ ℕ : ...)` — the carrier-side restriction `i ∈ ℕ` is in the comprehension, not transitively inherited. The same standard the previous cycle applied to NAT-wellorder and ActionPoint applies here: every foundation in this ASN that mentions `ℕ` explicitly (T0, NAT-zero, NAT-closure, NAT-order) lists NAT-carrier in its *Depends:* with prose anchored on the role `ℕ` plays. TA-Pos breaks the pattern. Relying on transitive availability through NAT-order or T0 is the silent-dependency pattern the prior cycle rejected.
**What needs resolving**: TA-Pos's *Depends:* list must explicitly cite NAT-carrier, with prose identifying that NAT-carrier supplies `ℕ` for the bounded existential `(E i ∈ ℕ : 1 ≤ i ≤ #t : ¬(tᵢ = 0))` of the Pos clause and the bounded universal `(A i ∈ ℕ : 1 ≤ i ≤ #t : tᵢ = 0)` of the Zero clause.