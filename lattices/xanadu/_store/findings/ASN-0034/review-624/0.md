### NAT-sub use-site annotation incomplete in T4a Forward Condition (i)
**Class**: OBSERVE
**Foundation**: T4a (SyntacticEquivalence) — Depends entry for NAT-sub
**ASN**: T4a Forward Condition (i): "the interior field segment `t[s_j + 1 .. s_{j+1} - 1] = t[i + 1 .. i]` has no indices, so it is empty." The Depends entry for NAT-sub annotates exactly one use-site: "right-telescoping clause `(m + n) − n = m`, instantiated at `m = #t, n = 1`, reduces the last-segment upper bound `s_{k+1} − 1 = (#t + 1) − 1` to `#t` in ℕ."
**Issue**: The Forward Condition (i) computation `s_{j+1} - 1 = (i + 1) - 1 = i` (after establishing `s_{j+1} = i + 1`) is a second instance of right-telescoping at `(m, n) = (i, 1)`. This use-site is not enumerated in the Depends annotation, which the ASN's convention treats as a complete inventory. The axiom itself (right-telescoping) is in scope, so soundness is unaffected — only the citation accounting is incomplete. (Alternatively, the proof could reframe the empty-interval argument to avoid subtraction altogether by appealing directly to NAT-discrete's no-interval Consequence on `s_j < x < s_{j+1}` with `s_{j+1} = s_j + 1`, which would moot the missing annotation.)

VERDICT: OBSERVE