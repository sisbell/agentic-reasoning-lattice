### "Distinctness" label undersells `0 < 1`
**Class**: OBSERVE
**Foundation**: NAT-closure (NatArithmeticClosureAndIdentity)
**ASN**: Axiom clause `0 < 1` annotated "(distinctness of the two named constants)".
**Issue**: The label "distinctness" suggests `0 ≠ 1`, but `0 < 1` is a strictly stronger claim — it locates `1` strictly above `0` in the order, not merely separates the two constants. Distinctness is a corollary (via NAT-order's exactly-one-trichotomy Consequence). The body prose ("names a second constant in ℕ and locates it in the strict order") is accurate; only the contract annotation underdescribes the claim.

VERDICT: REVISE