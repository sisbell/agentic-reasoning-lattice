### TumblerSub does not export the divergence-point inequality `âₖ > ŵₖ`
**Class**: REVISE
**Foundation**: (n/a — internal)
**ASN**: TumblerSub's Formal Contract Postconditions list only `a ⊖ w ∈ T`, `#(a ⊖ w) = L`, `Pos`/`Zero`, and `actionPoint`. The strict divergence-point inequality "`âₖ > ŵₖ` at `k = zpd(a, w)`" is established only in the prose preamble. TA3's Depends entry, however, cites it as a "precondition consequence":
> "TumblerSub's precondition consequence that when `zpd(x, w)` is defined under `x ≥ w`, `x_{zpd(x,w)} > w_{zpd(x,w)}` holds — invoked in the preamble to Sub-cases B2–B4 …, in Sub-case B3 to contradict `a_{dₐ} < w_{dₐ}`, and in Sub-case B4 applied to `(b, w)` for the strict divergence-point inequality `b_{d_b} > w_{d_b}`"

TA2's proof similarly relies on the inequality inline (e.g. "`âₖ ≥ ŵₖ` from `âₖ > ŵₖ` via NAT-order"), but TA2's contract also omits it.

**Issue**: TA3's argument in B2 (`a_d, b_d ≥ w_d`), B3 (the contradiction `a_{dₐ} > w_{dₐ}` vs `a_{dₐ} < w_{dₐ}`), and B4 (`b_{d_b} > w_{d_b}` for strict positivity) all consume a fact that no contract in scope exports. A downstream reader of TA3's contract who consults TumblerSub's contract will not find the cited consequence among the postconditions; it lives only in TumblerSub's body. The proof is correct in spirit, but the dependency chain is broken at the contract boundary.

A related symptom: TA3 writes `a_{dₐ}`, `w_{dₐ}`, `b_{d_b}`, `w_{d_b}` as if native, but `dₐ` may exceed `#w` (prefix-divergence sub-case), in which case `w_{dₐ}` is undefined and the inequality lives on padded projections (`â_{dₐ} > 0 = ŵ_{dₐ}`). The contract-level fact one would want to export is on padded projections.

**What needs resolving**: Either add the divergence-point inequality (on padded projections) as an exported Postcondition of TumblerSub — e.g. "when `zpd(a, w)` is defined, `â_{zpd(a,w)} > ŵ_{zpd(a,w)}`" — and propagate that wording in TA3's invocations; or have TA3 derive the inequality from facts already in TumblerSub's exported contract. Whichever route is taken, TA3's notation should distinguish padded from native components at indices that may lie in a padding zone.

VERDICT: REVISE