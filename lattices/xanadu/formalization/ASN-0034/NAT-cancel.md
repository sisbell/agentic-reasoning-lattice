**NAT-cancel (NatAdditionCancellation).** Addition on ℕ is cancellative on either side, and a sum equals one of its summands only when the other is zero.

Four related facts are stated together as one axiom because they all extract constraints on a summand from an identity between sums:

- Left cancellation: `m + n = m + p ⟹ n = p` for every `m, n, p ∈ ℕ`.
- Right cancellation: `n + m = p + m ⟹ n = p` for every `m, n, p ∈ ℕ`.
- Summand absorption, standard form: `m + n = m ⟹ n = 0` for every `m, n ∈ ℕ`. When the candidate-zero summand `n` appears on the right of `m`, the equality `m + n = m` forces `n = 0`.
- Summand absorption, symmetric form: `n + m = m ⟹ n = 0` for every `m, n ∈ ℕ`. When the candidate-zero summand `n` appears on the left of `m`, the equality `n + m = m` forces `n = 0`.

Both summand-absorption forms are stated as independent axiom clauses because T0's exhaustive NAT-* enumeration does not include commutativity of addition on ℕ, so neither form is derivable from the other within this axiom system. The bundling parallels NAT-addcompat, whose left and right order-compatibility clauses are likewise stated independently because the two cannot be collapsed in the absence of commutativity — "summand absorption" is understood here to cover both placements of the zero-forced summand, just as "order compatibility of addition on either side" covers both placements of the fixed summand.

These are standard properties of ℕ, stated here as an axiom so downstream proofs can cite them directly. The ASN's convention (T0) is that each proof cites only the ℕ facts it actually uses; within this ASN, right cancellation is invoked at four distinct sites. T1's part (c) Transitivity sub-case (ii, ii) — where T1 case (ii) supplies `k = m + 1 ≤ n` from `a < b` and `k = n + 1 ≤ p` from `b < c`, folding into the length-level equality `m + 1 = n + 1` — invokes right cancellation at the fixed summand `1` to pass from `m + 1 = n + 1` to `m = n`; this successor-injectivity step produces the equality that NAT-order's trichotomy at the length pair `(m, n)` refutes against the strict `m < n` derived separately in the same sub-case from NAT-addcompat's strict successor inequality composed with the unpacked `m + 1 ≤ n`. GlobalUniqueness Case 5's length-collision argument invokes right cancellation three times, at three distinct fixed summands supplied by the argument's structure. *Site (a):* in sub-case `k'₁ = k'₂`, cancelling the common `k'₁` from `#p₁ + k'₁ = #p₂ + k'₁` (the assumed equality `#p₁ + k'₁ = #p₂ + k'₂` with `k'₂` substituted to `k'₁`) yields `#p₁ = #p₂`, contradicting the Prefix-supplied `#p₁ < #p₂` via NAT-order's trichotomy. *Site (b):* in sub-case `k'₁ < k'₂`, cancelling the common `k'₂` rules out the equality disjunct `#p₁ + k'₂ = #p₂ + k'₂` — the equality would yield `#p₁ = #p₂`, contradicting `#p₁ < #p₂` via NAT-order's trichotomy — sharpening the non-strict `≤` that NAT-addcompat's right order compatibility supplied into the strict `<` that the chaining step consumes; this is the right-cancellation leg of the four-step strict-promotion chain enumerated in NAT-addcompat. *Site (c):* after the first two sub-cases force `(k'₁, k'₂) = (2, 1)` and NAT-addassoc's associativity rewrites `#p₁ + 2` as `(#p₁ + 1) + 1`, cancelling the trailing `+1` from `(#p₁ + 1) + 1 = #p₂ + 1` yields `#p₂ = #p₁ + 1`, the length-extension identity that underwrites the subsequent zero-count argument — that the single extending position at index `#p₂` contributes no zero by T4's clause (iii) applied via T10a.4's T4-validity of `p₂`, whence `zeros(p₂) = zeros(p₁)`. All four in-cone sites instantiate right cancellation at a common trailing summand; left cancellation and both summand-absorption forms have no in-cone consumer within this ASN. Their inclusion in the axiom is structural: it matches the left/right symmetry of NAT-addcompat's order-compatibility clauses so that every placement of the cancelling or zero-forced summand is stated as an axiom clause rather than routed through an unstated appeal to commutativity of addition on ℕ (which T0's exhaustive NAT-* enumeration does not admit). Without an explicit axiom the four cited sites would appeal to background arithmetic, breaking the citation policy that the other NAT-* axioms enforce.

*Formal Contract:*
- *Axiom:* `(A m, n, p ∈ ℕ : m + n = m + p : n = p)` (left cancellation); `(A m, n, p ∈ ℕ : n + m = p + m : n = p)` (right cancellation); `(A m, n ∈ ℕ : m + n = m : n = 0)` (summand absorption, standard form); `(A m, n ∈ ℕ : n + m = m : n = 0)` (summand absorption, symmetric form).
