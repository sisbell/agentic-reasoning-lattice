# Cone Review — ASN-0034/T1 (cycle 2)

*2026-04-17 09:20*

### T1 Depends undercounts NAT-addcompat use sites in part (b)'s reverse-witness rebuttals
**Foundation**: N/A (foundation ASN — internal consistency)
**ASN**: T1 (LexicographicOrder), part (b) Case 2, quoted: *"If `k' = k`, case (i) requires the opposite inequality at position `k`, contradicting NAT-order's trichotomy, and case (ii) requires `k = n + 1` (or `k = m + 1`), contradicting `k ≤ min(m, n) ≤ n`. If `k' < k`, the minimality of `k` gives `a_{k'} = b_{k'}`, so case (i) fails on equal components and case (ii) requires `k' = n + 1` (or `m + 1`), but `k' < k ≤ min(m, n)` gives `k' < n` and `k' < m`, contradicting both."* And part (c) Case `k₁ = k₂ = k` sub-case (i, ii): *"`aₖ < bₖ` with `k ≤ min(m, n)` requires `k ≤ n`, while `k = n + 1 ≤ p` requires `k > n`. The conjunction `k ≤ n` and `k > n` is a contradiction."*
**Issue**: The T1 Depends list enumerates NAT-addcompat's strict successor inequality at exactly "two distinct sites": part (a) Case (ii) and part (c) Case `k₂ < k₁`. But the refutations above each substitute `k = n + 1` (or `k' = n + 1`) into a hypothesis `k ≤ n` (or `k' < n`) and derive the contradiction `n + 1 ≤ n` (resp. `n + 1 < n`). Ruling out `n + 1 ≤ n` requires the strict successor inequality `n < n + 1` — NAT-order alone (trichotomy, transitivity, irreflexivity) does not relate `n` to `n + 1`, and NAT-discrete's citation list similarly omits these sites. The sub-case (i, ii) conversion of "`k = n + 1`" into "`k > n`" has the same structure. At T0's citation convention ("each proof cites only the ℕ facts it actually uses"), the Depends list and the proof text disagree on how many times NAT-addcompat is consumed: the prose says "at two distinct sites" while the proof has at least three additional uses in the rebuttal machinery (part (b) Case 2 at `k' = k` via case (ii), part (b) Case 2 at `k' < k` via case (ii), and part (c) sub-case (i, ii)).
**What needs resolving**: Either extend T1's NAT-addcompat citation enumeration to include part (b) Case 2's two reverse-witness rebuttals and part (c) sub-case (i, ii), or rephrase those rebuttals so they route through a site already in the enumeration (for instance, by deriving the contradiction via NAT-discrete's `m + 1 ≤ n ⟺ m < n` equivalence, which would shift the citation bookkeeping to NAT-discrete but would itself require that the NAT-discrete enumeration be extended to cover those sites). The same audit should reconfirm NAT-order's "four distinct sites" count against the uses at these rebuttals.
