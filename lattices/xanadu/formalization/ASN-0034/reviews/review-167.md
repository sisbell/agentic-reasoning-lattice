# Cone Review — ASN-0034/TA6 (cycle 1)

*2026-04-17 14:45*

### Subspace constraint stated in body prose but absent from T4's formal contract
**Foundation**: T4 (HierarchicalParsing), Formal Contract *Axiom* and canonical-form clause.
**ASN**: T4 body prose — "**Element field** `E₁. ... .Eδ`: identifies the content element. The first component distinguishes the *subspace*: 1 for text content, 2 for links." The Formal Contract's canonical form says only "where each `Nᵢ, Uⱼ, Dₖ, Eₗ > 0`"; no clause restricts `E₁ ∈ {1, 2}`.
**Issue**: The body asserts a constraint on the first element-field component (subspace identifier ∈ {1, 2}) as though it were part of the specification, but the Formal Contract's Axiom enumerates only `zeros(t) ≤ 3`, no-adjacent-zeros, `t₁ ≠ 0`, `t_{#t} ≠ 0`, plus the definitional separator-zero biconditional and the canonical written form. The canonical form constrains `Eₗ > 0` but admits every positive value, including `3, 4, 5, ...`. Any downstream ASN that cites T4 as the justification for "subspace is 1 or 2" finds no such clause in T4's exported statement, breaking the citation chain before it begins. The memory entry `subspace-identifier-error.md` already flags multiple ASNs misreading the subspace position; silently leaving the admissible value set unpinned in the foundation invites the same error in reverse (downstream ASNs assuming a constraint the foundation does not deliver).
**What needs resolving**: Either (a) incorporate the subspace-value constraint into T4's Formal Contract — as an additional axiom clause or as a dedicated postcondition (possibly a fourth consequence alongside T4a/b/c) — so citations to T4 for `E₁ ∈ {1, 2}` land on exported content, or (b) remove the subspace sentence from the body prose and relocate it to whichever ASN formalises subspace identification, so T4's body and contract agree on scope.

### TA-Pos preamble cites T3 but Depends omits it; parenthetical describes a "T1 → T3 jump in this property's citations" that does not appear in the formal contract
**Foundation**: TA-Pos (PositiveTumbler), Formal Contract *Depends* list.
**ASN**: TA-Pos preamble — "Under T3, the tumblers `[0]`, `[0, 0]`, `[0, 0, 0]`, etc., are *distinct* elements of T — they have different lengths. … (T2 (IntrinsicComparison), which sits between T1 and T3 in this ASN's numbering, is stated elsewhere in this ASN and is not invoked here; the T1 → T3 jump in this property's citations reflects TA-Pos's proof needs, not a numbering gap.)" TA-Pos Formal Contract *Depends*: T0, NAT-wellorder, NAT-zero, NAT-discrete, NAT-order, T1 — no T3.
**Issue**: The parenthetical asserts a "T1 → T3 jump in this property's citations," but TA-Pos's Depends list contains no T3 citation — the only jump visible in the formal contract is T1 → (nothing-numbered-below-TA-Pos), and T3 is absent entirely. Either (i) the preamble's "Under T3, … are distinct elements of T" is load-bearing motivation for the definitional framework ("there are infinitely many all-zero tumblers") and T3 belongs in Depends, or (ii) the T3 reference is decorative and the parenthetical's "T1 → T3 jump in this property's citations" describes a jump that does not exist. A reader trying to verify the citation chain can reconcile neither reading against the Formal Contract as given. The mismatch is self-contained inside TA-Pos — preamble prose and Depends list disagree about whether T3 is cited.
**What needs resolving**: Decide whether the preamble's appeal to T3 is part of TA-Pos's specification claims. If yes, add T3 (CanonicalRepresentation) to Depends with a note on which step it licenses (distinctness of zero tumblers of different lengths). If no, remove the "Under T3" invocation (and rephrase the parenthetical, which presupposes the T3 citation exists) so the preamble and Formal Contract agree on what TA-Pos cites.
