**T0 (CarrierSetDefinition).** `T` is the set of nonempty finite sequences over ℕ, written `d₁.d₂. ... .dₙ` with each `dᵢ ∈ ℕ`. For each `a ∈ T`, write `#a` for the length of `a` and `aᵢ` for the value of the `i`-th component of `a`. The component positions of `a` form the index domain `{1, …, #a} ⊆ ℕ`, and `aᵢ ∈ ℕ` at each `i ∈ {1, …, #a}`.

*Formal Contract:*
- *Axiom:* T is the set of all nonempty finite sequences over ℕ, equipped with length `#· : T → ℕ` and component projection `·ᵢ` whose index domain for each `a ∈ T` is `{1, …, #a} ⊆ ℕ`, with `aᵢ ∈ ℕ` at each `i ∈ {1, …, #a}`.
