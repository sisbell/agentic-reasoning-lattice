label: T10a.1
name: UniformSiblingLength
type: corollary
depends:
- T10a
- TA5
summary: All siblings produced by a single allocator share the same tumbler length as the base address. Because sibling production
  uses only inc(·, 0), which preserves length by TA5, the entire sibling stream is length-uniform.
