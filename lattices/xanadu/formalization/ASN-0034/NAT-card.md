**NAT-card (NatFiniteSetCardinality).** For every `n ∈ ℕ` and every subset `S ⊆ {1, 2, …, n} ⊆ ℕ`, the cardinality `|S| ∈ ℕ` is the unique `k ∈ ℕ` such that `S` admits a strictly increasing enumeration `s₁ < s₂ < … < s_k` under `<` on ℕ with `S = {s₁, s₂, …, s_k}` (with `k = 0` and the empty enumeration when `S = ∅`, so `|∅| = 0`). The length `k` is uniquely determined by `S` because two strictly increasing enumerations of the same set must agree element-by-element under NAT-order's trichotomy; hence `|·|` is a well-defined total function on subsets of every initial segment `{1, …, n}` of ℕ with codomain ℕ, and `|S| ≤ n` for every such `S`. The operator `|·|` is distinct from T0's tumbler-length `#· : T → ℕ`, which acts on sequences rather than sets.

The axiom body invokes symbols beyond ℕ's primitive membership. The strict order `<` appears in the strictly increasing enumeration `s₁ < s₂ < … < s_k` and the non-strict companion `≤` in the upper bound `|S| ≤ n` — both defined in NAT-order — and NAT-order's trichotomy `m < n ∨ m = n ∨ n < m` is appealed to when forcing two strictly increasing enumerations of the same set to agree element-by-element, securing uniqueness of `k`. The initial segment `{1, 2, …, n}` is built from the `1 ∈ ℕ` posited by NAT-closure and the successor operation closed over ℕ by NAT-closure. The literal `0` appearing in the empty-enumeration case `k = 0` and the consequent `|∅| = 0` is the `0 ∈ ℕ` posited by NAT-zero. The disambiguation remark referencing T0's tumbler-length `#· : T → ℕ` pins `|·|` as a distinct operator. All four foundations are declared in the Depends slot so that the axiom body can be read without silently importing them.

*Formal Contract:*
- *Axiom:* `(A n ∈ ℕ : S ⊆ {1, 2, …, n} :: |S| ∈ ℕ)` (conditional codomain); `|S|` is the unique `k ∈ ℕ` such that `S` admits a strictly increasing enumeration `s₁ < s₂ < … < s_k` under `<` on ℕ with `S = {s₁, s₂, …, s_k}` (empty enumeration when `k = 0`, giving `|∅| = 0`) (enumeration characterisation); `(A n ∈ ℕ : S ⊆ {1, 2, …, n} :: |S| ≤ n)` (upper bound). `|·|` acts on subsets of ℕ and is distinct from T0's tumbler-length `#·`, which acts on sequences.
- *Depends:*
  - NAT-order (NatStrictTotalOrder) — supplies the strict order `<` (used in the strictly increasing enumeration `s₁ < s₂ < … < s_k`), the non-strict companion `≤` (used in the upper bound `|S| ≤ n`), and the trichotomy `m < n ∨ m = n ∨ n < m` (used to force two strictly increasing enumerations of the same set to agree element-by-element, securing uniqueness of `k`).
  - NAT-closure (NatArithmeticClosureAndIdentity) — posits `1 ∈ ℕ` and closes ℕ under addition, so the initial segment `{1, 2, …, n}` — built from `1 ∈ ℕ` and successor — has ℕ-typed elements and `S ⊆ {1, 2, …, n} ⊆ ℕ` is grounded.
  - NAT-zero (NatZeroMinimum) — supplies `0 ∈ ℕ` for the literal `0` appearing in the empty-enumeration case `k = 0` and in the empty-set cardinality `|∅| = 0`.
  - T0 (CarrierSetDefinition) — supplies the tumbler-length operator `#· : T → ℕ` referenced in the disambiguation remark distinguishing `|·|` from `#·`.
