label: T3
name: SequenceEqualityIsComponentwise
type: axiom
depends:
- T0
summary: Tumbler equality is exactly component-wise sequence equality — two tumblers are equal if and only if they have the
  same length and identical values at every position. No normalization, quotient, or external identification is imposed; the
  raw component sequences must be literally identical.
