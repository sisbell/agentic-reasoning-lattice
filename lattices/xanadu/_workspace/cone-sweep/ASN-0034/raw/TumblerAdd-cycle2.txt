Reading the ASN as a system — tracing every claim boundary, every precondition chain, every case partition.

**Dependency graph.** The Depends DAG is acyclic: NAT-carrier feeds everything; NAT-zero → NAT-order; NAT-closure → {NAT-zero, NAT-order}; NAT-cancel → NAT-closure; NAT-addcompat → {NAT-order, NAT-closure}; NAT-addbound → NAT-addcompat; NAT-discrete → {NAT-order, NAT-closure}; NAT-sub → {NAT-addbound, NAT-addcompat, NAT-discrete, NAT-zero}; NAT-wellorder → NAT-order; T0 → {NAT-carrier, NAT-closure, NAT-order}; T3 → T0; T1 → {T0, T3, NAT-order, NAT-closure, NAT-addcompat, NAT-cancel, NAT-discrete, NAT-wellorder}; TA-Pos → {T0, NAT-carrier, NAT-zero, NAT-order, NAT-closure}; ActionPoint → {T0, TA-Pos, NAT-wellorder, NAT-zero, NAT-order, NAT-discrete, NAT-closure}; TumblerAdd → everything downstream. No cycle found.

**T1 irreflexivity.** Case (i) contradicts NAT-order irreflexivity directly. Case (ii) produces `m+1 ≤ m`; NAT-addcompat's `m < m+1` combined with exactly-one trichotomy rules out both `m+1 < m` and `m+1 = m`, exhausting the disjunction. Sound.

**T1 trichotomy.** The three divergence-position shapes (α)/(β)/(γ) are pairwise mutually exclusive — a position cannot satisfy two simultaneously without forcing `m+1 ≤ m` or `n+1 ≤ n`. NAT-wellorder on the nonempty set of divergence positions delivers the first. Case 1 (no divergence) reaches `a = b` via NAT-discrete's contrapositive plus NAT-order's trichotomy at (m,n) then T3. Cases 2 and 3 each produce exactly one of the three relations and argue the reverse direction cannot be witnessed — the arguments for k' < k, k' = k, and k < k' are each walked. The (γ)-impossible sub-case in the "m < n" branch (the subject of a prior declined finding) now correctly states the derivation using `n < n+1`.

**T1 transitivity.** All four sub-case pairings (i,i)/(ii,i)/(i,ii)/(ii,ii) are covered. Sub-case (i,ii) correctly terminates in contradiction: `k ≤ #b = n` from T1(i) and `k = n+1` from T1(ii) yield `n+1 ≤ n`, impossible. Sub-case (ii,ii) derives `m = n` via NAT-cancel then contradicts `m < n` forced by `m+1 ≤ n`. Agreement conditions at the witness position are properly established in each case.

**ActionPoint.** Uniqueness proof distributes the four pairings from `m₁ ≤ m₂ ∧ m₂ ≤ m₁` correctly, each terminated by NAT-order irreflexivity via transitivity or direct substitution. The zero-below-action-point postcondition argues by contradiction: if `wᵢ ≠ 0` for `i < actionPoint(w)`, then `i ∈ S` (establishing `i ≤ #w` by chaining `i < k ≤ #w` via NAT-order), giving `actionPoint(w) ≤ i` against `i < actionPoint(w)`. The `1 ≤ w_{actionPoint(w)}` postcondition walks correctly through NAT-zero → `0 < w_{actionPoint(w)}` → NAT-discrete → `0+1 ≤ w_{actionPoint(w)}` → NAT-closure identity → `1 ≤ w_{actionPoint(w)}`.

**TumblerAdd.** Result-length identity: `(k-1)+1 = k` via NAT-sub right-inverse at (k,1) with precondition `k ≥ 1`; `k+(n-k) = n` via NAT-sub left-inverse at (n,k) with precondition `n ≥ k`. Component typing is complete: `aᵢ ∈ ℕ` for `i < k` (from T0, using `i < k ≤ #a`); `aₖ + wₖ ∈ ℕ` (NAT-closure); `wᵢ ∈ ℕ` for `i > k` (from T0 on `w`). T0's comprehension closes membership. Strict advancement derives `aₖ < aₖ + wₖ` from `wₖ ≥ 1` via left order-compatibility and successor inequality, exhausting the `≤`-disjunction by transitivity in the strict branch and substitution in the equality branch.

Dominance (`a ⊕ w ≥ w`): the two main cases (some `aⱼ > 0` for `j < k`; all `aᵢ = 0` for `i < k`) partition exhaustively and are mutually exclusive. Within Case 2, the sub-case split on `aₖ > 0 ∨ aₖ = 0` is exhaustive by NAT-zero. The `aₖ > 0` branch applies right order-compatibility to lift `0 ≤ aₖ`, obtains `aₖ + wₖ ≥ wₖ`, then rules out equality by NAT-cancel's mirror-form summand absorption, leaving `aₖ + wₖ > wₖ`. The `aₖ = 0` branch collapses `rₖ = 0 + wₖ = wₖ` via NAT-closure left identity, finds all components agree, and concludes `r = w` by T3. The three leaf conclusions (`r > w`, `r > w`, `r = w`) unite to `r ≥ w`.

**NAT-sub strict monotonicity and positivity.** Both derivations are fully cased. The `b < a` branch of monotonicity correctly applies right order-compatibility at `b ≤ a` to get `b + p ≤ a + p`, then dispatches both disjuncts against existing inequalities. Strict positivity walks `m > n → n < m → n ≤ m → m ≥ n → conditional closure → m−n ≠ 0 → 0 < m−n → NAT-discrete → 1 ≤ m−n`.

**NAT-addbound.** Both dominance derivations (right and left) are complete four-step chains: NAT-zero gives `0 ≤ operand`; NAT-addcompat lifts through addition; NAT-closure identity simplifies; NAT-order converts to `≥`. No step is skipped.

**T3.** Forward direction is T0's extensionality axiom directly. Reverse direction is Leibniz's law applied to `#·` and `·ᵢ`. Forward reference from T1 is non-circular (T3 depends only on T0, which does not depend on T1).

**TA-Pos.** Complementarity follows by DeMorgan duality on bounded quantifiers; the existential and universal share the same domain and matrix (one is the negation of the other). T0's nonemptiness clause ensures neither quantifier is vacuous.

VERDICT: CONVERGED