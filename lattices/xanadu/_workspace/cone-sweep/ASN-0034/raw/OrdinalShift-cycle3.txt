The proofs walk their cases. I read every derivation step-by-step.

**T0 through TA-Pos.** Carrier set, length, component projection, nonemptiness, and the Pos/Zero partition are all internally consistent. The DeMorgan duality argument for complementarity is vacuousness-safe: T0's nonemptiness clause `(A a ∈ T :: 1 ≤ #a)` ensures neither quantifier is vacuous.

**ActionPoint.** The uniqueness argument discharges all four pairings of the antisymmetry case split using only irreflexivity and transitivity. The subsidiary lemma that `wᵢ = 0` for `i < actionPoint(w)` is correctly argued by contradiction: the `i ≤ #w` step is properly grounded by chaining `i < actionPoint(w) ≤ #w` via NAT-order transitivity. The `1 ≤ w_{actionPoint(w)}` step correctly routes through NAT-zero's disjunction, NAT-discrete's forward direction, and NAT-closure's additive identity.

**TumblerAdd.** Result-length identity: `(k−1)+1+( n−k) = n` collapses correctly via NAT-sub right- and left-inverse at `(k,1)` and `(n,k)` respectively. Strict advancement: the case split on `aₖ + wₖ ≤ aₖ + 1` and `aₖ + 1 > aₖ` discharges cleanly, and T1(i) is correctly invoked at divergence position `k` with `k ≤ #a` (precondition) and `k ≤ #(a ⊕ w) = n` (ActionPoint). Dominance: the two-level case split — (1) some `aⱼ > 0` for `j < k` and (2) all `aᵢ = 0` for `i < k` with sub-split on `aₖ` — is exhaustive by NAT-zero. NAT-wellorder is correctly invoked only in the nonempty-witness branch. The `k = 1` degenerate (vacuous prefix) falls into Case 2 without issue. The equality sub-branch (`aₖ = 0`) concludes `r = w` via T3 with correct component-wise agreement check.

**T1.** Irreflexivity: both sub-cases (aₖ < aₖ from NAT-order, m+1 ≤ m from NAT-addcompat + trichotomy) are properly contradicted. Trichotomy: all three cases — no divergence, clause-α divergence, clause-β/γ divergence — are established. The impossible-sub-case arguments (γ under `m < n`, β under `n < m`) correctly use NAT-addcompat's `n < n+1` and NAT-addcompat's `m < m+1` respectively, with the ≤-expansion → transitivity → contradiction chain in each branch. Transitivity: the four sub-cases of `k₁ = k₂ = k` are each discharged — (i,i) via NAT-order transitivity, (ii,i) directly, (i,ii) contradicts since `k ≤ n` and `k = n+1` give `n+1 ≤ n`, (ii,ii) contradicts via NAT-cancel right cancellation at 1.

**NAT-sub.** Strict monotonicity correctly abbreviates `m−p = a`, `n−p = b` and dispatches trichotomy's three branches. Strict positivity correctly routes `m−n = 0` through left identity to `n = m`, then applies `¬(n < m ∧ n = m)` to reach contradiction.

**OrdinalDisplacement, OrdinalShift.** Preconditions discharge correctly. The `n ≥ 1 ⟹ n ≠ 0` promotion is correctly grounded through NAT-closure's `0 < 1` consequence, NAT-order's ≤ unfolding, and exactly-one trichotomy.

---

### OrdinalShift Depends: NAT-zero annotation claims `0 ≤ n` directly but NAT-zero exports `0 < n ∨ 0 = n`
**Class**: OBSERVE
**Foundation**: NAT-zero (NatZeroMinimum) — Axiom: `(A n ∈ ℕ :: 0 < n ∨ 0 = n)`
**ASN**: OrdinalShift (OrdinalShift) — Formal Contract *Depends:* section, NAT-zero entry: "NAT-zero (NatZeroMinimum) — `(∀ n ∈ ℕ :: 0 ≤ n)` supplies `0 ≤ vₘ`"
**Issue**: NAT-zero's formal contract exports `(A n ∈ ℕ :: 0 < n ∨ 0 = n)`, not `(∀ n ∈ ℕ :: 0 ≤ n)`. Converting `0 < n ∨ 0 = n` to `0 ≤ n` requires invoking NAT-order's ≤ definition `m ≤ n ⟺ m < n ∨ m = n`. The annotation collapses this step and attributes the ≤ form to NAT-zero alone. The derivation is complete since NAT-order is also in OrdinalShift's Depends, but the attribution is inconsistent with the annotation style used elsewhere — NAT-addbound's derivation, for instance, explicitly chains "NAT-zero delivers `0 < m ∨ 0 = m`; NAT-order's ≤-definition rewrites this as `0 ≤ m`."

VERDICT: OBSERVE