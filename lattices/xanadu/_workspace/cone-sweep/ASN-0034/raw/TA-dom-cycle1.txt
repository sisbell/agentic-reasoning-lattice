Reading the full ASN as a system, tracing every dependency chain, walking every case analysis, and checking that no definition drifts across claim boundaries.

---

**NAT-* foundation layer.** NAT-carrier through NAT-addbound form a consistent chain. NAT-order's exactly-one trichotomy is correctly derived from the at-least-one axiom plus irreflexivity via indiscernibility-of-equality substitution in all three exclusion cases. NAT-zero's consequence `¬(n < 0)` is correctly derived. NAT-closure's `0 < 1` consequence walks correctly through successor-positivity and left-identity. NAT-discrete's no-interval consequence discharges both branches. NAT-addcompat's strict successor inequality is used correctly throughout. NAT-cancel's mirror summand-absorption form is legitimately derived and correctly cited in TumblerAdd. NAT-sub's strict-monotonicity and strict-positivity derivations walk their trichotomy cases; the `b < a` sub-case in monotonicity correctly appeals to right order-compatibility then discharges both `<` and `=` branches against irreflexivity and exactly-one trichotomy. NAT-addbound's right- and left-dominance proofs both correctly instantiate NAT-zero's minimality clause and the corresponding addcompat arm. No ungrounded symbols, no circular dependencies.

**T0 → TA-Pos → ActionPoint chain.** T0's extensionality and comprehension axioms are correctly cited. TA-Pos's DeMorgan complementarity argument is sound; T0's nonemptiness clause correctly licenses non-vacuous quantifiers. ActionPoint's existence argument via NAT-wellorder on S is sound. The uniqueness case analysis correctly distributes the conjunction of two ≤-hypotheses into four disjunct pairings and discharges each via irreflexivity and transitivity. The zeros-below-action-point argument correctly derives `i ≤ #w` from `i < k ≤ #w` and dispatches the `actionPoint(w) ≤ i` disjunction to contradiction in both branches. The `1 ≤ w_{actionPoint(w)}` derivation correctly chains NAT-zero → NAT-discrete → NAT-closure's left identity. All postconditions are established; all downstream consumers (TumblerAdd's three uses of ActionPoint's exported facts) find what they cite.

**T3 (CanonicalRepresentation).** Correctly packages T0's extensionality clause (forward direction) and Leibniz's law (reverse direction). No independent proof burden.

**T1 (LexicographicOrder).** Irreflexivity: both cases (i) and (ii) correctly reach contradiction — (i) via NAT-order irreflexivity, (ii) via NAT-addcompat's `m < m+1` against exactly-one trichotomy. Trichotomy: the three-case split on divergence positions is exhaustive; Case 1 correctly applies NAT-discrete contrapositive twice to force `m = n`, then T3. Case 2's no-reverse-witness argument exhausts k' < k, k' = k, k' > k with each sub-case dispatched. Case 3's m ≠ n argument in both clauses (β) and (γ) correctly expands ≤ into a two-branch argument reaching `m < n` or `n < m`; the (γ)-impossible and (β)-impossible sub-cases (in the two `m < n` / `n < m` paragraphs) correctly use NAT-addcompat at the relevant `n < n+1` instantiation — not the wrong `m < m+1` — consistent with the earlier declined finding's confirmation that this was already correct in the file. Transitivity: all three k-relationship cases and all four (i,i)/(ii,i)/(i,ii)/(ii,ii) sub-cases are walked; (i,ii) correctly derives `n+1 ≤ n` from `k ≤ n` and `k = n+1` and terminates in contradiction; (ii,ii) correctly applies NAT-cancel to `m+1 = n+1 ⟹ m = n`, then derives `m < n` from `m+1 ≤ n`, reaching a contradiction.

**TumblerAdd.** Result-length identity correctly applies NAT-sub's right-inverse at `(k,1)` and left-inverse at `(k,n)`, with both preconditions discharged by ActionPoint's `1 ≤ k ≤ n`. Each component is in ℕ; T0's comprehension clause is correctly instantiated. Strict advancement: the two-branch unfold of `aₖ+1 ≤ aₖ+wₖ` (from left-order-compatibility) correctly yields `aₖ < aₖ+wₖ` in both branches; T1(i) at k is correctly invoked with all conditions checked. Dominance: the outer case split is exhaustive; in the first case NAT-wellorder is legitimately applied to a nonempty ℕ-subset; the j-to-k agreement chain is correct; in the second case both sub-cases discharge correctly — the `aₖ > 0` branch rules out equality via NAT-cancel's mirror absorption and concludes `rₖ > wₖ` via NAT-order, the `aₖ = 0` branch applies NAT-closure's left identity to get `rₖ = wₖ`, then T3 closes the equality case. The merge step correctly combines `r > w` and `r = w` into `r ≥ w` via T1's `≥`-abbreviation.

**TA0, TA-dom.** Both correctly package the relevant TumblerAdd postconditions under the same preconditions.

---

### TA-dom precondition introduces unexplained local name

**Class**: OBSERVE
**Foundation**: ActionPoint (ActionPoint) — postcondition `1 ≤ actionPoint(w) ≤ #w`; TumblerAdd (TumblerAdd) — precondition `actionPoint(w) ≤ #a`; TA0 (WellDefinedAddition) — precondition `actionPoint(w) ≤ #a`
**ASN**: TA-dom (DisplacementDominance) — Formal Contract *Preconditions:* `k ≤ #a` where `k` is the action point of `w`
**Issue**: Every other claim that imposes this bound — TumblerAdd and TA0 — writes `actionPoint(w) ≤ #a` directly. TA-dom introduces a fresh local name `k` not used in the postcondition or proof, creating a notational inconsistency with the rest of the ASN. The TA-dom *Depends:* section itself reverts to the consistent form ("licenses `actionPoint(w) ≤ #a` precondition"), making the divergence visible within the same claim's contract. A reader consulting only the Preconditions slot will see a convention mismatch.

VERDICT: OBSERVE