Reading the ASN end-to-end as a system.

**Dependency graph integrity.** The DAG is acyclic. Tracing: NAT-carrier ← NAT-order ← NAT-zero ← NAT-closure ← NAT-addcompat ← NAT-discrete, with NAT-wellorder and NAT-addbound branching off before NAT-sub. NAT-addbound does not depend on NAT-sub; TA5-SIG sits at the apex. No cycles.

**NAT-order.** Exactly-one trichotomy derivation: the three mutual-exclusion clauses each reduce to `n < n` via indiscernibility of `=` and transitivity, then discharge against irreflexivity. All three pairings covered. `≤`-transitivity: four cases from the cross-product of `m < n ∨ m = n` and `n < p ∨ n = p`. Each discharges cleanly — strict-strict by `<`-transitivity, mixed by indiscernibility, equal-equal by `=`-transitivity. No skipped case.

**NAT-closure.** Consequence `0 < 1`: successor-positivity at `n := 0` gives `0 < 0 + 1`; left-identity at `n := 1` gives `0 + 1 = 1`; substitutivity yields `0 < 1`. Both instantiations have their preconditions (`0 ∈ ℕ` from NAT-zero, `1 ∈ ℕ` from NAT-closure itself). Sound.

**NAT-discrete.** No-interval consequence: the `m < n` branch of the `m ≤ n` split produces `m + 1 ≤ n` from the axiom, then both disjuncts of the ≤-unfolding contradict — `m + 1 < n` and `n < m + 1` violates `¬(a < b ∧ b < a)`; `m + 1 = n` rewrites `n < m + 1` to `n < n`. The `m = n` branch yields `n = m` directly. Case analysis exhaustive and closed.

**NAT-addbound.** Right dominance: NAT-zero at `k := m` → `0 ≤ m`; right compat at `(0, m, n)` → `0 + n ≤ m + n`; left-identity collapses to `n ≤ m + n`; ≥-definition completes. Left dominance is symmetric. Both derivations use only cited dependencies.

**NAT-sub strict monotonicity.** The three trichotomy branches on `(a, b)`: `a < b` is the goal; `a = b` collapses `a + p < b + p` to `a + p < a + p`, irreflexivity kills it; `b < a` lifts to `b ≤ a` via ≤-definition, right-compat delivers `b + p ≤ a + p`, splitting to `b + p < a + p` (contradicts `¬(x < y ∧ y < x)` against `a + p < b + p`) or `b + p = a + p` (collapses to irreflexivity again). All branches exhausted.

**NAT-sub strict positivity.** Suppose `m − n = 0`: right-inverse rewrites to `0 + n = m`, left-identity gives `n = m`, but `n < m` (from `m > n`) violates `¬(n < m ∧ n = m)`. Hence `m − n ≠ 0`. NAT-zero clause at `k := m − n` delivers `0 < m − n`. NAT-discrete at `(0, m − n)` gives `0 + 1 ≤ m − n`; left-identity reduces to `1 ≤ m − n`. Every step cites a listed dependency.

**TA5-SIG.** The maximum-derivation from the least-element principle is the most complex argument. The construction forms `U` = set of upper bounds of `S` and applies NAT-wellorder to get minimum `m`. The contradiction (`m ∉ S` implies `m − 1 ∈ U` with `m − 1 < m`) is discharged correctly: NAT-discrete gives `i + 1 ≤ m` for each `i ∈ S`; the chain `1 ≤ i₀ < i₀ + 1 ≤ m` (case-splitting on `1 ≤ i₀` and `i₀ + 1 ≤ m`) strictly yields `1 < m`; NAT-sub conditional closure at `m ≥ 1` delivers `m − 1 ∈ ℕ`; `m − 1 < m` from right-inverse and NAT-addcompat's strict successor; case-splitting `i + 1 ≤ m` into `i + 1 = m` (right telescoping gives `i = m − 1`) and `i + 1 < m` (strict monotonicity at `p = 1`, with both `i + 1 ≥ 1` and `m ≥ 1` established, gives `i < m − 1`) — both sub-cases yield `i ≤ m − 1`. So `m − 1 ∈ U`, contradicting minimality. Postcondition `1 ≤ sig(t) ≤ #t` holds: all-zero case gives `sig(t) = #t ≥ 1`; nonzero case gives `sig(t) = m ∈ S ⊆ {1, ..., #t}`. Case partition (at-least-one-nonzero / all-zero) is exhaustive and mutually exclusive.

No ungrounded symbols, no missing cases, no broken precondition chains, no hand-waved steps.

VERDICT: CONVERGED