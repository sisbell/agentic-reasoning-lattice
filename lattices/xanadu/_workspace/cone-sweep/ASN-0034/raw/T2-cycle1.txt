**NAT-order.** Exactly-one trichotomy is derived correctly from three axioms: `¬(m < n ∧ n < m)` chains through transitivity to `m < m` against irreflexivity; `¬(m < n ∧ m = n)` and `¬(m = n ∧ n < m)` both reduce to `m < m` by substituting the equality into the strict relation. The `≤`-transitivity walks all four cases. Sound.

**NAT-addcompat.** Pure axiom positing. Dependencies on NAT-carrier, NAT-order, NAT-closure are all required and correctly cited. Sound.

**T3.** Forward direction instantiates T0's extensionality clause directly. Reverse direction applies Leibniz's law: `#a = #b` first (establishing shared domain), then `aᵢ = bᵢ` on that domain. No gap.

**NAT-discrete.** Axiom plus no-interval derivation. Case `m < n` is discharged by contradiction: `m + 1 ≤ n` together with hypothesis `n < m + 1` exhausts to two sub-cases, each contradictory. Case `m = n` yields `n = m` directly. Exhaustive.

**NAT-wellorder.** Axiom only. Dependencies correct.

**T0.** The comprehension and extensionality axioms are internally consistent; all symbols (`#·`, component projection, `ℕ`, `≤`, `1`) are grounded in the cited dependencies. Sound.

**T1.** This is the primary claim. Reviewing systematically:

*Irreflexivity (a).* Case (i) contradicts NAT-order irreflexivity at `aₖ < aₖ`. Case (ii) requires `m + 1 ≤ m`; NAT-addcompat supplies `m < m + 1`; exactly-one trichotomy then rules out both `m + 1 < m` and `m + 1 = m` since `m < m + 1` already holds. Both branches contradictory.

*Trichotomy (b).* Divergence position set is a subset of ℕ, non-empty in Cases 2 and 3; NAT-wellorder delivers the first. Clauses (α), (β), (γ) are mutually exclusive: (α) and (β) require `k ≤ m` and `k = m + 1` simultaneously, yielding `m + 1 ≤ m`, impossible; (β) and (γ) together require `m = n` and `m + 1 ≤ m`, also impossible. So the first divergence position satisfies exactly one clause, and the case split is exhaustive. Case 1 derives `m = n` from the contrapositives of NAT-discrete and then uses T3. Cases 2 and 3 each establish a witness and rule out the reverse witness — agreement-at-`k` and the position-ordering arguments are all properly grounded.

*Transitivity (c).* Case `k₁ < k₂`: `k₁ ≤ p` is derived from `k₁ < k₂ ≤ p`; agreement at `k₁` follows from the `b < c` witness condition at positions `< k₂`; both T1(i) and T1(ii) sub-cases produce valid witnesses. Case `k₂ < k₁`: T1(ii) for `b < c` is ruled out by showing `k₂ = n + 1 > k₁` would contradict `k₂ < k₁`; T1(i) then applies; `k₂ ≤ m` is established by separate sub-cases on how `a < b` is witnessed, with NAT-discrete contrapositive handling the T1(ii) branch. Case `k₁ = k₂ = k`: four sub-cases by witness type; (i,i) and (ii,i) produce witnesses; (i,ii) gives `k ≤ n` and `k = n + 1`, yielding `n + 1 ≤ n`, impossible; (ii,ii) gives `m + 1 = n + 1` so `m = n` by NAT-cancel, then `m + 1 ≤ n = m` gives `m < m`, impossible. All realizable sub-cases produce witnesses.

*Dependency audit.* NAT-cancel is used precisely for right cancellation in sub-case (ii,ii): `m + 1 = n + 1 ⟹ m = n`. NAT-discrete's no-interval consequence provides the reverse inclusion `i < m + 1 ⟹ i ≤ m` in T2 and the contrapositive direction in T1 Case 1. NAT-wellorder is correctly invoked for the first divergence position. All cited dependencies are necessary.

**NAT-carrier, NAT-closure, NAT-cancel, NAT-zero.** Axiom clusters with correctly cited dependency chains. NAT-cancel's summand-absorption consequence walks both the posited and mirror forms. NAT-closure's `0 < 1` consequence chains successor-positivity at `n := 0`, left identity at `n := 1`, and substitutivity. NAT-zero's minimum consequence derives `¬(n < 0)` by transitivity and irreflexivity in both the `0 < n` and `0 = n` cases. All sound.

**T2.** The computation bound is established by case analysis: divergence within shared range uses NAT-wellorder for the first such position; exhaustion sub-cases use NAT-discrete and NAT-addcompat for domain identification. The frame condition — no external state consulted — follows from the postcondition that only `aᵢ`, `bᵢ`, `#a`, `#b` are accessed, with no references beyond tumbler components.

**Dependency DAG.** No cycles. NAT-carrier ← NAT-zero ← NAT-order; NAT-closure ← {NAT-carrier, NAT-zero, NAT-order}; T chain builds correctly above.

VERDICT: CONVERGED