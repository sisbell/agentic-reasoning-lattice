Reading the entire ASN as a system — tracking definition consistency, precondition chains across claim boundaries, and case coverage exhaustion.

**Dependency DAG.** No cycles. T1 depends on T3 (this ASN); T3 depends only on T0; no back-edge through T1. NAT-addbound is downstream of NAT-sub in the dependency graph (NAT-addbound depends on NAT-carrier, NAT-zero, NAT-addcompat, NAT-closure, NAT-order — none touch NAT-sub).

**NAT-order consequence (exactly-one trichotomy).** All three mutual-exclusion conjuncts discharge correctly. `¬(m < n ∧ n < m)`: transitivity gives `m < m`, against irreflexivity. `¬(m < n ∧ m = n)`: indiscernibility rewrites `m < n` to `m < m`, against irreflexivity. `¬(m = n ∧ n < m)`: symmetric. ✓

**TumblerAdd result-length.** `p = (k−1)+1+(n−k)` collapses to `n` via NAT-sub's right-inverse at `(k,1)` (requires `k ≥ 1`, supplied by ActionPoint) and left-inverse at `(n,k)` (requires `n ≥ k`, supplied by ActionPoint's `k ≤ #w`). ✓

**TumblerAdd strict advancement.** The chain `aₖ < aₖ+1 ≤ aₖ+wₖ` (from NAT-addcompat strict successor and left compatibility at `p=1,n=wₖ,m=aₖ`) gives `rₖ > aₖ` regardless of `aₖ`'s value, including `aₖ = 0`. T1 case (i) at `k` is licensed by `k ≤ m` (precondition) and `k ≤ n` (ActionPoint bound). ✓

**TumblerAdd dominance.** Case split covers all possibilities: (1) some `aⱼ > 0` for `j < k` — NAT-wellorder finds least such, rⱼ = aⱼ > 0 = wⱼ, T1(i) at j. (2) all `aᵢ = 0` for `i < k`, `aₖ > 0` — NAT-cancel rules out `aₖ + wₖ = wₖ`, T1(i) at k. (3) all `aᵢ = 0` for `i ≤ k` — `rₖ = 0 + wₖ = wₖ`, components match for all positions, T3 gives `r = w`. The three sub-cases partition the space (NAT-zero supplies the dichotomy on `aₖ`). ✓

**ActionPoint uniqueness.** Four pairings from `m₁ ≤ m₂` and `m₂ ≤ m₁` unfolded: three give `x < x` contradictions, one gives `m₁ = m₂`. The zeros-below argument correctly establishes `i ∈ S` from the contradiction assumption, then derives `actionPoint(w) ≤ i` and `i < actionPoint(w)` — both disjuncts of the unfolded bound collapse to `x < x`. ✓

**T1 irreflexivity.** Case (i): `aₖ < aₖ` against NAT-order's irreflexivity. Case (ii): `m+1 ≤ m` gives `m+1 < m ∨ m+1 = m`; NAT-addcompat supplies `m < m+1`; both disjuncts contradict via trichotomy or irreflexivity. ✓

**T1 trichotomy.** Case 1 (no divergence position): contrapositives of NAT-discrete exclude both `m < n` and `n < m`, forcing `m = n`; agreement at all shared positions then gives `a = b` by T3. Case 2 (clause α): NAT-order's trichotomy on `aₖ` vs `bₖ` resolves exactly one direction; reverse witness exclusion is fully argued for `k' < k`, `k' = k`, and `k < k'`. Case 3 (clause β or γ): `m ≠ n` derived from the `≤`-expansion; sub-case `m < n` eliminates γ via the `n + 1 ≤ m` → `n < m` → contradiction path; no reverse witness follows. ✓

**T1 transitivity.** All four sub-cases of `k₁ = k₂ = k` are covered: (i,i) uses NAT-order transitivity; (ii,i) passes `m+1 ≤ p` from `k₁ ≤ p`; (i,ii) gives `n+1 ≤ n`, discharged by NAT-addcompat + trichotomy; (ii,ii) gives `m+1 = n+1` → `m = n` by NAT-cancel, then `m+1 ≤ n` → `m < n` contradicts `m = n`. Cases `k₁ < k₂` and `k₂ < k₁` are fully argued including the T1(ii) branch for the reverse. ✓

**TA-assoc.** Action point of `s = b ⊕ c`: all three NAT-order trichotomy sub-cases on `(k_b, k_c)` correctly identify the first nonzero component of `s` and verify it lies in `{1, …, #s}`. Domain conditions for all four TumblerAdd invocations are met. Case 2 (equal action points) uses NAT-addassoc for the key component equality `(aₖ + bₖ) + cₖ = aₖ + (bₖ + cₖ)`. Case 3 (k_b > k_c): b's contribution above k_c is invisible on both sides since the tail copy from displacement replaces it. T3 closes all three cases. ✓

No definition shifts, no broken precondition chains, no ungrounded symbols, no missing cases.

VERDICT: CONVERGED