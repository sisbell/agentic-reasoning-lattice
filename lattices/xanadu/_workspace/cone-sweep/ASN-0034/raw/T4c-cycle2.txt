Reading the full dependency cone — NAT-carrier through T4c — with the goal of finding what no per-claim check can see.

---

## Full Review: ASN-0034 Foundation Cone

### Dependency graph

Checking for cycles and missing links:

```
NAT-carrier → (none)
NAT-order → NAT-carrier
NAT-zero → NAT-carrier, NAT-order
NAT-closure → NAT-carrier, NAT-zero, NAT-order
NAT-discrete → NAT-carrier, NAT-order, NAT-closure
NAT-addcompat → NAT-carrier, NAT-order, NAT-closure
NAT-card → NAT-carrier, NAT-order, NAT-closure, NAT-zero
T0 → NAT-carrier, NAT-closure, NAT-order
T4 → T0, NAT-zero, NAT-discrete, NAT-order, NAT-closure, NAT-card
T4c → T0, NAT-zero, NAT-card, NAT-order, NAT-closure, NAT-addcompat, T4
```

No cycles. Every symbol cited in each claim traces to a listed dependency. Grounding is complete end-to-end.

---

### NAT-order

Exactly-one trichotomy derived from three axiom clauses:

- `¬(m < n ∧ n < m)`: transitivity chains both to `m < m`; irreflexivity closes. Sound.
- `¬(m < n ∧ m = n)`: substituting `m = n` into `m < n` via indiscernibility yields `m < m`; irreflexivity closes. Sound.
- `¬(m = n ∧ n < m)`: same substitution into `n < m`; same close. Sound.

`≤`-transitivity: four cases, all discharged. The `m < n ∧ n = p` and `m = n ∧ n < p` cases each use indiscernibility of `=` correctly. The `m = n ∧ n = p` case uses transitivity of `=`. No case is skipped or hand-waved.

---

### NAT-zero

Consequence `¬(n < 0)` proof: suppose `n < 0`; axiom yields `0 < n ∨ 0 = n`; the first branch chains `0 < n ∧ n < 0` to `0 < 0` via transitivity against irreflexivity; the second rewrites `n < 0` to `0 < 0` via indiscernibility against irreflexivity. Both branches discharged. Sound.

---

### NAT-closure

`0 < 1` consequence: successor-positivity at `n := 0` gives `0 < 0 + 1`; left identity at `n := 1` (licensed by `1 ∈ ℕ`) gives `0 + 1 = 1`; substitutivity yields `0 < 1`. Three-step derivation is complete. Sound.

---

### NAT-discrete

No-interval consequence: assumes `m ≤ n < m + 1`; splits on `m ≤ n` into `m < n` and `m = n`. The `m < n` case: axiom gives `m + 1 ≤ n` i.e. `m + 1 < n ∨ m + 1 = n`; both sub-cases derive contradiction — the first by `¬(a < b ∧ b < a)` at `(m+1, n)`, the second by rewriting `n < m + 1` to `n < n` via `m + 1 = n` against irreflexivity. The case is correctly discharged by contradiction, not silently dropped. The `m = n` case gives `n = m` directly. Complete case analysis. Sound.

---

### NAT-card

The "unique `k ∈ ℕ`" phrasing carries both existence and uniqueness as a foundation axiom. Uniqueness holds because a strictly increasing enumeration of a finite set uniquely determines its cardinality (distinct elements, strict order); this is axiomatized rather than proved. Acceptable for a foundation claim. The `k = 0` / `n = 0` edge case is handled: `{j ∈ ℕ : 1 ≤ j ≤ 0} = ∅` follows from `0 < 1` (NAT-closure consequence), the empty function is vacuously strictly increasing, and `|∅| = 0` follows. The upper-bound clause `|S| ≤ n` is separately axiomatized rather than derived from the characterization — redundant but not incorrect.

---

### T0

Comprehension and extensionality axioms are well-formed. The inner quantifier `(A i ∈ ℕ : 1 ≤ i ≤ p : tᵢ = r(i))` is well-typed: the existential witness `t ∈ T` with `#t = p` licenses the projection. Extensionality's `bᵢ` access is licensed because `#a = #b` equates the two index domains. Sound.

---

### T4

**`tᵢ₊₁` well-definedness.** For `i` in range `1 ≤ i < #t`:

- Upper bound `i + 1 ≤ #t`: NAT-discrete at `(i, #t)` from `i < #t`. ✓
- Lower bound `1 ≤ i + 1`: successor-positivity at `n := i` gives `0 < i + 1`; NAT-discrete at `(0, i + 1)` gives `0 + 1 ≤ i + 1`; left identity at `n := 1` rewrites to `1 ≤ i + 1`. Three explicit steps, no gap. ✓

**Exhaustion proof.** The induction-like case split at `(zeros(t), 0)`, `(1)`, `(2)`, `(3)` is carried through:

- Each trichotomy application requires both arguments in ℕ: `zeros(t) ∈ ℕ` from NAT-card, `0 ∈ ℕ` from NAT-zero, `1 ∈ ℕ` from NAT-closure, `2 := 1 + 1 ∈ ℕ` and `3 := 2 + 1 ∈ ℕ` from NAT-closure closure. All grounded.
- At each step, the exclusion of the "less than" alternative uses the correct mutual-exclusion conjunct of exactly-one trichotomy together with the lower bound established by NAT-discrete + NAT-closure's left identity.
- The final step uses both `3 ≤ zeros(t)` (from the continuing branch) and `zeros(t) ≤ 3` (from T4-valid hypothesis) to exclude both remaining alternatives, leaving exactly `zeros(t) = 3`. Both exclusions walk the unfolding of `≤` and apply exactly-one; no step is skipped.

All four branches terminate with `zeros(t) ∈ {0, 1, 2, 3}`. The Exhaustion is airtight.

---

### T4c

**Injectivity.** The chain `0 < 1 < 2 < 3` is assembled as: `0 < 1` from NAT-closure's consequence; `1 < 2` and `2 < 3` from NAT-addcompat's strict successor at `n := 1` and `n := 2` (both instantiations licensed by `1 ∈ ℕ`, `2 ∈ ℕ`). Transitivity chains `m < n` for all six pairs with `m` preceding `n`. The conjunct `m < n ⟹ m ≠ n` (from `¬(m < n ∧ m = n)`) yields pairwise distinctness for all six pairs.

**Label disjointness.** The injectivity paragraph explicitly states "since `zeros(t)` is single-valued, distinct zero counts induce distinct labels." Single-valuedness is grounded by NAT-card's axiom (`|S|` is *the* unique `k`). The connection from numeral distinctness to disjoint label extensions is made, not left implicit.

**Exhaustion.** Cites T4's Exhaustion Consequence directly — every T4-valid tumbler satisfies `zeros(t) ∈ {0, 1, 2, 3}`. ✓

Postcondition "exactly one label per T4-valid tumbler" factors cleanly into the Exhaustion (at least one) and Pairwise Disjointness (at most one) paragraphs. Both are established. Sound.

---

### Cross-claim coherence checks

- `=` on ℕ elements: implied by set membership (NAT-carrier supplies ℕ as a set); indiscernibility of `=` is used as a logical axiom throughout, consistently.
- `0 ∈ ℕ` propagation: NAT-zero supplies `0 ∈ ℕ`; every claim using the literal `0` in the codomain of `+` or as a comparison target lists NAT-zero as a dependency.
- Numeral definitions `2 := 1 + 1`, `3 := 2 + 1`: introduced in T4, cited in T4c; the arithmetic grounding is NAT-closure closure in both sites.
- `≥` and `>` defined in NAT-order via `≤` and `<` respectively; used in T0's comprehension axiom (`p ≥ 1`) with NAT-order in T0's Depends. ✓
- `+` is never assumed commutative; the left- and right-identity laws are separate axioms; left/right order compatibility in NAT-addcompat are separately axiomatized. No implicit commutativity. ✓

---

No findings of any kind.

VERDICT: CONVERGED