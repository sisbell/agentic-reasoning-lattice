Working through each claim for cross-claim consistency, ungrounded symbols, and case coverage.

The NAT-* layer is internally consistent. The dependency order (NAT-carrier → NAT-order → NAT-zero → NAT-closure → NAT-addcompat, NAT-cancel, NAT-discrete, NAT-wellorder, NAT-sub, NAT-addbound) is acyclic and each claim's Depends matches its actual usage.

T0, TA-Pos, ActionPoint, and T3 look sound. ActionPoint's zeros-below derivation correctly dispatches the two disjuncts of actionPoint(w) ≤ #w (via < and = branches) before concluding i < #w, and the w_{actionPoint(w)} ≥ 1 derivation correctly chains NAT-zero's disjunction clause through NAT-discrete to reach 0 + 1 ≤ w_{actionPoint(w)} and then applies NAT-closure's left identity.

T1's irreflexivity and transitivity proofs walk their cases. The transitivity sub-cases (i,i), (ii,i), (i,ii), and (ii,ii) are each completed; sub-case (i,ii) correctly derives n+1 ≤ n and discharges by contradiction, and (ii,ii) correctly uses NAT-cancel right cancellation to pass from m+1 = n+1 to m = n before reaching contradiction. The T1 Depends section accurately records all of these.

TumblerAdd's result-length identity correctly applies NAT-sub's right-inverse at (k,1) and left-inverse at (#w,k). The strict advancement and dominance proofs cover their cases, including the vacuous prefix (k=1), the aₖ=0 equality branch for dominance (discharging to r=w via T3), and the aⱼ>0 case requiring NAT-wellorder on the set of prefix components.

TA0 is an immediate re-export.

TA1's proof structure covers all four combinations of T1's case (i) and (ii) for a<b against the action point position, and each sub-case leads to a valid conclusion.

---

### `min` is used in TA1's Formal Contract without definition or citation

**Class**: REVISE
**Foundation**: NAT-order (NatStrictTotalOrder) — total order on ℕ from which min is derivable, but not cited or named
**ASN**: TA1 (OrderPreservationUnderAddition) — Formal Contract *Preconditions*: "actionPoint(w) ≤ min(#a, #b)"; proof body, Case (ii): "min(#a, #b) = #a, so k ≤ #a"
**Issue**: The function symbol `min` appears in the Preconditions field and in the proof without definition or citation. Every other symbol used in formal positions at this level of precision — 0, 1, +, ≤, #·, actionPoint — is either axiomatized or derived from a named postcondition of a cited dependency. `min` is neither. The *Depends* section lists no claim that introduces or characterises min. A downstream claim instantiating TA1 and discharging its precondition "actionPoint(w) ≤ min(#a, #b)" cannot determine the meaning of `min` from the Formal Contract alone. The Case (ii) step "min(#a, #b) = #a" additionally relies on the property min(m,n) = m when m < n, which is likewise ungrounded.
**What needs resolving**: Either rewrite the precondition as `actionPoint(w) ≤ #a ∧ actionPoint(w) ≤ #b` — which is directly grounded by NAT-order and is exactly what TA0's well-definedness precondition requires for both operands — or introduce and cite a claim that defines `min` on ℕ with the property min(m,n) ≤ m ∧ min(m,n) ≤ n ∧ (min(m,n) = m ∨ min(m,n) = n), derivable from NAT-order's totality. The Case (ii) proof step must also be grounded against whichever definition is chosen.

---

VERDICT: REVISE