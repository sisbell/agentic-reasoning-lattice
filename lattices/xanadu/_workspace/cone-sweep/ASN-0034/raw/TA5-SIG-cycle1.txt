**Foundation consistency audit — ASN-0034**

I read each claim in dependency order and traced every proof's cases.

**NAT-carrier → NAT-order → NAT-zero → NAT-closure → NAT-addcompat / NAT-discrete → NAT-addbound / NAT-wellorder → NAT-sub → T0 → TA5-SIG**

**NAT-order.** Exactly-one trichotomy: all three mutual-exclusion arguments are present and correct. `≤`-transitivity: all four cases of the hypothesis unfolding are walked individually. Sound.

**NAT-zero.** Consequence `¬(n < 0)`: the proof handles both disjuncts of the minimality clause and reaches irreflexivity in each. Case analysis exhaustive.

**NAT-closure.** Consequence `0 < 1`: successor-positivity at `n := 0`, left-identity at `n := 1`, substitutivity — three steps, no gap. The successor-positivity universal closes the `m + 1 = 0` gap for all `m`, not merely `m = 0`; this is noted and correct.

**NAT-discrete.** No-interval consequence: case `m < n` is correctly shown vacuous via two sub-cases of the unfolded `m + 1 ≤ n`; case `m = n` yields the conclusion directly. Precondition chain through NAT-order and NAT-closure is unbroken.

**NAT-addbound.** Right and left dominance: each derivation instantiates NAT-zero's minimality, NAT-addcompat's respective compatibility clause, and NAT-closure's identity law. All variable renamings in the instantiations are consistent; domain conditions are met. The claim carries no axioms of its own — it is a pure derived lemma, which is architecturally sound.

**NAT-sub.** Strict monotonicity: trichotomy on `(a, b)` yields three cases; `a < b` is the desired conclusion; `a = b` contradicts irreflexivity; `b < a` unfolds through `b ≤ a`, right-compat, and ≤-splitting to reach irreflexivity and exactly-one trichotomy respectively. All sub-cases discharged. Strict positivity: the contradiction under `m − n = 0` uses right-inverse, left-identity, and exactly-one trichotomy at the right instantiation; NAT-zero's disjunction then lifts `m − n ≠ 0` to `0 < m − n`; NAT-discrete at `(0, m − n)` and left-identity complete the chain. Both consequence derivations cite the correct dependency subsets.

**T0.** Axiom slots are well-typed against NAT-carrier, NAT-closure, NAT-order. No derived claims; no proofs to audit.

**TA5-SIG.** The maximum derivation via the upper-bound set `U` is the most complex argument. I traced it in full:

- `U ≠ ∅`: every `i ∈ S` is bounded above by `#t`, so `#t ∈ U`. ✓  
- NAT-wellorder gives `m = min(U)`.  
- Contradiction proof for `m ∉ S`: every `i ∈ S` then satisfies `i < m` (≤-definition plus `i ≠ m`); NAT-discrete promotes this to `i + 1 ≤ m`. The chain `1 ≤ i₀ < i₀ + 1 ≤ m` is dispatched correctly through all four `(< / =) × (< / =)` sub-cases to yield `1 < m`, hence `m ≥ 1`. NAT-sub conditional closure delivers `m − 1 ∈ ℕ`. The strict bound `m − 1 < m` follows from right-inverse plus NAT-addcompat strict successor inequality. The bounding argument for `m − 1 ∈ U` splits `i + 1 ≤ m` into `i + 1 = m` (handled by right-telescoping and substitutivity) and `i + 1 < m` (handled by NAT-sub strict monotonicity, with preconditions `i + 1 ≥ 1` and `m ≥ 1` each established in the proof). Both sub-cases yield `i ≤ m − 1`. Contradiction with minimality of `m`. ✓  
- Postcondition `1 ≤ sig(t) ≤ #t`: nonzero case follows from `m ∈ S` and `#t ∈ U`; all-zero case follows from T0's nonemptiness axiom. ✓

Dependency citations are complete and consistent throughout. No ungrounded symbols, no hand-waved cases, no silent preconditions, no proof that asserts rather than establishes its conclusion.

VERDICT: CONVERGED