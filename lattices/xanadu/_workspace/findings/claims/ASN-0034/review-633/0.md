### Use-site inventory paragraph duplicates Depends slot in NAT-sub
**Class**: REVISE
**Foundation**: NAT-sub (NatPartialSubtraction)
**ASN**: The body paragraph beginning "The axiom body invokes symbols beyond ℕ's primitive membership. The strict order `<` together with its non-strict companion `≤` and reverse companions `≥`, `>` — all defined in NAT-order by …" continuing through "NAT-zero and NAT-discrete are declared in the Depends slot accordingly."
**Issue**: This paragraph is a pure use-site inventory: it walks each non-ℕ symbol to its source ASN, names where it appears in the axiom, and announces "X is declared in the Depends slot accordingly." The same information — often near-verbatim — already appears in the Depends slot's per-entry prose (e.g., the Depends entry for NAT-addbound says "supplies the right-dominance clause `(A m, n ∈ ℕ :: m + n ≥ n)`, which discharges the conditional-closure precondition `m + n ≥ n` implicit in the right-telescoping clause `(m + n) − n = m`"). The reader following the strict-monotonicity or strict-positivity proofs must skip past this paragraph; it does not advance the argument. Per the rubric, use-site inventories and meta-prose announcing what the Depends slot will declare are noise in structural slots.
**What needs resolving**: NAT-sub must remove the duplicative inventory from the axiom body. Any content here that is not already in the per-dependency Depends entries should migrate there; the body should retain only prose that advances the axiom statement or its derivations.