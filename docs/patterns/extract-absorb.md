# Extract/Absorb

*Not yet written.*

How the lattice grows inward. Shared concepts extracted into new foundation layers, duplicates absorbed.

## Realized in

The `note_extract` and `note_absorb` [Producer](../protocols/agents/producers.md) agents (operator-gated), plus the `provenance.extract` and `provenance.absorb` link types that record the structural moves. Both agents follow the modify-then-self-review composition pattern: each fire emits the structural change plus a scoped self-review; the [Correction Stigmergic Protocol](../protocols/README.md#stigmergic-protocol-primitives) cycle then drives the modified content back to quiescence.
