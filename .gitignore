.DS_Store
*.pyc
__pycache__/
.env
.claude/
.idea/
lattices/*/usage-log.jsonl
lattices/*/_docuverse/index.db
lattices/*/_docuverse/_failures/
_workspace/
_docuverse/.register_path.lock
channels/nelson/resources/literary-machines/*.pdf
channels/nelson/resources/literary-machines/raw/
/notes/

# Alloy checker output (receipt.json, solution files) — legacy path
# under _archive/ after the unified-docuverse migration; rules kept
# in case the archive is restored as a working tree.
lattices/xanadu/verification/alloy/**/receipt.json
lattices/xanadu/verification/alloy/**/*-solution-*.md

# Dafny per-ASN hash cache — operator-local state for skip-unchanged
# logic. Doesn't need to be versioned; fresh clones simply re-verify.
verification/dafny/**/_dafny-cache.json

worktrees/
articles/

# Rust build artifacts
target/
Cargo.lock
