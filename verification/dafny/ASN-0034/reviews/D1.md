# D1 — Contract FLAG

*2026-05-16 19:57*

postcondition weakened by an implication guard not present in the formal contract

The formal contract specifies:
  *Postconditions:* a ⊕ (b ⊖ a) = b

  (unconditional; Pos(w) and actionPoint(w) ≤ #a are established *inside* the proof from the preconditions — properties (2) and (3) of w, plus "Since k ≤ #a, TA0's precondition is satisfied" — not used as guards on the conclusion.)

The Dafny code has:
  ensures
    var w := TumblerSub.TumblerSub(b, a);
    PositiveTumbler.PositiveTumbler(w) && ActionPoint.ActionPoint(w) <= Length(a)
    ==> TumblerAdd.TumblerAdd(a, w) == b
  Missing/extra/wrong:
  The ensures is a conditional `P(w) ∧ ActionPoint(w) ≤ #a ==> a ⊕ w = b`, which is strictly weaker than the contract's unconditional equality. It is trivially satisfied whenever the antecedent fails, so the lemma does not actually guarantee `a ⊕ (b ⊖ a) = b` from the stated preconditions. The body of the proof already establishes `PositiveTumbler(w)` and `ActionPoint(w) = k ≤ Length(a)`, so these should be discharged internally — not exported as guards. The ensures should be:

  ensures TumblerAdd.TumblerAdd(a, TumblerSub.TumblerSub(b, a)) == b
