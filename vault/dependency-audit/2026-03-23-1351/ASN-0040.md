# Dependency Audit — ASN-0040

Declared depends: [34]


## Uncertain (needs LLM verification)

- UNCERTAIN ASN-0036: uses [S0, S1]

## LLM Verification

## Classification

**FLAGGED ASN-0036: uses [S0, S1]**

**S0** — **LOCAL**
ASN-0040 introduces S0 itself in the "The sibling stream" section: `(A i, j : 1 ≤ i < j : cᵢ < cⱼ)` — stream strictly ordered. It appears in the Properties Introduced table with status "introduced". The scanner matched the label text against ASN-0036's S0, but this is an independent property about ASN-0040's locally-defined sibling stream S(p, d).

**S1** — **LOCAL**
ASN-0040 introduces S1 itself in the same section: `(A n : n ≥ 1 : p ≼ cₙ)` — all stream elements extend parent. Also in the Properties Introduced table as "introduced". Same situation as S0 — independent local property, not a reference to ASN-0036.

---

`RESULT: 0 MISSING, 0 COLLISION, 2 LOCAL, 0 CLEAN`
