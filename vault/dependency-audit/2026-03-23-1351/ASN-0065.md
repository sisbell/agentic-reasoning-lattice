# Dependency Audit — ASN-0065

Declared depends: [34, 36, 47, 53, 58]


## Uncertain (needs LLM verification)

- UNCERTAIN ASN-0040: uses [B1, B2, B3]

## LLM Verification

## Analysis: ASN-0065 — FLAGGED ASN-0040 [B1, B2, B3]

### Checking where B1, B2, B3 are used in ASN-0065

The labels appear in the **Block Decomposition** section (R-BLK lemma):

> "Let B = {β₁, ..., βₘ} be a block decomposition of M(d) satisfying **B1–B3 (ASN-0058)**"

> "The resulting blocks satisfy **B3** (Consistency)"

> "Coverage (**B1**) and disjointness (**B2**) follow from π being a bijection"

The citation `(ASN-0058)` is explicit in the first occurrence. All three labels are used in the block decomposition context — Coverage, Disjointness, Consistency of a mapping block decomposition.

### Checking declared dependency exports

ASN-0058 is a **declared dependency** of ASN-0065, and its exports include exactly these labels:

- **B1 — Coverage (PREDICATE)**: `(A v ∈ dom(M(d)) : v₁ ≥ 1 : (E! j : ... : v ∈ V(βⱼ)))` — defined in ASN-0058
- **B2 — Disjointness (PREDICATE)**: `(A i, j : ... : V(βᵢ) ∩ V(βⱼ) = ∅)` — defined in ASN-0058
- **B3 — Consistency (PREDICATE)**: `(A j : ... : M(d)(vⱼ + k) = aⱼ + k)` — defined in ASN-0058

### Classification

| Label | Classification | Reason |
|-------|---------------|--------|
| B1 | **COLLISION** | ASN-0065 uses B1 from ASN-0058 (declared dependency), not ASN-0040. The label co-occurs in ASN-0040 but ASN-0065 explicitly cites ASN-0058 and the semantics match ASN-0058's Coverage predicate for mapping block decompositions. |
| B2 | **COLLISION** | Same as B1 — used from ASN-0058, not ASN-0040. Matches ASN-0058's Disjointness predicate. |
| B3 | **COLLISION** | Same as B1 — used from ASN-0058, not ASN-0040. Matches ASN-0058's Consistency predicate. |

`RESULT: 0 MISSING, 3 COLLISION, 0 LOCAL, 0 CLEAN`
