# Divergences — Valid composite (ValidComposite)

- **Line 33**: Elementary step predicates omit tumbler-level preconditions (IsElement for K.α, parent(e) ∈ E for K.δ) that constrain which addresses are valid operands. These are orthogonal to the coupling constraints and are modeled in ContentAllocatable and EntityCreatable respectively.
