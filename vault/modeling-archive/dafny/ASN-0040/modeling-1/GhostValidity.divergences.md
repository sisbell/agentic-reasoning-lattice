# Divergences — B3 (GhostValidity)

- **Line 11**: The ASN's baptismal state Σ contains only Σ.B. "Occupied" is a downstream concept defined by content operations. This module models occupancy as an explicit set to state the constraint; content-layer modules will instantiate what "occupied" means concretely.
