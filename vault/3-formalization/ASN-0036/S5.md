**S5 (Unrestricted sharing).** The same I-address may appear in the ranges of multiple arrangements, and at multiple V-positions within a single arrangement. The full set of state invariants — S0–S3 together with S7b, S8a, S8-vdepth, S8-depth, S8-fin, D-CTG, and D-MIN — is consistent with any finite sharing multiplicity; they place no constraint on `|{(d, v) : v ∈ dom(Σ.M(d)) ∧ Σ.M(d)(v) = a}|`:

`(A N ∈ ℕ :: (E Σ :: Σ satisfies S0–S3, S7b, S8a, S8-vdepth, S8-depth, S8-fin, D-CTG, D-MIN ∧ (E a ∈ dom(Σ.C) :: |{(d, v) : v ∈ dom(Σ.M(d)) ∧ Σ.M(d)(v) = a}| > N)))`

To see this, fix any `N`. Choose an element-level I-address `a` with `zeros(a) = 3` — such tumblers exist by T4 (ASN-0034) — and set `C(a) = w` for some `w ∈ Val` (non-empty by Σ.C). Construct state `Σ_N` with `N + 1` documents `d₁, ..., d_{N+1}`, each with `M(dᵢ) = {[1, 1] ↦ a}`. S0 and S1 are vacuous in this standalone model — no transitions are defined. S2 holds: each `M(dᵢ)` is a function with a single entry. S3 holds: `a ∈ dom(C)`. S7b holds: `zeros(a) = 3` by construction. Each V-position `[1, 1]` satisfies S8a (`zeros = 0`, first component `≥ 1`, all positive), S8-vdepth (`#[1, 1] = 2 ≥ 2`), and S8-depth (one position per subspace). S8-fin holds: each arrangement is a singleton. D-CTG and D-MIN hold trivially — each subspace contains one position `[1, 1]`, which is contiguous and is the minimum. The sharing multiplicity of `a` is `N + 1 > N`. Since `N` was arbitrary, no finite bound is entailed. The same holds within a single document: for any `N`, construct `Σ'_N` with one element-level I-address `a` where `zeros(a) = 3` and `C(a) = w`, and one document `d` with `M(d) = {[1, k] ↦ a : 1 ≤ k ≤ N + 1}`. The `N + 1` V-positions `[1, 1], …, [1, N+1]` satisfy S8a, S8-vdepth, and S8-depth (all depth 2 in subspace 1). S8-fin holds: `dom(M(d))` has `N + 1` elements. D-CTG holds: the positions form a contiguous range. D-MIN holds: `min(V₁(d)) = [1, 1]`. The within-document sharing multiplicity is `N + 1 > N`.

In any particular state, the sharing multiplicity of each address is a definite finite number — possibly zero for orphaned content (S6). The property is an architectural anti-constraint: the invariants place no finite cap on how many references may accumulate.

Nelson: "The virtual byte stream of a document may include bytes from any other document." And: "A document may have a window to another document, and that one to yet another, indefinitely. Thus A contains part of B, and so on. One document can be built upon another, and yet another document can be built upon that one, indefinitely." Transclusion is recursive and unlimited.

Gregory confirms the unbounded nature at the implementation level. The global index that records which documents reference which I-addresses accumulates entries without cap — "no counter, cap, MAX_TRANSCLUSIONS constant, or any other limiting mechanism anywhere in the code path." Each referential inclusion adds one entry. The only constraints are physical resources (memory and disk), not architectural limits.

The combination of S4 and S5 gives the system its distinctive character. S4 says identity is structural — determined by I-address, not by value. S5 says sharing is unlimited — any number of documents can reference the same content. Together they establish a regime in which quotation is a first-class structural relationship: any number of documents can quote the same passage, and the system knows they are all quoting — not independently writing — because they share I-addresses.

We observe that the state `Σ = (C, M)` makes the sharing relation computable: given any `a ∈ dom(C)`, the set `{d : (E v :: M(d)(v) = a)}` is determined by the state. Nelson requires this to be queryable: "It must also be possible for the reader to ask to see whatever documents window to the current document. Both are available at any time." The state model supports this — the information is present; only the efficiency of its extraction is an implementation concern.

*Proof.* We wish to show that for every `N ∈ ℕ`, there exists a state `Σ` satisfying S0–S3, S7b, S8a, S8-vdepth, S8-depth, S8-fin, D-CTG, and D-MIN in which some I-address has sharing multiplicity exceeding `N`. We give two constructions — one for cross-document sharing, one for within-document sharing — each succeeding for arbitrary `N`.

**Cross-document construction.** Fix `N ∈ ℕ`. Let `a` be an element-level I-address — a tumbler with `zeros(a) = 3`, whose existence is guaranteed by T4 (ASN-0034). Define state `Σ_N = (C_N, M_N)` by:

- `C_N = {a ↦ w}` for a value `w ∈ Val` — such `w` exists because `Val ≠ ∅` (Σ.C).
- `N + 1` documents `d₁, …, d_{N+1}`, with `M_N(dᵢ) = {[1, 1] ↦ a}`.

We verify each invariant. S0 (content immutability) and S1 (store monotonicity) are universally quantified over state transitions `Σ → Σ'`. We treat `Σ_N` as a standalone model in which no transitions are defined; the universal quantifier ranges over the empty set, so both predicates hold vacuously. (This is a model-theoretic argument: we demonstrate that the invariants are logically consistent with high sharing multiplicity, without asserting that `Σ_N` is reachable from any initial state `Σ₀` via a finite operation sequence.) S2 (arrangement functionality): each `M_N(dᵢ)` contains a single entry `{[1, 1] ↦ a}` — the domain has one element, so uniqueness of the image is immediate; `M_N(dᵢ)` is a function. S3 (referential integrity): the sole I-address referenced by any arrangement is `a`, and `a ∈ dom(C_N)` by construction. S7b (element-level I-addresses): `zeros(a) = 3` by choice of `a`. S8a (V-position well-formedness): `[1, 1]` has `zeros([1, 1]) = 0` (no zero components), `[1, 1]₁ = 1 ≥ 1`, and all components positive. S8-vdepth: `#[1, 1] = 2 ≥ 2`. S8-depth (fixed-depth V-positions): each document's sole subspace (subspace 1) contains one position, so the depth-uniformity predicate is trivially satisfied. S8-fin (finite arrangement): each `dom(M_N(dᵢ)) = {[1, 1]}` is a singleton, hence finite. D-CTG (contiguity): each subspace contains one position, so there are no intermediate positions to check — the contiguity predicate holds vacuously. D-MIN (minimum position): `min(V₁(dᵢ)) = [1, 1]`, whose post-subspace components are all 1 as required.

The sharing multiplicity of `a` in `Σ_N` is `|{(d, v) : v ∈ dom(M_N(d)) ∧ M_N(d)(v) = a}| = N + 1`, since each of the `N + 1` documents contributes exactly one pair `(dᵢ, [1, 1])`, and these pairs are pairwise distinct — the `dᵢ` are distinct documents. Thus the multiplicity exceeds `N`.

**Within-document construction.** Fix `N ∈ ℕ`. Let `a` be an element-level I-address with `zeros(a) = 3` as above. Define state `Σ'_N = (C'_N, M'_N)` by:

- `C'_N = {a ↦ w}` for a value `w ∈ Val` — `Val ≠ ∅` by Σ.C.
- One document `d` with `M'_N(d) = {[1, k] ↦ a : 1 ≤ k ≤ N + 1}`.

S0 and S1 hold by the same model-theoretic argument as above: `Σ'_N` is a standalone model with no transitions defined, so both predicates hold vacuously over the empty quantifier domain. S2 (arrangement functionality): the V-positions `[1, 1], [1, 2], …, [1, N+1]` are pairwise distinct — they differ at component 2 — so each maps to exactly one I-address (namely `a`); `M'_N(d)` is a well-defined function. S3 (referential integrity): the sole referenced I-address `a` satisfies `a ∈ dom(C'_N)` by construction. S7b: `zeros(a) = 3` by choice of `a`. S8a: each `[1, k]` with `1 ≤ k ≤ N + 1` has `zeros([1, k]) = 0` (no zero components), `[1, k]₁ = 1 ≥ 1`, and `k ≥ 1 > 0` so all components are positive. S8-vdepth: `#[1, k] = 2 ≥ 2` for each position. S8-depth: all `N + 1` positions lie in subspace 1 and have depth 2 — the depth-uniformity predicate is satisfied. S8-fin: `dom(M'_N(d)) = {[1, k] : 1 ≤ k ≤ N + 1}` has `N + 1` elements, hence is finite. D-CTG (contiguity): for any two positions `[1, j]` and `[1, k]` with `j < k` in `V₁(d)`, every intermediate `[1, i]` with `j < i < k` satisfies `1 ≤ i ≤ N + 1` (since `1 ≤ j < i < k ≤ N + 1`), so `[1, i] ∈ V₁(d)` — no gaps exist. D-MIN: `min(V₁(d)) = [1, 1]`, whose post-subspace component is 1 as required.

The within-document sharing multiplicity is `|{(d, v) : v ∈ dom(M'_N(d)) ∧ M'_N(d)(v) = a}| = N + 1 > N`, since the `N + 1` pairs `(d, [1, 1]), …, (d, [1, N+1])` are distinct — the V-positions are pairwise distinct and `d` is common to all.

**Conclusion.** Since both constructions succeed for arbitrary `N ∈ ℕ`, the full invariant set S0 ∧ S1 ∧ S2 ∧ S3 ∧ S7b ∧ S8a ∧ S8-vdepth ∧ S8-depth ∧ S8-fin ∧ D-CTG ∧ D-MIN is consistent with sharing multiplicity exceeding any given finite bound. No finite cap on `|{(d, v) : v ∈ dom(Σ.M(d)) ∧ Σ.M(d)(v) = a}|` is entailed by these invariants — neither across documents nor within a single document. The consistency demonstrated is model-theoretic: each constructed state is a standalone model satisfying all checked invariants, and the proof does not assert that these states are reachable from `Σ₀` via any finite operation sequence. Operational reachability — that one can construct a state with multiplicity `N + 1` by performing `N + 1` transclusions — is plausible and would strengthen the result from logical consistency to operational achievability, but falls outside the scope of this property. ∎

*Formal Contract:*
- *Preconditions:* `N ∈ ℕ` arbitrary; `Val ≠ ∅` (axiom, Σ.C); element-level I-addresses exist (T4, ASN-0034).
- *Postconditions:* There exists a state `Σ` satisfying S0 (content immutability), S1 (store monotonicity), S2 (arrangement functionality), S3 (referential integrity), S7b (element-level I-addresses), S8a (V-position well-formedness), S8-vdepth (minimal V-position depth), S8-depth (fixed-depth V-positions), S8-fin (finite arrangement), D-CTG (contiguity), and D-MIN (minimum position) such that for some `a ∈ dom(Σ.C)`, `|{(d, v) : v ∈ dom(Σ.M(d)) ∧ Σ.M(d)(v) = a}| > N`. The construction works both across documents (multiplicity `N + 1` over `N + 1` documents with V-position `[1, 1]` each) and within a single document (multiplicity `N + 1` at sequential V-positions `[1, 1], …, [1, N+1]`).
- *Frame:* The full set of state invariants is verified. The constructions use element-level I-addresses (S7b) and sequential V-positions in a single subspace at depth 2 (S8a, S8-vdepth, S8-depth, S8-fin, D-CTG, D-MIN). The consistency is model-theoretic: each constructed state is a standalone model satisfying all invariants, not necessarily a state reachable from `Σ₀` via operations.
