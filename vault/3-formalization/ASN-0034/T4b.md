**T4b (UniqueParse).** Under the constraints of T4 — at most three zero-valued components, no two zeros adjacent, `t₁ ≠ 0`, `t_{#t} ≠ 0` — the partial function

  `fields : T ⇀ Seq(ℕ⁺) × Seq(ℕ⁺) × Seq(ℕ⁺) × Seq(ℕ⁺)`

that decomposes a tumbler into its node, user, document, and element sub-sequences is well-defined and uniquely determined by `t` alone on the T4-valid subdomain, with absent fields represented by the empty sequence `ε` in the corresponding tuple position. Outside that subdomain — tumblers with `zeros(t) ≥ 4` or tumblers violating the field-segment constraint such as `[0, 1, 0, 2]` (leading zero) or `[1, 0, 0, 2]` (adjacent zeros) — `fields(t)` is not assigned a value by this corollary, and every caller must discharge T4-validity before invoking it.

We read `fields(t)` as the 4-tuple `(N(t), U(t), D(t), E(t))` and fix the projections `N, U, D, E : T → Seq(ℕ⁺)` accordingly, where `ℕ⁺ = {n ∈ ℕ : n > 0}` is the subcarrier of strictly positive naturals introduced in T0. Each projection returns a finite sequence over `ℕ⁺`: T0's carrier ℕ makes every non-zero component positive, and T4 assigns zero the role of separator only, so no field-content position carries the value zero — the value domain of the projections is therefore exactly T0's `ℕ⁺`. A projection may be the empty sequence `ε` — this is the representation chosen for an *absent* field. The representation is unambiguous: T4a (SyntacticEquivalence) records that every *present* field segment is non-empty under T4's field-segment constraint, so for each `X ∈ {N, U, D, E}`, `X(t) = ε` holds if and only if field `X` is absent in `t`.

The absence pattern of `fields(t)` is fixed by case analysis on `zeros(t)`, which by T4 lies in `{0, 1, 2, 3}`:

  - `zeros(t) = 0`: `N(t) = (t₁, ..., t_{#t})` (non-empty since `#t ≥ 1` by T0), `U(t) = D(t) = E(t) = ε`.
  - `zeros(t) = 1`: `N(t)` and `U(t)` are non-empty, `D(t) = E(t) = ε`.
  - `zeros(t) = 2`: `N(t)`, `U(t)`, and `D(t)` are non-empty, `E(t) = ε`.
  - `zeros(t) = 3`: all four projections are non-empty.

The component-access notation `t.X₁` (for `X ∈ {N, U, D, E}`) denotes the first component of `X(t)` and is therefore defined exactly when `X(t) ≠ ε`; by the table above, this reduces to a threshold check on `zeros(t)`. In particular, for every T4-valid `t ∈ T`, `t.N₁` is defined; `t.U₁` is defined iff `zeros(t) ≥ 1`; `t.D₁` is defined iff `zeros(t) ≥ 2`; `t.E₁` is defined iff `zeros(t) = 3`. Where `t.X₁` appears in downstream properties (for instance in T7, which compares `a.E₁` against `b.E₁`), the containing claim is understood to carry the corresponding threshold on `zeros` as a precondition.

The argument for well-definedness turns on a single observation: under T4's definitional role-assignment, the separator positions are exactly recoverable from the raw tumbler. We claim that a position `i` satisfies `tᵢ = 0` if and only if `i` is a field separator. The forward direction holds by construction: every separator has value 0 in the T4 address format `N₁. ... .Nₐ . 0 . U₁. ... .Uᵦ . 0 . D₁. ... .Dᵧ . 0 . E₁. ... .Eδ`. For the reverse direction: T4 assigns zero-valued positions the role of separators, so if `tᵢ = 0` then `i` is a separator position; the non-zero positions, which by T0's carrier ℕ are strictly positive, carry field components. Therefore the set `{i : 1 ≤ i ≤ #t ∧ tᵢ = 0}` is exactly the set of separator positions — computable by a single scan of `t`.

Given the separator positions, the field boundaries follow deterministically. Let the zero positions in increasing order be `s₁ < s₂ < ... < s_k`, where `k = zeros(t) ∈ {0, 1, 2, 3}`. We compute `fields(t)` case by case, exhibiting each projection explicitly:

  - *Case k = 0.* `N(t) = (t₁, ..., t_{#t})` and `U(t) = D(t) = E(t) = ε`. The node field occupies the entire tumbler; no separator carves off further fields. Non-emptiness of `N(t)` follows from `#t ≥ 1` (T0).
  - *Case k = 1.* `N(t) = (t₁, ..., t_{s₁ - 1})`, `U(t) = (t_{s₁ + 1}, ..., t_{#t})`, and `D(t) = E(t) = ε`. The single separator at `s₁` partitions the non-zero positions into two field segments. Non-emptiness of both is the field-segment constraint: `t₁ ≠ 0` forces `s₁ ≥ 2`, so `N(t)` has at least one component; `t_{#t} ≠ 0` forces `s₁ ≤ #t - 1`, so `U(t)` has at least one component.
  - *Case k = 2.* `N(t) = (t₁, ..., t_{s₁ - 1})`, `U(t) = (t_{s₁ + 1}, ..., t_{s₂ - 1})`, `D(t) = (t_{s₂ + 1}, ..., t_{#t})`, and `E(t) = ε`. The two separators at `s₁ < s₂` partition the non-zero positions into three field segments. Non-emptiness of the boundary segments follows as in case `k = 1`; non-emptiness of the middle segment `U(t)` follows from the no-adjacent-zeros clause (`s₂ ≥ s₁ + 2`), which forces `s₁ + 1 ≤ s₂ - 1`.
  - *Case k = 3.* `N(t) = (t₁, ..., t_{s₁ - 1})`, `U(t) = (t_{s₁ + 1}, ..., t_{s₂ - 1})`, `D(t) = (t_{s₂ + 1}, ..., t_{s₃ - 1})`, and `E(t) = (t_{s₃ + 1}, ..., t_{#t})`. The three separators partition the non-zero positions into four field segments. Non-emptiness of all four is the full field-segment constraint: the two boundary clauses handle `N(t)` and `E(t)`, and the two instances of no-adjacent-zeros (`s₂ ≥ s₁ + 2`, `s₃ ≥ s₂ + 2`) handle the interior segments `U(t)` and `D(t)`.

T4 directly asserts that no two separators are adjacent and that the tumbler neither begins nor ends with zero; T4a (SyntacticEquivalence) records the equivalent reading that each present field segment is therefore non-empty. Consequently, in every case above, every projection prescribed to be non-empty has at least one component, and every projection prescribed to be `ε` has no source positions to draw from. The canonical representation of T3 ensures that the components of `t` are fixed — there is no alternative encoding of the same tumbler with different component values. The separator positions are therefore uniquely determined, the field boundaries are uniquely determined, and the sub-sequences assigned to each field are uniquely determined.

Two distinct decompositions of `t` would require two distinct sets of separator positions. But there is exactly one such set. Therefore `fields(t)` is well-defined and unique. ∎

*Formal Contract:*
- *Preconditions:* `t` satisfies T3 (CanonicalRepresentation): the component sequence of `t` is fixed by sequence identity, with no alternative encoding yielding different component values. `t` satisfies the T4 constraints (at most three zero-valued components, field-segment constraint — no two zeros adjacent, `t₁ ≠ 0`, `t_{#t} ≠ 0`). Positivity of non-zero components is supplied by T0's carrier ℕ, not by a separate T4 clause.
- *Definition:* `fields : T ⇀ Seq(ℕ⁺) × Seq(ℕ⁺) × Seq(ℕ⁺) × Seq(ℕ⁺)` is the partial function whose domain is exactly the T4-valid subset of `T` (tumblers satisfying `zeros(t) ≤ 3`, no two zeros adjacent, `t₁ ≠ 0`, `t_{#t} ≠ 0`) and whose action splits the component sequence of `t` at its zero positions. Let `s₁ < s₂ < ... < s_k` enumerate the zero positions of `t` in increasing order, with `k = zeros(t) ∈ {0, 1, 2, 3}` by T4, and extend the indexing by the endpoint sentinels `s₀ = 0` and `s_{k+1} = #t + 1`; the `(i+1)`-th field segment of `t` (for `0 ≤ i ≤ k`) is the sub-sequence `(t_{s_i + 1}, ..., t_{s_{i+1} - 1})`, which is non-empty by T4's field-segment constraint and carries only strictly-positive components by T0's carrier ℕ. Then `fields(t) = (N(t), U(t), D(t), E(t))` is the 4-tuple that assigns these `k + 1` field segments to its first `k + 1` components in order — `N(t)` always, `U(t)` when `k ≥ 1`, `D(t)` when `k ≥ 2`, `E(t)` when `k = 3` — and the empty sequence `ε` to the remaining `3 - k` components. Outside the T4-valid subdomain — tumblers with `zeros(t) ≥ 4` or with `zeros(t) ≤ 3` but violating the field-segment constraint (e.g., `[0, 1, 0, 2]`, `[1, 0, 0, 2]`) — `fields(t)` is not assigned a value.
- *Depends:* T0 (CarrierSetDefinition) — invoked at four sites: in the subcarrier citation "`ℕ⁺ = {n ∈ ℕ : n > 0}` is the subcarrier of strictly positive naturals introduced in T0" (T0 supplies the definition of ℕ⁺ that grounds the type signature `Seq(ℕ⁺)` in the projections `N, U, D, E` and in the `fields` signature), in the projection-image claim "T0's carrier ℕ makes every non-zero component positive", in the separator-recovery claim "T0's carrier ℕ ensures that every non-zero component is strictly positive, so no value other than zero can play the separator role", and in the case `k = 0` length argument "Non-emptiness of `N(t)` follows from `#t ≥ 1` (T0)" — without T0 the definition of the ℕ⁺ subcarrier, the strict positivity of non-zero components, the carrier-length lower bound, and the exclusivity of zero as separator marker would all be unsourced. T3 (CanonicalRepresentation) — invoked at the closing of the well-definedness argument ("The canonical representation of T3 ensures that the components of `t` are fixed — there is no alternative encoding of the same tumbler with different component values"); T3 supplies the rigidity of component values that licenses the uniqueness conclusion "the separator positions are therefore uniquely determined, the field boundaries are uniquely determined, and the sub-sequences assigned to each field are uniquely determined". T4 (HierarchicalParsing) — invoked in the case-analysis range `zeros(t) ∈ {0, 1, 2, 3}` (the zero-count bound from T4), in the boundary clauses used for non-emptiness arguments in cases `k = 1, 2, 3` ("`t₁ ≠ 0` forces `s₁ ≥ 2`", "`t_{#t} ≠ 0` forces `s₁ ≤ #t - 1`"), in the no-adjacent-zeros clause used for interior segments ("non-emptiness of the middle segment `U(t)` follows from the no-adjacent-zeros clause (`s₂ ≥ s₁ + 2`)"), and in the role-assignment that fixes zeros as separator markers ("T4 assigns zero-valued positions the role of separators"); each case in the case analysis consumes a different combination of T4's clauses. T4a (SyntacticEquivalence) — invoked at the closing remark "T4 directly asserts that no two separators are adjacent and that the tumbler neither begins nor ends with zero; T4a (SyntacticEquivalence) records the equivalent reading that each present field segment is therefore non-empty" and again in the absence-marker clause "T4a (SyntacticEquivalence) records that every *present* field segment is non-empty under T4's field-segment constraint, so for each `X ∈ {N, U, D, E}`, `X(t) = ε` holds if and only if field `X` is absent in `t`"; T4a converts T4's positional clauses into the segment-non-emptiness statements that the case analysis references and supplies the unambiguity of the empty-sequence marker that the absence-pattern table records.
- *Postconditions:* `fields : T ⇀ Seq(ℕ⁺) × Seq(ℕ⁺) × Seq(ℕ⁺) × Seq(ℕ⁺)` is a partial function whose domain is exactly the T4-valid subset of `T` — those tumblers satisfying T4's constraints (at most three zero-valued components, no two zeros adjacent, `t₁ ≠ 0`, `t_{#t} ≠ 0`). On that subdomain `fields(t) = (N(t), U(t), D(t), E(t))` is well-defined and uniquely determined by `t`. Each projection returns a finite sequence over `ℕ⁺`; absent fields are represented by the empty sequence `ε`, and by T4a this marker is unambiguous — for each `X ∈ {N, U, D, E}`, `X(t) = ε` iff field `X` is absent in `t`. The absence pattern on the T4-valid subdomain, by case: `zeros(t) = 0` → only `N(t)` non-empty; `zeros(t) = 1` → `N(t), U(t)` non-empty, `D(t) = E(t) = ε`; `zeros(t) = 2` → `N(t), U(t), D(t)` non-empty, `E(t) = ε`; `zeros(t) = 3` → all four non-empty. The component-access notation `t.X₁` denotes the first component of `X(t)` and is defined iff `X(t) ≠ ε`: for every T4-valid `t ∈ T`, `t.N₁` is defined; `t.U₁` iff `zeros(t) ≥ 1`; `t.D₁` iff `zeros(t) ≥ 2`; `t.E₁` iff `zeros(t) = 3`. On tumblers `t ∈ T` that are not T4-valid — those with `zeros(t) ≥ 4` or with `zeros(t) ≤ 3` but violating the field-segment constraint (e.g., `[0, 1, 0, 2]`, `[1, 0, 0, 2]`) — this corollary assigns no value to `fields(t)`, so consumers must carry T4-validity as a precondition before reading any projection.
