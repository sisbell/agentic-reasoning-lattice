label: NAT-addcompat
name: NatAdditionOrderCompatibility
type: axiom
depends: []
summary: 'Addition on ℕ is monotone with respect to order: if `n ≥ p` then `m + n ≥ m + p` for all `m`. Additionally, every
  natural number is strictly less than its successor: `n < n + 1`.'
