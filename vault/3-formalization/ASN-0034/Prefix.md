**Definition (Prefix).** The prefix relation on tumblers: `p ≼ q` iff `#p ≤ #q ∧ (∀i : 1 ≤ i ≤ #p : qᵢ = pᵢ)`. A proper prefix `p ≺ q` requires `p ≼ q` with `p ≠ q`. This entails `#p < #q`: if `#p = #q`, then the prefix condition `(∀i : 1 ≤ i ≤ #p : qᵢ = pᵢ)` covers all positions of both tumblers, and T3 (CanonicalRepresentation) gives `p = q`, contradicting `p ≠ q`.

The relation `≼` is reflexive: every tumbler is a prefix of itself. Instantiating the Definition at `p = q = t`, the length conjunct `#t ≤ #t` follows from NAT-order's defining clause `m ≤ n ⟺ m < n ∨ m = n` at `(m, n) = (#t, #t)`, whose equality disjunct `m = n` is satisfied by `#t = #t`; the component conjunct `tᵢ = tᵢ` for every `i` with `1 ≤ i ≤ #t` is the reflexivity of equality at each index. Both conjuncts of the Definition hold at `p = q = t`, so `t ≼ t`.

*Formal Contract:*
- *Definition:* `p ≼ q` iff `#p ≤ #q ∧ (∀i : 1 ≤ i ≤ #p : qᵢ = pᵢ)`. Proper prefix: `p ≺ q` iff `p ≼ q ∧ p ≠ q`.
- *Depends:* T0 (CarrierSetDefinition) — the definition uses length `#p`, `#q` and component projection `pᵢ`, `qᵢ` for `p, q ∈ T`, which T0 introduces. NAT-order (NatStrictTotalOrder) — the bound `#p ≤ #q` invokes NAT-order's non-strict ordering `≤` on ℕ to compare the lengths of the two tumblers, and the universal quantification `(∀i : 1 ≤ i ≤ #p : qᵢ = pᵢ)` invokes NAT-order's `≤` on ℕ to range the component-projection index over the positions `1, ..., #p` of `p`; the derived postcondition `p ≺ q ⟹ #p < #q` further invokes NAT-order's defining clause for `≤`, namely `m ≤ n ⟺ m < n ∨ m = n` — given the conjunct `#p ≤ #q` of `p ≼ q` together with `#p ≠ #q` (the alternative `#p = #q` being discharged by the contradiction with T3 cited below), the disjunction collapses to `#p < #q`, supplying the strict ℕ-inequality at the length level that the postcondition asserts; the reflexivity postcondition `(∀t ∈ T :: t ≼ t)` further invokes the same defining clause at `(m, n) = (#t, #t)`, whose equality disjunct `m = n` is satisfied by `#t = #t`, yielding the length conjunct `#t ≤ #t` of the Definition at `p = q = t`; this discharges the appeals to length comparison, component-index range, the `≤`-with-strictness step, and the `≤`-at-equal-arguments step from NAT-order rather than leaving them implicit, matching the per-step citation convention established for `T1` and `TA5`. T3 (CanonicalRepresentation) — the derived postcondition `p ≺ q ⟹ #p < #q` invokes T3: when `#p = #q`, the prefix condition `(∀i : 1 ≤ i ≤ #p : qᵢ = pᵢ)` exhausts both tumblers' positions and T3 forces `p = q`, contradicting the strictness clause `p ≠ q` of `≺`.
- *Derived postcondition (proper-prefix length):* `p ≺ q ⟹ #p < #q` (by T3 to rule out `#p = #q`, then NAT-order's `≤`-unfolding to convert `#p ≤ #q ∧ #p ≠ #q` into `#p < #q`).
- *Derived postcondition (reflexivity):* `(∀t ∈ T :: t ≼ t)`. Instantiate the Definition at `p = q = t`: the length conjunct `#t ≤ #t` is discharged by NAT-order's defining clause `m ≤ n ⟺ m < n ∨ m = n` at `(m, n) = (#t, #t)`, whose equality disjunct is satisfied by `#t = #t`; the component conjunct `tᵢ = tᵢ` for `1 ≤ i ≤ #t` is the reflexivity of equality at each index. Both conjuncts hold, so `t ≼ t`.
