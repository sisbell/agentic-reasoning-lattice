label: T3
name: CanonicalRepresentation
type: corollary
depends:
- T0
summary: Each tumbler has exactly one representation as a component sequence — equality holds if and only if the sequences
  have the same length and matching components at every position. No normalization, quotient, or external identification is
  imposed on T.
