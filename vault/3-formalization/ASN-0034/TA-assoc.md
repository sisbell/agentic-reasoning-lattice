## What tumbler arithmetic is NOT

We must state explicitly what the tumbler algebra does not guarantee. These negative results constrain what an implementer may assume.

**The algebra is not a group.** There is no additive identity — the zero tumbler is a sentinel, not a neutral element for addition. There is no additive inverse for every element — subtraction is only defined when `a ≥ w`. The algebra is not closed under subtraction in general.

**TA-assoc (AdditionAssociative).** Addition is associative where both compositions are defined: `(a ⊕ b) ⊕ c = a ⊕ (b ⊕ c)` whenever both sides are well-defined. Three cases exhaust the possibilities. Let `k_b` and `k_c` be the action points of `b` and `c` respectively. When `k_b < k_c`: both sides produce `aᵢ` for `i < k_b`, `aₖ_b + bₖ_b` at `k_b`, `bᵢ` for `k_b < i < k_c`, `bₖ_c + cₖ_c` at `k_c`, and `cᵢ` beyond — identical. When `k_b = k_c = k`: both sides produce `aₖ + bₖ + cₖ` at `k` (by NAT-addassoc, which states `(m + n) + p = m + (n + p)` on ℕ and so identifies the two parenthesizations `(aₖ + bₖ) + cₖ` and `aₖ + (bₖ + cₖ)`) and `cᵢ` beyond — identical. When `k_b > k_c`: both sides produce `aₖ_c + cₖ_c` at `k_c` and `cᵢ` beyond — identical (the deeper displacement `b` is overwritten by the shallower `c` in both cases). The domain conditions are asymmetric — the left side requires `k_b ≤ #a`, while the right requires only `min(k_b, k_c) ≤ #a` — but on the intersection, the values agree.

The design does not depend on associativity. Shifts are applied as single operations in practice, never composed from multiple smaller shifts. An implementation with finite representations may break associativity through overflow at the action-point component, but the abstract algebra carries no such limitation.

*Proof.* We show that for all `a, b, c ∈ T` with `Pos(b)`, `Pos(c)`, whenever both `(a ⊕ b) ⊕ c` and `a ⊕ (b ⊕ c)` are well-defined, every component of the left side equals the corresponding component of the right side.

Throughout, write `k_b` for the action point of `b` and `k_c` for the action point of `c`. Recall TumblerAdd's constructive definition: for `x ⊕ w` with `w` having action point `k`, the result has `(x ⊕ w)ᵢ = xᵢ` for `i < k` (prefix copy), `(x ⊕ w)ₖ = xₖ + wₖ` (advance), and `(x ⊕ w)ᵢ = wᵢ` for `i > k` (tail copy), with `#(x ⊕ w) = #w` (the result-length identity from TA0).

*Lengths.* By the result-length identity, `#(a ⊕ b) = #b`. Applying it again: `#((a ⊕ b) ⊕ c) = #c`. For the right side, `#(b ⊕ c) = #c`, and `#(a ⊕ (b ⊕ c)) = #(b ⊕ c) = #c`. Both sides have length `#c`.

*Action point of `s = b ⊕ c`.* We must first establish `Pos(s)` so that ActionPoint and TA0 become applicable to `s` (both require `Pos(·)` on their tumbler argument), and only then compute `actionPoint(s)` to expand the right side `a ⊕ s`. By TumblerAdd on `b ⊕ c` (licensed by `Pos(c)`, which the formal contract supplies as a precondition): `sᵢ = bᵢ` for `i < k_c`, `s_{k_c} = b_{k_c} + c_{k_c}`, and `sᵢ = cᵢ` for `i > k_c`. For `i < min(k_b, k_c)`, we have `i < k_b` (so `bᵢ = 0` by definition of action point) and `i < k_c` (so `sᵢ = bᵢ = 0` by the prefix-copy rule). At position `min(k_b, k_c)` three sub-cases arise, exhaustive by NAT-order's trichotomy on ℕ instantiated at `m = k_b, n = k_c` (exactly one of `k_b < k_c`, `k_b = k_c`, `k_b > k_c` holds). If `k_b < k_c`: `s_{k_b} = b_{k_b} > 0`, since `k_b` is the action point of `b` and `k_b < k_c` places it in the prefix-copy region. If `k_b = k_c`: `s_{k_b} = b_{k_b} + c_{k_b}`, and `s_{k_b} > 0` routes per-summand positivity through NAT-addcompat rather than background monotonicity of `+`. NAT-addcompat's left order-compatibility (`n ≥ p ⟹ m + n ≥ m + p`, instantiated at `m = b_{k_b}, n = c_{k_b}, p = 1`), applied to ActionPoint's minimum-nonzero `c_{k_b} ≥ 1` at `w = c`, lifts to the non-strict `b_{k_b} + c_{k_b} ≥ b_{k_b} + 1`; NAT-addcompat's strict successor inequality (`n < n + 1`, instantiated at `n = b_{k_b}`) supplies the strict `b_{k_b} + 1 > b_{k_b}`; NAT-order's transitivity composes the non-strict `b_{k_b} + c_{k_b} ≥ b_{k_b} + 1` with the strict `b_{k_b} + 1 > b_{k_b}` into the strict `b_{k_b} + c_{k_b} > b_{k_b}`, and ActionPoint's minimum-nonzero at `w = b` giving `b_{k_b} ≥ 1` yields `s_{k_b} = b_{k_b} + c_{k_b} > 0` — the same per-summand-positivity-through-`+` pattern TumblerAdd's dominance proof applies at its structurally identical `aₖ + wₖ > wₖ` site. If `k_b > k_c`: `s_{k_c} = b_{k_c} + c_{k_c} = 0 + c_{k_c} = c_{k_c} > 0`, since `k_c < k_b` gives `b_{k_c} = 0`. In every sub-case `s` has a nonzero component at position `min(k_b, k_c)`, and this nonzero component witnesses TA-Pos's defining existential `(E i : 1 ≤ i ≤ #s : sᵢ ≠ 0)` at `i = min(k_b, k_c)` — thereby establishing `Pos(s)`, i.e., `Pos(b ⊕ c)`, the middle link in the precondition chain `Pos(b), Pos(c)` → `Pos(b ⊕ c)` → `actionPoint(b ⊕ c)` that the right-side expansion and the `actionPoint(b ⊕ c) = min(k_b, k_c)` postcondition both consume. With `Pos(s)` now in hand, ActionPoint is applicable to `s`: its definition `actionPoint(s) = min({i : 1 ≤ i ≤ #s ∧ sᵢ ≠ 0})` consumes the zeros `sᵢ = 0` for `i < min(k_b, k_c)` established above together with the nonzero component at `min(k_b, k_c)` to yield `actionPoint(s) = min(k_b, k_c)`.

*Domain conditions.* The left side `(a ⊕ b) ⊕ c` requires two well-defined additions: `a ⊕ b` requires `Pos(b)` and `k_b ≤ #a` (TA0), and `(a ⊕ b) ⊕ c` requires `Pos(c)` and `k_c ≤ #(a ⊕ b) = #b` (TA0 applied to the intermediate result). The right side `a ⊕ (b ⊕ c)` requires `b ⊕ c` with `Pos(c)` and `k_c ≤ #b` (TA0), and `a ⊕ s` with `Pos(s)` and `actionPoint(s) = min(k_b, k_c) ≤ #a` (TA0) — `Pos(s)` discharged by the *Action point of `s`* paragraph above, which is why that paragraph must precede this one. The domains are asymmetric: the left requires `k_b ≤ #a`, the right requires only `min(k_b, k_c) ≤ #a`. But since `k_b ≤ #a` implies `min(k_b, k_c) ≤ #a`, the left-side conditions subsume the right-side conditions. The intersection of both domains is therefore `k_b ≤ #a` and `k_c ≤ #b`. We assume these hold and show the values agree by exhaustive case analysis on the relationship between `k_b` and `k_c` — NAT-order's trichotomy on ℕ, instantiated at `m = k_b, n = k_c`, asserts exactly one of `k_b < k_c`, `k_b = k_c`, `k_b > k_c` holds, making Cases 1–3 below exhaustive.

*Case 1: `k_b < k_c`.* The action point of `s` is `k_b`, with `s_{k_b} = b_{k_b}` (from the prefix-copy region of `b ⊕ c`, since `k_b < k_c`).

Let `r = a ⊕ b`. By TumblerAdd: `rᵢ = aᵢ` for `i < k_b`, `r_{k_b} = a_{k_b} + b_{k_b}`, and `rᵢ = bᵢ` for `i > k_b`.

*Left side* `(r ⊕ c)` with action point `k_c`: for `i < k_b` we have `i < k_c`, so `(r ⊕ c)ᵢ = rᵢ = aᵢ`. At `i = k_b < k_c`: position `k_b` falls in the prefix-copy region of `r ⊕ c`, so `(r ⊕ c)_{k_b} = r_{k_b} = a_{k_b} + b_{k_b}`. For `k_b < i < k_c`: `(r ⊕ c)ᵢ = rᵢ = bᵢ` (prefix-copy from `r`, and `i > k_b` puts `rᵢ` in the tail-copy region of `a ⊕ b`). At `i = k_c`: `(r ⊕ c)_{k_c} = r_{k_c} + c_{k_c} = b_{k_c} + c_{k_c}`, since `k_c > k_b` gives `r_{k_c} = b_{k_c}` by the tail-copy rule of `a ⊕ b`. For `i > k_c`: `(r ⊕ c)ᵢ = cᵢ`.

*Right side* `(a ⊕ s)` with action point `k_b`: for `i < k_b`, `(a ⊕ s)ᵢ = aᵢ`. At `i = k_b`: `(a ⊕ s)_{k_b} = a_{k_b} + s_{k_b} = a_{k_b} + b_{k_b}`. For `i > k_b`: `(a ⊕ s)ᵢ = sᵢ` by the tail-copy rule. Expanding `sᵢ` via TumblerAdd on `b ⊕ c`: for `k_b < i < k_c`, `sᵢ = bᵢ` (prefix-copy, since `i < k_c`); at `i = k_c`, `s_{k_c} = b_{k_c} + c_{k_c}` (advance); for `i > k_c`, `sᵢ = cᵢ` (tail-copy).

Comparing position by position: `aᵢ = aᵢ` for `i < k_b`; `a_{k_b} + b_{k_b} = a_{k_b} + b_{k_b}` at `k_b`; `bᵢ = bᵢ` for `k_b < i < k_c`; `b_{k_c} + c_{k_c} = b_{k_c} + c_{k_c}` at `k_c`; `cᵢ = cᵢ` for `i > k_c`. Every component agrees.

*Case 2: `k_b = k_c = k`.* The action point of `s` is `k`, with `s_k = b_k + c_k`.

Let `r = a ⊕ b`: `rᵢ = aᵢ` for `i < k`, `r_k = a_k + b_k`, `rᵢ = bᵢ` for `i > k`. The left side `(r ⊕ c)` has action point `k`: for `i < k`, `(r ⊕ c)ᵢ = rᵢ = aᵢ`; at `k`, `(r ⊕ c)_k = r_k + c_k = (a_k + b_k) + c_k`; for `i > k`, `(r ⊕ c)ᵢ = cᵢ`. The right side `(a ⊕ s)` has action point `k`: for `i < k`, `(a ⊕ s)ᵢ = aᵢ`; at `k`, `(a ⊕ s)_k = a_k + s_k = a_k + (b_k + c_k)`; for `i > k`, `(a ⊕ s)ᵢ = sᵢ = cᵢ`.

At position `k`, the left gives `(a_k + b_k) + c_k` and the right gives `a_k + (b_k + c_k)`. These are equal by NAT-addassoc (NatAdditionAssociative), which states `(m + n) + p = m + (n + p)` for every `m, n, p ∈ ℕ`: instantiated with `a_k, b_k, c_k`, it gives `(a_k + b_k) + c_k = a_k + (b_k + c_k)`. All other positions agree by direct comparison.

*Case 3: `k_b > k_c`.* The action point of `s` is `k_c`, with `s_{k_c} = b_{k_c} + c_{k_c} = 0 + c_{k_c} = c_{k_c}` (since `k_c < k_b` gives `b_{k_c} = 0`).

Let `r = a ⊕ b`: `rᵢ = aᵢ` for `i < k_b`, `r_{k_b} = a_{k_b} + b_{k_b}`, `rᵢ = bᵢ` for `i > k_b`. The left side `(r ⊕ c)` has action point `k_c`. Since `k_c < k_b`: for `i < k_c` we have `i < k_b`, so `(r ⊕ c)ᵢ = rᵢ = aᵢ` (both prefix-copy rules apply). At `i = k_c < k_b`: `r_{k_c} = a_{k_c}` (position `k_c` falls in the prefix-copy region of `a ⊕ b`), so `(r ⊕ c)_{k_c} = r_{k_c} + c_{k_c} = a_{k_c} + c_{k_c}`. For `i > k_c`: `(r ⊕ c)ᵢ = cᵢ`. The components of `r` at and beyond `k_b` — where `b`'s contribution appears — are entirely overwritten by `c`'s tail, since `k_c < k_b`.

The right side `(a ⊕ s)` has action point `k_c`: for `i < k_c`, `(a ⊕ s)ᵢ = aᵢ`; at `k_c`, `(a ⊕ s)_{k_c} = a_{k_c} + s_{k_c} = a_{k_c} + c_{k_c}`; for `i > k_c`, `(a ⊕ s)ᵢ = sᵢ = cᵢ` (since `sᵢ = cᵢ` for `i > k_c` by the tail-copy rule of `b ⊕ c`).

Comparing: `aᵢ = aᵢ` for `i < k_c`; `a_{k_c} + c_{k_c} = a_{k_c} + c_{k_c}` at `k_c`; `cᵢ = cᵢ` for `i > k_c`. Every component agrees. The displacement `b` is entirely overwritten — TumblerAdd's tail-replacement semantics means the shallower displacement `c` discards everything below its action point on both sides, rendering `b`'s deeper contribution invisible in the final result.

In all three cases, both sides produce the same sequence of length `#c`, so `(a ⊕ b) ⊕ c = a ⊕ (b ⊕ c)` by T3 (CanonicalRepresentation). ∎

*Formal Contract:*
- *Preconditions:* `a ∈ T`, `b ∈ T`, `c ∈ T`, `Pos(b)`, `Pos(c)`, `k_b ≤ #a`, `k_c ≤ #b` (where `k_b = actionPoint(b)`, `k_c = actionPoint(c)`; these left-side conditions subsume the right-side conditions since `k_b ≤ #a` implies `min(k_b, k_c) ≤ #a`)
- *Depends:* T0 (CarrierSetDefinition) — supplies the carrier-set definition of T referenced by the preconditions `a ∈ T`, `b ∈ T`, `c ∈ T`: without T0, `T` is an undefined symbol and the three membership assertions have no meaning. T0 also supplies the length function `#·` on T as finite sequences over ℕ, which the proof consumes at every sequence-length step: the *Lengths* paragraph ("By the result-length identity, `#(a ⊕ b) = #b`. Applying it again: `#((a ⊕ b) ⊕ c) = #c`. For the right side, `#(b ⊕ c) = #c`, and `#(a ⊕ (b ⊕ c)) = #(b ⊕ c) = #c`") composes TA0's result-length postcondition `#(x ⊕ w) = #w` with T0's length function to obtain `#((a ⊕ b) ⊕ c) = #c` and `#(a ⊕ (b ⊕ c)) = #c`; the *Domain conditions* paragraph reads `k_b ≤ #a`, `k_c ≤ #b`, and `k_c ≤ #(a ⊕ b) = #b` as inequalities between ℕ-valued action-point indices and T0-supplied sequence lengths; the *Action point of `s`* paragraph fixes `#s = #c` for the subsequent right-side expansion; and the closing T3 step ("In all three cases, both sides produce the same sequence of length `#c`, so `(a ⊕ b) ⊕ c = a ⊕ (b ⊕ c)` by T3") appeals to the sequence structure T0 axiomatises — T3 converts component-wise agreement at each index into tumbler equality only because T0 characterises tumblers as finite sequences over ℕ individuated by their components. Without this citation the `∈ T` preconditions, every `#·` occurrence, and the sequence-equality step that T3 consumes would appeal silently to background definitions, the same gap TA0's Depends explicitly guards against. TumblerAdd (TumblerAdd) — invoked throughout the proof at every component-by-component computation. The standing convention is set at the opening ("Recall TumblerAdd's constructive definition: for `x ⊕ w` with `w` having action point `k`, the result has `(x ⊕ w)ᵢ = xᵢ` for `i < k` (prefix copy), `(x ⊕ w)ₖ = xₖ + wₖ` (advance), and `(x ⊕ w)ᵢ = wᵢ` for `i > k` (tail copy)"). The action-point analysis of `s = b ⊕ c` ("By TumblerAdd on `b ⊕ c`: `sᵢ = bᵢ` for `i < k_c`, `s_{k_c} = b_{k_c} + c_{k_c}`, and `sᵢ = cᵢ` for `i > k_c`") and the Case 1 expansions of both sides ("Let `r = a ⊕ b`. By TumblerAdd: `rᵢ = aᵢ` for `i < k_b`, `r_{k_b} = a_{k_b} + b_{k_b}`, and `rᵢ = bᵢ` for `i > k_b`" and the parallel right-side expansion "Expanding `sᵢ` via TumblerAdd on `b ⊕ c`"), together with the analogous `Let r = a ⊕ b` expansions opening Case 2 and Case 3, all invoke the piecewise prefix-copy / advance / tail-copy rule that TumblerAdd defines. TA0 (WellDefinedAddition) — invoked at two points. The result-length identity `#(a ⊕ w) = #w` is TA0's postcondition, used in *Lengths* ("By the result-length identity, `#(a ⊕ b) = #b`. Applying it again: `#((a ⊕ b) ⊕ c) = #c`. For the right side, `#(b ⊕ c) = #c`, and `#(a ⊕ (b ⊕ c)) = #(b ⊕ c) = #c`") and again in *Action point of `s`* to fix `#s = #c` for the subsequent right-side expansion. TA0's preconditions (`Pos(w)` and `actionPoint(w) ≤ #x` for `x ⊕ w`) are invoked in *Domain conditions* to characterise the well-definedness domain of each addition ("`a ⊕ b` requires `Pos(b)` and `k_b ≤ #a` (TA0)", "`(a ⊕ b) ⊕ c` requires `Pos(c)` and `k_c ≤ #(a ⊕ b) = #b` (TA0 applied to the intermediate result)", "`b ⊕ c` with `Pos(c)` and `k_c ≤ #b` (TA0)", and "`a ⊕ s` with `Pos(s)` and `actionPoint(s) = min(k_b, k_c) ≤ #a` (TA0)"), establishing that the intersection of left- and right-side domains is `k_b ≤ #a` and `k_c ≤ #b` — the formal contract's preconditions. The `Pos(s)` precondition of the last invocation — TA0 on `a ⊕ s` — is discharged not by the formal contract but by the *Action point of `s`* paragraph, which exhibits a nonzero component of `s` at position `min(k_b, k_c)` to witness TA-Pos's defining existential, supplying the middle link of the precondition chain `Pos(b), Pos(c)` → `Pos(b ⊕ c)` → `actionPoint(b ⊕ c)` that TA0 on the right side consumes. TA-Pos (PositiveTumbler, this ASN) — supplies the predicate `Pos(·)` used in the preconditions `Pos(b)` and `Pos(c)`; without this citation `Pos` is an undefined symbol in the preconditions, and the action-point existence steps in *Action point of `s`* that invoke ActionPoint on `b` and `c` — yielding `k_b = actionPoint(b)` and `k_c = actionPoint(c)` together with the zeros-below-action-point and minimum-nonzero companion facts — have no licensed source for the positivity hypothesis that ActionPoint requires as input. TA-Pos is additionally consumed in *Action point of `s`* to establish `Pos(s)` (equivalently `Pos(b ⊕ c)`) as an intermediate conclusion: the sub-case analysis exhibits a nonzero component of `s` at position `min(k_b, k_c)`, which witnesses TA-Pos's defining existential `(E i : 1 ≤ i ≤ #s : sᵢ ≠ 0)` at `i = min(k_b, k_c)` and so discharges the positivity predicate. This intermediate `Pos(s)` is then consumed at three downstream sites that would otherwise rest on unstated positivity: as the positivity precondition of ActionPoint's application to `s` (yielding `actionPoint(s) = min(k_b, k_c)` at the close of the same paragraph), as the positivity precondition of TA0's application to `a ⊕ s` in *Domain conditions*, and as the positivity presupposition of the `Pos(b ⊕ c)` and `actionPoint(b ⊕ c) = min(k_b, k_c)` postconditions in the formal contract — supplying the middle link in the precondition chain `Pos(b), Pos(c)` → `Pos(b ⊕ c)` → `actionPoint(b ⊕ c)` that the right-side expansion requires. ActionPoint (ActionPoint) — invoked at three categories of sites with three distinct facts consumed. (i) The action-point function definition `actionPoint(w) = min({i : 1 ≤ i ≤ #w ∧ wᵢ ≠ 0})` supplies the symbol `actionPoint(·)` and licenses the variable-naming step opening the proof ("Throughout, write `k_b` for the action point of `b` and `k_c` for the action point of `c`"), the precondition bindings `k_b = actionPoint(b)` and `k_c = actionPoint(c)` recorded in the formal contract, and — applied to `w = s = b ⊕ c` under the `Pos(s)` precondition discharged in *Action point of `s`* by TA-Pos's defining existential at the exhibited nonzero component — the postcondition `actionPoint(b ⊕ c) = min(k_b, k_c)`, derived in the closing sentence of that paragraph by instantiating the definition at `w = s` and consuming the zeros `sᵢ = 0` for `i < min(k_b, k_c)` together with the nonzero component at `min(k_b, k_c)`. (ii) The zeros-below-action-point postcondition `wᵢ = 0 for all 1 ≤ i < actionPoint(w)` is invoked in *Action point of `s`* at two sites: "For `i < min(k_b, k_c)`, we have `i < k_b` (so `bᵢ = 0` by definition of action point)" instantiates this postcondition at `w = b` with indices `1 ≤ i < k_b`, and Case 3's sub-case "`s_{k_c} = b_{k_c} + c_{k_c} = 0 + c_{k_c} = c_{k_c} > 0`, since `k_c < k_b` gives `b_{k_c} = 0`" instantiates it at `w = b` with index `i = k_c < k_b`. (iii) The minimum-nonzero postcondition `w_{actionPoint(w)} ≥ 1` is invoked at the three sub-cases establishing `actionPoint(s) = min(k_b, k_c)`: the Case `k_b < k_c` sub-case "`s_{k_b} = b_{k_b} > 0`, since `k_b` is the action point of `b`" instantiates it at `w = b`; the Case `k_b = k_c` sub-case "`s_{k_b} = b_{k_b} + c_{k_b} > 0`" instantiates it at `w = c` to obtain `c_{k_b} ≥ 1` (the premise NAT-addcompat's left order-compatibility lifts through `+` into `b_{k_b} + c_{k_b} ≥ b_{k_b} + 1`) and at `w = b` to obtain `b_{k_b} ≥ 1` (the lower bound that, composed via NAT-order's transitivity with the strict `b_{k_b} + c_{k_b} > b_{k_b}` NAT-addcompat derives, yields the sub-case's `s_{k_b} > 0` conclusion); and the Case `k_b > k_c` sub-case "`s_{k_c} = … = c_{k_c} > 0`" instantiates it at `w = c` to obtain `c_{k_c} ≥ 1`. Without this citation the `actionPoint(·)` function symbol and the variables `k_b`, `k_c` would have no licensed source, and the "first nonzero component" and "positive at the action point" inferences would appeal silently to background definitions, violating the per-step citation discipline TumblerAdd itself enforces at structurally identical sites. T3 (CanonicalRepresentation) — invoked at the closing step: "In all three cases, both sides produce the same sequence of length `#c`, so `(a ⊕ b) ⊕ c = a ⊕ (b ⊕ c)` by T3 (CanonicalRepresentation)" — T3 converts the case-by-case component-wise agreement, combined with the common length `#c` established in *Lengths*, into the tumbler equality that is the theorem's headline postcondition. NAT-addassoc (NatAdditionAssociative) — invoked in Case 2 to reconcile the two parenthesizations at the shared action point: the left side produces `(a_k + b_k) + c_k` and the right side produces `a_k + (b_k + c_k)`, and NAT-addassoc's axiom `(m + n) + p = m + (n + p)` on ℕ supplies the equality. Without this citation the Case 2 step would appeal to background arithmetic, contrary to the ASN's convention (T0) that each proof cites only the ℕ facts it actually uses. NAT-addcompat (NatAdditionOrderAndSuccessor) — invoked in the *Action point of `s`* paragraph's Case `k_b = k_c` sub-case to route per-summand positivity through `+` into strict positivity of the sum `s_{k_b} = b_{k_b} + c_{k_b}`. Two NAT-addcompat clauses are consumed in sequence: left order-compatibility (`n ≥ p ⟹ m + n ≥ m + p`, instantiated at `m = b_{k_b}, n = c_{k_b}, p = 1`) lifts ActionPoint's minimum-nonzero `c_{k_b} ≥ 1` at `w = c` into the non-strict `b_{k_b} + c_{k_b} ≥ b_{k_b} + 1`, and the strict successor inequality (`n < n + 1`, instantiated at `n = b_{k_b}`) supplies the strict `b_{k_b} + 1 > b_{k_b}`; NAT-order's transitivity composes the non-strict with the strict into `b_{k_b} + c_{k_b} > b_{k_b}`, and ActionPoint's minimum-nonzero at `w = b` giving `b_{k_b} ≥ 1` completes the derivation to `s_{k_b} > 0`. Without this citation the "both summands are positive action-point values → their sum is positive" step would appeal silently to background monotonicity of `+` on ℕ — the very appeal NAT-addassoc's rationale warns against and TumblerAdd's dominance proof routes explicitly through NAT-addcompat's right order-compatibility + NAT-closure's additive identity at its structurally identical `aₖ + wₖ > wₖ` site, making NAT-addcompat the canonical source for per-summand-positivity-through-`+` steps in this ASN's per-step citation discipline. NAT-addcompat's order-compatibility clauses deliver only non-strict conclusions, so the strict `b_{k_b} + c_{k_b} > b_{k_b}` is routed through NAT-order's composition of `≥` with `>` rather than discharged by NAT-addcompat alone — matching the per-step citation discipline T1, TA1, and TumblerAdd apply at their analogous strict-promotion sites. NAT-closure (NatArithmeticClosureAndIdentity) — invoked at two categories of sites with two distinct clauses consumed. (i) The additive identity clause `0 + n = n` is invoked at two passages forming the `0 + c_{k_c} = c_{k_c}` rewrite: *Action point of `s`* at the sub-case `k_b > k_c` ("`s_{k_c} = b_{k_c} + c_{k_c} = 0 + c_{k_c} = c_{k_c} > 0`, since `k_c < k_b` gives `b_{k_c} = 0`") instantiates it at `n = c_{k_c}` to rewrite `0 + c_{k_c}` into `c_{k_c}` after ActionPoint's zeros-below-action-point fact has reduced `b_{k_c}` to `0`, and Case 3's opening step ("The action point of `s` is `k_c`, with `s_{k_c} = b_{k_c} + c_{k_c} = 0 + c_{k_c} = c_{k_c}`") instantiates it at the same `n = c_{k_c}` to deliver the `s_{k_c} = c_{k_c}` equation the remainder of Case 3 consumes. This is the same additive-identity pattern TumblerAdd's dominance proof applies at `0 + wₖ = wₖ` and OrdinalShift's component-positivity chain applies at `0 + n = n`. (ii) The closure of ℕ under addition clause is invoked wherever the proof forms sums of ℕ-valued components and treats the result as a ℕ-valued component of a tumbler — the action-point expansions `s_{k_c} = b_{k_c} + c_{k_c}`, `r_{k_b} = a_{k_b} + b_{k_b}`, `(r ⊕ c)_{k_c} = b_{k_c} + c_{k_c}`, `(r ⊕ c)_k = (a_k + b_k) + c_k` in Case 2, `(a ⊕ s)_k = a_k + (b_k + c_k)` in Case 2, and `(r ⊕ c)_{k_c} = a_{k_c} + c_{k_c}` and `(a ⊕ s)_{k_c} = a_{k_c} + c_{k_c}` in Case 3 — each of which must lie in ℕ for the case-by-case component-wise agreement to underwrite the tumbler-membership conclusion T3 consumes at the closing step. NAT-closure's closure of ℕ under `+` discharges exactly this ℕ-membership obligation at every intermediate sum. NAT-closure's successor-closure clause is not consumed here — only the additive identity and addition-closure clauses are — matching the per-clause citation discipline T0's exhaustive NAT-* enumeration establishes. NAT-order (NatStrictTotalOrder) — invoked at two sites to discharge the exhaustiveness of a three-way split on the ordering of action-point indices in ℕ. (i) *Action point of `s`* — the sub-case analysis "At position `min(k_b, k_c)` three sub-cases arise, exhaustive by NAT-order's trichotomy on ℕ instantiated at `m = k_b, n = k_c`" appeals to NAT-order's trichotomy (exactly one of `k_b < k_c`, `k_b = k_c`, `k_b > k_c` holds) for exhaustiveness; the three sub-cases establishing `actionPoint(s) = min(k_b, k_c)` coincide with the three disjuncts of trichotomy, so the sub-case split's completeness is discharged from NAT-order rather than left to background arithmetic. (ii) *Case analysis (Cases 1–3)* — the top-level case split closing *Domain conditions* ("NAT-order's trichotomy on ℕ, instantiated at `m = k_b, n = k_c`, asserts exactly one of `k_b < k_c`, `k_b = k_c`, `k_b > k_c` holds, making Cases 1–3 below exhaustive") invokes the same NAT-order trichotomy instance at `m = k_b, n = k_c`, yielding Cases 1 (`k_b < k_c`), 2 (`k_b = k_c`), and 3 (`k_b > k_c`) as a partition over ℕ. Without this citation the top-level case analysis and the sub-case split in *Action point of `s`* would appeal to background `<`/`=`/`>` on ℕ — the same appeal to unsourced background arithmetic that NAT-addassoc's rationale warns against and that TumblerAdd's dominance proof carefully avoids by routing each dichotomy through NAT-zero + NAT-order.
- *Postconditions:* `(a ⊕ b) ⊕ c = a ⊕ (b ⊕ c)`; `#((a ⊕ b) ⊕ c) = #(a ⊕ (b ⊕ c)) = #c`; `Pos(b ⊕ c)`; `actionPoint(b ⊕ c) = min(k_b, k_c)`

**Addition is not commutative.** We do NOT require `a ⊕ b = b ⊕ a`. The operands play asymmetric roles: the first is a *position*, the second is a *displacement*. Swapping them is semantically meaningless. Gregory's `absadd` confirms: the first argument supplies the prefix, the second the suffix — the reverse call gives a different (and typically wrong) result.

**There is no multiplication or division.** Gregory's analysis of the complete codebase confirms: no `tumblermult`, no `tumblerdiv`, no scaling operation of any kind. The arithmetic repertoire is: add, subtract, increment, compare. Tumblers are *addresses*, not quantities. You don't multiply two file paths or divide an address by three.

**Tumbler differences are not counts.** Nelson is emphatic: "A tumbler-span is not a conventional number, and it does not designate the number of bytes contained. It does not designate a number of anything." The difference between two addresses specifies *boundaries*, not *cardinality*. What lies between those boundaries depends on the actual population of the tree, which is dynamic and unpredictable. Between sibling addresses 3 and 7, document 5 might have arbitrarily many descendants — the span from 3 to 7 encompasses all of them, and their count is unknowable from the addresses alone. The arithmetic is an *addressing calculus*, not a *counting calculus*.

The absence of algebraic structure beyond a monotone shift is not a deficiency. It is the *minimum* that addressing requires. An ordered set with an order-preserving shift and an allocation increment is sufficient. Anything more would be unused machinery and unverified obligation.
