# Cone Review — ASN-0034/TumblerSub (cycle 2)

*2026-04-18 09:57*

### NAT-discrete miscited for `aₖ ≠ 0 ⟹ aₖ > 0` in TumblerSub's precondition consequence
**Foundation**: (none — internal consistency.) NAT-discrete's axiom is `m ≤ n < m + 1 ⟹ n = m`; NAT-zero supplies `0 ≤ n` for every `n ∈ ℕ`; NAT-order's defining clause `m ≤ n ⟺ m < n ∨ m = n` unfolds a `≤` into a disjunction.
**ASN**: TumblerSub's *Depends*, NAT-discrete entry: "the precondition consequence's case (ii) step `aₖ ≠ 0 ⟹ aₖ > 0` … is licensed by NAT-discrete's axiom `m ≤ n < m + 1 ⟹ n = m` instantiated at `m = 0` … so the 'nonzero ⇒ `> 0`' inference is discharged from NAT-discrete rather than left implicit, matching the per-step convention TA-Pos and ActionPoint already follow for this same inference." ActionPoint's third postcondition does the analogous step differently: "0 ≤ w_{actionPoint(w)}; NAT-order's definition m ≤ n ⟺ m < n ∨ m = n at m = 0, n = w_{actionPoint(w)} unfolds to 0 < w_{actionPoint(w)} ∨ 0 = w_{actionPoint(w)}; the construction's w_{actionPoint(w)} ≠ 0 excludes the equality disjunct, leaving 0 < w_{actionPoint(w)}; NAT-discrete's axiom … yields 0 + 1 ≤ w_{actionPoint(w)}."
**Issue**: The "nonzero ⇒ `> 0`" step is discharged by NAT-zero + NAT-order (lower bound + defining-clause unfolding + equality-disjunct exclusion), not by NAT-discrete. ActionPoint uses NAT-discrete for a *different* step — `> 0 ⟹ ≥ 1` — where an interval argument is genuinely needed. TA-Pos is a definition and makes no such inference at all. The claim that TumblerSub's attribution "matches the per-step convention TA-Pos and ActionPoint already follow for this same inference" therefore misidentifies what ActionPoint does and attributes a step to an axiom that doesn't license it (NAT-discrete at `m = 0` yields `n ∈ ℕ ∧ 0 ≤ n < 1 ⟹ n = 0`, whose contrapositive gives `aₖ ≥ 1`, not `aₖ > 0`, and only by composing with NAT-zero and NAT-order).
**What needs resolving**: TumblerSub must route its `aₖ ≠ 0 ⟹ aₖ > 0` step through the same axiom set ActionPoint uses for the analogous step — NAT-zero + NAT-order's defining-clause unfolding + equality-disjunct exclusion — and the Depends narrative must correct both the citation and the "matches ActionPoint" claim, or else explicitly state that TumblerSub invokes NAT-discrete's stronger interval form and routes through `≥ 1 ⟹ > 0` (in which case NAT-zero and NAT-order still need co-citation alongside NAT-discrete at this site, and the claim of parity with ActionPoint's *inference* must be restated).

---

### D0's motivational reading-off chain appeals to right-identity `aₖ + 0 = aₖ`, which is not among T0's stated NAT-* clauses
**Foundation**: T0's exhaustive NAT-* enumeration states NAT-closure's additive identity as `0 + n = n` (left-identity only). TumblerAdd explicitly flags that "T0's exhaustive NAT-* enumeration does not include commutativity of addition on ℕ" and that the symmetric variants of additive identities are therefore unavailable; TumblerAdd's dominance proof deliberately routes through `0 ≤ aₖ` + NAT-addcompat's *right* order-compatibility + NAT-closure's *left*-identity (`0 + wₖ = wₖ`) precisely to avoid needing a right-identity `aₖ + 0 = aₖ`.
**ASN**: D0 motivational passage (paragraph preceding the `wᵢ = 0 for i < k` formulas): "the step `wₖ = bₖ − aₖ` is licensed by NAT-sub, whose conditional-closure clause requires `bₖ ≥ aₖ`, supplied here by `bₖ = aₖ + wₖ ≥ aₖ` via NAT-addcompat with NAT-closure's additive identity". D0's *Depends*, NAT-addcompat entry: "(1) At the reading-off derivation, supplies `aₖ + wₖ ≥ aₖ + 0 = aₖ` so that NAT-sub's conditional-closure precondition is met without assuming the stronger `bₖ > aₖ` known only after the main computation."
**Issue**: The chain `aₖ + wₖ ≥ aₖ + 0 = aₖ` uses NAT-addcompat's left order-compatibility (lifting `wₖ ≥ 0` to `aₖ + wₖ ≥ aₖ + 0`) followed by `aₖ + 0 = aₖ`, which is a right-identity. NAT-closure's axiom clause is `0 + n = n` (left-identity); `n + 0 = n` is not stated and, absent commutativity, not derivable from the other NAT-* axioms in T0's enumeration — precisely the asymmetry TumblerAdd calls out when it routes via right-compat + left-identity to avoid needing `1 + wₖ > wₖ` or `aₖ + 0 = aₖ`. The symmetric route (`aₖ ≥ 0` + right-compat + left-identity) gives `aₖ + wₖ ≥ 0 + wₖ = wₖ`, not the required `≥ aₖ`. So the motivational chain consumes a clause the axiom set does not supply, on the same operator orientation TumblerAdd explicitly flags as unavailable.
**What needs resolving**: D0's reading-off narrative and its NAT-addcompat/NAT-closure Depends entries must either (a) establish `bₖ ≥ aₖ` by a different route that uses only NAT-closure's stated left-identity (e.g., by strengthening the motivational step to require `bₖ > aₖ` up front, matching the formal proof's use of T1 case (i)), or (b) introduce an additional axiom supplying right-identity `n + 0 = n` and add it to T0's NAT-* enumeration with a corresponding citation. If the motivational passage is purely expository and not load-bearing for the formal proof, the Depends narrative must be adjusted so it does not claim to discharge a chain that the axiom set cannot support.
