# Cone Review — ASN-0034/TA7a (cycle 3)

*2026-04-18 18:05*

### Missing NAT-addcompat citation for `rₖ = oₖ + wₖ > 0` in TA7a Conjunct 1
**Foundation**: NAT-addcompat (NatAdditionOrderAndSuccessor) — the sole source of left/right order-compatibility of addition on ℕ, which every other derivation of strict-through-addition in this ASN cites explicitly. TumblerAdd's own strict-advancement chain `rₖ = aₖ + wₖ ≥ aₖ + 1 > aₖ` enumerates NAT-addcompat (both left order-compatibility and strict successor) as its axiom source.
**ASN**: TA7a Conjunct 1 S-membership derivation — "By TumblerAdd's constructive definition, the result `r = o ⊕ w` has components: `rᵢ = oᵢ > 0` for `1 ≤ i < k` (prefix copied from `o ∈ S`); `rₖ = oₖ + wₖ > 0` (since `oₖ > 0` because `o ∈ S`, and `wₖ > 0` because `k` is the action point of `w`)…". TA7a's Depends list enumerates T0, T4, T1, TA-Pos, ActionPoint, TumblerAdd, TumblerSub, NAT-order, TA0, TA2, TA6, NAT-sub, NAT-closure, NAT-zero — NAT-addcompat is not listed.
**Issue**: The step `rₖ = oₖ + wₖ > 0` is a fresh component-level strict inequality derived inside TA7a from TumblerAdd's *constructive definition* (not from any TumblerAdd postcondition, none of which assert `rₖ > 0` at the component level). Every route to this conclusion consumes NAT-addcompat: via left order-compatibility (`wₖ ≥ 1 ⟹ oₖ + wₖ ≥ oₖ + 1`) composed with strict successor (`oₖ < oₖ + 1`) and NAT-order to reach `oₖ + wₖ > oₖ > 0`; or via right order-compatibility (`oₖ ≥ 0 ⟹ oₖ + wₖ ≥ 0 + wₖ`) composed with NAT-closure's left identity and `wₖ > 0` to reach `oₖ + wₖ > 0`. Both routes require NAT-addcompat's order-compatibility. The ASN's per-step NAT-* citation discipline (explicitly enforced at TumblerAdd, TumblerSub, TA2, ActionPoint, and the precondition-consequence of TumblerSub for structurally identical strict-through-addition sites) therefore requires NAT-addcompat in TA7a's Depends list.
**What needs resolving**: Either add NAT-addcompat to TA7a's Depends with a citation block enumerating the `oₖ + wₖ > 0` step (matching the enumeration style TumblerAdd already applies), or reformulate TA7a's S-membership argument to consume only the pre-existing TumblerAdd/TA0 postconditions rather than deriving strict positivity afresh from the construction.
