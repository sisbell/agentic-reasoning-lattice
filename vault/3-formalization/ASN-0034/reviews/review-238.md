# Cone Review — ASN-0034/T10a.8 (cycle 3)

*2026-04-18 05:02*

### T10a.8 Depends: summary listing diverges from standalone's resolved NAT-* cites
**Foundation**: Previous Findings 1 (cycle 1) and 4 (cycle 2) required T10a.8's Depends to cite NAT-zero, NAT-discrete, and NAT-addcompat (alongside T0) at the "non-zero ⇒ strictly positive" and "+1 preserves positivity" steps. The standalone `**T10a.8 (UniformSiblingZeroCount)**` section's Formal Contract now carries all of these: its Depends lists "T0 (CarrierSetDefinition) ... NAT-zero (NatZeroMinimum) ... NAT-discrete (NatDiscreteness) ... NAT-closure (NatArithmeticClosureAndIdentity) ... NAT-addcompat (NatAdditionOrderAndSuccessor) ..." alongside T10a, T10a.4, T4, TA5, TA5-SigValid.
**ASN**: The summary listing for T10a.8 inside T10a's own Formal Contract (the Postconditions block of T10a, enumerating T10a.1–T10a.8 and T10a-N): "T10a.8 (Uniform sibling zero count): ... Depends: T10a (sibling production restricted to `inc(·, 0)`), T10a.4 (T4-validity at every output, supplying the preconditions of TA5-SigValid and T4's field-segment constraint at each step), T4 (HierarchicalParsing — `t_{#t} ≠ 0` forces the sig-position strictly positive), TA5 (TA5(b) preserves non-sig positions; TA5(c) advances sig by `+1` and preserves length), TA5-SigValid (`sig(t) = #t` for T4-valid `t`, identifying the advanced position as the terminal component)." The inline proof sketch in this same summary block likewise handwaves: "T4's field-segment constraint (with T0's ℕ carrier) forces that position strictly positive ... so the `+1` step keeps it positive."
**Issue**: Two listings of T10a.8's Depends now disagree. The summary inside T10a's Formal Contract lists 5 dependencies (T10a, T10a.4, T4, TA5, TA5-SigValid) and preserves exactly the handwaved "(T0's carrier ℕ)" / "keeps it positive" phrasing that cycles 1 and 2 required to be replaced with per-step NAT-* citations; the standalone T10a.8 statement lists those 5 plus T0, NAT-zero, NAT-discrete, NAT-closure, NAT-addcompat. A reader consulting the T10a summary sees a different contract for T10a.8 than the one the standalone advertises, and a dependency extractor consuming the summary would miss every NAT-* edge that the resolved standalone carries. This is the same pattern cycle 1's Finding 3 identified for T10a.4 (summary omitting T10a while standalone cites it), now manifesting for T10a.8's NAT-* cites.
**What needs resolving**: The T10a.8 entry inside T10a's Formal Contract Postconditions block — including both its proof-sketch prose and its Depends clause — must match the standalone T10a.8 Formal Contract: it must cite T0, NAT-zero, NAT-discrete, NAT-closure, and NAT-addcompat with the same per-step attribution the standalone section uses, so the two Depends listings for this single property are identical.
