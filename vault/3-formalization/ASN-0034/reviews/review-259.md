# Cone Review — ASN-0034/TS1 (cycle 8)

*2026-04-18 07:55*

### ActionPoint not listed in OrdinalShift's or TS1's Depends despite direct consumption of the `actionPoint(·)` operator
**Foundation**: Per-step citation discipline, as articulated in TA1-strict's own Depends: "The per-step citation discipline … requires T0 to be listed here rather than left to flow transitively through Divergence, T1, ActionPoint, TumblerAdd, or TA0, so that a reviser tightening T0 … has Depends-backed visibility into TA1-strict's consumption sites." TA1-strict's Depends accordingly lists ActionPoint directly for its consumption of `actionPoint(w)` at the TA1-strict length-bound discharge steps.
**ASN**: OrdinalShift's proof discharges TA0 precondition four: "the action point of δ(n, m) is m = #v by OrdinalDisplacement, so actionPoint(δ(n, m)) ≤ #v holds trivially." OrdinalShift's *Depends* lists OrdinalDisplacement, T0, TA-Pos, TA0, TumblerAdd, NAT-zero, NAT-addcompat, NAT-closure, NAT-order — no ActionPoint.

TS1's precondition checks (vi), (vii), (viii) each directly apply the `actionPoint(·)` operator: "actionPoint(δ(n, m)) ≤ #v₁ — the action point of δ(n, m) is m (OrdinalDisplacement)"; "actionPoint(δ(n, m)) ≤ #v₂"; "actionPoint(δ(n, m)) ≥ divergence(v₁, v₂)". TS1's *Depends* lists OrdinalShift, OrdinalDisplacement, Divergence, T3, TA1-strict, T0, T1, TA-Pos, NAT-order, NAT-wellorder — no ActionPoint.
**Issue**: Both properties consume the `actionPoint(·)` operator at named steps. Under the per-step discipline TA1-strict documents — which treats "transitively reachable through another cited lemma" as insufficient and requires direct citation at each consumption step — ActionPoint should appear in both Depends lists. The fact `actionPoint(δ(n, m)) = m` is sourced to OrdinalDisplacement, but the function `actionPoint(·)` itself is defined by ActionPoint, and its meaning is load-bearing on each of OrdinalShift's TA0-precondition-four step and TS1's three precondition-check steps. A reviser tightening ActionPoint's defining minimum-position formula would have Depends-backed visibility into TA1-strict's consumption sites but none into OrdinalShift's or TS1's, despite structurally identical usage.
**What needs resolving**: Either ActionPoint must be added to OrdinalShift's and TS1's Depends with an enumeration of the `actionPoint(·)` consumption sites (matching TA1-strict's treatment), or the per-step discipline must be narrowed in a way that explains why `actionPoint(·)`'s transitive reachability through OrdinalDisplacement is exempt while T0/T1/TA-Pos/NAT-order/NAT-wellorder's transitive reachability through the same lemma is not.

### OrdinalShift's proof uses `δₙ` before OrdinalDisplacement introduces the abbreviation
**Foundation**: Every symbol must be defined before it is used; the Lamport precision standard is that "every symbol has exactly one meaning, bound where used" (the same standard previous finding #9 applied to TS1's free `m`).
**ASN**: Document order: TA1-strict, then OrdinalShift (with its proof), then the header "### Ordinal displacement and shift", then OrdinalDisplacement. OrdinalDisplacement's definition section ends with: "When the depth is determined by context (typically m = #v for the tumbler being shifted), we write δₙ." OrdinalShift's proof — which appears *above* OrdinalDisplacement — contains: "Additionally, shift preserves structural properties. When m ≥ 2, the action point of δₙ leaves position 1 unchanged — shift(v, n)₁ = v₁."
**Issue**: `δₙ` has no definition at the point OrdinalShift's proof uses it. The only introduction of the abbreviation appears later in OrdinalDisplacement's text. A reader encountering OrdinalShift's structural-properties paragraph in document order cannot resolve the symbol against any in-scope definition. Elsewhere OrdinalShift's proof consistently uses the full `δ(n, m)` notation; this single `δₙ` usage breaks the one-meaning-per-symbol standard the ASN otherwise enforces.
**What needs resolving**: Either OrdinalShift's structural-properties paragraph must use `δ(n, m)` (consistent with the rest of its proof), or the `δₙ` abbreviation must be introduced before its first use (e.g., in OrdinalShift's Definition, or by reordering OrdinalDisplacement to precede OrdinalShift so the abbreviation is in scope).

### OrdinalShift re-derives OrdinalDisplacement's exported postconditions instead of citing them, silently relying on a promotion step it does not discharge
**Foundation**: OrdinalDisplacement's *Formal Contract* — *Postconditions:* `δ(n, m) ∈ T (by T0), Pos(δ(n, m)) (by TA-Pos), actionPoint(δ(n, m)) = m (by ActionPoint)`. The contract exports these as named postconditions precisely so callers can cite them directly, as TS1 does at (iii)/(v) ("by OrdinalDisplacement's postcondition `δ(n, m) ∈ T`"; "by OrdinalDisplacement's postcondition `Pos(δ(n, m))`"). OrdinalDisplacement's own proof discharges the promotion `n ≥ 1 ⟹ n ≠ 0` via NAT-addcompat (anchor `0 < 1`) and NAT-order (defining clause, transitivity, irreflexivity), and then uses it at TA-Pos's witness `δₘ = n ≠ 0`.
**ASN**: OrdinalShift's TA0-precondition checks bypass the exported postconditions: "Second, by OrdinalDisplacement, δ(n, m) = [0, …, 0, n] is a finite sequence of length m ≥ 1 over ℕ, so by T0, δ(n, m) ∈ T. Third, since n ≥ 1 the m-th component of δ(n, m) is nonzero, whence by TA-Pos Pos(δ(n, m))."

The third-precondition step packages two distinct inferences: (a) δ(n, m)ₘ = n from OrdinalDisplacement's definition, and (b) `n ≥ 1 ⟹ n ≠ 0` (the component-nonzero promotion). Step (b) is left implicit — OrdinalShift's proof does not discharge it, and OrdinalShift's *Depends* entries for NAT-addcompat and NAT-order cover only the *component-positivity lower bound* (`vₘ + n ≥ 1`) chain, not the `n ≥ 1 ⟹ n ≠ 0` promotion.
**Issue**: The re-derivation path creates two problems. First, the step "m-th component is nonzero, whence by TA-Pos Pos(δ)" consumes the `n ≥ 1 ⟹ n ≠ 0` promotion at a point where it is not cited — OrdinalShift's *Depends* names no site where this promotion is discharged. Second, when TS1 downstream cites "OrdinalDisplacement's postcondition `Pos(δ(n, m))`" (per its Depends entry at (v)) and OrdinalShift directly derives Pos from TA-Pos without going through that postcondition, the two callers route the same content through different paths — a reviser tightening OrdinalDisplacement's exported `Pos(δ(n, m))` postcondition cannot tell whether OrdinalShift respects or bypasses that export. The same asymmetry applies to `δ(n, m) ∈ T`: OrdinalShift re-derives through T0 from the sequence form; TS1 cites the exported postcondition.
**What needs resolving**: OrdinalShift's TA0-precondition checks must either (a) cite OrdinalDisplacement's exported postconditions `δ(n, m) ∈ T` and `Pos(δ(n, m))` directly (matching TS1's routing and respecting the contract's export structure), or (b) enumerate the `n ≥ 1 ⟹ n ≠ 0` promotion chain explicitly in the proof and in *Depends* (so the re-derivation is fully discharged). The current state — re-derivation in prose with an undischarged promotion step — leaves the TA0 precondition three discharge incomplete under the per-step citation discipline the ASN enforces.

## Result

Cone not converged after 8 cycles.

*Elapsed: 5452s*
