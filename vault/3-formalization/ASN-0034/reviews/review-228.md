# Cone Review — ASN-0034/TA1-strict (cycle 8)

*2026-04-18 03:08*

### ActionPoint's NAT-closure citation references a TA-Pos remark that doesn't exist
**Foundation**: TA-Pos's current Depends list contains a single entry, T0, with prose describing the carrier/length/projection role. Nothing in the TA-Pos definition or its Depends discusses NAT-* axioms, transitive sourcing handles, or ActionPoint as an indirect citation route. The TA-Pos prose explicitly notes that "the T1 ordering consequence … and the ActionPoint consumption it rests on, are carried by a separate downstream ordering theorem (TA-PosDom) with its own Depends list" — i.e., TA-Pos defers downstream behavior, it doesn't retain handles into ActionPoint.
**ASN**: ActionPoint's NAT-closure Depends entry closes with: "preserving this per-step citation discipline for NAT-* steps gives a reviser tightening NAT-closure's additive identity Depends-backed visibility into ActionPoint's use of it, **and closes TA-Pos's 'retained as a transitive sourcing handle through ActionPoint' remark so that the transitive handle now points at a site where the axiom is named**."
**Issue**: The remark TA-Pos is alleged to make — that NAT-closure (or any NAT-* axiom) is "retained as a transitive sourcing handle through ActionPoint" — does not appear in TA-Pos's current text. TA-Pos's Depends lists only T0 and contains no NAT-* citation, transitive-handle phrasing, or pointer to ActionPoint as a deferred citation site. ActionPoint's NAT-closure entry therefore advertises that it discharges an obligation TA-Pos does not in fact carry. A reviser following the citation back to TA-Pos to verify what handle is being closed will find no such handle present.
**What needs resolving**: Either remove the "closes TA-Pos's 'retained as a transitive sourcing handle through ActionPoint' remark" clause from ActionPoint's NAT-closure entry (since there is no such remark in TA-Pos to close), or restore the referenced remark to TA-Pos so that the citation chain — TA-Pos retaining a transitive handle, ActionPoint closing it — exists where the citations claim it does. Whichever route is taken, the remark and its closure must be co-located in the actual text.

### Divergence's symmetry postcondition's case-(i) sub-argument elides the bound symmetry
**Foundation**: Divergence case (i)'s qualifying set is `{i ∈ ℕ : 1 ≤ i ∧ i ≤ #a ∧ i ≤ #b ∧ aᵢ ≠ bᵢ}`, with `k` the NAT-wellorder least element. The membership predicate has four conjuncts; the bound conjuncts `i ≤ #a ∧ i ≤ #b` are independently consumed at downstream sites (TA1-strict's case-(ii) rule-out reads off `j ≤ #a` and `j ≤ #b` separately as licensing premises for two distinct instantiations).
**ASN**: Divergence's symmetry argument for case (i): "In case (i), the condition `aₖ ≠ bₖ` with agreement on all prior components is unchanged by swapping `a` and `b`, so the least such `k` is the same."
**Issue**: The argument names only the disagreement and prior-agreement conjuncts as swap-invariant; it does not address the bound conjuncts `k ≤ #a ∧ k ≤ #b`. Under swap, the qualifying set's predicate becomes `i ≤ #b ∧ i ≤ #a ∧ bᵢ ≠ aᵢ`, which is logically equivalent to the original by `∧`-commutativity and `≠`-symmetry but only because each conjunct individually permutes — a step the prose elides at the bound conjuncts while spelling out only the disagreement and agreement conjuncts. Compared to the elaborated per-step discipline applied at downstream consumption sites of these same bounds (TA1-strict separately discharges `k ≤ #a` and `k ≤ #b` against `j = #a + 1` and `j = #b + 1`), the symmetry argument's coverage of the swap-invariance at the bound conjuncts is left implicit.
**What needs resolving**: Either expand the case-(i) symmetry sub-argument to enumerate the swap-invariance of the bound conjuncts (`k ≤ #a ∧ k ≤ #b` is preserved under swap because `∧` is commutative, exposed at the same per-step granularity as the disagreement and agreement conjuncts), or document a convention that swap-invariance of `∧`-conjuncts is taken as background and need not be itemized — applying that convention uniformly so downstream consumers reading the symmetry argument's per-step style do not infer that only the named conjuncts are checked.
