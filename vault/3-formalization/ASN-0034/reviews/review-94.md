# Cone Review — ASN-0034/TumblerAdd (cycle 7)

*2026-04-16 20:09*

### T0 does not enumerate well-ordering of ℕ, which ActionPoint's `min` in its definition requires
**Foundation**: `T0 (CarrierSetDefinition)` — enumerates ℕ closure under successor/addition, additive identity, strict-total-order properties of `<` (irreflexivity, trichotomy, transitivity), discreteness, order-compatibility of `+`, and strict successor inequality.
**ASN**: `ActionPoint`, Definition: "actionPoint(w) = min({i : 1 ≤ i ≤ #w ∧ wᵢ ≠ 0})"; body: "Since Pos(w), PositiveTumbler guarantees at least one component of w is nonzero, so the set {i : 1 ≤ i ≤ #w ∧ wᵢ ≠ 0} is nonempty and the minimum exists." ActionPoint's `Depends` T0 entry cites T0 for the carrier set, length, component projection, and the discreteness-licensed "nonzero ⇒ `≥ 1`" step.
**Issue**: The definition asserts the existence of `min(S)` for a nonempty `S ⊆ {1, ..., #w} ⊂ ℕ`. The existence of least elements in nonempty subsets of ℕ is *well-ordering* (or equivalently, induction), which is not among the ℕ properties enumerated in T0's `Axiom` field. The cycle-6 extension of T0 added strict-total-order properties, discreteness, and additive facts, but well-ordering was not included — yet it is the property that makes `min` well-defined. A formalizer translating ActionPoint's definition into TLA+ or a proof assistant cannot discharge the "min exists" step from T0 as currently axiomatized: strict total order plus discreteness does not entail well-ordering (e.g., ℤ satisfies all T0 ℕ-properties as written except closure under predecessor, yet ℤ is not well-ordered under `<`). The downstream propagation appears in `TumblerAdd`, whose preconditions invoke `actionPoint(w)`, and in the `PositiveTumbler` proof's "there exists a smallest index `k`" step that invokes the same principle.
**What needs resolving**: Either (i) extend T0's `Axiom` field to include well-ordering of ℕ (or an equivalent principle such as strong induction over ℕ) and cite it in ActionPoint's `Depends` T0 entry at the "`min` is well-defined" site; or (ii) rephrase ActionPoint's definition so that existence is established constructively from a principle already in T0 — and, in either case, carry the citation through to the `PositiveTumbler` proof's "smallest index" step, which invokes the same principle.

### `PositiveTumbler`'s postcondition proof invokes the discreteness contrapositive `m < n ⟹ m + 1 ≤ n` without citation
**Foundation**: `T0 (CarrierSetDefinition)` — discreteness states `m ≤ n < m + 1 ⟹ n = m`; its contrapositive combined with trichotomy yields `m < n ⟹ m + 1 ≤ n`.
**ASN**: `PositiveTumbler`, postcondition proof, case `#z < k`: "From `#z < k ≤ #t` we obtain `#z + 1 ≤ #t`, so by T1 case (ii) with witness `#z + 1`, `z < t`." `PositiveTumbler`'s `Depends` T0 entry calls out discreteness only for the "nonzero ⇒ `≥ 1`" step: "the postcondition proof's step `zₖ = 0 < tₖ` because `tₖ ≥ 1` as a nonzero natural number is licensed by T0's discreteness axiom".
**Issue**: The inference `#z < k ⟹ #z + 1 ≤ k` is an independent use of T0's discreteness (via its contrapositive and trichotomy) — a distinct step from the "nonzero ⇒ `≥ 1`" inference the `Depends` entry documents. Without that step, the proof cannot produce the `#z + 1 ≤ #t` needed as T1 case (ii)'s witness. The `Depends` T0 entry names only the "nonzero ⇒ `≥ 1`" use, so a formalizer building the proof-obligation set has no source in scope for the `m < n ⟹ m + 1 ≤ n` step. The pattern matches the cycle-5/6 convention of naming each specific discreteness or additive inference that downstream proofs rely on.
**What needs resolving**: `PositiveTumbler`'s `Depends` T0 entry should be widened to record the additional discreteness-derived inference `m < n ⟹ m + 1 ≤ n` at the case `#z < k` proof site (or T0 should expose that inference as a named consequence of discreteness that downstream proofs cite directly), matching the per-step citation convention now used elsewhere in the ASN.
