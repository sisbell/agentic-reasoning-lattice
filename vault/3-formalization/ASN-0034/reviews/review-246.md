# Cone Review — ASN-0034/T12 (cycle 3)

*2026-04-18 05:57*

### Span Depends cites "T1(d)" but T1 has no part (d)
**Foundation**: T1 (LexicographicOrder). T1's structure: proof parts (a) Irreflexivity, (b) Trichotomy, (c) Transitivity, followed by an unlabeled paragraph introducing `≤` and `≥`; Formal Contract sections are Definition, Abbreviations, Depends, Postconditions (a)/(b)/(c). No item is labelled (d).
**ASN**: Definition (Span), Depends entry for T1: "T1 (LexicographicOrder) — supplies the strict total order `<` used in `t < s ⊕ ℓ` and, **via T1(d)'s derived non-strict companion** `a ≤ b ⟺ a < b ∨ a = b`, the relation `s ≤ t` that fixes the span's lower bound".
**Issue**: The label "T1(d)" does not exist in T1. The non-strict companion `a ≤ b ⟺ a < b ∨ a = b` lives in T1's *Abbreviations* block (Formal Contract), not in a postcondition (a)/(b)/(c) or a labeled proof part. T12's parallel use of the same fact cites it descriptively ("T1's definition of `≤`"), so the labelled citation in Span's Depends is uniquely incorrect within this ASN slice. A reviser searching T1 for "(d)" finds nothing — the citation is a dangling label.
**What needs resolving**: Replace "T1(d)" with the actual citation handle for the `≤`/`≥` companions (e.g., "T1's Abbreviations block", or whatever stable label the ASN uses for that block). If the abbreviations are intended to become a labelled postcondition (d), promote them in T1's Formal Contract and ensure all consumers (Span here, T12's transitivity-of-`≤` construction) use the new label uniformly.

### TA-strict's Formal Contract precondition introduces `k` not present in its statement
**Foundation**: TA-strict (StrictIncrease) — should present preconditions in a single, consistent vocabulary so callers can match by syntactic substitution.
**ASN**: TA-strict statement: `(A a, w ∈ T : Pos(w) ∧ actionPoint(w) ≤ #a : a ⊕ w > a)`. TA-strict Formal Contract Preconditions: "`a ∈ T`, `w ∈ T`, `Pos(w)`, `k ≤ #a` **where `k` is the action point of `w`**". Compare TA0's parallel contract, which uses `actionPoint(w) ≤ #a` in both statement and preconditions list with no introduced symbol.
**Issue**: TA-strict's contract introduces a free variable `k` in the precondition list that does not appear in the quantifier statement and that has no binder in the contract. The equivalence with `actionPoint(w) ≤ #a` is asserted by the side-clause "where `k` is the action point of `w`", but consumers (e.g., T12's proof writes "since `Pos(ℓ)` and `k ≤ #s`, TA-strict gives `s ⊕ ℓ > s`") then have to silently identify their local `k` with TA-strict's introduced `k`. The form mismatch between statement (`actionPoint(w)`) and contract (`k`) is the kind of one-symbol-two-presentations slip that breaks mechanical instantiation; TA0 demonstrates the uniform alternative one definition above.
**What needs resolving**: Either rewrite TA-strict's Formal Contract Preconditions in the same `actionPoint(w) ≤ #a` form used in the statement (and used by TA0), or, if `k` is wanted as a local abbreviation, bind it explicitly (e.g., "let `k = actionPoint(w)`") before the precondition that uses it, and apply the same convention consistently across TA0, TA-strict, and T12.
