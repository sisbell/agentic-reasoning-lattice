# Cone Review — ASN-0034/TA1-strict (cycle 5)

*2026-04-18 02:35*

### TA1-strict's opening asserts T1 case (i) at the specific index `j` without ruling out T1 case (ii) or witnesses `k' ≠ j`
**Foundation**: T1 (LexicographicOrder) defines `a < b` as `∃ k ≥ 1` with (i) `k ≤ #a ∧ k ≤ #b ∧ aₖ < bₖ` or (ii) `k = #a + 1 ≤ #b`, where case (i)'s `k` is only existentially witnessed — not required to be the minimum divergence index. Divergence case (i) at `j` asserts `aⱼ ≠ bⱼ` with agreement on `1 ≤ i < j` and `j` minimal by NAT-wellorder, but does not select a direction.
**ASN**: TA1-strict's opening, immediately after ruling out Divergence case (ii): "Since `a < b`, the T1 case (i) direction gives `aⱼ < bⱼ`." The T1 entry of the Depends list labels this "Opening (directed case-(i) at the divergence point): once in the opening to derive `aⱼ < bⱼ` from `a < b` at the divergence point".
**Issue**: Three sub-steps are collapsed into a one-line assertion that the ASN's per-step discipline elsewhere enumerates explicitly. (1) T1 case (ii) for `a < b` — `a` a proper prefix of `b` — must be ruled out from Divergence case (i)'s shared-position disagreement at `j ≤ #a ∧ j ≤ #b`, which is incompatible with `a` being exhausted. (2) For any T1 case (i) witness `k'` of `a < b`, the cases `k' < j`, `k' = j`, `k' > j` must be resolved: `k' < j` contradicts Divergence's agreement on `i < j` at position `k'` (which would force `aₖ' = bₖ'` against T1 case (i)'s strict inequality via NAT-order irreflexivity); `k' > j` contradicts Divergence case (i)'s `aⱼ ≠ bⱼ` against T1 case (i)'s agreement requirement at `i < k'`. Only `k' = j` survives, at which T1 case (i) delivers `aⱼ < bⱼ`. (3) NAT-order and T1's Depends entries do not enumerate the irreflexivity use at the `k' < j` sub-case or the index-pair trichotomy/equality reasoning at the `k' vs j` split.
**What needs resolving**: Expand the opening to enumerate the elimination of T1 case (ii) (from shared-position disagreement) and the forcing of the T1 case (i) witness to coincide with Divergence's `j` (via minimality + the case split on `k' vs j`), then cite the licensing NAT-order irreflexivity/trichotomy and T1-case-(i) agreement-condition uses in the Depends list — or document a convention waiving per-step discharge of "T1 witness equals Divergence index" at this caller and apply it uniformly wherever the pattern recurs.
