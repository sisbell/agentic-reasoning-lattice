# Cone Review — ASN-0034/T10a.5 (cycle 6)

*2026-04-18 11:31*

Cross-cutting review follows. I verified that findings #1-7 in the prior-cycle list have been addressed in the current text; only new issues below.

### T10a.2 Formal Contract Depends list omits T3 while the per-step convention requires it
**Foundation**: T3 (CanonicalRepresentation) — the claim "equal-length tumblers are prefix-related only if identical" is not a consequence of the Prefix definition alone. The Prefix definition gives `a ≼ b ⟹ (∀i : 1 ≤ i ≤ #a : bᵢ = aᵢ) ∧ #a ≤ #b`; combining this with `#a = #b` yields componentwise agreement at every position plus equal length, but converting that pairing into the identity `a = b` requires T3's extensional characterization of tumbler equality. T10a.5's Formal Contract Depends already cites "T3 (divergence of distinct tumblers)" in parallel with Prefix, establishing the ASN's convention of listing T3 directly at every step that converts componentwise agreement into sequence equality (or its contrapositive).
**ASN**: T10a body Formal Contract, Consequence 2 entry: "T10a.2 (Non-nesting sibling prefixes): For all siblings a, b from the same allocator, same_allocator(a, b) ∧ a ≠ b → a and b are prefix-incomparable. Depends: T10a (AllocatorDiscipline — supplies the `same_allocator(·, ·)` predicate…), T10a.1 (#a = #b), Prefix (equal-length tumblers are prefix-related only if identical)."
**Issue**: The Depends list cites Prefix for the content "equal-length tumblers are prefix-related only if identical" but omits T3, even though reaching that conclusion consumes T3's extensional equality at the final step. The standalone Prefix definition does expose a derived "proper-prefix length" postcondition whose own Depends cites T3, but the per-step citation convention the ASN follows elsewhere (T10a.5 Formal Contract, T10a.5 body Case 1, and the previous cycle's Finding #5 on NAT-closure) is to name T3 directly in the consuming property's Depends rather than route it transitively through Prefix. T10a.2 breaks that convention — the body Consequence 2's one-line proof sketch "tumblers of equal length are prefix-related only if identical (Prefix)" parenthesizes only Prefix and propagates this narrowing into the Formal Contract.
**What needs resolving**: Either add T3 (CanonicalRepresentation) to T10a.2's Depends list with the per-step attribution ("applied at the step converting componentwise agreement at every position plus equal length into the sequence equality `a = b`") matching T10a.5's Depends pattern, or justify a convention under which T10a.2 may cite Prefix alone for an equal-length-distinct-siblings incomparability step while T10a.5 and other consuming properties must list T3 directly — and record that convention explicitly so the two representations do not appear to contradict each other on what Prefix supplies on its own.

### Consequence 8 body prose omits NAT-closure at the `+1` step that its own Formal Contract and the standalone T10a.8 both cite
**Foundation**: NAT-closure (NatArithmeticClosureAndIdentity) — the closure clause `(A n ∈ ℕ :: n + 1 ∈ ℕ)` places the post-increment value in ℕ before any NAT-* order axiom can speak of it. NAT-addcompat's strict successor inequality `n < n + 1` asserts a `<` relation on ℕ; it does not on its own establish that the term `n + 1` lies in ℕ — that membership is NAT-closure's content. The standalone T10a.8 theorem's body and its Formal Contract Depends both cite NAT-closure at the `+1` step alongside NAT-addcompat, and T10a body's own Formal Contract entry for T10a.8 likewise enumerates NAT-closure: "closure of ℕ under successor places `(tₙ)_{sig(tₙ)} + 1 ∈ ℕ` at the `+1` step".
**ASN**: T10a body, Consequence 8 prose: "The `inc(·, 0)` step then advances only that position (TA5(b), TA5(c)): the post-increment value `(tₙ)_{sig(tₙ)} + 1` remains strictly positive — NAT-addcompat's strict successor inequality `n < n + 1` for every `n ∈ ℕ` combined with NAT-zero's lower bound `0 ≤ (tₙ)_{sig(tₙ)}` gives `(tₙ)_{sig(tₙ)} + 1 > (tₙ)_{sig(tₙ)} ≥ 0`, matching the per-step citation convention TA5 and T10a.8's proof already follow for this `+1` arithmetic — and TA5(c) preserves length…"
**Issue**: Consequence 8's body prose cites only NAT-addcompat and NAT-zero at the `+1` step, omitting NAT-closure. The same step in the standalone T10a.8 body is discharged jointly by NAT-closure (placing `(tₙ)_{sig(tₙ)} + 1 ∈ ℕ`), NAT-zero (lower bound), and NAT-addcompat (strict successor), and T10a body's Formal Contract entry for T10a.8 enumerates all three. The body prose's self-reference ("matching the per-step citation convention TA5 and T10a.8's proof already follow") asserts consistency with T10a.8's convention, but the cited convention uses three axioms where the body prose uses two — so the prose's claim of matching the convention is textually incorrect. Because Consequence 5's body Case 2 inductive step (the parallel `1, 2, 3, …` iteration at position `j = #bₓ`) exhibits the same loose sourcing — "producing values `1, 2, 3, …` starting from `bₓ[j] = 1` (TA5(c))" without explicit NAT-closure — the gap is systematic across the body prose and creates an asymmetry with the Formal Contract and the standalone T10a.5, both of which cite NAT-closure at that step.
**What needs resolving**: Either (i) extend Consequence 8's body prose at the `+1` step to cite NAT-closure's closure of ℕ under successor (parallel to the standalone T10a.8 body's "NAT-closure (successor `(tₙ)_{sig(tₙ)} + 1 ∈ ℕ`)"), and apply the same extension to Consequence 5's body Case 2 iteration step, so that body prose, Formal Contract, and standalone theorems agree on what the `+1` step consumes; or (ii) record a convention under which body prose may abbreviate the three-axiom pairing (NAT-closure + NAT-zero + NAT-addcompat) to a two-axiom citation while Formal Contracts and standalone theorems must name all three, and update the body prose's self-referential "matching the per-step citation convention" claim to reflect that the body's convention is narrower than the one it invokes.
