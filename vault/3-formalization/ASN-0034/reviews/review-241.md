# Cone Review — ASN-0034/T10a.8 (cycle 6)

*2026-04-18 05:27*

### T10a-N's Depends cross-references nonexistent standalones for T10a.3 and T10a.5
**Foundation**: The ASN structure is such that only T10a.4 and T10a.8 exist as first-class separate blocks with their own standalone Formal Contracts (after T10a's axiom block). T10a.1, T10a.2, T10a.3, T10a.5, T10a.6, T10a.7 are defined exclusively as entries in T10a's Formal Contract Postconditions block — they have no separate standalone sections with their own independent Depends clauses.
**ASN**: T10a-N's Depends under NAT-addcompat: "chaining sharpens TA5(d)'s equation `#a₂ = #a₁ + k'` to the strict length inequality `#a₂ > #a₁` — matches the per-step convention T10a.8's summary Depends applies for the structurally identical strict-successor step (**T10a.3 and T10a.5 apply both this strict-successor step and the left order-compatibility step from NAT-addcompat in their standalone Depends, but their summary listings within this contract are pending alignment with their standalones**)."
**Issue**: The parenthetical explicitly distinguishes "standalone Depends" from "summary listings within this contract" for T10a.3 and T10a.5, asserting the former apply NAT-addcompat while the latter are pending alignment. But T10a.3 and T10a.5 have no standalone Depends — they exist only as entries in T10a's Formal Contract. The claim posits a two-tiered structure (standalone vs. summary) for these two properties that the ASN does not in fact contain, so a reader attempting to verify the cross-reference by locating T10a.3's or T10a.5's "standalone" finds no such object. The cross-reference terminates at a nonexistent artifact and cannot be checked.
**What needs resolving**: T10a-N's Depends parenthetical must be rewritten so every cross-reference it makes to a companion property resolves to an object that actually exists in the ASN — either by confining the comparison to T10a.8 (the one companion with a genuine standalone block that applies the convention) or by aligning the characterization of T10a.3 and T10a.5 with their actual form as summary-only entries in T10a's Formal Contract.
