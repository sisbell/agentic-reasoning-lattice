# Cone Review — ASN-0034/TA4 (cycle 5)

*2026-04-18 13:31*

### TA4 writes T0 vocabulary directly yet omits T0 from Depends
**Foundation**: T0 (CarrierSetDefinition) — characterises T as finite sequences over ℕ with length ≥ 1 and supplies the `∈ T`, `#·`, and `·ᵢ ∈ ℕ` vocabulary. The per-step citation discipline TA0's own Depends articulates: "TumblerAdd consumes T0 itself for its own preconditions and postconditions and does not re-export T0's vocabulary as a handle the way TA0 re-exports TumblerAdd's first two postconditions… the preconditions `a ∈ T`, `w ∈ T`, `actionPoint(w) ≤ #a` write T0's `∈ T` and `#` vocabulary directly as entry conditions. This matches the per-step citation discipline." T1, T3, TumblerAdd, TumblerSub, TA-Pos, ActionPoint, and TA0 each cite T0 directly under this convention.
**ASN**: TA4 (PartialInverse). Preconditions write `a ∈ T, w ∈ T, k = #a, #w = k, (A i : 1 ≤ i < k : aᵢ = 0)` — T0's `∈ T`, length operator `#`, and component projection `aᵢ` appear directly. Step 2's dichotomy entry states "`aₖ ∈ ℕ` since `k = #a` places position `k` within `a` and `a ∈ T` supplies ℕ-valued components at each such position" — reading component-typing off T0 explicitly. TA4's Depends lists TA-Pos, ActionPoint, TumblerAdd, TA0, T1, TumblerSub, T3, ZPD, NAT-closure, NAT-addcompat, NAT-cancel, NAT-zero, NAT-order, NAT-sub — T0 is absent.
**Issue**: TA4 writes T0's vocabulary (`∈ T`, `#`, component projection) in its own precondition list and invokes T0's carrier characterisation to justify the `aₖ ∈ ℕ` premise that underwrites NAT-zero's lower-bound instantiation for the Step 2 dichotomy. Every sibling property in this ASN that similarly writes T0's vocabulary cites T0 directly; transitive availability through TumblerAdd/TumblerSub/TA0 does not satisfy the convention that TA4 already follows for ZPD (also reachable through TumblerSub), NAT-zero (also reachable through TumblerAdd), and NAT-order (also reachable through T1/TumblerSub). The omission breaks the per-step discipline this ASN uniformly enforces, leaving the `aₖ ∈ ℕ` step — load-bearing for restricting the dichotomy to the two disjuncts `aₖ > 0 ∨ aₖ = 0` against an ℤ-valued third disjunct — without a Depends-backed source.
**What needs resolving**: TA4 must either add T0 to its Depends list with a per-site entry naming the precondition-list use of `∈ T`/`#` vocabulary and the Step 2 dichotomy use of `aₖ ∈ ℕ` via T0's component-typing, or restructure TA4 so these T0 invocations are routed through an already-cited property's postcondition that exports T0's component-typing as a handle (analogous to how TA0 re-exports TumblerAdd's first two postconditions).
