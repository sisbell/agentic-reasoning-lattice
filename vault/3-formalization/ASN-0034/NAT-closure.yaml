label: NAT-closure
name: NatArithmeticClosureAndIdentity
type: axiom
depends: []
summary: ℕ is closed under successor (`n + 1 ∈ ℕ`) and binary addition (`m + n ∈ ℕ`), with `0` serving as the additive identity
  (`0 + n = n`).
