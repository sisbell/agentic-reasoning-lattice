label: T8
name: AllocationPermanence
type: theorem
depends:
- AllocatedSet
- NoDeallocation
summary: Once an address is allocated it is never removed — the allocated set grows monotonically across every state transition.
  This follows directly from the NoDeallocation axiom, which precludes any removal operation from the transition vocabulary Σ.
