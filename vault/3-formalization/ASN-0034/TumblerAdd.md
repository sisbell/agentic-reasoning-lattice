## Tumbler arithmetic

We now turn to the arithmetic operations. The system requires operations that advance a position by a displacement (for computing span endpoints and shifting positions) and that recover the displacement between two positions (for computing span widths). These operations — tumbler addition (⊕, constructed in TumblerAdd) and subtraction (⊖, constructed in TumblerSub, § Tumbler subtraction above) — are not arithmetic on numbers but position-advance operations in a hierarchical address space.

A displacement `w` is a tumbler whose leading zeros say "stay at these hierarchical levels" and whose first nonzero component says "advance here." Components after the advance point describe the structure of the landing position within the target region.

### Definition of ⊕

Tumbler addition is not arithmetic addition — it is a **position-advance operation**: given a start position `a` and a displacement `w`, compute where you land. The displacement encodes both the distance and the hierarchical level at which the advance occurs.

```
START:  1.0.3.0.2.0.1.777
  DIF:  0.0.0.0.0.0.0.300
        ──────────────────
AFTER:  1.0.3.0.2.0.1.1077
```

Reading the displacement `[0,0,0,0,0,0,0,300]`: seven leading zeros mean "same server, same account, same document, same subspace." Component 8 is 300: "advance 300 elements." No trailing components: the landing position has no further sub-structure.

A displacement that acts at a higher level:

```
START:  1.0.3.0.2.0.1.777
  DIF:  0.0.0.0.3.0.1.1
        ──────────────────
AFTER:  1.0.3.0.5.0.1.1
```

Reading `[0,0,0,0,3,0,1,1]`: four leading zeros mean "same server, same account." Component 5 is 3: "advance 3 documents." Trailing `[0,1,1]`: "land at element 1.1 in the target document." The start position's element field `[1,777]` is replaced by the displacement's trailing structure `[1,1]`.

**TumblerAdd (TumblerAdd).** Let `a = [a₁, ..., aₘ]` and `w = [w₁, ..., wₙ]` with `a, w ∈ T` and `Pos(w)`. By ActionPoint, the precondition `Pos(w)` yields a well-defined action point `k = actionPoint(w)` — the least index with a nonzero component — satisfying `1 ≤ k ≤ n` and `wₖ ≥ 1`. Require `k ≤ m`: the action point must fall within the start position's length.

```
         ⎧ aᵢ           if i < k        (copy from start)
rᵢ   =  ⎨ aₖ + wₖ      if i = k        (single-component advance)
         ⎩ wᵢ           if i > k        (copy from displacement)
```

The result `a ⊕ w = [r₁, ..., rₚ]` has length `p = (k - 1) + 1 + (n - k) = n = #w`, where the partial-subtraction terms `k - 1 ∈ ℕ` and `n - k ∈ ℕ` are well-defined by NAT-sub's conditional closure (the former under `k ≥ 1` from ActionPoint, the latter under `n ≥ k` supplied by ActionPoint's `k ≤ #w = n`), and the collapses `(k - 1) + 1 = k` and `k + (n - k) = n` are NAT-sub's right-inverse characterisation `(m − n) + n = m` (instantiated at `m = k, n = 1` under `k ≥ 1`) and left-inverse characterisation `n + (m − n) = m` (instantiated at `m = n, n = k` under `n ≥ k`) respectively. Since `w ∈ T` requires `n ≥ 1`, the result has at least one component. We record this as the *result-length identity*: **`#(a ⊕ w) = #w`** — the length of the sum is determined entirely by the displacement, not the start position.

Each component of the result is a natural number: for `i < k`, `rᵢ = aᵢ ∈ ℕ` since `a ∈ T` and `k ≤ m` ensures position `i` exists within `a`; at the action point, `rₖ = aₖ + wₖ ∈ ℕ` by NAT-closure's closure of ℕ under addition; for `i > k`, `rᵢ = wᵢ ∈ ℕ` since `w ∈ T`. The result is therefore a finite sequence over ℕ with length ≥ 1, hence **`a ⊕ w ∈ T`** by T0.

The construction also yields strict advancement. ActionPoint supplies `wₖ ≥ 1` (the minimum-nonzero value at the action point), from which we derive `rₖ > aₖ` via the chain `rₖ = aₖ + wₖ ≥ aₖ + 1 > aₖ`, each link explicitly sourced: the equality `rₖ = aₖ + wₖ` is the action-point clause of the construction; NAT-addcompat's left order-compatibility (`n ≥ p ⟹ m + n ≥ m + p`, instantiated at `m = aₖ, n = wₖ, p = 1`) lifts `wₖ ≥ 1` to the non-strict `aₖ + wₖ ≥ aₖ + 1`; NAT-addcompat's strict successor inequality (`n < n + 1`, instantiated at `n = aₖ`) supplies the strict `aₖ + 1 > aₖ`; and NAT-order composes the non-strict `aₖ + wₖ ≥ aₖ + 1` with the strict `aₖ + 1 > aₖ` into the strict `aₖ + wₖ > aₖ` — NAT-order's defining clause `m ≤ n ⟺ m < n ∨ m = n` instantiated at `m = aₖ + 1, n = aₖ + wₖ` unfolds the `≥` into `aₖ + wₖ > aₖ + 1 ∨ aₖ + wₖ = aₖ + 1`; the first disjunct chains with `aₖ + 1 > aₖ` through NAT-order's transitivity of `<` to yield `aₖ + wₖ > aₖ`; the second disjunct substitutes `aₖ + wₖ = aₖ + 1` into `aₖ + 1 > aₖ` to yield the same conclusion. NAT-addcompat's order-compatibility clauses deliver *only* non-strict conclusions, so this `≥`/`>` composition to strict is discharged from NAT-order — matching the per-step citation discipline T1 and TA1 apply at their analogous strict-promotion sites. For all `i` with `1 ≤ i < k`, `rᵢ = aᵢ` by the construction. The precondition `k ≤ m` gives `k ≤ #a`, and the result-length identity gives `k ≤ n = #(a ⊕ w)`, so `k ≤ #a ∧ k ≤ #(a ⊕ w)` and both tumblers have a component at position `k` — the conjunction form avoids any reliance on a primitive binary-minimum operator on ℕ, matching the convention Divergence's definition establishes. T1 case (i) with divergence position `k` — agreement on positions `1, ..., k - 1` and strict inequality `aₖ < rₖ` — yields `a < a ⊕ w`. We record this as the *ordering guarantee*: **`a ⊕ w > a`** — tumbler addition strictly advances the start position.

The construction also yields dominance over the displacement. Since `#(a ⊕ w) = #w` (result-length identity), the T1 comparison of `a ⊕ w` and `w` reduces to finding the first position where `rᵢ ≠ wᵢ`. For `i < k`, `rᵢ = aᵢ` and `wᵢ = 0` (by definition of action point). The per-position dichotomy `aⱼ > 0 ∨ aⱼ = 0` holds at each `j` with `1 ≤ j < k`, sourced per step: for each such `j`, the precondition `k ≤ m` places `j` within `a`'s length so T0 gives `aⱼ ∈ ℕ`; NAT-zero's axiom `(A n ∈ ℕ :: 0 ≤ n)` supplies the lower bound `0 ≤ aⱼ`; NAT-order's defining clause `m ≤ n ⟺ m < n ∨ m = n` instantiated at `m = 0, n = aⱼ` unfolds this lower bound into `0 < aⱼ ∨ 0 = aⱼ`, i.e., `aⱼ > 0 ∨ aⱼ = 0`. The top-level case split is the bounded excluded middle `(∃j ∈ [1, k-1] : aⱼ > 0) ∨ ¬(∃j ∈ [1, k-1] : aⱼ > 0)` on the existential — a strictly stronger disjunction than the universal closure `(∀j ∈ [1, k-1] : aⱼ > 0 ∨ aⱼ = 0)` of the per-position dichotomy (the universal closure permits each `j` to select either disjunct independently, while bounded LEM asserts the existential uniformly true or uniformly false), so the move is not a closure of the dichotomy but a separate instance — itself discharged by the decidability of each atomic `aⱼ > 0` (the mutually-exclusive dichotomy just established: NAT-order's defining-clause unfolding at `m = 0, n = aⱼ` produces the disjuncts `aⱼ > 0` and `aⱼ = 0`, and NAT-order's irreflexivity at `n = aⱼ` rules out their conjunction since `aⱼ = 0` substituted into `aⱼ > 0` yields `0 < 0`) over the finite index range `[1, k-1]` (with `k - 1 ∈ ℕ` discharged by NAT-sub's conditional closure under ActionPoint's lower bound `k ≥ 1`), which renders the bounded existential a finite disjunction of decidable atomic propositions and the bounded-LEM instance the disjunction of its truth and falsity. The second disjunct `¬(∃j ∈ [1, k-1] : aⱼ > 0)` reduces to the universal `(∀j ∈ [1, k-1] : aⱼ = 0)` via DeMorgan on the bounded existential — `¬(∃j ∈ [1, k-1] : aⱼ > 0) ⟺ (∀j ∈ [1, k-1] : ¬(aⱼ > 0))` — composed with the per-position dichotomy: at each `j`, `¬(aⱼ > 0)` excludes the strict disjunct of `aⱼ > 0 ∨ aⱼ = 0` and leaves `aⱼ = 0`, yielding the universal the equality sub-case consumes. If some `aⱼ > 0` for `j < k`, NAT-wellorder applied to the nonempty subset `{j : 1 ≤ j < k ∧ aⱼ > 0} ⊆ ℕ` — nonempty by the sub-case hypothesis, a subset of ℕ because `j` ranges over natural-number indices `1 ≤ j < k` — supplies the least such `j`, which discharges T1 case (i) at the divergence position `j` via four sub-claims at the granularity the sibling sub-case `aₖ > 0` applies. *Agreement on positions `1 ≤ i < j`*: at each such `i`, the per-position dichotomy `0 < aᵢ ∨ 0 = aᵢ` already established for the outer case split (T0's `aᵢ ∈ ℕ` from `k ≤ m` placing `i` within `a`'s length, NAT-zero's `0 ≤ aᵢ`, NAT-order's defining clause unfolding at `m = 0, n = aᵢ`) has its strict disjunct excluded by the minimality of `j` (any `i` with `1 ≤ i < j` and `aᵢ > 0` would be a smaller member of the same set, contradicting the least-element claim NAT-wellorder discharges), leaving `aᵢ = 0`; ActionPoint's zeros-below-action-point fact applied at index `i` (which satisfies `1 ≤ i < j < k = actionPoint(w)`) supplies `wᵢ = 0`; the construction's `i < k` clause gives `rᵢ = aᵢ`, so `rᵢ = aᵢ = 0 = wᵢ`. *Bound `j ≤ #w`*: the minimality of `j` places it in the set, so `j < k`; ActionPoint's upper bound `k ≤ #w` (with `n = #w` by typing) composes with `j < k` to yield `j < #w` — in the strict disjunct `k < #w` of the unpacked bound `k ≤ #w` via NAT-order's transitivity of `<`, in the equality disjunct `k = #w` via substitution of `k = #w` into `j < k`; NAT-order's defining clause `m ≤ n ⟺ m < n ∨ m = n` instantiated at `m = j, n = #w` then returns `j ≤ #w` via the strict disjunct. *Bound `j ≤ #r`*: the result-length identity `#(a ⊕ w) = #w` (i.e., `#r = #w`) substitutes into `j ≤ #w` to yield `j ≤ #r`. *Strict inequality `wⱼ < rⱼ`*: ActionPoint's zeros-below-action-point fact applied at index `j` (which satisfies `1 ≤ j < k = actionPoint(w)` by minimality) gives `wⱼ = 0`; the construction's `j < k` clause gives `rⱼ = aⱼ`; the sub-case hypothesis `aⱼ > 0` (i.e., `0 < aⱼ`) substitutes via these two equalities into `wⱼ < rⱼ`, i.e., `rⱼ > wⱼ`. T1 case (i) with divergence position `j` — agreement on positions `1, ..., j - 1`, bounds `j ≤ #r ∧ j ≤ #w`, and strict component inequality `wⱼ < rⱼ` — yields `w < r`, i.e., `r > w`. If instead `aᵢ = 0` for all `i < k`, then at position `k` we have `rₖ = aₖ + wₖ`. The inner case split `aₖ > 0 ∨ aₖ = 0` at the action point is the same NAT-zero + NAT-order unfolding the outer dichotomy consumes, applied at `m = 0, n = aₖ`, with `aₖ ∈ ℕ` because `k ≤ m` places position `k` within `a`: NAT-zero supplies `0 ≤ aₖ`, and NAT-order's defining clause unfolds this into `0 < aₖ ∨ 0 = aₖ`. When `aₖ > 0`, we derive `rₖ > wₖ` — i.e., `aₖ + wₖ > wₖ` — via a four-step chain consuming NAT-addcompat, NAT-closure, NAT-cancel, and NAT-order together (with the NAT-zero lower bound already supplied for the dichotomy above): the symmetric transfer from the earlier strict-advancement chain `rₖ = aₖ + wₖ ≥ aₖ + 1 > aₖ` is unavailable because the mirrored route would require `1 + wₖ > wₖ` against NAT-addcompat's strict successor inequality `wₖ < wₖ + 1`, and T0's exhaustive NAT-* enumeration does not include commutativity of addition on ℕ. NAT-addcompat's right order-compatibility (`n ≥ p ⟹ n + m ≥ p + m`, instantiated at `n = aₖ, p = 0, m = wₖ`) lifts NAT-zero's lower bound `0 ≤ aₖ` (the same instance the inner dichotomy consumes) into the non-strict `aₖ + wₖ ≥ 0 + wₖ`; NAT-closure's additive identity `0 + wₖ = wₖ` rewrites the right-hand side, yielding `aₖ + wₖ ≥ wₖ`; NAT-cancel's summand absorption in its symmetric form `n + m = m ⟹ n = 0` (instantiated at `n = aₖ, m = wₖ`) rules out the equality `aₖ + wₖ = wₖ` — that equality would force `aₖ = 0` and contradict the sub-case hypothesis `aₖ > 0` via NAT-order's irreflexivity (substituting `aₖ = 0` into `0 < aₖ` yields `0 < 0`); and NAT-order's defining clause `m ≤ n ⟺ m < n ∨ m = n` instantiated at `m = wₖ, n = aₖ + wₖ`, applied to the non-strict `aₖ + wₖ ≥ wₖ` together with the just-discharged non-equality, returns the strict `aₖ + wₖ > wₖ`, i.e., `rₖ > wₖ` — the same strict-through-addition pattern TA1's sub-case `j = k` applies at its analogous site. T1 case (i) with divergence position `k` again gives `r > w`; when `aₖ = 0`, `rₖ = wₖ`; combined with `rᵢ = aᵢ = 0 = wᵢ` for `i < k` and `rᵢ = wᵢ` for `i > k`, every component agrees, and `#r = #w` by the result-length identity, so `r = w` by T3, hence `r ≥ w`. In every case: **`a ⊕ w ≥ w`** — the result of tumbler addition dominates the displacement in T1 ordering.

These four results are load-bearing for subsequent properties that depend on TumblerAdd.

Three properties of this definition require explicit statement:

**No carry propagation:** The sum `aₖ + wₖ` at the action point is a single natural-number addition. There is no carry into position `k - 1`. This is why the operation is fast — constant time regardless of tumbler length.

**Tail replacement, not tail addition:** Components after the action point come entirely from `w`. The start position's components at positions `k + 1, ..., m` are discarded. `a ⊕ w` does not add corresponding components pairwise — it replaces the start's sub-structure with the displacement's sub-structure below the action point.

**The many-to-one property:** Because trailing components of `a` are discarded, distinct start positions can produce the same result:

```
[1, 1] ⊕ [0, 2]       = [1, 3]
[1, 1, 5] ⊕ [0, 2]    = [1, 3]
[1, 1, 999] ⊕ [0, 2]  = [1, 3]
```

This is correct and intentional: advancing to "the beginning of the next chapter" lands at the same place regardless of where you were within the current chapter. ∎

*Formal Contract:*
- *Preconditions:* a ∈ T, w ∈ T, Pos(w), actionPoint(w) ≤ #a
- *Definition:* k = actionPoint(w); rᵢ = aᵢ if i < k; rₖ = aₖ + wₖ; rᵢ = wᵢ if i > k
- *Depends:* T0 (CarrierSetDefinition) — membership `a ⊕ w ∈ T` is concluded via T0's characterisation of T as finite sequences over ℕ with length ≥ 1; the dominance proof's two dichotomy sites (`aⱼ > 0 ∨ aⱼ = 0` for `j < k`, and `aₖ > 0 ∨ aₖ = 0` at the action point) also read `aⱼ ∈ ℕ` and `aₖ ∈ ℕ` off T0's carrier so that NAT-zero's `(A n ∈ ℕ :: 0 ≤ n)` applies at those components. NAT-closure (NatArithmeticClosureAndIdentity) — the "each component of the result is a natural number" step at the action point (`rₖ = aₖ + wₖ ∈ ℕ`) invokes NAT-closure's closure of ℕ under addition; the dominance proof's equality sub-case (`aₖ = 0 ⟹ rₖ = wₖ`) and strict-advancement sub-case (`aₖ > 0 ⟹ aₖ + wₖ > wₖ`) both invoke NAT-closure's additive identity (`0 + wₖ = wₖ`) — in the equality sub-case to substitute `rₖ = aₖ + wₖ = 0 + wₖ = wₖ` directly, in the strict-advancement sub-case to rewrite the right-hand side of NAT-addcompat's right-order-compatibility conclusion `aₖ + wₖ ≥ 0 + wₖ` as the non-strict `aₖ + wₖ ≥ wₖ` that the remaining NAT-cancel + NAT-order promotion consumes. NAT-addcompat (NatAdditionOrderAndSuccessor) — the strict-advancement chain `rₖ = aₖ + wₖ ≥ aₖ + 1 > aₖ` invokes two NAT-addcompat clauses: left order-compatibility (`n ≥ p ⟹ m + n ≥ m + p`, instantiated at `m = aₖ, n = wₖ, p = 1`) lifts `wₖ ≥ 1` into the non-strict `aₖ + wₖ ≥ aₖ + 1`, and the strict successor inequality (`n < n + 1`, instantiated at `n = aₖ`) supplies the strict `aₖ + 1 > aₖ`. The dominance proof's inner sub-case `aₖ > 0` invokes NAT-addcompat's right order-compatibility (`n ≥ p ⟹ n + m ≥ p + m`, instantiated at `n = aₖ, p = 0, m = wₖ`) to lift NAT-zero's lower bound `0 ≤ aₖ` into the non-strict `aₖ + wₖ ≥ 0 + wₖ` — the first step of the strict-through-addition chain that derives `aₖ + wₖ > wₖ` (equivalently `rₖ > wₖ`) for the sub-case's conclusion; right order-compatibility is the form required here because the symmetric route through left order-compatibility with `aₖ ≥ 1` would need the strict `1 + wₖ > wₖ`, not available from NAT-addcompat's strict successor inequality `wₖ < wₖ + 1` without commutativity of addition on ℕ (which T0's exhaustive NAT-* enumeration does not include). NAT-addcompat's order-compatibility clauses deliver *only* non-strict conclusions — as NAT-addcompat's own contract articulates — so the composition of the non-strict `aₖ + wₖ ≥ aₖ + 1` with the strict `aₖ + 1 > aₖ` into the strict `aₖ + wₖ > aₖ`, and the promotion of the non-strict `aₖ + wₖ ≥ wₖ` (from right order-compatibility + NAT-closure's additive identity) to the strict `aₖ + wₖ > wₖ`, are routed through NAT-order (with NAT-cancel additionally ruling out the equality disjunct in the latter case) rather than discharged by NAT-addcompat alone. NAT-cancel (NatAdditionCancellation) — the dominance proof's inner sub-case `aₖ > 0` invokes NAT-cancel's summand absorption in its symmetric form (`(A m, n ∈ ℕ : n + m = m : n = 0)`, instantiated at `n = aₖ, m = wₖ`) to rule out the equality disjunct `aₖ + wₖ = wₖ` inside NAT-order's defining-clause unfolding of the non-strict `aₖ + wₖ ≥ wₖ` — itself obtained from NAT-zero's lower bound `0 ≤ aₖ` via NAT-addcompat's right order-compatibility at `n = aₖ, p = 0, m = wₖ` and NAT-closure's additive identity `0 + wₖ = wₖ`. The equality `aₖ + wₖ = wₖ` would force `aₖ = 0` and contradict the sub-case hypothesis `aₖ > 0` via NAT-order's irreflexivity (substituting `aₖ = 0` into `0 < aₖ` yields `0 < 0`), leaving the strict disjunct `aₖ + wₖ > wₖ` as the sub-case's `rₖ > wₖ` conclusion. NAT-addcompat's order-compatibility clauses deliver only non-strict conclusions, so the strict promotion from `aₖ + wₖ ≥ wₖ` to `aₖ + wₖ > wₖ` must be discharged through NAT-cancel (for the equality-ruling-out sub-step) together with NAT-order (for the defining-clause unfolding and irreflexivity) rather than NAT-addcompat alone — the same strict-through-addition pattern TA1's sub-case `j = k` applies at its analogous site (with right cancellation instead of summand absorption), and the only route to `aₖ + wₖ > wₖ` that respects T0's exhaustive NAT-* enumeration: the symmetric chain through left order-compatibility with `aₖ ≥ 1` would require `1 + wₖ > wₖ` against NAT-addcompat's strict successor inequality `wₖ < wₖ + 1`, which differ by a commutativity step absent from the enumeration. NAT-zero (NatZeroMinimum) — invoked at the dominance proof's two dichotomy sites to fix the lower bound on the ℕ-valued components being case-split, and additionally consumed at the strict-through-addition chain in the inner sub-case `aₖ > 0`. (i) For each `j` with `1 ≤ j < k`, the precondition `k ≤ m` places `j` within `a`'s length so T0 gives `aⱼ ∈ ℕ`, and NAT-zero's axiom `(A n ∈ ℕ :: 0 ≤ n)` supplies `0 ≤ aⱼ` — the lower-bound premise that NAT-order's defining clause `m ≤ n ⟺ m < n ∨ m = n` at `m = 0, n = aⱼ` consumes to unfold into `0 < aⱼ ∨ 0 = aⱼ`, i.e., the `aⱼ > 0 ∨ aⱼ = 0` dichotomy the top-level case split consumes over `j < k`. (ii) At the action point, `k ≤ m` places position `k` within `a` so T0 gives `aₖ ∈ ℕ`, and NAT-zero supplies `0 ≤ aₖ` — the lower-bound premise the same NAT-order unfolding at `m = 0, n = aₖ` consumes to deliver the inner `aₖ > 0 ∨ aₖ = 0` dichotomy; in the inner sub-case `aₖ > 0`, the same `0 ≤ aₖ` instance additionally feeds NAT-addcompat's right order-compatibility at `n = aₖ, p = 0, m = wₖ` to lift into `aₖ + wₖ ≥ 0 + wₖ` — the first step of the strict-through-addition chain that derives `aₖ + wₖ > wₖ` (equivalently `rₖ > wₖ`) for the sub-case's conclusion, a reuse of the same NAT-zero instance rather than a fresh site. No other listed NAT-* axiom supplies this lower bound: NAT-order's trichotomy compares `0` and `aⱼ` (resp. `aₖ`) without fixing the winner, and NAT-wellorder's least-element principle gives ℕ a minimum without naming it `0`. NAT-zero's axiom discharges exactly the `0 ≤ ·` premise at each site, matching the per-step citation convention TA-PosDom's Case `#z ≥ k` and ActionPoint's third postcondition establish for the structurally identical `0 ≤ t_⋅` step. NAT-order (NatStrictTotalOrder) — supplies four steps in the strict-advancement and dominance proofs. (i) The strict-advancement chain `rₖ = aₖ + wₖ ≥ aₖ + 1 > aₖ` composes the non-strict `aₖ + wₖ ≥ aₖ + 1` (from NAT-addcompat's left order-compatibility) with the strict `aₖ + 1 > aₖ` (from NAT-addcompat's strict successor inequality) into the strict `aₖ + wₖ > aₖ` — NAT-order's defining clause `m ≤ n ⟺ m < n ∨ m = n` instantiated at `m = aₖ + 1, n = aₖ + wₖ` unfolds the `≥` into `aₖ + wₖ > aₖ + 1 ∨ aₖ + wₖ = aₖ + 1`, the first disjunct chains with `aₖ + 1 > aₖ` through NAT-order's transitivity of `<` to yield `aₖ + wₖ > aₖ`, and the second disjunct substitutes `aₖ + wₖ = aₖ + 1` into `aₖ + 1 > aₖ` to yield the same conclusion. Since NAT-addcompat's order-compatibility clauses deliver only non-strict conclusions, this `≥`/`>` composition to strict must be routed through NAT-order — matching the per-step citation discipline T1 and TA1 apply at their analogous strict-promotion sites. (ii) The dominance proof's dichotomy at `0` on ℕ, at both instances (`aⱼ > 0 ∨ aⱼ = 0` for `j < k`, and the inner `aₖ > 0 ∨ aₖ = 0` at the action point): NAT-order's defining clause instantiated at `m = 0, n = aⱼ` (resp. `n = aₖ`) unfolds the lower bound `0 ≤ aⱼ` (resp. `0 ≤ aₖ`) supplied by NAT-zero into the disjunct `0 < aⱼ ∨ 0 = aⱼ` (resp. `0 < aₖ ∨ 0 = aₖ`) that each dichotomy site consumes. Without NAT-order the dichotomies would appeal to background `<`/`=` trichotomy on ℕ, breaking the per-step citation convention that TA-Pos and ActionPoint already enforce for analogous dichotomies. The same per-position unfoldings at indices `1 ≤ i < k` are reused by the divergence sub-case's agreement-condition argument at the sub-range `1 ≤ i < j`: there the surviving disjunct `0 = aᵢ` is what minimality of `j` (NAT-wellorder) leaves once the strict disjunct `0 < aᵢ` is excluded — same NAT-order instance per index, new consumption rather than a fresh site. (iii) The dominance proof's inner sub-case `aₖ > 0` step `aₖ + wₖ > wₖ`: NAT-order's defining clause instantiated at `m = wₖ, n = aₖ + wₖ` unfolds the non-strict `aₖ + wₖ ≥ wₖ` (obtained from NAT-zero's lower bound `0 ≤ aₖ` via NAT-addcompat's right order-compatibility and NAT-closure's additive identity) into `aₖ + wₖ > wₖ ∨ aₖ + wₖ = wₖ`; NAT-cancel's summand absorption in its symmetric form rules out the equality disjunct by forcing `aₖ = 0`, which contradicts the sub-case hypothesis `aₖ > 0` via NAT-order's irreflexivity (substituting `aₖ = 0` into `0 < aₖ` yields `0 < 0`), so the strict disjunct `aₖ + wₖ > wₖ` is the remaining outcome — equivalently `rₖ > wₖ`, the strict component inequality T1 case (i) with divergence position `k` consumes to give `a ⊕ w > w`. Since NAT-addcompat's order-compatibility clauses deliver only non-strict conclusions, this strict promotion from `aₖ + wₖ ≥ wₖ` to `aₖ + wₖ > wₖ` must be routed through NAT-order (with NAT-cancel ruling out the equality disjunct) rather than NAT-addcompat alone — matching the strict-through-addition pattern TA1's sub-case `j = k` applies at its analogous site, and the only route that respects T0's exhaustive NAT-* enumeration: the symmetric chain through NAT-addcompat's left order-compatibility with `aₖ ≥ 1` would require `1 + wₖ > wₖ` against the strict successor inequality `wₖ < wₖ + 1`, which differ by a commutativity step absent from the enumeration. (iv) The dominance proof's divergence sub-case bound `j ≤ #w`: NAT-order's transitivity of `<` composes the minimality bound `j < k` (from NAT-wellorder applied to `{j : 1 ≤ j < k ∧ aⱼ > 0}`) with the strict disjunct `k < #w` of ActionPoint's unpacked bound `k ≤ #w` (with `n = #w` by typing) into `j < #w`; in the equality disjunct `k = #w`, substitution of `k = #w` into `j < k` yields `j < #w` directly without a transitivity step. NAT-order's defining clause `m ≤ n ⟺ m < n ∨ m = n` instantiated at `m = j, n = #w` then returns `j ≤ #w` via the strict disjunct — the T1 case (i) length condition for the divergence-position witness. The sibling bound `j ≤ #r` is obtained by substituting the result-length identity `#r = #w` into `j ≤ #w` (no fresh NAT-order site). Without NAT-order's transitivity and defining clause, the bound `j ≤ #w` would rest on a background `<`/`≤` chaining step rather than the per-step citation convention TA-Pos and ActionPoint enforce for analogous bounds. NAT-wellorder (NatWellOrdering) — the dominance proof's divergence sub-case invokes the well-ordering of ℕ at two coupled sites that share a single application to the nonempty subset `{j : 1 ≤ j < k ∧ aⱼ > 0} ⊆ ℕ` — nonempty by the sub-case hypothesis that some `aⱼ > 0` for `j < k`, a subset of ℕ because `j` ranges over natural-number indices `1 ≤ j < k`. (a) The naming step "the least such `j` is a divergence position" reads off the least-element principle directly: NAT-wellorder supplies the minimum of the set, which the proof names `j`. (b) The agreement-condition argument `rᵢ = wᵢ` for `1 ≤ i < j` consumes the same minimality to exclude the strict disjunct `0 < aᵢ` from the per-position dichotomy `0 < aᵢ ∨ 0 = aᵢ` (that dichotomy itself supplied at each `i` by T0 + NAT-zero + NAT-order's defining clause as it is at the outer case split), leaving `aᵢ = 0`: any `i` with `1 ≤ i < j` and `aᵢ > 0` would be a smaller member of the same set, contradicting the least-element claim of (a). The two sites share the single NAT-wellorder application; without the minimality cited at (b), the agreement zeros `aᵢ = 0` for `i < j` would rest on a tacit appeal rather than the same NAT-wellorder invocation that names `j`. This use of well-ordering is independent of the well-ordering propagated into TumblerAdd through `actionPoint(w)` (which `ActionPoint` discharges internally over the set `{i : 1 ≤ i ≤ #w ∧ wᵢ ≠ 0}`), so both load-bearing claims of the divergence sub-case are discharged from NAT-wellorder rather than left implicit, matching the per-step citation convention established for `TA-Pos` and `ActionPoint`. NAT-sub (NatPartialSubtraction) — the result-length identity `p = (k - 1) + 1 + (n - k) = n` consumes NAT-sub at three sites enumerated at the granularity TumblerSub, D0, D1, D2, TA3, TA4, TA3-strict, TA-LC, TA7a, ReverseInverse, and TA5a apply for partial subtraction on ℕ: (i) conditional closure discharges `k - 1 ∈ ℕ` under the precondition `k ≥ 1` (supplied by ActionPoint's lower bound `1 ≤ k`); (ii) conditional closure discharges `n - k ∈ ℕ` under the precondition `n ≥ k` (supplied by ActionPoint's upper bound `k ≤ #w` together with the typing `n = #w`); (iii) the right-inverse characterisation `(m − n) + n = m` discharges the collapse `(k - 1) + 1 = k` (instantiated at `m = k, n = 1` under `k ≥ 1`), and the left-inverse characterisation `n + (m − n) = m` discharges the collapse `k + (n - k) = n` (instantiated at `m = n, n = k` under `n ≥ k`). The result-length identity is load-bearing — promulgated as the postcondition `#(a ⊕ w) = #w` and consumed by TA0, Definition (Span), and every downstream user of `a ⊕ w`'s length — so without NAT-sub the identity's arithmetic would tacitly rest on background subtraction on ℕ, leaving a reviser tightening NAT-sub without Depends-backed visibility into TumblerAdd. ActionPoint (ActionPoint) — supplies `k = actionPoint(w)` with bounds `1 ≤ k ≤ n`, the zeros-below-action-point fact `wᵢ = 0 for i < actionPoint(w)`, and the minimum-nonzero value `wₖ ≥ 1`; the bounds `1 ≤ k` and `k ≤ n` also discharge the preconditions of NAT-sub's conditional-closure clauses for `k - 1 ∈ ℕ` and `n - k ∈ ℕ` in the result-length identity; the dominance proof `a ⊕ w ≥ w` consumes ActionPoint's facts at three sites within the divergence sub-case and one site within the equality sub-case. Within the divergence sub-case (least `j < k` with `aⱼ > 0`): (a) the zeros-below-k fact applied at index `j` gives `wⱼ = 0`, which combined with `rⱼ = aⱼ` (construction's `j < k` clause) and `aⱼ > 0` (sub-case hypothesis) delivers the strict component inequality `wⱼ < rⱼ` required by T1 case (i); (b) the same zeros-below-k fact applied at each index `i` with `1 ≤ i < j` (and therefore `i < j < k = actionPoint(w)`) gives `wᵢ = 0`, which combined with `rᵢ = aᵢ` (construction) and `aᵢ = 0` (minimality of `j` excluding the strict disjunct of NAT-order's per-position dichotomy at `i`, NAT-wellorder discharging the minimality) delivers the agreement zeros `rᵢ = aᵢ = 0 = wᵢ` required by T1 case (i) — same per-index instances of the zeros-below-k fact reused at the sub-range `1 ≤ i < j` rather than fresh sites; (c) the upper bound `k ≤ #w` (with `n = #w` by typing) composes with the minimality bound `j < k` through NAT-order's transitivity (in the strict disjunct `k < #w` of the unpacked bound) or substitution (in the equality disjunct `k = #w` into `j < k`) to yield `j < #w`, whence NAT-order's defining clause delivers the T1 case (i) length condition `j ≤ #w`; the sibling length condition `j ≤ #r` follows from `j ≤ #w` via the result-length identity `#r = #w`, so ActionPoint's `k ≤ #w` is what underwrites both length conditions for the divergence-position witness rather than only the bound it directly supplies. Within the equality sub-case (`aᵢ = 0` for all `i < k`): the zeros-below-k fact gives `wᵢ = 0` for all `i < k`, which combined with `rᵢ = aᵢ = 0` (construction and case hypothesis) yields `rᵢ = aᵢ = 0 = wᵢ` for all `i < k`. TA-Pos (PositiveTumbler) — defines the predicate `Pos(w)` used in the precondition. T1 (LexicographicOrder) — both ordering postconditions invoke T1 case (i) at the first divergence position. T3 (CanonicalRepresentation) — the equality sub-case of `a ⊕ w ≥ w` concludes `r = w` from component-wise agreement and equal length.
- *Postconditions:* a ⊕ w ∈ T, #(a ⊕ w) = #w, a ⊕ w > a (T1), a ⊕ w ≥ w (T1, T3)
