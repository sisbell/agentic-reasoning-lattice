**OrdinalShift (OrdinalShift).** For a tumbler v ∈ T of length m = #v and natural number n ≥ 1:

`shift(v, n) = v ⊕ δ(n, m)`

We verify the four preconditions of TA0 for this application. First, v ∈ T by assumption. Before invoking OrdinalDisplacement at `(n, m)` we discharge its four preconditions one-for-one: `n ∈ ℕ` transfers directly from OrdinalShift's own precondition `n ∈ ℕ`; `m ∈ ℕ` follows from T0's length operator typing `#·: T → ℕ` instantiated at `v ∈ T`, placing `m = #v ∈ ℕ`; `n ≥ 1` transfers directly from OrdinalShift's own precondition `n ≥ 1`; and `m ≥ 1` follows from T0's length axiom `#a ≥ 1 for all a ∈ T` instantiated at `a = v ∈ T`, yielding `m = #v ≥ 1`. Second, δ(n, m) ∈ T by OrdinalDisplacement's exported postcondition `δ(n, m) ∈ T` — discharged inside OrdinalDisplacement's own proof from δ's sequence form and length via T0, and re-exported as a named postcondition that callers cite directly rather than re-derive. Third, Pos(δ(n, m)) by OrdinalDisplacement's exported postcondition `Pos(δ(n, m))` — discharged inside OrdinalDisplacement's own proof through the `n ≥ 1 ⟹ n ≠ 0` promotion (NAT-addcompat anchor `0 < 1`, NAT-order's defining clause + transitivity of `<` + irreflexivity) witnessing TA-Pos's existential at `i = m`, and re-exported as a named postcondition so that the component-nonzero promotion need not be re-discharged here. Fourth, the action point of δ(n, m) is m = #v by OrdinalDisplacement's exported postcondition `actionPoint(δ(n, m)) = m`, so actionPoint(δ(n, m)) ≤ #v holds trivially.

With all TA0 preconditions satisfied, shift(v, n) = v ⊕ δ(n, m) ∈ T. By TumblerAdd: shift(v, n)ᵢ = vᵢ for i < m, shift(v, n)ₘ = vₘ + n, and #shift(v, n) = #δ(n, m) = m = #v. The shift advances the deepest component by exactly n, leaving all higher-level components unchanged.

Additionally, shift preserves structural properties. When m ≥ 2, the action point of δ(n, m) leaves position 1 unchanged — shift(v, n)₁ = v₁. When m = 1, letting c denote the sole component so that v = [c] with c ∈ ℕ, shift([c], n) = [c + n] changes the first component. The component-positivity postcondition `shift(v, n)ₘ = vₘ + n ≥ 1` is an arithmetic lower bound on ℕ — TumblerAdd supplies only the equation `shift(v, n)ₘ = vₘ + n`, not any bound on the sum — and is discharged per step. T0's carrier characterisation applied to v ∈ T places `vₘ ∈ ℕ`; NAT-zero's axiom `(A n ∈ ℕ :: 0 ≤ n)` instantiated at `n = vₘ` supplies `0 ≤ vₘ`; NAT-addcompat's right order-compatibility (`n ≥ p ⟹ n + m ≥ p + m`, with axiom-`n = vₘ`, axiom-`p = 0`, and axiom-`m = n` — the shift amount filling the axiom's fixed right summand) lifts `0 ≤ vₘ` into the non-strict `vₘ + n ≥ 0 + n`; NAT-closure's additive identity `0 + n = n` rewrites the right-hand side, yielding `vₘ + n ≥ n`; and NAT-order composes this non-strict inequality with the precondition `n ≥ 1` into `vₘ + n ≥ 1` — NAT-order's defining clause `m ≤ n ⟺ m < n ∨ m = n` instantiated at `m = n, n = vₘ + n` unfolds `vₘ + n ≥ n` into `vₘ + n > n ∨ vₘ + n = n`, the same clause at `m = 1, n = n` unfolds `n ≥ 1` into `n > 1 ∨ n = 1`, the three cases with at least one strict disjunct (both strict chaining through NAT-order's transitivity of `<`, and each mixed case substituting the equality into the strict) yield the strict `vₘ + n > 1`, which the defining clause at `m = 1, n = vₘ + n` folds back into the non-strict `vₘ + n ≥ 1` via its strict disjunct, and the all-equal case substitutes both equalities to yield `vₘ + n = 1`, which the same defining clause folds into the non-strict via its equality disjunct. NAT-addcompat's order-compatibility clauses deliver *only* non-strict conclusions — as NAT-addcompat's own contract articulates — so the composition of the two non-strict inequalities `vₘ + n ≥ n` and `n ≥ 1` into the non-strict `vₘ + n ≥ 1` is routed through NAT-order rather than NAT-addcompat alone, matching the per-step citation discipline TumblerAdd applies at its analogous non-strict-arithmetic composition sites. ∎

*Formal Contract:*
- *Preconditions:* v ∈ T, n ∈ ℕ, n ≥ 1
- *Definition:* shift(v, n) = v ⊕ δ(n, m) where m = #v
- *Depends:* OrdinalDisplacement (OrdinalDisplacement) — invoked to construct `δ(n, m)` and supply its three exported postconditions, each consumed at a separate TA0 precondition check: postcondition `δ(n, m) ∈ T` discharges TA0's second precondition ("δ(n, m) ∈ T by OrdinalDisplacement's exported postcondition `δ(n, m) ∈ T`"); postcondition `Pos(δ(n, m))` discharges TA0's third precondition ("Pos(δ(n, m)) by OrdinalDisplacement's exported postcondition `Pos(δ(n, m))`"); and postcondition `actionPoint(δ(n, m)) = m` supplies the value consumed at TA0's fourth precondition check ("the action point of δ(n, m) is m = #v by OrdinalDisplacement's exported postcondition `actionPoint(δ(n, m)) = m`"). Routing through OrdinalDisplacement's three named postconditions rather than re-deriving them inline at each TA0 precondition check matches the routing TS1 applies downstream — TS1's checks (iii), (v), (vi), (vii) likewise cite the same three exported postconditions directly — so that a reviser tightening any of OrdinalDisplacement's three exported postconditions (in particular the `n ≥ 1 ⟹ n ≠ 0` promotion that OrdinalDisplacement's own proof discharges and that `Pos(δ(n, m))` consumes as its witness `δₘ = n ≠ 0`) has a single discharge contract to update rather than two duplicated re-derivations across OrdinalShift and TS1. OrdinalDisplacement's own four preconditions `n ∈ ℕ, m ∈ ℕ, n ≥ 1, m ≥ 1` are discharged one-for-one at the invocation site: `n ∈ ℕ` transfers directly from OrdinalShift's own precondition `n ∈ ℕ`; `m ∈ ℕ` is discharged via T0's length operator typing `#·: T → ℕ` instantiated at `v ∈ T`, placing `m = #v ∈ ℕ`; `n ≥ 1` transfers directly from OrdinalShift's own precondition `n ≥ 1`; and `m ≥ 1` is discharged via T0's length axiom `#a ≥ 1 for all a ∈ T` applied to `v ∈ T` — matching the per-precondition discharge discipline this proof applies to TA0's four preconditions. T0 (CarrierSetDefinition) — invoked at two sites: (i) to discharge two of OrdinalDisplacement's four preconditions at the invocation site — `m ∈ ℕ` via T0's length operator typing `#·: T → ℕ` instantiated at `v ∈ T`, placing `m = #v ∈ ℕ` ("`m ∈ ℕ` follows from T0's length operator typing `#·: T → ℕ` instantiated at `v ∈ T`, placing `m = #v ∈ ℕ`"), and `m ≥ 1` via T0's length axiom `#a ≥ 1 for all a ∈ T` instantiated at `a = v ∈ T`, yielding `m = #v ≥ 1` ("`m ≥ 1` follows from T0's length axiom `#a ≥ 1 for all a ∈ T` instantiated at `a = v ∈ T`, yielding `m = #v ≥ 1`"), so that every precondition of the invoked OrdinalDisplacement has a named source — the remaining two preconditions `n ∈ ℕ` and `n ≥ 1` transfer directly from OrdinalShift's own preconditions without T0 involvement; and (ii) to place component `vₘ ∈ ℕ` via T0's carrier characterisation of `T` as finite sequences over ℕ, so that NAT-zero's `(A n ∈ ℕ :: 0 ≤ n)` applies at `n = vₘ` for the component-positivity lower bound's first step. The membership `δ(n, m) ∈ T` consumed at TA0's second precondition check is no longer a direct T0 site here — OrdinalShift now cites OrdinalDisplacement's exported postcondition `δ(n, m) ∈ T (by T0)` rather than re-deriving from sequence form and length inline — so T0's role at that site flows transitively through OrdinalDisplacement's contract; T0 nevertheless remains listed for its two direct sites above under the per-step citation discipline. TA-Pos (PositiveTumbler) — invoked at TA0's third precondition check to supply the predicate `Pos(·)` that the check consumes. Although OrdinalShift reads `Pos(δ(n, m))` off OrdinalDisplacement's exported postcondition rather than re-deriving the witness `δₘ = n ≠ 0` inline (the `n ≥ 1 ⟹ n ≠ 0` promotion being discharged inside OrdinalDisplacement's own proof through NAT-addcompat's anchor `0 < 1` and NAT-order's defining clause + transitivity of `<` + irreflexivity), the predicate symbol `Pos(·)` itself is introduced by TA-Pos's Definition `Pos(t) ⟺ (E i : 1 ≤ i ≤ #t : tᵢ ≠ 0)` — OrdinalDisplacement's postcondition merely re-exports what TA-Pos defines at the argument `t = δ(n, m)`. Under the per-step citation discipline — the same discipline this Depends follows by citing T0 directly even though T0 flows transitively through OrdinalDisplacement and TA0, by listing ActionPoint directly even though ActionPoint's value `actionPoint(δ(n, m)) = m` is read off OrdinalDisplacement's exported postcondition rather than re-derived inline, and the same discipline TS1's Depends follows at its analogous `Pos(δ(n, m))` consumption site through OrdinalDisplacement's exported postcondition — TA-Pos must be listed here rather than left to flow transitively through OrdinalDisplacement (whose postcondition only fixes the truth of `Pos(δ(n, m))`, not the defining meaning of the predicate), so that a reviser tightening TA-Pos (its defining existential or its paired `Zero(·)` dual) has Depends-backed visibility into OrdinalShift's TA0-precondition-three consumption site. ActionPoint (ActionPoint) — introduces the operator `actionPoint(·)` consumed at TA0's fourth precondition check ("the action point of δ(n, m) is m = #v by OrdinalDisplacement's exported postcondition `actionPoint(δ(n, m)) = m`, so actionPoint(δ(n, m)) ≤ #v holds trivially"). Although the specific value `actionPoint(δ(n, m)) = m` is read off OrdinalDisplacement's exported postcondition rather than re-derived from ActionPoint's defining minimum-position formula inline, the function symbol `actionPoint(·)` itself is defined by ActionPoint's `actionPoint(w) = min({i : 1 ≤ i ≤ #w ∧ wᵢ ≠ 0})` — OrdinalDisplacement's postcondition merely re-exports the value ActionPoint defines at the argument `w = δ(n, m)`. Under the per-step citation discipline — the same discipline this Depends follows by citing T0 directly at its length operator and carrier characterisation even though T0 flows transitively through OrdinalDisplacement and TA0, and by citing NAT-zero, NAT-addcompat, NAT-closure, and NAT-order at their own consumption sites in the component-positivity lower bound, and the same discipline TA1-strict's Depends follows by listing ActionPoint directly for its `actionPoint(w)` consumption at the length-bound discharge steps — ActionPoint must be listed here rather than left to flow transitively through OrdinalDisplacement (whose postcondition only fixes the value of `actionPoint(δ(n, m))`, not the defining meaning of the operator), so that a reviser tightening ActionPoint (its defining minimum-position formula or its paired upper-bound postcondition `1 ≤ actionPoint(w) ≤ #w`) has Depends-backed visibility into OrdinalShift's TA0-precondition-four consumption site. TA0 (WellDefinedAddition) — invoked to discharge `shift(v, n) = v ⊕ δ(n, m) ∈ T` ("With all TA0 preconditions satisfied, shift(v, n) = v ⊕ δ(n, m) ∈ T"); TA0 also supplies the result-length identity used to conclude `#shift(v, n) = #v`. TumblerAdd (TumblerAdd) — invoked to compute the components of the result via the piecewise prefix-copy / advance / tail-copy rule ("By TumblerAdd: shift(v, n)ᵢ = vᵢ for i < m, shift(v, n)ₘ = vₘ + n, and #shift(v, n) = #δ(n, m) = m = #v"), which yields the postcondition that all components below position `m` are unchanged and position `m` is advanced by `n`; TumblerAdd supplies the equation `shift(v, n)ₘ = vₘ + n` but not the arithmetic lower bound `vₘ + n ≥ 1`, which is discharged downstream through NAT-zero, NAT-addcompat, NAT-closure, and NAT-order. NAT-zero (NatZeroMinimum) — invoked at the first arithmetic step of the component-positivity lower bound: T0 places `vₘ ∈ ℕ`, and NAT-zero's axiom `(A n ∈ ℕ :: 0 ≤ n)` instantiated at `n = vₘ` supplies the lower-bound premise `0 ≤ vₘ` that NAT-addcompat's right order-compatibility consumes to lift into `vₘ + n ≥ 0 + n`. No other listed NAT-* axiom supplies this premise: NAT-order's trichotomy compares `0` and `vₘ` without fixing the winner, and NAT-wellorder names a least element of ℕ without identifying it as `0`. NAT-zero's axiom discharges exactly the `0 ≤ ·` premise at this site, matching the per-step citation convention TumblerAdd applies at its structurally identical `0 ≤ aⱼ` and `0 ≤ aₖ` sites. NAT-addcompat (NatAdditionOrderAndSuccessor) — invoked at the second arithmetic step of the component-positivity lower bound: right order-compatibility (`n ≥ p ⟹ n + m ≥ p + m`, with axiom-`n = vₘ`, axiom-`p = 0`, and axiom-`m = n` — the shift amount filling the axiom's fixed right summand) lifts NAT-zero's `0 ≤ vₘ` into the non-strict `vₘ + n ≥ 0 + n`. Right order-compatibility is the form required here because the proof places the shift amount `n` on the right of both sides of the inequality. NAT-addcompat's order-compatibility clauses deliver only non-strict conclusions, which is compatible with the non-strict target `vₘ + n ≥ 1`; the final composition of two non-strict inequalities is nevertheless routed through NAT-order rather than NAT-addcompat alone, matching the per-step discipline TumblerAdd and TA1 enforce at analogous non-strict sites. NAT-closure (NatArithmeticClosureAndIdentity) — invoked at the third arithmetic step of the component-positivity lower bound: the additive identity `0 + n = n` (instantiated at the shift amount `n`) rewrites the right-hand side of NAT-addcompat's conclusion `vₘ + n ≥ 0 + n` into the non-strict `vₘ + n ≥ n`, setting up the final composition with the precondition `n ≥ 1`. This is the same additive-identity pattern TumblerAdd's dominance and strict-advancement proofs apply at `0 + wₖ = wₖ`. NAT-closure's closure clauses (successor closure, addition closure) are not consumed here — only the additive identity clause `0 + n = n` is — matching the per-clause citation discipline T0's exhaustive NAT-* enumeration establishes. NAT-order (NatStrictTotalOrder) — invoked at the fourth and final arithmetic step of the component-positivity lower bound: the composition of the non-strict `vₘ + n ≥ n` (from NAT-addcompat + NAT-closure) with the precondition `n ≥ 1` into the non-strict `vₘ + n ≥ 1`. NAT-order's defining clause `m ≤ n ⟺ m < n ∨ m = n` instantiated at `m = n, n = vₘ + n` unfolds `vₘ + n ≥ n` into `vₘ + n > n ∨ vₘ + n = n`, and the same clause at `m = 1, n = n` unfolds `n ≥ 1` into `n > 1 ∨ n = 1`; the three cases with at least one strict disjunct — (i) both strict (`vₘ + n > n` and `n > 1`), chaining through NAT-order's transitivity of `<` to yield `vₘ + n > 1`; (ii) the first strict (`vₘ + n > n`) and `n = 1`, substituting the equality `n = 1` into `vₘ + n > n` to yield `vₘ + n > 1`; (iii) the second strict (`n > 1`) and `vₘ + n = n`, substituting the equality `vₘ + n = n` into `n > 1` to yield `vₘ + n > 1` — each yield `vₘ + n > 1`, which the defining clause at `m = 1, n = vₘ + n` folds into the non-strict `vₘ + n ≥ 1` through its strict disjunct; the all-equal case substitutes both equalities to yield `vₘ + n = 1`, which the same defining clause folds into the non-strict through its equality disjunct. Since NAT-addcompat's order-compatibility clauses deliver only non-strict conclusions, this `≥`/`≥ → ≥` composition must be routed through NAT-order (defining clause + transitivity of `<`) rather than discharged by NAT-addcompat alone — the same non-strict composition pattern TumblerAdd applies at its analogous sites, and the only route that respects T0's exhaustive NAT-* enumeration's reservation of transitive composition to NAT-order.
- *Postconditions:* shift(v, n) ∈ T, #shift(v, n) = #v, shift(v, n)ᵢ = vᵢ for i < #v, shift(v, n)_{#v} = v_{#v} + n ≥ 1
