**TA4 (PartialInverse).** `(A a, w : Pos(w) ∧ k = #a ∧ #w = k ∧ (A i : 1 ≤ i < k : aᵢ = 0) : (a ⊕ w) ⊖ w = a)`, where `k` is the action point of `w`.

The precondition has three parts. First, `k = #a` — the action point falls at the last component of `a`. This is necessary because addition replaces `a`'s trailing structure below the action point with `w`'s trailing structure (tail replacement, defined below). When `k < #a`, components `aₖ₊₁, ..., a_{#a}` are discarded by addition and cannot be recovered by subtraction. Concretely: `[1, 5] ⊕ [1, 3] = [2, 3]` (action point 1, position 2 replaced by `w`'s trailing `3`), then `[2, 3] ⊖ [1, 3] = [1, 3] ≠ [1, 5]`.

Second, `#w = k` — the displacement has no trailing components beyond the action point. When `#w > k`, the result acquires trailing components from `w` that were not present in `a`. The trailing `7` from `w` persists through subtraction: `[0, 5] ⊕ [0, 3, 7] = [0, 8, 7]`, then `[0, 8, 7] ⊖ [0, 3, 7]` yields `[0, 5, 7] ≠ [0, 5]`.

Third, `(A i : 1 ≤ i < k : aᵢ = 0)` — all components of `a` before the action point are zero. This ensures the subtraction's divergence-discovery mechanism finds the action point at the right position. If `a` has a nonzero component at some position `j < k`, then the result of addition has `rⱼ = aⱼ ≠ 0`, and the subtraction's divergence falls at `j`, not at `k`. Concretely: `[5, 3] ⊕ [0, 7] = [5, 10]`, then `[5, 10] ⊖ [0, 7]`: divergence at position 1, producing `[5, 10] ≠ [5, 3]`.

When all three conditions hold, recovery is exact. The restriction is not a deficiency but a precise statement of when the operations are inverses.

The precondition `Pos(w)` (TA-Pos) guarantees that the action point `k` exists; by TA-Pos, this excludes every all-zero displacement regardless of length.

*Proof.* We show that under the stated preconditions, the round-trip `(a ⊕ w) ⊖ w` recovers `a` exactly. Throughout, `k` denotes the action point of `w` — the least position `i` with `wᵢ > 0` — so by definition `wᵢ = 0` for all `i < k` and `wₖ > 0`.

**Step 1: the structure of `r = a ⊕ w`.** By TumblerAdd (applicable since `k = #a` gives `k ≤ #a`, satisfying TA0's precondition), the result `r` is built in three regions relative to the action point: `rᵢ = aᵢ` for `i < k` (prefix copy), `rₖ = aₖ + wₖ` (single-component advance), and `rᵢ = wᵢ` for `i > k` (tail copy from displacement). We determine each region under the preconditions.

For `i < k`: the precondition `(A i : 1 ≤ i < k : aᵢ = 0)` gives `rᵢ = aᵢ = 0`.

At `i = k`: `rₖ = aₖ + wₖ`, and since `wₖ > 0` (definition of action point), `rₖ ≥ wₖ > 0`.

For `i > k`: by the result-length identity (TA0), `#r = #w`. The precondition `#w = k` gives `#r = k`, so there are no positions beyond `k` — the tail-copy region is empty. The precondition `k = #a` ensures that no components of `a` beyond position `k` are discarded by tail replacement.

Therefore `r = [0, ..., 0, aₖ + wₖ]` — a tumbler of length `k` with zeros at all positions before `k`.

**Step 2: computing `s = r ⊖ w`.** TumblerSub requires `r ≥ w` (T1). Since `r = a ⊕ w`, TumblerAdd's dominance postcondition gives `r ≥ w`, discharging this obligation. Subtraction scans `r` and `w` for the first divergence after zero-padding both operands to length `L` — the longer of `#r` and `#w`, named by NAT-order's trichotomy on the length pair `(#r, #w)` (sub-case (α) `#r = #w` with `L = #r`; sub-case (β) `#r < #w` with `L = #w`; sub-case (γ) `#w < #r` with `L = #r`) rather than by a primitive binary-maximum operator on ℕ, reproducing TumblerSub's own length-pair dispatch for the same operand pair. Step 1's identity `#r = k` combined with the precondition `#w = k` places `(#r, #w)` in sub-case (α) `#r = #w` with `L = #r = #w`, furnishing `L = k`; both operands are already at this length, so no padding is needed. At each position `i < k`, both `rᵢ = 0` (established above) and `wᵢ = 0` (definition of action point), so `rᵢ = wᵢ` and no divergence occurs before position `k`.

Two cases arise at position `k`. The dichotomy `aₖ > 0 ∨ aₖ = 0` — restricted to two disjuncts rather than admitting a third negative branch `aₖ < 0` that NAT-order's trichotomy on `(0, aₖ)` alone would not exclude — is sourced per step: `aₖ ∈ ℕ` since `k = #a` places position `k` within `a` and `a ∈ T` supplies ℕ-valued components at each position; NAT-zero's axiom `(A n ∈ ℕ :: 0 ≤ n)` instantiated at `n = aₖ` supplies the lower bound `0 ≤ aₖ`; NAT-order's defining clause `m ≤ n ⟺ m < n ∨ m = n` instantiated at `m = 0, n = aₖ` unfolds this lower bound into `0 < aₖ ∨ 0 = aₖ` — equivalently, the dichotomy. This reproduces the NAT-zero + NAT-order unfolding TumblerAdd's two dichotomy sites (the dominance proof's `aⱼ > 0 ∨ aⱼ = 0` for `j < k` and the inner `aₖ > 0 ∨ aₖ = 0` at the action point) and TumblerSub's case (ii) (`aₖ ≠ 0 ⟹ aₖ > 0` at the prefix-divergence zpd point) apply at the structurally identical pattern.

*Case 1: `aₖ > 0`.* Then `rₖ = aₖ + wₖ > wₖ` (since `aₖ > 0`), so `rₖ ≠ wₖ` and the first divergence is at position `k`. Since `rₖ > wₖ`, TumblerSub's precondition `rₖ ≥ wₖ` at the divergence point is satisfied. TumblerSub produces: `sᵢ = 0` for `i < k` (zeroing pre-divergence positions), `sₖ = rₖ - wₖ = (aₖ + wₖ) - wₖ = aₖ` (reversing the advance) — the equality `(aₖ + wₖ) - wₖ = aₖ` is NAT-sub's right-telescoping clause applied to the naturals `aₖ, wₖ ∈ ℕ` — and `sᵢ = rᵢ` for `i > k` (tail copy). Since `#r = k`, there are no positions beyond `k`, so the tail-copy region contributes nothing. The result length is `L = k` (by the trichotomy dispatch at Step 2's opening), giving `s = [0, ..., 0, aₖ]` of length `k`. By the precondition, `aᵢ = 0` for all `i < k` and `#a = k`, so `s = a`.

*Case 2: `aₖ = 0`.* Every component of `a` is zero: `aᵢ = 0` for `i < k` by precondition, and `aₖ = 0` by the case hypothesis, so `a` is the zero tumbler of length `k`. At the action point the construction gives `rₖ = aₖ + wₖ`, and substitution of the case hypothesis `aₖ = 0` yields `rₖ = 0 + wₖ`; NAT-closure's additive identity `(A n ∈ ℕ :: 0 + n = n)` instantiated at `n = wₖ` rewrites the right-hand side to `wₖ`, yielding `rₖ = wₖ` — the same per-step citation convention TumblerAdd's dominance sub-case `aₖ = 0` applies at the structurally identical site (rewriting the same `0 + wₖ` to `wₖ`) and ActionPoint's third postcondition applies at `n = 1` (rewriting `0 + 1` to `1`), rather than elided as a syntactic identification of numerals. Combined with `rᵢ = 0 = wᵢ` for `i < k` and `#r = k = #w`, this yields `r = w`. Now `s = r ⊖ w = w ⊖ w`: the sequences agree at every position, so no divergence exists and TumblerSub yields the zero tumbler of length `L = k` (by the same trichotomy dispatch, unchanged by the case-2 specialisation `r = w` since the length pair `(#r, #w) = (#w, #w)` remains in sub-case (α)). This zero tumbler of length `k` is exactly `a`.

In both cases, `(a ⊕ w) ⊖ w = a`. ∎

*Formal Contract:*
- *Preconditions:* `a ∈ T`, `w ∈ T`, `Pos(w)`, `k = #a`, `#w = k`, `(A i : 1 ≤ i < k : aᵢ = 0)`, where `k` is the action point of `w`
- *Depends:* TA-Pos (PositiveTumbler) — invoked through the precondition `Pos(w)` and consumed by the action-point existence remark ("The precondition `Pos(w)` (TA-Pos) guarantees that the action point `k` exists; by TA-Pos, this excludes every all-zero displacement regardless of length"). ActionPoint (ActionPoint) — invoked at the opening of the proof to fix `k` as "the least position `i` with `wᵢ > 0`" and to license the standing fact `wᵢ = 0` for `i < k` and `wₖ > 0`, both of which are unfolded against TumblerAdd's three regions and TumblerSub's pre-divergence-zero phase. TumblerAdd (TumblerAdd) — invoked at Step 1 to compute `r = a ⊕ w` ("By TumblerAdd (applicable since `k = #a` gives `k ≤ #a`, satisfying TA0's precondition), the result `r` is built in three regions relative to the action point: `rᵢ = aᵢ` for `i < k` (prefix copy), `rₖ = aₖ + wₖ` (single-component advance), and `rᵢ = wᵢ` for `i > k` (tail copy from displacement)"), and again at Step 2 for the dominance postcondition `r ≥ w` ("Since `r = a ⊕ w`, TumblerAdd's dominance postcondition gives `r ≥ w`, discharging this obligation"). TA0 (WellDefinedAddition) — invoked twice: at Step 1 to license TumblerAdd's applicability ("satisfying TA0's precondition") and to supply the result-length identity ("by the result-length identity (TA0), `#r = #w`"); the precondition `#w = k` then collapses the tail-copy region. T1 (LexicographicOrder) — invoked at Step 2's discharge of TumblerSub's input precondition ("TumblerSub requires `r ≥ w` (T1)"); T1 supplies the comparison `≥` against which the dominance fact `r ≥ w` is read. TumblerSub (TumblerSub) — invoked at Step 2 to compute `s = r ⊖ w` via the scan-for-divergence rule ("TumblerSub scans `r` and `w` for the first divergence, zero-padding the shorter to the length of the longer"), and at Cases 1 and 2 for the three-region production rule (`sᵢ = 0` for `i < k`, `sₖ = rₖ - wₖ`, tail copy beyond) used to conclude `s = a`. T3 (CanonicalRepresentation) — invoked implicitly at the closing of both cases when component-by-component agreement and length agreement are converted into tumbler equality ("By the precondition, `aᵢ = 0` for all `i < k` and `#a = k`, so `s = a`" in Case 1; "this yields `r = w`" and "This zero tumbler of length `k` is exactly `a`" in Case 2); T3 supplies the principle that tumblers agreeing in length and at every component are equal. NAT-closure (NatArithmeticClosureAndIdentity) — Case 2 invokes NAT-closure's additive identity `(A n ∈ ℕ :: 0 + n = n)` instantiated at `n = wₖ` to rewrite the right-hand side of `rₖ = 0 + wₖ` — obtained from the construction's action-point clause `rₖ = aₖ + wₖ` by substituting the case hypothesis `aₖ = 0` — to `wₖ`, yielding `rₖ = wₖ`, which combined with `rᵢ = 0 = wᵢ` for `i < k` and `#r = k = #w` feeds T3 to conclude `r = w` (and thereby `s = r ⊖ w = w ⊖ w`, the zero tumbler of length `k = a`). The rewrite is an axiomatic step on ℕ, not a syntactic identification of numerals: without NAT-closure the Case 2 conclusion `r = w` would rest on the additive identity on ℕ as an unsourced primitive. This citation reproduces TumblerAdd's dominance sub-case `aₖ = 0`, where NAT-closure's additive identity licenses the same `0 + wₖ = wₖ` rewrite at the structurally identical site (TumblerAdd lists NAT-closure in its Depends with the matching per-site entry), and ActionPoint's third postcondition, where NAT-closure's additive identity at `n = 1` licenses `0 + 1 = 1` after NAT-discrete's contrapositive yields `0 + 1 ≤ w_{actionPoint(w)}` — preserving the per-step citation discipline across TA4, TumblerAdd, and ActionPoint for the same axiomatic step on ℕ. NAT-zero (NatZeroMinimum) — Step 2's case split `aₖ > 0 ∨ aₖ = 0` invokes NAT-zero's axiom `(A n ∈ ℕ :: 0 ≤ n)` instantiated at `n = aₖ` (with `aₖ ∈ ℕ` since `k = #a` places position `k` within `a` and `a ∈ T` supplies ℕ-valued components at each such position) to supply the lower bound `0 ≤ aₖ`; NAT-order's defining clause `m ≤ n ⟺ m < n ∨ m = n` at `m = 0, n = aₖ` then unfolds this lower bound into `0 < aₖ ∨ 0 = aₖ` — equivalently `aₖ > 0 ∨ aₖ = 0` — which the Case 1 / Case 2 split consumes. Without NAT-zero, NAT-order's trichotomy on `(0, aₖ)` alone would return the broader disjunction `aₖ > 0 ∨ aₖ = 0 ∨ aₖ < 0` (valid on ℤ), and the Step 2 case split's restriction to two disjuncts would have no axiomatic source: NAT-zero is precisely what fixes `0` as a lower bound of ℕ and thereby excludes the `aₖ < 0` branch. No other listed NAT-* axiom supplies this lower bound — NAT-order's trichotomy compares `0` and `aₖ` without fixing the winner, NAT-closure gives closure under `+` and the additive identity without ordering, and NAT-sub's conditional closure supplies partial subtraction on ℕ without identifying `0` as the minimum. The citation matches the per-step convention TumblerAdd's two dichotomy sites (the dominance proof's `aⱼ > 0 ∨ aⱼ = 0` for `j < k` and the inner `aₖ > 0 ∨ aₖ = 0` at the action point — both listed under TumblerAdd's NAT-zero Depends entry with per-site entries), TumblerSub's case (ii) (`aₖ ≠ 0 ⟹ aₖ > 0` at the prefix-divergence zpd point, listed under TumblerSub's NAT-zero entry), and ActionPoint's third postcondition (the `≠ 0 ⟹ ≥ 1` inference on `w_{actionPoint(w)}`, listed under ActionPoint's NAT-zero entry) apply for the structurally identical `0 ≤ ·` lower-bound premise. NAT-order (NatStrictTotalOrder) — invoked at two distinct ℕ-pairs for two distinct roles: one trichotomy instance on the length pair `(#r, #w)` (Step 2's opening) and one defining-clause unfolding of `≤` as `< ∨ =` on the component pair `(0, aₖ)` (Step 2's case-split dichotomy). *Length pair `(#r, #w)`*: trichotomy's exactly-one clause discharges the length-pair dispatch that names the result length `L`, selecting exactly one of sub-case (α) `#r = #w` (`L = #r`), sub-case (β) `#r < #w` (`L = #w`), or sub-case (γ) `#w < #r` (`L = #r`); under Step 1's identity `#r = k` combined with the precondition `#w = k`, the pair falls in sub-case (α), furnishing `L = #r = k`, which both Case 1 and Case 2 consume as the result length of `s = r ⊖ w` — Case 1 reading `s = [0, ..., 0, aₖ]` of length `L = k`, Case 2 reading the zero tumbler of length `L = k` (the case-2 specialisation `r = w` leaves the length pair `(#r, #w) = (#w, #w)` in sub-case (α) unchanged). The citation reproduces TumblerSub's own length-pair dispatch for the same operand pair `(r, w)` rather than treating `max(#r, #w)` or `max(#w, #w)` as an unsourced primitive, and matches the convention that every operator in TumblerSub's Definition must be discharged by a Depends entry. *Component pair `(0, aₖ)`*: the defining clause `m ≤ n ⟺ m < n ∨ m = n` instantiated at `m = 0, n = aₖ` unfolds NAT-zero's lower bound `0 ≤ aₖ` (supplied at NAT-zero's Depends entry above) into the disjunction `0 < aₖ ∨ 0 = aₖ`, i.e., the dichotomy `aₖ > 0 ∨ aₖ = 0` the Case 1 / Case 2 split consumes; without NAT-order's defining clause this case split would rest on a background `<`/`=` trichotomy on ℕ rather than the per-step citation convention the length-pair dispatch site already enforces. The instantiation at the component pair `(0, aₖ)` is a distinct ℕ-pair from the length-pair `(#r, #w)` — trichotomy and defining-clause unfolding are distinct clauses of NAT-order's Axiom, applied at different symbols — so under the per-instance convention this use earns a separate site, matching the per-step convention TumblerAdd's dominance-proof dichotomies (at `(0, aⱼ)` for `j < k` and at `(0, aₖ)` at the action point) and TumblerSub's case (ii) (at `(0, aₖ)` for the prefix-divergence step `aₖ ≠ 0 ⟹ aₖ > 0`) apply at their analogous component-pair sites. NAT-sub (NatPartialSubtraction) — Case 1 invokes NAT-sub's right-telescoping clause to close `(aₖ + wₖ) - wₖ = aₖ` as a single-step natural-number identity; without NAT-sub the step would use partial subtraction on ℕ as an unsourced primitive.
- *Postconditions:* `(a ⊕ w) ⊖ w = a`

Gregory's analysis confirms that `⊕` and `⊖` are NOT inverses in general. The implementation's `absadd` is asymmetric: the first argument supplies the high-level prefix, the second supplies the low-level suffix. When `d = a ⊖ b` strips a common prefix (reducing the exponent), `b ⊕ d` puts the difference in the wrong operand position — `absadd`'s else branch discards the first argument entirely and returns the second. The operand-order asymmetry causes total information loss even before any digit overflow.

The reverse direction is equally necessary:
