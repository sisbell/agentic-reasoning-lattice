# Status Lint — ASN-0036

*Audited: 2026-04-11 14:04*

| Label | Current Status | Recommendation | Reason |
|-------|---------------|----------------|--------|
| Σ.C | introduced | flag ** | Conflicting evidence: the section prose explicitly states "Σ.C is a definition, not a derived property," yet the formal contract labels `Σ.C : T ⇀ Val` as `*Axiom:*`. A pure definition (naming a mathematical object) needs no axiom status, but asserting that the content store *is* a partial function `T ⇀ Val` is a foundational modelling postulate — it constrains the system design and cannot be derived. The prose and the contract contradict each other on whether this is a definition or an axiom. Resolve by deciding: if it is a naming convention, drop the `*Axiom:*` tag and keep `introduced`; if it is a foundational postulate about system structure, change Status to `axiom`. |
| Σ.M(d) | introduced | flag ** | Prose says "Σ.M(d) is a definition, not a derived property" but the formal contract labels the core statement `Σ.M(d) : T ⇀ T` as "*Axiom:*" — these are contradictory classifications. If it is a definition (introducing notation for a modelling choice), the contract should label it *Definition:* and status `introduced` is correct. If it is an axiom (positing structural fact about the system), the contract's label is correct but status should be `axiom` and the prose claim "definition" is wrong. Resolve the contradiction before setting status. |
| S0 | design requirement | ok | S0 asserts a system-wide invariant (content immutability) that constrains all operations. It is not derived from other properties, nor is it a definition or axiom in the mathematical sense — it is a design requirement imposed on the architecture. Current status "design requirement" is correct.` |
| S1 | from S0 | ok | Proof correctly derives domain monotonicity from S0 (content immutability) by extracting the first conjunct; status `from S0` accurately reflects the derivation. |
| S2 | axiom | ok | Status `axiom` is correct — S2 asserts that the arrangement map is a function (each V-position maps to exactly one I-address) as a foundational postulate inherent in the concept of a virtual byte stream, with no derivation from other properties. |
| S3 | design requirement | ok | S3 is a system constraint asserting referential integrity across state transitions; the wp analysis is informal justification, not a derivation from other properties — "design requirement" is correct.` |
| S4 | from GlobalUniqueness, T3 (ASN-0034) | ok | Proof derives S4 from GlobalUniqueness and T3 (ASN-0034) via direct application; status accurately reflects the derivation chain. |
| S5 | consistent with S0, S1, S2, S3 | ok | Proof by construction is present and valid — two witness states are explicitly built and verified against S0–S3, establishing the consistency claim. Status accurately reflects this as a derived result. |
| S6 | from S0 | ok | Proof derives S6 from S0 in two clean steps (domain persistence, then arrangement independence); status `from S0` correctly reflects the derivation. |
| S7a | design requirement | ok | Property explicitly states "this is a design requirement, not a convention"; justified by Nelson's baptism principle (external design authority), not derived from other properties; status correctly reflects its nature as a system constraint. |
| S7b | design requirement | ok | Status `design requirement` is correct: S7b asserts a system-wide constraint ("we require") on address structure with no derivation from other properties — it is a design decision, not a theorem or definition. |
| S7c | design requirement | ok | Status `design requirement` is correct: the section explicitly states "this is a design requirement," provides justification for why δ ≥ 2 is needed but does not derive it from other properties — it is a system constraint imposed by design choice, not a theorem. |
| S7 | from S7a, S7b, S0, S4, T4, T3, GlobalUniqueness (ASN-0034) | ok | Proof present deriving S7 from S7a, S7b, S0, S4, T4, T3, GlobalUniqueness; status correctly reflects derived property with dependencies. |
| S8-fin | design requirement | ok | The justification appeals to "operational reality" and unstated premises about the operational model (finitely many operations, each finite), not to formally established properties within the ASN. This is a system invariant asserted by design, making `design requirement` correct. |
| S8a | axiom | flag ** | Proof present (derives all three conjuncts from T4 + the design assumption that V-positions are element-field tumblers, ending with ∎), so Status `axiom` is wrong for the derived property. But the proof's own opening sentence — "S8a is a design requirement" — contradicts the derivation that follows. The Formal Contract splits correctly (Axiom: V-positions are element-field tumblers; Postconditions: the three conjuncts derived from T4), yet the Status labels the whole property as `axiom` rather than only the foundational assumption. Three conflicting signals: (1) Status says `axiom`, (2) proof text calls S8a "a design requirement", (3) the section contains a complete derivation from T4. Repair should resolve whether S8a is the axiomatic commitment (V-positions are element-field tumblers → `design requirement`) or the derived conjuncts (→ `from T4, S7b`), and split if necessary. |
| S8-depth | design requirement | ok | Property is explicitly a system constraint ("This is a design requirement, not a convention"), justified by implementation necessity (two-blade knife requires uniform depth) and empirical evidence (Gregory's V-address forms), not derived from other properties. Status matches. |
| S8 | theorem from S8-fin, S2, S8a, S8-depth, T1, T3, T5, T10, TA5 (ASN-0034) | ok | Proof present and complete: constructs singleton runs, proves coverage and uniqueness (within-subspace via case analysis on divergence position using T1/T3/TA5/S8-depth, cross-subspace via T5/T10 prefix contiguity). Dependencies in Status match those actually used in the derivation. No conflicting evidence. |
| ord(v) | introduced | ok | Definition: ord(v) is defined as the tail of a tumbler after stripping the subspace identifier. The postconditions derive membership consequences but do not constitute a proof obligation — they follow directly from the definition and S8a. Status "introduced" is correct for a definition.` |
| vpos(S, o) | introduced | ok | Definition: vpos is defined as a construction (prepend S to ordinal), inverse properties are sequence identities derived directly from the definition — Status "introduced" is correct for a definition.` |
| w_ord | introduced | ok | Definition: w_ord defines ordinal projection as a pure function extracting components [w₂, ..., wₘ]; postconditions are immediate consequences of the construction, not separate proof obligations.` |
| OrdAddHom | lemma (from ord, w_ord, TumblerAdd, TA0 (ASN-0034)) | ok | Proof present: component-by-component derivation from TumblerAdd and ord definition, concluding with ∎. Status correctly reflects lemma with dependencies. |
| OrdAddS8a | lemma (from OrdAddHom, S8a, TumblerAdd (ASN-0034)) | ok | Proof present: derives the biconditional from TumblerAdd (component partitioning), S8a on v (prefix positivity), and OrdAddHom (ordinal equivalence). No circularity or conflicting evidence. Status correctly reflects a lemma with its dependencies. |
| OrdShiftHom | corollary of OrdAddHom, OrdAddS8a, OrdinalShift, OrdinalDisplacement (ASN-0034) | ok | Proof derives the result from OrdAddHom via δ(n,m)₁ = 0 and ordinal projection; status correctly reflects corollary with proof present (∎). |
| D-CTG | design requirement | ok | D-CTG is a system constraint asserted without derivation; the proof in the section derives a *consequence* of D-CTG (shared components at depth ≥ 3), using D-CTG as a premise — it does not establish D-CTG itself. |
| D-MIN | design requirement | ok | D-MIN asserts that the minimum V-position has all-1 components; the derivation paragraph uses D-MIN as a premise (not a conclusion), confirming it is posited, not proved. `design requirement` is correct. |
| D-CTG-depth | corollary of D-CTG, S8-fin, S8-depth, T0(a), T1, T3 (ASN-0034) | ok | Proof by contradiction is present and complete: assumes disagreement at component j (2 ≤ j ≤ m−1), constructs infinitely many intermediates via D-CTG and T0(a), contradicts S8-fin. Status correctly lists the dependencies used. |
| D-SEQ | from D-CTG, D-CTG-depth, D-MIN, S8-fin, S8-depth, T1 (ASN-0034) | ok | Proof present and complete: four-step derivation (shared prefix, minimum k, contiguity, finiteness) plus assembly, using exactly the cited dependencies D-CTG, D-CTG-depth, D-MIN, S8-fin, S8-depth, T1 (ASN-0034). Status accurately reflects a derived property. |
| ValidInsertionPosition | introduced | ok | Definition (explicitly labeled "Definition (ValidInsertionPosition)") introducing a predicate; structural claims are verified but the property itself is a definition, not a derived theorem. Status `introduced` is correct. |
| S9 | theorem from S0 | ok | S9 is a theorem derived from S0 by specialization; the proof correctly shows S9's consequent is a special case of S0's unconditional guarantee, and the status `theorem from S0` accurately reflects this. |

*29 properties audited. 3 recommended changes.*
