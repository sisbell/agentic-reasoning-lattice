# Status Lint — ASN-0036

*Audited: 2026-04-11 11:58*

| Label | Current Status | Recommendation | Reason |
|-------|---------------|----------------|--------|
| Σ.C | introduced | flag ** | Section body explicitly calls Σ.C a definition ("Σ.C is a definition, not a derived property"), yet the formal contract labels the partial-function typing `Σ.C : T ⇀ Val` as "*Axiom:*". A pure definition introduces notation and has no truth value; an axiom asserts a foundational claim. These are contradictory classifications — the section cannot be both. If the typing is merely naming a mathematical object, the contract should say *Definition*, not *Axiom*; if it is a substantive postulate (e.g., that the store **must** be a function rather than a relation), then the body should not call it "a definition, not a derived property." Conflicting signals require resolution before Status can be set correctly. |
| Σ.M(d) | introduced | flag ** | Prose says "Σ.M(d) is a definition, not a derived property" but the formal contract labels the core statement `Σ.M(d) : T ⇀ T` as "*Axiom:*" — these conflict. If it is a definition (introducing notation for the partial-function type), the contract's "Axiom" tag is wrong. If it is an axiom (postulating a structural constraint on the model), the prose's "definition" claim is wrong. The Status `introduced` is compatible with either reading, but the two authoritative sources within the section disagree on the property's epistemic character; repair should reconcile the formal contract label with the prose before the Status can be confirmed. |
| S0 | design requirement | ok | S0 asserts a system-wide invariant (content immutability) that constrains all operations; it is not derived from other properties and is not a definition — it is a design requirement, and the current status correctly reflects this.` |
| S1 | from S0 | ok | Proof derives S1 from S0 by extracting the first conjunct of content immutability; status `from S0` correctly reflects the derivation. |
| S2 | axiom | ok | Axiom status is correct: functionality of the arrangement map is asserted as inherent in the concept of a virtual byte stream, not derived from other properties. No proof present or needed — this is a foundational postulate about the system model. |
| S3 | design requirement | ok | S3 is a system constraint asserting referential integrity across state components; the wp analysis is informal justification, not a formal derivation from other properties in this ASN — "design requirement" is correct.` |
| S4 | from GlobalUniqueness, T3 (ASN-0034) | ok | Proof present and sound: derives a₁ ≠ a₂ from GlobalUniqueness (ASN-0034) by instantiation, uses T3 for decidability; status accurately reflects both dependencies. |
| S5 | consistent with S0, S1, S2, S3 | ok | Proof present: two explicit witness constructions (cross-document, within-document) verify S0–S3 for arbitrary N, establishing the consistency claim. Status accurately names the dependencies. |
| S6 | from S0 | ok | Proof present and valid: S6 is derived from S0 by observing that S0's unconditional domain persistence entails arrangement-independence. Status `from S0` correctly reflects this derivation. |
| S7a | design requirement | ok | Property explicitly self-identifies as "a design requirement, not a convention"; justified by appeal to Nelson's baptism principle as external design authority, not derived from other properties — current status is correct. |
| S7b | design requirement | ok | Status `design requirement` is correct: S7b asserts a system-level constraint ("We require that…") about which tumblers may appear in dom(Σ.C); it is not derived from other properties and has no proof — it is a design decision imposed on the system. |
| S7c | design requirement | ok | Status `design requirement` is correct: the section explicitly states this is a design requirement ensuring δ ≥ 2, not derived from other properties but imposed as a structural constraint on the system. |
| S7 | from S7a, S7b, S0, S4, T4, T3, GlobalUniqueness (ASN-0034) | ok | Proof present deriving S7 from S7a, S7b, S0, S4, T4, T3, and GlobalUniqueness (ASN-0034); status accurately lists these dependencies. |
| S8-fin | design requirement | ok | Status `design requirement` is correct: the justification appeals to "operational reality" (finite operations, each adding finitely many V-positions), which are themselves unstated system constraints, not proven properties. This is an informal rationale for a design requirement, not a derivation from established facts. |
| S8a | axiom | flag ** | Status says "axiom" but the section contains a complete proof (with ∎) deriving all three conjuncts from T4; simultaneously the proof's opening sentence calls S8a "a design requirement," contradicting both the axiom status and its own derivation — the axiomatic premise (V-positions are element-field tumblers, per the Formal Contract's Axiom line) is conflated with the derived postconditions (zeros, positivity), which should be separated or the status should reflect that the stated formula is derived, not postulated.` |
| S8-depth | design requirement | ok | Property is explicitly a system constraint ("This is a design requirement, not a convention — parallel to S7a"), no proof derives it from other properties, and the current status `design requirement` correctly reflects this. |
| S8 | theorem from S8-fin, S2, S8a, S8-depth, T1, T3, T5, T10, TA5 (ASN-0034) | ok | Proof present and complete: constructs singleton runs, proves coverage and uniqueness (within and across subspaces) from cited dependencies; status correctly reflects a derived theorem. |
| ord(v) | introduced | ok | Pure definition — names a construction (strip first component) with no truth claim. Status `introduced` is correct for a definition. |
| vpos(S, o) | introduced | ok | Definition: vpos is a construction rule defining how to build a virtual position from a subspace identifier and ordinal; the inverse properties are sequence identities derived directly from the definition, not independent claims requiring proof.` |
| w_ord | introduced | ok | Definition: introduces w_ord as a pure projection extracting components [w₂, ..., wₘ] from a displacement; no truth claim, no proof needed, "introduced" is correct.` |
| OrdAddHom | lemma (from ord, w_ord, TumblerAdd, TA0 (ASN-0034)) | ok | Proof present: component-by-component derivation from TumblerAdd and ord definition, with ∎. Status correctly reflects lemma with cited dependencies. |
| OrdAddS8a | lemma (from OrdAddHom, S8a, TumblerAdd (ASN-0034)) | ok | Proof present: derives the biconditional from TumblerAdd's three-region partition, S8a on v, and OrdAddHom, concluding with ∎. Status correctly cites these dependencies. |
| OrdShiftHom | corollary of OrdAddHom, OrdAddS8a, OrdinalShift, OrdinalDisplacement (ASN-0034) | ok | Proof derives the result from OrdAddHom via δ(n,m)₁=0 and ordinal projection; status correctly reflects corollary with matching dependencies. |
| D-CTG | design requirement | ok | D-CTG itself is a posited system constraint (contiguity of V-positions); the proof in the section derives a *consequence* of D-CTG combined with S8-fin (shared components at depth ≥ 3), not D-CTG itself. Status `design requirement` correctly reflects that D-CTG is asserted, not derived. |
| D-MIN | design requirement | ok | D-MIN asserts the minimum V-position without derivation; the "general form" paragraph derives consequences *from* D-MIN (combined with D-CTG, S8-fin), not D-MIN itself. It is a design constraint on address-space origin, correctly marked `design requirement`. |
| D-CTG-depth | corollary of D-CTG, S8-fin, S8-depth, T0(a), T1, T3 (ASN-0034) | ok | Proof by contradiction is complete: constructs infinitely many intermediates via T0(a)/T3 to contradict S8-fin, correctly deriving shared-prefix from the cited dependencies. |
| D-SEQ | from D-CTG, D-CTG-depth, D-MIN, S8-fin, S8-depth, T1 (ASN-0034) | ok | Proof present and valid: 4-step derivation (shared prefix, minimum k, contiguity, finiteness) correctly derives D-SEQ from the cited dependencies, ending with ∎. Status accurately lists all dependencies used in the proof. |
| ValidInsertionPosition | introduced | ok | Definition (explicitly labeled); the section introduces the ValidInsertionPosition predicate and verifies its structural consistency — no proof obligation exists, and `introduced` is correct for a definition. |
| S9 | theorem from S0 | ok | S9 has a valid proof deriving it as a special case of S0's universal guarantee; "theorem from S0" correctly reflects this derivation. |

*29 properties audited. 3 recommended changes.*
