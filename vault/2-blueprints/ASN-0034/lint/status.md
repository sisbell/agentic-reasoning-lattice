# Status Lint — ASN-0034

*Audited: 2026-04-07 21:57*

| Label | Current Status | Recommendation | Reason |
|-------|---------------|----------------|--------|
| T0(a) | introduced | flag ** | Proof present (valid witness construction, ∎) but Formal Contract labels this "*Axiom:*" — contradictory classification. If derived from the definition of T, contract should list a dependency, not "*Axiom:*"; if genuinely axiomatic, the proof is superfluous and misleading. Resolve the contract label to match the proof status. |
| T0(b) | introduced | ok | Proof present and valid: constructs explicit witness (constant sequence of n ones), verifies membership and length bound, derives T0(b) from the carrier-set axiom. Status `introduced` correctly reflects a derived property. |
| T1 | introduced | ok | Definition of lexicographic order with full proof of strict total order (irreflexivity, trichotomy, transitivity) derived from ℕ properties and T3. Status `introduced` is correct. |
| T2 | introduced | ok | Proof present and valid: derives both claims (intrinsic data, min(#a,#b) bound) from the T1 definition via case analysis on divergence vs. prefix exhaustion. No circularity, no conflicting evidence. Status `introduced` is correct. |
| T3 | introduced | `flag` ** | Proof derives T3 from T0 (extensional sequence equality) with a complete forward/reverse verification ending in ∎, but the Formal Contract labels it `*Axiom:*`. A property with a valid derivation from a prior definition is a corollary, not an axiom. Either the proof is genuine (then the contract should drop the Axiom label) or T3 is truly a postulate (then the proof section is misleading). Status `introduced` is compatible with the proof but incompatible with the contract's Axiom tag — resolve the contradiction before accepting either reading. |
| T4 | introduced | `axiom` ** | T4 is explicitly a design constraint on valid address tumblers ("T4 is an axiom: it constrains which tumblers the system admits as valid addresses"); the formal contract labels it *Axiom*; the proof section derives consequences (a)–(c) *from* the axiom, it does not derive the axiom itself. Status should be `axiom`, not `introduced`. |
| T5 | introduced | ok | Proof present and valid: derives T5 from T1 (lexicographic order) and T3 (distinct lengths ⇒ distinct tumblers) via case analysis and contradiction. Status `introduced` correctly reflects an established property. |
| T6 | corollary of T4 | ok | Proof derives decidability of (a)–(d) from T4's structural constraints via explicit decision procedures; status `corollary of T4` accurately reflects the derivation. |
| T7 | corollary of T3, T4 | ok | Proof is present and complete: case analysis on separator positions derives a ≠ b from T3 (canonical representation) and T4 (positive-component constraint). No conflicting evidence; status correctly cites both dependencies. |
| T8 | introduced | flag ** | Proof present but self-contradictory: the case analysis shows existing operations preserve allocation, yet the proof itself states "The absence of any removal operation is a deliberate design axiom, not a derived property," and the formal contract labels this "*Axiom:* … This is a design constraint, not a derived property." The conclusion (no address is ever removed) is identical to this admitted design axiom, making the derivation circular — the proof asserts as a premise exactly what it claims to establish. Status says `introduced` (derived), but the proof and contract say the core claim is a design constraint accepted without derivation. |
| T9 | lemma (from T10a, TA5) | ok | Proof by induction on allocation gap is present, derives T9 from T10a and TA5(a) (with T1(c) transitivity); status `lemma (from T10a, TA5)` correctly reflects a derived property. |
| T10 | introduced | `ok` ** | Property has a complete, valid proof deriving a ≠ b from non-nesting prefixes via case analysis and T3; Status `introduced` correctly reflects a derived result. |
| T10a | introduced | flag ** | Status says `introduced` (implying derived), but text explicitly states "T10a is a design axiom — it constrains allocator behavior rather than following from prior properties," and the formal contract labels it `*Axiom:*`. The proofs present derive consequences OF T10a (uniform length, non-nesting, length separation), not T10a itself — the property is posited, not established. Three conflicting signals: `introduced` implies derivation, "design axiom" implies behavioral constraint, `*Axiom:*` implies mathematical postulate. Recommend `design requirement` — T10a is an allocation rule justified by necessity, not derived from prior properties. |
| PrefixOrderingExtension | lemma (from T1) | ok | Proof is present and complete: derives a < b from T1 (lexicographic order) via case analysis, correctly eliminating the prefix case using the non-nesting hypothesis. Status `lemma (from T1)` accurately reflects a derived result depending on T1. |
| PartitionMonotonicity | theorem from PrefixOrderingExtension, T1, T3, T5, T9, T10a, TA5 | ok | Proof present and complete: derives PartitionMonotonicity from PrefixOrderingExtension, T1, T5, T9, T10a, TA5 via structured case analysis ending with ∎. Status correctly reflects a theorem with cited dependencies. |
| GlobalUniqueness | theorem from T3, T4, T9, T10, T10a, TA5 | ok | Proof present and valid: four exhaustive cases derive uniqueness from T3, T4, T9, T10, T10a, TA5; status correctly reflects a theorem with its dependencies. |
| T12 | from T1, TA0, TA-strict | ok | Proof present and valid: derives endpoint existence from TA0, non-emptiness from TA-strict, and contiguity from T1 transitivity. Status `from T1, TA0, TA-strict` correctly reflects the dependencies used in the derivation. No conflicting evidence. |
| TA0 | introduced | `ok` ** | Property has a complete constructive proof deriving well-definedness from TumblerAdd definition and tumbler membership; `introduced` correctly reflects a proven property. |
| TA1 | introduced | ok | Property has a complete case-analysis proof deriving the result from T1, T3, TA0, and TumblerAdd; `introduced` correctly reflects a derived property with no conflicting evidence. |
| Divergence | from T1 | `ok` ** | This is a definition (introduces the computation of `divergence(a, b)` by case analysis), and the current status `from T1` correctly indicates it is derived from/defined in terms of T1's case structure. No proof obligation exists for a definition, and the status does not claim `axiom`. |
| TA1-strict | from Divergence, TumblerAdd | ok | Proof derives a ⊕ w < b ⊕ w by case-splitting on k = j vs k > j, using Divergence (agreement below j, strict inequality at j) and TumblerAdd (prefix-copy, single-advance, tail rules). Status `from Divergence, TumblerAdd` correctly reflects these dependencies. |
| TA-strict | from TumblerAdd, T1 | ok | Proof derives TA-strict from the TumblerAdd construction (prefix-copy + additive action point) and T1 (lexicographic ordering witness at position k); status `from TumblerAdd, T1` accurately reflects these dependencies. |
| TA2 | from TumblerSub, T1 | ok | Proof is present and thorough (case analysis deriving membership in T from TumblerSub and T1); status `from TumblerSub, T1` correctly reflects a derived property with its dependencies. |
| TA3 | from TA2, T1, TA6, TumblerSub | ok | Proof present: detailed case analysis derives the conclusion from TA2, T1, TA6, and TumblerSub; status correctly reflects these dependencies; no conflicting evidence. |
| TA3-strict | introduced | ok | Proof present and complete (case analysis on divergence structure, deriving conclusion from T1, TumblerSub, TA2, TA6); `introduced` correctly reflects a derived property. |
| TA4 | from TumblerAdd, TumblerSub | ok | Proof derives the round-trip identity from TumblerAdd (Step 1, structure of r) and TumblerSub (Step 2, case analysis on aₖ), concluding with ∎. Status correctly cites both dependencies. |
| ReverseInverse | corollary of TA3-strict, TA4, TumblerAdd, TumblerSub | ok | Proof present: three-step derivation (subtraction structure, TA4 partial inverse, contradiction via TA3-strict) closes with ∎; status correctly reflects established property with dependency citations. |
| TumblerAdd | introduced | `ok ** | Section is explicitly labeled "Definition (TumblerAdd)" — it introduces a computation rule (how to compute a ⊕ w), assigns meaning rather than asserting truth, and requires no proof. Status "introduced" is appropriate for a definition.` |
| TumblerSub | from Divergence, T1 | introduced ** | TumblerSub is explicitly labeled "Definition (TumblerSub)." — it is a constructive definition introducing the ⊖ operation, not a theorem derived from Divergence and T1. Definitions assign meaning; they have no truth value and require no proof. The subsequent TA1/TA3/TA4 verifications are proofs of other properties that use this definition, not proofs of the definition itself. Status should reflect that this is an introduced definition, not a derivation. |
| TA5 | introduced | ok | Proof present: constructs `inc(t,k)` by cases and verifies all four postconditions (a)–(d) from T1 and the construction, closing with ∎. Status `introduced` correctly reflects a derived property. |
| TA6 | from T1, T4 | ok | Proof derives both conjuncts from T1 (ordering definition) and T4 (positive-component constraint) via case analysis; status `from T1, T4` correctly reflects the derivation. |
| PositiveTumbler | introduced | ok | Pure definition (introduces "positive tumbler" and "zero tumbler" terminology); `introduced` is correct for a definition. |
| TA7a | introduced | ok | Proof present (derives ⊕-closure from TA0, ⊖-closure from TA2, with case analysis via TumblerAdd/TumblerSub constructive definitions); `introduced` correctly reflects a proven property. |
| TA-assoc | theorem from TumblerAdd, T3 | ok | Proof present: exhaustive case analysis on k_b vs k_c derives associativity component-wise from TumblerAdd's constructive definition, concludes equality via T3. Status correctly reflects theorem with primary dependencies. |
| TA-LC | lemma (from TumblerAdd, T3) | ok | Proof present and complete: derives x = y from TumblerAdd (component-wise determination) and T3 (canonical representation equality), matching the cited dependencies exactly. |
| TA-RC | lemma (from TumblerAdd, T3) | ok | Proof constructs an explicit counterexample and verifies a ⊕ w = b ⊕ w by direct computation from TumblerAdd and a ≠ b from T3; status `lemma (from TumblerAdd, T3)` correctly reflects the derivation. |
| TA-MTO | lemma (from TumblerAdd, T3) | ok | Proof present and sound: derives the biconditional from TumblerAdd's component rules and T3 (CanonicalRepresentation) via case analysis on positions; status `lemma (from TumblerAdd, T3)` correctly reflects the derivation. |
| D0 | from T3, TA0, TumblerAdd, TumblerSub | ok | Proof present and thorough: derives displacement well-definedness step-by-step from cited properties; Status correctly reflects a derived property. |
| D1 | lemma (from TumblerAdd, TumblerSub, T3, Divergence) | ok | Proof is present and complete; derives the round-trip identity component-by-component from the cited dependencies. Status correctly reflects a proven lemma. |
| D2 | corollary of D1, TA-LC | ok | Proof derives uniqueness from D1 (second witness) and TA-LC (left cancellation); status `corollary of D1, TA-LC` accurately reflects both dependencies. |
| OrdinalDisplacement | introduced | `ok ** | Pure definition — introduces notation δ(n,m) for a specific tumbler construction. Status "introduced" is appropriate for a definition.` |
| OrdinalShift | introduced | ok | Definition — introduces `shift(v, n) = v ⊕ δ(n, m)` as a named construction; the subsequent text derives consequences from TumblerAdd, not a proof of the definition itself. `introduced` is correct for a definition. |
| TS1 | lemma (from TA1-strict) | `ok` ** | Proof is present and complete: derives the result by verifying the four preconditions of TA1-strict and applying it. Status `lemma (from TA1-strict)` correctly reflects a derived property with its principal dependency. |
| TS2 | lemma (from TA-MTO, T3) | `ok` ** | Proof is present and complete: derives injectivity from TA-MTO and T3, matching the stated status `lemma (from TA-MTO, T3)`. |
| TS3 | lemma (from TumblerAdd, T3) | `ok ** | Proof present: expands OrdinalShift, applies TumblerAdd componentwise, uses ℕ associativity, concludes by T3 (CanonicalRepresentation). Status "lemma (from TumblerAdd, T3)" correctly reflects the derivation and its dependencies.` |
| TS4 | corollary of TA-strict | `ok` ** | Proof derives TS4 from TA-strict (with OrdinalShift and OrdinalDisplacement), verifying both preconditions; Status `corollary of TA-strict` correctly reflects this derivation. |
| TS5 | corollary of TS3, TS4 | `ok ** | Proof derives TS5 from TS3 (ShiftComposition) and TS4 (ShiftStrictIncrease) via composition then strict increase; status "corollary of TS3, TS4" accurately reflects this.` |

*47 properties audited. 16 recommended changes.*
