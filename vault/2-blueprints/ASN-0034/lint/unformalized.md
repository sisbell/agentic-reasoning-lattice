# Unformalized Properties — ASN-0034

*Scanned: 2026-04-07 18:21*

Two unformalized definitions:

```
LINE 665 | defines the *action point* of a displacement as k = min({i : 1 ≤ i ≤ n ∧ wᵢ ≠ 0}), used in the preconditions and formal contracts of TA0, TA1, TA1-strict, TA-strict, TA2, TA4, D0, D1, OrdinalDisplacement, and all TS properties | missing both bold header and table entry

LINE 1536 | defines the *last significant position* sig(t) = max({i : 1 ≤ i ≤ #t ∧ tᵢ ≠ 0}), used in TA5's construction, formal contract, and postcondition (c) | missing both bold header and table entry
```
