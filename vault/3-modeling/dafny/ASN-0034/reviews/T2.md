# T2 — Contract FLAG

*2026-04-09 08:51*

Postcondition (b) — complexity bound `min(#a, #b)` — has no ensures clause

The formal contract specifies:
  *Postconditions:* (b) At most `min(#a, #b)` component pairs are examined.

The Dafny code has:
  No `ensures` on `IntrinsicComparison` or `CompareAt` bounding the number of pairs examined. The three `ensures` clauses on `IntrinsicComparison` cover only postcondition (a) — correspondence with T1.

Missing:
  The complexity bound is *structurally implied* by `CompareAt`'s recursion (it increments `i` from 0 and terminates when `i == |a|` or `i == |b|` or divergence), but the contract explicitly lists it as a postcondition and it is expressible in Dafny — e.g., via a depth-counting function with an ensures `<= if |a| <= |b| then |a| else |b|`. As stated, the `decreases |a| - i` clause proves termination but is looser than the `min(|a|, |b|)` bound the contract requires.

Postconditions (a) and (c) are clean: (a) is covered by the three `LT/EQ/GT <==>` ensures; (c) and the frame condition are encoded by `Compare`/`CompareAt` being pure `function`s whose only parameters are the two tumblers.
