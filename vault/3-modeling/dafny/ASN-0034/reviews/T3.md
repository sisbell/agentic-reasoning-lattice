# T3 — Contract FLAG

*2026-04-09 08:38*

Axiom encoded as lemma (proved) instead of assumed

The formal contract specifies:
  *Axiom:* Tumbler equality is sequence equality: a = b ⟺ #a = #b ∧ (A i : 1 ≤ i ≤ #a : aᵢ = bᵢ).
  No quotient, normalization, or external identification is imposed on T.

The Dafny code has:
  lemma CanonicalRepresentation(a: Tumbler, b: Tumbler)
    ensures (a == b) <==> (|a.components| == |b.components| && ...)
  { }

Missing/extra/wrong:
  T3 is labeled *Axiom* in the formal contract. The Dafny encoding uses
  a `lemma` with an empty body, which Dafny auto-verifies via structural
  datatype equality. Per validation rules, axioms must be `axiom` or
  `assume`, not `lemma`. The encoding should use `lemma {:axiom}` with
  no body, or an `assume` statement, to mark this as an assumed axiom
  rather than a derived result.

  Note: the ensures clause is semantically correct (0-based indexing is
  the standard encoding of the 1-based contract). The only issue is the
  construct type.
