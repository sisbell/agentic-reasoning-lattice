# Divergences — D10a (PublishOperation)

- **Line 11**: account(d) = actor(op) precondition omitted. Authorization is a protocol-layer constraint; the state transformation is independent of account structure.
