# Divergences — Bop (FRAME) (OnlyRegistryModified)

- **Line 10**: The BaptismState used in RegistryGrowth has only field B, making the frame trivially structural. This module models a richer SystemState with an opaque rest component to give the frame condition non-trivial content: baptize preserves rest.
