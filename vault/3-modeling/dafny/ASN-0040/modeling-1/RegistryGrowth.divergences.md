# Divergences — Bop (POST) (RegistryGrowth)

- **Line 44**: The ASN defines next(B, p, d) as: if children = ∅ then inc(p, d) else inc(max(children), 0). This model uses the B2-equivalent: next = StreamElement(p, d, |children|+1). Equivalence holds when B1 (ContiguousPrefix) is satisfied.
