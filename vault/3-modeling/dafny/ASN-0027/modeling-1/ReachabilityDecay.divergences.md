# Divergences — A9 (ReachabilityDecay)

- **Line 13**: The ASN requires Σ' to be reachable from Σ via a finite sequence of DELETE operations. We prove the weaker existential: a state exists where a is unreachable. Formalizing the operational trace (iterated A2 steps) is deferred.
