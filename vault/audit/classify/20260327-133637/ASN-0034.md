# Classification Audit — ASN-0034

*Audited: 2026-03-27 13:36*

| Label | Current Status | Recommendation | Reason |
|-------|---------------|----------------|--------|
| T0 | axiom | ok | T0 explicitly posits the carrier set by definition, not by derivation; the formal contract marks it as an axiom; Status `axiom` is correct. |
| T0(a) | introduced | `ok` ** | Property has a complete constructive proof that exhibits a witness t' for arbitrary t, i, M, verifies membership in T, agreement on other components, and the bound condition; `introduced` correctly reflects a derived property. |
| T0(b) | introduced | ok | Proof is present and valid: for arbitrary n ≥ 1, constructs witness t = 1.1.….1 (n ones), verifies t ∈ T and #t = n ≥ n, closing the universal quantifier. The formal contract's "Axiom" line identifies the premise (definition of T), not the conclusion — no circularity. Status `introduced` is appropriate for a derived property. |
| T1 | introduced | ok | T1 introduces a definition (lexicographic order) and provides a complete proof that it is a strict total order (irreflexivity, trichotomy, transitivity); `introduced` is appropriate for a property that defines and establishes its claims. |
| T2 | introduced | ok | Proof present: detailed case analysis derives both claims (intrinsic computability and min(#a,#b) bound) from T1's definition and T3, ending with ∎. Status `introduced` correctly reflects a property established by proof in this ASN. |
| T3 | from T0(a) | ok | Proof derives T3 (extensional sequence equality) from T0(a)'s definition of tumblers as finite sequences, using standard sequence equality and Leibniz's law; status `from T0(a)` accurately reflects this derivation with no conflicting evidence. |
| T4 | introduced | axiom ** | T4 explicitly self-identifies as an axiom ("T4 is an axiom: it constrains which tumblers the system admits as valid addresses"), the formal contract labels the core constraints as "*Axiom:*", and the proofs present derive consequences (a)(b)(c) from the axiom rather than deriving the axiom itself; status should be `axiom`, not `introduced`. |
| T5 | introduced | ok | Proof present: two-case analysis (Case 1: #b ≥ #p by contradiction on divergence position; Case 2: #b < #p shown impossible) derives T5 from T1 and T3. No conflicting evidence; `introduced` is correct for a derived property. |
| T6 | corollary of T4 | ok | Proof present: detailed derivation from T3 (componentwise comparison) and T4 (field extraction), establishing terminating decision procedures for all four cases. Status `corollary of T4` correctly reflects that T6 is derived, not postulated. |
| T7 | corollary of T3, T4 | ok | Proof present and sound: T7 is derived from T3 and T4 via case analysis on separator positions; status `corollary of T3, T4` correctly reflects the derivation. |
| T8 | introduced | `flag` ** | Proof text states "The absence of any removal operation is a deliberate design axiom, not a derived property"; formal contract labels this "*Axiom:* … This is a design constraint, not a derived property"; yet Status says `introduced` (derived). The case analysis is valid *given* the design constraint, but the constraint itself — that no deallocation operation exists — is the very content of T8, admitted as an axiom within its own proof. This is circular: the conclusion (permanence) is assumed as a premise (design axiom: no removal). T8 should be `design requirement`. |
| T9 | lemma (from T10a, TA5) | ok | Proof by induction on index gap is present and valid, deriving T9 from T10a (allocation mechanism), TA5(a) (strict increase), and T1(c) (transitivity); status correctly reflects a derived lemma. |
| T10 | introduced | ok | Property has a complete, rigorous case-analysis proof deriving a ≠ b from the non-nesting prefix hypothesis via T3 contrapositive; `introduced` correctly reflects a derived property. |
| T10a | introduced | `design requirement` ** | T10a is explicitly "a design axiom: it constrains allocator behavior rather than following from prior properties"; the formal contract labels it *Axiom*; the proofs derive consequences FROM T10a but do not derive T10a itself — it is an imposed allocation rule, not a derived property, so `introduced` understates its axiomatic status. |
| PrefixOrderingExtension | lemma (from T1) | ok | Proof is present and valid: derives a < b for arbitrary extensions from T1 lexicographic order and the prefix relation, correctly eliminating the proper-prefix case via the non-nesting hypothesis. Status `lemma (from T1)` accurately reflects a proven result depending on T1. |
| PartitionMonotonicity | theorem from PrefixOrderingExtension, T1, T3, T5, T9, T10a, TA5 | ok | Proof present and complete: derives partition monotonicity from PrefixOrderingExtension, T1, T5, T9, T10a, TA5 via case analysis on same/cross sub-partition addresses, concluding with ∎. Status correctly reflects a theorem. |
| GlobalUniqueness | theorem from T3, T4, T9, T10, T10a, TA5 | ok | Proof present and sound: four exhaustive cases derive `a ≠ b` from the cited dependencies; status correctly reflects a theorem with proof. (T1 used in Case 1 but not cited in status — dependency citation accuracy is a separate concern.) |
| T12 | from T1, TA0, TA-strict | ok | Proof present deriving all three postconditions (endpoint existence from TA0, non-emptiness from TA-strict, contiguity from T1 transitivity); status `from T1, TA0, TA-strict` accurately reflects the dependencies. |
| TA0 | introduced | ok | Proof present and valid: derives TA0 from T0 (carrier-set) and TumblerAdd (constructive definition) by verifying length ≥ 1 and component membership in ℕ across all three regions. No conflicting evidence. |
| TA1 | introduced | ok | Proof present: detailed case analysis (prefix case, divergence at j<k, j=k, j>k) derives the postcondition from T1, TumblerAdd, TA0, and T3; no circularity or conflicting evidence; `introduced` correctly reflects an established property. |
| Divergence | from T1 | `ok` ** | Definition of divergence function — computes a value from two tumblers. Status `from T1` correctly indicates this definition is derived from (structured around the cases of) property T1. No proof needed; no conflicting evidence. |
| TA1-strict | from Divergence, TumblerAdd | ok | Proof present and complete: two-case analysis (k=j, k>j) derives a⊕w < b⊕w from Divergence, TumblerAdd, TA0, and T1 without circularity; status correctly reflects derived property. |
| TA-strict | from TumblerAdd, T1 | ok | Proof derives TA-strict from TumblerAdd (prefix-copy + single-component advance) and T1 (lexicographic witness at position k); status `from TumblerAdd, T1` accurately reflects the derivation. |
| TA2 | from TumblerSub, T1 | ok | Proof present and thorough: derives TA2 from TumblerSub (construction) and T1 (order) via case analysis, showing the result is in T. Status `from TumblerSub, T1` correctly reflects the dependencies used. |
| TA3 | from TA2, T1, TA6, TumblerSub | ok | Proof present: detailed case analysis (Cases A and B with sub-cases) derives TA3 from T1, TA2, TA6, TumblerSub; status correctly lists these dependencies; no conflicting evidence. |
| TA3-strict | introduced | ok | Proof present: rigorous case analysis derives the conclusion from T1, TumblerSub, TA2, and TA6; `introduced` correctly reflects a derived property. |
| TA4 | from TumblerAdd, TumblerSub | ok | Proof present and complete: detailed step-by-step derivation from TumblerAdd/TumblerSub definitions with case analysis on aₖ, concluding with ∎. No conflicting evidence — formal contract matches proof, no circularity. |
| ReverseInverse | corollary of TA3-strict, TA4, TumblerAdd, TumblerSub | ok | Proof present and valid: derives `(a ⊖ w) ⊕ w = a` from TA3-strict, TA4, TumblerAdd, TumblerSub via contradiction, concluding with ∎. No conflicting evidence. |
| TumblerAdd | introduced | ok | Definition with explicit computation rule and no truth claim; `introduced` is correct for a definition. |
| TumblerSub | from Divergence, T1 | ok | TumblerSub is a definition (introduces the ⊖ operation via a computational rule), and `from Divergence, T1` correctly records that the definition is constructed in terms of the Divergence concept and the T1 ordering — this is a dependency citation for a definition, not a proof-status claim, so no change is needed.

Wait — let me reconsider. The status format `from X, Y` in this system denotes derivation (a property established from other properties), not definitional dependency. TumblerSub is explicitly labeled **Definition (TumblerSub)** and gives a computation rule assigning meaning to ⊖. It has no truth value to prove. The proofs in the section (TA1, TA3, TA4) are verifications of *other* properties about TumblerSub, not proofs of TumblerSub itself.

```
introduced | TumblerSub is a definition (computation rule for ⊖), not a derived property; `from Divergence, T1` incorrectly implies derivation — should be `introduced` with Divergence and T1 as definitional dependencies |
| TA5 | introduced | ok | Definition of `inc(t, k)` with a complete constructive proof verifying all four postconditions (a)–(d) from T1 and the construction; no conflicting evidence. `introduced` is correct. |
| TA6 | from T1, T4 | ok | Proof derives both conjuncts from T1 (lexicographic order) and T4 (positive-component constraint); status `from T1, T4` correctly reflects the proof dependencies. |
| PositiveTumbler | introduced | ok | Definition with proof establishing a derived consequence; Status `introduced` correctly reflects that the property is defined and its ordering claim is proved from T1. |
| TA7a | introduced | ok | Property has a detailed proof deriving T-closure from TA0 and TA2 via case analysis, with S-membership analyzed constructively; `introduced` is appropriate for a proved property. |
| TA-assoc | theorem from TumblerAdd, T3 | ok | Detailed componentwise proof by exhaustive case analysis on action-point ordering, correctly derives associativity from TumblerAdd and T3 with ∎; status accurately reflects a theorem with proof. |
| TA-LC | lemma (from TumblerAdd, T3) | ok | Proof present and complete: derives x = y from TumblerAdd's component rules and T3 via case analysis on action points, then component-wise and length equality. Status `lemma (from TumblerAdd, T3)` correctly reflects a derived result depending on TumblerAdd and T3. |
| TA-RC | lemma (from TumblerAdd, T3) | ok | Proof present: constructive witness exhibition derives the existential claim from TumblerAdd and T3 by direct computation; status `lemma` is appropriate. |
| TA-MTO | lemma (from TumblerAdd, T3) | ok | Full biconditional proof derives the equivalence from TumblerAdd's componentwise expansion and T3's equality criterion; status correctly reflects a derived lemma. |
| D0 | from T3, TA0, TumblerAdd, TumblerSub | ok | Proof present and complete: derives all four postconditions (well-defined subtraction, positivity, action point, well-defined addition) from cited properties via step-by-step argument ending with ∎. Status correctly reflects derived property. |
| D1 | lemma (from TumblerAdd, TumblerSub, T3, Divergence) | `ok` ** | Detailed component-wise proof derives the round-trip property from TumblerAdd, TumblerSub, T3, and Divergence; status `lemma (from ...)` correctly reflects a proven result with its dependencies. |
| D2 | corollary of D1, TA-LC | ok | Proof present and valid: derives w = b ⊖ a from D1 (second witness), D0/TA0 (well-definedness), and TA-LC (left cancellation) in a standard uniqueness-by-cancellation argument. Status `corollary of D1, TA-LC` correctly reflects the derivation. |
| OrdinalDisplacement | introduced | ok | Pure definition — introduces notation δ(n,m) for a specific tumbler construction. No truth claim, no proof needed. Status `introduced` is correct. |
| OrdinalShift | introduced | `ok` ** | Definition with derived postconditions: the section defines shift(v,n) = v ⊕ δ(n,m) and derives its properties from TumblerAdd and OrdinalDisplacement — `introduced` is correct for a definition whose consequences are proved from prior results. |
| TS1 | lemma (from TA1-strict) | ok | Proof present and complete: all four preconditions of TA1-strict are explicitly verified, and the status `lemma (from TA1-strict)` correctly reflects derivation from that property. |
| TS2 | lemma (from TA-MTO, T3) | ok | Proof present and valid: reduces shift to tumbler addition via definitions, applies TA-MTO forward direction for componentwise agreement, assembles full equality via T3. Status `lemma (from TA-MTO, T3)` correctly reflects the derivation chain. |
| TS3 | lemma (from TumblerAdd, T3) | ok | Proof is present and complete: expands definitions, applies TumblerAdd componentwise, invokes ℕ associativity, and concludes via T3. Status `lemma (from TumblerAdd, T3)` correctly reflects a derived property with its key dependencies. |
| TS4 | corollary of TA-strict | ok | Proof is present and complete: reduces shift to tumbler addition via OrdinalShift, verifies both preconditions of TA-strict, and concludes. Status `corollary of TA-strict` correctly reflects derivation from that property. |
| TS5 | corollary of TS3, TS4, TA0, OrdinalShift, OrdinalDisplacement | ok | Proof present and valid: decomposes n₂ into n₁ + d, applies TS3 (composition) then TS4 (strict increase) on the intermediate result, with TA0 confirming length preservation. Status correctly reflects derivation from cited dependencies. |

*48 properties audited in 589s. 7 recommended changes.*
