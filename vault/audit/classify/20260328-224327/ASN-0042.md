# Classification Audit — ASN-0042

*Audited: 2026-03-28 22:43*

| Label | Current Status | Recommendation | Reason |
|-------|---------------|----------------|--------|
| O0 | introduced | design requirement ** | O0 asserts that ownership is decidable without mutable state and names prefix containment as the decision procedure — this is a system design constraint, not derived from other properties, and not a pure definition (it asserts a truth about the system's behavior). |
| O1 | introduced | ok | Definition: `≡` introduces the `owns` relation as a definitional equivalence over prefix matching. No proof needed; `introduced` is correct. |
| O1a | introduced | design requirement ** | O1a asserts a constraint on principal structure (zeros(pfx(π)) ≤ 1) justified entirely by implementation evidence (tumbleraccounteq stopping at second zero, absence of finer-grained authorization in codebase) and Nelson's design intent — no mathematical derivation from prior properties or axioms. |
| O1b | introduced | design requirement ** | Asserts prefix injectivity on Π with no proof — only motivational justification ("without injectivity, ω would not yield a unique result"). This is a constraint on how the system allocates principal prefixes, not a derivation from other properties. |
| O2 | introduced | ok | Proof present: well-definedness of ω derived from O4 (coverage), an inline proof that covering prefixes are totally ordered, finiteness, and O1b (unique principal per longest prefix). Status `introduced` correctly reflects an established property. |
| O3 | introduced | ok | Property has a proof deriving it from T8 (address permanence), O13 (PrefixImmutability), and O12 (PrincipalPersistence); `introduced` is correct for a derived property. |
| O4 | introduced | ok | Proof present: O4 is derived by induction from O5, O12, O13, O14, O16; status `introduced` is consistent with a proven property. |
| O5 | introduced | design requirement ** | O5 asserts a system constraint on who may allocate — justified by Nelson's design intent and Gregory's implementation, not derived from other properties. The Delegation definition *references* O5 (condition ii) as a foundational constraint; it does not prove it. |
| AccountPrefix | introduced | `flag` ** | No section content found — cannot verify whether a proof, definition, axiom, or constraint exists; the property needs its section text to assess its status. |
| O6 | introduced | ok | Proof present: detailed case analysis deriving O6 from AccountPrefix, FieldParsing, T4, O1a, O17, and prefix transitivity; `introduced` is appropriate for a derived property. |
| O7 | introduced | ok | Postcondition (a) is derived from the `delegated` relation conditions (i, vi) and O2; postconditions (b, c) state inherited rights. Status `introduced` correctly reflects a property established by proof within this ASN. |
| O8 | introduced | ok | Proof sketch derives O8 from O3, O12, O13 (prefix length is permanent, only delegate can sub-delegate); `introduced` is acceptable for a derived property. |
| O9 | introduced | ok | Proof by case analysis on zeros(pfx(π)) derives the property from O1a, T4, and T10a; `introduced` is correct for a derived property. |
| O10 | introduced | ok | Property is introduced with a substantive existence proof deriving from O1a, O6, O15, T0a, TA5(d) via case analysis on zeros(pfx(π)); part (b) (original unchanged) follows from the construction being a fresh allocation, not a modification. Status `introduced` correctly reflects a derived property. |
| O11 | introduced | `axiom` ** | O11 explicitly declares principal identity as "an axiom of the session, not a theorem of the ownership model" — it is a foundational postulate accepted without proof, so Status should be `axiom` rather than `introduced`. |
| O12 | introduced | `design requirement` ** | The property asserts a system invariant (no operation removes a principal) justified by architectural arguments (no revocation concept, no deletion path, would violate other properties) but is not derived from axioms or definitions within this ASN — it is a design constraint on system behavior. |
| O13 | introduced | design requirement ** | Justification bottoms out at "an operation the system does not support" — a system design constraint, not a mathematical derivation. The appeal to tumbler algebra lacking mutation and T8 permanence explains *why the design works*, but the immutability guarantee is ultimately enforced by the system refusing to provide the operation, which is a design decision. |
| O14 | introduced | design requirement ** | O14 stipulates what the initial state Π₀ must satisfy — non-emptiness, coverage, prefix constraints, T4, pairwise non-nesting — as base cases enabling inductive proofs of O1a, O1b, T4, O4. The prose provides informal justification (single-node and multi-node scenarios showing satisfiability) but no formal derivation from other properties; the constraints are design requirements on system initialization, not theorems. |
| O15 | introduced | `design requirement` ** | O15 asserts a closure constraint on principal introduction (bootstrap or delegation only, at most one per transition) justified by appeal to Nelson's design intent and Gregory's codebase — not derived from other properties. This is a system constraint imposed by design, not a theorem. |
| O16 | introduced | `design requirement` ** | O16 asserts a system-wide closure property (every newly allocated address was allocated by some principal) with no proof deriving it from other properties or axioms in this ASN. The justification is an appeal to system architecture ("Gregory confirms: every allocation path in udanax-green originates from a session with an account tumbler") — this is an empirical claim about implementation behavior, not a mathematical derivation. It constrains how the system must behave rather than following from the formal apparatus. |
| O17 | introduced | axiom ** | Text explicitly identifies O17 as an axiom twice ("This axiom is load-bearing", "the ownership model requires as an axiom"), derives nothing from prior properties, and defers the proof obligation to the baptism specification (out of scope). Status should be `axiom`, not `introduced`. |
| ω(a) | introduced | flag ** | No section content found — cannot verify whether the property has a proof, is a definition, or is an axiom. The status `introduced` cannot be confirmed or corrected without the actual section text. |
| dom(π) | introduced | flag ** | No section content found — cannot verify whether a proof, definition, axiom, or constraint exists. The property needs manual inspection to determine its correct status. |
| acct(a) | introduced | `flag` ** | No section content found — cannot verify whether the property has a proof, is a definition, or is an axiom; needs manual inspection to determine correct status. |
| allocated_by_Σ(π, a) | introduced | `flag` ** | No section content found — cannot verify whether the property has a proof, is a definition, is an axiom, or is a system constraint. The status `introduced` cannot be confirmed or corrected without the actual section text. |
| delegated_Σ(π, π') | introduced | flag ** | No section content found — cannot verify whether a proof exists, whether this is a definition/axiom/constraint, or whether the status is correct. The property needs manual inspection. |
| pfx(π) | introduced | flag ** | No section content found — cannot verify whether pfx(π) has a proof, is a definition, or is an axiom. The status `introduced` cannot be confirmed or corrected without the actual section text. |

*27 properties audited in 333s. 18 recommended changes.*
