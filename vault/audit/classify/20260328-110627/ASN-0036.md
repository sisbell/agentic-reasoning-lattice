# Classification Audit — ASN-0036

*Audited: 2026-03-28 11:06*

| Label | Current Status | Recommendation | Reason |
|-------|---------------|----------------|--------|
| Σ.C | introduced | ok | Σ.C is a definition — it names a partial function and specifies its signature. No proof needed, no axiom asserted. Status `introduced` is correct for a definition. |
| Σ.M(d) | introduced | ok | Definition: introduces Σ.M(d) as a partial function T ⇀ T mapping V-positions to I-addresses. No truth claim, no proof needed. `introduced` is correct. |
| S0 | introduced | `design requirement` ** | S0 asserts a system-wide invariant ("once content is stored… fixed for all future states") that constrains every operation, but provides no derivation from other properties or axioms — the section explains *what* S0 means and *why* it matters, not *that* it follows from something else. It is a design constraint on the system, not a derived theorem and not a foundational axiom of mathematics. Current status `introduced` should be `design requirement`. |
| S1 | corollary of S0 | ok | S1 (domain monotonicity) follows from S0 (content immutability) by trivial implication: if no entry can be removed, the domain can only grow. The section's "design choice" language describes why S0 holds, not S1's dependency on S0 — the corollary relationship is valid regardless of S0's own foundational status. |
| S2 | introduced | `axiom` ** | Functionality of M(d) is asserted as "inherent in the concept" of a virtual byte stream, not derived from other properties; it is a foundational modeling postulate accepted without proof. |
| S3 | introduced | design requirement ** | S3 asserts a system invariant (no dangling V-references) that is imposed on state transitions, not derived from other properties; the section's wp analysis explains what is needed to *maintain* S3, confirming it is a constraint the system must satisfy. |
| S4 | from GlobalUniqueness (ASN-0034) | ok | Proof present: S4 is derived from GlobalUniqueness (ASN-0034) via direct implication; status correctly reflects this dependency. |
| S5 | consistent with S0–S3 (witness construction) | ok | Witness construction is present and valid — constructs explicit states Σ_N and Σ'_N satisfying S0–S3 with arbitrarily large sharing multiplicity, establishing the consistency claim. Status accurately describes the proof method. |
| S6 | corollary of S0 | ok | S6 is correctly marked as a corollary of S0; the section derives S6 from S0's unconditional domain persistence guarantee, and the reasoning is valid — S0 does not condition on arrangement references, so arrangement-independence follows directly. |
| S7a | introduced | design requirement ** | Section explicitly states "This is a design requirement, not a convention." Justification appeals to Nelson's baptism principle as design philosophy, not formal derivation from other properties — this is a system constraint on allocation behavior. |
| S7b | introduced | `design requirement` ** | S7b asserts a system-wide constraint ("We require that…") about where content resides in the tumbler hierarchy; it is not derived from other properties and has no proof — it is a design decision that content lives at element-level tumblers. Status should be `design requirement`, not `introduced`. |
| S7 | from S7a, S7b, T4, GlobalUniqueness (ASN-0034) | ok | Proof present: S7 is derived from S7a, S7b, T4, and GlobalUniqueness via explicit argument; status correctly reflects derivation. |
| S8-fin | introduced | design requirement ** | Justification appeals to "operational reality" (finitely many operations, each finite) — these are system design constraints, not proven premises. The argument has proof structure but its foundations are operational assertions, making S8-fin a consequence of design requirements rather than a derived property. |
| S8a | introduced | ok | Property is derived from T4's positive-component constraint on element-field tumblers; the prose provides an informal but clear derivation (V-positions are element-field tumblers, T4 requires strictly positive components, therefore zeros(v)=0 ∧ v>0 ∧ v₁≥1); `introduced` correctly reflects established status. |
| S8-depth | design requirement | ok | Section explicitly states "this is a design requirement, not a convention — parallel to S7a"; justification is motivational (Gregory's evidence, two-blade knife dependence) rather than a formal derivation, so `design requirement` is correct. |
| S8 | theorem from S8-fin, S8a, S2, S8-depth, T1, T3, T5, T10, TA5, TA7a (ASN-0034) | ok | Proof present: constructs singleton decomposition, proves partition via case analysis (same-subspace uniqueness from S8-depth/T3/TA5, cross-subspace disjointness from T5/T10). Status correctly reflects derived theorem with dependencies. |
| D-CTG | introduced | design requirement ** | D-CTG is explicitly labeled "(DESIGN)" and its contiguity assertion is posited, not derived. The proof in the section establishes a *consequence* of D-CTG (shared intermediate components at depth m ≥ 3) by combining D-CTG with S8-fin — it does not derive D-CTG itself. Status should reflect that D-CTG is a design constraint accepted without proof. |
| D-MIN | introduced | design requirement ** | D-MIN is labeled "(DESIGN)" and posited without derivation; the subsequent derivation *uses* D-MIN as a premise (not proves it). It is a system constraint on minimum V-position, not a derived result. |
| D-CTG-depth | corollary from D-CTG, S8-fin, S8-depth, T0(a), T1, T3 (ASN-0034) | ok | Status says corollary from six cited properties; the section sketches the reduction argument (shared prefix forces contiguity to collapse to the last component, matching depth-2), which is the standard form for a corollary whose heavy lifting is done by its dependencies. No conflicting evidence — no axiom claim, no circularity, no contradiction between prose and status.

Wait — let me re-examine more carefully.

The section content contains **no derivation**. There is no *Proof.*, no ∎, no "we show that," no case analysis, no step-by-step argument from the cited dependencies. The first sentence restates the claim. The second paragraph discusses an ordinal-numbering convention (starting at 1) that is contextual, not part of any derivation. The phrase "contiguity reduces to contiguity of the last component alone" is an assertion of the result, not a proof of it.

The Status claims `corollary from D-CTG, S8-fin, S8-depth, T0(a), T1, T3` — meaning derived — but the section never shows **how** the shared-prefix property follows from those six dependencies.

```
flag | Status claims corollary (derived) but section contains no proof — only a restatement of the claim and an ordinal-numbering convention discussion. The derivation from D-CTG, S8-fin, S8-depth, T0(a), T1, T3 is missing. |
| D-SEQ | corollary from D-CTG, D-CTG-depth, D-MIN, S8-fin, S8-depth | ok | Derivation is present within the property statement: S8-depth gives common depth m, D-CTG-depth forces agreement on all but last component, D-MIN gives last component starts at 1, D-CTG gives no gaps, S8-fin gives finiteness — yielding the consecutive {1..n} form. D-CTG being a design constraint does not invalidate the corollary; a corollary derived from axioms/design constraints is still a valid derivation. |
| ValidInsertionPosition | introduced | ok | Definition (DEF, predicate): defines what constitutes a valid insertion position. The accompanying derivations (distinctness, depth preservation, subspace identity, S8a consistency) verify structural consequences of the definition, not the definition itself. Status `introduced` is correct. |
| S9 | theorem from S0 | ok | S9 has a valid proof deriving it as a special case of S0's universal guarantee; "theorem from S0" correctly reflects this dependency. |

*22 properties audited in 325s. 8 recommended changes.*
