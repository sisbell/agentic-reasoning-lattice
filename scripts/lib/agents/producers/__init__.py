"""Producers — emit new substrate identity.

A producer's signature contribution is creating new substrate nodes
or new top-level link surfaces (claims, notes, findings, sidecar
documents, structural classifications). Fired by triggers when an
upstream predicate first becomes true; their writes seed the surface
that refiners will later close on.
"""
