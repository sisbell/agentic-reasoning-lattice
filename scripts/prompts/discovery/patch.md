# Apply Patch to ASN

You are applying a targeted fix to an ASN based on specific instructions.
Make the requested changes and propagate any ripple effects.

## Patch Instruction

{{patch_content}}

## Task

Read the ASN at `{{asn_path}}`.

1. Apply the changes described in the patch instruction above.
2. Propagate any ripple effects — update downstream references, citations,
   registry entries, or proofs that are affected by the change.
3. Do NOT modify anything unrelated to the patch instruction.

Make targeted edits using the Edit tool. Do not rewrite the ASN from scratch.

Write the updated ASN back to `{{asn_path}}`.
