This M7 design checks out as buildable and faithful across all seven review axes. I verified the load-bearing claims individually: the MAKELINK `iextent`-verbatim recording does satisfy ML1's recovery equation (consecutive sibling subtrees tile the run's half-open I-extent under T1); the `age` formula `home_count[origin(a)] − ordinal(a)` equals ASN-0128's `f_d − 1 − j`; the nullify self-emitter via `elem_addr(doc, s_L, 1 + home_count)` equals `a_emit` by FrontierUnification; the factual claims about M1 (`SpanSet` read-opacity, no `coverage` function, `classify_spans` being level-gate-free, the falsity of the "⊕ never lengthens" law via `([5,3],[0,2,7])`) are all accurate against the M1 interface as given; every upstream call (`mint_link`/`link_lock_key` pairing, `stage_seat_link`, `resolve`+`Run::iextent`, `transact` working-state minting, zero-step hit semantics, non-reentrancy honored by inlining `emit_core` in `editlink`) matches the M2/M3/M5 interfaces verbatim. The four hard conflicts (open-vs-managed surfaces, the Df-DIR/S2 slot flip with its EL11(a) seam consequence, `[K_sup]` idem⊤ vs EL6's fresh-address guarantee, the `[K_sup]` routing fence) are resolved explicitly with sound rationale, and the boundary discipline (no R, no V-projection, in/out composition left to M8, seating selector left to M5) matches the decomposition.

**Revision list**

1. `[SHARPENING]` Add a rejecting precondition to `stale(ty, h)` and `retract_stale(d_retr, ty, horizon)` that `ty` is registered with BH4 (hence idem⊥), surfacing a typed error otherwise. The doc comments frame the intended domain ("for an app-registered idem⊥ BH4 ty"), but nothing enforces it — as written, a caller could run `retract_stale` against the idem⊤ `Supersedes` class and mass-nullify old claims. The path is dormant in v1 (no BH4 types exist), so this is a foot-gun closure, not a correctness hole; it also completes the "declared ⇒ served" discipline with its converse ("served only where declared").

2. `[SHARPENING]` State explicitly (in §Invariants or §Core data model) that every class-sensitive *guard* — `emit`'s `[R]`/`[K_sup]` fences, `editlink`'s DC check, `nullify`'s gate — and `apply_link`'s class *recognition* ([R]→`nullified`, [K_sup]→`sup_fwd`, idem⊤→`dedup`) evaluate the same `coverage_class` function. This is what makes the `Extents` over-discrimination safe end-to-end: a hand-built endset whose coverage equals a reserved class but whose class differs (e.g. mixing `unit(r_addr)` with a deeper non-unit span under `r_addr`) is admitted by DC *and* consistently treated as non-[R] by the fold, so the engine never half-recognizes a tuple. The totality argument is present; this coherence-under-over-discrimination invariant is currently only implicit, and it is the actual reason the documented "safe direction" claim extends from type-matching/dedup to the fences.

3. `[SHARPENING]` Align `is_filtered`'s probe domain with `is_k`'s: take `&Tumbler` rather than `&Address`. ASN-0128 groups both in the same membership-predicate regime (total, coverage-tested), and the design already gives `is_k` the raw T-wide probe; the asymmetry is harmless (BH1's rewrite only ever feeds it store-derived addresses) but gratuitous.

4. `[SHARPENING]` Tag the `editlink` successor arity narrowing as an explicit divergence in the Conflicts section: ASN-0125 EDITop's precondition is `ℓ' L3-conforming` (N ≥ 3), which the design narrows to arity == 3 (`IllFormedSuccessor`) as part of the arity-3-store commitment. The commitment and its consequences are already well documented under §Invariants; listing it beside Conflicts §8/§9 (the other deliberate note-divergences) would make the traceability uniform.

5. `[SHARPENING]` Name the source of `LinkStore`'s construction-time `Arc<TypeRegistry>` cache in one sentence (e.g. cloned off `kernel.snapshot().world().links()` at construction, or handed in by the engine at assembly). §3 establishes the cache is safe (registry genesis-immutable) and names the snapshot-read fallback, but the constructor itself is the one small piece of plumbing left to inference.

VERDICT: CONVERGED
