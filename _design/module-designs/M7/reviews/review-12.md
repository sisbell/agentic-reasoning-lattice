**Revision list**

1. `[SHARPENING]` **Pin `apply_link`'s `[R]`-fold behavior on a to-set with 0 or ≥2 denoted roots.** §1 says the fold inserts "the to-root (`value.to_slot().addrs()` single) into `nullified`" — the "single" is an assumption inherited from the surface discipline, not a pinned behavior. `apply_link` is the replay-critical fold M2 requires to be *total and deterministic*; even though every v1 path provably yields a single root (nullify's `enc({target})`, with `emit`/MAKELINK/`editlink` fenced off `[R]`), state what the fold does off that discipline — e.g. "insert every denoted to-root" (the faithful generalization of ASN-0086's coverage-based `nullified`), so a builder doesn't invent behavior at the one place determinism is contractual.

2. `[SHARPENING]` **Give `SlotIndex` a v1 definition.** `Hints.spanfilade: [SlotIndex; 3]` names a type that is never defined even as a placeholder (`Hints` derives `Default`, so `SlotIndex` must at least be `Default + Clone`). Pin the bootstrap concretely — `struct SlotIndex;` (unit, ignored by `stab`'s brute scan) or drop the field from v1 `Hints` and reintroduce it with the deferred index — so the struct as written compiles and the Open decision stays about the *index structure* only.

3. `[SHARPENING]` **State `emit`'s pre-transact rejection plumbing explicitly.** `retract_stale` spells out that `NotBh4` returns `Err(TxnError::Rejected(..))` with *no transaction opened* (citing M3 `fork`'s precedent); `emit`'s `NonAddressDenotingType` and `SupersessionClass` (and `assert_sup`/`nullify`'s pre-transact key computation failures, if any) are only implied to work the same way by §3 step 1. Add the same one-line statement so the M10-facing error contract is uniform and a builder doesn't wrap these in a throwaway transact.

4. `[SHARPENING]` **Scope the "never compute a dedup key at all" claim.** §Core data model says "MAKELINK/idem⊥ types never take a dedup lock *and never compute a dedup key at all* (§1)" — but §1's fold rule keys on the *tuple's class registration*, so a MAKELINK deposit whose resolved type class coincidentally equals a registered idem⊤ app class (the degenerate case §2/Conflict §1 already acknowledge) does get an in-memory `DedupKey` with `Extents` from/to folded into the hint. Harmless (no `LockKey` ever serializes it; over-discrimination is the safe direction), but restrict the "never" to the emit lock path or note the degenerate-class exception beside the existing graceful-degradation sentence, so the "no `Extents` in a `LockKey`" safety claim isn't resting on an overbroad premise.

5. `[SHARPENING]` **Fix the "V-position" mislabel in §4's self-emitter computation.** `elem_addr(ElemPos{ doc: d_retr, subspace: s_L, ordinal: 1 + home_count[d_retr] })` produces the next link-chain **I-address** (the element-level link-store address `d_retr·0·s_L·(n+1)`), not a V-position; the formula is correct but calling it "the (1 + home_count)-th link V-position" invites confusion with M5's depth-2 `VPos`. Also note the `Result` from `elem_addr` is infallible here (P0's registered-document check precedes it, so `doc.level() == Document` holds).

6. `[SHARPENING]` **Give `LinkStore`'s constructor as a signature.** The construction-time registry cache is described only in §3 prose ("takes one `kernel.snapshot()` and clones the `Arc` off `snapshot.world().links()`"). Add `pub fn new(kernel: &'k Kernel<W>) -> LinkStore<'k, W>` (or equivalent) to §C/§D so the type's one non-obvious construction step is in the interface, not the internals.

7. `[SHARPENING]` **Add a traceability note for the wf depth narrowing.** MAKELINK's spec check requires `#σ.start() == 2`, while ASN-0120's `wf` admits `#u_j ≥ 2`. The narrowing is forced by M5 as given (its POOM keys and `resolve` precondition are depth-2, so a deeper V-spec could only ever resolve to ∅), and rejecting as `IllFormedSpec` is the honest move — but the design records the checks as "mirror M5's `resolve` precondition" without noting they narrow the note's `≥ 2`. One clause in the makelink text (or a line in Conflicts, beside §8/§9/§11) keeps every deliberate note-divergence traceable in one place, per the design's own stated policy.

**Assessment.** I verified the load-bearing machinery against the upstream interfaces and the notes and found no material defect: every upstream call typechecks as given (M3 `mint_link`/lock-key discipline, M5 `resolve`/`Run::iextent`/`stage_seat_link`, M2 `transact`/working-vs-base reads/zero-step Seq semantics, M1's readable `Span` surface — including the correct avoidance of M1's read-opaque `SpanSet` as a stored endset); MAKELINK's run-extent endsets provably satisfy ML1's recovery equation (consecutive unit subtrees tile the run interval); the BH4 `age` arithmetic matches ASN-0128 (`home_count − ordinal`); the dedup key equals I0 identity via I0a's minimal-antichain reduction; editlink's same-home consecutive mints correctly read `stg.working()`; and the eleven documented conflict resolutions (slot-direction flip, idem⊤ over EL6's fresh-address reading, the `[K_sup]` public-op fence, the hoisted home check, the arity-3 narrowing) are each stated, argued, and consistently applied through `sup_fwd`, `current`, the M8 `in/out` seam, and the EL11(a) projection note. The module is buildable from this document alone.

VERDICT: CONVERGED
