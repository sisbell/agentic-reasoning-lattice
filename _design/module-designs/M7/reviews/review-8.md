Resuming the review. I completed the deep checks (signature typechecking against M1/M2/M3/M5, the `coverage_class` totality argument, the dedup/lock split, the two-surface architecture, the conflict resolutions, and the invariant cross-check). Remaining work: write up the findings.

I traced every upstream call (`mint_link`/`link_lock_key`/`is_registered_document`, `resolve`/`Run::iextent`/`stage_seat_link`, `transact`/`snapshot`/`Staging`, and the M1 algebra) — none are invented or contradicted. I verified the BH4 `age` formula (`home_count[origin(a)] − ordinal(a)` = ASN-0128's `f_d − 1 − j`), the P-tgt self-emit computation (`inc^{home_count}([d.0.s_L.1],0)` = `a_emit` by FrontierUnification), the slot-direction conflict resolution (ASN-0128 F=old, rewired consistently through `sup_fwd`/`in`/`out`), ML1 coverage-exactness (iextent runs ∩ dom(C) = ρ since allocated content is #E=2 siblings), and the arity-3-store invariant. All hold. The two-impl-block split (MAKELINK +HasM5, managed ops no-HasM5) typechecks, and the engine-composition-contract checklist passes (slice/record/accessor/fold present; generic over W; stages upstream `M3Rec`/`M5Rec` via `.into()`; LockKey space tag from the central enum).

I found no defect that would stop or mislead a builder. The items below are genuine but non-load-bearing.

---

## Revision list

1. **[SHARPENING] Make the M1 property that `coverage_class` totality rests on explicit.** The totality argument's load-bearing step — *level-uniform (`#width == #start`) ⇒ `#reach == #start`* — is an inference about M1's `⊕` (`#(a⊕w) == max(#a,#w)`, true for the BigUint/no-carry model but **not stated in M1's interface**, which leaves `add`'s output length unspecified). In normal operation no fault can occur regardless: every endset reaching `coverage_class` is built via `subtree_of`/`iextent`/`enc`, all of which go through `from_endpoints`+`shift` (length-preserving, M1-guaranteed), so `#reach == #start` holds *by construction* — the `⊕`-length inference is only needed to rule out an adversarial `Span::new`-built span. Either (a) cite the relied-upon M1 contract clause and request M1 promise `#(a⊕w)=max(#a,#w)`, or (b) note that the documented construction paths give `#reach==#start` directly, so the `is_level_uniform()` proxy is belt-and-suspenders, not the safety basis. As written, a builder must reconstruct this proof themselves to justify the `.expect()` inside the otherwise-total `apply_link`.

2. **[SHARPENING] Annotate the home-check hoist as a confirmed-acceptable divergence from ASN-0128 I6.** Conflict §8 hoists `is_registered_document(home)` ahead of the dedup short-circuit, so an unregistered-home `emit` is rejected on a hit too — directly contradicting ASN-0128 I1/I6's explicit "Home validation — branch-local… on a hit, `d` is not read at all." The divergence is sound (it strengthens a precondition; unregistered-home emit is never a legitimate call) and is documented, so it is not a defect — but it changes an explicitly-stated note guarantee. Add a one-line note confirming no M9/M10 caller relies on hit-succeeds-regardless-of-home (register_pred/rule-fires/app relations always home at a registered document), so the strengthening is provably invisible.

3. **[SHARPENING] Compute the P-tgt self-emitter in O(1) instead of O(home_count) iterated `inc`.** §4's `inc^{home_count[d_retr]}([d_retr.0.s_L.1], 0)` is correct but loops `home_count` sibling-advances; since each `inc(·,0)` only bumps the terminal ordinal, the result is just `[d_retr.0.s_L.(1 + home_count[d_retr])]` — build it directly with `elem_addr(ElemPos{ doc: d_retr, subspace: s_L, ordinal: 1 + home_count })` (or `shift_ordinal`). Avoids a linear scan on the rare self-emit nullify path.

4. **[SHARPENING] Spare M8 the `Tumbler→Address` re-validation on the §G→§F handoff.** §G returns raw `im::OrdSet<Tumbler>` (correct, for cursoring), but to then `readlink`/`observe` a result M8 must lift each `Tumbler` back to `Address` via `validate(...).expect(...)`. Either expose a `Tumbler`-keyed `readlink_t(&self, &Tumbler)` for the M8 path, or state in the seam that M8 lifts via `validate` (infallible by M3's mint) — currently the prose says "M7 lifts Tumbler → Address at the §F boundary," which is slightly inverted (it is the M8 caller that must lift a §G key before calling §F).

5. **[SHARPENING] Tighten two prose imprecisions.** (a) `emit_core`'s error-mapping note says `HomeNotRegistered` can originate "defensively at `mint_link`," but `mint_link`'s `MintError::HomeNotRegistered` routes through the `From<MintError>` impl into `EmitCoreError::Mint(...)`, not `EmitCoreError::HomeNotRegistered` (and the hoisted check makes it unreachable anyway). (b) `retract_stale`'s "an already-nullified target dedups to a hit" holds only for a *same-`d_retr`* prior retraction; a cross-`d_retr` already-nullified target deposits a redundant (harmless) `[R]` tuple. Neither affects the postcondition; just sharpen the wording. (Both are in dormant/defensive paths.)

---

The module is buildable from this document alone: every owned capability (MAKELINK/READLINK/FOLLOWLINK, Emit/Observe/Nullify, assert_sup/editlink/current, the registry+gate, dedup, the spanfilade+matchers, BH1–BH4) has concrete types, signatures, and algorithms; upstream is called as given; the source-note contracts (Permanence, append-only audit, retraction stability, type-by-coverage, ghost permission, ML1 coverage-exactness, idem-uniqueness, no-sterilization, edit-discipline) are honored or correctly flagged as discharged at a neighbor (non-transcludability → M5, R → M5); and the data model, interface, invariants, and seams agree.

VERDICT: CONVERGED
