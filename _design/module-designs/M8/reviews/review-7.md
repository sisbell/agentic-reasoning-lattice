I reviewed the M8 design against the decomposition, the eight source-note statement sets, and the five upstream interfaces as given. The result: **no material defects found.** Every owned capability (region-family discovery, four-set match, count, windowed enumeration, RETRIEVEENDSETS, project/discoverability, delete-orphan preview, archival in/out) is specified to implementable depth with real signatures and algorithms; every upstream call exists in the interfaces as published (including the non-`Copy` `View` workaround, the `validate`-at-the-lift discipline, `relative_complement` vs. `im`'s symmetric `difference`, and M7's prescribed `match_links ∩ type_slice` archival recipe); the delete-orphan set identity `findlinks(A_del) \ findlinks(retained)` checks out against ASN-0117's last-witness characterization including the link-subspace retained range; and the two deliberate divergences (active-filtered results, content-only `project`) are loudly resolved conflicts with documented escape hatches, not silent weakenings. What remains is wording-level polish.

**Revision list**

1. **[SHARPENING]** In `delete_orphans` (§6), fix the misleading non-emptiness claim. The code comment `// cand non-empty under the bounds check` and the prose "(`cand` is always non-empty under the bounds check…)" are false as written about `cand` — `cand` is the *stab result*, which is legitimately empty when no link touches the deleted range. What is non-empty under the bounds check is the *`a_del` query endset* fed to `stab_union`. Reword both to name the query, so a builder does not add a `debug_assert!(!cand.is_empty())` that fires in normal operation.

2. **[SHARPENING]** Scope the "mirrors M5's `DeleteError` granularity" claim in `delete_orphans`. M5's DELETE distinguishes `NotArranged` (p beyond the arranged run) from `OutOfBounds` (range overruns), while M8's single bounds check folds both into `OutOfBounds`. Under the width ≥ 1 guard the two are jointly equivalent to M8's check, so nothing is lost — but state the folding explicitly (or add a `NotArranged` variant) so the preview's rejection variant is not expected to match M5's label byte-for-byte on the `np > n_C` case.

3. **[SHARPENING]** Tighten the Ŝ^Σ = S^Σ argument in §7. The claim "no component raw-`emit`s the Supersedes class" is an assembled-system convention, not an enforced invariant: M7's `emit` gates only the retraction class (`RetractionClass`), and `Supersedes` is a registered Binary type, so an `emit(home, reserved(Supersedes), from, to)` with non-resident or self-referential endpoints is upstream-representable. Add one sentence recording that `claims_on`'s read-out is panic-free even under such a violation — every emit-shaped `[K_sup]` tuple still carries unit-depth single-address F and G (`|F|=1` forced, Binary `to` cardinality), so `addrs().next().unwrap()` cannot fault — making the discipline reliance semantic (result purity) rather than safety-bearing. This distinguishes which unwraps depend on EL-DM discipline (none) from which depend only on M7's shape gates (all of them).

4. **[SHARPENING]** Document `View::Default` behavior on `in_claims`/`out_claims`. The public docs say `Active` → operative graph, `Audit` → full history, but the parameter admits `Default`, which M7's §G primitives coerce to `Active`. One doc line ("`Default` behaves as `Active`") closes the gap between the accepted type and the described domain.

VERDICT: CONVERGED
