**Review summary.** I checked every upstream call against the M1/M2/M3/M5/M7 interfaces as given, re-derived the survival-check set identity and the window/cursor mechanics against ASN-0117/0108, and traced each documented divergence (active filtering, flipped supersession slots, content-only `project`, `BadRegion` strictness) back to its source note. All upstream calls exist with matching signatures (including the `im::OrdSet` `relative_complement`-vs-`difference` trap, correctly handled), the orphan identity `findlinks(A_del) \ findlinks(retained)` is sound including the link-subspace retained range, the arity-3 exactness argument for the `{FROM,TO,TYPE}` union holds against M7's write paths, and every note divergence is stated with a defensible rationale rather than papered over. The panics/unwraps are all guarded (bounds checks pin `vspan` widths ≥ 1; `nc = 0` is unreachable past `OutOfBounds`; claim-slot unwraps are safe for every Supersedes tuple any v1 write path can produce). I found no unbuildable component, no invented upstream API, no dropped owned capability, and no unstated conflict. What remains are non-load-bearing improvements.

**Revision list**

1. `[SHARPENING]` Fix the monotonicity wording in §3 and the Invariants block. "The active slice only grows" and "M7's append-only active slice" are false as literal statements — `dom(L)` is append-only, but the active slice shrinks under nullification. The cited lemmas (FL-MON, CN-MONO) carry an absent-retraction hypothesis; state it: "a found link stays found *unless retracted*; `dom(L)` only grows." The correct behavior is already implied elsewhere (the result-drop invariant covers retraction), so this is wording, not logic.

2. `[SHARPENING]` In `retrieve_endsets`, derive slot attribution from the three per-slot `stab(i, q, Active)` sets (already computed inside `stab_union` — keep them separate instead of unioning immediately: slot `i` of candidate `a` touches iff `a ∈ s_i`) and drop the local `touches()`/`classify_spans` loop. This removes M8's one residual piece of match-test logic, eliminates any drift risk against M7's overlap semantics, and makes Conflicts #1's claim ("M8 implements no slot matching") literally true — today §4 quietly contradicts it. Alternatively, soften the #1 claim to acknowledge the pointwise touch test.

3. `[SHARPENING]` `discoverable_from` stabs the entire document range (`stab_union` over all of `ran(M(d))`), materializing every doc-reaching link to test membership of one address. A per-link test — `readlink(a)`, per-slot span-overlap against the `content_runs + link_runs` extents, conjoined with `is_active(a)` — is O(arity × |runs|) with identical semantics (same compound "reachable AND active"). Prefer it; the current form is correct but pays a whole-document cost for a single-link question.

4. `[SHARPENING]` `image`'s "the result is a SET (ASN-0127 image)" over-claims: dedup is by exact `Run` equality, so overlapping *input* region spans can yield partially-overlapping runs whose address multiset has repeats (a caller summing widths overcounts |image|). Either coalesce overlapping runs at the boundary or scope the doc-comment to "no exact-equal Run repeat; overlapping input spans may yield overlapping runs." Internal uses are set-idempotent, so this only affects the public seam's reading.

5. `[SHARPENING]` `retrieve_endsets` collects its dedup `std::collections::HashSet` straight into the output `Vec`, so the pair order is hash-nondeterministic across identical calls at the same snapshot — in tension with the "Result determinism at a snapshot" invariant. Pin an output order (e.g., by slot, then first-span start) or explicitly exempt the pair set from the ordering half of that invariant.

6. `[SHARPENING]` The Dependencies section lists M5's `point` as consumed, but no M8 algorithm calls it — it appears only in the §5 *caller* recipe for probing the opaque projection. Drop it from the consumed list or annotate it as caller-recipe-only, so the dependency inventory matches the code.

VERDICT: CONVERGED
