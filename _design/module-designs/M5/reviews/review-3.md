I reviewed M5's design against the four upstream interfaces (M1–M4), the eleven source notes, the decomposition, and the composition contract. The core is solid: every operation (INSERT/DELETE/COPY/REARRANGE/VERSION + link-seating) has concrete types, signatures, and a fold; upstream is called as given; the J-couplings, S3★, CL-OWN/CL-UNIQ, P2, and the single-`(M,R)`-snapshot guarantee are enforced by-construction or actively guarded; the conflicts are resolved soundly (notably #2 M4-presence-not-M3-allocation, #3 I-adjacency coalesce, #4 effect-level deltas, #6 R-index ownership); and the altitude calls (m=2, RunList structure, hints) are flagged as genuine Open decisions, not papered-over under-specification. The implicit-position run-list makes D-SEQ★/D-CTG★/D-MIN★ free, the `Tumbler`-keyed maps correctly work around `Address: !Ord`, and the `#[non_exhaustive]` discipline correctly enforces the M/R coupling through assembly. I found only non-load-bearing refinements.

# Revision list

1. **[SHARPENING] Tighten the read-side totality claims to match their own preconditions.** `project`'s same-length branch calls `M1::intersect` (level-gated), but "same *start-length*" does not imply level-uniform: a constructible same-start-length-but-non-level-uniform coverage span would return `Err(LevelMismatch)`, contradicting `project`'s "total — no `Result`" claim. Route by `Span::is_level_uniform()` (level-uniform same-length ⇒ `intersect`, otherwise ⇒ the total `denotes` path the different-length branch already uses), and state the precondition that coverage spans are level-uniform I-extents. Symmetrically, `resolve` defends `#start < 2 ⇒ ⟨⟩` but then indexes `span.width().get(2)` unguarded — a level-non-uniform depth-2-start V-span (`Span::new([s,k],[n])` is T12-valid) panics; either guard `is_level_uniform()`/`#width` or note explicitly that the `#start` check is a defensive bonus atop the stated depth-2 level-uniform precondition (the "cannot fault" claim is precondition-relative).

2. **[SHARPENING] Consider encapsulating the level-class discipline inside M5.** SHOWDELETIONS' `ever_placed(d) \ content_image(d)` and FINDDOCSCONTAINING's filter are handed to M6 as raw possibly-mixed-length `SpanSet`s with the per-class partitioning specified only in prose; since the mixed-length semantics are a property of M5's own iextent/R data, a disciplined `deletions(d)`/`difference` helper on M5's side would keep a consumer from naively calling a length-gated op and faulting. (The boundary itself — M6 owns the queries — is correct; this is about where the subtlety lives.)

3. **[SHARPENING] State that `project` is content-subspace-only by construction.** It scans content runs and answers "content I-address falls in coverage"; link-to-link reverse discovery is M7's BH3, not this. The decomposition's "content-subspace restriction supplied per-caller" reads as parameterizable, but only the V→I `resolve` is subspace-flexible (via `span.start().get(1)`) — `project` is fixed to content. Make that asymmetry explicit so a builder doesn't expect a subspace argument.

4. **[SHARPENING] Clarify or remove `RearrangeError::RegionEmpty`.** Given the `NotAscending` guard, strict ascent already forces every region width ≥ 1, so a zero-width region is unreachable. State that this variant covers the empty-content-subspace case (`V_{s_C}(d)=∅`, R-PRE(ii)) or fold it into `OutOfBounds`.

5. **[SHARPENING] Naming/pseudo-code precision.** M3's lock-key constructors are associated functions on `M3State` (`M3State::content_lock_key`), not a module path `M3::`; and §1/§2 drop the `&` on `shift`'s `Nat` argument (`M1::shift(a.tumbler(), &c)`, `&offset`). Minor, but a literal reading misleads.

VERDICT: CONVERGED
