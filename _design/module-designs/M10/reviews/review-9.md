**Review summary.** I checked every dispatch-table row against the upstream signatures as given (M3's four entity ops and two pure queries, M5's five editors, M7's five write ops and raw reads, M6's seven `Query` methods, M8's thirteen `*_on` twins) — all typecheck, including the `Vec<Address> → &[Address]` coercions, the `View`-by-value moves, and the `WriteCtx.principal` threading into exactly the four principal-taking ops. The `RejectCode` union covers every upstream error variant (I enumerated all of them, including the nested `MintError`/`SeatError`/`ContentError` wrappers); the `FaultSite` claims about which upstream variants carry localization are factually correct (M6's `DocNotRegistered(Address)` vs M8's fieldless one, `ExtentError`/`OriginError` fieldless). ASN-0134 commitments are honored: A7 structurally (response = the driver's post-commit return), one snapshot per read (clause 6 by construction), `as_of`/`committed_at` stamping per V1, no batch fusion (A5), lost-ack duplication handled by the client-keyed idempotency cache exactly as §SAFE(b)(iii) prescribes, and the G0 serializability-not-SC contract stated rather than hidden. The two flagged upstream amendments (store-driver constructors; the type-only `M10 → M4` edge forced by `Vstream::insert`'s public bound) are real, honestly surfaced as amendments rather than silently invented, and correctly argued as load-bearing for whole-system assembly. The zero-occupants resolution of cross-family orchestration follows the decomposition's own criterion and keeps the latent path. I found no defect; the remaining items are polish.

**Revision list**

1. `[SHARPENING]` State the deliberate expressiveness narrowing of `SuccessorSpec`: its `from`/`to` slots are content-resolved only (`Vec<VSpec>`), so an address-denoting successor — e.g. a claim-schema `[K_sup]`-typed link, which M7's DC guard explicitly *admits* by schema-conforming it rather than rejecting — is not constructible from the FEBE surface. Say in §4 that this is intentional (M7's seam frames M10's editlink job as forming a *content* successor) and that supersession of managed tuples is reached via `Emit` + `AssertSup` instead, so a builder neither "fixes" the gap ad hoc nor reads it as an accident.

2. `[SHARPENING]` Pin the two unnamed operational knobs of the ephemeral state: (a) `LruCache` requires a capacity at construction — make it a parameter of `Operation::new` (or a named constant) rather than leaving the sizing implicit under Open decision 3; (b) add "the transport must call `close_session` on connection drop" to the downstream-seam obligation list — otherwise `sessions` entries (and their idem entries, up to LRU pressure) leak until restart, since nothing else retires a binding.

3. `[SHARPENING]` Give the rationale for the one in-band error: `Response::Follow` carries `Err(Invalid)` as a *value* because M7's ⟨⟩ ≠ ⊥ distinction is a defined FOLLOWLINK answer, not a lifecycle failure — and contrast it with `Project`, where M8's `NotALink` *is* lowered to a `Rejection`. One sentence in §2 or the table footnote prevents a builder from "normalizing" the asymmetry in either direction.

4. `[SHARPENING]` Name `RejectCode::Gate` explicitly in the disposition table instead of letting it fall through the `Permanent` catch-all: M3 documents `MintError::Gate` as defensive, firing only on a corrupted frontier, so its meaning is closer to an operator condition than a client-side "your request can never succeed"; at minimum a table row (and possibly a `detail` note) should record that a `Gate` rejection signals store corruption, not client error.

5. `[SHARPENING]` Tighten the `WriteCtx` doc-comment claim that a write op mistakenly listed as a read is "a build-time prod toward the write path": that holds only for principal-consuming writes (`Version`, the M3 ops); a principal-free write like `Insert` misfiled into `is_read` compiles fine and surfaces only as the runtime `Malformed` from `dispatch_read`'s defensive arm. The universally true guarantee — typed rejection, never a panic — is the one to state.

VERDICT: CONVERGED
