This is review-8 of M10 after seven prior cycles, and it holds up: I traced every dispatch row against the upstream signatures as given (M2 `transact`/`snapshot`/`TxnError`, M3's four entity ops + two pure queries, M5's five `Vstream` ops + `resolve`/`Run::iextent`, M6's seven `Query` methods and their error-variant fault fields, M7's five write ops + raw reads + the editlink assembly recipe, M8's thirteen `*_on` twins), checked the `RejectCode` union against every upstream error variant, verified the trait bounds cover every store M10 dispatches into, and checked the lifecycle against ASN-0134's A1/A2/A7/G0/MIC-clause-6 and the SAFE(b)(iii) lost-ack story. No invented upstream call, no dropped contract, no signature mismatch, no unbuildable component. The two required upstream amendments (store-driver constructors; the type-only M10→M4 edge forced by `Vstream::insert`'s public bound) are genuine gaps in the upstream interfaces, correctly surfaced as flagged amendments with exact signatures rather than silently assumed. The zero-occupants resolution of cross-family composites and the parallel-M9 call both answer the decomposition's open questions soundly. What remains is polish:

**Revision list**

1. `[SHARPENING]` §5's named conservative-`Permanent` enumeration is incomplete against its own stated purpose ("named here so neither falls through the catch-all silently"). Other codes are equally future-satisfiable by other actors' commits yet default to `Permanent` silently: `OutOfBounds` (a later INSERT extends `n_C`, making an identical re-DELETE/REARRANGE succeed), `EmptySource`/`EmptyContentSubspace` (a later INSERT populates the source document or subspace), and M6's `RangeNotPresent`/`EmptySubspace` (later placements bind the range). Add these to the named list, or state explicitly that the enumeration is representative rather than exhaustive.

2. `[SHARPENING]` In `dispatch_write`/`dispatch_read`, make the complementary-half defensive arm an explicit list of the complementary `Op` variants rather than a `_` catch-all (and note the same for `is_read`'s `matches!`). Totality is preserved either way, but with `_` a newly added `Op` variant compiles silently into a runtime `Malformed`; an explicit list makes every new variant a compile-time error at all three sites, which is the cheaper enforcement of the never-silent invariant the design already leans on.

3. `[SHARPENING]` Have `close_session` also purge that session's `idem` entries (or state why it deliberately doesn't). Cross-session confinement currently rests entirely on the transport's never-reissue/non-forgeability contract plus the monotonic counter; purging on close enforces the confinement locally and reclaims LRU space held by dead sessions.

4. `[SHARPENING]` `FaultSite` doc nit: "(and, for M5, never `addr`)" reads as though M8's errors could fill `addr`. M8's `QueryError` variants are all fieldless (its `DocNotRegistered` carries no `Address`), so neither M5 nor M8 ever populates any `FaultSite` field — state it symmetrically so a builder writing the mechanical `Lower` impls doesn't hunt for an address that isn't there.

VERDICT: CONVERGED
