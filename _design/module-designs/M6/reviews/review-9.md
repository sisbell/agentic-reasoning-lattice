I reviewed M6 against the decomposition, the seven source-note digests, and the M1–M5 interfaces as given: every upstream call checks out against a published signature (including the fiddly ones — `shift`/`shift_ordinal`/`elem_addr` round-trips, `sort_by_cached_key` stability, `denotes` being outside M1's level-gate list, the `Option<&Address>` debug-assert form), the note contracts are honored (R0–R11, V0–V18, W0–W20, WF_V(i)–(vi)/O13, D-predicates, X12 R1–R3 with R4 correctly treated as optional, FD-SOUND/FD-COMPLETE), and the three deliberate deviations — the ordinal-level domain narrowing, the SHOWORIGIN_I de-scope, and the R-index placement in M5 — are each stated, forced by upstream-as-given (M5's `resolve` force-empties non-ordinal-level spans; M4's boundary forbids the I-ordered scan the I-arity needs; M5 already owns `docs_containing`), and surfaced as typed rejections or recorded amendments rather than papered over. The load-bearing correctness arguments (co-chain totality of COMPARE's ordinal arithmetic, the V-reconstruction lemma under D-CTG★, SHOWDELETIONS' denotational exactness, the cross-subspace `from_endpoints` round-trip in `doc_vspan`) all hold when checked against the algebra. I found no defect-grade problem; what remains is documentation tightening.

**1. Revision list**

1. **[SHARPENING]** Document COMPARE's second determinism reliance: X12 R3 ("the emitted report is a function of (ρ₁, ρ₂, res_Σ|P, res_Σ|Q), no hidden input") holds only because M5's POOM block decomposition is *canonical* (unique maximal runs, ASN-0058 split/merge/canonicalize), making `resolve`'s run splits — and hence the finer-than-maximal pair granularity M6 emits — a pure function of the arrangement restriction rather than of edit history. Name this reliance in the *Delegated* list (beside D-CTG★) and in the COMPARE bullets, so a future M5 relaxation of run canonicality is caught at the seam instead of silently making report granularity history-dependent.

2. **[SHARPENING]** Pin the "M5's `resolve` serves link-subspace spans" reliance at the boundary. RETRIEVEV's link items (`DeliveryItem::Ref`) and SHOWORIGIN_V's `s_L` arity are correct only if `resolve` covers link-subspace V-spans; M5's interface supports this reading (its defensive conditions name no subspace, unlike `project`, which is explicitly content-only), but never states it. Add it to the M5 seam notes / the existing D-CTG★ ask, so the dependency is documented rather than inferred — a content-only `resolve` would silently violate R3 exactness for link specs.

3. **[SHARPENING]** Unify registration-error payloads: `ExtentError::DocNotRegistered` and `OriginError::DocNotRegistered` carry no `Address`, while `RetrieveError`/`DeletionsError`/`CompareError`/`FindError` variants do. Carry the offending document uniformly — cost-free for M6 and gives M10 one marshaling shape. (Single-document operations make the culprit recoverable from the request, so this is convenience, not correctness.)

**2. Verdict**

VERDICT: CONVERGED
