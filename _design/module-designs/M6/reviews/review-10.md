I reviewed M6 against the seven source notes, the M1–M5 interfaces as given, the decomposition, and the engine composition contract. I checked every upstream call for existence and typechecking (including the `Nat`/`Tumbler`/`Address` threading, `sort_by_cached_key` stability, the `debug_assert!`-vs-`debug_assert_eq!` no-`Debug` point, and the borrow-through-snapshot pattern), re-derived the co-chain totality argument for COMPARE's ordinal arithmetic (overlapping I-intervals force one chain even for prefix-nested version documents), verified the V-reconstruction lemma and second-foot offset against X12 R1/R2, checked SHOWDELETIONS's membership-test combine against D-EXH/D-WIT semantics (`a ∉ ran(M(d))` vs. content-image coincide on `dom(C)`; R carries only content by J-LV), verified both extent syntheses against V0–V9b/W0–W13 (including the cross-subspace `⊖` round-trip and W13 normality of the concat), and confirmed the FDC filter's completeness rests on D-WIT/FD-SUPER through M5's no-false-negatives `docs_containing`. All seven owned capabilities are present; the two ownership tensions (R index in M5, SHOWORIGIN_I de-scope) are resolved explicitly and match the settled state of those questions. I found no material defect — the remaining items are polish.

**Revision list**

1. `[SHARPENING]` **State the derive set for the public request/result types.** `Spec`, `Region`, `Delivery`, `DeliveryItem`, `Deletions`, `CorrPair`, `CompareReport`, and the six error enums specify no derives (only `Operand` gets one), yet M10 must marshal all of them and "surface `Err` verbatim." Every payload they carry (`Address`, `Span`, `Nat`, `Val`, `VPos`) is `Serialize`-capable, so either declare the intended derives (at least `Clone + Debug`-or-not, `Serialize` where marshaled) or state explicitly that M10 destructures field-by-field and no serde surface is promised. As written, a builder must guess at the seam M10 codes against.

2. `[SHARPENING]` **Align the two broken-invariant policies on the read path.** RETRIEVEV panics (`expect`) when S3★ is observed broken (content position with no stored value), but the foreign-subspace arm — an S3★-aux violation, the same class of upstream corruption — is a `debug_assert` plus silent drop in release. The invariants section says M6 "panics (not silently skips) if S3★ is observed broken." Either make the else-arm panic in all profiles for symmetry, or amend the invariants text to record that S3★-aux violations deliberately degrade to silent drop in release. Currently the code and the stated policy read differently.

3. `[SHARPENING]` **Record the `SpanSet::is_empty` ask alongside the two M5 asks.** The FINDDOCSCONTAINING filter's `!= SpanSet::empty()` is justified by M1's no-zero-width-member invariant, and the design itself notes a denotational emptiness predicate "would be strictly more robust." The seam section already carries two documentation asks to M5 (D-CTG★ naming, `resolve` serving link-subspace spans); add this third ask (to M1) in the same list so it travels with the design rather than living only inside the FDC prose.

4. `[SHARPENING]` **Disambiguate the two things both called "simplicity oracle" for COMPARE.** The code comment labels the shipped O(|P|·|Q|) double loop the "simplicity-oracle form," while the open build decision names the per-position *hash join* as "the simplicity oracle to validate against" and picks "the block interval join" as production. Say once, in one place: v1's reference implementation is the exhaustive double-loop block join; sort+sweep is a drop-in optimization of the same join; the hash join is the independent test oracle. Right now a builder can read the spec as recommending a sweep it never provides.

VERDICT: CONVERGED
