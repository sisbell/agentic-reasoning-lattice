Reviewing M3 against the decomposition, the upstream M1/M2 interfaces, and the six source notes (ASN-0040/0042/0047/0093/0103/0123), I verified the load-bearing mechanics rather than the prose: the `next_in` closed form against ASN-0040 B2 (`shift` of the gated `c₁` by `m` does yield `c_{m+1}`, and the `shift` hazard doesn't apply since `c₁` carries its ordinal last); the decompose-and-range membership claim (for T4-valid inputs the longest-valid-proper-prefix drops exactly one or two components, so `g ∈ {1,2}` is total and decompose is injective — the "no false positives" claim holds, and anchors like `b_C(d)` correctly report unallocated); byte-identity of lock keys and frontier keys (one `*_ns` path, injective encoding); every M1 call against M1's actual signatures (`validate` consumes a clone, `parent` over `&Address`, `zeros`/`ordinal` over `&Tumbler`, `checked_inc` gate discharged on every mint, 1-based `get` never out of range); every M2 call against `transact`/`Staging`/`Snapshot`/`LockKey` as given, with the composition contract's slice/record/accessor/fold/two-forms pattern fully present; the delegation gate against O15/O17b/O17c including the in-closure race analysis (the cross-namespace id race genuinely needs the global principals key; the OrdMap top-down probe is sound by T5 contiguity); the ω-stability arguments that license M5's pre-read branch resolution and `create_new_document`'s defensive lock (they hold via P8-induction + freshness + the (iii) narrowing); and all eight conflict resolutions, each of which is stated, sourced, and sound (the O15(iii) `≤1→==1` narrowing is forced by NodeBaptism as argued; the eager-`M(d)=∅` and node-tier-fork drops are sanctioned or explicitly reasoned). I found no invented upstream call, no dropped contract, no boundary breach, and no internal type/invariant mismatch.

**Revision list**

1. `[SHARPENING]` In `delegate`'s doc (Public interface §B / §6), state explicitly that the two pre-transaction rejections — `NotValid` from the validate-lift and `NotAccountTier` from the hoisted (iii) check — surface as `Err(TxnError::Rejected(…))` **without opening a transaction**, exactly as `fork` already states for its unknown-id path. The signature's only rejection channel makes this inferable, but `fork` sets the precedent of saying it, and a builder wiring M10's error handling shouldn't have to infer which failures consume a transaction slot.

2. `[SHARPENING]` Add a `debug_assert!` (or a stated fail-stop check) in `apply_ns`'s `Allocate` arm that the decomposed ordinal equals the current frontier count + 1. The fold currently `insert`s the ordinal unconditionally, so a corrupt or out-of-domain record that *regresses* or *jumps* a frontier would be silently absorbed rather than fail-stopping like the `expect`s on malformed shapes do; the totality-domain note frames such records as corruption, so the assert makes the fold's behavior match its own stated policy at negligible cost.

3. `[SHARPENING]` The multi-key acquisition assumption in §8 ("the whole key set is acquired atomically/deadlock-free and the `base()` root is taken only after acquisition") is stated as M2's obligation but appears only in M3's document; M2's interface says merely "hold `keys` for the txn's duration." Flag it for propagation into M2's interface contract (or the composition contract) at its next revision, so the obligation is owned where it's discharged rather than living solely in a dependent's spec.

4. `[SHARPENING]` `delegate` can fail several gate conditions at once (e.g. a `new_prefix` that is simultaneously non-fresh, non-next-form, and not covered by the delegator), and the design fixes no evaluation order among `NotAncestor`/`NotAuthorized`/`NotFresh`/`NotNextForm`/`DuplicateId`/`ParentNotRegistered`. Either pin the check order (the §6 listing order is the natural choice) or state that any one applicable typed rejection is conformant — otherwise conformance tests and M10 error handling will encode an order the spec never promised.

5. `[SHARPENING]` `principal_tier_prefixes` takes `&self` but consults no state; make it a free or associated function. The design elsewhere is scrupulous about the pure/state split (e.g. `owns`, the `*_lock_key` associated functions), and this one method's signature contradicts that discipline for no gain.

VERDICT: CONVERGED
