**Review summary.** I checked every owned capability against the decomposition, every M1/M2 call against the upstream interfaces as given, the gate logic against ASN-0040/0042/0047/0093/0103/0123, and the data model against the interface and invariants. I found no defect: every component is implementable as written (real signatures that typecheck against M1/M2, closed-form allocator, exact membership decision procedure with its correctness argument, complete error taxonomies mapping all ten delegation conditions), the upstream calls are faithful (`checked_inc`/`shift`/`parent`/`ordinal` used within their stated preconditions, `validate`'s consume semantics respected, `transact`/`Staging`/`Snapshot` used per M2's contract including the pure-step/standalone-op dual form), the known traps are all handled (version-vs-document frontier separation via the true `(parent,g)` key, ω-vs-`owns` authorization divergence, the subspace-first-component misread, the node-minting-via-delegation composition hazard, the pre-snapshot delegation race), and the deviations from source notes and from the composition contract's guidance (O15(iii) narrowing, node-tier O10 drop, three space tags, M3-owned key constructors superseding the base-crate `key(home,…)` sketch, the (A,1) sixth chain family) are each explicitly confronted, argued soundly, and where policy-like, flagged under Open decisions. The membership-correctness invariant (decompose-and-range = exact chain membership), the ω-stability arguments (delegation induction + P8 + account-tier closure), and the single-probe top-down check (extensions form the contiguous block immediately after the prefix in T1 order) all verify.

**1. Revision list**

1. `[SHARPENING]` **State `apply_ns`'s totality domain at `apply_ns` itself, not only at the private `decompose`.** M2's `WorldState` contract demands a *total* fold, and the engine wires `World::apply` directly to the `pub apply_ns` — but the "≥ 2 components, T4-valid by caller contract" obligation that makes its `expect`s unreachable is documented only on `decompose`. Add one doc line on `apply_ns`: total over records staged by M3's own paths (mints extend a registered parent; `delegate` tier-checks first); a hand-constructed malformed `M3Rec` is outside its domain and fail-stops by design. The engine implementer reading the seam they wire should see this without descending into M3's internals.

2. `[SHARPENING]` **Make the multi-key acquisition assumption explicit in §8.** `delegate` and `create_new_document` hand M2 *two* `LockKey`s. Correctness of the in-closure gate discipline assumes M2 acquires the key set atomically/deadlock-free and takes `base()` after acquisition — trivially true under v1's single applier, but a per-key M2 realization must acquire in canonical order. One sentence naming this as M2's obligation (mirroring how the design already refuses to lean on the v1 global lock elsewhere) future-proofs the two-key ops.

3. `[SHARPENING]` **`next_account_prefix`'s "or if the namespace gate trips" None-cause is dead for all reachable inputs.** After the registered-node/account pre-gate, the only namespaces peeked are `(N,2)` with `zeros(N)=0` and `(A,1)` with `k=1` — both pass the TA5a gate unconditionally, so `checked_inc` cannot fail there. Either drop the clause or mark it defensive-only (corrupted-state), so a builder doesn't wire a live error path for an unreachable case.

VERDICT: CONVERGED
