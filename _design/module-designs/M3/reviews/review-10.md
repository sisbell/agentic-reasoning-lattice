Reviewed M3's build-spec against the decomposition, the M1/M2 interfaces as given, and the six source-note digests. The design is buildable and faithful: I traced the allocator math, the membership machinery, the delegation gate, and every upstream call, and found no material defect — only documentation-level sharpenings.

What I verified before concluding:

- **Allocator correctness against M1 as given.** `next_in`'s use of `validate` (consuming, clone-first), `checked_inc` (the TA5a gate is exactly B6(ii)/(iii) given a T4 parent — `k=1` always passes, `k=2` iff `zeros ≤ 2`, matching `zeros(p)+(g−1) ≤ 3`), and `shift` (called only on the full gated `c₁`, never a bare `doc·0·subspace` base, with `m ≥ 1`) all conform to M1's signatures and preconditions. The closed form `c_{m+1} = p ++ [0]^{g−1} ++ [m+1]` matches ASN-0040's `next`/`B2` exactly.
- **Membership is exact, for every chain shape.** I checked `decompose`'s `(parent, g)` inference against each mint family — documents `[A.0.n]`, versions `[d.n]` (including version-of-version, so V6's unbounded depth holds), sub-accounts `[A.j]`, node-parented accounts `[N.0.u]`, and content/link elements `[d.0.s.n]` under both documents and versions. In every case M1's `parent` (longest T4-valid proper prefix) recovers the true minting namespace, so the decompose-and-range check is genuine chain membership with no aliasing between `(A,2)`, `(A,1)`, `(d,1)`, `(b_C(d),1)`, `(b_L(d),1)`, and `(N,2)`.
- **The delegation gate is O15-faithful where it claims to be and explicitly documented where it deviates.** The ω-based condition (ii) is equivalent to O15(ii) given (i) (comparability of common-address prefixes plus O1b excludes ties); the single OrdMap probe for (iv) is correct because extensions sort contiguously immediately above the prefix; next-form is O17c-faithful and genuinely mandatory under the counter representation. The two deliberate narrowings — (iii) `≤1 → ==1` (NodeBaptism conflict) and the dropped node-tier O10 fork — are stated, argued, and flagged with re-admission paths, which is what the conflict-resolution bar requires.
- **Concurrency story holds without M2 v1's global lock.** Lock keys and frontier keys are byte-identical by one code path; the cross-namespace id-collision race is correctly identified as the reason `principals_lock_key()` must be global; the stale-safety claims (ω of an existing account/document is stable) actually follow from the design's own P8-at-delegate plus the `==1` narrowing — the argument chain is closed, including M5's pre-read for the branch-dependent VERSION lock.
- **M2 fidelity.** `transact`/`Staging`/`Seq`/`TxnError` usage, the `HasM3` + `From<M3Rec>` pattern, pure-step vs standalone-op dual forms, base-vs-working discipline, no nested `transact` (fork delegates the commit rather than nesting), default `rebuild_derived` with fully serialized authoritative state — all per the composition contract. Genesis satisfies O14/Σ₀ point-by-point.

## Revision list

1. **[SHARPENING] Reconcile the lock-key constructor's home with the Engine Composition Contract.** The contract (and M2's seam sketch — `key(home, subspace)`) places the shared namespace-key constructor in the shared base crate over `skep-address`+`skep-kernel`, with "every store building a namespace lock key calls that one constructor." M3 instead defines `ns_lock_key` + the four `*_lock_key` constructors in its own crate and directs M5/M7 to them. The substance (one constructor, injective encoding, byte-identity, acyclicity — M3 sits below every key-builder, and M4 builds no keys in this design) is fully achieved, but the divergence from the contract's stated placement is silent, and a dependent following M2's sketch literally could reach for a base-crate `key(d, s_C)` that doesn't exist. Either move `ns_lock_key` (keyed on `NsKey`, which the sketch's `key(home, …)` cannot express for version/document/account chains) into the shared base crate, or add an explicit note that M3's constructors supersede the sketch and why that placement satisfies the contract's intent.

2. **[SHARPENING] Add a Conflicts entry for sub-account delegation vs ASN-0047's K.δ case vocabulary.** The design admits sub-account chains `(A, 1)` (via `next_account_prefix`'s account arm and `delegate`'s gate), which ASN-0042 explicitly licenses (O7(c): `next(B, pfx(π'), 1)`; multi-component `U` in `acct`). But ASN-0047's K.δ has no admissible case for it — `e = inc(A, 1)` requires `t ∈ E_doc`, and `(A,1)` is absent from AllocatorHierarchy's five chains. The resolution (ASN-0042 governs delegation; the uniform registry treats `(A,1)` as a sixth chain family) is sound and already implicit, but it is currently undocumented — the one cross-note tension the Conflicts section doesn't state.

3. **[SHARPENING] Tighten `next_account_prefix`'s None conditions to include an unregistered parent.** As specified it gates only on the parent's *level* (node/account) and the namespace gate, so a peek under a validly-classified but unregistered parent returns a prefix that `delegate` is guaranteed to reject with `ParentNotRegistered`. Checking `entity_level(parent).is_some()` in the peek (it already reads a snapshot) makes the helper's answer honest about the one monotone gate it can pre-discharge, at no cost.

VERDICT: CONVERGED
