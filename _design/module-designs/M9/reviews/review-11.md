I audited M9 against the three source notes (ASN-0129/0130/0133 plus the consumed ASN-0128), the six upstream interfaces it builds on, and the decomposition. Every upstream call I traced typechecks against the interfaces as given (`observe`/`emit`/`nullify` signatures, `Vstream::insert`, `value_at(&Tumbler)`, `is_registered_document`, snapshot discipline); the audit-slice rebuilds match V-AUD's own equations; the UV self-exclusion, BH4 totalization, PD0/FP transcriptions, PR0–PR5a validation chains, and the Q0/Q3/Q-EXT machinery are faithful; all eight listed conflicts are real conflicts, resolved soundly; and the deliberate narrowings (BH2 at `Supersedes` only, no refs in rule domains, single-body scoping, single-deposit fires) are each flagged, typed, and safe-direction. I found no unbuildable component, no invented upstream call, and no dropped owned capability. What remains are precision issues in justification prose and two small API-contract gaps — none alters a signature, algorithm step, or error path a builder would implement.

**Revision list**

1. `[SHARPENING]` Make the two-transaction race analysis in §5 ("Two-transaction race, benignly absorbed") exact; as written it over-claims in two corners, though the shipped machinery already handles both correctly. (a) "Both fire actions are idem⊤" is unchecked for Marker: `register_rule` admits any cataloged Unary non-PredLayer `ty`, including an app-declared `idem = ⊥` type, for which a gap-planted witness produces a second redundant deposit (reported `Fired`), never a `Deduped` — extinction still holds, but the dedup-absorption story doesn't. Preferred fix: add an `idem = ⊤` check on `Marker.ty` at `register_rule` (one finite catalog check, matching the Q3/I1a analysis the certifier and race argument lean on); otherwise state the idem⊥ behavior. (b) "A nullify of an already-retracted target likewise dedups" is true only for a **same-home** prior retraction — S3's hit branch keys on `coverage(F′) = subtree(d_retr)`. A different-home gap retraction leaves the target resident (P-tgt is residence, so no error arises), but the call **misses** and deposits a redundant second `[R]` tuple, correctly reported `Fired`, not `Deduped`. State the three cases so the `Deduped`/`Fired` accounting and the benignness conclusion rest on M7's actual contract.

2. `[SHARPENING]` `register_pred` is specified as validating "against ONE pinned snapshot σ," but its (iii) leg calls `signature(r)`, which on a miss pins its **own** snapshot — so the validation is not literally single-snapshot. Add the one-line soundness reconciliation: signature facts are content-intrinsic and ever-registration is monotone, and (iii)'s own ever-registration check at σ gates first, so the mixed pins cannot disagree with a σ-only derivation. Without it, the Invariants entry ("read all constituents off one M2 Snapshot; sites: … register_pred/certify_stable validation") reads as contradicted by §Internal 4.

3. `[SHARPENING]` Scope the DefMemo poisoning claim "the failure cannot heal." It is exactly true for parse failures and poisoned-referent chains (content immutable), but a WT failure caused by a `DanglingReference` to a **never-registered** referent can heal in principle if that referent is later legitimately registered — reachable only via a PR-DISC breach, but the parenthetical asserts impossibility rather than policy. Restate permanent poisoning of breach-deposited starts as a deliberate freeze-on-breach policy (safe: no false positives) rather than a consequence of immutability.

4. `[SHARPENING]` The shared-registry injection (conflict #7) kills registry divergence, but `(reserved, decls)` is still passed twice — once to `LinkState::genesis` by the engine, once to `Coordinator::new`. Specify the catalog-build failure mode when a `decls` key finds no registration in the injected `Arc<TypeRegistry>` (fail `Coordinator::new` at construction, mirroring genesis's validate-once-or-fail), so the residual drift is caught at assembly rather than surfacing as spurious `UnregisteredType` at type-check.

5. `[SHARPENING]` `StepOutcome::{Fired, Deduped}` carry `rule`/`arg`/`seq` but drop the `effect` address that `FireOutcome` carries; carry it through (or state the omission), so a driver can log the deposit and reconcile the divergence monitor against the journal without re-deriving the deposited tuple's address.

6. `[SHARPENING]` `certify_rule(&self, rule: &Rule)` takes the raw submission, but legs (a)–(c) need the checked artifacts (`TypedDom`, ref-free trigger form) that only `register_rule`'s normalization produces. State the input contract: either `certify_rule` re-runs that normalization internally (and what it returns on a malformed rule), or it takes a `RuleId` and reads the stored `CheckedRule`.

VERDICT: CONVERGED
