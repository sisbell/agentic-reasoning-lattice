Reviewed M4 against the decomposition, the composition contract, M1/M2 as given, and the ASN-0036/0093 statements. The module is buildable as written: the slice/record/accessor/fold quartet matches the engine contract exactly (including the assembler's own names), both composable forms are present with bodies that typecheck against M1/M2's signatures, every invariant in the notes is either enforced, by-construction, or explicitly discharged upstream with the discharger named, and the five conflict resolutions are stated with sound rationale — including the load-bearing one (Conflict #3's reconciliation of M2's `stg.working().content()` minting example against M3's frontier ownership, which is what justifies `im::HashMap` over `OrdMap` and is supported by the decomposition's no-`M3 → M4` edge and M2's own M3-seam example reading `ns()`). I found no invented upstream call, no dropped guarantee, and no boundary breach. What remains are polish items.

**Revision list**

1. `[SHARPENING]` Pin the crate home of `S_C_SUBSPACE: Nat`. The design sources it as "a shared address-level constant … (M1 / shared address vocabulary)," but M1's interface as given exports no such constant, and the composition contract's skep-kernel `s_C` is the LockKey space-tag, not this numeral. Since the constant names `Nat`, its home is the shared base crate over `skep-address` + `skep-kernel` — or, given the feature is M4-local, off by default, and the value is pinned by ASN-0093's SubspaceConventionAxiom (`s_C = 1`), define it locally under `content-addr-guard` and say which. While there, drop `classify` from the guard's cfg-gated import list — the guard body as sketched uses only `level()` and `subspace()`.

2. `[SHARPENING]` In Open #1 (out-of-line values), state the constraint that M2's `apply` obligation (deterministic, total, side-effect-free) forbids blob-file I/O inside the fold — so an out-of-line variant must materialize blobs outside `apply` (e.g., at stage/commit time, or as a `rebuild_derived`-seeded cache). The current tradeoff text gestures at this only as "a blob-durability concern"; a builder picking that option needs the hard constraint named.

3. `[SHARPENING]` Reconcile the standalone `write` with Open #4's stated check ordering: with `content-addr-guard` on (full-guard sub-choice), a non-element `Address` handed to `write` panics at `document_of(...).expect` during lock-key derivation, before `stage_write`'s `NotContentAddress` check can fire — so "routing rejected on its own terms" holds on this path only for wrong-subspace element addresses. Either check level before deriving the key (returning the typed rejection when the feature is on) or document that `write`'s trusted-address contract supersedes the guard for wrong-level inputs.

4. `[SHARPENING]` Add one sentence reconciling `ContentWrite`'s private fields with the composition-contract checklist item "constructible by upstream producers … no private-field type that crosses a seam unusably": the type is constructible by its one sanctioned producer (`stage_write`, public to M5), lifted by the engine via `From`, and deserialized by serde at the definition site — so every seam crossing works and the restriction is the S0(b) guard, not an unusable type. The pieces are all present in the doc; stating the reconciliation once preempts checklist churn on every future review pass.

VERDICT: CONVERGED
