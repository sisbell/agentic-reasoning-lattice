Reviewed against buildability, upstream fidelity (M1/M2 as given), faithfulness to ASN‑0036/ASN‑0093, boundary discipline, conflict resolution, altitude, and internal consistency. This module is unusually tight: the engine‑plug names match the composition contract verbatim (`ContentStore`/`ContentWrite`/`HasContent::content`/`apply_write`/`Record::Content`), the write‑once guard is correctly anchored in `stage_write` as the private‑field sole constructor, the read‑side `&Tumbler` vs write‑side `&Address` split is principled (arrangement range is flat Tumblers), the M3/M4 decoupling (frontier ≠ `dom(C)`, ghosts, no `M3→M4` edge) is honored, and every M4‑relevant statement (S0/S1/S4/S5/S7*/C0/C1*/C2/C‑fin/SD) is attributed and either enforced or correctly discharged upstream. The Conflict‑#2 disentangling of the decomposition's loose "checked via M3" — S3‑proper is M4's `contains` over `dom(C)`, source‑exists is M3 — is the correct reading and improves on the seam prose.

I specifically scrutinized the build‑blocking serde note (`Tumbler: Serialize + DeserializeOwned` absent from M1 as given). It is **not** an M4 defect: M4 states M1's derive list truthfully, correctly shows it cannot repair it locally (orphan rule), and escalates it as a cross‑module precondition that the composition contract (`Clone + Serialize + Deserialize` slices) and M2's `WorldState` bound already require of *every* store. M4 is just the thinnest store to hit it and surfaces it first. Nothing in M4's text needs changing for it; the obligation must simply be carried out on M1.

No defects found. The remaining items are non‑load‑bearing.

## Revision list

1. **[SHARPENING] Give `FixedHasher` a concrete default so the slice type compiles as written.** The slice is declared `im::HashMap<Tumbler, Val, FixedHasher>`, but `FixedHasher` is only described (Open decision #5 fixes the *property* — fixed‑seed/deterministic — and defers the *algorithm*). Add the default alias inline, e.g. `type FixedHasher = core::hash::BuildHasherDefault<FxHasher>;`, and confirm it satisfies the derives' bounds (`BuildHasher + Default + Clone`, needed by `ContentStore`'s `Clone`/`Default`/serde). Keep the algorithm swap as the open part.

2. **[SHARPENING] Pin the shared `key(...)`/`s_C` seam M4 calls against, and fix the slice‑arg form.** The `write` body references `key(document_of(addr), s_C)` and `s_C` as imports from the shared base crate but never states their shapes; pin the expected signature M4 builds against (e.g. `fn key(doc: &Address, subspace: Nat) -> LockKey`, `s_C: Nat`) so the lock‑key derivation typechecks against a named seam rather than an unstated one. Also correct the pseudocode `transact([key], …)` to `transact(&[key], …)` (the param is `keys: &[LockKey]`).

3. **[SHARPENING] Reclassify S3 out of M4's "By construction" list.** S3 does not fall out of M4's data model/API shape — it is achieved by M2's commit atomicity and M5's composite enforcement, with M4 contributing only the `contains` oracle (the parenthetical already says exactly this). Move it to an explicit cross‑store framing so the heading ("falls out of the data model/API shape") doesn't mis‑attribute the strongest substrate guarantee to M4's mechanism.

4. **[SHARPENING] Split the S4 attribution.** As listed under "By construction," S4 reads as fully established inside M4. State the split: M4 provides only the value‑obliviousness half (keyed by address, never by value, no value→identity collapse); the distinct‑allocation‑events ⇒ distinct‑addresses half is M3's allocator discipline (T10a) that M4 *relies on*. This keeps the S4 theorem's two halves correctly homed.

VERDICT: CONVERGED
