**Review summary:** I traced every public signature against the ASN-0034/0045/0053 statements, re-derived the load-bearing constructions (`subtree_of`'s exact-subtree claim, `hull`/`cover`'s `shift(·,1)` reach convention, the `SpanRel` decision procedure vs SC's five cases, the `difference` dispatch table vs S11a/S11c/S11/S11b/S11d, `parent`'s two-component peel, the TA5a gate closed form), and checked the serde shadow-pair scheme against the composition contract. The decision procedure for SC is a correct and exhaustive refinement (I verified adjacency ⟺ `max start = min reach` and the remainder-case argument hold for non-degenerate spans); the difference table matches the S11 family case-by-case including the boundary-coincidence drop; the scoping against M3 (allocator state out, `inc` + gate predicate in) matches the decomposition exactly; the declared conflict resolutions (empty tumbler via T0, zero-sentinel quarantine, coverage-class mechanism/policy split) are sound and stated rather than papered over. Every owned capability in the decomposition's component list is present with a formula, dispatch, or named algorithm. I found no defect — only polish items.

**1. Revision list**

1. `[SHARPENING]` Pin the level gate's position relative to branch dispatch so out-of-contract edge behavior is builder-invariant. §6 says the gate "sits after classification, before construction" on `intersect`/`merge`/`split`/`difference`, but the `merge` bullet ("one comparison suffices … else WF") and `intersect`'s "self-guarding" wording can be read as skipping the gate on non-constructing branches — under that reading, `merge`/`difference` on *Separated* operands with mismatched levels return `Ok(None)`/`Ok({a})` instead of `Err(LevelMismatch)`. State explicitly that the gate (per-span level-uniformity **and** mutual compatibility) runs unconditionally before the branch (recommended), or specify the alternative; also fix the check precedence inside `from_endpoints` (does a pair failing both `s<r` and `#s=#r` yield `NotIncreasing` or `LevelMismatch`?).

2. `[SHARPENING]` Specify the remaining edge behaviors once: `normalize`/`canonical_key`/`is_normalized` on the empty span-set (S8's n = 0 case → `Ok(⟨⟩)`, vacuously normalized); `equiv` on two internally-uniform sets in *different* length classes (`Ok(false)` — denotationally sound, since equal non-empty denotations share a minimum element that is a start in both sets, forcing one length class); a note that `cover`'s output over a mixed-length point set is un-normalizable by design (S8's gate fires); and that `difference`'s ≤2-span output is emitted in N1 order and already normalized by construction.

3. `[SHARPENING]` Re-anchor the mixed-length justification in the M7 seam paragraph. The extracted S9 statement carries no length-class hypothesis in its preconditions; the one-length-class scoping enters through S8's normalization preconditions (level-uniform components, mutually level-compatible) and the `MutuallyLevelCompatible` definition. Cite those instead of "S9's uniqueness is proven only within one length class." The behavior (`canonical_key` → `LevelMismatch` on mixed endsets; per-class policy in M7) is right and stays unchanged — only the attribution needs correcting.

4. `[SHARPENING]` Name the source notes' algebraic laws as property-test obligations: S4a (split-then-merge identity), S3b (merge-then-split identity on adjacent pairs), S5 (split widths compose, `d ⊕ d′ = ℓ`), S3a/S10 (merge/union order-independence to canonical form), TS1–TS5 (shift order/injectivity/composition/monotonicity), TA-LC vs TA-MTO/TA-RC (left-cancellation, right-cancellation failure), and D1/D2 round-trip for `displacement`. These hold automatically when the ops follow the constructive definitions, so they cost nothing to satisfy and are the cheapest drift detectors for the arithmetic core.

5. `[SHARPENING]` The serde `try_from` shadows impose an unstated bound: serde requires `TryFrom::Error: Display`, so `EmptySequence`, `T4Error`, and `T12Clause` need `Display` impls (and `WfError` if ever used at a serde boundary) — as written the derives will not compile. One line in the error-type block closes it.

**2. Verdict**

The module is buildable from this document alone: every function has a concrete formula or dispatch, the arithmetic is implemented straight from the constructive definitions, the invariants section maps one-to-one onto the notes' guarantees (with the by-construction vs actively-enforced split correctly drawn), the M3/M7 boundaries match the decomposition, and the declared conflict resolutions are sound. The items above tighten determinism at out-of-contract edges and fix one citation; none blocks or misleads a builder.

VERDICT: CONVERGED
