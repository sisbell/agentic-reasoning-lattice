I checked the design against the three source-note digests (ASN-0034/0045/0053), the decomposition's ownership and seam assignments, and the engine composition contract, and re-derived the load-bearing constructions (the `merge`/`intersect` one-comparison tests, `subtree_of`'s exactness, the `[1,5] ⊖ [1,3,5]` level-gate example, `hull`'s tightness, the `parent` peel, the serde shadow symmetry). The arithmetic, the T4/T6/T12/S8-S11 contracts, and the M1/M3 and M1/M7 boundary resolutions all hold up; everything I found is polish, not blockage.

**Revision list**

1. **[SHARPENING]** `SpanRel` loses orientation, and the five case predicates are never spelled out. `Containment` doesn't say which span contains which, yet the `difference` dispatch table turns on exactly that (β⊂α → 1–2 spans; α⊂β → 0), and `ProperOverlap` orientation decides left- vs right-complement. This is faithful to SC (whose case (iv) is likewise undirected, with S11d splitting on orientation), and M1's internal `difference` can dispatch on raw endpoint comparisons — so it's buildable — but `classify_spans` is a public seam consumed by M6/M8, and every consumer must re-compare endpoints to recover direction. Either encode orientation in the variants (e.g., `Containment { first_contains_second: bool }`, `ProperOverlap { first_starts_first: bool }`) or state explicitly that the relation is undirected and give the disambiguation rule. While there, spell the five boundary predicates once (separated: max start > min reach; adjacent: max start = min reach; equal: both endpoint pairs equal; containment: one span's endpoints bracket the other's; proper overlap: the remaining case) so a builder doesn't reconstruct them from S11c's ordering chains.

2. **[SHARPENING]** `CanonicalForm` is write-only across the seam. It wraps the normalized `SpanSet` but exposes no read access (`Hash + Eq` only). M7's coverage-class policy — as this design itself specifies — must partition an endset by length class and *compose per-class canonical forms*, and the flagged open decision (serializing `CanonicalForm` into a collision-free `LockKey`) also needs the inner set. Add a read-only accessor (`fn as_set(&self) -> &SpanSet` or `into_inner`), which preserves the normalized invariant. This is the composition contract's "no private-field type that crosses a seam unusably" checklist item.

3. **[SHARPENING]** The preamble's parenthetical "M1 (or the shared base crate owning `Tumbler`) carries those obligations" contradicts the composition contract's crate graph. The contract fixes `Tumbler` in `skep-address` (M1) and places the shared base crate *above* it (it depends on `skep-address` and `skep-kernel`); `Tumbler` living in the base crate would be a dependency cycle. Rewrite as: the `num-bigint`/`im` serde-feature obligations land in `skep-address`, the crate owning the types.

4. **[SHARPENING]** `subspace(&self) -> Option<Nat>` returns an owned `BigUint` clone while every sibling projection borrows (`ordinal -> &Nat`, fields -> `&[Nat]`). Make it `Option<&Nat>`: it's the projection M5's S3★ content-vs-link routing calls per V-position, and the asymmetry is a needless allocation plus a style inconsistency.

5. **[SHARPENING]** `cover(points: &[Tumbler])` claims `|Σ| = |P|` for a point *set* but accepts a slice: duplicated input points yield duplicate unit spans, so `|Σ|` equals the slice length, not the set cardinality. One doc line resolves it (duplicates produce duplicate spans, still a cover; dedup is the caller's if the S7 cardinality reading matters).

No defects found: every owned capability from the decomposition's component list is present with concrete types and algorithms; the module correctly claims no upstream edges; the allocator/ω/spanfilade exclusions match the decomposition; the conflict resolutions (allocator boundary, empty tumbler via T0, zero-sentinel quarantine, coverage-class mechanism/policy split, `(start,width)`, account-vs-user) are stated and sound; and the Invariants section maps each source guarantee to a specific enforcement site consistent with the interface.

VERDICT: CONVERGED
