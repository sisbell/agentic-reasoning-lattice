I reviewed M1's design against ASN‑0034/0045/0053 (commitments, "what must be built," and the formal statements), the module decomposition, and internal consistency. This is a strong, faithful design: literal‑sequence storage (T3 free), `BigUint` components (T0a), the validate‑and‑classify single scan with a five‑way sum (Partition/Vacuity by construction), the `inc`/gate‑predicate split with enforcement pushed to M3, the `subtree_of` (δ(1,#p)) vs `hull` (`inc(max,0)`) distinction, intersect/merge self‑guarding, field‑absence⇒NO, and the `ElemPos` TA7a wrapper are all correct and well‑motivated. I verified the load‑bearing arithmetic (⊕, ⊖/zpd, `inc`/TA5a, `parent`/`document_of` peel, `elem_addr` guards, hull‑via‑convexity, normalize sweep) and found no algorithmic error, no invented/contradicted call (M1 has no upstream), no false claim about a note, and clean boundary discipline (allocator→M3, spanfilade→M7, content map→M4/M5, ownership→M3 all correctly excluded). The remaining items are precision/self‑containment improvements, none load‑bearing.

## Revision list

1. **`[SHARPENING]` Spell out `classify_spans`'s five SC conditions in the document body (§6 / the interface comment).** Today only Separated/Adjacent are pinned (via merge's `max start ≷ min reach` trichotomy); ProperOverlap/Containment/Equal — and specifically the SC convention that *one* coinciding boundary is **Containment**, not ProperOverlap — live only in the source statement SC, which the builder cannot consult. `classify_spans` is public (M6/M8/`difference` consume it). Add: among `max start < min reach`, `Equal` iff `start(a)=start(b) ∧ reach(a)=reach(b)`; `Containment` iff `start(a)≤start(b) ∧ reach(b)≤reach(a)` (or the symmetric); else `ProperOverlap`. (Buildable from standard interval relations without this, and difference is robust to the boundary convention — hence sharpening.)

2. **`[SHARPENING]` Make `difference`'s containment row emit complements conditionally (S11 sub‑cases).** The table's `Containment (β⊂α) → left + right complements → 2` is the generic case only; per S11 a coinciding boundary yields **1** span. A literal build constructs `from_endpoints(start(α), start(β))` when `start(α)=start(β)`, which the WF/T12 constructor rejects (`NotIncreasing`/`ZeroWidth`). State the self‑guard explicitly (as already done for `intersect`): emit the left complement only when `start(α)<start(β)` and the right only when `reach(β)<reach(α)` → 1 or 2 spans. The headline `≤2 (S11d)` is correct; only the row needs the drop‑empty guard.

3. **`[SHARPENING]` Define `divergence(a,b)` or restate `displacement`'s gate operationally.** §D/§4 gate `displacement` on `divergence(a,b) ≤ #a`, but `divergence` is never defined in the document and is *not* `zpd` (it is the first shared‑position mismatch, `#shorter+1` in the prefix case). Either give its definition, or restate the contract as "return `Some(w)` iff `add(a, w)` succeeds and equals `b`" — which is directly implementable and makes the "store endpoints, don't recompute" discipline self‑evident.

4. **`[SHARPENING]` State the `usize`‑length discharge explicitly against T0(b).** The design uses `usize` for length and argues "2⁶⁴ components is unreachable." ASN‑0034 requires arbitrary precision *or* a discharged unreachability proof; surface this as a deliberately discharged obligation (length grows ≤1 per version, OOM long before 2⁶⁴) alongside the `BigUint` component‑magnitude point, so a reader sees T0(b) was addressed, not overlooked.

5. **`[SHARPENING]` Tighten three internal‑consistency wordings.** (a) The S2 invariant "every algebra result is built through WF" should also credit `Span::new` (T12 likewise forces `reach > start`); (b) the level‑compat enforcement list omits `from_endpoints`, which enforces `#s=#r` itself via `WfError::LevelMismatch`; (c) consider one level‑mismatch representation instead of three (`LevelMismatch` struct, `WfError::LevelMismatch`, `SplitError::LevelMismatch`).

6. **`[SHARPENING]` Optionally package the S7 `cover(points) -> SpanSet`.** The decomposition lists "coverage" as an M1 handoff; the design provides `hull` (size‑1 cover) and documents the `|Σ|=|P|` fold but leaves it unpackaged. `hull` plus the documented `from_endpoints(t, inc(t,0))` fold suffice, so this is marginal — but a one‑line `cover` helper would close the listed handoff cleanly.

VERDICT: CONVERGED
