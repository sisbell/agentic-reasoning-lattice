The digest is strong — accurate on the load-bearing commitments, disciplined about altitude, and it correctly resolves the one genuinely conflicting piece of evidence (home-existence enforcement: it follows the code-level resolution and grounds it in L1a). The content-addressing warning, the stateless allocator with document-scoped concurrency, and the extensional-vs-coverage equality split are exactly the right things to call out. Below are the improvements worth making, most-important first.

---

**1. "Guarantees to uphold" table — the subspace-disjointness row is mis-scoped and erases a cross-layer dependency the note is careful about.** The row reads "links and content never collide," but the note proves disjointness *only* over the `s_C`-resident slice (L14: `dom(Σ.L) ∩ dom(Σ.C)|_{s_C} = ∅`; L1d(b)), and it explicitly lists extending this to all of `dom(Σ.C)` as an **open question** ("Should a content-side invariant fix a global content-subspace constant…"). Unqualified, the table claims more than the note supports and hides that the link layer *cannot unilaterally* guarantee disjointness — the content allocator must also keep content in `s_C`. Do: scope the row to "links and `s_C`-resident content," and surface this as a layer seam (parallel to how you correctly flag non-transcludability) — the link layer keeps links in `s_L`; disjointness holds only if the content layer maintains `s_C`-residence, and the universal version is an open spec question, not an invariant. (Commitment 2's "automatic (T7)" is already scoped to `s_C`, so it needs only a pointer to this caveat.)

**2. Builder decision #6 — surface that empty from/to endsets are conforming, or the create-boundary check will over-validate.** L3 requires non-emptiness on slot 3 (type) *only*; from/to may be empty — L9's conforming witness is literally `(∅, ∅, {(g, …)})`, L7's heading link populates a single content endset, and the Endset definition states `∅` is valid. As written, #6 tells the builder to "reject a missing or empty type endset" without stating the converse, inviting a builder to reject empty from/to by analogy and thereby refuse legitimate heading/single-endset links. Do: state explicitly that the create-boundary gate must reject *only* a missing/empty **type** endset; empty from/to endsets are conforming and must be accepted. Note the spec/impl gap from the evidence: Green's `setlinkvsas` always populates from/to, but the *model* permits them empty — build to the model.

**3. Persistence approach — "non-injectivity is required" misstates L11b.** L11b is explicit that non-injectivity is *permitted, not required* ("they do not require it"). The actionable conclusion (don't key by value, don't dedup) is correct, but the wording reads as contradicting the note. Do: reword to "the store must not *enforce* injectivity (non-injectivity is permitted — L11b), so keying by a hash of the endset value would collapse distinct links into one."

**4. Persistence/allocator approaches — repo-specific claims (`links.jsonl`, `paths.json`) are asserted as fact but aren't grounded in the note, evidence, or documented Green structure.** The design content they illustrate (append-only journal that never needs rewriting; an index/registry as a recomputable hint over the journal) follows directly from L12/L12a and stands on its own. Do: if these are genuine current repo conventions, keep them but mark them as repo-local examples rather than spec-derived; otherwise drop the filenames and state the principle, which is what's actually grounded. (The reviewer can't verify the filenames or their stated behavior — "never needs rewriting," "recomputable registry" — from the reviewable sources.)

**5. Link-search approach — minor altitude slip.** "with `im`'s persistent ordered maps" names a specific Rust crate; the design-relevant property is "a persistent/immutable ordered (interval) map giving cheap structurally-shared snapshots." Do: state the property; drop or parenthesize the crate.

---

Genuinely solid and needing no change: the **Implementation approaches** section overall (the content-addressing trap, stateless query-and-increment with the store-as-counter framing, scan-when-small, and the durability/query decoupling are well-grounded and Lampsonian); builder decision **#6's arity/conformance-gate insight** drawn from the empty-type-specset evidence (Q5) and **#5's coverage-materialization** caveat; the **non-transcludability layer-seam flag**; and the observation in **"How it fits"** that `PrefixSpanCoverage` belongs in the span/tumbler-algebra layer rather than the link ontology.
