This is a strong digest — accurate on the forced/conventional split, sound on every proposed approach (it correctly *forbids* the value-keying anti-pattern rather than walking into it), and it resolves the evidence's own internal disagreement on home-existence the right way (the KB synthesis for Q2 said "not enforced at allocation"; the code trace corrected to "enforced via `isaexistsgr`" — the digest sided with the code trace). The items below are the sharpenings worth making; most are precision, two touch real grounding/accuracy.

## Revision list (most important first)

**1. "What must be built" → link-search-index bullet: the pagination ordering basis is miscited to L5, which is about a different ordering.** The bullet grounds "enumerate matches in a stable order… I-address order (the order Green returns endsets in — L5 evidence) is the natural ordering basis." But the L5 evidence concerns the order *spans come back within a single endset* (the basis for arguing span order is semantically void) — not the order of *link matches* in a `FINDNEXTNLINKS` result. Neither the note nor the evidence specifies a pagination order over link results. Do this instead: drop the "L5 evidence" attribution, and present link-result ordering as a builder decision the note leaves open (address-derived order is a reasonable default, but flag it as a *choice*, not a grounded commitment). This converts a miscited assertion into a surfaced decision the digest currently buries.

**2. "Design commitments → Forced," subspace bullet: "told apart… not by a side flag" is misclassified as forced and contradicts the digest's own later recommendation.** What the note *forces* is "links reside in a distinct subspace `s_L`, disjoint from `s_C`-content" (L0/L1d/L14). "Not by a side flag" describes the *model's* structural discrimination — it is not a constraint downstream must honor. Green uses a side flag (`infotype`/`istextcrum`/`islinkcrum`), and the digest itself recommends keeping a leaf-type tag *authoritative* (Physical layout, decision 8). As written in the Forced list it reads as "you must not use a tag," which the digest later overturns. Fix: keep the forced content (disjoint `s_L`; the self-describing address makes a tag *unnecessary in principle*) and add that a redundant tag is permitted — and recommended until the global `s_C`-residence contract lands. While there, soften "This *is* the dual-primitive architecture": subspace separation supplies the *disjointness* half of L14; the "no third stored category" half is a separate claim.

**3. "Guarantees to uphold" → add L12b (HomeDocumentPersistence).** The table's "Home existence (L1a)" row covers home-exists-*at-create*, but the note separately names L12b: a link's home document stays allocated across *every* transition, so a link can never be orphaned by a vanishing home. It's derivative of document monotonicity (which the digest already invokes), but it's load-bearing for a builder reasoning about referential integrity over time. Add a row: "No orphaned links (L12b) — a link's home stays allocated forever — *Construction* (documents are append-only)."

**4. (Sharpening) "What must be built" → conformance-suite bullet: "exercises every L-invariant non-vacuously" overstates.** The note's own worked example explicitly *omits* L7 ("a META property… carries no per-state check and is omitted"), and L4 is likewise META. Change to "exercises every state-local L-invariant that carries a per-state check, non-vacuously after the six-step extension." The core recommendation — use the worked example directly as the acceptance vector — is good and should stand.

**5. (Sharpening) Forced bullet + table, non-transcludability: state its `s_C`-residence precondition.** L14a holds "*in any `s_C`-resident system*"; the digest asserts it unconditionally ("No arrangement may *ever* map a position to a link address"). The digest already flags this precondition for disjointness — add one clause that non-transcludability inherits the same content-side contract (discharged via S3 + L1d(b) over the `s_C`-slice), so the two cross-layer dependencies are named together rather than one stated absolutely.

## Solid as-is (no change needed)
- **Soundness of approaches.** Journal-authoritative / map-, index-, snapshot-, and high-water-hint-as-recomputable is consistently Lampsonian; the stateless query-and-increment default (the store *is* the counter) is the right default with the right escape hatch; nothing proposed violates a commitment.
- **Decision 6** (reject *only* a missing/empty **type** endset at the create boundary; don't generalize to from/to, with the L9 `(∅,∅,{g})` witness and the L7 heading link) is exactly right and well-grounded in the Q5 evidence — leave it.
- **"Do not reify allocation events"** (decision/allocator) is a sharp, correctly-grounded reading of the Q4 evidence; keep it verbatim.
