This is a strong, carefully-grounded digest. It reads the note's forced/conventional split accurately, makes the right soundness callouts (no value-addressing, no reified allocation events, no canonicalized stored form, no exact-span index), and correctly distinguishes the two levels of positional access (L6 slots vs. L5 spans). The cross-layer seam flags (non-transcludability, `s_C`-residence) are well-placed and honest about what the link layer can and cannot discharge. Most of what follows is sharpening, not repair.

**Revision list, most-important first:**

1. **"What must be built" → conformance-suite bullet: the omitted-property count is wrong. Fix it.** The bullet says "the META properties **L4 and L7** carry no per-state check and are omitted." But the worked example's verification line omits only **L7** ("L7… carries no per-state check and is omitted") and *includes* an explicit `L4 (EndsetGenerality) … ✓` step (it checks T12 span well-formedness for each span). Since this bullet tells the builder to "use it directly as the acceptance vector," getting the coverage description wrong is a real inaccuracy. Correct to: L7 is the sole omitted property; L4's state-checkable aspect (span well-formedness) *is* exercised. While here, note the worked example also exercises lemmas (L2, L8–L11b, L13) and the transition invariants (L12/L12a in the six-step extension), not just the state-local set — the current phrasing undersells what the vector actually covers.

2. **"Implementation approaches" → Address allocator (and Decision 2): the stateless allocator's "only cost is a max-under-prefix query" hides a structural prerequisite — surface it.** "The store is the counter" is cheap *only if the store can answer max-under-prefix cheaply.* Green gets this for free because the granfilade is an ordered enfilade; the digest's persistence section, by contrast, recommends replaying into "an in-memory map" without fixing its ordering. If that map is hash-keyed (the obvious choice for address→link lookup), max-under-prefix is O(n) and the per-home high-water hint stops being a "when it proves hot" optimization and becomes a prerequisite. Sharpen: state that the stateless query-and-increment presupposes an *address-ordered* store, tie the persistence representation's ordering to the allocator decision, and frame the high-water hint as required-when-the-primary-index-is-unordered rather than purely an at-scale add-on.

3. **"Implementation approaches" → Link store persistence: surface the note's own L11b "removable" vs. L12a monotone tension behind "no tombstones."** "No tombstones, no compaction for correctness, never needs rewriting" is correct *for the current operation set* (five FEBE ops, none a delete), but the note's L11b prose explicitly calls same-valued links "independently **removable**," which sits in visible tension with L12a's strict monotonicity. The digest resolves this silently. A builder reading both will hit "removable" and wonder whether to design for deletion. Add one sentence: "removable" in L11b denotes object-identity independence (two same-valued links are distinct, separately ownable), not a delete operation — the current model has none, so append-only is correct — but the "no compaction for correctness" claim is contingent on that delete-free op set, and a future deletion capability would reintroduce tombstones. This keeps the builder from baking "deletion is impossible" into the foundation.

4. **A cluster of minor groundings to tighten (all low-impact):**
   - *"What must be built" → search index:* "report match cardinality **cheaply** (count)" — the note names `FINDNUMOFLINKSFROMTOTHREE` but does not commit to it being cheap. The inference (a separate count op exists to avoid materializing all matches) is reasonable; mark it as inference, not a note commitment, so a builder doesn't treat "cheap count" as a grounded guarantee.
   - *"Design commitments" → forced bullet 2:* listing "Links live in a distinct, disjoint subspace `s_L`" as flatly *forced* is defensible (L0 is an invariant) but collapses a real seam — the note *forces* categorical distinguishability from content and *chooses* subspace separation as the mechanism (whose payoff is the self-describing address). Distinguishing the forced requirement from the chosen mechanism would also make the later "side flag unnecessary in principle" / "redundant tag recommended" pair read less like a contradiction.
   - *Allocator / Decisions:* the digest covers cross-document allocation independence but not the note's open question on *within-document* allocation ordering of links vs. content. The evidence answers it cleanly (content under `home.0.1.*` / TEXTATOM, links under `home.0.2.*` / LINKATOM — separate element-subspaces, so they don't contend), and saying so reinforces the dual-primitive separation at allocation granularity. One clause.

The rest stands. The forbidden-approach analysis (hash-keying collapses non-injectivity per L11b; don't reify L11a's "allocation events"; keep `Θ_split ≠ Θ_single` uncanonicalized; index on coverage-overlap not exact spans) is exactly the right set of soundness checks and is correctly grounded in the note plus the verified evidence. The L14 two-halves split, the StateExtension-is-a-definition-not-an-invariant catch, and Decision 6's scoping of the conformance gate to slot 3 alone (with the `(∅,∅,{(g,…)})` and heading-link counterexamples against generalizing to from/to) are all sharp and correct — leave them as is.
