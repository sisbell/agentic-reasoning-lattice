Reviewed against the note, the claim statements, and the evidence. The core termination treatment is accurate and well-pitched (more on that below); the defects cluster at the edges — two of them where the digest contradicts itself, one in an overstated "forced" commitment.

## Revision list

**1. [DEFECT] — The reaching-quiescence summaries are internally inconsistent and each individually inaccurate; reconcile to Q6's three distinct routes.** Design Commitment #7 lists "(extinction)" as a needed ingredient for reaching, but Q6 regime (i) (environment eventually idle) reaches-and-holds for *any* registry with **no extinction**. Worse, the Guarantees item "Reaching quiescence … needs … an environment hypothesis (idle, or turn-fairness)" is *false for regime (ii)*: a grow-only, all-SF, extinction-disciplined, bounded-growth registry reaches **and holds** under **weak fairness alone, no environment hypothesis** — which the Scheduler section itself states correctly ("sufficient to reach and hold quiescence (regime ii) — no environment-idle hypothesis"). So Guarantees contradicts Scheduler, and the "full termination is conditional and environmental" gloss erases the structural route. Fix: present Q6's three sufficient *packages* (not a conjunction): **(i)** environment-idle, any registry, no extinction/SF; **(ii)** grow-only + all-SF + extinction + bounded growth under weak fairness, *no environment hypothesis*; **(iii)** non-grow-only + all-SF + bounded growth, which reacquires H-SFAIR or per-rule environment-idle *to reach* (Q5a still bounds the work either way).

**2. [DEFECT] — Forced commitment #6 overstates monotonicity: "the surface is append-only/monotone … the only way the engine moves state is monotone deposition" is false.** The surface is `{Emit_K, Nullify_Binary}`, and `Nullify_Binary` is a *retraction* — it shrinks the active slice `A_K`, which is Q-FLIP's *first* re-armer for non-SF triggers. The digest even cites the Green evidence selectively: the same answer that shows the granfilade is append-only shows the POOM (the active-view analog) *deletes* V→I mappings. Append-only holds at the **substrate-domain/audit-slice** level (every `→_sh` step grows `dom M/C/L`; born-nullified tuples still enter `L_K`, so journaling-by-replay is sound) — *not* the active view. And "the engine deposits monotonically" is the *recommended SF/audit-slice discipline*, not a *forced* property: RG lets a rule's `Post_ρ` emit `Nullify_Binary`. As written, this also sits in tension with the change-propagation tier, which correctly indexes "retraction of a read slice." Fix: qualify the monotonicity to the substrate/audit level (keep the replay takeaway), state that Nullify retracts the active view, and move "engine moves state only by deposition" out of "Forced" into the recommended-discipline column.

**3. [DEFECT] — The scope restrictor omits the note's load-bearing tuple-domain constraints.** The digest commits to building a scope restrictor but specifies only "filter through the canonical bodies, preserving S-monotonicity." SC's sharp builder content is missing: the scoping body is the *rule's to declare* because over `dom(Tup)` several inequivalent bodies are well-typed and realize *different tiers*; a tuple rule reaches the **per-target** tier *only* through the finite `∃ y ∈ addrs_G(x) :: S(y)` — never through `addr` (which makes a per-target restriction **vacuous**, as the note shows for ρ_R), and never by quantifying over `coverage_G` (the infinite downward closure — non-finite, *ill-formed* as a QD domain; use a coverage *membership* test at each finite `y ∈ addrs_G(x)`). The digest already has this finite/infinite insight in "Indexing the Marker trigger" but fails to apply it to the scope body. Also surface Q8 scope re-entry (an out-of-scope fire can re-arm an in-scope trigger, detectable per-state by Q7) and Q9's anti-monotone nesting as the *reason* S-monotonicity is the standing constraint. Fix: specify the body choices, the per-target=finite-`addrs_G` rule with both wrong-ways called out, and the re-entry/nesting behavior.

**4. [SHARPENING] — Recognizability is presented as unconditionally forced, but Q0's PL-membership is conditional on PR-DISC for pdef-trigger registries.** The note: `expand` terminates only under PR-DISC, so "Q0's PL-membership is conditional on it for them"; inline-trigger registries carry no such premise. The digest has both pieces — it names PR-DISC (as a persistence "cost") and `register_pred` (How-it-fits) — but never connects them. Surface that pdef-trigger recognizability is conditional on PR-DISC, *discharged* by routing every pdef deposit through `register_pred` (ASN-0130), and make that a **registration obligation**, not just an auditing cost. (Auto-discharged on the standard path, hence sharpening, not defect.)

**5. [SHARPENING] — Surface the "fold non-⊥-stable conditions into the domain, not the trigger" technique.** The worked composition moves `needs_attention` into ρ_P's *domain* precisely to keep `T_P` a pure negated-existential SF spelling, at the cost of a non-grow-only domain that Q5a's union bound absorbs. This is a concrete, reusable move for *making* a trigger SF-classifiable and belongs next to the registration lints / "spell triggers SF over audit slices" guidance.

---

The spine is genuinely solid and I won't pad complaints onto it: the Q0–Q6 / Q-EXT / Q5a treatment, the three-tier evaluation design (full-scan → monotone work queue → change-propagation keyed off Q-FLIP), the type-isolation insight from the worked registry, the registration lints, and the admission-control/open-model honesty are accurate and at the right altitude. The Green grounding is disciplined: it correctly flags the Q3 dedup-miss argument as note-derived-not-Green, uses the coverage finite/infinite evidence faithfully, and navigates the "POOM is a separate V-space projection" nuance rather than over-claiming substrate monotonicity (which makes defect #2's slip the more conspicuous).

VERDICT: REVISE
