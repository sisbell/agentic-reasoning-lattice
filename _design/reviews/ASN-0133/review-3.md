The digest is accurate, well‑grounded, and at design altitude throughout. It navigates the evidence's POOM‑vs‑granfilade tension correctly (substrate domain append‑only, active view shrinkable), keeps every Green source‑claim grounded, and is disciplined about provenance — it explicitly flags the dedup/born‑nullified reasoning as note‑derived (Q3) rather than a Green‑source claim, and marks its repo analogies "not verified in‑session." One real defect.

**1. Revision list**

1. **[DEFECT] "Design commitments → Termination is conditional," parenthetical "…under Q4 mutual re‑arm or Q5a's 'stays ⊤ forever,' a non‑SF registry re‑fires without bound" mislabels Q5a's example and undercuts the note's central lesson.** Q5a's stays‑⊤ registry is explicitly an *SF* trigger lacking extinction discipline (it emits something other than its own falsifier — the `cmt`‑trigger `¬(∃ c ∈ L_cmt :: t ∈ coverage_G(c))` fired by emitting `res`); it is *not* non‑SF, and a genuinely non‑SF registry could not exhibit that SF‑trigger phenomenon at all, so the sentence is incoherent under any reading. It conflates the two failure modes Q5a deliberately separates — non‑SF (Q4 mutual re‑arm) vs. SF‑but‑not‑extinction (Q5a stays‑⊤) — and carries the misleading implication that SF‑spelling *alone* immunizes against unbounded re‑firing, when Q5a's whole point is "both hypotheses are load‑bearing." Fix: attribute precisely — "Q4 mutual re‑arm (a non‑SF registry) *or* Q5a's stays‑⊤ (an SF‑but‑not‑extinction‑disciplined registry)" — and state the consequent as "a registry *outside the all‑SF+extinction class* re‑fires without bound." (The digest gets this right elsewhere — "all‑SF+extinction registry" in the same bullet, "internal re‑arm" in regime (i) — which is exactly why the local mislabel should be brought into line.)

2. **[SHARPENING] "What must be built → status monitor" and "Decisions → What you report": carry the note's Open Q3 as a third done‑ness axis.** The digest cleanly separates "registry inert" from "system quiescent" but drops Open Q3 — a registry can be quiescent at *every scope of a tier* without being globally quiescent ("every document converges but the corpus doesn't"), and whether per‑scope settling implies global is open. An operator watching per‑scope quiescence could wrongly infer global done‑ness; the status monitor is exactly where this bites. Add it as a third reportable state, flagged as open.

3. **[SHARPENING] "Guarantees to uphold" omits the scope theorems.** The checklist lists Q0/Q1/observer‑uniformity/Q‑EXT/H‑ATOM/H‑FIN/reaching but not Q7 (scope recognizability — `quiescent_{R,S} ∈ PL`, by construction from Q0) or Q9 (anti‑monotone nesting — upheld by keeping scope bodies S‑monotone). Both are enforced obligations the scope restrictor must preserve; listing them makes the checklist match the components actually built.

Genuinely solid, needing nothing: the open‑substrate framing and the registry‑inert‑vs‑quiescent honesty surfaced repeatedly; the three‑regime termination decomposition (i/ii/iii) with the "bounded input is environmental in every route; regime (ii)'s only edge is needing no idle" synthesis; the Marker‑trigger indexing (single‑tuple coverage = tumbler‑prefix truncation, aggregate = spanfilade/prefix‑trie containment), correctly grounded in the coverage_G evidence; the dedup‑is‑tuple‑identity‑not‑content‑hashing guard; and the SF‑folding / acyclic‑dependency‑graph registration pattern with the cmt/res one‑way‑edge reading (sharper than the note's own loose "no fire of either rule enlarges the other's domain").

VERDICT: REVISE
