Reviewed against the note, the formal claims, and the verified evidence. This is a strong, careful digest: the forced/conventional tags are accurate, the implementation approaches respect the note's commitments (agenda-as-hint with Q0 as authority, views-as-filters over the grow-only base, coverage-as-membership, MVCC discharging the snapshot obligation), the altitude is right throughout, and the component list is complete. Two things deserve specific praise: the Green-vs-note distinction on active-by-subtraction (correctly grounded — granfilade/spanfilade append-only *and* the POOM mutable, per the evidence, with the nullification model flagged as the note's own choice rather than a Green inheritance) is exactly the grounding work this should do; and the chain/audit/default treatment in Commitment 6 is precise (chain is the exception to *fixed-view-base reconstruction*, not to *audit serving*).

No material defects found. The following are sharpenings.

## Revision list

1. **[SHARPENING] Commitment 4 + lint check (a): name the body-stability condition for SF.** The note is careful — "negated existentials over grow-only audit slices *with step-constant bodies* — SF spellings." The digest drops "step-constant body" in both places. A negated existential over a grow-only audit slice whose body reads an *active/mutable* slice is **not** SF (a witness can disappear, re-arming `⊥→⊤`). For the Marker pattern (coverage-membership body, which reads the quantified tuple's own content) this holds automatically, and lint (a)'s reference to "PD0's quantifier rule" carries the full check — so the lint is sound — but the prose principle should state the body condition so it isn't over-applied to arbitrary audit-domain existentials. Also cite "PD0's quantifier **and Boolean** rules" (the leading `¬` needs the Boolean rule), per the note's worked example, not just the quantifier rule.

2. **[SHARPENING] Commitment 3: registry-inertness is itself conditional on H-RF + H-FAIR.** "The engine may promise 'I will go inert,' never 'the system settles'" reads as a clean dichotomy (registry-inert achievable; only system-settling at risk). But the note's own cyclic-non-SF pair shows a registry can diverge *with no environment help* — drop H-RF and the rules re-arm each other forever, never going inert. Going inert is the Q6 *registry-side, unconditional-in-the-environment* guarantee — but only **under H-RF + H-FAIR**. Tie the headline to the digest's own divergence-monitor/lint sections (which correctly treat registry-internal divergence), so the dichotomy isn't read as "the registry always goes inert."

3. **[SHARPENING] Commitment 1: the H-HOME justification is inverted.** Granting rules `K.σ`/`K.α` power would not "break H-HOME's discharge" — it would *collapse* H-HOME from an external standing hypothesis (homes supplied by `Σ₀`/environment, "never by the fire itself") into one the fire self-discharges, dissolving the deliberate weakness of the rule layer. State the boundary (rules deliberately cannot own the namespace; the surface exclusion is forced) without the back-to-front mechanism.

4. **[SHARPENING] Incremental-agenda approach: fix the falsifier-inventory citation.** "ASN-0129's falsifier inventory (Q-FLIP)" conflates two things: the inventory is ASN-0129's **FP**; **Q-FLIP** is *this note's* proposition that reads it off (with PD1/PD2). Write "the falsifier inventory (ASN-0129's FP, cataloged by Q-FLIP)" so the builder cites the delta-rule source correctly. (Elsewhere the digest uses Q-FLIP correctly as a 0133 result, so this is purely local.)

VERDICT: CONVERGED
