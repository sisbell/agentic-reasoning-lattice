Skeptical read complete. This is a strong, scrupulously-grounded digest: it correctly separates substrate-domain (append-only) from active-view (Nullify/POOM-retractable) monotonicity using the evidence answers; it reads dedup as `I0`/`A_K` *tuple-identity*, explicitly *not* content-hashing (avoiding the value-based-identity trap); it forbids quantifying over `coverage_G`; it renders the three-regime termination structure and the H‑RF-is-separate-outside-all-SF point accurately; and it sources every Green claim to the note, evidence, or documented enfilade types (hedging the unverifiable repo analogies). I found no material defect. The items below are refinements.

**Revision list (most-important first):**

1. **[SHARPENING] Scheduler / Guarantees — "the engine's total work is bounded by total external input (Q5a)."** Q5a's *literal* bound is on the domain union `Σ_ρ |⋃_k [D_ρ]|`, which equals external input only when own emissions don't amplify it — i.e., under the acyclic/type-isolated structure the digest itself recommends in Registration Analysis. A *cyclic* all-SF+extinction registry can grow its own domains unboundedly from bounded (even single-item) external input, so "bound external input ⟹ termination" holds only when paired with the acyclic-dependency discipline. The note's own framing ("Q5a is a sufficient condition on external input") backs the phrasing, but the worked example earns it via type-isolation — link the admission-control advice to that structural requirement so a builder doesn't under-provision for a non-acyclic engine.

2. **[SHARPENING] Design commitment 4 — "'the registry is done firing' is a strictly weaker claim than 'the system is quiescent.'"** As worded this invites a false reading: *currently* quiescent and registry-done-firing are **incomparable** (env re-entry breaks the first→second direction; residual env-driven non-quiescence breaks the second→first), which is exactly why the digest's own status-monitor calls them "genuinely different," not ordered. The strict-weakening is true only against "*reaches and holds* quiescence." Write "reaches (and holds) quiescence" to make the ordering hold and to stop contradicting the status-monitor line.

3. **[SHARPENING] Scheduler — "a plain FIFO agenda is weakly fair and sufficient to reach and hold quiescence (regime ii)."** The standalone sentence drops regime (ii)'s *bounded-growth* hypothesis: FIFO + SF + grow-only still never *reaches* quiescence if the environment floods the grow-only domain unboundedly. Add "under bounded growth" to align the aside with the Guarantees section, which states regime (ii) correctly.

4. **[SHARPENING] How it fits — "A_K ⊆ L_K … is what makes the audit-slice SF discipline … go through."** `A_K ⊆ L_K` is the ingredient of the **Marker dedup** argument (an active hit injects into `L_K`, contradicting a trigger-true fire), not the source of **SF-ness**: an audit-slice trigger is SF because `L_K` is *grow-only* (PD0 ⊥-stability), independent of the subset relation. Attribute SF to `L_K`'s append-only growth and reserve `A_K ⊆ L_K` for the dedup argument (the digest's parenthetical already pairs it correctly there).

5. **[SHARPENING] Open-question coverage (minor).** The digest surfaces Open Q1 (`pd_extinct`), Q2 (divergence detector), Q3 (per-scope vs global) but not Q4 (bounding cross-scope re-entry, or whether *monotone progress across entries* is the right demand — the cascade/re-opening theory) or Q5 (contract *necessity*). Q4 is the theory a builder reaches for precisely where the digest discusses Q8 re-entry across scopes; name it there.

(Optional, lowest priority: the two unverified repo analogies — pipeline buffer/flush, `links.jsonl`/`paths.json` — are honestly hedged but carry no load; trim or verify.)

VERDICT: CONVERGED
