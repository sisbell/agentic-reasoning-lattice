The digest is strong: it reads the note accurately, gets the hard forced/conventional distinctions right (especially R3-binding vs R4-conventional, and content-subspace losslessness-vs-bindingness), and its central contribution — diagnosing the reference's fan-out bug, localizing it to exactly the budget-bounded merge step, and confirming via the evidence that the enumeration beneath it is reusable wholesale — is precise and well-grounded. The five-component pipeline and the guarantee→{by-construction/enforced/inherited} mapping are complete. Below are the improvements worth making, most-important first.

---

**1. "Implementation approaches → Matching" + "Decisions → Matching algorithm / Region representation": the hash-join default contradicts the digest's own block guidance and miscasts block-structure as a special case. Reconcile.**

The digest recommends *hash/sort join on per-position address* as "the default," but a per-position hash join must **expand every block to positions** to build/probe the multimap. That directly contradicts the region-representation guidance "Resolve to blocks; **expand only where fan-out forces it**" — which is interval-join discipline (only the overlap-refinement step expands), not hash-join behavior. Worse, the digest frames block-structure as a condition ("prefer interval *when* the arrangements are block-structured") when D-SEQ★ + the POOM enfilade make block representation the substrate's *general* shape (the resolver's own output is `(document, V-start, address-start, width)` blocks, one per bottom crum). So the recommended default throws away the block structure the resolver just produced, then asks the presenter to re-coalesce it.

Fix: keep the hash join as the simplicity baseline and test oracle (that framing is legitimate and valuable), but (a) state its full-expansion cost explicitly rather than letting "simplest" hide it; (b) recharacterize block-structure as the substrate norm, so the interval/block-preserving join is the natural *production* default for non-trivial regions, not a fallback; (c) make "expand only where fan-out forces it" consistent — note it is an interval-join property and that the per-position hash expands unconditionally.

**2. "Implementation approaches → Matching" (hash/sort bullet): the build/probe phrasing invites a silent operand-order (within-pair) violation.** "Build a multimap for the smaller side, probe with the larger, emit cross-products" — if the smaller side is operand 2 and the builder emits in build/probe order, the report comes out transposed: `compare(B,A)` mislabeled as `compare(A,B)`. The note makes within-pair order **semantic** (X3), and the whole symmetry guarantee rests on slot *i* = operand *i*. Add the explicit guard: output slot order must follow operand order regardless of which side is built or probed. (The commitment is stated elsewhere; the approach paragraph is exactly where the mistake gets made, so the reminder belongs there.)

**3. "Implementation approaches → Resolving a region": the "Rust/`im` stack" specific is ungrounded.** The persistent-ordered-map design and its properties (windowed range-scan, O(1) cheap snapshots from structural sharing, lock-free reads) are sound and at the right altitude — but naming the Rust `im` crate introduces a platform assumption that appears in neither the note nor the evidence (both of which concern the C reference only). Either cite the project's actual chosen target stack if that is known, or generalize to "a persistent ordered map," so the digest doesn't smuggle in an unverified platform commitment.

**4. (Sharpening) "Implementation approaches → Presenting": ground the canonical order in the reference's actual ordering and surface the free correctness check.** Q14 establishes that the reference already emits in first-operand V-order, which **coincides with X11's canonical order exactly on injective windows** — the second-foot tie-break only matters under fan-out. Stating this gives the builder (a) a confirmation that an operand-1-V-ordered resolver is already canonical for the common case, and (b) the sharpest test for the tie-break in isolation: a fan-out region where two runs land on one first-foot start. This strengthens the otherwise-correct "the tie-break is load-bearing" claim with the reason *why* it is the only divergence point.

---

Two places I checked hard and found solid, so flagging only that they're fine: the precondition-gate treatment of `σ = ([1,5],[3])` (content-start span denoting link positions is legal and clipped; only a link-*start* span is the precondition matter — exactly the note's distinction), and the multi-document single-Σ snapshot requirement (a real builder consideration the note only implies via X5; the digest correctly derives that resolving named documents at different states breaks R3). Both are genuine value-adds, not defects.
