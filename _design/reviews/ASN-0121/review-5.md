I worked through the digest against the note, the formal claims, and the evidence. The digest is unusually disciplined on the soundness traps — it gets the load-bearing things right, so most of my scrutiny went to whether any "forced" claim is actually conventional, whether any approach violates a commitment, and whether the Green-grounded "repair" claims hold. They do. What remains is precision, not substance.

## Revision list

**[SHARPENING] "What must be built" (request-decoder bullet): "FL-WILD and FL-EMP are both unrealized" overstates how much of each lemma the reference fails.** The reference realizes most of *both*: it correctly short-circuits a constrained-empty from/to slot to ∅ (Q7), correctly drops individual wildcard slots (Q15), and correctly omits an empty *link* endset from its slot index (Q2). Only one corner of each diverges — FL-WILD's all-wildcard *limit* (returns ∅) and FL-EMP's *Θ = ∅ expressibility*. Reword to "the all-wildcard case (FL-WILD) and the constrained-empty-type case (FL-EMP) both fail in the reference, for distinct reasons," so a builder doesn't think the lemmas are wholesale unimplemented. (Repairs (b)/(c) already scope the actual fixes correctly — this is just aligning the headline to them.)

**[SHARPENING] "How it fits" (dependency enumeration): the "leans on" list omits ASN-0127, which is load-bearing.** The list names ASN-0034/0043/0086/0093/0098 but skips ASN-0127, whose F-PRES underpins "K.λ is the unique result-changing transition" (hence the cache-invalidation policy), and whose F-CIL routes FL-STB. It is discussed in the *following* bullets (F-FIND, `findlinks_V`), so a builder won't miss it — but the foundational-machinery list should include it (F-PRES, F-CIL) for completeness, since those are proof machinery the query leans on, not just sibling operations.

**[SHARPENING] "Design commitments" (first bullet): "served from a link index" overcommits to mechanism inside a *forced* commitment.** The note's forced content is only "function of Σ.L" (FL-LOC); the digest's own implementation section then recommends a *scan first* (no index). Change to "served from the link store" (or drop the clause). The load-bearing point — "global, content-identity, not a per-document walk" — stands without asserting an index.

**[SHARPENING] "Implementation approaches"/"Decisions": "keying buys stability and reach for free" sits in tension with the precise Guarantees-section statement.** The Guarantees section gets this exactly right ("the keying choice does not create the guarantee … only decides whether the index stays faithful"). The earlier heuristics should be aligned to that precision: content-identity keying buys a *faithful index* for free, not the guarantee — the answer is stable regardless because the operation never reads Σ.M.

**[SHARPENING] Open-questions coverage: the home-set→single-subtree-prefix reduction is the note's fifth open question but isn't flagged as one.** The digest flags four of the note's five open questions; it presents the unit-depth-prefix residence reduction purely as a fast-path design choice. The note marks the *precise condition* under which residence collapses to one containment check (rather than span-set membership) as open — note it as open alongside the others.

**Solid:** the soundness-trap handling is exactly right and directly guards the note's commitments — dedup **by address, never by value** (with the a₁/a₅ witness), never-GC orphan entries, never filtering the retraction slice to addressable retractors (R6b non-fixpoint), indexing retraction links as ordinary results, and storing retraction *coverages* not resolved addresses so born-nullified and self-retraction fall out. The FL-JUNK treatment is also genuinely good: separating the answer-invariance lemma from Nelson's complexity intent (flagged as the digest's *own* engineering goal, not a note claim) is the correct reading, and the "junk is harmless — except retractions that touch the standing store" caveat (FL-WP(c)) is precisely stated. The three repairs (a)/(b)/(c) are each grounded in the evidence and each fixes a real divergence without violating a note commitment.

VERDICT: CONVERGED
