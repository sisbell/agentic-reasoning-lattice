The digest is strong: every Green claim is grounded in the evidence answers (no fabrication, no ungrounded function-level claims), it stays at design altitude (no signatures/types, never vague), and it correctly catches several subtle load-bearing points — `findlinks_V` wraps F-FIND not `findlinks_FTT`; R6b's non-fixpoint means *don't* filter the retraction slice to addressable retractors; dedup must be by address not value (with the a₁/a₅ witness); born-nullified requires testing *fresh* addresses against standing coverages; orphan reach forbids GC-on-arrangement-departure. The forced/conventional tags are all justified, the guarantees are stated with correct "by construction" vs "active enforcement" splits, and the proposed approaches violate no commitment. I found no material problem.

# Revision list

**[SHARPENING]** *"What must be built" → request decoder.* The headline "FL-WILD and FL-EMP are *both* unrealized" mislabels the FL-EMP gap. The note explicitly says "Gregory's back end **realises** exactly this asymmetry" — a constrained slot that resolves to empty short-circuits to ∅ correctly (Q7); FL-EMP's *semantics are implemented*. What is *not* realized is the **expressibility** of a grammar-level `Θ = ∅`, which the wire format collapses to NOSPECS (Q2) — an encoding gap, not a semantic one. This is precisely what the digest's own repair (c) says ("the fix is the encoding alone, not the short-circuit logic"), so the "unrealized" umbrella here is in mild tension with the digest's later, correct framing. Reword to keep the distinct-causes structure but make the asymmetry explicit: *FL-WILD's all-wildcard answer is semantically wrong in the engine (returns ∅); FL-EMP's short-circuit semantics are already correct, and only the `Θ = ∅` input is inexpressible in the encoding.* (Optionally note the gap is specifically the specset-level `SpecSet([])`→NOSPECS collapse, since a present-but-empty constrained slot already short-circuits correctly per Q7.)

**[SHARPENING]** *Design commitment 1.* "served from a link index" is embedded inside a commitment tagged **Forced**, but no index is forced — the digest's own implementation section ships a no-index full scan as the reference oracle. Soften to "served from the link store (whether by scan or index), not by a per-document walk," so the genuinely-forced property (function of `Σ.L` alone, document-independent) is not conflated with the optional index optimization.

VERDICT: CONVERGED
