Overall this is a strong digest: the Forced/Conventional split is accurate (mutates-arrangement-only, relabeling/π-bijection, piecewise-uniform translation, two shapes, partiality, origin-verbatim, and the depth‑2 *scope* boundary are all correctly classified as forced; run-partition-as-storage correctly as conventional). Every Green claim is grounded in the evidence (`tumbleradd`/`sortknives`/`makeoffsetsfor3or4cuts`/`rearrangecutsectionnd`, success-before-work, `abort()`-on-straddle, no-guard cross-subspace corruption, `logbertmodified`, the commented-out `ht`/spanfilade update, text at `1.x`/links at `2.x`). The "implicit-positions = no displacement arithmetic, with the note's π/displacements as a verification oracle" recommendation is exactly the right Lampson move and violates no commitment. The `R-PRE(ii)`-subsumed-by-coverage-given-CS5, last-cut-≤-N+1, and width-positivity-needs-both-CS2-and-coverage observations are all correct and show real reading of the note. The defect below is contained but is a shipped false statement, so it blocks convergence.

**Revision list**

1. **[DEFECT] "Canonicalization (merge)" — the no-cascade argument's clause (b) is false as written.** It claims a seam-merge product "inherits the *outer* endpoints of the two runs it fuses, and those endpoints were already region-boundary positions." When a region holds more than one run, the fused runs are that region's *last/first* run, whose outer endpoints are *interior* to the region (at an internal run boundary), not region boundaries — so the justification is wrong. (Counterexample: pivot with β = two distinct-origin runs; a β|α seam merge's left endpoint sits at the run-run boundary inside β.) The conclusion (`#seam-merges ≤ n`, hence `|Δ canonical runs| ≤ n`) is correct, but it follows from clause **(a)**, not (b): a seam-merge product's only inward-facing neighbor is the adjacent interior run, and (a) already establishes that uniform per-region translation keeps every interior pair non-mergeable, so the product cannot merge further inward; cross-seam adjacencies are counted in the ≤ n seams. **Fix:** delete the "region-boundary positions" justification and derive no-cascade from (a) plus the seam count. (The bound itself stays; flagging because a digest must not ship a false claim even when the actionable takeaway — "size storage for ≤ 4 added runs" — survives.)

2. **[SHARPENING] "How it fits" reverse-index lever — strengthen the argument, soften the Green citation.** The load-bearing claim is sound and well-hedged: because `ran(M') = ran(M)` (R‑RI), a *membership* reverse index ("which documents reference this content") is invariant under rearrange, and only a *V-position*-recording index must be touched (incrementally via π). But "consistent with Green's commented-out index update on rearrange" is weak and double-edged: the same Green rearrange path is this digest's cautionary tale elsewhere, and the evidence does not establish whether Green's spanfilade records V-positions (if it does, Green's omission is a staleness bug, not a safe optimization). Rest the claim on ran-invariance alone and demote/drop the Green "consistency" corroboration.

3. **[SHARPENING] "The arrangement map" / "Decisions" — trim the code-level pointer toward altitude.** The abstract contract ("persistent ordered sequence with O(log n) split/concat and structural sharing") is the correct altitude and is stated well. The concrete `im::Vector` / "RRB-vector of spans" pointer is borderline implementation-level; it's hedged as swappable so it's defensible, but consider reducing it to a single bracketed "e.g." so the digest stays implementation-neutral. Optional.

VERDICT: REVISE
