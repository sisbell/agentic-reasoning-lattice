Overall this is a strong digest: the core commitments (content-preserving permutation, piecewise-uniform block translation, partiality/precondition-gating, origin-verbatim, depth-2 scope) are read accurately; the Green claims are all grounded in the evidence (no fabricated source-level behavior); the implicit-position recommendation, the μ-sign trap, eager seam-merge, and structural subspace-confinement are sound and well-justified; and the beyond-note material (durability, run-count bound) is honestly flagged. The forced/conventional split is correct. The problems are concentrated in a few prose claims.

**Revision list (most important first):**

1. **[DEFECT] — *Cut validation and atomicity*: "It's that strict ordering — not coverage — that forbids degenerate zero-width regions" is false.** The note's Width-positivity derivation uses *both* preconditions: "By R-PRE(iv) [coverage] and D-SEQ … the widths are computable from the cut-point ordinals" (this is what makes region count = ordinal-difference), *and* CS2 supplies ord(c\_{i+1}) − ord(c\_i) ≥ 1. Coverage is not optional here: a strictly-ordered sequence whose cuts sit past max(V\_S(d)) produces an empty region (take a pivot with c₁, c₂ both beyond N ⟹ β = ∅, w\_β = 0), and it is precisely coverage that excludes that. The conclusion "every region width ≥ 1" is correct; the attribution is wrong. Fix: credit both CS2 (ordinal gap ≥ 1) and R-PRE(iv) (count = ordinal gap); delete the "not coverage" exclusion. (The build instruction survives — coverage is separately listed as a required check — but the claim as written contradicts the note.)

2. **[SHARPENING] — *How it fits* (find-documents lever): soften the Green causal claim.** The design observation is sound and grounded (`ran(M')=ran(M)` ⟹ a *content-membership* reverse index is invariant under rearrange; only a *V-position*-recording index must be touched, via π). But "which explains why Green could leave its index update on rearrange commented out" overreaches: the evidence lists the commented-out "ht stuff" *alongside* Green's other rearrange defects (success-before-check, no bounds, abort-on-straddle) and does not establish the spanfilade is membership-only. Present this as a design observation, not as Green's rationale.

3. **[SHARPENING] — Design commitment #2 ("relabeling, not an edit"):** this reads as a contradiction with "one editing primitive" in *What this is*, and it bundles two *independent* guarantees under one cause. Clarify it's not a *content-mutating* edit, and separate the reasons: content-store immutability (`C'=C`) is an independent frame condition (separate store, untouched), whereas referential integrity / range-invariance is what actually follows from π being a bijection. Don't derive content permanence from the bijection.

4. **[SHARPENING] — *The arrangement map*: "Sorted span/run list … rearrange is O(spans touched)" undercounts a contiguous representation.** A sorted array/list pays O(total spans) to shift a reordered block; genuine O(spans-touched) splice with O(log n) lookup is the *tree/rope* row, not the flat list. Attach the cost to the right representation.

5. **[SHARPENING] — Canonicalization run-count bound: lead with the robust direction.** The *increase* bound (Δ ≤ n: splits add ≤ n, merges only reduce) — which is exactly what the note's open question asks — is airtight. The two-sided `|Δ| ≤ n` and especially the no-cascade justification (b) ("inherits the outer endpoints … already region-boundary positions") are hand-wavier than the rest of the section. Foreground the increase bound; tighten or trim (b).

VERDICT: REVISE
