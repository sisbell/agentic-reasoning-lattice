The digest is accurate, well-grounded, and stays at design altitude; its Green source-level claims (`makeoffsetsfor3or4cuts`/`rearrangecutsectionnd`/`tumbleradd`/`sortknives`/`logbertmodified`, the success-before-work handler, the `abort()` on a straddled span, the commented-out index update) all trace to the evidence answers. The forced/conventional split is right — especially the call that run-partition-as-storage is *conventional*, not forced. But it ships one false causal claim.

## Revision list

**1. [DEFECT] — Section "Cut validation and atomicity": "It's that strict ordering — *not coverage* — that forbids degenerate zero-width regions, so every region width is ≥ 1" is false.** The note derives Width positivity from *both* preconditions: "Under R-PRE(iii) **and** R-PRE(iv)…". CS2 (strict ordering) only gives positive *cut-ordinal gaps* (`ord(c₁) > ord(c₀)`); it is R-PRE(iv) (coverage) + D-SEQ that makes each gap *count actual V-positions* (`|α| = ord(c₁) − ord(c₀)`). Drop coverage and strictly-ordered cuts placed past `max(V_S(d))` delimit *empty* regions — e.g. `V_S(d) = {[1,1]}`, cuts `[1,1],[1,2],[1,3]` are strictly ordered yet `β = ∅`, rejected *only* by coverage. The build instruction (validate both) survives intact elsewhere, but the prose denies coverage's role and contradicts the note. Fix: credit strict ordering for non-zero gaps **and** coverage for populating them; don't say "not coverage."

**2. [SHARPENING] — Design commitments / Guarantees: surface that rearrange preserves *sharing* (S5/transclusion).** The note states "bijectivity of π preserves any pre-existing sharing pattern" — multiple V-positions onto one I-address survive the relabeling. For a Xanadu system this is load-bearing intuition; "no I-address created/dropped/duplicated" implies it but doesn't say it. Add a line: shared/transcluded content is carried through *by construction* (the permutation acts on the domain, not the multiset of references).

**3. [SHARPENING] — "What must be built": the validator checklist omits R-PRE(i) and (ii).** It enumerates count/order/subspace/depth/ordinals/coverage but not "M(d)/document exists" (i) or "V_S(d) ≠ ∅" (ii). (ii) is in fact subsumed by coverage given CS5, and (i) is implicit, so this is non-load-bearing — but say "enforcing R-PRE *in full*" and name (i)/(ii) so the checklist isn't read as exhaustive.

**4. [SHARPENING] — "How it fits": hedge "explains why Green could leave its index update commented out."** The ran-invariance reasoning is sound and the evidence confirms the update *is* commented out — but the evidence does not establish that *this* is why Green did it. Phrase as "makes it *safe* to leave un-updated" / "is consistent with," not a claim about Green's intent.

**5. [SHARPENING] — Durability: "≤ 5 numbers" undercounts.** A document is identified by an ISA tumbler (multi-component, e.g. `1.1.0.1`), not one number; the intent record is "doc ISA + 3–4 cut ordinals." Say that instead of "≤ 5 numbers."

Genuinely solid and worth keeping as-is: the absolute-vs-implicit-position analysis with the **μ-term sign-flip as the single most error-prone term** (correctly mapped to the note's three separate worked swaps); the recommendation to treat π/displacements as the *verification oracle* not the algorithm; the per-subspace-storage = subspace-confinement-by-construction = implicit-position-axis collapse into one decision; the membership-reverse-index invariance insight; and the merge-bound `|Δ| ≤ n` derivation — correctly flagged as *beyond* the note (the note lists run-count bounds as open), with a no-cascade argument that holds. The digest is also *more* precise than the note's own intro, which loosely calls the swap's middle "fixed" where the digest correctly says it shifts by `w_β − w_α`.

VERDICT: REVISE
