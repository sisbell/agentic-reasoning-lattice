The digest is unusually strong: every "forced" commitment maps to an actual theorem/invariant; the one "merely conventional" call (subspace-by-leading-digit) is correctly unforced; all Green-specific claims (2-D I×V enfilade, per-dimension cwid/cdsp, immutability-held coupling, tumbler-prefix origin, the two-blade knife, link-creation straddling two trees, tumblerincrement prefix-extension) check out against the evidence answers; and the subtle traps are handled correctly — notably that M12's order-independence governs *merge* order not *journal/edit* order, that the live tree is a Lampson "hint" only contingent on a persistence choice, and that the cross-origin guarantees rest on an *input contract* (prefix allocation) that "does not propagate for free." The scoping (store + split + merge + canonicalize + resolve; edits are the consumer layer) is right. I found no material defects — only the following tightenings.

**Revision list**

1. **`[SHARPENING]`** — *"What must be built" (Resolve):* the parenthetical "by C0 necessarily an ordinal displacement" is attached to "a level-uniform V-span," but C0's ordinality conclusion rests on **well-formedness** (the depth-`m` covered domain being finite, S8-fin) — a level-uniform but non-well-formed `σ` could carry a sub-`m` action point. Say "well-formed level-uniform V-span" here so the builder sees the (start, scalar-count) shape is a *consequence of* the well-formedness precondition that the Resolution approach already tells them to validate, not a property of level-uniformity alone.

2. **`[SHARPENING]`** — *Implementation approaches > Merge / canonicalize:* the justification "only consecutive blocks can ever merge (no-overlap, plus canonical = the maximal runs)" carries a dead clause. "Canonical = the maximal runs" plays no role in *why* only consecutive blocks merge; the reason is solely that merge requires V-adjacency `v₂ = v₁ + n₁` (M7), which no-overlap (M7-cov) makes satisfiable only between V-consecutive blocks. Drop the "canonical = maximal runs" half of the parenthetical.

3. **`[SHARPENING]`** — *Implementation approaches > The store (enfilade):* "shift a whole downstream region by adjusting **one ancestor's** displacement" is in slight tension with the "O(log n)" in the same sentence — if it were literally one node it would be O(1) after locate. A downstream V-region generally spans the O(log n) nodes along the cut path, so it's the displacements along that path (still O(log n)). The complexity figure is right; reword "one ancestor" to "the nodes along the cut path."

4. **`[SHARPENING]`** — *Guarantees > Functionality (S2):* "one I-address per V-position … holds as long as coverage stays a disjoint partition (B1 + B2)" overstates the dependency — the no-double-mapping property follows from disjointness (B2) plus each block's own functionality; coverage (B1) governs *completeness* of the representation, not functionality. The "disjoint partition" gloss is fine as the store's standing invariant, but if the intent is the *minimal* condition for functionality, name B2.

VERDICT: CONVERGED
