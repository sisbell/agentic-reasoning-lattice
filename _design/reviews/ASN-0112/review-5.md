This digest is strong: the forced/conventional split is accurate, every Green claim (depth-2 placement, the "1.1 for 1.2" bounding box, empty-zeros sentinel, negative intermediate displacements, `findnextlinkvsa`/`findvsatoappend`/`retrievedocumentpartofvspanpm`, open-list gating) is grounded in the Q1–Q20 evidence, the V-claim coverage is complete, and the Lampson-style approach ranking (read-the-ends default; enfilade only if other ops earn it; hint+recompute for recovery) is sound. The negative-origin tradeoff (approach 2 can surface a negative root displacement that direct min-reading cannot, OQ5) is a genuinely sharp, correctly-grounded observation, and the depth-policy collapse (equidepth ⇒ always-tight, always-level-uniform) is right. I found no material problem. The items below are refinements only.

**Revision list**

1. **[SHARPENING] Design commitments (reach construction): "the deeper zero-extension `w.0` is a strictly smaller *valid reach*"** — the note is careful to call `w.0` a "strictly smaller *upper bound*" and to reserve *attainable reach* for the open question, precisely because whether any well-formed span has `reach(σ) = w.0` is OQ6. "Valid reach" risks reading as "attainable," which the very next clause says is open. Tighten to "strictly smaller upper bound (it covers `O(d)` denotationally; whether a well-formed span can *attain* it as its reach is OQ6)," matching V3's own wording.

2. **[SHARPENING] Design commitments (composition sensitivity): the "Only clearing content…, adding first content…, or moving the link max, moves the span" list folds a regime-*transition* into a bullet headed "composition insensitivity in the cross-subspace regime."** Within *strict* cross-subspace only a link-max move shifts the span (content edits keeping `n'_{s_C}≥1` move neither extreme); "clearing content" and "first content into a link-only doc" are the two occupancy-toggle transitions at the regime's *boundary* (V18). Say so, so a builder doesn't read "first-content" as something that happens while already cross-subspace.

3. **[SHARPENING] How it fits (version creation): "a concrete instance of the redirect-fragmentation-sensitive-callers thesis" is loosely framed.** Version creation isn't fragmentation-sensitive per se; like content-append it needs the *content-only* extent (to avoid copying the link-space bridge), which Green reads via a separate content-only primitive (`retrievedocumentpartofvspanpm`, Q14). Reframe as the same "needs content-only extent, not the bounding box" family as the content-append redirect — the cleaner and more accurate characterization.

4. **[SHARPENING] Discriminator exposure / Decisions: surface the note's *predictive* (weakest-precondition) lever, not only the after-the-fact discriminators.** The note establishes that a caller can decide *before* querying whether the answer will be exact (`wp = ` single-subspace occupancy) and tight (`wp = O(d)=∅ ∨ #origin ≤ #reach`), in addition to reading exact/tight off the returned width afterward (V9a/V9b). The digest captures only the read-off-the-width side. Worth one line, since it gives callers a planning lever the note explicitly supports.

VERDICT: CONVERGED
