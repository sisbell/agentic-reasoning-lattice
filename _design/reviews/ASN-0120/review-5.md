I reviewed the digest against ASN-0120, its claim statements, and the verified Green evidence. This is a strong, disciplined digest: the forced/conventional split is accurate (identity-not-position, snapshot-at-creation, fresh-never-reused-home-scoped, home/endset orthogonality, three-ordered-endsets-with-mandatory-type, coverage-pinned/decomposition-free, link immutability are all correctly marked forced; `m=2`, decomposition choice, from/to semantics, and which slot is empty are correctly conventional). The central architectural calls are sound and Lampson-shaped — the index/counter as recomputable *hints*, WAL for the composite, "discovery is a view, not a seventh component," and the explicit refusal to write a back-reference into the target. Every Green source-level claim I checked is grounded in the evidence answers (clipping zero/negative-width failure Q1, `docISA.0.2.N` per-home Q11, shift-is-no-op Q17, home-nulled spanfilade Q14/Q20, ISPANID direct path Q2, silent empty-slot storage Q3/Q2). The point-membership reduction in the Discovery section correctly reproduces the note's Fact (a) (arrangement ranges hold only store addresses, so matches land on resolved roots), and the digest does **not** propose value-based dedup — it respects no-coalescing throughout. The clipping caution is the digest's best contribution: it correctly identifies that the note's ρ is enumerate-and-image, that this is safe on the in-scope depth-mismatched class, and that Green's clip path is not.

I found no material problems — only sharpenings.

**Revision list (most valuable first):**

1. **[SHARPENING]** *Implementation approaches → Endset packaging.* The parenthetical "(only the enfilade does the run-grouping for free, one region per contiguous I-run)" sits immediately after the recommendation to "group by maximal *I-chain-adjacent* resolved runs," so it reads as if the enfilade yields the *canonical maximal* merge gratis. It does not: Green emits one region per **crum** — insertion-history-dependent and explicitly non-canonical, as the digest itself states one sentence later (Q2-second). Achieving the maximal merge requires an adjacency test from *either* representation; the enfilade only spares you assembling runs from individual positions. Tighten so the two adjacent parentheticals don't pull against each other (e.g., "the enfilade emits run-structured output for free, but crum-level and non-maximal; maximal merging costs the sibling-step test in either case — and since decomposition is free, this is a compactness, not correctness, concern").

2. **[SHARPENING]** *Guarantees / Discovery.* Make the **future-state half of ML9** explicit — it is currently only implicit in the timeless "findable from any region any endset references." The note guarantees that the link's discoverable-through set is *frozen* to the originally-resolved I-addresses (ML1 tightness → LP19a: no later-allocated address ever enters coverage), so discoverability *spreads* to any document that later comes to share that content (transclusion, Q20) yet **never** through content created after the link. This is the design payoff, and it is also the *reason* the packager must trace `F`, not the store: the note shows the rejected store-trace would let an unallocated-frontier address leak into discoverability once later allocated and arranged. The digest gives the packaging rule (no frontier merge) but frames its rationale only as "satisfy the recovery equation" — naming the leak it prevents makes the rule self-justifying.

3. **[SHARPENING]** *What must be built → validation gate.* The component enumerates the four `wf` conditions and the type precondition but omits the first conjunct of `enabled`, `d ∈ dom(Σ.M)` (home document allocated). It is recovered later via "check the full `enabled` predicate," and the home seater presupposes it — but listing it in the gate would make the component self-complete and reinforce that it, too, must be checked *up front* (before allocating), since otherwise the rare rejection burns an address.

Solid and needing nothing: the contracted-home selector divergence (address-branch vs seat-branch, agreeing at a link-virgin home and splitting under a retention-0 contraction) is captured exactly and matches both note and inferred Green behavior; the validation-ordering / common-case-fast argument is right; the hint-vs-registry allocation discussion and the WAL-single-record atomicity are the correct minimal choices.

VERDICT: CONVERGED
