The digest is unusually strong: the forced/conventional tags check out, Green inferences are carefully separated from note-grounded claims, and it carries real load-bearing synthesis (the sequence-makes-invariants-free pattern; version-history-is-arrangement-side-not-content-side; the Option-1-forces-fused-INSERT coupling; the non-injective-M(d) disambiguation hole). One real defect; the rest are sharpenings.

**Revision list:**

1. **Implementation approaches → "The arrangement map," re-key ordering: the descending-key rule is wrong for the backward (contraction) shift. [DEFECT]** The digest correctly derives "re-key in descending key order" for the *forward* shift (link INSERT — a target like `[2,2]` can still be a live source) and then extends it: "the same descending-key discipline binds Option 3's whole-tail re-key." But Option 3 also performs the *backward* shift (text DELETE/contraction, σ: `[1,k]→[1,k−c]`), and there descending **clobbers**: with `R={[1,4],[1,5],[1,6]}`, `c=2`, processing `k=6` first writes `σ([1,6])=[1,4]` over the still-unread source `[1,4]`, so `σ([1,4])=[1,2]` later reads a corrupted value. Backward shifts must re-key in *ascending* order. This is not cosmetic: the digest recommends Option 3 as the differential-test **oracle**, and an oracle whose DELETE re-keys descending is silently wrong — it defeats the oracle's purpose. Fix: state the rule as direction-dependent — re-key *opposite* to the shift direction (descending for INSERT's forward shift, ascending for contraction's backward shift) — or point to the staging-into-a-fresh-map technique (already offered parenthetically, and direction-agnostic via TS2 injectivity). Add that the sparse *link* map only ever shifts forward (contraction is text-only), so descending is correct there; the error is solely its extension to Option 3's text DELETE.

2. **"What must be built" / "Shift-contraction at depth 2": "the full machinery is exercised on the deep-version-chain I-addresses" over-extends. [SHARPENING]** The "full machinery" was defined to include ord/vpos (ordinal extraction/reconstruction), which are V-position constructs — they strip/prepend the *subspace identifier*, a notion the I-address (account.document.version) does not carry. What the deep I-side actually exercises is tumbler ⊕/⊖ and lexicographic compare (allocation, ordering), not the ord/vpos shift machinery. The actionable point survives (deep tumbler arithmetic lives on the I side; the V side is integer arithmetic at depth-2; the V-specific machinery returns only at deep V-positions) — just don't imply ord/vpos run on I-addresses. It is already correctly hedged as an inference about the allocation layer.

3. **Implementation approaches → Option 1 tradeoff: the "append-mostly" grounding is thinner than stated. [SHARPENING]** "largely moot if link creation is append-mostly — mildly grounded in Green allocating fresh link endpoints at creation" — the evidence (`setlinkvsas` building 1.1/2.1/3.1) establishes link-endpoint *depth*, not an append-mostly *allocation pattern*. The clause is already heavily hedged ("an assumption to validate ... not something the note establishes"), so drop the "mildly grounded in Green" half and present it purely as a workload assumption to validate.

Genuinely solid, no change needed: the design-commitments block (the I3-C/D-I equal-strength reading is correct; the text-vs-all-non-text dense/sparse asymmetry and the S=3 inclusion are properly grounded; the depth-2-is-contraction-only-but-floor-≥2-is-forced separation is exactly right); the external-references/staleness section (I-anchor + spanfilade passive validity + within-doc reverse-of-M(d) + non-injective disambiguation); atomicity (op-log plus the INSERT-fill-needs-content-record nuance and snapshots-as-retained-roots); and "How it fits."

VERDICT: REVISE
