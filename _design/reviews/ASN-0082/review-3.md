This is a strong digest — accurate on the load-bearing commitments, disciplined about flagging inferences (version-history-is-arrangement-side, I-side arithmetic), and free of fabricated source-level claims. Three sections are genuinely excellent and need no change: the S0-vs-I3-C/D-I strength analysis (commitment 1), the "version history is an arrangement-side choice, not a content-side freebie" insight (commitment 6 + recovery), and the text-only-is-intrinsic vs depth-2-is-a-genuine-proof-boundary decomposition (commitment 8), which correctly grounds the depth boundary in the note's Open Questions 2–3. The Lampson-style representation analysis (sequence makes the invariant lemmas free; keep Option 3 as a differential oracle) is the right shape.

The improvements below, most-important first.

**1. Implementation approaches → "The arrangement map," Option 1 (link half): the overlap ordering hazard is missing from the *recommended* build.** The digest treats only the tombstone-landing case for links ("a shifted image landing in a former tombstone is just an overwrite of an empty key"). But the note's own link example ([2,1]→[2,2] while [2,2]→ℓ₂ is *live*, then [2,2]→[2,3]) shows a shifted image landing on a **still-occupied** source key. A naive in-place re-key of the sparse map clobbers ℓ₂ before relocating it. This is a correctness gap in the recommended representation, not a fallback concern. Fix: state that sparse-map re-keying must proceed in **descending key order** (or build a fresh map / stage via the shift's injectivity TS2), and that this occupied-landing case — not just the empty-key case — is what the note's I3-V/overlap apparatus encodes; note the same discipline binds Option 3.

**2. Implementation approaches → "Shift/contraction at depth 2": "the proof and Green both stop at depth 2" mis-states insertion's reach.** Per the digest's own commitment 8, insertion is proven for *any* depth ≥ 2; only **contraction's** proof (and Green) stop at depth 2. As written this contradicts commitment 8. Fix: attribute the depth-2 ceiling to contraction + Green specifically, and note insertion's proof is general but there is no point inserting deeper than you can delete (or than Green supports) — so depth-2 remains the sound build target without misdescribing the insertion proof.

**3. Design commitments #7 + Decisions → "Span maintenance": the third deletion case is missing.** Both places present a two-case world — rigid-within-region vs straddle. For DELETE there is a third case the note implies: a span/selection lying **wholly inside the deleted range X** is not shifted and not merely shrunk — it is de-referenced (dangling/empty), since D-DOM discards X's mappings. An upper selection/link layer must handle it. Fix: add the wholly-within-deletion case (destroyed/dangling) alongside rigid-shift (in R) and straddle (shrinks).

**4. Decisions → "Insertion shape" (coupled to the arrangement-map choice): Option 1 forecloses the spec's two-phase open-then-fill, and the digest treats these decisions as independent.** A dense sequence cannot hold the note's I3 intermediate (an *unfilled* gap [p, shift(p,n))) without either fusing open+fill into one splice or inserting placeholders that destroy the density-by-construction win. The note models "open gap" (I3) as a first-class sub-operation; a builder who wants to mirror that (reserve-then-fill across a transaction/async boundary) cannot on a rope. Fix: note that choosing Option 1 *pre-empts* the insertion-shape decision (forces the fused one-step INSERT), whereas the map/tree (Options 2/3) can persist the reserved-but-unfilled gap.

**5. Implementation approaches → "Pick" + "External references": surface within-doc reverse lookup as an Option-2 trigger, and justify the enfilade claim.** (a) The digest elevates I→V reverse lookup to a must-build (Open Question 1) and notes the rope needs a bolted-on reverse index while the enfilade serves it natively — yet the Option-2 selection criteria ("link edits hot, lazy V-materialization, deep ordinals") omit it. Add "or if within-document I→V reverse lookup is a hot path." (b) "a displacement enfilade can serve it directly" asserts a non-obvious capability — a V-keyed enfilade answering an I-keyed query relies on the enfilade's 2D (V-axis and I-axis) crum structure. State that justification or soften the claim.

**6. Implementation approaches → Option 1 tradeoff: "links are append-mostly, as it usually is" is uncited.** Neither the note nor the evidence establishes link *insertion* patterns, yet the whole O(k)-re-keying tradeoff is dismissed on this. There is mild grounding (Green allocates fresh link endpoints at creation), so either cite that or mark it explicitly as an assumption the builder must validate against their link-edit workload — don't let a load-bearing cost dismissal rest on an unstated "usually."

**7. Design commitments #5: generalize the contiguity exemption beyond links.** It names only "links (S=2)," but the note's "Case S ≠ 1" exempts *every* non-text subspace, and the evidence confirms Green has a third (link-type endpoints, S=3). One clause: the dense/sparse split is text (S=1) vs all other subspaces, not text vs links — so the subspace-partitioning component must tolerate ≥3 sparse-permitted subspaces.
