I worked through the digest against the note, the claim file, and the evidence. This is a strong digest: accurate on the load-bearing commitments, clean on grounding, and consistently at design altitude. Let me record where I checked hardest and what (little) I'd still apply.

**Sections that are genuinely solid (verified, not just plausible):**
- *Design commitments.* The S0 / I3-C / D-I strength analysis is correct — I3-C (`dom(C')=dom(C)` + values) is functionally `C'=C`, equal to D-I's `Σ'.C=Σ.C`, and both add only the reverse inclusion over S0. The subspace-asymmetry commitment correctly reads the note's "Case S ≠ 1" as *all* non-text subspaces (not just links), and the depth-2-vs-general split between contraction and insertion is read off the preconditions correctly (`#p=2` vs `#p≥2`).
- *Arrangement-map options.* The descending/ascending re-key clobber reasoning is correct on both concrete examples (forward link shift `[2,1]→[2,2]` over live `ℓ₂`; backward DELETE `R={[1,4],[1,5],[1,6]}, c=2` clobbering under descending), and correctly ties this to the note's I3-V/overlap apparatus. The "rope cannot hold the unfilled gap `[p, shift(p,n))` without placeholders → Option 1 forces the fused one-step INSERT" observation is sharp and correct.
- *Grounding.* Every Green claim traces to the evidence (S=3 link-type endpoints, NPLACES 11→16 for version chains, Gregory's "suspicious of this shift," depth-2 implementation limit, POOM 2D crums via `findcbcinarea2d`) or to allowed Green types (permascroll/spanfilade/POOM). No fabricated function-level claims. The append-only/origin-not-value framing correctly *avoids* the forbidden content-dedup.
- *Reference-anchoring.* The I-address-anchoring recommendation plus the non-injective-M (transclusion) disambiguation hole is a real, sound builder caveat the note doesn't spell out.

**Revision list (all non-blocking):**

1. **[SHARPENING]** *Implementation approaches → "Shift/contraction at depth 2" and the Depth builder-decision.* The "put the V-side arithmetic behind a narrow seam so depth > 2 can slot in later" advice is clean for Options 2/3 but glosses that the *recommended* Option 1 cannot extend behind a thin seam: its whole win is V-positions being implicit sequence indices, and a single-integer index can only carry a depth-2 (single-component) ordinal. So for Option 1 the depth->2 "seam" is a representation swap to the Option 2 enfilade, not a swap of arithmetic behind a function boundary. State this so a builder doesn't pick Option 1 expecting cheap deep-ordinal extension. (Same spot: note that insertion's span corollary I3-S is proven at *general* depth, so only contraction's width-preservation is genuinely depth-2-bound — the "automatic at depth 2" framing slightly undersells the insertion side.)

2. **[SHARPENING]** *Guarantees to uphold.* The by-construction list omits the ASN-0036 allocation invariants (S7a/S7b/S7d), which the note explicitly proves preserved (I3-S7, S7-post). They hold for free precisely because the shift touches neither the content store nor the document set — the same content-invariance the digest already leans on. Add a one-line "allocation discipline — free; the shift allocates/deallocates nothing" entry, which also makes the operation's allocation-neutrality explicit for whoever integrates the content-allocation half.

3. **[SHARPENING]** *Guarantees to uphold → Width preservation.* "actionPoint(ℓ)=#ℓ — automatic at depth 2" is true only because the span is *within-region* (both endpoints share the subspace prefix, forcing the width's first component to zero); a depth-2 span whose width acts at component 1 is not ordinal-level. The decisions section already flags the "structurally wide spans" exception, so tighten this parenthetical to "automatic for a within-region span at depth 2" to remove any standalone-false reading.

No accuracy, soundness, grounding, completeness, or altitude defects found — the three items above are tightenings, not corrections, and none changes a build instruction.

VERDICT: CONVERGED
