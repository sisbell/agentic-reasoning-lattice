I worked through the digest against the note, the formal claims, and the evidence answers. It is, on the whole, a strong digest: the design-commitments section reads the note accurately, the "forced vs. conventional" calls are defensible, the Green groundings (`insertseq`/`findpreviousisagr`/`next` contiguity, ownership-blind allocation, no longest-match arbitration) are all backed by the evidence answers, and the **Guarantees** section's treatment of `owns` vs. `ω` ("only ω() arbitrates… Conflating them reproduces the bug") is exactly right and is the load-bearing insight of the whole note. The recompute-the-delegation-forest-from-prefixes move (parent = most-specific cover, stable under conditions (ii)/(iv)) is sound and well-grounded in NestingByDelegation, and the atomicity/journal/recovery reasoning is faithful Lampson.

But there is one material internal contradiction.

**Revision list (most important first):**

1. **[DEFECT] — *Decisions for the builder*, "Session↔principal binding": "One principal per session (Green)… makes the resolver mostly unnecessary (you check `owns` against your own prefix)" is wrong and contradicts your own Guarantees section.** The note's *final open question* and the Exclusivity section both insist `ω` (longest-match) is an obligation a conforming implementation must discharge *regardless* of Green's single-account shortcut, and your Guarantees section says so verbatim ("the one thing you must actively build… using containment where the contract requires ω makes the node operator appear to own delegated subdomains — the exact divergence"). One-principal-per-session does **not** remove the need for `ω`: a node operator session with `pfx = [1]` checking `owns([1], [1,0,2,0,3])` gets `TRUE` by containment, but `ω` of that address is the delegate `π_A` (worked example, state Σ₁). Authorizing by containment there reproduces precisely the bug. The contrast "multi-principal sessions require real longest-match" is the error — it ties `ω` to the *session-binding* choice when in fact `ω` is required in any model with multi-tier principals (node-over-account), which is the note's model. **Fix:** reframe the bullet so the real fork is "scan vs. trie vs. cache *for ω*, and whether sessions may name only their own prefix (which collapses **O5**)" — not "ω vs. no-ω." State that one-principal-per-session legitimately collapses O5 and lets a principal authorize writes *within its own undelegated subtree* by containment, but that `ω` is still required for any principal sitting above delegated accounts; containment≈ω only in a flat, account-only hierarchy (Green's actual reality, not this note's model).

2. **[SHARPENING] — *Design commitments*, first bullet consequence: "Authorization-by-containment can be checked anywhere — at the edge… without a round trip to authoritative state" mis-labels the stateless check.** What is stateless is `owns()` (a necessary edge filter), not authorization — authorization is `ω(a)=π`, which *does* consult the registry. Your "How it fits" section already draws this correctly ("owns() … pushed to the edge … ω() centralized resolver"). Align the consequence with it: say the *containment check* is the cheap edge-pushable part, while effective-ownership authority requires the central `ω`. (Same conceptual slip as #1; fix in one pass.)

3. **[SHARPENING] — *Implementation approaches*, effective-owner cache: "so a miss is cheap and safe to recheck" describes the wrong failure mode.** The cache hazard is a stale *hit* (you served an ancestor owner), not a miss. The staleness-direction analysis is correct and valuable (monotonic refinement ⇒ a stale entry is a prefix-ancestor of the true owner, never more specific — holds only while transfer is absent, which you note). Restate as: *a stale hit can only under-resolve to an ancestor, so revalidating against the registry is cheap and the cache never over-claims.*

4. **[SHARPENING] — *Implementation approaches*: the repo-specific precedents (`links.jsonl`→`paths.json` "rebuilt by replay," and the "Rust/`im` target") are asserted but not grounded in the note, evidence, or documented Green structure.** They're plausibly real repo context, and the recommendation ("Pick A — tag the journal") stands independently on its Green grounding (account creation inserts the prefix via `insertseq` in the same operation) and on O17b/O18/PrefixBaptismCoupling. Either confirm these are this repo's actual substrate or soften them to "a journal→derived-index pattern" so the recommendation doesn't rest on an unverifiable precedent.

5. **[SHARPENING] — *What must be built*, "Bootstrap initialization (O14)": the enumeration drops O14.8 (each bootstrap prefix is itself baptized / in Σ₀.B) and O14.3 (finite).** O14.8 is load-bearing for your recommended Option A: if a seeded principal's prefix is not in the journal, replay can't recover it and PrefixBaptismCoupling is violated. You state PrefixBaptismCoupling in the commitments, so just add "and each seed prefix is itself baptized" to the bootstrap bullet to close the loop.

VERDICT: REVISE
