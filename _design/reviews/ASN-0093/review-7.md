## Assessment

The core is accurate and unusually well-grounded. The forced/conventional split is correct on every line I checked (the `s_C=1/s_L=2` "convention but downstream-immutable" framing is right, and the alternative `s_C=2` chain that justifies it actually verifies); the "What must be built," "Guarantees," and "How it fits" sections map cleanly onto the note's invariants, lemmas, and deferred-scope list; every Green source-level claim traces to the note or the evidence answers (Q6/Q8), with no fabrication and no altitude slips into code. The nested-document catch (K.σ must *not* add a prefix-incomparability check) and the "don't re-fuse Green's CREATELINK" warning are genuine, note-grounded builder value. The problems I found are all refinements, not corrections.

## Revision list

1. **`[SHARPENING]` Implementation approaches → Indexing / Address allocation: make the containment check explicit and cite Bug 0013.** The "predecessor-below-bound query" realization is correct *only* when also bounded below by the anchor prefix `b_C(d)`/`b_L(d)`. Without verifying the predecessor lands under the document's subspace prefix, a first-emission allocation under a *fresh* document (no same-origin entry) returns the global predecessor of `b_L(d)` — a foreign document's address. This is exactly Green's Bug 0013 (containment guard via `tumblertruncate`+`tumblereq`; evidence Q8). The digest's spec ("max same-origin … *or emit the first form*") is right, so this isn't an error — but the ordered-map realization needs the guard stated to honor it, and the Green bug is evidence it's a real trap, not a hypothetical. Highest builder value.

2. **`[SHARPENING]` Implementation approaches → Indexing: disambiguate "exactly the granfilade/spanfilade shape."** As written it reads as a structural claim, which the very next subsection appears to contradict ("granfilade/spanfilade are *enfilades* — cumulative-index trees that compress … over spans," i.e., strictly more than an ordered map). In context "shape" plainly means the *query profile* just described (predecessor + prefix-range), which the granfilade does expose — so it's true, but tighten to "the query shape the granfilade/spanfilade expose (ordered predecessor + prefix-range)" to remove the apparent tension with the enfilade paragraph.

3. **`[SHARPENING]` Guarantees → Permanence: don't tie the permanence *invariant* to the persistence layer.** "by construction if the persistence layer is append-only" slightly contradicts the digest's own (correct) earlier point that C0/L12/M1 constrain the *transition relation* and a purely in-memory store already satisfies them. Recast as "by construction if the store exposes no overwrite/delete path and the allocator never reuses addresses" — the API shape, not durability, is the enforcement; the following sentence ("expose no overwrite/delete API; the only writer appends") already says this.

4. **`[SHARPENING]` How it fits → provenance: don't over-specify a deferred component.** "a new R component recording who allocated what" pins semantics the note leaves undefined; documented Xanadu provenance is content-origin/transclusion lineage, not allocator identity. Drop the gloss or align it — the note only says "the provenance relation R," so the digest shouldn't confidently characterize it.

5. **`[SHARPENING]` Design commitments → subspace digits: compress the forced-vs-conventional derivation.** The alternative `s_C=2` chain `(d,[d.0.1],[d.0.2],[d.0.2.1])` is correct, but it's reviewer-facing detail in a builder digest. Compress to the conclusion: the values are Green-grounded axiomatic convention, downstream-immutable; the inc-algebra forces only their *distinctness* (`s_C≠s_L`), the load-bearing fact for SD via T7. Footnote or drop the chain.

VERDICT: CONVERGED
