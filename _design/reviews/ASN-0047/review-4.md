## Review

This is a strong, unusually careful digest. It reads the note accurately, keeps the right altitude, and grounds every Green-level claim in the evidence rather than fabricating source behavior. The hardest technical commitments are handled exactly right: the fork **φ-bijection / multiplicity-preservation** warning (copy the V→I *map*, never the I-address *set*) is load-bearing and correctly identified as the origin-identity commitment biting at the representation level; the **R-is-not-a-hint** Lampson point (R is the only surviving record once K.μ⁻ removes content from M) is sharp and correct; the **commit-marker recovery contract** is the right fix for Green's crash gap; and the treatment of the *contested* DELETEVSPAN evidence (gap via `strongsub` exponent guard vs. compact-and-renumber via case-2 `tumblersub`) is handled honestly rather than papered over. The "forced vs. conventional" tags, the active-vs-by-construction guarantee split, and the orphan-link / J-LV asymmetry are all faithful to the note.

I found no material problem. The items below are genuine but non-load-bearing.

### Revision list

1. **`[SHARPENING]` Decisions → Concurrency granularity (and the composite-atomicity tradeoff): the prefix-disjointness mechanism is mis-attributed.** The digest says per-home-document writers buy concurrency "at the cost of coordinating the node-baptism boundary to hand out disjoint prefixes." But distinct documents already receive disjoint sub-allocator ranges by the note's own cross-document disjointness lemmas (CrossDocDisjoint / CrossDocEntityDisjoint) — and crucially those hold *even under nested node addresses, which NodeBaptism explicitly permits* (SSGU's zero-separator divergence). So per-document writers within a single node are non-colliding by the allocator hierarchy itself, with no node-baptism coordination required. Node-baptism coordination is the *separate* multi-node federation concern (the note's open question on cross-node provisioning). Split these two so a builder doesn't needlessly couple intra-node concurrency to the baptism boundary; the only genuinely unresolved case is the note's flagged one — concurrent allocation *within a single home document*.

2. **`[SHARPENING]` Guarantees to uphold: name P6 and P7 explicitly.** The active referential checks are listed (S3★, P8, CL-OWN, CL-UNIQ, L3) but two referential invariants are only implicit: **P6** (`origin(a) ∈ E_doc` — content cannot be allocated under a non-existent document, the active check being K.α's origin precondition) and **P7** (every `(a,d) ∈ R` has `a ∈ dom(C)`, the active check being K.ρ's content precondition). They fall out of the "allocator mints under a parent prefix" and coupling discipline the digest already describes, so the digest is sound without them — but naming them completes the referential-invariant set a builder is enforcing.

3. **`[SHARPENING]` Implementation approaches → Arrangements: tighten the historical-reconstructability framing.** The claim that the note routes Nelson's "every previous arrangement remains reconstructable" through R + versioning (not retained M-states), and that R "does not retain ordering," is correct and a useful builder clarification. Worth one added clause: P4a (trace witnessing) is a *proof obligation over the transition history*, not a mandate to store past states — which is precisely why per-document snapshots are an optional enhancement rather than a requirement. This reinforces the point you're already making.

The remaining sections — design commitments, what-must-be-built, store/registry/allocator approaches, the depth-`m` semantic-inertness argument (well-reasoned: D-SEQ★ contiguity makes the inner 1's fixed and unsubdividable, so `m=2` is safe), and the "leans on / hands to" mapping — are accurate and need no change.

VERDICT: CONVERGED
