Reviewed against ASN-0134, its claim statements, and the verified Green evidence. This is a strong digest — accurate on the genuinely hard content and well-grounded. I found one material gap and three worthwhile tightenings.

Where it is solid (do not "fix" these): the G1 treatment correctly keeps **step-level confluence** distinct from **operation-level order-dependence**, and carries the note's sharpest correction faithfully — instance (ii) (a lone `idem=⊤` emit racing a `Nullify` of its active incumbent) reaches divergent committed states and is reduced by *neither* clause 8 nor emit-before-retract; the **W5 trichotomy** is read precisely (the disjunction means "absent **and** not the retractor's own `a_emit` → reject," with the self-nullify case firing, not the lazy "absent → reject"); the **A6 contiguity boundary** is exactly right (ChainMembershipForOrigin/L-ContiguousPrefix are serialization-borne W3, holding only on a valid `𝔼`, while the rest of A6 holds from A0); the **Green grounding** is faithful (Q1 frontier descent, Q19 contiguity-as-artifact, Q15 response-before-check); and recognizing that the note's "global critical section" for clause 7 is *mechanism, not requirement* — so MVCC root-capture satisfies V2's "one committed index" and answers Open Question 2 — is a real, sound insight, not an overreach.

# Revision list

1. **[DEFECT] Read surface / Guarantees / the clause-4-vs-clause-7 split: the digest spotlights `age` as a clause-4-free single bounded access but never states that `stale` (an age-thresholded enumeration) is a *multi*-bounded-access read that belongs to clause 7 — even for a single member home.** This is the note's central forcing example for the whole "access count, not type-confinement" discriminator: `stale` realizes as one *global* active-view read (it consults `L_R`, whose nullifiers may be homed anywhere) **plus one per-home frontier descent per member home** — `N+1 ≥ 2` accesses that can drift across indices — so MIC clause 4 *explicitly excludes* it and clause 7 *explicitly includes* it. Because the digest tells the builder "age is one bounded access, free" and is silent on `stale` (which is literally `{a ∈ A_K : age(a) > h}`), it actively invites the wrong generalization: implement `stale` as one `Observe_K` + per-element `age`, all "clause-4 free," yielding an **unsound** verdict (the frontier reads can move relative to the active-view read). This bites on the "per-type reads only" API path the digest itself offers as a choice. Fix: add `stale` (and any age-thresholded / frontier-touching enumeration) to the clause-7 read surface, explicitly contrasting it with the lone clause-4-free `age`; note its active-view read is global and its frontier constituents are per-member-home; and note that a *different-type* emit into a member home is `Q`-affecting for a `stale` verdict. State that root-capture and single-thread both dissolve this, but the per-type-reads-only path must clause-7-pin it.

2. **[SHARPENING] Approach 1 drops below design altitude into unverified repo/library specifics.** "exactly this repo's `links.jsonl` mechanism," "the way `paths.json` is," and "the `im` crate's whole purpose" are code-level artifacts asserted as grounding; `im` is a Rust crate while the codebase is Python (so the concrete RCU/atomic-pointer-swap framing won't transfer cleanly, and the GIL changes the concurrency story). The *design* — append-only step journal as authority, registration recovered by replay, persistent/immutable state + atomic publish — is sound and language-agnostic; restate it at that altitude and verify or drop the specific file/crate names (and note the single-thread route as the natural fit if the target is Python). Does not block: no builder gets the design wrong, only the decoration.

3. **[SHARPENING] Approach 5: the Open Question 5 tie-in conflates two different windows.** "the exclusion span cannot drop below the run's duration; shrinking that would require batch read-atomicity" reads as if reader-atomicity could shrink the *writer-side* exclusion — it cannot. The writer-exclusion window for contiguity is irreducible (any foreign same-`(d,s_C)` write fragments the run, full stop); separately, even a W4-contiguous run is **not** atomic *to a reader* (a reader can still see a strict prefix, A5), and closing *that* reader-side gap is OQ5 — a strictly stronger, orthogonal property, not a lever on the writer window. Split the two cleanly. The load-bearing guidance (hold the lock for the run; pre-reservation only saves redundant frontier reads) is correct.

4. **[SHARPENING] "clause 7 … costs nothing" / "the single biggest Lampson win" slightly overclaims.** Root-capture eliminates *coordination/blocking* for multi-read verdicts, but not cost — it carries the persistent-structure and version-retention overhead the digest itself credits to Green's mutable route. Qualify "costs nothing" as "costs no coordination."

# Final verdict

VERDICT: REVISE
