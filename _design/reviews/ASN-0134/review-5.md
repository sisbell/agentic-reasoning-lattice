Reviewed the digest against the note, its formal claims, and the evidence answers. The digest is unusually strong: its accuracy is high, its Green claims are all grounded in the evidence channel (apex width aggregate Q1, counter-vs-coupled allocator Q19, response-before-check Q15/Q17, spanfilade two-write crash divergence Q17, visibility-at-dispatch Q13, single-loop stall Q16), and it stays at design altitude throughout. Two observations are genuine value-adds beyond the note: the **single-loop fault line** (which clauses become non-free only when you leave run-to-completion) and the sharp point that **clause 6 drifts even under the single loop** unless the recognizer bundles its constituent reads into one dispatch — the §8 pathology needs no per-home concurrency. The G0 serializability/¬SC/real-time-linearizability compression, the clause-7 "no-duplicate ≠ unique-outcome" distinction (§4 instance ii), the register-before-allocate-is-unconditional / account-collision-is-conditional split, and the "reserve-m-and-fill-as-one-m-step over-satisfies clause 5 = OQ4" argument are all faithful and correctly reasoned. I found no misread, no fabricated claim, no altitude slip, no internal contradiction, and no missing load-bearing commitment.

# Revision list

1. **[SHARPENING]** *Implementation approaches → idem=⊤ de-duplication (clause 7):* the coverage-class index is described as "a hint, recomputable," but for the **primary** (lock-striped serialization) recommendation it must be an **exact, eagerly-maintained derived index**, not a Lampson hint — a stale "no coverage-equal active tuple" reading would admit precisely the duplicate clause 7 exists to suppress. The label *is* apt for the optimistic-retry variant (where the CAS verifies it), so the fix is to split the two: under lock striping the index is trusted/exact (maintained under the class lock at deposit time); under optimistic check-and-deposit it is a hint verified by retry. This also resolves an internal inconsistency with the frontier section, which carefully defines "Lampson hint" as stale-tolerated-and-verified and (correctly) says verifying a frontier hint costs as much as recomputing — reasoning that, applied to a *trusted* clause-7 index, would wrongly discourage it.

2. **[SHARPENING]** *Guarantees to uphold → "By construction":* the list mirrors the note's §5 model-intrinsic partition (W0 monotonicity, W1 contiguity, W4 registry, H1 uniqueness, A4/A6) but omits **W3 — active-slice coherence (`A_K = L_K ∖ nullified` coherent at every state, definitionally)**. It is subsumed under "no torn read / snapshot reads" and a builder won't get it wrong, but for completeness with §5 name it explicitly as a model-intrinsic guarantee — it is *why* a single `Observe_K` computes audit and active views mutually consistently off one state.

3. **[SHARPENING]** *Implementation approaches → organizing insight:* grouping clauses 4 and 6 as "one obligation … each must land on one committed state" is true of the underlying requirement, but the note makes the **scope** difference load-bearing — clause 4 is per-call/local, clause 6 is per-verdict/**global**. The digest does flag "clause 6, global," so this is minor; sharpen by noting that only persistent-immutable state collapses both to the same cheap mechanism (capture one root), whereas a mutable store satisfies clause 4 with a narrow per-access snapshot and clause 6 only with a global one.

The digest has no material problem: every approach respects the note's commitments (notably it never proposes value-based identity, never assumes global ordering is *required*, and correctly refuses to claim persistent state solves clause 7), the tradeoffs are real, and the defaults (single loop first, persistent state, coupled allocator, disjoint subspaces, WAL, surfaced rejection) are defensible and Lampson-shaped.

VERDICT: CONVERGED
