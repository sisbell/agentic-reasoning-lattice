The digest is accurate, comprehensive, and sound. It tracks the note closely, and on the two places where the note and the separate claim-statements file diverge it makes the *right* call: it takes W3 as **model-intrinsic** (the note's position, which the evidence's detail — `inc(max)`+retry stays contiguous, the gaps are W4/run or Green's fused namespace — supports against the evidence's looser headline), and it follows the note's **7-clause** MIC numbering (clause 6 = verdict pinning, clause 7 = idem dedup) rather than the claim file's 8-clause scheme. Every Green claim is grounded in the evidence (`findpreviousisagr`, the apex `cwid` aggregate, `writeenfilades`-at-quiescence, response-before-check on INSERT/DELETEVSPAN/REARRANGE, `next_doc_addr = max_child+1`, single-thread snapshot isolation). The contiguous-run pre-reservation refutation and the root-capture/clause-6 insight are both correct and genuinely useful. No ungrounded claim, no altitude slip, no missing load-bearing commitment, no approach that violates a note guarantee (the coverage-class dedup index is scoped to `idem=⊤`, not value-identity for all tuples).

The remaining items are all non-load-bearing:

**1. [SHARPENING]** "What must be built" → read surface: *"the home-relative `age`/frontier read — the **one** single-bounded-access read that is clause-4-free"* carries a false uniqueness implicature, since per-type `Observe_K` — named in the same sentence — is equally a single-bounded-access clause-4 read (note V0: the discriminator is *access count*, not type-confinement). Reword to "the one *non-`Observe_K`* single-bounded-access read clause 4 still covers," echoing the note's "`age` … is the one case needing an argument," so it doesn't read as contradicting the active-view reads listed beside it.

**2. [SHARPENING]** "Design commitments" → G0 bullet: *"Under A7 the substrate strengthens to linearizability"* misattributes the strengthening. The note (§3) has the *implementation* strengthen G0 to linearizability by honoring A7; the substrate-as-abstract-model guarantees only serializability. Say "a faithful realization honoring A7 strengthens to linearizability" to preserve the substrate/realization line the note draws.

**3. [SHARPENING]** Approach 4: *"If you are forced into mutable state, the root pointer is the natural version coordinate"* is locally inconsistent — the mutable route is exactly the one with no persistent immutable root to capture. Replace "root pointer" with "a monotonic commit/version counter" (the coordinate OQ2 notes the substrate does *not* expose and the implementation must add) for the optimistic read-then-revalidate loop.

**4. [SHARPENING]** "Guarantees to uphold": the digest cites `W1`/`W2`, labels that exist only in the separate claim-statements file, not in the note's own Claims-Introduced table (the note states the same cross-home-uniqueness-model-intrinsic / same-home-uniqueness-serialization-borne content inline in §5 without W-numbers). Tag them as claim-file labels or restate as the note does, so a builder cross-referencing the note doesn't hunt for a non-existent "W1" — and so the W-label sourcing stays consistent with the note (whose W3 the digest already, correctly, prefers over the claim file's).

VERDICT: CONVERGED
