The digest is unusually strong. It nails the per-home/global fault line, scopes clause 7 carefully (`idem=⊤` vs `idem=⊥`, no-duplicate vs unique-outcome), keeps realization-conditional facts (document-registration contention, subspace fusion) out of the "forced" column, and grounds its Green claims in the evidence (run-to-completion linearization point, apex width aggregate, counter-vs-coupled allocator, crash-mid-write divergence). The §4-instance-(ii) capture and the "persistent state does *not* solve clause 7" point are both subtle and correct. Findings below.

1. **[DEFECT]** *Guarantees / By construction*: "Cross-home (and cross-subspace) address uniqueness — distinct origins" states the wrong mechanism for the cross-subspace half. Per H1, cross-home (`d ≠ d'`) uniqueness is by distinct origins, but cross-subspace addresses (same `d`, `s_C` vs `s_L`) **share origin `d`** — H1 separates them by the subspace-digit divergence of the sibling anchors `[d.0.s_C]` vs `[d.0.s_L]` (`DisjointSubAllocatorChains` / T10), explicitly *because* "origin cannot separate them." The classification (model-intrinsic, order-independent) is right; the reason is false for one of the two axes. Attribute the two axes to their two distinct mechanisms — otherwise the digest ships a claim the note directly contradicts, and a builder reading it could let a document's content and link chains (same origin) collide.

2. **[SHARPENING]** *Implementation approaches / organizing insight*: "a single run-to-completion loop satisfies all seven MIC clauses for free… once each logical operation maps to one dispatch" is sound for runs (the reference confirms a multi-span COPY is one dispatch) but glosses clause 6. Because the substrate exposes only per-type `Observe_K` (§8), a recognizer that issues its constituent reads as separate requests drifts across indices *even under the single loop* — the §8 pathology is reachable with no per-home concurrency at all. Make explicit that "verdict = one dispatch" requires a substrate-side compound read (not a native primitive); that is where clause 6's cost actually lives, not solely at the per-home transition the digest locates it at.

3. **[SHARPENING]** *Implementation approaches / Contiguous runs (clause 5)*: reconnect to the note's emphatic contiguity ≠ atomicity point. Clause 5 buys the writer-side half only — a perfectly contiguous run still passes through interior states each carrying a strict prefix (A5/W2), so a reader can land mid-run on a canonical state; reader-atomicity is OQ4, above the substrate. Also pin down the "reserve `m` slots then fill" optimization: the fill must complete atomically/under the lock, else a reader sees reserved-but-empty addresses — states not on `𝔼`, contradicting A0/A6.

4. **[SHARPENING]** *State representation / The frontier as a hint*: the Lampson framing is slightly miscast. A frontier recomputed by query-and-increment is derived-*exact* (always correct, not a may-be-stale hint); only a maintained/cached aggregate (the reference's apex width) is a "hint" in the check-and-recompute sense. Keep the principle (don't store authoritative duplicate state) but apply "hint" to the cache, not the recomputation.

5. **[SHARPENING]** *Decisions for the builder*: surface the note's OQ7 lever — if a home's sub-allocator is statically partitioned among agents, two allocations the model treats as same-home conflicts can be proven independent, weakening clause 2 below per-home exclusion. It is an optional optimization the note explicitly raises and the decision list omits.

VERDICT: REVISE
