This digest is strong: it accurately separates the note's *forced* theorems (G1's per-home liberation, W0's operation-intrinsic permanence, A6's canonical≠settled) from its *conventional* choices (clause 7's one-index, clause 6's redundancy), correctly isolates the two genuinely non-per-home disciplines (clauses 7 and 8), and grounds every udanax-green claim (single-thread run-to-completion, `writeenfilades`-only durability, stateless `findpreviousisagr` allocation with O(1) apex aggregate but O(log n) type-under-home frontier, contiguity-as-serialization-artifact, response-before-check bug) in the verified evidence. Its three load-bearing engineering theses all check out: **clause 7 is genuinely free under whole-state root-capture** (each constituent `c_i` becomes a function of one frozen Σ, so `g(c₁(Σ_r),…,c_p(Σ_r)) = Q(Σ_r)` with no exclusion — the note itself says the one-index construction is "constructible … not automatic," and snapshot-capture makes it automatic); **pre-reservation cannot shorten the run-exclusion** (a foreign same-`(d,s_C)` commit at `φ+m` while `φ..φ+m−1` are empty is a gap, hence a non-canonical intermediate state — A6 forbids it, so the writer must be excluded for the whole run); and **a single writer dissolves clause 8** (global serialization makes dedup-read-and-deposit atomic). I found no material defect. The items below are all refinements.

**Revision list (most useful first):**

1. **[SHARPENING] Approach 5 — the OQ5 tail mis-frames read-atomicity as a cost-reducer.** "shrinking [the exclusion span] would require batch read-atomicity" reads as if read-atomicity could *shorten* the writer-exclusion. It cannot: read-atomicity (OQ5) is an orthogonal, strictly-stronger *reader-side* property that closes A5's interior-prefix gap "even for a W4-contiguous run" — it requires *more* mechanism, not less, and the contiguity exclusion (clause 5) is simply the irreducible floor by any means. Reword so OQ5 is a separate harder problem layered on top, not a cheaper substitute. (The pre-reservation argument itself is sound and is one of the digest's best builder insights.)

2. **[SHARPENING] Approach 3 / "given up" — make the same-home idempotency failure explicit.** The digest recommends the correct mechanism (coverage-class atomic get-or-insert) but under-states *why* per-home serialization is insufficient even within one home — the note's stated *primary* idempotent case (a retry, or two workers asserting the same edge in one document). Clause 2 serializes the frontier-read-and-deposit but **not** the preceding *global* dedup-read of `A_K`, so two same-home `idem=⊤` emits both read a stale `A_K`, both miss, and clause 2 merely *spaces* their deposits to `φ, φ+1` (both born active) rather than suppressing one. State this so a builder doesn't conclude that per-home deposit serialization — or even a per-home actor that serializes only deposits — covers same-home idempotency; the dedup-read must be fused into the deposit's critical section, which is exactly what your coverage-class get-or-insert must do.

3. **[SHARPENING] Approach 1 — "one append = clause 1" attributes A0 to the wrong mechanism.** A0/clause 1 is an *observability* property (no observer reads a state between Σ_i and Σ_{i+1}); it is discharged by the atomic publish of the immutable root, not by the journal append. The append establishes ordering (𝔼) and durability/recovery (W0). The digest gets this right in "Guarantees" ("clause 1 plus atomic publish") — make approach 1 consistent, so a builder doesn't read clause 1 as satisfied by a log whose records a reader could tail mid-write.

4. **[SHARPENING] Approach 1 — verify or generalize the repo-artifact references.** "exactly this repo's `links.jsonl` mechanism" and recovery "the way `paths.json` is" are stated as fact but are grounded only in the working repo (not the note or the Green evidence). Confirm these files actually implement the append-only-journal / replay-recovered-registration pattern; if not exactly, generalize to "an append-only step log with replay-recovered home registration" so the recommendation doesn't hinge on a specific artifact that may differ.

5. **[SHARPENING] Co-locate the nullification non-determinism contrast.** The digest correctly states both that instance (ii) (Nullify of an *already-emitted* coverage-equal incumbent) is irreducible — emit-before-retract vacuous, clause 8 inapplicable — and that an out-of-order retraction of a *not-yet-emitted* target is cleanly rejected and removable by the coordination layer's emit-before-retract. But these live in separate sections ("given up" vs. approach 6). Pairing them — already-emitted target ⇒ survives full discipline; not-yet-emitted target ⇒ coordination-layer's job — sharpens the builder's model of exactly which non-confluence the substrate hands upward.

The accuracy, completeness across all eight MIC clauses, altitude (approaches, never code/signatures), and committal stance are otherwise solid; the durability-coupling, frontier-as-hint, and surface-the-rejection recommendations are well-judged Lampson calls.

VERDICT: CONVERGED
