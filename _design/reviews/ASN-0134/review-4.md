The digest is well-organized, stays at design altitude, and uses the Green evidence well. Its original moves are largely sound — the **root-capture insight** (Approach 1: handing the reader an immutable snapshot handle makes the multi‑read‑verdict discipline free, with no reader‑side lock) is correct and the best engineering observation in the document; the **pre‑reservation debunking** (Approach 5) correctly shows the run‑exclusion window is irreducible; and the **self‑emit/self‑nullify** precision (Approach 6) and the **persistent idem=⊤ outcome non‑confluence** ("Explicitly given up") are accurate and sharp. But it carries three material errors. All three propagate the *claim‑statements* extraction at exactly the points where that extraction contradicts the **note prose** — and the prose is the spec, and is the technically correct side.

---

**1. [DEFECT] — "Guarantees to uphold," Approach 2, and the A6 framing: W3 (dense chain contiguity) is mis-partitioned as serialization-borne; the note says model-intrinsic.** The digest puts "dense chain contiguity (W3)" under *Require active enforcement* (clause 2) and asserts A6's chain-contiguity conjuncts "are exactly W3, *serialization-borne*, holding only because clause 2 produces a gapless 𝔼." The note's §5 places W3 in the *kept-by-the-model-itself* column and argues it explicitly: 𝔼 is committed to the `inc(max,0)` allocator, so a stale frontier read can only land at an already-occupied *lower* slot (a collision, rejected to zero by A1) — never *past* the frontier — so a **hole never forms under any interleaving**; "the one genuinely serialization-borne contiguity claim is W4's run contiguity, not this one." Per-home serialization buys *same-home uniqueness* (W2 — both racing emissions commit), **not** contiguity. The evidence agrees (Q19: a collision "retries and gets I.12 — *still contiguous*"; the gaps it reports are cross-type/run fragmentation, i.e. W4 in Green's *fused* namespace). This is the digest's central "intrinsic vs. serialization-borne" thesis getting one of its entries backwards, and it recurs in 3+ places. Fix: move W3 to *Hold by construction*; state that clause 2 secures W2 uniqueness, not W3 contiguity; drop the claim that A6's contiguity conjuncts are conditional on clause 2 (every state on a valid 𝔼 is fully canonical, contiguity included, because rejected steps are *not on* 𝔼); and drop the dependent muddle that the frontier count-hint "is only valid under clause 2" (the count is `|P_S|`, recomputable from the journal always). *(The digest's framing matches the claim-statements' W3 "ChainContiguitySerial"; the note prose's "ChainContiguityStructural" is both authoritative and provably correct — follow it.)*

**2. [DEFECT] — "What must be built" / "How it fits": the K.σ shared-frontier conditional is dropped, and the builder is told the opposite of the note.** The digest says document registration is "out of scope," "there is no document frontier to advance," and "a builder should **neither serialize registration as an allocation** nor mishandle the collision." The note's position is *conditional on the realization*, and for the canonical case it is the **opposite**: under a **shared-frontier** document allocator — Nelson's owned-numbers and Gregory's `next_doc_addr = max_child(account)+1`, **both confirmed by the evidence** — two same-account creations read one account frontier and compute the *same* address (H2 one tier up), so registration is the **account-tier instance of clause 2 and must be serialized per-account** (MIC clause 2; §9 minimality: "drop 2 and same-home allocations collide, *same-account document creations included*"; SAFE(c); H3). Only a **collision-free** allocator (distinct fresh addresses, no shared max) escapes this and resolves a same-`d` race by rejecting the loser. The digest collapses everything to the collision-free reading and so misadvises anyone using the natural Green-style allocator (they would skip serialization and silently drop concurrent same-account creations). Fix: restore the shared-frontier/collision-free split; state that a shared-frontier realization serializes same-account K.σ exactly as same-home content (account-as-home, H3); add the **unconditional register-before-allocate ordering** (an allocation into `d` presupposes `d ∈ dom(M)`) which the digest omits entirely; add the **ASN-0047 (A_doc) dependency** the note declares and the digest drops; and surface "shared-frontier vs collision-free document allocator" as a builder decision.

**3. [DEFECT] — Throughout: MIC clause and Open-Question cross-references are off relative to the note.** The digest numbers the verdict-pinning clause **7** and idem-dedup clause **8**, and cites **Open Question 5** (batch read-atomicity) and **Open Question 9** (out-of-order retraction). The note's canonical MIC has **seven** clauses — verdict = **6**, idem = **7**, registry confinement *folded into W6* ("MIC needs no registry-write clause of its own"; "each of its seven clauses"; "Registration adds no eighth clause") — and **eight** Open Questions, with batch read-atomicity = **OQ4** and out-of-order retraction = **OQ8** (there is no OQ9). A builder cross-referencing the note hits a non-existent "clause 8" and "OQ9." Fix: re-align every clause and OQ reference to the note's 7-clause / 8-OQ scheme. *(Mechanical, but factually false as written; same root cause — the claim-statements' 8-clause extraction.)*

**4. [SHARPENING] — Design commitments / G0: state the positive half (a sequential client recovers SC/linearizability).** The digest correctly says "serializable, not SC" and that an agent must self-serialize, but omits that under A7 (commit-before-ack) the substrate strengthens to *linearizability*, and a sequential (issue-after-ack) client thereby recovers SC as an emergent property of its own discipline — the note's framing of what the ack-ordering actually buys a disciplined client. One sentence.

---

All four items are improvements; items 1–3 are material (a builder following the digest would mis-attribute contiguity enforcement, misimplement document allocation, and mis-resolve note cross-references). The digest's approaches do not otherwise violate the note's commitments — notably it correctly refuses content-dedup for `idem=⊥` and correctly keeps the two non-per-home disciplines out of the per-home liberation.

VERDICT: REVISE
