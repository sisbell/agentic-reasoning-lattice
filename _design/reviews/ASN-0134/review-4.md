The digest is strong: it reads the per-home/global fault line correctly, grounds every Green claim in the evidence (single loop, apex width aggregate, `findpreviousisagr` descent, counter-vs-coupled contiguity, the crash-mid-write/no-startup-validation hazard, response-before-check), stays at design altitude, and gets the hard subtleties right — clause 7 buys no-duplicate but not outcome-determinism (§4 instance (ii)), persistent state is read-side-only, A3's "even a lone `Observe_K` straddles a cross-home commit." The MIC clauses, SAFE guarantees, all eight OQs, and the dependency map are faithfully covered. Most of what follows is polish; one item is a genuine inaccuracy.

## Revision list

**1. [DEFECT] — "Design commitments → Forced → Batches are not atomic": the claim "a content run *or* a `retract_stale` can be observed partially" is false for content runs and contradicts the digest's own body.** A5 is definitive: a content run is "non-atomic only *structurally* — its mid-batch state exists and is canonical … but its prefix is witnessed by no read the note models" (the read surface is link-only, §8). So *only* a `retract_stale` (link-store batch) is read-observable partially; a content run's interior states exist and a reader can *land* in one, but no modeled read *witnesses* the prefix. The digest's own "Contiguous runs" section states this correctly ("that partial is structural and witnessed by no read the note models … the reader hazard is forward-looking"), so the summary bullet is internally inconsistent as well as mis-stating A5. Fix the bullet to draw A5's distinction (link-store batch = read-witnessed partial; content run = structurally non-atomic, not witnessed by any current read). (The note's §2 prose is parallel-loose — "see only a prefix of its atoms" — but A5 governs, and the digest demonstrably knows the precise version.)

**2. [SHARPENING] — "Design commitments → G0" and "Guarantees": state the client-model lever the note supplies.** The digest correctly says the substrate presents serializability not SC, but omits a real builder position G0 makes explicit: the consistency level is *client-model-dependent* — pipelining clients get only serializability, while "a *sequential* client recovers SC by its own acknowledgment discipline (issuing each operation after the prior's response), not by any substrate promise." Add this; it tells a builder who needs SC exactly where to get it (impose ack-before-next at the client/coordination layer, not the substrate).

**3. [SHARPENING] — "Implementation approaches → The frontier as derived state": the dismissal of a Lampson hint is phrased in a way that reads as contradicting the endorsed optimistic frontier-CAS.** The reason given — "the coupled allocator needs the exact maximum at deposit time, not a check-and-recompute guess" — taken at face value also indicts the optimistic-CAS-with-retry the digest recommends (read frontier → compute → install-if-unchanged → else retry *is* check-and-retry). Reconcile: optimistic CAS reads the frontier *fresh* each attempt and CAS-detects concurrent change, whereas a Lampson hint *retains* a possibly-stale value across calls; the real reason a retained frontier hint buys little is that verifying it against the population costs about as much as recomputing — not that "guessing" is forbidden.

**4. [SHARPENING] — "Implementation approaches → The frontier as derived state": "the reference keeps exactly this" overstates the apex-aggregate analogy.** The recommendation is an aggregate *at the home node*; the reference keeps a whole-granfilade aggregate *at the apex* and recovers the per-`(home,subspace)` frontier by *descent* — so it is not "exactly this," it is a coarser, whole-store instance plus descent for the per-home value. The "albeit whole-store rather than per-type" caveat half-corrects this; drop "exactly" and say the reference demonstrates the eager-aggregate technique at apex/whole-store granularity, recovering per-home by bounded descent.

The organizing insight (single loop free → per-home concurrency ends the free ride for clauses 2/4/5/6/7, with 3 and 6 needing conscious handling even under the loop), the persistent-state recommendation with its read-side-only caveat, the population-coupled allocator argument, the "reserve-and-fill-as-one-m-step over-satisfies clause 5 / no middle path" reasoning, and the WAL/multi-store-atomicity caution are all sound and well-grounded — leave them.

VERDICT: REVISE
