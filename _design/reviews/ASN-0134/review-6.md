Reviewed the digest against ASN-0134, its claim statements, and the evidence channel. This is a strong, faithful digest: it navigates the note's hard distinctions correctly — forced vs. conventional, per-home vs. global, clause 4 vs. clause 6, `idem=⊤` vs. `idem=⊥`, no-duplicate vs. outcome-determinism, clause 5 vs. OQ4, structural-canonical vs. settled — and every Green claim (apex width aggregate, bounded-descent frontier recovery, response-before-check, spanfilade crash-divergence, counter-allocator-loses-contiguity) traces to the evidence answers. The forced/conventional split is accurate, no guarantee is mis-stated, no proposed approach violates a commitment (idem dedup is correctly kept distinct from content reference-sharing), and the altitude holds (design, not code). I found no material defects.

## Revision list

1. **[SHARPENING] Implementation approaches / clause 6: strengthen the "global, not per-home" claim with the retractor-home rationale.** The digest correctly says clause 6's reader snapshot is global and must exclude "any interleaving writer step," but it omits *why the scope cannot be narrowed* — which is the one place a builder is tempted to optimize. Add the note's reason (§8): a verdict's nullification constituents are moved by active-`K_i`-hitting `Emit_R` steps homed at an *arbitrary retractor home* bearing no relation to `Q`'s types, so excluding only "the homes carrying `Q`'s types" undercounts by dropping exactly those nullifications. This forecloses the tempting wrong optimization and is the affirmative argument that clause 6 is irreducibly global (unlike clause 2, which decision #11/OQ7 *can* narrow). The digest is sound without it — it already says "global, every writer step" — hence sharpening.

2. **[SHARPENING] Decisions for the builder #6: tighten "decides whether `K.σ` contends at all."** As written this can be misread as "under the collision-free scheme `K.σ` carries no constraint." Qualify to "decides whether `K.σ` contends *on the document frontier*," since the unconditional register-before-allocate ordering binds `K.σ` in *every* realization (the digest states this correctly under "What must be built," so this is only a local wording slip in #6, not a substantive inconsistency).

Genuinely solid and worth not touching: the organizing single-loop/per-home fault line (the "free for 1/2/4/5/7, handle 3 and 6" analysis is precise, including that A7 is orthogonal to the one-dispatch premise and a multi-request recognizer drifts even under the single loop); the reserve-`m` reasoning (φ_S = |P_S| forbids reserved-empty states, so "reserve cheaply, fill lazily" is incoherent and the atomic m-step is OQ4, not a cheaper clause-5 substitute); the §4-instance-(ii) point that clause 7 buys no-duplicate but not outcome-determinism; and the persistent-state lever scoped honestly to the read side (does *not* dissolve clause 7).

VERDICT: CONVERGED
