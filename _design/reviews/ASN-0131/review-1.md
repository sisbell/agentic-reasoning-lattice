Strong digest overall — the "one index, two readers," the hint/journal/replay framing, the refcount-vs-RE-UNIT tension, the content-identity-vs-rendered mapping to OQ3, and the union-not-intersection caching caution are all sharp and well-grounded in the evidence. The revisions below are where it still misreads, mislabels, or under-specifies.

**1. Decisions → "Whole-endset vs touching-spans" (and "Content-identity vs rendered"): "green returns the whole stored extent, matching [whole-endset]" is unsupported and probably inverted.** The Q13/Q14 evidence answers the *clipping* question — a matching span is returned at full stored extent, not truncated to the region (that is RE-CLIP). It does **not** establish *whole-endset* surfacing. Green's RETRIEVEENDSETS reads the spanfilade SPANRANGE-restricted, one entry per contiguous endset span, so a discontiguous endset contributes only the span(s) intersecting the region; its non-touching spans are never found — i.e. green implements the *touching-spans-only* economy. Whole-endset fetch in green lives in FOLLOWLINK (which reads the link's own POOM), not RETRIEVEENDSETS. And RE-WHOLE is baked into RE-DEF ("the returned `e = Σ.L(a).eᵢ` is the whole slot endset"), so the note's RE *diverges from green's RETRIEVEENDSETS at the definition level*. Fix: attribute green's RETRIEVEENDSETS to RE-CLIP only; state that on the whole/touching axis green is evidence *for the economy* in OQ1 (against the note's adopted convention), not confirmation of it — and verify against a discontiguous endset before asserting either way. Keep the two invariants separated exactly as the note insists.

**2. Design commitments, bullet 1: marking dedup / "multiplicity is not recoverable" as forced ("a consequence the builder cannot escape") is wrong — OQ2 reopens it.** Identity-withholding is forced (the operation's reason for being); collapsing two distinct links' identical `(slot, endset)` to one pair, and the resulting loss of multiplicity, is the note's *adopted* choice that OQ2 explicitly questions ("must the answer preserve their multiplicity, or is collapsing them faithful?"). This is exactly parallel to RE-WHOLE, which you correctly mark provisional. Re-mark the collapse/multiplicity rider as conventional/provisional (OQ2), keeping identity-withholding and non-pairability as the forced parts. Note too that your incremental-refcount insight is *contingent* on this choice: if OQ2 resolves toward preserving multiplicity, the refcount tension dissolves.

**3. Implementation approaches → Addressability / How it fits / Decisions: "retraction only removes" is conditional, and the load-bearing builder decision behind it is missing.** The note proves a retraction's from-set and to-set are content-disjoint *unconditionally*, but the retraction's own *type* endset Θ is not: `coverage(Θ) ∩ dom(Σ.C) = ∅` is an **imposed discipline, not a theorem** (OQ6). If Θ can meet the content image, a retraction surfaces `(3, Θ)` and *adds* anchoring instead of only removing — breaking RE-RET's net-removal-only and your refcount-decrement model. The builder decision the note implies — seat the retraction type at a dedicated, content-disjoint, unit-depth element-level subspace so field-agreement transfers — is absent from the digest. Evidence Q2 sharpens the risk: green's type addresses are *not* structurally content-disjoint (they live in a type document's link subspace, within content), so a build that mimics green can violate this. Add the conditional to the retraction handling and add "where to seat the retraction type" to Decisions.

**4. What this is / Design commitments: "deliberately poorer answer" misframes RE, and RE-EXST is never surfaced.** RE is not a degraded findlinks: it delivers the *endset structure* (the spans where links attach) that findlinks withholds, while withholding the identity findlinks gives — a *different* deliverable, not a lossy one. RE-EXST states the two existence/discovery axes are *orthogonal* (discovery on query-mode, existence-of-anchoring on deliverable). Reframe "poorer" as "different / non-followable but structure-revealing," and surface the orthogonality so the builder doesn't model RE as lossy findlinks. Minor, fold in: the one-liner's "same selection" glosses the addressability filter — selection is `findlinks_V ∩ addressable`, strictly narrower than `findlinks_V`.

**5. Implementation approaches → anchoring index / Assembling the answer: index and reader are specified against a fixed role set, but RE-DEF is arity-general.** RE ranges `1 ≤ i ≤ |Σ.L(a)|`, and L3 admits "at least three" endsets ("possibly more"); green hardcodes exactly from/to/three bands. State that the coverage index's role dimension and the collector's slot key must support arbitrary slot index — otherwise higher-arity links silently lose their fourth-and-beyond endsets, violating RE-CMP.

**6. Implementation approaches → Recovery: the `links.jsonl` / `paths.json` repo-substrate claim is grounded in neither the note nor the evidence.** It is asserted as fact ("exactly this repo's working substrate"). Verify it against the repo; if it is not the real substrate, soften to a generic "append-only link journal recovered by replay" — which is all the argument actually needs.

**7. Implementation approaches: drop the specifics that exceed design altitude or assume an unstated target.** `im::OrdMap` and "the Rust target" presume an implementation language the note never establishes and dip below design altitude. Use "persistent ordered map / interval map"; if a Rust target is a genuine project constraint, say where it comes from, otherwise remove the assumption.

**8. Design commitments, bullet 2: coverage-permanence is misattributed.** You write that transclusion-blindness and coverage-permanence both "fall out of content-keying for free." Transclusion-blindness does; coverage-permanence comes from link immutability (L12) + LP3★ — your own Guarantees section says exactly this. Align them: content-keying makes the answer a content-identity answer; immutability makes that answer *permanent*.

**9. (Sharpening) Implementation approaches: surface RE-CWP / RE-EDIT as the builder's "when can I skip work" rules for the incremental cache.** You propose incremental maintenance but never connect it to RE-CWP — a *checkable, pre-edit* precondition (`∀ available (i,e): coverage(e) ∩ Δ ≠ ∅ ⟹ coverage(e) ∩ I_R ≠ ∅`) under which a contraction leaves RE unchanged — or to RE-EDIT, which classifies every mover by how it moves the answer. Cite both as the skip-recomputation rules.

Solid and leave as-is: the region resolver / touch-oracle decomposition, the scan-vs-index tradeoff with scan as recovery oracle, the no-clip = full-extent-routine guarantee (Q14), the append-only-index-as-hint reasoning, and the global-vs-per-document index discussion (RE-TRANS / OQ5).
