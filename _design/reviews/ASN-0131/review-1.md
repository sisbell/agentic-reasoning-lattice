The digest is accurate, well-grounded, and at the right altitude. Its **forced/chosen tags track the note's own "load-bearing vs adopted/design-decision" distinctions** — RE-CLIP forced, RE-WHOLE marked *chosen, provisional* (the note literally says "this is a convention, not a forced consequence"), addressability *chosen* (the note calls it "a design decision") — all correct. The Green claims (write-only spanfilade, content-address key + role dimension, the non-clipping endset path vs. the clipping content path, global search, V-rendering as a lossy ghost-filtering/fragmenting layer "Green effectively baked in") are squarely supported by the evidence answers. The spine (one shared index → two read-outs; recomputable hint; correctness restored at read time by live resolution + addressability filter; stale entries tolerated) is sound and proposes nothing that violates a note commitment — and it correctly notes that a per-span index *naturally yields touching-spans*, so whole-endset needs a join. One material inconsistency, then sharpenings.

**Revision list**

1. **[DEFECT] "What this is": "differs only in what it reads out" is false and contradicts the digest's own spine.** Per the note, `findlinks_V` ranges over *all* of `dom(Σ.L)` — withdrawn links included — and RE's contributing set is `findlinks_V ∩ addressable`. The spine says exactly this (`sel = findlinks_V ∩ addressable`); the design commitments headline "Only addressable links contribute." So RE differs from `findlinks` in **two** ways — the read-out *and* the restriction to the addressable population — not one. The "only" erases the addressability filter, contradicting the rest of the digest. Fix: "shares that query's selection *index* and differs in what it reads out *and* in reporting only the addressable population."

2. **[SHARPENING] The intersection law (RE-UDIST-∩) is missing.** The digest sells union-composability hard ("querying a passage is the union of querying its lines") but never states that intersection does *not* compose — a real builder trap. Add the note's result: `RE(W₁∩W₂) ⊆ RE(W₁)∩RE(W₂)` is one-sided and unconditional, but the reverse fails *even under an injective arrangement* (split witnesses — one address from each region's exclusive part), so an intersection-region answer must not be computed by intersecting sub-answers. (Equality's exact condition / a structural sufficient form is OQ4.)

3. **[SHARPENING] "Green's spanfilade … proven at scale" is unsupported.** The evidence establishes the spanfilade's *structure* and read-time behavior, nothing about scale; udanax-green is a released research codebase, not a system demonstrated at production scale. The defensible claim is that the enfilade family is *designed* for scale (logarithmic access over a large address space). Soften or cut "proven at scale."

4. **[SHARPENING] The [forced] gloss ("follows from the data model; downstream cannot violate") doesn't fit no-clipping or overlap-not-containment.** Those are forced by *faithful reporting* (a clip misreports the link's grip; containment answers a different question), not by the data model — and the digest itself says Green *has* a clipping path, i.e., downstream physically *can* clip. Widen the gloss to "forced (by the data model *or* by faithful reporting)," or annotate those two items, so the category stays honest. (Content-identity keying and purity *do* genuinely follow from the data model; discovery-anchored is properly conditionalized.)

5. **[SHARPENING] Surface the conservative-lift modelling assumption beneath the cache-invalidation contract.** The contract treats user-facing insert/delete as pure arrangement edits (image-movers only); the note reaches that only by *explicitly adopting* the conservative lift — shift-based insert/delete touch `Σ.M(d)` alone, framing `Σ.L/Σ.E/Σ.R/Σ.C`. State it: if a builder's insert/delete also mutate the link store, the addressability half of the invalidation contract must be re-derived.

VERDICT: REVISE
