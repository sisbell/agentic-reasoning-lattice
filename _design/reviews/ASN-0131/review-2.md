This is a strong, accurate digest. The Lampson "spine" (build the selection index once as a recomputable, over-approximating hint; restore correctness at read time by live arrangement-resolution + addressability filtering) is the right frame and is well-grounded in the write-only-spanfilade evidence. The whole-endset/touching-spans fork is handled with real precision — the digest correctly sees that a per-span index yields touching-spans naturally and that whole-endset needs a join back to the store, and it correctly ties that choice to union-distributivity. The `[forced]`/`[chosen]` tags all hold up against the note; every udanax-green claim (clipping vs non-clipping paths, global search, V-rendering with ghost-filtering and pivot-fragmentation, multi-document SpecSet) is supported by the evidence answers; dedup-by-value-not-identity correctly avoids the RE-UNIT leak; and the RE-UDIST-∩ "never intersect sub-answers" warning is correct. No misread of a forced/chosen commitment, no fabricated Green claim, no altitude slip into code, no missing load-bearing component or guarantee. The problems below are all refinements.

**Revision list**

1. **Guarantees → RE-UDIST-∩ bullet:** the parenthetical "split witnesses: one shared address drawn from each region's exclusive part" is muddled and risks conflating the two obstructions the note is explicitly at pains to *separate*. The injective obstruction turns on **two distinct witnesses** — the endset covers one address from `image(W₁)`'s exclusive part *and a different one* from `image(W₂)`'s — precisely **not** a shared address (the shared-address mechanism is the *other*, non-injective obstruction). "split witnesses" already signals distinctness, so the dominant reading is recoverable, but reword to remove "shared," e.g. "the endset covers a *distinct* address in each region's exclusive image — two split witnesses — so it lands in both sub-answers yet not the intersection-region answer." `[SHARPENING]`

2. **Implementation approaches §5 (caching) / Decisions → type-slot:** the digest surfaces the "type coverage disjoint from content" hypothesis only as an OQ6 *build choice* and lists retraction as a cache-invalidator, but never connects them. That hypothesis (`coverage(Θ) ∩ dom(Σ.C) = ∅`) is exactly RE-RET's **net-removal-only** condition: absent it, a retraction emits a fresh, born-addressable retraction link whose type-slot `Θ` can meet the content image and *add* the pair `(type, Θ)` — so a retraction can **add** anchoring, not only remove it. Say this, so a builder optimizing a cached view doesn't assume retraction is removal-only. `[SHARPENING]`

3. **Implementation approaches §1 (the index):** "role-banded by from/to/type" mirrors Green's three fixed bands, but RE-DEF surfaces *every* slot `1 ≤ i ≤ |Σ.L(a)|` and links carry **≥3** endsets ("possibly more", L3) — so completeness (RE-CMP) requires indexing higher slots too. The digest's own `(slot, link)` entries and "per-slot" matcher already imply general-slot banding; make it explicit that the band is the *slot index* (from/to/type just name the first three), so a builder copying Green's three-band spanfilade doesn't silently under-index `>3`-endset links. `[SHARPENING]`

4. **What must be built (final paragraph):** "The only durable artifact this motivates is the index" sits in tension with the recommended default (rebuild-by-replay), under which the index is *ephemeral* (rebuilt on load) and the link journal/store is the durable authority. Reword: the only *new structure* RE motivates is the index, and it is a rebuildable **hint** — the durable authority is the link store/journal it is derived from. `[SHARPENING]`

VERDICT: CONVERGED
