Overall this is a strong, well-grounded digest: it correctly separates RE-CLIP from RE-WHOLE, nails the content-identity-vs-rendered distinction (and that green's output-gating would *violate* RE-IDENT if applied to the primitive), catches the arity-band completeness hazard (L3 admits >3 endsets vs green's three bands), builds the index-as-hint/replay-recovery model in proper Lampson discipline, and reads the RE-EDIT/RE-CWP invalidation rules accurately. It stays at design altitude and is committal. There is one material defect, plus three sharpenings.

**Revision list**

1. **[DEFECT] — Decisions §"Whole-endset vs touching-spans payload" / §"Caching by region cover" / Guarantees §"Union-distributivity": the claim that whole-vs-touching is "purely a wire-payload-size choice" is false, and it leaves the recommended union-caching strategy unsoundly unconditioned.** The note is emphatic (OQ1, §Extent, §Composing regions, RE-UDIST, RE-UDIST-∩) that the choice *determines the composition algebra*: whole-endset surfacing **uniquely** affords union-distributivity (RE-UDIST) and the unconditional `⊆` half of RE-UDIST-∩; **both are forfeit under the touching-spans reading (`RE_clip`)**, for which the note gives explicit counterexamples. The note calls this "a properly algebraic argument for the adopted reading" and "*more decisive* than §Extent's faithfulness appeal." Fix three places: (a) replace "purely a wire-payload-size choice" with a statement that the choice **also** decides whether the composition laws hold, and that this is the note's *strongest* argument for whole-endset; (b) in "Caching by region cover," condition the union-composition strategy on the whole-endset reading — under a touching-spans payload, RE-UDIST fails, so composing cached region answers by union is **unsound** (this is a soundness precondition on a *recommended* approach, not a stylistic note); (c) qualify the Guarantees line "Union-distributivity (by construction)" as holding only under the adopted whole-endset reading.

2. **[SHARPENING] — Design commitments, "Pure read over the *addressable* population. *(Forced.)*": disambiguate the forced/conventional boundary.** The purity/locality content (`Σ' = Σ`; reads only `Σ.M(d)`, `Σ.L`) is forced. But the note explicitly labels the choice the bullet's header also carries — reporting over the *active/addressable* population *rather than the full permanent store* — "a **design decision**" (§"The unit of the answer"). As written, "(Forced.)" reads as covering both. Split them: purity is forced; reporting-over-addressable-vs-full-store is the note's settled design decision, so labeled in the note.

3. **[SHARPENING] — Decisions §"Whole-endset vs touching-spans payload": note green's *further* divergence beyond touching-spans.** Per evidence Q11, green returns three flat per-role specsets (one from-endset, one to-endset, one three-endset), i.e. a *union of touching spans per role* — it does not preserve endsets as units or group spans by link/slot at all. So green is not just touching-spans; it is fully role-flattened, even more destructured than RE-DEF's `(i, e)` pairs (and than RE-UNIT's per-`(slot,endset)` dedup). Worth one line so a builder doesn't mistake green's answer shape for a reference implementation of RE-DEF's deliverable. Reinforces the existing (correct) recommendation to build the content-identity `(role, endset)` primitive rather than copy green.

4. **[SHARPENING] — Design commitments, retraction-emitter bullet: tighten the RE-NCD citation.** "from-set (∅) and unit-depth to-set are content-disjoint *unconditionally* (RE-NCD)" attributes both to RE-NCD; per the note only the *to-set* uses RE-NCD (a unit-depth span at a non-content `s_L` start). The from-set's disjointness is just `coverage(∅) = ∅`. The claim is true; only the citation is loose — split it.

The "How it fits," "What must be built," touch-test/RE-BND handling, addressability-as-separate-filter, and RE-CWP precondition sections are sound as written.

VERDICT: REVISE
