This digest is unusually disciplined and accurate. It correctly separates the forced commitments from the conventional ones, gets the hard technical calls right (the arity-general index gap, the whole-endset/touching-spans divergence, union-yes/intersection-no caching, content-identity-vs-rendered), and grounds every Green claim in the evidence. I found no material problem — no misread of the note, no fabricated Green claim, no altitude slip, no missing load-bearing commitment. A few genuine but non-blocking refinements follow.

**Solid sections (no change needed):** "What must be built" (complete component list); the "one index, two readers" approach with the **three-band-drops-slot-4+** completeness catch (RE-DEF is arity-general, L3 admits >3, Green fixes exactly from/to/three — this is a real, correctly-flagged soundness hazard); the whole-endset vs touching-spans treatment (Green's SPANRANGE-restricted search returns only intersecting crums, each unclipped — correctly read as *touching-spans economy diverging from RE-DEF*, with the right hedge to verify on a discontiguous endset); and the cache-composition section (RE-UDIST up, never RE-UDIST-∩ down; RE-EDIT/RE-CWP as invalidation rules).

# Revision list

1. **Decisions / "Where to seat the retraction type" (and the parallel line in Design commitments): tighten the Green type-address characterization.** [SHARPENING] The clause "they sit in a type document's link subspace, *within content*" is internally tense (link-subspace ≠ content-subspace) and the two evidence answers genuinely differ — one places Green's type addresses at the link-subspace `1.0.2.x`, the other says "within `dom(Σ.C)`." The *load-bearing* fact (supported by both) is that Green's Θ–content disjointness is **convention/allocation-enforced, not type-system-structural** — `acceptablevsa()` is a no-op and the backend never rejects a type endset pointing at content. Rest the caution on "not enforced, so a violated convention lets Θ meet content," not on "within content," which is the contested clause.

2. **"Caching by region cover" / RE-EDIT invalidation rules: complete the transition classification.** [SHARPENING] The digest enumerates the transitions that leave `RE` *fixed* (skip recompute) and handles contraction via RE-CWP, but never states that **extension `K.μ⁺` and reorder `K.μ~`** (content-subspace edits to `d`) *do* move the answer through the image and force a recompute. For an exhaustive invalidation rule a builder can implement directly, name the "must-recompute" set alongside the "safe-to-skip" set.

3. **"What this is" opening: sharpen "anchoring pairs of every live link that touches that region."** [SHARPENING] `RE` surfaces the *touching endsets*, not all endsets of a touching link — a link whose from-endset touches while its to-endset points outside `W` contributes only the from-pair. The body gets this right (per-endset testing), but the one-line summary slightly overstates. Phrase it as "the touching endsets of every live link that reaches the region."

4. **Make RE-ADDR explicit in the emission/addressability discussion.** [SHARPENING] The digest leans on "append-only index + filter nullified at query time," which implicitly covers it, but never states the lemma a builder relies on: a freshly-emitted **non-retraction** link is immediately addressable in its post-state (RE-ADDR), so ordinary emission can only *add* pairs (monotone). Stating it closes the emission side of the stability story to match the explicit retraction (RE-RET) and contraction (RE-CWP) treatments.

5. **"Assembling the answer": note that incremental maintenance also needs refcount *increments* on emission, not only decrements on retraction.** [SHARPENING] The refcount insight (answer hides multiplicity, incremental maintenance needs it internally) is correct and well-observed, but framed only around the retraction-drop direction. The same per-pair bearer count must be incremented on emission for the cached set to stay correct — worth one clause so a builder doesn't implement a decrement-only counter.

VERDICT: CONVERGED
