This is a strong digest — accurate, well-grounded in both the note and the verified evidence, and consistently at design altitude. The Lampson spine (one index, two read-outs; a write-only over-approximating hint cancelled at read time by live resolution + addressability filtering) is exactly right and matches the Green evidence (Q12/Q16/Q18). The hardest technical points are handled correctly: the clipping-vs-non-clipping read paths (Q13/Q14), the split-witness failure of intersection-composition *even under injectivity* (RE-UDIST-∩), the rendering layer's ghost-filtering and pivot-fragmentation as a *separate lossy layer* (Q17/Q20), and the cache-invalidation contract derived from the stability theorems. The forced/chosen labels are all defensible. I found no material misreads, no fabricated Green claims, and no missing load-bearing component or guarantee. The items below are genuine but non-load-bearing.

**Revision list**

1. **Implementation approaches §1 — substrate references presume the wrong ecosystem.** The `im` crate (§1b) is a Rust library, but this repo's substrate is Python; and `paths.json` (§1a) isn't verifiable from here (`links.jsonl` is confirmed in the tree, `paths.json` is not). State the design point language-neutrally ("a persistent immutable index with structural sharing — cheap snapshots, lock-free readers, point-in-time queries"), and either confirm `paths.json` or generalize the journal reference to "the link/endset journal (e.g. `links.jsonl`)." The design content survives untouched. **[SHARPENING]**

2. **"The spine" / §3 / Guarantees — separate the note's model layer from the Green reference, and surface the retraction-discipline assumption.** Two things. (a) The verified evidence (Q2) records that udanax-green has *no* retraction/nullification operation at all, so the **addressability filter is purely the note's model layer (ASN-0086), not part of Green**. As written, "(a) resolving through the live arrangement and (b) filtering to addressable links. This is precisely how udanax-green works…" risks reading as if Green filters addressability; its staleness cancellation is POOM-resolution (a) only. Say (b) is a model addition over the Green pattern. (b) "non-retraction emissions are always addressable … the mark-set grows monotonically" is presented as a bare "structural fact," but the note scopes RE-ADDR/RE-RET under ASN-0086's **standing unit-depth retraction-discipline** (the freshness argument needs unit-depth retraction to-sets + the link-domain prefix-antichain). Surface this assumption exactly as you already surface the conservative-lift caveat, so a builder with a non-unit-depth retraction knows the monotonicity must be re-derived. **[SHARPENING]**

3. **Design commitments / "What must be built" — state that the operation performs no subspace validation.** `W ⊆ s_C` is an *unchecked* caller obligation: the note says so explicitly ("not a check the operation performs"), and the evidence (Q3) confirms Green accepts arbitrary V-positions and returns *empty, not an error*, on link-subspace input. This is a real builder decision (don't add a guard) and pairs with the note's OQ7; "caller obligation" alone leaves it implicit. **[SHARPENING]**

4. **Implementation §5 — "invalidated … by retraction" is slightly too broad.** Conservatively safe, but a **self-retraction** (emitter retracts its own address; the emitter is born-nullified) is *inert* and leaves the answer fixed (note's "Under self-retraction"). Note the exception so a reactive view can skip recompute on a self-emit. **[SHARPENING]**

5. **Decisions for the builder / "Region presentation" — flag that the note's RE is single-document.** `RE(W,d,Σ)` resolves through one `d`; Green's multi-span/multi-document SpecSet is an *extension* beyond the note's model, not a free option under its contract. It's benign (a multi-document region decomposes into per-document queries unioned, consistent with RE-UDIST's spirit) — say that, rather than presenting multi-document as in-scope. **[SHARPENING]**

6. **Open-question coverage — OQ2 is the one omission.** The digest maps OQ1/OQ3/OQ4/OQ5/OQ6. It is *correct* to treat dedup as a firm commitment (RE-UNIT is not marked provisional, unlike RE-WHOLE), but a one-line note that *whether* value-twins must collapse is itself open (OQ2) would complete the map. **[SHARPENING]**

The revision list contains only `[SHARPENING]` items; the digest has no material problem.

VERDICT: CONVERGED
