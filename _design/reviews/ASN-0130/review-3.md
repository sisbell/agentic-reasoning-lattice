Overall this is a strong digest — accurate on the note's load-bearing distinctions (the (iii)/(iv) ever-vs-active split, permanence-splits-across-conjuncts, identity-by-start as two separate facts, expansion certifies-the-expansion-not-the-spelling), well-grounded in the Green evidence (the contiguous-run/event-loop story and the caller-supplied-state analogy are used precisely), and it lands several genuinely sharp implementation insights the note only implies (ST⁺ verdicts are *not* per-address cacheable; content-deterministic vs. global fresh-naming; "defer reference resolution into the evaluator and you silently re-open structural-reads-only"). The altitude is right — concrete approaches without dropping into signatures. I found one factual defect and a few sharpenings.

## Revision list

1. **[DEFECT] — "Implementation approaches → The active-pdef-by-start index": the reader enumeration is factually wrong (undercount).** The digest says *"Two distinct queries hit it"* and *"the active slice only at registration-endorsement."* Both are false. **Certification (PR5a) also reads this index**: check (0) "predicate sort" requires `sig(a)` *defined*, which presupposes ever-registration (the **audit** slice), and check (i) "target status" reads the **active** slice. The **universal stability lint** (PR5, `M_pdef` at view `active`) also reads the active slice to enumerate registered definitions. Fix: replace the "two queries / only / both" framing with the full reader set — *audit/ever slice* at registration-typing (iii), the evaluate precondition, **and certification (0)/`sig`**; *active slice* at registration-endorsement (iv), **certification target-status (i), and the universal lint**. The active/audit *partition* design is correct and survives; only the reader count is wrong, and a builder wiring lookups needs the certification and lint call-sites too.

2. **[SHARPENING] — "Design commitments" vs "Resolution and expansion": reconcile the α-variant point in one place.** The commitment marks "the *same concrete term* … not merely α-equivalent" as *forced*, while the implementation recommends a counter that produces "an α-variant … *not the note's exact term*." The digest does explain why this is harmless, but the surface tension can read as a contradiction. State once, at the commitment, that the forced property is **cross-evaluator agreement on one representative** (which any content-deterministic naming satisfies, and which alone licenses shared caching); the note's *particular* representative is a proof device, not a build obligation.

3. **[SHARPENING] — "Retraction" / "What must be built": note that de-registration is reversible.** The note (PR2, PR5a) commits that after a `Nullify_Binary` the dedup class is empty, so a later `register_pred` for the same run deposits afresh, and re-certification deposits a new `pd_stable` at a new address ("to certify it, re-register it first"). This falls out of the dedup+nullify semantics the digest already specifies, but it's a non-obvious operational consequence — a de-registered definition is *not* permanently dead — and is worth one explicit line so a builder doesn't treat re-deposit-after-nullify as a bug.

4. **[SHARPENING] — "Certification": preserve the three-way refusal distinction.** The note carefully separates *non-predicate* (check 0), *ill-posed/view-dependent* (check ii, "refused not as unstable but as ill-posed"), and *unknown/unproven-stable* (check iii, "unknown, not unstable"). The digest collapses toward only the last. For a certification API's diagnostics and remediation (respell to pin the view; respell a live-state test to a recorded-event test, per the note's v1-refused/v2-certified lesson), the three categories tell a user different things. Worth restoring as distinct refusal reasons.

The "Storing the artifact," "contiguous run + concurrency," "encoding," "signature-as-hint," "expansion-don't-materialize," "evaluation," "guarantees to uphold," and "decisions for the builder" sections are sound and committal — no action needed beyond the above.

VERDICT: REVISE
