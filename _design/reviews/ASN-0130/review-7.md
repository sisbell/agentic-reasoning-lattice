This is a strong, accurate digest. The commitments are faithful and the load-bearing distinctions are drawn sharply — identity-by-origin (S4) vs. well-definedness-of-identity-by-start (prefix-freeness/PR-ENC-uniq) are correctly kept apart; the permanence split (ii/iii content-intrinsic vs. iv deposit-time endorsement) and the ever- vs. active-registration roles of gates (iii)/(iv) are right; the contiguous-run/concurrency treatment is correctly grounded in the Q1/Q5 evidence (intra-call run, single-threaded loop, no allocator guard → per-(home,subspace) critical section); and the ST⁺ non-compositionality analysis ("no per-referent verdict cache," with the `helper(x)=count(L_W)≥x` / `host=helper(count(L_X))` counterexample) is a correct and valuable elaboration the note only implies. Grounding is clean and altitude holds. My findings are all non-material.

**Revision list**

1. **[SHARPENING] "How it fits": supersedes/tip is mis-sourced.** The link-ontology bullet credits ASN-0043 for "the supersedes lineage link," and the ASN-0128 bullet omits supersedes/tip entirely — but the note explicitly and repeatedly sources both to ASN-0128 ("the shipped S2 class, ASN-0128"; "the shipped semantics carry over without addition: `tip(a)`…"). supersedes *is* a link, so ASN-0043 governs its endsets, but the reusable shipped class with lineage/`tip`/branch→⊥ semantics is ASN-0128's. Move supersedes + `tip` to the ASN-0128 reuse bullet (as the versioning *approach* already does — "reuses the shipped `supersedes` class and `tip`"); leave ASN-0043 for endset denotation and `PrefixSpanCoverage`.

2. **[SHARPENING] Design commitments, "Expansion is a deterministic pure function": tighten the cross-evaluator claim.** "every evaluator … obtains the same concrete term … Any content-deterministic naming achieves it" is loose: agreement on the *literal* term requires all evaluators to share *one* naming rule (two evaluators on different content-deterministic rules agree only up to α). State the lock as: expansion must be a deterministic function of content (so one system's evaluators agree and caching is coherent), the note's exact names are not obligatory, and — as the implementation section already observes — since nothing downstream reads bound-variable names, only the α-class is load-bearing (an α-canonical/de Bruijn form makes the representative naming-independent). This aligns the commitment with the correct implementation discussion.

3. **[SHARPENING] Implementation approaches, "Certification": the diagnostic taxonomy is incomplete.** "refuses for three distinct reasons" enumerates (0) non-predicate, (ii) ill-posed, (iii) unknown — visibly skipping (i). PR5a has a fourth refusal, target-status (i): a de-registered-but-ever-registered target types and expands fine yet is refused because a certificate is a *new* endorsement (the note: "to certify it, re-register it first"). Add this fourth diagnostic, or scope "the three" explicitly as the *term-certifiability* reasons sitting atop the active-target precondition.

4. **[SHARPENING] Reconcile the DAG-recursion recommendation with the structural-reads-only guarantee.** The recommendation is sound (it substitutes argument *values*, keeping content reads at reference-node handling), but the warning that "a builder who defers reference resolution into the evaluator silently breaks it" lives in a separate section. Co-locate the reconciliation: DAG-recursive evaluation must keep resolution/expansion in the `evaluate` wrapper (the operation surface) and hand only pure sub-terms to ASN-0129's `⟦·⟧`; folding reference resolution into the denotation rules is the one prohibited move. This forecloses the only way the recommended approach could violate the boundary.

5. **[SHARPENING] Guarantees / "scope the universal stability lint": note the lint's own snapshot-relativity.** The note is explicit that the lint verdict is a multi-slice read over `A_pdef`/`A_pd_stable`, sound only about the single committed state its reads pin to, *retrospective*, and carrying no standing guarantee (a later registration or `pd_stable` retraction can falsify it). A builder exposing the lint as a diagnostic should not treat `lint=true` as a standing invariant. The general "single-coherent-pre-state reads" guarantee covers the mechanism; this just extends it to the lint, which the note calls out separately.

VERDICT: CONVERGED
