I checked the digest against ASN-0130, its claim statements, and the verified Green evidence. The digest is unusually strong — comprehensive on commitments, components, and builder decisions, and it proactively forbids two *unsound* approaches (a per-referent ST⁺ verdict cache; a history-dependent global rename counter). One internal contradiction keeps it from converging.

**Revision list**

1. **[DEFECT] — "Guarantees → Hold by construction → Acyclicity (DAG)": attributing acyclicity to "the endorsement check" is wrong and contradicts two other sections.** The design-commitments section correctly grounds the DAG in "references can only name *already-registered* definitions" (ever-registration), and "What must be built" says the endorsement gate (iv)'s "*sole job* is to refuse new references to de-registered definitions." Acyclicity does not rest on (iv): a deposit's referent is forced earlier in first-registration order by *ever*-registration (the typing gate (iii)'s audit-slice condition; active ⟹ ever, so (iv) is sufficient but not the ground). A builder who keeps (iii) and weakens or defers (iv) still has a DAG — the digest as written implies the reverse. Re-attribute, e.g.: "Acyclicity (DAG) — references can only name already-(ever-)registered definitions (the typing gate's ever-registration condition); registration events are totally ordered, so the reference relation is a strict order and no cycle detection exists or is needed." (Note PR2(a) cites (iv) as its proof *witness*, but the structural cause is ever-registration, exactly as the digest's own design-commitments line states.)

2. **[SHARPENING] — Implementation approaches: "the Rust target," the `im` crate, and the `links.jsonl`/`paths.json` working-substrate filenames are stated as fact but are grounded in neither the note nor the evidence.** The concurrency argument's real precondition is a *concurrent* reimplementation (Green's contiguity is load-bearing on its single-threaded loop — evidence Q1/Q5), not "Rust" per se; and "shares structure only between a value and versions derived from it, never interns two independently-built equal values" is true of any persistent structure without hash-consing, not just `im`. Either flag these as repo/project knowledge with a pointer, or restate language- and storage-neutrally so the (sound) advice doesn't hinge on an unverified environment assumption.

3. **[SHARPENING] — Make the `pdef`/`pd_stable` slot conventions and the Multi-shape rationale explicit.** PS1/PS2 are real commitments a class-implementer needs: `pdef` is **Multi** with `F = enc({a})` (start = identity) and `G = enc(A_def)` (the run) *because a single merged span would denote nothing — AD collects unit-depth spans only*, so both `addrs(F)={a}` and `addrs(G)=A_def` are recoverable by denotation; `pd_stable` is **Unary** (`G = ∅`), so dedup keys on slot-F coverage alone. The digest only gestures at this ("marks a content run," "Multi/Unary conformance"); state the slot semantics and the recoverability rationale.

4. **[SHARPENING, low] — "Guarantees → Validated ⇒ referenceable": name the precise condition.** The note makes the born-nullified edge vanish on *surface-disciplined derivations (DR, ASN-0128)* — an additional discipline (no standing range-retraction pre-covers the frontier slot) beyond the surface seal that gives registration-discipline. The digest's "under the surface seal + shipped retraction" is defensible for the shipped binary-only retraction but conflates the seal with DR; cite DR/surface-discipline for precision.

**Solid sections (no action):** the contiguous-run/concurrency analysis maps evidence Q1/Q5 precisely and draws the correct MIC-clause-5 critical-section obligation; the ST⁺-non-compositionality warning (`helper(x:ℕ)=count(L_W)≥x` ∈ ST⁺ but `host=helper(count(L_X))` ∉ ST⁺, forbidding a per-referent verdict cache) is correct, non-obvious, and the digest's best original contribution; the "structural-reads-only stays closed only because references resolve at the surface, not in the evaluator" insight and the evaluate/caller-supplied-state mapping (grounded in evidence Q2) are both right and load-bearing.

VERDICT: REVISE
