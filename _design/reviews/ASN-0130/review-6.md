Overall this digest is accurate, well-grounded, and at the right altitude. Its Green claims are all supported by the evidence (and it is honest where the Q5 evidence is internally split on link-allocation contiguity); it stays out of code while still committing to defaults; and several of its non-note contributions are genuinely sharp and correct — the diamond-blowup / renaming-deferred-sharing analysis (the distinct-names-on-diamonds reasoning is right, and naive hash-consing *would* violate the determinacy contract), the directional `k=0` ST⁺ oracle, the "scan the *expanded* term not the body" point (a body referencing a view-sensitive definition is view-clean until expanded), the seal-establishes-PR-DISC-but-not-DR distinction, and the unwired-scope-filter cautionary parallel. I checked the dense claims (coverage-exactness vs. point-lookup separation, the caching read/write split, the wp/born-nullified scoping) and found no misreads or fabrications. What remains are sharpenings only.

## Revision list

**1. [SHARPENING] Guarantees — add certification permanence parallel to validation permanence.** The Guarantees list states "Validation permanence (parse + typing)" but omits the parallel guarantee the note commits in PR5a ("a certificate is a permanent proof of certification, exactly as a `pdef` tuple is of validation"). Add it: a deposited certificate permanently proves deposit-time ST⁺ membership, view-independence, and Boolean sort — the certified object `expand(a)` never drifts, so the verdict cannot go stale and no re-certification-on-read path is needed — with the same divided-permanence caveat as PR1's (iv): the target's *active* registration is **not** permanent, so a de-registered-but-certified definition keeps its standing certificate (harmless, because the lint's `M_pdef`-at-view-active domain drops the de-registered definition). The validation/certification asymmetry in the current list is the only completeness gap I found.

**2. [SHARPENING] Guarantees (Frontier-ghost) — clean up the (iii)/(iv) division in the supporting rationale.** The bullet correctly (and more precisely than the note's own adversary check, which credits "(iv)") attributes frontier-ghost closure to ever-registration via `sig` undefinedness in (iii), and correctly says to test (iv) against the *active* slice. But the closing sentence — "residence is insufficient because it admits references to resident-but-de-registered (or resident-but-unvalidated) runs — a resident, parse-valid, unregistered run must still fail (iv)" — re-attributes the *never*-registered cases (resident-but-unvalidated, resident-but-parse-valid-unregistered) to (iv), where they actually fail **(iii)** first (no `sig` ⇒ no typing judgment, per the note's own (iii) text). This sits in mild tension with the bullet's own "(iv) adds only currency, not soundness." Tighten: (iii)/ever-registration rejects *all* never-registered referents (frontier-ghost and resident-but-unregistered/unvalidated alike); (iv)'s *unique* marginal contribution over (iii) is rejecting the *de-registered* (ever-registered-but-inactive) referent — that single case is what makes ever-registration insufficient and the *active* slice required. The load-bearing instruction (test against the active slice, not residence) is correct as written; only the case-attribution needs sharpening.

**3. [SHARPENING] Expansion approach — optionally surface the specific capture the renaming forecloses.** The expander guidance is strong on the diamond/determinacy hazard but states capture-avoidance only generically. The note's worked composition pins a non-obvious point worth one line: the capture being foreclosed is a *referent binder* capturing a *free variable of a substituted argument* (the `chkW` case), while a referent *parameter* merely sharing a name with a host variable is benign (the `gate` case) — which is why both parameters and binders are renamed in one freshness pass rather than binders alone.

Sections I'd flag as solid and leave alone: the entire "Design commitments" list (forced/conventional tags all defensible), "What must be built" (complete against the note's operations), the storing/encoding/registration/evaluation approaches, and "How it fits."

VERDICT: CONVERGED
