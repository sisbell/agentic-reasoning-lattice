The digest is strong and clearly written by someone who understood the note: the forced-vs-conventional calls are accurate, the Green evidence is used correctly (the contiguous-run analysis cleanly extracts the intra-call/event-loop dependency from Q1/Q5 and maps it to the W2 critical-section obligation — it does *not* claim the Rust target inherits Green's freebie), and the two-reference-gate distinction, the active/audit index, the structural-reads-only guarantee, and the certificate-vs-registration permanence asymmetry are all sharp. The expansion/caching section is genuinely excellent — the ST⁺ verdict-cache counterexample is correct and the "don't materialize the flat string" reasoning is sound. One material problem, in that same section.

**Revision list**

1. **[DEFECT] Implementation approaches → "Resolution and expansion," rec (a): "since denotation and ST⁺ are compositional" is false for ST⁺ and contradicts the digest's own counterexample.** ST⁺-classification is *not* compositional — as the digest itself shows two sentences later, `helper(x:ℕ)=count(L_W)≥x` is ST⁺ but `helper(count(L_X))` is not, so a reference node's verdict is *not* a function of the referent's standalone verdict ("ST⁺ is … argument-sensitive at reference nodes"). Using "compositional" to justify "recursion over the DAG rather than a flattened string" endorses, in isolation, exactly the verdict-caching the digest then forbids. Fix: split the justification. *Denotation* is compositional → DAG-recursive **evaluation** (value-substitution at reference nodes) is sound and avoids the flat string. *ST⁺* is **non**-compositional → driving its classification "over the DAG" is only *lazy* flattening (substitute each call site's actual arguments into the referent body, classify that node's flattened form); it saves the up-front materialization but not the classification work, and admits no per-referent verdict cache. The build recommendation survives intact; only the "compositional" framing is wrong.

2. **[SHARPENING] "Guarantees to uphold → Hold by construction": add expansion well-typedness (PR3a).** The list has "Determinism of expansion" and "Validation permanence for parse + typing" (the *body* well-types) but omits the note's proven guarantee that `expand(a)` is well-typed at the recorded result sort `C_D` — the fact that makes `evaluate` always well-defined (a denotation exists at `C_D`). State it, so a builder isn't tempted to add runtime sort checks at the evaluation boundary.

3. **[SHARPENING] Surface the note's open questions, OQ3 first.** The digest presents "de-registration does not cascade / eval keys on ever-registration" as settled — correct as the *committed* behavior, but the note explicitly flags **dangling live references** (OQ3) as a genuinely open policy fork (check transitively at evaluation, only at registration [status quo], or reject a `pdef` retraction while live referents exist). A builder should know blocking/surfacing was deliberately left open, so a diagnostic is sanctioned extension, not a spec violation. Note OQ1 (a shipped naming class), OQ2 (cross-substrate portability), and OQ4 (certificate classes beyond ST — SF, FP, standalone view-independence) as deferred extension points distinct from the must-pick-now decisions.

4. **[SHARPENING] "Resolution and expansion": pin the fresh-name counter to one monotonic counter per top-level `expand(a)`, spanning the whole traversal — not reset per recursive sub-call.** "per-expansion-local (reset each expand call)" can be misread (expand is recursive) as resetting at each referent sub-expansion, which would collide names and break capture-freeness. Say "one counter for the entire top-level expansion under the fixed traversal," since capture-freeness depends on it.

5. **[SHARPENING] "How it fits": name ASN-0128 as the distinct operational-semantics dependency.** The emit/nullify, active-vs-audit slices, idempotent dedup, gate-first / enforce-by-rejection contract, and the surface seal are ASN-0128's (Substrate Type Operational Semantics), not the typed-*relation* types of ASN-0086 they're currently folded under. Given that the entire register/certify contract lives in 0128, credit it by name.

VERDICT: REVISE
