I reviewed the digest against the note, the formal claims, and the verified evidence. This is a strong, unusually careful digest: the central conceptual move — de-bundling the *recorded* end from its *resolution*, and showing the reference's bundled path returns empty for orphaned/ghost links while the abstract operation returns the recorded non-empty end — is correct and well-argued, and it correctly distinguishes that divergence from the separate F7 empty-*recorded*-endset bug (Q17 vs Q15/Q19 are genuinely different code paths, and the digest reads them right). All of F0–F8 are covered, every Green-specific claim is grounded in the note or evidence, the altitude holds (no code/signatures for the new system; Green source citations are used as evidence, which is correct), and all five open questions are surfaced. It is also more careful than the note in one place — it declines to repeat the note's loose synthesis claim that "the reference satisfies F1–F6" (the reference's resolving output violates F1-exactness-to-recorded for orphaned links), surfacing that divergence instead.

There is one material problem.

# Revision list

1. **[DEFECT] — "Implementation approaches → Endset→span-set emitter," the Q13 sentence: internal contradiction + misattribution of the V-spec resolution path to F3.** The passage states "Q13, read whole, belongs at the representation level F3 leaves free," then calls the I-space pure-copy and the V-space tumbler-subtraction recompute "two legitimate decompositions of one coverage, which is F3's point" — and then immediately says "its normal (V-spec) path **resolves**." These contradict. The V-spec recompute is **resolution** (I→V through the queried document's arrangement), into a *different coordinate space*, and per Q15 it can **drop addresses** — so it is neither "denotationally-equal" to the recorded end in general nor "a decomposition of one coverage" in the note's tumbler-space sense, and it does **not** "belong at the representation level." F3 ranges only over span-sets *over T* that satisfy F1; the V-spec result is not one. The takeaway ("emit verbatim; don't reuse the reference path") survives, but the prose ships a self-contradictory, mis-attributed claim. **Fix:** split Q13 into its two cases cleanly from the start — the raw-I-span path is a genuine F3 example (same T-coverage, exponents preserved, verbatim copy, in scope); the V-spec path is **resolution** (out of scope, different coordinate space, can change the denoted set), and is the reason not to reuse the reference's normal followlink path. Attribute only the raw-I-span path to F3, and drop "two decompositions of one coverage" / "denotationally-equal" as applied to the V-spec recompute.

2. **[SHARPENING] — same section: ground "emit verbatim" in the proven extraction step.** The abstract FOLLOWLINK is precisely the reference's `link2sporglset` endset extraction **without** the downstream `linksporglset2specset` resolution; "emit the recorded end verbatim" means stop after extraction and skip I→V. Saying this maps the design directly onto a verified Green step and tightens the build instruction, and it dovetails with the fix in item 1 (the recorded end is what `link2sporglset` already produces, in T, before the V-spec path resolves it).

Everything else holds. In particular these are solid and need no change: the domain-guard ordering and its justification (upper-bound check is *forced* post-lookup once arity is unfrozen, lower bound is the only boundary-checkable part — correctly separating forced from optimization); the F7 "copy RETRIEVEENDSETS's always-succeed empty-handling, not the followlink path's" recommendation; the F2 "no separate check — merging across a gap is itself an F1 violation" reasoning plus the CREATELINK-faithfulness assumption it correctly externalizes; the disclosure analysis restricting home-document recovery to the `zeros(t) ≥ 2` document-bearing slice and keeping ghost-ness orthogonal (an F8, not a disclosure, matter); the monotone-map / lock-free-reads-need-atomic-publish point, correctly delegated to ASN-0093; and the forced-vs-conventional split. The verbatim-vs-normalize default (normalize at write time, keep reads cheap) is a defensible Lampson-style call.

VERDICT: REVISE
