I checked the digest against ASN-0098, its claim statements, and the verified Green evidence. This is a strong, careful digest — the permanent/live split, the displacement catalog, the coverage-density handling, the dense-vs-contiguous V-position analysis, and the coverage-keyed-reverse-index recommendation are all accurate, well-grounded, and at the right altitude. One material error, in the subspace-integrity reasoning.

## Revision list

**1. [DEFECT] "Guarantees to uphold → Subspace integrity": the link-canonical asymmetry is stated backwards — wrong subspace, wrong outcome, and it misidentifies what the note leaves open.** The digest writes "it has no proven link-side mirror … Do not assume emptying a document's *link* subspace orphans a link-canonical link the way emptying its content subspace orphans a content-canonical one; the note leaves this case open." Three things are wrong here:
   - **Wrong subspace.** The note's open question (Q7) is about emptying the **content** subspace while **retaining** link-subspace positions, for a link-canonical endset — *not* emptying the link subspace.
   - **Wrong outcome.** Q7 asks for a discoverability-**preservation** guarantee, because a link-canonical link's reach lives in the *retained* link subspace and therefore tends to **survive** — the opposite of "orphans."
   - **The "no proven mirror" claim is false.** The actual mirror of LP12b (empty the **link** subspace → orphan a link-canonical link) is provable by the symmetric argument: a link-canonical endset's coverage is disjoint from the *content* store (`coverage ∩ dom(C) = ∅`, via LP-Fin Corollary at `X = s_L` plus content addresses being `s_C`), so `project ⊆ V_{s_L}(d)`, and retaining only content makes `project ∩ R = ∅`. That case is a clean orphan, not a gap.

   Fix: pair LP12b with the note's real open question. State it as: *Proven (LP12b)* — emptying the **content** subspace (retaining link) orphans a **content-canonical** link from `d`. *Open (Q7)* — under the **same** contraction, a **link-canonical** link's reach sits in the retained link subspace so it tends to be **preserved**, but the precise preservation guarantee is open because link-canonical coverage can intersect `dom(L)` (other links), so LP12b's "coverage disjoint from the link store" step inverts and the case entangles with link-to-link discovery (open Q4). Drop "no proven link-side mirror"; if you mention the empty-link case at all, note it is the straightforward symmetric orphaning, not the open one.

**2. [SHARPENING] "What must be built → discoverability test": the type-slot discoverability claim should flag its construction dependency.** "L3 forces the type endset … non-empty — so a link is discoverable from any document that arranges its *type* endset's coverage" is correct in the formal model (L3 + well-formed positive-width spans ⇒ non-empty coverage). But it silently assumes non-empty *coverage*, and the Green evidence (Q4, link allocation) shows the creation path does **not** guarantee this — empty endsets are accepted silently and zero-width spans crash. Add one clause: this property holds only if non-empty, positive-coverage type endsets are enforced at construction; the substrate (per Green) does not give it for free.

**3. [SHARPENING] "How it fits": "transclusion and version-comparison … all expressed in terms of `project` and `discoverable_from`" is loose.** Link-following/endset-retrieval/link-finding are genuinely expressed via `project`/`discoverable_from`; transclusion and version-comparison rest on the same V↔I projection *machinery* (and, for transclusion, the displacement laws LP9/LP16) but not on the endset-taking `project` or on `discoverable_from` per se — version-comparison has no endset and no link. Split the sentence so the dependency isn't overstated for the last two.

**4. [SHARPENING] "Implementation approaches → Discoverability + reverse index": tighten the V-position stabbing-query phrasing.** "stabbing queries (given an I-address or *V-position*, which links' *coverage covers* it)" conflates two things: the index is keyed on I-addresses, and a V-position query must first map V→I through the document's arrangement, after which the answer is "which links' *projection contains* it" (the note's open-Q1 framing), not "whose coverage covers it." State the V→I step explicitly.

The remaining sections — design commitments (every "Forced"/"Conventional" tag checks out), the displacement catalog, the projection forward-filter vs inverse-index pair with its LP-Fin cost bound, the coverage normalization-as-hint discipline, the insertion/V-position-identity decision tree, and the reverse-index keying recommendation — are accurate and need no change.

VERDICT: REVISE
