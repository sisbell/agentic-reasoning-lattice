I reviewed the digest against the note, its formal claims, and the verified Green evidence, focusing on the load-bearing claims (the displacement laws, the subspace/LP12b reasoning, the dense-vs-contiguous V-position tradeoff, and every Green-sourced claim).

This is an unusually strong digest. The design-commitments section is accurate and correctly labels forced vs. conventional throughout (tightness and the subspace partition are correctly flagged as convention/builder-owned). Every Green claim — `permute` as a single dimension-parametric routine, POOM-as-multimap, insert/delete shift, the write-only spanfilade returning a stale superset, the home-doc filter no-op, emergent tightness, `acceptablevsa` stub, empty-endset/zero-width behavior — is grounded in the evidence answers. The deep reasoning all checks out: LP12a's `project' = project ∩ R` genuinely survives for any retained `R` (only the prefix structure is D-SEQ-specific); LP9–LP11/LP20/LP21 genuinely use only E1/E2, subset+agreement, the bijection, and coverage, so the D-CTG★ relaxation is free *for those laws*; and the LP12b/Q7 disentangling — including the digest's own *mirror* argument for the clean (non-open) empty-link-retain-content orphan case — is sound. The LP20 image-vs-projection distinction and the coverage-keyed-and-filter-live reverse index are both correct and well-motivated.

I found no defects — no misread guarantee, no ungrounded Green claim, no altitude slip, no internal contradiction, no missing load-bearing commitment. Only sharpenings.

**Revision list**

1. **[SHARPENING] "Implementation approaches → Arrangement + edits" and "Guarantees to uphold": "green's INSERT/DELETE shifts all downstream V-positions" overstates scope.** Per evidence Q2 the two-blade knife (insert) and the `strongsub` exponent guard (delete) bound the shift to the *affected subspace* — cross-subspace entries are not shifted. Qualify both to "all downstream V-positions *within the insertion/deletion subspace*." The decompose-into-extend/contract-plus-reorder conclusion is unaffected (within-subspace shift still violates E2), so this is prose precision only; the digest even cites the two-blade knife, so the bounding is already half-acknowledged.

2. **[SHARPENING] "What must be built → discoverability test": locate the type-coverage enforcement gap more precisely.** The point that a non-empty type *span set* needs *positive-width* spans to yield non-empty *coverage* is true in general, but the note's own span well-formedness (`Pos(ℓ)`, stated in the coverage definition) already forces positive coverage *within the spec* — so in-model a non-empty type endset is automatically discoverable-conferring. Sharpen "the substrate does not give it for free" to: the spec gives it via well-formedness, but lax substrates (Green accepts empty endsets and crashes on zero-width) don't enforce well-formedness, so you re-impose it at construction. This keeps the (correct, valuable) builder advice while not implying the note's model has the gap.

3. **[SHARPENING] "How it fits" / open-question coverage: acknowledge the two open questions the digest passes over.** The digest substantively engages five of the note's seven open questions (return-shape contiguity, V-order, reverse-discovery primitive, link-to-link discovery, Q7 link-canonical contraction) but is silent on Q5 (when must two documents under "the same" edit sequence yield identical projections) and Q6 (fork composite where the source's link-subspace V-positions are not transcluded). Neither translates to an immediate build decision, but a one-line "left open by the note" for each prevents a builder from assuming they were resolved.

VERDICT: CONVERGED
