The digest is strong: accurate readings of the note, sound approaches, and careful Green grounding. A few sections are genuinely excellent and I won't second-guess them — the **forced/conventional split** is right (tightness and the subspace convention correctly marked builder-owned; the projection/displacement laws correctly forced); the **LP12b/Q7 subspace analysis**, including the four-quadrant content/link-canonical × empty-content/empty-link case split and the "mirror" clean-orphan derivation, is correct and extends the note soundly; the **dense-V-positions vs extend+reorder tradeoff** (which laws survive a D-CTG★ relaxation, which must be re-derived) is precise; and the **coverage-keyed reverse-index** reasoning is exactly the Lampson hint discipline. No ungrounded source-level Green claims — `permute`, POOM-as-multimap, spanfilade-as-stale-superset, the `acceptablevsa` stub, emergent tightness, and the home-doc-filter no-op are all backed by the evidence.

# Revision list

1. **[DEFECT]** — *Guarantees to uphold → "Monotone/rigid displacement (LP9/LP10/LP11) — from not implementing any primitive that adds *and* removes in one step (decompose insert-with-shift)."* This characterization is false and self-contradictory. Insert-with-shift adds new entries and **moves (rebinds)** the existing downstream ones — it removes nothing; the domain only grows. The digest's own *Arrangement + edits* section says this correctly ("adds *and* moves entries in one primitive — neither a clean LP9 grow nor a clean LP11 permute"). A builder reading only this summary line could conclude that a grow+reorder primitive (which doesn't remove) is safe, and ship green-style insert-with-shift — defeating the clean displacement laws. Fix: state the pattern to actually forbid — any primitive that combines a *domain change* (grow/LP9 or shrink/LP10) with a *rebinding of retained positions*. The two real instances are insert-with-shift (grow + rebind) and green's delete-with-shift (shrink + rebind — green's DELETE decrements downstream V-positions, per evidence Q2). Decompose each into the note's single-class operations.

2. **[SHARPENING]** — *Implementation approaches → Arrangement + edits* treats insertion's composite/shift problem thoroughly but is silent on the symmetric deletion case. The note's K.μ⁻ retains a D-SEQ **prefix** (suffix-truncation), so a mid-document deletion under contiguous V-positions is not a single K.μ⁻ — it needs reorder-then-truncate, exactly mirroring insertion's extend+reorder (and green's DELETE takes the shift shortcut). Add the symmetric paragraph so the builder sees both edit directions carry the same decomposition obligation; this also reinforces the fix in item 1.

3. **[SHARPENING]** — *Implementation approaches → "Extend-at-frontier + reorder … treat insertion as the note does (a composite of allocate + arrange)."* The note's stated "allocate + arrange" composite (K.α + K.μ⁺) is the **frontier-append** case; the reorder (K.μ~) required to land mid-document is the digest's own addition, not something the note describes as part of that composite. Tighten: allocate+arrange is the note's composite for frontier insertion; placing it mid-stream under contiguous V-positions requires the additional K.μ~, which is what keeps LP9 and LP11 each exact.

4. **[SHARPENING]** — *Implementation approaches → Projection:* "any canonical span's reach contains at most `n` substrate-emittable addresses (LP-Fin Corollary)." The cited Corollary gives **exactly** `n` (`F ∩ [s, s⊕ℓ) = {[d_0,0,X,k] : k_s ≤ k < k_s+n}`). "At most n" is true but weaker than the result being cited; say "exactly n" — the downstream "at most n *allocated*" is then correctly the weaker statement (allocated ⊆ emittable).

VERDICT: REVISE
