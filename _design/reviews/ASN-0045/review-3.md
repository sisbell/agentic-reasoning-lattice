This is a strong digest — it captures the note's hardest content faithfully (the at‑least‑one/at‑most‑one structure, why T4c does no work in the partition, the sum‑type "lemmas by construction" insight) and keeps the level/containment/ownership layers clean using the evidence. The improvements below are real but mostly localized.

## Revision list

**1. "Decisions for the builder" → empty tumbler: the digest claims the note resolves this; it does not. Reframe as an upstream gap, not a settled answer.**
The bullet asserts "the note already fixes one [answer]" and that the empty tumbler's "classification is already determined at the T4/T0 layer — the builder adopts that resolution." This is inaccurate. The note never mentions the empty sequence, and its actual claim — "T4‑valid is well‑formed for any `t : T`" — guarantees the proposition is *meaningful*, not that its *truth‑value* on `[]` is *decided*. With `#t = 0`, the boundary clauses `t₁ ≠ 0` and `t_{#t} ≠ 0` index non‑existent elements, so T4‑valid(`[]`) is genuinely under‑specified (vacuously‑true → Node, or undefined/false → Invalid, depending on an out‑of‑range‑indexing convention the note never states). Keep the correct half — the classifier must *not* invent its own convention — but replace "the note already fixes one / already determined" with: this is an upstream gap to escalate to T0/T4 (does T0 admit `[]`? how does `t₁ ≠ 0` evaluate when `t₁` doesn't exist?), which the builder should *surface*, not assume resolved. Conflating "well‑formed" with "decided" risks telling the builder a hole is closed when it's open.

**2. "Implementation approaches" → On Green's encoding: the stated hazard mechanism is over‑specified and partly contradicted by the evidence's own literals. Re‑ground it.**
"The trailing double-zero reads as a forbidden adjacent-zero pair" doesn't match the evidence. The `defaultaccount` literals are `{0,0,0,0, 1,1,0,1, 0,0,0,0}` (be.c) and `{0,0,0,0, 1,1,0,14, 0}` (socketbe): both carry a **four‑zero leading header** and *variable* trailing zeros (four vs. one), so "a double‑zero terminator" is not uniform, and a raw scan trips the **leading‑zero** clause (and blows the `≤3` bound) long before any trailing "adjacent‑zero" reads — the firing clause is mis‑attributed. The evidence's "terminates with two zeros" describes `tumbleraccounteq`'s account/document **prefix‑comparison boundary**, not a universal physical array end‑marker; the digest has reified a comparison‑semantics detail into a per‑array terminator. Keep the correct, valuable conclusion (Green's *logical* `1.1.0.1` **is** T4‑valid; decode the raw array before running the scan; borrow the structure, not the byte layout), but re‑ground the hazard in the actual literal shape — a leading structural header plus trailing padding zeros — rather than a clean "double‑zero → adjacent‑zero" rejection.

**3. "Implementation approaches": two ecosystem/repo‑specific touches presume a stack the note doesn't fix and add nothing the point needs.**
(a) "Don't reach for a persistent‑structure library (`im` or similar)" names the Rust `im` crate, but this substrate is Python — either drop the library name or cite the Python analog (`pyrsistent`) so the advice isn't tied to a foreign ecosystem. The design point ("an immutable flat sequence suffices; structural sharing buys the address value nothing") is language‑agnostic and stands without naming a library. (b) "as this repo's appears to be, an append‑only journal and a `paths.json`‑style registry" is a hedged guess about the host system; since the recommendation explicitly doesn't depend on it, cut the repo‑specific detail or demote it to a plain example. Both are minor altitude/grounding slips, not load‑bearing.

**4. (Sharpening) Sum‑type bullet / "How it fits": "the name↔level correspondence … used only for reporting" flattens two things the note keeps distinct.**
The note separates the level *names* (nomenclature, genuinely reporting‑only) from the per‑predicate *level‑correspondence postconditions* (`Account(t) ⟺ t is a user address per T4c` on the valid subdomain). The latter are semantic‑equivalence theorems, not cosmetics: they are the *warrant* that the label `Account` is faithful to T4c's address class. A downstream consumer trusting "`Account(t)` denotes a T4c account address" relies on that derived equivalence, not on the bare `zeros=1` definition. Say the correspondence is *separable from the classification core* (correct, and the real engineering win) — but not "only reporting"; it is the guarantee binding the name to the T4c level.

**5. (No change — solid.)** The sum‑type "Partition/Off‑Domain Vacuity by construction" argument; the faithful mapping of at‑least‑one (membership from the `≤3` bound, *never* T4c's bijection domain — else circular) vs. at‑most‑one (single‑valued count + distinct numerals, no T4c injectivity); the "scan not parse, cheap because of the count‑bound not the length" insight; and the one‑place `Account(t)` vs. two‑place `tumbleraccounteq` vs. ownership `isthisusersdocument` layering are all accurate, grounded, and load‑bearing. Leave them as written.
