This digest is unusually faithful to the note's hardest content. The parts a skeptic would expect to be botched are correct: the **name‑free core vs. separable T4c‑correspondence** distinction, the two different circularity/necessity arguments (mutual‑exclusion needs no injectivity → "unnecessary, not circular"; membership must come from the bound, not the bijection → "circular"), the **sum‑type makes Partition/Off‑Domain‑Vacuity true by construction** lift, the depth‑ceiling‑must‑be‑checked point, the immutability‑contingent level‑stability caveat, and the Green grounding (account canonical; `user` = session‑slot index + ownership predicate; containment ≠ ownership ≠ level). The empty‑tumbler edge is handled with the right discipline — escalate to T0/T4, don't silently resolve. The bound‑is‑on‑zero‑count‑not‑length / "Node is any zero‑free sequence, hence unbounded" observation is sharp and correct. I could not find a material misread, ungrounded Green claim, altitude slip, contradiction, or missing load‑bearing commitment.

**Revision list**

1. **[SHARPENING] Implementation approaches → "On Green's encoding": the clause attribution is wrong.** "The firing clause is leading‑zero, not adjacent‑zero" is inaccurate — the leading `0,0,0,0` run of `{0,0,0,0,1,1,0,1,0,0,0,0}` is *itself* an adjacent‑zero violation, and it also blows `zeros ≤ 3`, so **leading‑zero, adjacent‑zero, and the bound all fire on the front of the array**. Restate as: "the *front* trips it — leading‑zero, adjacent‑zero, and the `zeros ≤ 3` bound all fail there — long before the trailing padding is reached; so don't diagnose a raw Green array as a *trailing‑padding* problem." The actionable content (decode the encoding, then scan) is right and unaffected; only the "not adjacent‑zero" denial needs fixing.

2. **[SHARPENING] Implementation approaches → "let the verified spec be the implementation": genericize the toolchain reference.** Neither the note nor the evidence establishes an "oracle/Dafny toolchain." Phrase it as "*if a verification/extraction toolchain is available*" so the digest isn't asserting project infrastructure the sources don't confirm. The underlying recommendation — the definitions are small, total, and executable, so extract the classifier with Partition/Off‑Domain‑Vacuity machine‑checked rather than hand‑maintained — is sound and worth keeping.

3. **[SHARPENING] Decisions → "Naming": carry "U stays U" into the codebase decision.** The design‑commitments section already records that the projection symbol `U` is deliberately unrenamed, but the naming decision ("commit to *account* everywhere") doesn't repeat it. Add one clause so a builder committing to *account* for the level doesn't also rebind the user‑component projection `U`/`t.Uᵢ`, which the note holds fixed. Pure consolidation of something already stated — non‑load‑bearing.

VERDICT: CONVERGED
