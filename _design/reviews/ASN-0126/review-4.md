I checked the digest's technical core against the note and claims, and its Green-fidelity claims against the evidence. The substrate-mechanics content is accurate and well-grounded — the two-stage gate (construction-time C0 vs runtime emit), coverage-class keying with the `coverage([k])=coverage([k']) ⟺ k=k'` reduction and its fragility warnings, effect-identity / the π bridge / layer-scoped non-transfer, the C2-vs-C3 born-nullified split and the hint-discipline consequences, FrontierUnification, the two-gap single-tuple-scope analysis, range sterilization (finite-block vs up-set, with P-tgt load-bearing for *both* scope and sterilization), and the four R-configurations are all correct and don't need rework. The defects are confined to two Green-fidelity prose claims.

**Revision list**

1. **[DEFECT]** *Implementation approaches → "Faithful to Green, at the grain":* "Green likewise has no delete opcode" is false and self-contradictory — Green has `DELETEVSPAN` (opcode 12), which the *same sentence* then relies on ("a single range delete can withdraw a contiguous run of links"). Both the evidence (`DELETEVSPAN` exists and range-deletes multiple link POOM entries in one call) and the note ("an arrangement-level delete excises only the document's V-stream entries, leaving the link object and its discovery index intact") deliberately preserve the distinction the digest collapses. Fix: say Green has no *link-object* delete opcode — the delete that exists (`DELETEVSPAN`) excises V-stream/POOM mappings over a range while the link orgl and spanfilade stay permanent. That distinction *is* Green's link permanence, which the digest invokes repeatedly (e.g. "follow_link / find_links still succeed"), so erasing it is material, not pedantic.

2. **[DEFECT]** *Decisions for the builder → "Gate-failure signaling":* the claim that Green's "silently stores the malformed link, returning success" is "the analog of this framework's unregistered-type / empty-registry configs (a would-be emit with no image at all)" inverts the storage outcome. Green *stores* the malformed link; those configs are "permanently link-inert" and store *nothing* — which is the digest's own (correct) characterization in the Σ_init bullet, so this clause is internally inconsistent and could mislead a reader into thinking the empty registry behaves like Green. Green has no analog among the framework's outcomes: it corresponds to *no admission control* (accept + store), which neither reporting choice reproduces — error and silent no-op both *decline to store*, and that is exactly the un-Green-like part; the empty-registry config and a fired-gate rejection are *equally* un-Green-like (both store nothing). Keep the correct lead ("a silent no-op does not reproduce Green"); delete/rewrite the "analog of empty-registry … not of a silent rejection" clause.

3. **[SHARPENING]** *Design commitments, registry-immutability bullet:* "concurrency-free" and the parenthetical "the *emit* it guards is not concurrency-free" are builder-added engineering consequences — the note is a sequential model and commits nothing about concurrency. The *Address allocation* section flags this; the keystone bullet (in a section headed "forced by the claims") does not. Mark the concurrency framing consistently as a consequence of immutability rather than a note-forced commitment.

VERDICT: REVISE
