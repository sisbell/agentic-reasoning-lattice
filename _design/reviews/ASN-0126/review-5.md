I checked the digest section by section against the note, the claim statements, and the evidence answers, with particular attention to the retraction/sterilization machinery and every Green source-level claim.

This is a strong digest. It is accurate against the note, scrupulously grounded (every Green claim — `putvspaninlist` non-coalescing, `findnextlinkvsa` frontier allocation, `tumbleradd` monotonicity for valid addresses, `DELETEVSPAN` range deletion, silent empty-from `CREATELINK`, the permanent-orgl / write-only-spanfilade structure — traces to the evidence answers), committal where the note supports a position, and at design altitude throughout. The hard parts are right: it avoids the value-based-identity trap (restricting the canonical-key index to the unit-depth-singleton case where tumbler-equality *coincides* with coverage-equality, with both failure modes spelled out), keeps the static shape gate and the dynamic nullification filter apart, and nails the subtle two-causes (C2/C3) born-nullified point and the two-independent-gaps decomposition of single-tuple scope. I found **no defects**. The items below are refinements only.

**Revision list (all sharpenings):**

1. **[SHARPENING]** *Decisions for the builder → Σ_init authoring interface / R's registered shape.* The digest frames the registry as entirely app-declared ("apps populate at construction," "you choose how an app declares its entries") and never surfaces the note's **Open Q4** — whether the substrate ships any *pre-registered/standard* types or `Σ_init.registry` is composed wholly of app entries. Add it as a builder-visible decision; it connects directly to the digest's own foot-gun warning, since a substrate that shipped R pre-registered Binary would foreclose *both* the unregistered-R (retraction-free) and Unary-R (silently inert) foot-guns the digest flags. Note the framework accommodates either answer (construction writes the registry regardless of author).

2. **[SHARPENING]** *Sterilization containment.* The parenthetical equating gap 2 ("a P-tgt-invalid target") with "any to-span at an interior node above the chain" over-identifies the two. The *up-set* footprint comes specifically from a **prefix-cone** to-coverage spanning the chain tail (a target at/above an interior node). A P-tgt-invalid target that is an unfilled *leaf* chain slot has coverage `{t : chain_d(j) ≼ t}`, which catches only `chain_d(j)` among chain slots (its siblings `chain_d(j+1)…` are not under it) — a singleton block, not an up-set. The load-bearing claim (P-tgt-validity averts the up-set; closing gap 1 alone does not) is correct and survives; tighten the gloss so a builder doesn't read "any P-tgt-invalid target ⟹ total sterilization."

3. **[SHARPENING]** *Sterilization containment (Open Q7).* The digest asserts sterilization is "irreversible" without saying *why*, leaving a tempting futile cure open. Add the mechanism: `nullified` reads the audit slice `L_R`, never the active subset `A_R`, so the offending retractor's to-coverage stays live as long as the retractor persists in `L_R` — and it persists permanently (L12). **Even nullifying the retractor itself lifts nothing.** This forecloses an "admin un-poison by retracting the retractor" design and strengthens the prevention-over-cure framing.

4. **[SHARPENING]** *Two outcomes, two mechanisms.* "earns a tuple a place in the permanent audit slice `L_K` (`dom(Σ.L)`)" conflates the type-K slice with the full address domain — `L_K` is the K-typed tuples, `dom(Σ.L)` is *all* link addresses; they are neither the same object nor the same extent. Reword to "the permanent link store (its K-slice `L_K`; addresses entering `dom(Σ.L)`)" or drop the parenthetical.

5. **[SHARPENING]** *Design commitments → registry immutable.* "it makes the gate decidable and **stateless**" — the gate does read `shape(K)` from `Σ.registry`, which is (immutable) state, as the very next clause ("runs against a finite, fixed table") makes plain. Replace "stateless" with "its verdict state-independent (P4)" so the phrasing doesn't imply the gate needs no state access.

VERDICT: CONVERGED
