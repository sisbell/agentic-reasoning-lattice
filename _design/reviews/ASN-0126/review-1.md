The digest is accurate on the core framework — the gate, effect‑identity / projection bridge, the no‑coalesce span‑count discipline, the write‑once registry — and its Green grounding is clean: every source‑level claim (`putvspaninlist`, `findnextlinkvsa`, write‑only spanfilade, `tumbleradd`'s `s ⊕ ℓ > s` for valid addresses) traces to the evidence answers, none fabricated. The improvements below concentrate on the retraction story, where the note is most intricate and the digest most compressed.

**1. "Retraction wrapper" / "Sterilization containment" / Guarantees table — single‑tuple‑scope is flattened and the `Nullify_Binary` contract is dropped.** The note is emphatic that single‑tuple‑scope survives only if the app closes *two independent* gaps: gap 1, the non‑unit G‑span (closed by the unit‑depth wrapper); and gap 2, a P‑tgt‑valid *target* — `a ∈ A_rel^Σ` (a resident link) or `a = a_emit` (the fresh self‑emit) — which "the gate cannot see at all" and the wrapper does *not* enforce. The digest mentions "aim at a P‑tgt‑valid target" once, in passing, and the guarantees‑table row pairs "single‑tuple scope" with "(or optional containment)," implying the proposed substrate check (reject a to‑coverage reaching the frontier) delivers it. It does not: single‑tuple‑scope is the *target‑side* predicate P‑tgt, whereas containment tests the *to‑coverage‑vs‑frontier*; containment addresses gap 1's frontier‑poisoning cost (OQ7), not gap 2. Do: (a) state the two independent gaps; (b) make explicit the wrapper closes only gap 1, leaving target‑validity a second, gate‑invisible app obligation; (c) carry the `Nullify_Binary` conditional postconditions — coverage‑nullification is *unconditional* (every resident link under the target dies, even a ghost target's whole subspace, which is exactly why a mis‑aimed wrapper is *harmful*, not inert), target‑nullified‑iff‑resident, single‑tuple‑scope‑iff‑P‑tgt, persistence; (d) decouple "single‑tuple scope" from "optional containment" in the table.

**2. "Nullification / active‑subset index" — the maintenance recipe misses born‑nullified.** "Maintain a nullified‑address index incrementally as retractions are emitted (each retraction's to‑coverage marks addresses dead)" describes only the retraction→address direction and would mark a later deposit *active* when it should be born nullified. By RangeSterilization a retraction's to‑span poisons *future, not‑yet‑resident* frontier slots; those cannot be pre‑materialized as dead addresses, and a regular emit landing in a pre‑existing retraction's coverage (wp conjunct C3) is dead‑on‑arrival. Do: characterize the authoritative structure as the set of retraction *to‑coverages*, queried by address at *both* emit time (born‑nullified / C3) and query time (active filtering); or, if a materialized dead‑address set is kept, state it must also update on *every* regular emit, not just on retraction emit. The "Two outcomes" paragraph already names C3 — tie the index design to it.

**3. "Design commitments" — the arity‑exactly‑3 narrowing is missing.** Precondition (0) restricts the emit to arity *exactly* 3, so every `N > 3` tuple ASN‑0086 admitted (its `K.λ` allows `N ≥ 3`) now has *no* `→_sh` image. This is a forced commitment, and together with the `|F| = 1` single‑source restriction it is precisely the pair Open Question 6 ("Extension beyond F=1 and N=3") reopens. The digest lists "arity‑3" only as a gate step, never as a narrowing commitment with an excluded class. Add it to the commitments list and connect both exclusions (multi‑span source, richer arity) to OQ6 in "How it fits."

**4. "Design commitments" / "The conformance check" — surface "shapes classify registrations, not tuples."** The note states this twice; it is the guard against a real builder error. You cannot read a tuple's shape off its `(F,G)` span counts: a `(|F|=1, |G|=0)` tuple conforms under Unary *and* Multi, and the worked example exhibits a Multi `(F,∅,K)` shape‑indistinguishable from a Unary one. The digest's check is functionally correct (it consults `shape(K)`) but never states the principle, which is exactly what tells the builder the classifier must look up the registration, not inspect the tuple.

**5. "Retraction wrapper" / "Decisions for the builder" — the four [R] configurations are not surfaced.** The digest reasons only under the standing Binary configuration. The note's configuration sweep is a real composition decision with a foot‑gun: *unregistered* R ⇒ permanently retraction‑free; *Unary* R ⇒ retraction silently inert (forces `G=∅`, withdraws nothing, and the wrapper itself has no `→_sh` image); *Binary/Multi* ⇒ range withdrawal. Add a line: R's registered shape *is* the retraction semantics, and Unary‑R is a silent no‑op worth warning against.

**6. "Two outcomes" / "Sterilization containment" — say *why* the hazard is new (C3 newly live).** The note's load‑bearing point is that C3 was *vacuous* under ASN‑0086's unit‑depth discipline (unit‑depth to‑spans + FlatLinkDomain forced `a_emit ∉ coverage(G')`) and is activated *only* because `→_sh` admits non‑unit retraction to‑spans; C2 (self‑nullification) is inherited and was already live. The digest treats C2 and C3 as co‑equal "dynamic decisions." Flag C3 as the conjunct the framework newly turns on — it is the causal root of range sterilization.

**7. Minor precision.** (a) "What must be built" lists `a_emit` without noting it is *inherited* from ASN‑0086 (EmitAddress); the framework's contribution is FrontierUnification's characterization, not the allocator — flag it inherited as the nullification derivation already is. (b) The realizability direction (P5: every conforming emit actually fires — the gate is not over‑restrictive) is implied but never stated next to P3's soundness; one line completes the "exactly the conforming tuples" picture. (c) "Shape lookup is therefore a constant against a finite, fixed table" reads as O(1); the decision is O(registry‑size) coverage‑equality, as the digest's own linear‑scan option concedes — reword to "a fixed table." (d) "Faithful to Green" for retraction holds only at the *grain* (region‑scale withdrawal); Green's POOM excision leaves the link discoverable (evidence Q1: `follow_link`/`find_links` still succeed), whereas nullification drops a tuple from the active subset — a half‑sentence keeps the fidelity claim from being over‑read.

Solid and needing no change: the effect‑identity / "don't normalize inside the gate" argument (and its link to the no‑coalesce evidence), the explicit endset‑representation discipline, the hint‑vs‑authoritative‑state framing for the nullified set and frontier counter, and the registry‑storage recommendation (the `im`‑crate caution is language‑specific but the reasoning from write‑once immutability is sound and at altitude).
