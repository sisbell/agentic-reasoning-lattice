Reviewed against the note, its formal claims, and the verified Green evidence. The digest is unusually careful — it correctly avoids the two traps this note sets (it does *not* propose the containment index as an origin mechanism, and it does *not* let the single-origin fast path skip the non-emptiness conjunct). Grounding in the Green evidence is thorough and, with one exception, correct. That exception is the only material problem.

# Revision list

**1. [DEFECT] — *Implementation approaches* (Pointwise origin) and *Permanence/recovery*: the content-address shape `…doc.0.3.N` is wrong.** It appears twice ("Green's granfilade allocates them document-scoped (`…doc.0.3.N`, the same addresses the projector truncates)" and again under Permanence). The note's own worked example places content at `[d₁.0.1.1]…[d₁.0.1.5]`, and Q14's code exploration confirms text allocates at `docisa.0.1.x` (`TEXTATOM = 1`, upperbound `docisa.0.2`); the content subspace identifier is **1**, link is **2**. The "3" is lifted from the Q12 KB-*synthesis* line "element field 3 = text," which that same Q12 *code* exploration and the note both contradict — the digest trusted the wrong layer of the evidence over the primary source. This is not merely cosmetic: a builder reading "content lives at `doc.0.3.N`" could encode the wrong subspace number into the very dom(C)-vs-dom(L) filter the digest (correctly) insists on for the boundary-walk. Fix both occurrences to `…doc.0.1.N`, or state it abstractly as "the content subspace `s_C`" with the note's literal (`doc.0.1.k`) so the reviser cannot reintroduce a contradicting number.

**2. [SHARPENING] — *Guarantees to uphold* (Span-containment monotonicity): the Nelson gloss runs the reduction backwards.** "Name a chapter's home and you have named a character's home" inverts the actual direction — containment monotonicity gives `origins(character-span) ⊆ origins(chapter-span)`, and the note's reduction runs character → paragraph (paragraph origins are the *union* of character origins), not chapter → character. Naming a (possibly multi-origin) chapter's set does not isolate a given character's home. It also conflates O8/O12 (set-inclusion across nested spans) with O3 (the size-*independent mechanism* — the deeper sense in which the note is "scale-invariant: no procedural case distinction on size"). The formal statement printed alongside is correct, so this is non-load-bearing; tighten the gloss to the subset relation and keep the "all three I-span paths must jointly satisfy it" point, which is a genuine builder insight.

**3. [SHARPENING] — *What must be built* (V-span admissibility check): the conjunct enumeration reads as exhaustive but isn't.** The bullet names WF_V conjuncts (ii),(iii),(v),(vi) and says "verify the full WF_V predicate," but omits (i) `d ∈ Σ.E_doc` and (iv) T12-on-`(u,ℓ)`. Since (iv) is the same span well-formedness already surfaced for the I-span resolver, add a half-clause naming (i) and (iv) so the four-item list doesn't read as the complete check.

**Solid, leave alone:** the I-span approach trichotomy (enumerate / granfilade boundary-walk with the dom(C) filter / allocation-keyed index-as-hint), the single-origin fast-path gating on an existence check (correctly derived from the wp's separate non-emptiness conjunct and from the Q14 fragmentation evidence), and the per-block-`homedoc`-cache-vs-result-set-cache distinction (per-block invalidation-free by O5/permanence; result-set dropped on reorder/contraction per O13/O14, preserved across extension per O11). These are accurate, well-grounded, and at the right altitude — do not churn them.

VERDICT: REVISE
