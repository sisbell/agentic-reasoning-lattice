Overall this is an unusually strong digest: the central negative (origin needs no stored state) is correctly identified as load-bearing, the forced/conventional split is accurate, the Green evidence (Q2/Q11–Q19) is used precisely and never fabricated, and every proposed implementation approach respects the note's commitments (no content-dedup, indices kept reconcilable, I-span filtered to `dom(C)`, V-span answered against the live arrangement). The edit-contract section (extension = equality, contraction = inadmissibility, reorder = incomparable) and the cache-invalidation reasoning (per-block `homedoc` invalidation-free vs. result-set caches unsafe) are genuinely sharp. I found no material inaccuracy, ungrounded claim, altitude slip, contradiction, or missing load-bearing commitment. The items below are all refinements.

**Revision list**

1. **`[SHARPENING]` "How it fits → Beside it": "the spanfilade as the wrong tool for origin" is too flat and sits in tension with both the note and your own *Implementation approaches* section.** The note's summary lists "spanfilade lookup" as a *legitimate* possible realization, and your I-span section correctly says an **allocation-keyed** 2-D index can serve origin. What is wrong for origin is Green's *containment-keyed* DOCISPAN spanfilade specifically (historical superset). Reword to "Green's containment-keyed spanfilade is the wrong tool" so a reader doesn't infer that all spanfilade-shaped indices are disqualified — which would contradict your "key it on allocation, not containment" recommendation.

2. **`[SHARPENING]` "What must be built → V-span resolver": "The block / one-origin-per-block reasoning assumes a single-subspace, level-uniform input" mis-attributes the dependency.** The note explicitly calls the C1a decomposition (and O2's per-block uniformity, which dispatches on content-vs-link block) **subspace-agnostic**. Single-subspace is not assumed by the block machinery; it is a *derived consequence* of level-uniformity (C0/C0a) that the resolver inherits through `WF_V` conjunct (ii). Re-attribute: the resolver depends on **level-uniformity** as the precondition; single-subspace falls out — which is exactly what your own admissibility bullet already says ("not a separate test — it follows from level-uniformity"). Align the two so they don't read as if the block reasoning itself needs single-subspace.

3. **`[SHARPENING]` "Implementation approaches → single-origin fast path": the summary clause "characterises exactly the spans whose allocated content lies wholly under one document's allocation prefix" silently drops the non-emptiness conjunct.** A span with *no* allocated content vacuously satisfies "content lies wholly under one prefix" yet has `|result| = 0`, not `1`. You correct this two sentences later ("the precondition also carries non-emptiness as a separate conjunct") and the build guidance (gate on an existence check) is right — but tighten the lead clause to "the spans with **non-empty** allocated content all lying under one prefix" so the characterization is exact on first statement.

4. **`[SHARPENING]` "Implementation approaches → boundary-walk": "Cost ∝ number of distinct origins" is slightly optimistic given the fragmentation you cite elsewhere.** Q14 (and your own gap-existence argument) establishes that a single document's content can be non-contiguous in I-space, so a walk encounters it as multiple *runs*; the walk cost is ∝ runs-crossed, which can exceed distinct origins (dedup collapses them only in the result). Either say "cost ∝ document-blocks crossed (≥ distinct origins under fragmented allocation)" or note the gap explicitly; the "skip a document's whole contiguous block in one step" assumption is the thing fragmentation softens.

5. **`[SHARPENING]` "Design commitments → conventional": "Exposing both arities … is a presentation choice."** The two lifts answer functionally distinct queries (you say so in the next clause — "neither trivially yields the other"), so it is a *capability* choice, not merely presentation. Reword to "exposing both arities is chosen, not forced — a minimal system could provide only the I-space primitive or only the reader-facing V-span" to avoid undercutting the not-interchangeable point that follows.

VERDICT: CONVERGED
