This is a strong, unusually precise digest. Before the list, the parts that are genuinely solid, so a reviser doesn't touch them:

- **Accuracy / forced-vs-conventional split is correct.** Forced (result type, purity, two-kinds, arranged semantics, allocation gate, contiguity-reliance) and conventional (the `1`/`2` numbering) are both right, and the digest correctly resists the tempting error of treating the working depth `m_S` as conventional — it flags `m_S ≥ 2` as structural (S8-depth) and that depth 2 is the collapsed common case, not an assumption (the note's `m_S = 3` instance backs this).
- **Grounding is clean.** Every udanax-green claim traces to the evidence answers — the link-internal `3.x` (Q19), the `?`-marker failure path (Q1), no-cache root reads reflecting deletions (Q16), `setwispnd` bounding-box (Q11/Q13), asymmetric `maxtextwid`-vs-root mechanisms (Q14), the mislabeled-KB and digit-stripping (Q12), the `acceptablevsa` stub and REARRANGE boundary-crossing (Q18). No fabricated source-level claim.
- **The central technical insight is correct and load-bearing.** I verified the gap arithmetic: `V_S = {[S,1],[S,3]}`, `n_S = 2` → `ext = ([S,1],[0,2])`, reach `[S,3]`, covers `{[S,1],[S,2]}` — omits the real `[S,3]`, invents `[S,2]`. So the count-based span *mis-locates* where Green's bounding box would *overshoot conservatively*, and "strictly less robust on exactly that input" is right for span coverage — correctly tied to open question 1. The write-admission framing (enforce D-CTG★ and subspace confinement on the *write* path so this read can trust them), the arranged-vs-homed trap, and the registration-separate-from-content requirement are all surfaced.

No accuracy, soundness, grounding, completeness, or altitude defects. The revision list is sharpenings only.

**Revision list:**

1. **[SHARPENING]** *Implementation approaches → recommendation:* scope "gap-faithful" inline. The sentence keeping Option 1 "as gap-faithful defense-in-depth … or the production path if you do not trust the write path" reads as if Option 1 yields fully correct output under a gap. It does not — only the **count** stays faithful (which *is* this operation's headline output, W20); Option 1's **span denotation still mis-locates** under a gap, since the span is "exact only under D-CTG★ either way" (the digest's own synthesis line). Co-locate that caveat with the recommendation so a builder picking Option 1 for an untrusted write path doesn't assume the emitted span is gap-correct. Non-load-bearing — position-enumeration is explicitly other operations' business (W0); the count is the contract.

2. **[SHARPENING]** *Guarantees → "Two-kinds-only (W9) and disjointness (W11)":* W11 is filed under "Inherited from invariants enforced elsewhere" and then walked back in place ("W11 itself holds by construction … listed under this heading only to stay beside W9"). Move W11 into the "Hold by construction" block where the digest's own analysis puts it, with a one-line pointer to W9, so the three-way taxonomy (by-construction / write-enforced / actively-enforced-here) stays clean rather than self-correcting. The substantive claim — W11 = SC-NEQ + W10's pure span math, no write-path invariant — is correct; only the filing is off.

3. **[SHARPENING]** The count-vs-boundary faithfulness asymmetry is stated five times (commitment bullet, Options 3/4/5, recommendation, closing synthesis). It is correct everywhere, but the repetition taxes the reader. State it once — ideally at the "frame it by *what is stored*, not by option number" synthesis — and have the per-option lines reference it instead of re-deriving it. Purely editorial.

VERDICT: CONVERGED
