This is a strong digest — accurate, well-grounded, at the right altitude, and genuinely actionable. The faithfulness-asymmetry analysis (count vs. stored-extent; mislocate vs. conservative-overshoot) is sharp, mathematically correct (I verified the `{[S,1],[S,3]}, n_S=2 → covers {[S,1],[S,2]}` worked case), and goes beyond restating the note while staying grounded in the evidence. Every Green source-level claim (`setwispnd` bounding box, `maxtextwid` text traversal, `acceptablevsa` stub, `findorgl`/`checkforopen` failure, the KB mislabeling) traces to the evidence answers. The W9/W11 decomposition (exhaustiveness from S3★-aux vs. distinctness from SC-NEQ) is a real insight and correct. The forced/conventional calls all match the note. No defects.

A handful of sharpenings follow.

**Revision list**

1. **[SHARPENING]** *Guarantees → "Two-kinds-only / disjointness" bullet:* the phrase "enforce **subspace confinement** at write admission" collides with the note's claim **W10 "SubspaceConfinement"**, which is a *read-side, by-construction* property (`⟦ext(d,S)⟧ has t₁=S`) that needs no enforcement. What you actually mean is enforcing **subspace-exhaustiveness / the boundary discipline (S3★-aux)** at write time. Rename to "subspace-boundary discipline" (or "exhaustiveness enforcement") so a builder cross-referencing the note doesn't read this as "enforce W10 at write." Builder action is unaffected; this is purely to remove a cross-reference trap (you also use "the confinement bullet" as a back-reference in the W4 bullet, so fix both).

2. **[SHARPENING]** *Design commitments / case matrix:* surface the **zero-width-placeholder trap** explicitly, symmetric with the two traps you already name (allocated-empty-vs-unallocated; homed-vs-arranged). The evidence (Q15) specifically tests it: an empty subspace contributes **no member**, never a zero-width placeholder VSpec to keep output positional. Your case matrix ("exactly one empty → one member") already entails the correct behavior, so this is making an implied, grounded trap explicit — the tempting wrong move is emitting a `([S,1], width 0)` placeholder for the absent kind.

3. **[SHARPENING]** *How it fits / Decisions:* the note's open question #4 ("derivable from / consistent with any single **overall extent** the document also exposes") is left abstract. Ground it: that overall extent is concretely **Green's `RETRIEVEDOCVSPAN`** — the broken single-bounding-span sibling (Q17) that spans both subspaces. Naming it sharpens both the deferred question and your "whole-document sibling of the range-scoped content-region-query family" line, and clarifies that Option 4's *internal* combined aggregate is a different thing from that *user-facing* overall-extent operation.

4. **[SHARPENING]** *Option 3:* the Green parenthetical — "Green's query reads the root with no cache layer, so deletions reflect immediately" (true, Q16) — illustrates **synchronous authoritative maintenance**, not the **recompute-on-load hint** discipline it's attached to (Green's root is the authority and is *not* rebuilt on load). Tighten so it doesn't imply Green exemplifies the hint approach; the drift-tolerance claim rests on your Lampson recommendation, not on Green.

5. **[SHARPENING]** *Implementation approaches → recommendation paragraph:* the faithfulness-asymmetry is already developed precisely in the commitments and per-option (3/4) text; the recommendation-paragraph restatement ("Mind the faithfulness asymmetry — and frame it by what is stored…") is largely redundant and can be trimmed to a one-line pointer without losing the position.

VERDICT: CONVERGED
