This is a strong digest — accurate on the note's commitments, well-grounded in the evidence channel (every Green source-level claim I checked traces to Q11–Q20/Q1), correctly at design altitude, and genuinely valuable in two places: the "count-based span does not degrade gracefully" analysis (correct math, and it sharpens the note's first open question), and the arranged-vs-homed / faithfulness-asymmetry treatment. The W19 dispatch matrix and the registration-separation requirement are exactly right. The problems below are real but localized.

## Revision list

1. **`[DEFECT]` Implementation approaches: a load-bearing approach is missing, and the cost landscape is mischaracterized.** The digest pins the *minimum* via D-MIN★ ("you never need to find a minimum") but then gets the *count* by O(n) range scan (Option 1, the recommended default) or a custom size-augmented tree (Option 2), and states that O(log n) counting requires augmentation. That is only true for counting *arbitrary* occupancy. Under D-CTG★/D-MIN★ — which this operation already trusts for exactness — the count is *also* pinned: `n_S` = the **last component of the maximum occupied key under prefix `S`**, recoverable by a single O(log n) floor/predecessor query on a *plain* ordered map (find the largest key below subspace `S+1`'s start; if its first component is `S`, the subspace is occupied, `n_S` is its last component, `m_S` is its depth). This is the note's own "the count is read off the boundary" (W2 discussion) and Green's "read the bounding box max" (Q13/Q14), and it dominates the recommended scan. Add it as an approach; classify it in the faithfulness framework as *boundary-based* (faithful to the count only under D-CTG★, like Option 4's free-ridden extent — **not** like actual-counting Options 1/2); and reconcile the recommendation: boundary-read is the natural cheap default on a plain map once D-CTG★ is enforced at write (which the digest separately mandates), with O(n) scan-count reserved as gap-faithful defense-in-depth. The digest exploited D-MIN★ but overlooked the symmetric D-CTG★ shortcut for the count.

2. **`[DEFECT]` "Guarantees to uphold," W4 bullet: internal inconsistency in the word "confinement."** The bullet says contiguity (W4) is "**unlike** confinement — it is not safely inherited" *and*, two clauses later, that contiguity enforcement is "**symmetric with** the confinement bullet below." Those cannot both hold for one referent: if confinement is the thing W4 is "unlike" (safely inherited, no action), it cannot also be a write-admission obligation symmetric with contiguity. The digest's own W9/W11 bullet establishes that "confinement"-family guarantees split into two bases — *write-enforced exhaustiveness* (W9/S3★-aux, the `acceptablevsa` lesson Green stubbed) and *self-certified span facts* (W10/W11 via SC-NEQ). The W4 bullet conflates them. Fix: write "unlike the **self-certified span facts** (W10/W11)" for the first contrast and keep "symmetric with the **exhaustiveness-enforcement** obligation (W9's basis)" for the second. As written, a builder could read "confinement is safely inherited" and skip the very write-admission check (subspace confinement) the digest elsewhere insists on — reproducing Green's documented bug.

3. **`[SHARPENING]` "Hold by construction," W13/W14 bullet: "disjointness makes merging impossible" misattributes the reason.** Disjoint half-open spans can still *abut* and merge; what actually precludes merging here is **separation** — `reach(ext(d, s_C)) < start_{s_L}` by T1, the full subspace gap (the note's stated justification). The operational instruction ("emit text-then-link, no normalizer") is correct; restore the separation reason rather than leaning on disjointness.

4. **`[SHARPENING]` "Inherited from invariants enforced elsewhere," W11 placement.** W11 (disjointness) is grouped under invariants "enforced elsewhere (the editing operations and the placement discipline)," but the body correctly notes it rests on SC-NEQ — a constant convention fact — plus W10 (pure span math). It is *self-certified*, not inherited from any write-enforced invariant, and belongs with the "Hold by construction" guarantees (or flagged explicitly as self-certified, unlike its bullet-mate W9). This is the same self-certified-vs-write-enforced clarity that item 2 turns on.

5. **`[SHARPENING]` "How it fits," leans-on list omits CL-OWN/CL-UNIQ.** These (ASN-0047) are the foundation basis for W20's faithful-count bijection (`M(d)|_{V_{s_L}}` injective onto the arranged links). The W20 discussion captures the *semantics* (arranged ⊆ homed) but the dependency list should cite them alongside S2/S3★.

6. **`[SHARPENING]` Two-kinds-only could be empirically reinforced from Q19.** Green reserves a `3.x` type subspace, but it is *link-internal* (third endpoint storage inside link orgls) and never surfaces as a third member of the document's span-set — confirming W9 against the one structure a builder might worry about. A one-line note preempts the "what about the type subspace?" question.

VERDICT: REVISE
