The digest is accurate, well-grounded, and at the right altitude. Its central insight — that the spec's *count-based* extent span (W2) "does not degrade gracefully" under an interior gap, mis-locating where Green's max−min bounding box would merely overshoot — is correct (I checked the `V_S = {[S,1],[S,3]}` arithmetic: count-based reaches `[S,3]`, covers `{[S,1],[S,2]}`, omitting the real `[S,3]` and inventing `[S,2]`) and genuinely valuable; it sharpens the note's first open question rather than restating it. The W9/W11 separation (exhaustiveness via S3★-aux vs. distinctness via SC-NEQ, with W11 actually holding *by construction*) is precise and correct. The arranged-vs-homed (W20) emphasis, the allocation-gate / case-matrix dispatch, and the implementation-approach taxonomy are sound. Every Green source-level claim (setwispnd bounding box, maxtextwid traversal, acceptablevsa stub, findorgl/checkforopen/`?`, the 3.x link-internal subspace, the KB's own mislabeling) is grounded in the evidence answers. No defects. Sharpenings follow.

**Revision list:**

1. **[SHARPENING] "Implementation approaches" / faithfulness asymmetry: surface that the *recommended* Option 3, under a hypothetical gap, produces Green's bounding-box span — NOT the spec's mis-locating span — and that this is exactly what makes the cross-check a sound gap detector.** The headline is "the count-based span mis-locates"; the headline recommendation is Option 3 (boundary read). These have *different* gap-failure modes the digest never connects: feeding the *true count* `|V_S|` to the synthesizer (Options 1/2) under a gap mis-locates (reaches `1+|V_S|`, omits real positions); reading the *boundary max* (Options 3/5) over-counts and reaches the *true max* — i.e., reproduces Green's conservative bounding box. So "gap-faithful" (1/2) buys a faithful *count* at the price of a *mis-locating* span; the boundary paths buy a bounding-box span at the price of an *over-count*. The digest rightly says "it is about the count, not the span," but should state the consequence: the two families *diverge* under a gap in both count and span, which is precisely why the Option-3-vs-Option-1 cross-check fires. Also tighten the "Option 1 as production path if you don't trust the write path" line — Option 1 maximizes *count*-faithfulness (the contract, W1), but its synthesized span still mis-locates; a faithful *span* under relaxed D-CTG★ is the unresolved open question for every option.

2. **[SHARPENING] "Guarantees" / W9 bullet: REARRANGE-crossing is mis-filed under exhaustiveness.** The bullet attributes "the exhaustiveness invariant was degraded" to both `acceptablevsa` (a stub) *and* "REARRANGE could cross the boundary." But moving text into the link subspace keeps every position within `{s_C, s_L}` — it creates no *third* subspace, so it does not threaten S3★-aux (exhaustiveness, W9's basis); it misclassifies content, threatening S3★ (referential integrity, W17's basis) and thereby the *faithful* count (W20). Given the digest's own insistence three sentences earlier on keeping W9's and W11's bases separate, file REARRANGE-crossing under classification/faithfulness (S3★/W17/W20) and let `acceptablevsa`'s stub carry the exhaustiveness point (a stubbed write-admission check is what admits an out-of-`{s_C,s_L}` write). The actionable lesson — enforce subspace discipline at write admission — is unchanged.

3. **[SHARPENING] "Implementation approaches" / Option 4: the Green parenthetical doesn't illustrate the discipline it's attached to.** "Green's query reads the root with no cache layer, so deletions reflect immediately" (true, Q16) is offered to support "treat the summary as a hint, recomputed on load, so drift degrades performance not correctness." But Green's root is an *incrementally-maintained authoritative* aggregate, *never* recomputed on load — a `setwispnd` bug would corrupt it with no reload self-heal, the *opposite* of the recompute-on-load robustness being recommended. Recontextualize the citation as "a correctly-maintained aggregate stays consistent," or note the contrast (Green is the maintained-but-not-recomputed case the recommendation improves on).

4. **[SHARPENING] "Implementation approaches" / Option 3: the W2 citation conflates two different boundary reads.** Calling Option 3 "the note's own 'the count is read off the boundary' (W2)" elides that W2 is *consumer*-side (recover `n_S` from the emitted span's `reach − start`), whereas Option 3 is *implementation*-side (compute `n_S` from the arrangement's maximum key); they coincide only under D-CTG★. The Q13/Q14 "read the bounding-box max" citation already carries Option 3's actual mechanism — lean on that and drop or qualify the W2 identification.

VERDICT: CONVERGED
