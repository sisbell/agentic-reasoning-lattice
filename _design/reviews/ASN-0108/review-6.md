Reviewing the digest against ASN-0108, its claim statements, and the evidence answers. This is a strong, unusually careful digest: it gets the three-key taxonomy right (address = intended, least-covered = parameter, position = foil), correctly identifies the discoverability-vs-monotone fork as adopted-not-inherited, and in two places *improves on the note* in ways grounded in evidence — it catches the note's overclaim that Green "breaks ties by link address" (Q1 tie-break: no address tiebreak, just dedup + traversal order) and surfaces the key-cut-vs-exact-match-scan distinction the note's W8 silently assumes away (Q12/Q17). Its central technical claim — that restricting the spanfilade lookup to the query's *current image* yields exactly `Match` with no per-candidate post-filter — is sound and well-grounded (Q15/Q20). I found no defects. The items below are all genuine but non-load-bearing.

**Revision list**

1. **§3 (least-covered bullet) and Design-commitment 4: "least *content* tumbler" is imprecise — use "least *covered* tumbler."** `[SHARPENING]` The note is explicit that when the type slot is the only non-empty contributor (a link with empty from/to endsets — which the digest *itself* says gets a key, two sentences later), the covered tumbler is "necessarily a type-classifier address … possibly outside `dom(Σ.C)`," i.e. *not* a content address. The digest already uses the correct term ("least-covered-tumbler key") in three other places; align these two instances and add that the value may be a non-content type address. Highest-priority because the note went out of its way to make this distinction.

2. **§3 evidence note: "Two riders the evidence corrects" mislabels rider 1.** `[SHARPENING]` Rider 1 (Green keys on whichever slot the query *matched*, query/state-dependent) is *confirmed* by the evidence and is already stated by the note ("query- and state-dependent, hence not a function of the immutable link value"); the evidence does not correct it. Only rider 2 (link-ISA is dedup-only, no order tiebreak) is a genuine correction of the note. Reword to "the evidence confirms (rider 1) and corrects (rider 2)" so the digest doesn't imply the note erred on rider 1.

3. **"How it fits" / ASN-0127: "windowing delivers that set, never re-derives it" reads as contradicting per-call re-derivation.** `[SHARPENING]` Taken literally ("it" = the set) this conflicts with W3 and the digest's own approach (A) ("each call re-executes the full search"). The intended point is separation of concerns — windowing does not re-derive the *matching predicate* (ASN-0127 owns it) though it re-invokes `findlinks_V` every call. Disambiguate to "never re-derives the matching predicate."

4. **Design-commitment 4: "parameterizing the formal key *only to admit Gregory's* alternative" understates the note's purpose.** `[SHARPENING]` The note keeps `κ` a parameter to state each guarantee's requirement abstractly in terms of key-properties — admitting *both* Nelson's address key and Gregory's, and rejecting the position foil — not solely to admit Gregory's. Broaden the clause.

5. **§3 / commitment 4: "which is exactly why the note builds it" overstates the note's stated rationale for the least-covered key.** `[SHARPENING]` The note fixes the slice a priori to keep the key a function of the *immutable link value* (permanence); the image-shrinkage immunity is a consequence the digest derives, not the motive the note states. Soften to "which is the property the a-priori-fixed slice secures."

6. **Design commitments: tagging short-window termination "Forced" is slightly strong.** `[SHARPENING]` The note adopts the short window as the signal and says no terminal marker is *required*, but does not establish a marker is impossible (a server could emit one on the empty window without breaking statelessness). It is the *cheapest sufficient* mechanism, not strictly forced. The genuinely forced part — "must read ≤N including 0 or loop forever" — is already stated correctly; retag the mechanism itself.

7. **§2: the digest's "ghost (Q20)" collides with the note's reserved term.** `[SHARPENING]` The note reserves "ghost" for LP17 (globally unreachable) and uses "orphaned" for per-`d_q` Match-loss. The digest's "ghost" is a *third* phenomenon (matches the query, renders empty endsets on follow) — correctly identified as downstream, but the label clashes. Add a half-line noting this is the evidence's (Q20) usage, distinct from the note's LP17 ghost.

**Solid as-is:** the implementation-approach trichotomy (A/B/C) with its cost analysis and Lampson hint-discipline; the index-layering argument (spanfilade = matching/ordering index, `findlinks_V` = discoverability layer, follow-time endset rendering = where the Q20 ghost surfaces); the matched-slot-order "moves both ways under mutation" analysis (partial orphaning → key up → re-delivery; LP9 extension → key down → skip — both clause-1 violations while still in `Match`, hence safe only on a frozen set); and the termination treatment (permanent key forecloses the W9c re-ascension livelock but not finite-inflow, which is legitimate non-termination, not a bug). These are correct, grounded, and at the right altitude.

VERDICT: CONVERGED
