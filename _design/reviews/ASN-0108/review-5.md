This is a strong, unusually careful digest. Before the list, the parts a reviser should *not* touch, because they are the digest's real value:

- **§1(A) — the key-cut vs. exact-match-scan insight.** Correctly identifies that Green's address cursor still goes dark on orphaning *because the resume is a `tumblereq` scan for the cursor link*, not because of the key (Q12/Q17), and that the note's W8 survival guarantee holds only under key-cut resume. This is grounded, not in the note, and exactly what a builder needs. Keep it.
- **§2/§3 — matched-slot order is state-unstable.** The analysis that Green's native order keys on the *least matched in-image* address (so it slides up under partial orphaning → duplicate, down under image extension → skip, both clause-1 violations *while in Match*), and is therefore safe only on a frozen set — distinct from the note's a-priori least-covered-tumbler key — is subtle and correct. The "no permanent key is the index's free native order" coupling (decisions 1↔3) follows soundly.
- **The tie-break correction.** Catching that the note's "onlinklist breaks ties by ISA … the link's permanent address is the de facto low-order key, not a traversal accident" is *false* per Q1 (ISA is a dedup key; tie order among distinct same-boundary links is enfilade traversal order, no address tiebreak), and drawing the right builder consequence (the `(boundary, address)` composite is an obligation to add, not behavior Green realizes), is exactly the job.
- Reconciling Q11's "V-address" as terminological against SPANRANGE = content I-address; the (A)/(B)/(C) cost models and the (C) "you'd need versioning the substrate lacks" critique; the W9b-legitimate-non-termination vs W9c-livelock distinction; honest labeling of the journaling recommendation as non-derived. All sound.

# Revision list

1. **§2 ("the genuine ghost") — generalize the ghost mechanism. `[SHARPENING]`** The digest pins the empty-on-follow ghost to "whose *other, non-matched* endpoints have orphaned." The described case is real, but the general, evidence-grounded condition (Q20/Q15) is "*any* endset whose content currently lacks a V-mapping renders empty on follow" — which, in a transclusion / home-doc-deletion configuration, can include the *matched* endpoint itself (matched via `d_q`'s POOM, rendered via the home doc's). The design conclusion ("filter downstream, not at window-build") is unaffected; just broaden the mechanism sentence so a builder doesn't under-handle the matched-endpoint case. Also note that this "ghost" is the digest's/Q20's sense (empty-on-render, still in `Match`), distinct from the note's per-`d_q` *orphaned* and LP17 *global ghost* — worth one clause to avoid terminology collision with the note's "orphaned."

2. **§3 / "What must be built" — surface the note-correction more prominently. `[SHARPENING]`** The correction of the note's ISA-tiebreak claim is currently buried in a parenthetical inside the content-position-key bullet. Because it converts into a concrete builder obligation (you must add the injective `(boundary, address)` tiebreaker yourself; Green's `onlinklist` does not give it), promote it to a first-class line — e.g., in "What must be built" under the ordering key, or as an explicit "note correction." The digest already handles it correctly; this is prominence only.

3. **Guarantees → "no duplicate / no skip" — tighten the enforcement wording. `[SHARPENING]`** "Requires active enforcement only if you stray to a position key" is loose: a position key cannot be made to satisfy W5 by enforcement — W5 is broken for it (that's why it's rejected). The correct framing is "free for either permanent key; *avoid* the position key" — you choose a key that yields it, you don't police it.

The digest reads no design commitment as "forced" that the note treats as conventional (or vice versa), proposes no approach that violates a note guarantee, and contains no ungrounded source-level claim or altitude slip. The three items above are genuine but non-load-bearing.

VERDICT: CONVERGED
