This digest is unusually strong. Before the revision list, the genuinely solid spots, so a reviser doesn't waste effort on them: the **live-containment-vs-historical-provenance split** (the note's `Contains_C` vs `Σ.R`) and the explicit warning against rebuilding `Σ.R` from arrangements or a truncated journal is exactly the trap a builder would fall into, caught; the **run-coalescing analysis** correctly separates storage compaction from M14 occurrence-merging and gates on I-contiguity (M16a) so CP11 survives; the **cross-depth immunity** grounding (read `M(d_s)(v)`, never offset-from-start, vs udanax's `context2span`/`tumblerintdiff` clip) is precise and correctly tied to OQ2; and the **journal idempotence** point (arrangement splice apply-once vs provenance set-union at-least-once) is a real, load-bearing builder distinction. Forced/conventional classifications are accurate, no proposed approach violates a note commitment (it actively guards against the violations), and every udanax source-level claim is grounded in the evidence answers.

The two items below are sharpenings, not defects.

**1. [SHARPENING] "What must be built" / "Decisions → API-edge admissibility": the applicability predicate is incomplete and one phrase is loose.** The digest covers the precondition with a real failure mode — content-residence — thoroughly, plus `W ≥ 1` and valid-`p`. But `enabled(COPY)` as the note states it also requires: destination allocated (`d ∈ dom(M)`), each source allocated (`d_s ∈ dom(M)`), each span **T12-well-formed**, and each source subspace non-empty (`V_{subspace(s)}(d_s) ≠ ∅`). These are routine (and the intersection resolver naturally needs source/destination to exist), so the digest is sound without them — but a section that names the guard should name the whole guard. Relatedly, sharpen "span depth/shape is neither normalized nor validated": the note drops **only** ASN-0058 condition (iii) (the depth pin `#ℓ = #u = m` / level-uniformity) and *retains* conditions (i)/(ii) — i.e. T12-well-formedness and non-empty subspace. As written, "depth/shape neither validated" reads as "no well-formedness check at all," which over-relaxes the note (udanax does drop everything, but the note keeps T12 abstractly; the pure-intersection resolver the digest recommends needs neither). Add the retained conjuncts and pin the "no validation" claim to condition (iii).

**2. [SHARPENING] "Provenance recorder + reverse index": ground the subset relation with P4★, not P2 alone.** The claim "arrangements yield only live containment, a strict subset" of `Σ.R` rests on two facts the digest half-cites: the **⊆ direction** (live containment is contained in historical provenance) is the note's P4★ (`Contains_C(Σ) ⊆ R`, available at the composite boundary), and the **strictness** (the gap is deleted-position pairs) is P2. The digest cites P2 for strictness but leaves the subset direction itself uncited even though P4★ is exactly the note's name for it (and the digest already invokes P4★ in the atomicity section). Cite P4★ alongside P2 here so the "strict subset" claim is grounded in both directions.

VERDICT: CONVERGED
