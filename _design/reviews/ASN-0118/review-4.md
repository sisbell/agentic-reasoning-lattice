This is a strong digest — accurate forced/conventional split, no fabricated Green claims (the udanax citations all trace to the evidence), good Lampson-style tradeoff reasoning, and it surfaces the two load-bearing subtleties well: *why* content-residence is required (otherwise resolution reads `dom(L)` and placement breaks S3★) and the read-the-image immunity result. It stays at design altitude throughout. It ships one false claim, plus a couple of minor consistency gaps.

**Revision list**

1. **[DEFECT]** *Design commitments → Forced, "Cross-origin material never merges":* "Assembling from **k sources** … **≤ k distinct homes**" is false. The number of distinct homes (origins) is fixed by the content's *allocators*, not by the source-document count. Under chained transclusion — which the note explicitly designs for (CP5: "a third document the source had itself transcluded from"; CP0(c): resolution "records as many distinct origins as the source content had homes") — a single source span can resolve to addresses from several origins, so distinct homes can *exceed* the number of source documents. The note's own `k` ("names `k` distinct parents") counts origins, not sources. Drop the "≤ k" bound; restate as: the multiset carries one entry per placed address and as many distinct homes as the resolved content has origins, which under chaining can exceed the source count. (The cross-origin-never-merge takeaway is correct and survives — only the quantitative bound is wrong.)

2. **[SHARPENING]** *Guarantees to uphold → "Require active enforcement":* add an explicit item that cross-origin runs must never be coalesced/merged (gate any run-coalescing on I-contiguity, per CP11/M16). Right now CP11 preservation sits only under "hold by construction (*provided* cross-origin runs never merge)," and the I-contiguity gate appears only in the approaches prose. If a builder enables coalescing, not merging across origins is a real get-it-right obligation, not an automatic consequence — it belongs on the enforcement checklist.

3. **[SHARPENING]** *Atomicity → "Atomic visibility":* the root-swap set ("the destination's arrangement, the reverse index") omits the authoritative provenance records `Σ.R`, which the recorder and snapshot sections correctly treat as authoritative durable state *distinct* from the rebuildable query index. List `Σ.R` among the staged-then-published roots so the atomic-swap set matches the snapshot set; otherwise the one structure you call authoritative is the one not named in the commit.

Everything else I checked holds: the content-residence rationale, the read-the-image-not-offset-arithmetic discipline (and its tie to udanax's `context2span`/`tumblerintdiff` cross-depth bug), the homedoc-vs-structural-origin divergence (grounded in Q14), the live-containment-vs-historical-`Σ.R` distinction with the warning not to rebuild historical provenance from arrangements, the splice/vacate functionality invariant, the non-idempotent-arrangement / idempotent-provenance replay split, and the captured-root-not-immutability concurrency argument are all sound and well-grounded.

VERDICT: REVISE
