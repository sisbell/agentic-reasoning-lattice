This is a strong digest: accurate against the note, well-grounded in the evidence (every Green reference — spanfilade, DOCISPAN `[4,5)` type tag, half-open overlap, the 12/20/84 loosening, no-BERT/no-`findorgl`, write-only index, fork records whole-extent — traces to Q11–Q20), correctly altitude'd, and committed throughout. The forced/conventional split is right, the scan→C→B recommendation is defensible, and the "filter is the *whole* difference between live and historical" framing is exactly the note's thesis. I found no inaccuracy, no ungrounded source-level claim, no code-drift, and no missing load-bearing commitment. The items below are sharpenings, ordered by value.

---

**1. Section "Implementation approaches" (B, and the "exact tradeoff" bullet under C): the failure analysis of B is one-sided, which inflates the clean B↔C duality.** The digest names only B's *under-deletion* risk ("a missed deletion returns a ghost — a silent soundness violation") and builds the duality "B's missed deletion = uncatchable soundness bug ↔ C's missed recording = uncatchable completeness bug." But B carries a *second* silent vector: contraction is per-vacated-address (remove `d` only from addresses in `ran_C(d,Σ) ∖ ran_Ret`); a wrong-scope deletion that drops `d` from addresses it *still* arranges after a **partial** contraction silently omits a live container — `d` survives iff `ran_Ret ∩ I ≠ ∅` (FD-STEP/FD-CWP), and nothing catches the omission. So B risks *both* silent directions while C risks only completeness. Fix: state that B's deletion must be scoped to vacated addresses (not the document), add over-deletion as B's second uncatchable failure mode, and recast the comparison as "B = two silent failure modes; C = one, structurally prevented by recording discipline" — which strengthens the (correct) recommendation against B.

**2. Sections "What must be built" / "Decisions for the builder" (expose historical query) / "Recovery and the historical summary": the recommendation to expose the "ever contained" query first-class omits its load-bearing limitation.** The provenance relation records membership *without order, time, or version rank* (note Open Question 2), so `finddocs_R` answers "whether ever," never "when" or "in which version." Add this caveat wherever the historical query is surfaced; a consumer must not read recency or sequence into the result. This is a real limitation of a path the digest actively recommends.

**3. Section "Guarantees to uphold → active enforcement → Completeness": the framing reads near-universal but is an *indexed-approach* obligation.** Scan (approach A) gets completeness *and* soundness for free with zero enforcement — it reads current arrangements directly. State that recording discipline is the price of the *index*, not of the *operation*. This sharpens "pick scan first" and stops a builder from importing recording obligations they don't need.

**4. Section "Guarantees to uphold → Completeness (no omission)" / approach C: the placement-audit lesson carries only one of the evidence's two examples.** The digest has the version-fork trap ("a route that looks like it needn't record but must"). The dual trap is the APPEND route whose `insertspanf` is commented out yet whose content *is* recorded because it funnels through the common copy path (Q13) — "a route that looks like it skips recording but doesn't; verify the common path records, and don't bolt on redundant/wrong recording." Add it so the audit instruction covers both directions, since both are exactly what bite a builder wiring up the recording discipline.

**5. Section "Implementation approaches → C" / "Recovery": the "exactly this repo's `links.jsonl`/`paths.json` pattern" reference is build-context, not grounded in the note (a domain-neutral spec) or the evidence.** Replay-from-placement-log is sound and worth keeping; either mark the file reference as "this substrate's existing append-only journal" or state it generically, so the approach doesn't read as if the note named a persistence mechanism. Minor.

**6. Two small clarity items.** (a) "Guarantees to uphold": version-fork appears both as a by-construction guarantee (FD-VERS) and as an active recording obligation — one line reconciling them (the duplication is the *operation's* guarantee; realizing it in an index is the recording obligation) removes the apparent inconsistency. (b) "What must be built": "the resolved material is *naturally a union of intervals*" slightly oversells — the resolved value is a *set* (FD-RES), and its interval structure is incidental; the range-overlap need comes as much from documents' stored holdings being contiguous I-runs. Tighten so a reader doesn't infer interval structure is intrinsic to the resolved set.

---

Genuinely solid, leave as-is: the **Design commitments** section (forced/conventional split is accurate and complete), the **core tradeoff analysis** in approach C (the hint-not-authority / separation-of-concerns / common-case-fast reasoning, and especially the *distribution-is-a-soundness-exposure-under-C* extension, which is a correct and valuable inference beyond the note), and the **green-grounded "filter is the only thing green omits"** narrative.
