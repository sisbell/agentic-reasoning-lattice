This is a strong, well-grounded digest: the central commitment (present-tense, address-keyed containment) is read correctly, the scan/live-index/monotone-index-plus-filter framing is sound and matches the note's own "obvious strategy," every Green claim is backed by the evidence answers (no fabricated function names), and altitude stays clean throughout (structure names only, no types/signatures). The items below are mostly sharpenings; one is a genuine missing guarantee.

**1. Add self-inclusion (FD-SELF) to the commitments/guarantees — it is a missing load-bearing guarantee, distinct from origin-neutrality.** The note foregrounds five framing questions; question 3 has *two* halves — "must the source document appear?" (FD-SELF: yes) and "does the origin enjoy privilege?" (FD-NEUT: no). The digest answers only the second (origin-neutrality, in the guarantees list) and silently drops the first. Add a guarantee: a document named in the query appears in its own answer whenever its named region resolves non-empty. Make the distinction explicit, because it is exactly the subtle part — under transclusion the document you query *through* (FD-SELF) is **not** the document that *allocated* the material (FD-NEUT); covering one does not cover the other. It is a property users rely on ("query through my doc, find my doc") and a correctness property a builder must not break.

**2. "Global reach under distribution" (Guarantees + Decisions) treats distribution as a completeness-only problem, but the recommended approach (C) makes it a *soundness* problem too.** Because C recovers soundness via a per-candidate present-tense filter that must read each candidate's *current* arrangement (POOM), any architecture that serves candidate-discovery and candidate-filtering from separately-reachable components — e.g., a global/replicated index over distributed arrangements — exposes a new failure mode: a candidate discovered from the index whose home arrangement is unreachable cannot be filtered, so it may be a ghost. Add that under distribution *both* discovery (completeness) and filtering (soundness) are availability-sensitive, and an unfilterable candidate must produce a *qualified* result, never a silently-unfiltered (ghost) or silently-dropped one. This is the higher-stakes form of the note's "temporarily unreachable ≠ absent," and a builder following the C recommendation could ship it as a soundness bug while believing distribution was only a completeness exposure.

**3. Co-locate C's residual completeness risk with the C recommendation (Implementation approaches → C).** The recommendation is correct, but its exact tradeoff is currently scattered. State it where C is recommended: append-only-index-plus-filter *eliminates* the soundness/ghost bug (the filter strips stale entries) but *retains* an **uncatchable** completeness risk — a missed placement-recording omits a live container and the filter, which only removes, can never recover it. This is the precise dual of B's risk and is *why* the "no silent-omission channel, including version fork" enforcement is load-bearing specifically for C, not a generic caution. Saying so in place sharpens "C is safer than B" into the accurate "C removes one whole bug class and keeps the other."

**4. Reconcile "Pick this first" (A) with "recommended default" (C) at the approach headers.** The two headers send opposing signals; the scale-conditioned resolution (scan for small/low-query-rate docuverses and as the always-correct oracle against which the others are validated; C as the default *as the docuverse grows*) appears only later in "Decisions for the builder." Fold that conditioning into the headers so the recommendation reads coherently where it is made.

**5. Option B's index-maintenance rule is incomplete (Implementation approaches → B).** "Add on content extension, remove on contraction" omits version-fork — a placement that registers a *new holder of existing addresses* (the same non-obvious route the digest itself flags under completeness) — so as written B would be quietly unsound (incomplete) on fork. Add fork to B's "add" events, and optionally note reorder needs no update (range unchanged, FD-STEP). Complete B's maintenance contract.

**6. Minor accuracy — coupling citation (How it fits).** "the couplings — notably … (J1★) and … (P4★) — that make the provenance relation a sound historical index": the *soundness* of the historical index (every entry a true once-contained — no fabrication) is **J1'★**; P4★ is the live⊆historical bound (FD-SUPER), a different role. Either add J1'★ or scope the P4★ mention to its actual live-bounded-by-historical function.

Solid as-is, leave unchanged: the resolver requirements (transclusion-transparency via FD-ASKER plus multi-region input via FD-CONVEX), the within-query-consistency-vs-deliberate-caching distinction (the torn-read hazard the note only implies, surfaced correctly), the "key by I-address only, never V-position" rationale, the "authorization is external, post-filter only" decision (grounded in FD-COMPLETE + Q12/Q20), and the "interval-overlap encoding is a fidelity constraint, not a free knob" decision (FD-PART + the 12/20/84 evidence) are all decisive, correctly grounded, and at the right altitude.
