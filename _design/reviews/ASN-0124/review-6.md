I reviewed the digest against ASN-0124, its claim statements, and the verified Green evidence. The core is accurate, grounded, and at the right altitude: the central design decision (present-tense, address-keyed containment) is correctly identified as the forced commitment, the forced/conventional split holds up, every FD- guarantee is mapped, the A/B/C strategy analysis with its failure-mode comparison is correct, and every Green source-level claim traces to the evidence answers (Q11–Q20) or the note. I found no defects — only sharpenings.

# Revision list

1. **[SHARPENING] Implementation approaches → Resolving regions → "Within-query consistency."** The instruction "the snapshot must be a *composite boundary* … apply composites atomically" is correct and load-bearing, but the justification is mis-scoped. It cites the coupling invariants (P4★/FD-SUPER, OQ1) as the reason — yet those bound *provenance against containment* and matter only for an index-based (approach C) or historical query. The scan (A) and the resolution phase this paragraph is actually about read only arrangements (Σ.M) and never touch R/P4★, so the coupling reason does not bind them. The operative reason for *them* is that interior states tear the **arrangement itself**: FD-FRESH's insertion composite passes through a K.μ⁻ clear that transiently empties a document's content subspace, so a scan or a resolve observing that interior state returns a wrong (transiently empty) answer. Add the arrangement-tearing reason so an approach-A builder cannot reason "I don't use the index, so the coupling concern doesn't bind me, so I may read interior states." The statement as written is true and the conclusion is right; it just under-covers the recommended-first approach.

2. **[SHARPENING] Recovery/historical summary, and Decisions → historical query.** "It answers … never *when* or *in which version*" glosses the note's "version rank" imprecisely. Each version is its own document entity (FD-COMPLETE: "each version is its own document entity"; FD-VERS), so the historical query *does* name which version-documents ever held the material — they are literally the elements of the answer set. What the provenance relation lacks is temporal order/rank (when, in what sequence relative to other versions). Replace "in which version" with "nor the order/rank among versions" so a builder isn't led to think the historical query can't distinguish version-documents (it can; it just can't time-order them). The correct term "version rank" is already present, which is why this is a tightening rather than a defect.

3. **[SHARPENING] What must be built → region resolver.** Surface FD-V's definedness precondition explicitly: the operation is defined only when *every document named in Q is registered* (FD-IMGC is undefined for `d ∉ dom(Σ.M)`). This is a genuine builder fork the digest currently presupposes away — Green fails the entire query when a named document is absent (resolution's `findorgl` fails), versus a design that skips the missing region. One line naming the precondition and the fail-whole-vs-skip choice closes it.

The strongest sections need no change: the A/B/C comparison (especially "B has two silent failure modes; C has one," and C's risk as "the precise dual of B's over-scoped-deletion") is exact; the filter-cost analysis ("total proportional to `|finddocs_R|`, not the ghost residue") correctly resists the "nearly free" trap; the self-inclusion-vs-origin-neutrality distinction and the "distribution is a soundness exposure under C, not only a completeness one" insight are sharp, correct, and earn their place beyond the note.

VERDICT: CONVERGED
