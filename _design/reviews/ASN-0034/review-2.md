This is a strong digest — accurate on the load-bearing commitments, correctly subordinating the reference implementation's mistakes to from-spec design, and at the right altitude throughout. The defects below are localized; none undermines the overall shape.

**Revision list (most important first):**

1. **Design commitments / Allocation: the digest never states that tumbler order is *structural* (depth-first), not *temporal* — a point the note hammers.** The note devotes the entire "Monotonic allocation" discussion to it ("the tumbler line as a whole does NOT grow monotonically by creation time"; `2.1 < 2.1.1 < 2.2` even though `2.1.1` is created *after* `2.2`; per-allocator T9 ≠ global T9). The digest conveys per-allocator counters ("Serialize only within a single allocator") but not the consequence consumers must know: a later-created child sorts *before* an earlier-created sibling, and across allocators sort order is independent of creation time. Add this as an explicit commitment + builder gotcha — **max tumbler ≠ newest; tumbler order is not a recency proxy; any temporal/recency index needs its own ordering.** Also tighten "permanent and monotone": "monotone" is true only of the allocated *set* (T8), not of allocation order globally; say so to avoid the wrong global reading.

2. **Implementation approaches → Allocation: "GlobalUniqueness, Case 4" is the wrong citation for the account/document example.** The digest writes that nested allocators' independence "is not the non-nesting case (T10) but length separation… (GlobalUniqueness, Case 4)" and illustrates with "a document's prefix extends its account's." But account (zeros=1) and document (zeros=2) differ in *zero count*, so the proof distinguishes them by **Case 3** (level determination), not Case 4. Case 4 (same zero count, length separation) is specifically the **versioning** safeguard — a document and its versions via `inc(·,1)`, both zeros=2. This directly contradicts the digest's own Design-commitments section, which correctly splits "different levels → zero-counts (Case 3)" from "same level → length separation (Case 4)." The *mechanism* claim (length separation holds across a lineage) is true, so this is a precision/citation fix: attribute different-level nesting to zero-count (Case 3), reserve Case 4 for same-type children/versioning, and note both operate across a lineage.

3. **Implementation approaches → Durability: extend the durability requirement to child-allocator activation, not just the sibling frontier.** The whole section is framed around the per-allocator sibling counter ("reserve a block of sibling slots," "the high-water mark"). But spawning a child *activates a new allocator*, and T10a makes each `(frontier, k')` spawn at-most-once; if a spawn record is lost on crash, the child can be re-spawned and its addresses **reused** — the exact "never hand out an address twice" violation the section is built to prevent. State explicitly that child-spawning (new active prefix + base) must be journaled/durable *before* any child address is handed out, and that the "frontier registry" you call "the only mutable persistent state" must include child frontiers (which is also how child existence is recorded). The "only mutable persistent state" claim is then fine — it just needs to span active-allocator set + frontiers, not a single counter.

4. *(Sharpening)* **Representation: "bignum makes associativity of ⊕ hold" is real but not load-bearing — flag it as a non-required nicety.** The note is explicit: "The design does not depend on associativity. Shifts are applied as single operations… never composed." Bignum is fully justified by T0(a) alone; presenting associativity as a reason slightly overstates its relevance. Either drop the aside or mark it "not required by the contract."

5. *(Genuinely solid — leave as-is.)* The representation decision (store the literal sequence → T3 identity free, deleting the normalization/leading-zero-alias bug class) correctly names the single highest-leverage choice and the Lampson move. The durability composition (journal-of-record + recomputable frontier registry as *hint* + in-memory batch reservation, "skip-ahead never reuse") is textbook log/hint/common-case-fast/rare-case-correct reasoning and is sound. The "hold by construction vs. require active enforcement" split is accurate, including the sharp and correct observation that T5 contiguity is free for *any* prefix (the proof never parses fields) whereas T6/T7 need T4 validation — and the "all-zero is rejected as an address but legal as a span sentinel, so don't reuse the address validator at the span boundary" distinction, with the zero-endpoint behavior correctly flagged as the note's open question rather than a settled rule.

All Green-structure references check out as grounded: `granfilade`, `spanfilade` are widely-documented enfilade types, and `Istream allocation` / `V-enfilade traversal` appear verbatim in the note's formal-summary table. No fabricated source-level claims (the `absadd`/leading-zero/`NPLACES` material is all paraphrased from the note, not invented).
