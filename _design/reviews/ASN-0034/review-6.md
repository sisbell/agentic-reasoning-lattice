Reviewed against ASN-0034 and the extracted claim statements. This is a strong digest: the design commitments map cleanly onto the note's properties, "forced" vs. "deliberate-but-not-forced" is labeled correctly throughout (the T4 flat-arithmetic separation is rightly called a "do one thing well" choice, not a lock), the GlobalUniqueness decomposition (non-nesting→T10 / nesting-different-level→T4 / nesting-same-level→length-separation) matches the proof's Cases 2–4 exactly, the structural-vs-temporal-order gotcha is grounded and useful, and every source-level claim about the reference implementation (mantissa/exponent normalization, the leading-zero alias breaking transitivity, NPLACES widening, the operand-order add asymmetry) is supported by the note. The allocation gate (`inc(·,1)` needs `zeros≤3`, `inc(·,2)` needs `zeros≤2`, `k≥3` breaks T4; descend-a-level only from node/user/document) is correct per TA5a, and the durability reasoning (skip-ahead never reuse; over-shoot → allocated ghosts bounded by the frontier; child activation is itself durable allocation state) is sound. No code-level slips, no ungrounded Green claims, no missing load-bearing property.

I found no material defects — only two precision improvements.

## Revision list

1. **Design commitments §8 ("Uniqueness…") and Allocation §:** the clause *"Non-nesting prefixes — two siblings, two unrelated subtrees — … this case is T10 (PartitionIndependence), and it holds with or without the discipline"* is imprecise as attached to the sibling example. What holds discipline-free is only T10's *implication* (given non-nesting prefixes → distinct addresses, which needs only T3). But for two sibling *allocators*, the non-nesting of their prefixes is itself **produced by** the discipline (T10a.2 / the T10a-N necessity result): relax `inc(·,0)`-only and a sibling's child can equal the next sibling — e.g. with siblings `[6],[6,1]`, spawning a child of `[6]` gives `inc([6],1)=[6,1]`, colliding. Add a half-clause separating "T10's implication is discipline-free" from "sibling prefixes are non-nesting *because of* the discipline," so the example can't be read as 'sibling uniqueness needs no discipline' (everything else in the digest already insists the discipline is mandatory, so this is purely a wording fix). `[SHARPENING]`

2. **Durability §, child-respawn hazard:** *"the child can be re-spawned from the same parent slot and its base re-handed-out — the very 'never hand out an address twice' violation"* understates the danger. Re-creating the deterministic base is itself a duplicate allocation event (correct, by GlobalUniqueness's "distinct events → distinct addresses"), but the *damaging* collision is re-issuing the child's **already-advanced stream** (`c₁, c₂, …`) against references consumers have already persisted. The stated fix — journal the activation/base before handing out any child address, and carry child frontiers in the registry — covers both; name the stream-reissue explicitly so the reason child frontiers must be durable is unmistakable. `[SHARPENING]`

VERDICT: CONVERGED
