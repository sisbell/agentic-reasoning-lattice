## Overall

This is a strong, well-grounded digest. It correctly translates the note's hardest commitments into build terms — the reified-AST-not-closures fork, the symmetric-reverse-index-with-name-gating move (correctly Green-grounded), the BH4 store-`j`/read-`f_d`-fresh split, arithmetic-stays-inside-atoms, the immutable-index-root snapshot mechanism, and the "structural-reads-only is a discipline you wire in, not a theorem" framing. The `reach`→agent-time vs `self-emit`→emitting-surface distinction is a genuine catch most readers would flatten. The Green evidence (reverse lookup without opt-in; point-lookup granfilade; home-doc read from stored record; add-then-compare inside traversal) is used accurately and only where it actually supports a claim. The defect below is localized; most sections are sound.

## Revision list

**[DEFECT] "Design commitments → Forced," self-emit bullet — `is_doc` is misattributed to the P-tgt gate.** The digest says PL can state "the residence disjunct of that same P-tgt gate (via `is_doc` / the reflected `L_dom`)." This is false for `is_doc`. Per the note (QD-audit), `is_doc` expresses **P0** — the document/home-residence check `d ∈ dom(Σ.M)` the surface performs on every emit and every Nullify call — a *different gate*. Only the reflected `L_dom` expresses **P-tgt**'s residence disjunct `a ∈ A_rel^Σ`. Fix: separate them — "PL can state the home-residence check P0 (via `is_doc`) and P-tgt's residence disjunct (via the reflected `L_dom`); only P-tgt's *self-emit* disjunct is the surface's alone" — or drop `is_doc` from this sentence, since it grounds P0, not P-tgt. The surrounding takeaway (self-emit is the surface's own, not agent-time) is correct and should stand.

**[SHARPENING] Missing the "types are not a queryable domain" boundary.** The note commits, with dual-authority grounding (`Reg` is quantification-only; "a type census is middle-end work"), and **evidence Q2** directly vindicates it (Green has *no* read that enumerates, filters, or aggregates over link types; types appear only as caller-supplied selectors). The digest conveys the two *admitted* cross-type routes (V-IDX expansion; meta-arithmetic per-type sums) but never states the prohibition — no filter/fold over `Reg`, only `count(Reg)` is variable-free. Add this boundary alongside the ASN-0127 content boundary the digest already draws (it parallels it exactly: a `group-by-type`/`count-links-per-type` query is *not* a substrate predicate). This also puts an unused, on-point evidence consultation to work.

**[SHARPENING] "Set semantics for counting" is framed both as conventional and as forced.** It appears under "Conventional — not forced by the spec," yet "Guarantees → Hold by construction" derives it from "the domain interpretation (already a set)." Set-cardinality of a set-valued domain is forced (inherited from ASN-0086/0128 slices); what OQ2 actually *settled* — the note's resolution rather than a forced consequence — is that cross-type totals are meta-arithmetic sums, **not a new counting mode**. Reclassify: move set-semantics to "by construction," and keep the cross-type-sum resolution as the settled choice.

**[SHARPENING] The third inexpressibility conjecture and the three-axis relativization-cost analysis are under-captured.** The digest handles C-reach and C-emit but omits PC6's **parity candidate** (the third conjecture, OQ6) and the note's distinction that the three dropped restrictions fail *differently*: granularity (forces the base's stopping point), node-combinator gaps (benign — closable by V-PRIM-style admission without touching the base), and **new fold forms** (a computation class, not a combinator — closable only fold-by-fold). The digest's "no feedback/loop" covers the control axis; add the fold-axis exclusion to sharpen the extension guidance, since "don't admit arbitrary fold accumulators" is the non-obvious half.

**[SHARPENING] Over-specific tech references are unverifiable from the grounding sources.** `links.jsonl` + `paths.json` (State substrate) and "the `im`-crate persistent maps" (Snapshots) appear in neither note nor evidence and presume a particular stack/language. The underlying design pattern — append-only audit journal + derived recomputable active hint; immutable structurally-shared index roots as the version pin — is well-grounded (PD0 monotonicity + Green's append-only stores). Keep the pattern; generalize or cite the concrete names rather than assert them.

**[SHARPENING] "Only the comparisons are composable leaves" reads as broader than intended.** In the tumbler-arithmetic paragraph this is true *of address/span operations* (comparisons compose; `⊕` stays inside atoms), but stated absolutely it appears to exclude ℕ `+`, which V-PRIM *does* admit as a composable leaf (load-bearing for PC2a cross-type count sums). Scope the sentence to address/tumbler ops and note ℕ `+` as the one admitted composable arithmetic.

## Solid as-is

"What must be built," the evaluator section (streaming short-circuit + no feedback primitive), the dynamics-analyzer fusion (FP + PD0–PD2 in one pass, sound-but-incomplete certifier), and the snapshot/multi-read isolation analysis (access-count discriminator, obligation inherited not discharged) are all accurate and at the right altitude. No soundness violations found — the recommended approaches (unconditional symmetric reverse index gated by name, active-as-hint, stability-driven incrementality) each respect the note's commitments.

VERDICT: REVISE
