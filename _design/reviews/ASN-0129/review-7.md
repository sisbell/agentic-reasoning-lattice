I worked through the digest against ASN-0129, its claim statements, and the Green evidence. This is a strong digest: accurate on the load-bearing commitments, disciplined about altitude, and — where digests usually fail — meticulous on the proven/conjectured boundary. Two passages deserve explicit "do not touch" status because a reviser might be tempted to "simplify" them:

- The **PC6 / C-reach / C-emit / PC6a** decomposition is correct and subtle. It rightly calls PC6 a theorem, PC6a a proven grammar fact, and the three inexpressibility claims conjectures; and it correctly identifies the *asymmetry* of baselines — C-reach's baseline **is** PC6a (closure ⇔ fixpoint, so C-reach implies it by modus tollens), whereas C-emit's baseline is a *separate* read-off-V fact (self-emit is frontier arithmetic, not closure, so no-fixpoint has no bearing). This matches the note exactly.
- The **soundness discipline** is exemplary and should be preserved verbatim: memoization keyed to *syntactic identity + state-version, never semantic equality* (respects OQ3); the explicit refusal of value-based identity; the structural-reads boundary; and the use of Green's query mechanism (symmetric span-intersection) *without* inheriting Green's stale-superset spanfilade flaw (the digest upgrades it to a recomputable hint). No proposed approach violates a note commitment.

I found no material misreads, no ungrounded Green claims (every source-level claim — `movetumbler`, `tumbleradd`/`tumblercmp`, intersection matching, point-lookup store, no type/document census — is in the evidence), no altitude slips, and no missing *substrate-build* commitment or component. What remains are two genuine but non-load-bearing improvements.

## Revision list

1. **[SHARPENING]** *"What must be built — read-base adapter" (BH4 home-chain route):* the digest says home is "resolv[ed] out of the stored record rather than recomputing it from the address, **which is why home-grouping is no atom**." Two tightenings. (a) The note's own model derives home via **T4 field extraction from the address** (PC6: "the T4 field extraction behind `home`"); record-read is *Green's implementation pattern*, which the note cites approvingly. Frame the record-read as an optional denormalization/cache over an address-derived quantity, not as "the" resolution that replaces an impossible recompute. (b) Fix the causal claim: home-grouping is no atom because home stays **confined inside the base call and is never exposed as a composable atom** — true whether home is T4-computed or record-read — *not* because of the record-vs-compute choice. The digest's own "keep span arithmetic inside the leaf" section already states the correct reason ("keep both out of the base"); align the two.

2. **[SHARPENING]** *"How it fits — Hands to (above)":* the digest surfaces the recency/`max_{T1}` per-home edge and the PD1 active-view un-termination edge, but omits a third protocol-author edge the note surfaces in the worked trace — that **quiescence-shaped predicates (`OPEN(t)=∅`, `count=0`) are vacuously true at the empty store**, so a fire-until-quiescent loop can terminate *before any activity occurs*; a sound termination gate therefore needs an **activity witness** alongside the quiescence term (the note's audit companion `ever_res`, a PD0 `ST` term, is the worked example). Add this beside the recency edge — it's the same class of hand-off and rounds out the set the digest already partially surfaces.

Verdict rationale: both items are sharpenings — the affected build instructions (keep home inside the base and unexposed; supply the language and dynamics certificates to the protocol layer) are already correct and present elsewhere in the digest; neither item is a misread, a false load-bearing claim, or a missing substrate-build component.

VERDICT: CONVERGED
