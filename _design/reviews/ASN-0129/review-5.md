The digest is unusually strong: accurate to the note, well-grounded in the evidence, and at the right altitude throughout. Several things are done with rare care and should be left untouched: the **conjecture/theorem distinction** is maintained correctly everywhere (grammar facts PC6a / no-direct-spelling kept separate from the *semantic* conjectures on `reach`, self-emit, and parity; none promoted to theorem); the **content-dedup trap is avoided** (address-based identity affirmed, interning scoped to term structure as a recomputable hint, never store-value dedup); the **Green evidence is applied with the right split** (Green vindicates the coverage regime — add-then-compare inside traversal — and the symmetric reverse index, but the digest explicitly declines to claim Green for the denotation-side minimal-element index); the **Forced/Conventional split is correct**; and the framings "structural-reads-only is a discipline on adapters, not a theorem" and "snapshot isolation is inherited, not discharged" are sharp and right. I checked the subtle UV/`elems` length claim and the per-home `max_{T1}` claim against the note verbatim — both faithful.

I found no material problems. The revision list is sharpenings only.

**Revision list:**

1. **[SHARPENING]** *Implementation approaches → atom backing (BH4):* the digest gives `age(a) = f_d − 1 − j` (FP's *defined-case* formula) but never states that `age` returns `⊥` when `a` is not an active K-tuple, nor that `age(a) ≠ ⊥` doubles as a tuple-activeness test (V-PRIM). The note flags this totalization as **load-bearing** — a *partial* `age` would make definedness state-dependent (a retraction turning a defined term undefined) and poison PC4/PC5. The general principle ("there is no undefined evaluation") is present, but localize it: write the codomain as `ℕ∪{⊥}`, add the `⊥` case, and note the activeness-test use.

2. **[SHARPENING]** *Design commitments → Partiality guarded (and the matching guarantee):* surface two facts the note makes load-bearing about the binder guard — that it is **PL's only conditional former** (there is no general if-then-else), and that its else-branch may be **any PL term, including a state-reading one**, not just a constant. The note says the term-valued else is exactly "what keeps the value codomains (`T`, `ℕ`) as expressive as the Boolean one." As written, a builder could under-build the else to constants and silently lose value-codomain expressiveness. (It is recoverable from a faithful WT implementation, hence a sharpening, but it is worth making explicit.)

3. **[SHARPENING]** *Design commitments → read-only/residence checks:* "the surface runs … on every emit *except* an `idem=⊤` dedup hit" silently broadens the note's "every *admitted* `idem = ⊥` call (I5)" to *all* `idem=⊥` calls. Restore "admitted" so the claim about which surface operations perform home validation matches the note exactly.

4. **[SHARPENING]** *Dynamics section:* the note's worked trace draws a concrete lesson the digest omits — a quiescence-shaped predicate is **vacuously `⊤` at the empty store**, so a fire-until-`quiescent` gate fires before any activity exists; a sound termination gate needs an *activity witness* (e.g., an audit-class `ever_X` term that is `⊥` at the empty store). Surface this next to the PD1 oscillation warning. (Borderline scope — it is protocol construction — but the digest already gives protocol-checker guidance, so it fits, and it is a real pitfall.)

VERDICT: CONVERGED
