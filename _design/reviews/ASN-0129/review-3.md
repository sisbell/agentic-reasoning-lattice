Reviewed the digest against ASN-0129, its claim statements, and the Green evidence. It is a strong, disciplined digest — the conjecture/theorem bookkeeping and evidence-grounding are unusually careful. The defects worth fixing, most-important first:

**1. "What must be built" → read-base adapter: the V-TUP derivability claim is inverted and conflates two opposed results.**
The digest says `addrs_F/G` and `coverage_F/G` are "neither derivable from the other," citing "PrefixSpanCoverage / I0a's separating pair" as why the coverage tests must be their own leaves. The note states the *opposite* asymmetry: coverage-membership **is** derivable from denotation (`t ∈ coverage(e) ⟺ ∃ y ∈ addrs(e): y ≼ t` — this *is* PrefixSpanCoverage), while denotation is **not** recoverable from coverage (I0a fixes the denoted set only up to its ≼-minimal elements) — "which is why `addrs_F` and `addrs_G` are V-TUP primitives." So I0a is the reason the *denotation projections* are irreducible; PrefixSpanCoverage points the other way and actually refutes "neither derivable." The coverage tests are derivable from `addrs + ≼` and are admitted as primitives only to keep the base's span arithmetic inside the leaf (PC6 granularity). Keep "expose both regimes," but rewrite the rationale: `addrs` irreducible by I0a; coverage tests admitted-but-derivable for granularity; the PC6 converse needs *per-tuple* tests (atoms conflate per-tuple structure), with `addrs` the genuinely irreducible one.

**2. PC2a / aggregates: the T1-extrema scoping caveat is dropped.**
The note makes this a commitment co-equal with set-semantics counting ("Two commitments ride on PC2a"): `max_{T1}`/`min_{T1}` recover emission order *only within one home's chain*; cross-home T1 comparison is well-defined but *temporally meaningless*, and any "latest" built from it inherits the per-home scope. The digest ships T1-extrema as an aggregate but never carries this — a protocol author wiring a "most-recent-link" trigger with `max_{T1}` across homes gets silently meaningless results. Add the caveat where extrema are introduced, and flag it at the protocol hand-off (recency triggers are exactly the construct that hits it).

**3. "What must be built" → read-base adapter: the BH4 home-chain read route is missing.**
PC4 names three read routes; the adapter bullet lists two (Observe_K/active-subset machinery; store-domain reads for `L_dom`/`is_doc`) and folds BH4 into "slice enumeration." `age`/`stale` are a *distinct* mechanism — home-chain frontier arithmetic over `dom(Σ.L)`'s homed segments, requiring `home(a')` resolution, which (per the Green evidence) is read out of the stored record via `movetumbler`, never recomputed from the address, and is why home-grouping is no atom. Add it as the third base read so a builder doesn't try to serve `age`/`stale` off the slice index.

**4. Design commitment 1: "safety comes from inexpressibility" mislabels the keystone.**
Purity (PC4) and termination (PC5) are *closure properties every term has*, not inexpressibility results — the language is closed under formers that preserve them, it doesn't "fail to express" impurity. The keystone is: closed under safe formers (every term provably pure/terminating/structural by induction) **+** lacks unsafe formers (no fixpoint PC6a, no foreign-code injection, no content read) **+** the substrate runs no foreign code — hence no runtime guard. Reserve "inexpressibility" for the ceiling (PC6a's no-fixpoint and the reach/self-emit conjectures). The proven-vs-conjectured split the parenthetical draws is right and should stay; just stop relabeling PC4/PC5 as "inexpressibility."

**5. Design commitments → Partiality (PC2): the guard's else-branch can read state, not just a constant.**
The note stresses `c_default` is "any PL term of codomain C₂," and that a *term-valued* (state-reading) else is what keeps the value codomains (`T`, `ℕ`) as expressive as Boolean. "Eliminated only through an explicit binder guard behind a definedness test" reads as a constant default; a builder must allow an arbitrary term in the else-branch. Worth also stating once, for the term representation, that this guard is PL's *only* conditional former.

**6. Implementation → read base: the `dom(Σ.C)` exclusion is grounded on the wrong layer.**
The digest grounds "no content-domain read" via Green's "address exists ⇒ content exists" invariant — but that is an *emit/link-creation*-path fact. The note grounds the *PL read* exclusion as a layer boundary: no contract consults `dom(Σ.C)`; content is reached only through arrangement, which is ASN-0127's territory ("an agent-time operation rather than a substrate predicate"). Lead with the layer boundary; keep the Green invariant as supporting color for why no occupancy check was ever needed *anywhere*. (Leave `is_doc`-membership-only — it is correctly grounded by Green's point-lookup granfilade.)

---

Genuinely solid, leave alone: the theorem/conjecture discipline (PC6 vs C-reach/C-emit/parity, OQ6) is precise throughout; every Green source-level claim (append-only granfilade, symmetric intersection index with no per-type opt-in, point-lookup store, `tumbleradd`/`tumblercmp`/`movetumbler`) is cited to the evidence with none fabricated; the footprint-indexed-dispatch soundness warning — that the win is sound *only if* the footprint honors FP's cross-type cases (BH4 home-wide, `targets_keyed`, active + `L_R`, default + BH1) — is exactly right and is the digest's best single contribution; and altitude is controlled (Green function names appear only as grounded illustration, never as signatures for the new system).
