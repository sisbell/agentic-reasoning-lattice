The digest is strong overall — accurate on the great majority of the note's commitments, carefully grounded in the evidence answers (the Green claims all trace to Q1–Q3: symmetric reverse index, add-then-compare-inside-traversal, home read from stored crum, point-lookup-only document store, no type aggregation), at design altitude throughout, and genuinely actionable. The forced/conventional split, the by-construction/require-enforcement split, and the three-axis ceiling analysis are all faithful and useful. One factual error and a few enumeration gaps remain.

**Revision list (most important first):**

1. **[DEFECT]** — *Design commitments → Forced → "no fixpoint operator":* the claim that the home-residence gate "runs on **every emit** and every Nullify call" is false, and mislabels the emit-side gate. The note is explicit that emit-side home validation is *branch-local*: it runs on every `idem=⊤` **miss** (I1), every admitted `idem=⊥` call (I5), and every Nullify call (P0) — but an `idem=⊤` dedup **hit** "never reads `d`" (QD-audit; and the spec's own dedup-hit investigation, evidence Q3). "Every emit" overcounts and erases a deliberate fast-path the note flags in a parenthetical. Also, `P0` is the note's label for the *Nullify* precondition specifically; the emit-side check is I1/I5, not P0. Fix to: "a residence check the surface runs on every Nullify call (P0) and on every emit *except* an `idem=⊤` dedup hit (I1, branch-local: a hit returns the existing address without reading `d`)."

2. **[SHARPENING]** — *What must be built → Base read adapters:* the V-PRIM enumeration "(address comparisons, finite-set ops, ℕ arithmetic, definedness tests, constants)" drops two admitted primitives the build actually needs — the per-class **map lookup `·[K] : Map_fin → T∪{⊥}`** (the only consumer of the `targets_keyed`/BH3 `Map_fin` join that the approaches section tells you to materialize) and **`elems : Seq_fin→℘_fin`** (count-faithful on `chain`, so `count(elems(chain(t)))` reads the walk length). Add both — unlike the per-type atoms, these are *this note's* V-PRIM admissions and aren't backstopped by "thin adapters over ASN-0128."

3. **[SHARPENING]** — *What must be built → three-view slice readers* (and the conventional "fixed-view slices `A_K`/`L_K`" remark): the digest flags `A_K`/`L_K` as fixed-view but never extends the point to the **behavior atoms**. State that BH1–BH4 are fixed-view too — they read their active slices at *every* term view and receive **no audit reading** (V-AUD), so the term view rebinds only the core atoms and `M_K`. (It's implicitly covered by "atoms are thin adapters over ASN-0128," but spelling it out prevents a builder from trying to thread an audit slice into a behavior atom that has none.)

4. **[SHARPENING]** — *Implementation approaches → age/staleness:* "storing `j` alone is wrong" reads as contradicting the immediately preceding recommendation to store `j` with the tuple. The real point is: store the immutable `d` and `j`, but never cache the *computed* `age` (= `f_d − 1 − j`), which is live because `f_d` grows. Reword to target cached-age, not stored-`j`.

VERDICT: REVISE
