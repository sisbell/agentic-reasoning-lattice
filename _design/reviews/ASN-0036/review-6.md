I reviewed this digest hard against the note, the claims, and the evidence — looking for a misread, an ungrounded Green claim, a sound-violating approach, a missing commitment, or an altitude slip. I found none that bites a builder. This is a careful, well-grounded digest; the items below are refinements, not corrections.

What it gets right and I want to credit explicitly:

- **The S5 reading is exactly correct and is the subtlest point in the note.** The digest does *not* claim the formal core forces unbounded sharing; it correctly identifies S5 as a consistency/licence existence-claim (no cap is *entailed*, a cap could be bolted on) and pins the actual mandate on Nelson's intent. Most digests get this wrong. This one nails it, and flags it as the one exception in the "Forced" list.
- **It catches the CAS/S4 trap proactively** — content-addressed storage as the *identity* model is forbidden by S4, legitimate only as a dedup layer beneath the authoritative address→value map. That is precisely the "does an approach violate a commitment" check, self-applied and correct.
- **Grounding discipline is strong.** Every Green source-level claim (`levelpull` stubbed, `isanextensionnd` write-time coalescing, two-blade knife at `(N+1).1`, `acceptablevsa` stub, `findnextlinkvsa`, spanfilade, `span_count: 1`, text-under-`doc.3.x`) traces to the evidence answers; the `doc.3.x`/note-`…1.3` numbering mismatch is flagged honestly rather than papered over. The note/Green contiguity boundary is kept clean: D-CTG/D-MIN/D-SEQ are note-mandated *for text only*; link-subspace contiguity is correctly attributed to Green-observed behavior (Q5), not the note.
- **The "kind from I-address structure, not V-subspace" recommendation is genuinely sharp** — it correctly leverages that the note's alignment condition makes the two sources agree *only where alignment holds*, and that S5 multi-referencing plus unenforced alignment makes the intrinsic address the reliable source.
- **Soundness of the no-canonical-form, opportunistic-coalesce-plus-recompute-on-demand stance is correct and Lampson-aligned**, and the contract boundary (positional queries representation-independent; run-structure queries must canonicalize first) is the right distinction.

# Revision list

1. **Design commitments, "Constrained but deferred" heading — `[SHARPENING]`** The heading "locked for the text subspace" mislabels its own contents. Only the D-CTG/D-MIN/D-SEQ bullet is text-specific; S8a (depth ≥ 2) and S8-depth (uniform depth within a subspace) apply to *every* subspace. The bullets themselves are correct (and the run-list and Guarantees sections both treat S8-depth as all-subspace), so a careful reader recovers the right scope — but a skimmer taking the heading as the classification could wrongly conclude depth-uniformity is text-only. Retitle to something like "Constrained but deferred — operations-layer obligations, not storage-enforced (text contiguity is the text-only subset)."

2. **Sharing inverse — `[SHARPENING]`** "generalizing the spanfilade's endpoint-indexing role in Green (which indexes link endpoints by fixed key)" slightly conflates the fixed role-keys (FROM/TO/THREE = 1/2/3, which index *within* a link object) with the spanfilade's actual job (indexing content spans → the links that reference them). The recommendation — a rebuildable `I-address → {(d,v)}` inverted index treated as derived state — is correct; tighten the gloss to "the spanfilade's role as Green's reverse index from content to referencing links" so the analogy points at the right structure.

3. **Design commitments, "An arrangement's canonical run structure is always recoverable (S8)" bullet — `[SHARPENING]`** This sits under "Forced — downstream design cannot violate," but S8's uniqueness is a *mathematical guarantee about the abstract map*, not a commitment downstream code could "violate." The bullet's own prose already self-corrects ("a guarantee about the map, not a mandate on storage… the formal license for run/enfilade-style storage, not a requirement to use it"), so this is purely a framing nit — consider relabeling it as a *guarantee/license the system provides* rather than a *constraint downstream cannot violate*, to match the prose.

Every other section — content store (journal + recomputable index hint + no-GC + no-logical-write-conflict), run representation and canonicity, allocation/attribution, recovery/snapshots, contiguity/alignment, the Guarantees and Decisions tables — is accurate, grounded, at the right altitude, and actionable. The open questions are correctly mapped to builder decisions (Val typing, sharing-inverse cost, S3 timing, reachability, depth `m`, alignment). No load-bearing commitment, component, or guarantee is missing; no approach violates a note commitment; no claim is ungrounded.

VERDICT: CONVERGED
