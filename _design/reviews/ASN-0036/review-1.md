The spine is sound — the forced-vs-conventional split is accurate (S0–S5, S7, D‑CTG/MIN/SEQ correctly forced; m‑depth, subspace‑alignment, and run‑canonicality correctly deferred), the content‑addressing trap correctly enforces S4, the append‑only‑journal + hint‑index framing is right, and the by‑construction‑vs‑active‑enforcement split is well done. The defects cluster in the udanax‑grounded recommendations and one tradeoff.

**VERDICT: REVISE**

1. **Impl. approaches ("Canonicality… (Q3)"), Guarantees ("…given Q2"), How‑it‑fits, Decisions — the "Q2/Q3/Q5" citations misrepresent the note's open questions.** The note's open questions in order are Q1=Val, Q2=sharing‑inverse cost, Q3=when‑S3‑holds, Q4=reachable‑vs‑unreachable, Q5=editing‑ops‑preserve‑contiguity, Q6=m‑depth, Q7=subspace‑alignment. The digest pins canonicality to "Q3" (note Q3 is S3 visibility; canonicality is not an open question at all), `acceptablevsa`/alignment to "Q2" (that is note Q7; note Q2 is the sharing‑inverse cost), and calls them "settled by evidence" / "Q2 confirms" / "Q5's finding" — yet the note lists every one of these as **open**. The digest even contradicts itself: "Decisions for the builder" correctly treats S3‑visibility as the open Q3. Fix: drop the "Q#" labels (or cite the real external source by path), and stop describing the note's open questions as settled — present these as the digest's own engineering judgment.

2. **Impl. approaches / Guarantees — the specific udanax‑green source‑code claims are ungrounded and likely fabricated.** `levelpull` stubbed, `recombine*` "merges at most one pair per pass," `isanextensionnd` write‑time‑only coalescing, `acceptablevsa` "returned TRUE unconditionally," `ispanset2vstuffset` "assume alignment," and the "two‑blade knife" appear nowhere in the note (whose only udanax grounding is enfilade *types*, the 17 MODIFY‑free FEBE commands, and the `s.x` V‑address form) and cannot be verified from the supplied materials. The recommendations they prop up are independently sound and the note already grounds them: non‑canonical storage follows from S8 proving only the *decomposition* unique (silent on representation); the per‑subspace shift bound follows from S8‑depth (subspaces are independent bands) + T1 order, not a "knife"; the alignment guard follows from Q7 making alignment an operations obligation. Fix: remove the unverifiable function‑level claims (or replace with a real, checkable citation) and re‑ground each recommendation on the note's own properties. (granfilade/permascroll/spanfilade are used correctly and can stay.)

3. **Impl. approaches (Displacement) — "the link subspace is itself contiguous‑append from 2.1 … per‑subspace contiguity machinery is reusable, not text‑only" contradicts the note's scoping.** The note deliberately proves D‑CTG/D‑MIN/D‑SEQ for the **text subspace (S=1) only** and is silent on every other subspace (and never assigns links to subspace 2). Asserting subspace‑2 contiguity as a finding could lead a builder to wrongly enforce contiguity on link arrangements. Fix: state that the strand model guarantees contiguity only for subspace 1; if link‑subspace contiguity is asserted, attribute it to its real source as conjecture beyond this note.

4. **Impl. approaches (Trap — content‑addressing) — "identical‑bytes‑from‑independent‑writes is rare … payload dedup buys little" is unsound.** Under the note's element‑level storage (S7b: every content address has `zeros=3`; the worked example stores one character per address), equal values across independent allocations are the *common* case — and S4's own motivating example is "two independent writings of the word 'hello'." The stated rationale is therefore false and anti‑thematic. The "skip it initially" conclusion can stand, but on different grounds (units too fine‑grained for dedup metadata to pay; deferrable optimization). Fix: drop the "rare/buys little" reasoning and re‑justify the defer.

Minor (non‑blocking): POOM is cited for both the flat map (a) and the enfilade (b) — POOM *is* the enfilade, so the (a) attribution is loose. S7b (element‑level granularity, `zeros=3`) is load‑bearing for `origin`'s well‑definedness and the store's key shape but is never surfaced. "Links occupy subspace 2" is not from this note — attribute or verify.
