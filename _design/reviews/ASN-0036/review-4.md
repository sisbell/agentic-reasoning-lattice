Reviewed the digest against the note, the formal claims, and the four evidence answers. It is a strong digest — accurate on the core architecture, careful with the S5 "forced vs. intent" distinction, and sound on the load-bearing soundness check (correctly forbidding content-addressed *identity* under S4). The defects below are real but mostly local. No tool calls needed; this is an analysis of the provided text.

## Revision list (most important first)

1. **Implementation approaches → "The arrangement": D-CTG/D-MIN/D-SEQ are over-scoped to "each subspace."** The line "contiguity (D-CTG/D-MIN/D-SEQ) and depth-uniformity hold by construction within each subspace" mis-reports the note. The note states D-CTG, D-MIN, D-SEQ **only for the text subspace (S = 1)** ("the contiguity properties below are stated for the text subspace") — they range over `V_1(d)`, not arbitrary subspaces. Your commitments section gets this right ("Text V-positions are a gapless 1..n block"); the implementation section then silently generalizes. Fix: scope the D-CTG/D-MIN/D-SEQ citation to text, and ground *link-subspace* contiguity in the **evidence** instead (Question 5: links append-only and contiguous from `2.1`) — the per-subspace run-list does deliver contiguity everywhere by construction, but for non-text subspaces that is Green-observed/implementation-provided, not note-mandated. This both fixes the scope and strengthens grounding.

2. **Implementation approaches → "The arrangement": drop "the `im` crate."** Naming a specific Rust library is an altitude slip below design level and assumes a language the note does not support. The underlying recommendation — persistent/structurally-shared structures so each edit shares structure with its predecessor and delivers cheap historical versions — is well-grounded (it's the mechanism behind Nelson's "historical backtrack"). Replace the crate name with the generic mechanism ("a persistent, structurally-shared map/tree via copy-on-write path sharing"). Keep the rationale; lose the library.

3. **Design commitments → "Attribution is structural": "S7c" is not a property in this note.** The note defines S7a, S7b, S7d, and S7; the discrimination guarantee `origin(a₁) ≠ origin(a₂)` is **S7 postcondition (c)**, not a standalone "S7c." The substance is correct and grounded, but a builder/reviser checking the note for "S7c" finds nothing. Re-cite as "S7 postcondition (c)."

4. **Design commitments → "Forced" list: S8 is a representation-independent guarantee, not a storage mandate.** "An arrangement is structurally a finite set of correspondence runs (S8, S8-fin)" sits under the header "Forced — downstream design cannot violate these," which implies a *storage* constraint. But S8 is a theorem about the abstract map `M(d)` — the maximal-run decomposition *exists and is unique* — and is satisfied equally by a per-position map (which you yourself recommend as the prototype representation). The item is well-worded internally ("license for run/enfilade-style storage"); it's the header that overstates. Reframe what's forced as the *guarantee* (you can always recover the unique canonical run structure on demand), explicitly noting that storage representation stays free — this also removes the apparent tension with your later "per-position map is fine for a prototype."

5. **Content store → "`Val` … untyped" and the Decisions "Content typing" line: reconcile Green's `doc.3.x`/`doc.2.x` with the note's worked example.** The "recover kind from the I-address's own structure" recommendation is sound and the principle (kind is intrinsic to the address, hence alignment-independent) is the right one. But the specific numbering you cite (`text under doc.3.x, link orgl under doc.2.x`) is **Green's** scheme from the evidence, while the note's worked example puts text element fields under `…1.x` (e.g., `1.0.1.0.1.0.1.3`). State the transferable principle and mark the `2.x`/`3.x` numbers as Green's instantiation, so a builder cross-referencing the note's own worked example isn't tripped by the mismatch.

6. **Subspace-alignment: reconcile your "boundary check" recommendation with the evidence's "precondition on every operation."** You recommend boundary-check + blessed paths and list per-operation precondition as an alternative; the evidence's stated conclusion is that the spec "must model subspace membership as a **precondition on every V-position-accepting operation**." These aren't actually in conflict — one is a proof obligation (how the spec models it), the other an enforcement site (where you physically check). Say so explicitly, so the recommendation doesn't read as overriding the evidence: model it as a per-operation precondition; discharge it by construction (blessed paths) plus one boundary check on untrusted input.

## Solid — no change needed
- The S0/S1 + S4 analysis, especially relegating content-addressed storage to a *sub-identity* dedup layer that "must never surface as identity": this is the exact trap the note's S4 sets, and you caught it cleanly.
- The canonicity stance (opportunistic write-time coalescing + canonicalize-on-demand, no global re-merge, query-interface confluence) is faithfully grounded in the Question 3 evidence and correctly leans on S8's uniqueness to license recompute-don't-store.
- The S5 "Nelson-mandated, formally cappable" reading is precisely right: S5 is a non-entailment/consistency result over models, so the no-cap mandate genuinely comes from intent, not the formal core. Good restraint.
- Origin-as-computation-not-table and contention-free prefix-partitioned allocation are accurate to S7a/S7b/S7d and correctly justified.
