The digest is unusually faithful and well-grounded. The idem/dedup core (I0/I1/I1a), the DR/sterilization safety argument, the AM forward-exact/reverse-coverage asymmetry, the born-nullified C2-vs-C3 split, and the concurrency analysis (dedup partitions by coverage-class, *never* by home) are all read correctly and at the right altitude. Every Green claim is grounded in the evidence answers — including the honest Q8 "client-side convention, not backend-enforced" correction and the explicit "replay-on-load is our recommendation, not Green's mechanism" caveat. Soundness of approaches is clean: no proposed approach violates a note commitment without the digest flagging it (registry-as-content→R1, per-home dedup→I1a both correctly warned). One material gap, then sharpenings.

**1. [DEFECT] "What must be built" / behavior sections: the R-C0 compatibility constraints and the three standard-registration triples are referenced but never stated — and a builder needs both.** The validator item says "every behavior compatible with shape and idem *per the table*" but the table's contents are absent, and the behavior descriptions drop each behavior's "Applies to" entirely. State them: read-filter→Unary; determinate-walk and typed-reverse-lookup→Binary; age-staleness→idem=⊥ (no shape clause). This is load-bearing with a correctness consequence — the BH4↔idem=⊥ rule is exactly what keeps age-staleness off an idem=⊤ type, where re-emission is a dedup hit (no fresh chain slot) so `age` would count from first utterance and `retract_stale` would retract just-renewed assertions: the lease bug the note flags as the *forced* reason for the clause. Likewise the three pre-seeded triples are never given in full (retired's shape and idem aren't even derivable from the digest): state retired = Unary/idem=⊤/{read-filter}, supersedes = Binary/idem=⊤/{determinate-walk}, R = Binary/idem=⊤/{}. The registry builder must *enforce* the former and *pre-seed* the latter; the digest transcribes comparably concrete facts everywhere else, so this is a gap, not an altitude choice.

**2. [SHARPENING] BH4 section: present idem=⊥ as *forced*, with its reason, not just a constraint to enforce.** The note derives it — age is per-address, and under idem=⊤ the address is pinned to first emission, so renewal-by-re-emission cannot restart the clock. A type designer building a lease needs that rationale to see why "renewal = re-emission" only works under idem=⊥. (Pairs with #1, which supplies the bare constraint.)

**3. [SHARPENING] "Sameness is coverage equality" → supersedes consequence: tighten "keyed on `F = old-version`."** The dedup key is the full pair `(coverage(old), coverage(new))`; the F-only phrasing, read literally, would predict that same-old/different-new claims dedup — but those are precisely the *branch* case (`succs = {v3, v3'}`) the digest relies on in the BH2 walk. The real point is slot-located: supersedes' F carries *content* (old-version), not the proposer — unlike R, whose from-fill folds the proposer into F — so two proposers asserting the *same edge* share both F and G and collapse. Saying it that way also makes the "fold the author into F" escape land harder.

The remaining sections (default predicates, views/selector, retraction wrapper, born-nullified handling, age denomination, durability/checkpointing, the decisions-for-builder forks) are sound and need no change.

VERDICT: REVISE
