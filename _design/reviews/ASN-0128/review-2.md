I verified the digest's repo-specific claims directly. There is **no `links.jsonl`, no `paths.json`, and no "worker-buffer / orchestrator-flush" pattern** anywhere in the project tree — the only `orchestrator`/`worker-buffer` hits are inside `node_modules` (the CLI's own bundle), and this is a spec repo (`_docuverse/`, `prompts/`, `scripts/`, design-note `.md`s), not an implementation with a journal substrate. That makes one of the digest's framing claims factually false, which drives the top of the list.

---

# Revision list

1. **Implementation approaches (opening) & throughout: strike the fabricated local-repo substrate.** The digest asserts "the proven [substrate] you already have it: … this repo's `links.jsonl` + `paths.json` registry," names "this repo's worker-buffer-then-orchestrator-flush pattern" as the per-home realization, and calls per-operation journaling "the repo's substrate." None of these exist here (verified by search). This is asserted as present fact and anchors the whole approaches section. **Do W:** delete every "this repo / you already have it" attribution; reframe as a *recommendation to build* an append-only journal of deposits recovered on load, grounded where it is legitimately grounded — udanax-green's permascroll/granfilade and the note's append-only `L` (L12 immutability). The substrate-agnostic engineering content (derived hints, prefix trie, antichain key, spanfilade) is sound and should be kept; only the false "already have it" grounding must go.

2. **BH3 (Typed-reverse-lookup) approach: "udanax's split is the same" misreads the evidence and inverts the note's own point.** The digest says the two AM index shapes — "forward keyed by exact source address, reverse by target subtree" — match udanax's "content-keyed reverse index plus per-link forward-by-position." But evidence Q4 is explicit that udanax's `find_links` matches the from-side *and* to-side **symmetrically by content/I-address intersection** ("no asymmetry between from and to in the matching rule"), and the note's AM uses exactly this fact to argue the forward-exact keying is the *surface's deliberate departure* from the substrate, not something inherited from it. `follow_link` (read a known link's endsets by position) is also not the analog of `succs(x)` (find target-tuples by source address) — answering `succs` in udanax goes through the symmetric content spanfilade, not the per-link POOM. **Do W:** keep the reverse-index-via-spanfilade recommendation (that part is correct and well-grounded), but drop "udanax's split is the same" and instead state that the forward-exact / reverse-coverage asymmetry is the note's surface-layer choice *over* udanax's symmetric matching — which is precisely why the forward adjacency index has to be built deliberately and "does not come for free."

3. **Registry approach (bootstrap-document option): surface the R1-isolation constraint, and weaken the "exact precedent" framing.** Recommending "store types as content references … under one storage mechanism" without qualification invites a builder to place registry entries in the same `L`-space the operation surface reads and writes — at which point Emit/Nullify could mutate a registration and **R1 (registry constant at every reachable state)** falls, dismantling the immutability every invariance/stability proof rests on. **Do W:** add that the bootstrap-document realization must keep the registry subspace *outside the reach of the operation surface* (reserved, never an Emit/Nullify target) to preserve R1. Also temper the precedent claim: the digest already notes the code is "mixed," but Q8's code exploration is stronger than "mixed" — it finds **no protected backend type registry at all** (named-type constants live in the Python *client*), so the bootstrap-registry precedent is largely a KB-synthesis artifact, not backend behavior; present it as a design option, not a proven pattern.

4. **Concurrency / Decisions: "the idem-⊤ dedup … cannot be sharded" is over-strong.** The load-bearing claim — *you cannot shard dedup by home* — is correct and valuable (different-home same-coverage calls would both miss and both deposit, breaking I1a). But "a single global atomic critical section regardless" / "cannot be sharded" overstates it: dedup only ever compares *within* an I0-class, so it can be partitioned by **coverage-class hash** (same class → same shard → serialized) while staying sound. **Do W:** rephrase to "cannot be sharded by home; partition only by coverage-class if you need dedup throughput" — preserving the real warning without forbidding the standard scaling move.

5. **Concurrency vs BH4: warn that the single append point's order must not become the age denominator.** The digest recommends a single global append point ("a clean total order") for serialization *and* separately says "add no global counter" for age — both correct, but a builder has the global serialization index physically in hand and will be tempted to use it as `age`. That silently replaces the home-relative staleness the spec promises (BH4) with a global clock the note forbids. **Do W:** add one sentence: the serialization total order is for ordering `→_sh` steps only and must never be exposed as a cross-home age/staleness denominator.

6. **Implementation approaches (opening): don't cite udanax as "recovered by replay on load."** Per Q7/Q6, udanax persists via session-shutdown enfilade snapshots (`writeenfilades`), not journal replay — and the digest's own Durability item says exactly this ("over udanax's session-shutdown snapshot"). The opening contradicts it. **Do W:** present replay-on-load as the digest's (good) recommendation, distinct from udanax's snapshot mechanism; udanax grounds *append-only/monotonic addressing*, not the recovery method.

7. **Emit surface (What must be built): add the caller-facing portability contract.** The digest gives the implementation rule ("validate `home` only on the branch that reads it") but omits the note's derived *caller* contract: because invalid-`d` admission is state-dependent (it turns on whether a hit exists, which the caller can't evaluate), the portable contract is to **supply `d ∈ dom(Σ.M)` unconditionally**. **Do W:** state that as the API guidance alongside the implementation rule — it's the half a builder designing the caller side needs.

8. **AD nonconforming-tuple verdict under-surfaced for the raw-path decision.** The raw-deposit decision lists "non-denoting spans" as a complication but never states the note's actual rule: an off-surface tuple with a non-unit-depth span is **omitted from address-typed enumerations** (a non-denoting span names no address) yet **remains fully visible to membership predicates and raw `Observe_K`** (which read coverage). That split is what a raw/import path must implement. **Do W:** add the verdict to the raw-path decision so it's actionable, not just named.

---

**Solid — leave alone:** the born-nullified analysis is genuinely sharp and should not be softened — it correctly isolates that DR vacates only **C3** (not C2), that no *non-R* deposit is born-nullified on the disciplined path, and that the lone residual surface case is the **self-emit retraction** (C2), with the right conclusion that no separate sterilization gate is needed because `A_K = L_K \ nullified` computes it uniformly. Likewise keep: the **minimal-antichain dedup key** grounded in I0a (a correct, coverage-faithful canonicalization, explicitly *not* value-keyed); the **audit-roots-for-nullification vs active-roots-for-filtering** distinction (correctly tracks which slice each reads); and the **"surface-routing is necessary but not sufficient — concurrent cross-home emits need global ordering"** point, which is a real builder insight that I1a's sequential-derivation framing hides.
