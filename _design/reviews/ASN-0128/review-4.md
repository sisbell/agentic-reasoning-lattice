This digest is unusually strong: it reads the note accurately, keeps Green claims grounded (and is in fact *more* careful than the note on the Q8 type-registry evidence, correctly downgrading "the precedent is exact" to a client-side convention with no backend enforcement), stays at design altitude throughout, and flags exactly where an approach would violate a commitment (bootstrap-doc registry → R1; home-sharded dedup → I1a/I4). I checked the load-bearing chain — I0/I0a coverage-equality and the minimal-antichain key, I1/I6 hit/miss and the coverage-typed POST, I2/I3 born-nullified, DR's C3-only vacuity, S3's P-tgt-as-rejection, AD/AM, BH1–BH4, the four-component/no-clock argument — and found no misread, no ungrounded claim, no altitude slip, no internal contradiction, and no missing load-bearing component or guarantee. The born-nullified treatment in particular is a genuine catch: it correctly refuses the tempting over-simplification "no born-nullified tuples" and lands on the precise "no *non-R* born-nullified on the surface path; wrapper self-emit retraction is the residual, and it falls out of `A_K = L_K \ nullified` rather than a separate gate."

What's left is polish, not repair.

**1. Revision list**

- **[SHARPENING] *Guarantees → Resurrection.*** The line "Resurrection … by construction, given dedup reads active-only" drops I2's caveat: resurrection is guaranteed *only where the home chain's next slots are unsterilized*. It holds unconditionally on the digest's surface-only default (DR makes sterilization unreachable), so the statement isn't wrong — but the caveat is exactly the kind of thing the "raw deposit / import" fork must respect, so name it where resurrection is asserted, cross-referencing the same sterilization bound the nullification section already develops.

- **[SHARPENING] *What must be built → registry builder.*** R-C1 is folded into the generic "coverage-class keys pairwise distinct." Call it out explicitly: the *three shipped* designated classes must be pairwise non-`~`-equivalent **and** must not collide with any app declaration; a colliding designation or declaration yields *no `Σ_init` at all* (no substrate), enforced at construction. This is a concrete construction-failure condition a builder codes, distinct from ordinary app-key uniqueness, and it interacts with the still-open multi-app merge protocol (OQ8) — worth surfacing rather than burying.

- **[SHARPENING] *Implementation approaches → de-duplication index.*** "O(1) dedup" is precise for the *lookup* (perfect canonical key → one probe) but glosses the canonicalization to the minimal-target antichain, which is O(k log k) in target-set size — trivial for Unary (`G = ∅`) and Binary (`|G| = 1`), real only for Multi with large `G`. Reframe as common-case-fast: O(1) probe on a key whose construction is dominated by a small endset in the shapes that matter. The takeaway (build the antichain index, scan only off-path) is unaffected.

- **[SHARPENING] *Default predicates / raw-deposit decision.*** The digest correctly states AD's verdict for non-denoting spans (omitted from address-typed enumerations, visible to membership and raw `Observe_K`), which is the *consequence* of the missing hypothesis. Make the hypothesis itself explicit once: the `is_K(addr) ⟺ ∃x ∈ members(·,active) : x ≼ addr` and the coverage-keyed `targets_under` equivalences hold only at **F-denoting states** (every active tuple's F address-denoting) — automatic on the surface path, false off it. This is what licenses serving the assertion-level query from enumeration data, and it's the precise reason the raw path "forces audit/active divergence."

Everything else — the forced/conventional split, the L7 slot-direction obligation, the spanfilade-for-reverse vs deliberately-built forward-adjacency asymmetry (correctly traced to AM departing from Green's symmetric matching), the per-coverage-class (never per-home) dedup sharding argument, the prefix-trie/spanfilade split sized to Binary-R's single-span confinement, and the "nothing here is authoritative except journal + manifest" framing — is sound and I'd ship it as-is.

VERDICT: CONVERGED
