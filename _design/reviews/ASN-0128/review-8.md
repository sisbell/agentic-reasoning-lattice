This is a strong, unusually careful digest. Before the list, the things it gets *right* that are easy to get wrong, so a reviser doesn't disturb them:

- The **C2/C3/born-nullified** treatment is precise and better than a naïve reading: it correctly states DR vacates only **C3**, that **no *non-R* deposit is born-nullified** on the surface (C2 free via `K ≁ R`, C3 vacuous), and that the residual born-nullified case is the **self-emit retraction** (C2 fails) — and that this needs *no separate gate* because it falls out of `A_K = L_K \ nullified`. Keep all of this.
- The **cross-home dedup** analysis is load-bearing and correct: don't shard the idem-⊤ check-and-deposit by home; partition by I0-class. The home-locality of frontier/journal/non-idem emits vs. the cross-home requirement of dedup is exactly right.
- **Grounding discipline** is good: every Green claim traces to the evidence (append-only/monotonic addressing, single-pass link query, symmetric from/to matching, no persistent clock, no cross-home order, single-threaded loop), and it *correctly hedges* the bootstrap-registry "precedent" against Q8's code exploration (client-side convention, no backend enforcement) rather than repeating the note's over-strong claim — and flags the registry-subspace isolation as the digest's own invention. The **antichain key from I0a** and **single-address-source → dedup-key soundness** are sound.

## Revision list

1. **[SHARPENING] "What must be built → The default predicates": `targets_under` is listed among substrate must-build items, but D3 makes it app-side.** The list ends "...plus the derived assertion-level forward composition." D3 calls `targets_under` "an app-side composition *rather than a fourth shipped predicate*," with its selectors "part of the recipe" left to the app. State that the substrate ships only D1/D2/D3, and that `targets_under` is an app-side composition over those primitives — shipping it into the substrate would prematurely fix the active-vs-default selector choice D3 assigns to the app. (The digest already calls it "derived," so this is a placement/scoping fix, not a substantive one.)

2. **[SHARPENING] "Design commitments → Sameness is coverage equality": the `supersedes` "slight tension" is slightly mis-scoped, and the escape doesn't apply to `supersedes` itself.** The note's "owned links that accumulate / admits branching" language is about *competing* claims (same-old/different-new — the branch case `succs = {v3, v3'}`), which idem=⊤ does **not** collapse; only *identical cross-author* claims collapse. Scope the tension to that. Also note the escape ("fold author into F, or register idem=⊥") is for *app-registered* directional Binary types — `supersedes` ships locked, and folding author into F would break BH2's requirement that F denote the version-vertex. So for `supersedes` the identical-claim collapse is an accepted property, not a fixable one.

3. **[SHARPENING] "Implementation approaches → Read-filter / Nullification": make the view-source contrast explicit so a builder doesn't unify the two prefix indexes.** Both `is_filtered` and `nullified` are prefix-containment tests, and the digest states each view correctly in its own section — but only implicitly. Spell it out: the **filter-root index is built from the *active* view** (retracting a filter mark un-filters it), while the **nullification-root index is built from the *audit* slice** (a since-nullified retraction still nullifies — R6b reads `L_R`, I3). These cannot share one index.

4. **[SHARPENING] "Design commitments → Conventional, not forced": the wording "that the three ... ship pre-registered ... is forced — the note's resolution of ASN-0126 OQ4, which left open whether anything ships" reads awkwardly.** Resolving an open question is a choice, so "forced" invites a "no-alternative" reading the very next clause undercuts. The digest's actual split is correct (names + slot-direction conventional; shapes/idem/behaviors determined once you ship). Re-phrase as "*locked* by the note's OQ4 resolution" (a decision now binding on every substrate), reserving "forced/determined" for the registration parameters given that decision.

VERDICT: CONVERGED
