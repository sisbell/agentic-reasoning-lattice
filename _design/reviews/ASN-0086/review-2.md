This is a strong digest: the design commitments are accurately read, the forced/convention labels are all defensible, the altitude holds (data-structure choices, no code), and the udanax-green grounding is uniformly sound — every source-level claim (stateless query-and-increment freshness, no cached frontier, no contiguity/T4 dependence for freshness, cross-home contamination as the lone descendant path, coverage/address-range type matching, multi-entry `CREATELINK` fan-out, *no* retraction op in Green, single-threaded isolation) traces cleanly to the evidence answers, with no fabricated function-name or source-behavior claims. The revision items below are mostly sharpenings; two rise to real defects.

**Revision list (most-important first):**

1. **Implementation approaches → Coverage paragraph: the routing of nullification's `a ∈ coverage(G')` to "the reverse index's job" is wrong under the digest's own recommended design.** Under the recommended unit-depth discipline + P-tgt + R0a, every retraction names exactly one target whose covered `A_rel`-intersection is the singleton `{target}` (R-Scope) — so nullification needs *no* coverage-containment search: it is a direct target-keyed tombstone insert, and "is x nullified?" is a plain set lookup. This directly contradicts the later nullified-set paragraph, which (correctly) realizes nullification as a tombstone, not a reverse-index query. Fix: route only `Observe`'s `F̂ ⊆ coverage(F)` to the reverse index; state that nullification-containment collapses to the tombstone under the discipline, and that a `coverage(G') ∩ A_rel` containment computation is needed for nullification *only* in the undisciplined wide-span fallback.

2. **Guarantees / Recovery: a load-bearing capacity consequence is missing — logical retraction is permanent (R6a) and the audit slice is monotone (R3), so the tombstone set and journal grow without bound and admit no compaction of retracted material** (a retracted tuple stays load-bearing for audit and can never be dropped). The note flags exactly this (open question: cardinality bound of `nullified(Σ)` relative to `dom(Σ.L)`). The recovery paragraph discusses checkpointing only for replay cost — which compacts startup time, not the logical footprint. Add: footprint grows monotonically with emissions+retractions; checkpoints bound recovery cost, not storage, and there is no reclamation path.

3. **What must be built → Observe: the pattern domain is `T` (arbitrary tumblers, ghost/unallocated addresses included), not `A^Σ`.** The note makes this deliberate so a query can target ghost addresses, with the match `F̂ ⊆ coverage(F)` decided per-span by T2 membership. The digest describes the pattern only as "a from/to coverage pattern" and attaches ghost-permission solely to `[R]`. State that `Observe` patterns are unrestricted tumbler sets (not limited to live addresses) — this shapes the query API.

4. **Weakest-precondition / Guarantees: surface wp Case 1's actual result, not just P-tgt enforcement.** The note proves the weakest precondition for single-tuple scope *coincides* with the operation's own precondition `P0 ∧ P-tgt` — i.e. that precondition is not merely necessary but *sufficient*: every legal `Nullify` attains single-tuple scope and no legal call fails to, so no runtime check beyond `P0 ∧ P-tgt` is needed for scope correctness. Add one line stating this tightness; the digest currently lists P-tgt as an obligation without conveying that it is the complete condition.

5. **Decisions → Multi-arity: add R-Scope's arity-independence.** wp Case 1 does not constrain `|Σ.L(a)|`: `Nullify` may target any link address including higher-arity non-triples, and nullifying a non-triple is harmless (it enters the tombstone set but indexes no `L_K` tuple, so affects no `A_K`). This tells the builder `Nullify` need not validate target arity and complements the multi-arity-serving decision already surfaced.

6. **Guarantees, "born-nullified" line (minor): tighten the qualifier.** "Never accidentally born-nullified by a pre-existing retraction" covers wp Case 2's third conjunct; add the complementary escape — a retraction-typed (`K ~ R`) emission whose to-set does *not* cover its own address (e.g. empty to-set) is *not* born-nullified (the second-conjunct escape the note marks non-redundant). So "retraction-typed" does not imply "born inactive."

**Genuinely solid, leave as-is:** the allocator treatment (derive `a_emit` as a recomputable hint, query-the-max default, scope-to-home framed as the antichain-preservation job) and the tombstone realization are the best parts and need only items #1/#2. The two-decisions-over-coverage framing (equality vs. containment) is a real clarification the note only implies. The `Observe`-ordering decision ("address order = emission order within a home's chain but not across interleaved homes") is a sharp, correct observation worth keeping verbatim.
