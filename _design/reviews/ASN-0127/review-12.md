The digest is unusually strong, and I'll say where before the sharpenings. The forced/conventional split is accurate (it correctly isolates the *forced* M-for-resolution / L-for-matching state-dependency from the *conventional* explicit staging). The two-regime existence/discovery cacheability framing is faithful to the note's anchoring taxonomy. The per-transition cache-maintenance derivations are not merely cited but independently correct — I checked the μ⁻ Δ-bounded recheck, the μ~ gain/loss split retested against the retained core (`(old ∖ failed-loss) ∪ findlinks(Δ_gain)` does reduce to `findlinks(image')`), and the "image-motion **or** matching-K.λ" invalidation trigger (D-ABSORB's necessity holds only across Σ.L-preserving steps, so K.λ is the gap — caught correctly). The Green evidence is used precisely: conjunctive-Q2-vs-disjunctive-primitive, the empty-*conjunct* vs empty-*input* short-circuit distinction, the THREE-filter divergence, and the homedocids "coincidentally global" reading are all right. The slot-collapsed-index identity (`(⋃ᵢ coverage(eᵢ)) ∩ I ≠ ∅ ⟺` F-MATCH) and the "L3 gives n≥3, don't hard-code three slots" caution are load-bearing and correct.

**I found no defects.** The items below are genuine but non-load-bearing.

1. **[SHARPENING]** §What must be built (region resolver): "C1a still applies within each subspace portion" silently assumes each per-subspace portion of a cross-subspace `W` is itself *contiguous*. The note grounds C1a only for a *contiguous* `W` within a *single* subspace; the per-subspace decomposition is the digest's (sound) extension via subspace disjointness, but a general non-contiguous `W` needs a within-subspace contiguity split before C1a applies to each piece. State the contiguity-of-each-portion assumption so a builder doesn't hand C1a a scattered per-subspace portion.

2. **[SHARPENING]** §Recovery and durability: the repo-substrate analogies ("`links.jsonl`-style" link log, "`paths.json`-registry pattern" for caches) point at the spec-authoring pipeline's artifacts, not a runtime engine's stores. The "-style" hedge keeps them analogical, but paired with "maps onto the repo's working substrate" they risk reading as a literal layer mapping. Either drop the specific filenames or mark them explicitly as analogy for an append-only-log / registry *shape*, so a builder doesn't treat pipeline buffers as the runtime substrate.

3. **[SHARPENING]** §Recovery and durability: "the permascroll proper is Green's content scroll" is non-load-bearing color not established by the note or the evidence. The actual design point (the link log is append-only permanent by L12) stands without it; hedge it as a characterization or drop the parenthetical.

VERDICT: CONVERGED
