The digest is, overall, unusually strong: the forced/conventional split is accurate (it correctly nails the two-phase combinator as a *presentation* choice while the M-vs-L state-dependency is forced); the discovery-cache treatment is sound (I checked the μ⁻ Δ-bounded recheck and the μ~ `(old ∖ failed-loss) ∪ findlinks(Δ_gain)` formula — both reduce correctly to `findlinks(image(Σ'))`); the D-ABSORB necessity-scope catch (image-motion necessity holds *only* across Σ.L-preserving steps, so K.λ needs a separate invalidation disjunct) is exactly right; the slot-collapsed-vs-per-slot index distinction and the empty-*input* vs empty-*conjunct* short-circuit distinction are both precise and well-grounded in the evidence; and the recovery insight that the arrangement log is non-reconstructible and that K.δ *births* must be journaled is genuinely sharp. Green source-claims all trace to the evidence; no fabrication.

The problems:

1. **[DEFECT] — Implementation approaches → Recovery and durability.** The arrangement-log enumeration "per-document arrangement edits (K.μ⁺/μ⁻/μ~)" **drops K.μ⁺_L** (link-subspace arrangement extension). This is an in-scope omission, not shorthand: μ⁺_L modifies Σ.M, the resolver's `image` reads link-subspace positions (S3★ routes them to link addresses), and the digest *itself* makes link-document queries first-class — so the POOM recovery must replay μ⁺_L or it loses all link-subspace arrangement, breaking exactly those queries. The digest writes "μ⁺/μ⁺_L" together everywhere else (design commitments, the μ⁺/μ⁺_L discovery-maintenance clause), so this is an internal inconsistency. Add K.μ⁺_L to the list.

2. **[SHARPENING] — Recovery and durability.** "the authoritative state is three independent append-only logs, not one" silently includes a content log (which this query never dereferences — coverage is computed from endset spans stored in `Σ.L`, `image` reads `Σ.M`) while omitting provenance `R` (K.ρ) and folding entity registration `E` into K.δ. Three logs is the right *scope* — but state the inclusion principle (M and L are read directly; C is the I-address space the index/results live in; R and E are real Σ components this query doesn't consult) so the omission of R/E reads as deliberate scoping rather than an oversight.

3. **[SHARPENING] — The link matcher.** "neither composes from the other" is imprecise: `findlinks(I) = ⋃ᵢ slotmatch(·,i,I) = ⋃ᵢ Q2({(i,I)})`, so the disjunctive primitive *does* compose from single-slot Q2 calls (it is their union); it is **Q2** that does not compose from `findlinks` (you can't recover a per-slot-different-filter intersection from common-I unions). State the asymmetry; the load-bearing claim (both rest on the shared `slotmatch`, so build per-slot tagging as the common substrate) is unaffected.

4. **[SHARPENING] — How it fits.** The ASN-0093 line ("allocation freshness/injectivity that the worked witnesses rest on") undersells it: ASN-0093's freshness also underpins F-LAMBDA's disjoint-increment and E-CONS's creation anchoring (the existence-cache "append-only" maintenance discipline), not just the worked witnesses.

5. **[SHARPENING] — D-CWP discussion.** The note explicitly frames D-CWP as "the discovery analog, on the contraction side, of ASN-0098's LP12a (ContractionDiscoverabilityWP)." Citing that analogy would strengthen grounding and signpost the WP family the open Q3 generalizes.

VERDICT: REVISE
