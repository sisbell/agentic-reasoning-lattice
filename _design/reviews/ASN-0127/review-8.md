The digest is unusually strong: I verified the load-bearing algebra (slot-collapsed vs. per-slot matcher, the μ⁻ Δ-bounded maintenance, the μ~ retained-core procedure, the image-motion-or-matching-K.λ invalidation trigger, F-FULL reorder-immunity with the content-sharing refcount caveat, the empty-input vs. empty-conjunct short-circuit distinction, the THREE-divergence/homedocids-coincidence reading against the evidence) and it all holds. The Green claims are grounded in the note, the evidence, or documented enfilade structure; no fabricated source-level behavior. There is one material gap.

**Revision list**

1. **[DEFECT] *Recovery and durability*: the "three independent append-only logs" design omits document registration (K.δ), so `dom(Σ.M)` is not reconstructible — and the digest itself makes `dom(Σ.M)` load-bearing.** The digest enumerates the authoritative state as exactly content (`K.α`), link (`K.λ`), and *arrangement-edit* (`K.μ⁺/μ⁻/μ~`), and says the POOM is "rebuildable only by replaying its own arrangement-edit log." But `dom(Σ.M)` — the set of registered documents — is established by **K.δ** (the note's "`K.δ` Document post-state," `dom(Σ.M(d)) = ∅`), not by any μ edit. A document registered and never edited leaves no trace in any of the three named logs, so on recovery it vanishes — and the resolver section's own enforced precondition (`d ∉ dom(Σ.M)` ⟹ *undefined*, "signal as an error, not a silent ∅") then misclassifies a legitimate registered-empty document (image `= ∅`, *defined*) as unregistered. dom(M) is also what tells recovery *which* per-document POOMs to replay, so this is foundational, not an edge case. Fix: add document registration (K.δ) to the durable arrangement state — the arrangement log must record document births, or a separate registry must persist `dom(Σ.M)` — so the registered-vs-unregistered definedness distinction survives recovery. (E and R remain legitimately out of query scope; the gap is specifically K.δ → `dom(Σ.M)`.)

2. **[SHARPENING] *Region resolver*: "this is not C1a but an inference from subspace disjointness" undersells C1a.** Each subspace portion's run structure *is* C1a (applied within that single subspace); subspace disjointness is only what makes the two run sets independent and lets you union them. Reframe as "C1a applies *per subspace*; subspace disjointness is why there is no single cross-subspace run map" — the builder takeaway (no unified run map across `s_C`/`s_L`) is already correct.

3. **[SHARPENING] *Discovery caching*: "the same kind of step-by-step maintenance as the existence cache" overstates the parallel.** Only the K.λ sub-case is identical to the existence move; μ⁻ (Δ-bounded drops with `matches(·, I_R)` recheck) and μ~ (gain/loss against the retained core) are genuinely richer than the append-only existence cache. The load-bearing claim — incremental per-step, not blanket recompute — is right; just don't imply existence's append-only *simplicity* carries over.

4. **[SHARPENING] *Recovery*: the granfilade is tangential to this capability.** This note's query reads only `Σ.M` and `Σ.L`; it never dereferences a content address to its value, so "granfilade-as-index (from C)" is not part of *this* note's build (it belongs to the content-keyed Q1 variant). Keep it as substrate framing if desired, but don't list it among the indexes a builder of this capability must rebuild.

5. **[SHARPENING] *μ⁺ maintenance*: "newly-arranged in-region I-addresses" should read "newly-imaged."** Under content sharing a newly-arranged in-region V-position can resolve to an already-imaged I-address, contributing nothing — slightly at odds with the digest's own many-V→one-I emphasis. The cited object `Δ⁺ = image(W,Σ') ∖ image(W,Σ)` is already correct; just align the prose to it.

Everything else — the forced/conventional split, the disjunctive-vs-conjunctive (Q2) framing, the per-slot-matcher-as-shared-substrate argument, the regime-split-from-F-CIL principle, the two-phase snapshot-coherence concurrency hazard, and the divergences-not-to-replicate section — is accurate, well-grounded, and at the right altitude.

VERDICT: REVISE
