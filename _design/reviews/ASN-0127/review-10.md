This is a strong, unusually careful digest: the disjunctive‑vs‑conjunctive distinction (F‑MATCH vs Green's `find-links-from-to-three`/Q2), the existence/discovery regime split anchored on F‑CIL, the Δ‑bounded μ⁻ maintenance with D‑CWP as a front‑end (not a free skip), the K.δ/registered‑empty recovery inversion, and the Green‑divergence triage (type slot to repair, homedocids coincidentally matching, conjunctive short‑circuit not to import) are all accurate and genuinely actionable. The problems are localized.

## Revision list

1. **[DEFECT] — Recovery and durability: "Σ.C … is the I-address space the index and results live in" is false and self-contradictory.** `findlinks` results are link addresses in `dom(Σ.L)`, not `Σ.C`; and by the digest's *own* Forced commitment 7 and the resolver build-note, image outputs — the I-set handed to the matcher — span `dom(Σ.C) ∪ dom(Σ.L)` (link-subspace V-positions resolve to link addresses via S3★), while coverage targets range over all of `T` (L4, including unallocated ghosts). So the query consults `Σ.C` exactly as much as it consults `Σ.R` and `Σ.E` — never (the digest even admits "no content value is ever dereferenced here") — yet the scope line keeps `C` in "this builder's logs" while excluding `R`/`E` for precisely the reason that also excludes `C`. **Fix:** redraw the scope — this read-query reads and recovers from `Σ.L` (link log) and `Σ.M` (arrangement log *including* K.δ document registrations); list `Σ.C` alongside `Σ.R`/`Σ.E` as real authoritative substrate state this query never reads (`Σ.C` is written by the K.α allocation/write path, not consulted by this read path). If instead you mean whole-substrate recovery, journal all components and say plainly the query reads only `L` and `M`. Either way, delete the "results live in Σ.C" rationale. (The rest of this section — `M` not reconstructible from `L`, K.δ load-bearing, the registered-empty-vs-unregistered inversion, checkpoints, MVCC snapshot coherence — is correct and should stand.)

2. **[SHARPENING] — Phase-2 matcher / Divergences-from-Green: make link arity explicit.** F-MATCH ranges over `1..|Σ.L(a)|`, and L3 fixes only `n ≥ 3` (ASN-0043), *not* `n = 3`. The pervasive Green "from/to/three" framing risks a builder hard-coding exactly three slots; state that the slot-collapsed union (and the per-slot `slotmatch`) must range over the link's actual arity, which can exceed three.

3. **[SHARPENING] — Coverage evaluator / index: surface ghost types (L9).** A coverage generating-address — especially a type endset's — may be *unallocated* (the note's own worked illustration pins `a_θ` as an unallocated ghost type). The recommended prefix-trie/interval index handles this iff it keys purely on address structure over `T`; a builder who gates index insertion on "generating address is allocated in some store" would silently drop ghost-type coverage. Note that coverage indexing keys on prefix/interval structure, never on allocation. (The digest cites L4 for "target any address" but not L9 for "target an address that need not exist.")

## Verdict

VERDICT: REVISE
