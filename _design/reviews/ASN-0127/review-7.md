This is a strong, unusually rigorous digest. I checked the load-bearing technical claims directly: the slot-collapsed-index identity `(⋃ᵢ coverage(eᵢ)) ∩ I ≠ ∅ ⟺ ∃i : coverage(eᵢ) ∩ I ≠ ∅`, the Δ-bounded μ⁻ maintenance against `I_R`, the μ~ loss/gain reconstruction `(old ∖ failed-loss) ∪ findlinks(Δ_gain) = findlinks(image(Σ'))`, and the invalidation-trigger completeness argument (image-motion **or** matching-`K.λ`, never under-invalidates) all hold. The forced/conventional split is accurate, the partiality-vs-empty distinction (`d ∉ dom(M)` undefined vs. unarranged-positions `∅`) is correctly drawn, the link-addresses-on-both-sides observation (L4 + S3★) is a genuine catch, and the Green divergences (broken THREE filter to *not* replicate; disabled homedocids that *coincidentally* matches the note's global `findlinks`) are correctly grounded in the evidence. No fabricated source-level claims, no value-identity violations, good altitude.

The remaining items are all non-load-bearing precision improvements.

**Revision list**

1. **`What must be built` (caching line) — `[SHARPENING]`.** The summary prescribes "recompute-or-invalidate discipline for discovery queries," but `Implementation approaches` then establishes — correctly and at length — that discovery caches admit *per-transition incremental maintenance* ("do not blanket-recompute"), with invalidation only as a conservative fallback. The one-liner undersells the digest's own best result and reads as out of step with the detailed section and the `Decisions` list (which frames all three as a choice). Rewrite the summary to "incremental per-transition maintenance (conservative invalidation as fallback)" so the floor matches the recommended ceiling.

2. **`Divergences` / `Decisions` (homedocids ↔ Q2) — `[SHARPENING]`.** Home-document scoping is repeatedly tied to "Q2" (e.g., "an enhancement belonging to the Q2 ... layer"; "not part of this note's query contract (see Q2)"). But Q2, as the note defines it, is specifically the slot-indexed *conjunctive* match; homedocids is a *separate* filter dimension (the 4th arg, the one the `TRUE||` guard disables). Both live inside the full `find-links-from-to-three` operation, but they are different features and different from each other. Decouple them: point home-scoping at "the full `find-links-from-to-three` layer," not at Q2.

3. **`Query planning` (no-disjointness attribution) — `[SHARPENING]`.** "The union laws hold without a disjointness side-condition for a load-bearing reason: under content sharing..." blurs two distinct facts the note keeps separate: F-UDIST is unrestricted *unconditionally*, by pure set algebra (its proof "consults `I₁ ∩ I₂`" nowhere), whereas content sharing (M13/M14) is what makes that unrestrictedness *necessary for the region composition* F-VDIST (disjoint `W₁,W₂` can still have overlapping images). State it as: F-UDIST holds for any `I₁,I₂` by set algebra; content sharing is why the *region-level* law cannot carry a disjointness side-condition. The builder takeaway (image is a deduped set, set-union per run) is unaffected.

**Solid as-is (no action):** the slotmatch decomposition and "build per-slot anyway as forward-investment for Q2" reasoning; the prefix-trie-default-vs-interval-once-T1-contiguity-confirmed call; the F-FULL footprint hint with reorder-immunity (LP11) and the refcounting caveat under content sharing; the three-independent-logs recovery model with the arrangement-log-is-not-derivable warning; and the snapshot-isolation argument from two-store torn reads. These are accurate, grounded, and at the right altitude.

VERDICT: CONVERGED
