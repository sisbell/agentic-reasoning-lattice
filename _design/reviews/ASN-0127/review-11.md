Both phases of this digest survive scrutiny well. I verified the load-bearing mathematical claims that a builder would actually rely on — the slot-collapse equivalence (`(⋃ᵢ coverage(eᵢ)) ∩ I ≠ ∅ ⟺ ∃i …`), the Δ-bounded μ⁻ cache-maintenance (old = `findlinks(I_R) ∪ findlinks(Δ)`, post = `findlinks(I_R)`, candidates = `findlinks(Δ)` rechecked against `I_R`), the reorder gain/loss maintenance (`(old ∖ failed-loss) ∪ findlinks(Δ_gain)`), the D-ABSORB scope restriction and the resulting "image-motion **or** matching-`K.λ`" invalidation trigger — and they are all sound. The forced/conventional tagging is accurate, the disjunctive-vs-conjunctive split is correctly tied to Q2 and the Green evidence, and the ghost-type/arity->3/contiguity cautions are exactly the traps a porter of Green's spanfilade would hit. I found no defect.

# Revision list

**1. `[SHARPENING]` — Section "Recovery and durability": soften "two independent append-only logs, not one" and surface the single-Istream realization.** The forced, load-bearing content is that `Σ.M` and `Σ.L` are *independent authoritative state* — `M` is not derivable from `L`, so arrangement transitions (`K.δ` doc-births + `K.μ*` edits) must be durably journaled in their own right (this is correct and important). But "two … logs, not one" over-commits to a physical two-log layout, which is one realization among several and arguably not the simplest: a single append-only transition log (the Istream) recording *all* K-transitions and materializing `M` and `L` as separate structures satisfies the same independence, and — per Lampson, prefer the simple thing — buys a *global transition order* and hence the (L,M)-snapshot consistency the digest itself requires, for free, instead of needing cross-log coordination to reconcile two independently-sequenced logs. Reframe: the independence of `M` and `L` is forced; partitioning into two physical logs is an optional realization, and a unified Istream is the simpler one.

**2. `[SHARPENING]` — Sections "Coverage evaluator" / "The link matcher": disentangle "interval-overlap (stabbing) test."** These name two distinct queries. Coverage is a union of intervals; the query argument `I`, under C1a, is itself a union of contiguous I-runs — so the natural test is *interval-overlap* (interval-vs-interval), with *stabbing* (point-in-interval) being the degenerate case when `I` is handed over as individual points. State which one the resolver→matcher contract uses (run-structured `I` ⟹ overlap) so the index-query interface is precise rather than naming both.

# What's solid (no action)

- The **caching-across-evolution** section is the strongest part: the per-transition incremental discipline, the Δ-bounded μ⁻ maintenance with D-CWP correctly positioned as a *front-end check, not a free skip*, and the reorder gain/loss decomposition are all mathematically correct and at exactly the right altitude.
- The **forced/conventional split** is accurate — including the subtle correct calls that the two *state-dependencies* are forced while the explicit staging is conventional, and that fixed-filter Q2 inherits existence-stability from F-CIL for free.
- **Grounding is clean**: no ungrounded Green source-level claims; the subtree-contiguity caveat ("the note does not prove it, but it holds in Green") and the permascroll-style hedge are exactly the right epistemic discipline, and the type-slot divergence, homedocids-coincidence, and empty-input-vs-empty-conjunct distinctions are all correctly drawn from the evidence.
- The **ghost-type indexing trap** (never gate insertion on allocation, L9), the **arity > 3 trap** (L3 lower-bounds, doesn't fix, the slot count), and the **registered-empty vs unregistered definedness** distinction are real builder hazards the note implies and a lesser digest would miss.

VERDICT: CONVERGED
