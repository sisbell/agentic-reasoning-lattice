I probed this hard — accuracy of each "forced/conventional" tag, soundness of every cache recipe against the lemmas it cites, the Green evidence, and the unit-depth assumptions. I found no material defect: the disjunctive-vs-conjunctive split, the `slotmatch` substrate decomposition, the two-regime cache design, the D-CWP fast front-end, the μ~ gain/loss split, the empty-input-vs-empty-conjunct distinction, the snapshot/torn-read argument, and the undefined-vs-empty resolver distinction are all accurately read and correctly derived. What remains is polish.

## Revision list

1. **[SHARPENING] Coverage evaluator — generalize beyond unit-depth.** *Implementation approaches / coverage evaluation* (and the matching *What must be built* bullet) builds the evaluator and trie on `coverage({x}) = subtree(x)` with a prefix test, and treats "multi-span" as the only generalization. But F-MATCH consults general `coverage(Σ.L(a).eᵢ)`, a function of the endset's spans (ASN-0043), and a single span `(a, δ(depth, width))` need not be unit-depth — a non-unit-depth span is not a full subtree. State that coverage is a union over the endset's (possibly non-unit-depth) spans, so the evaluator/index must key on spans generally; flag that the note's worked illustration is *entirely* unit-depth (`{(x, δ(1, #x))}`), so this generality is inherited from ASN-0043's `Coverage`, not from the note's examples. The interval-index framing already covers it; the prefix-trie framing is the unit-depth special case.

2. **[SHARPENING] μ⁻ maintenance clause — scope and citation.** In *Caching / μ⁻*, "any link not reaching `Δ` already reaches the retained `I_R` and survives (F-IMONO)" is true only for links **in the result** (pre-result `= findlinks(I_R) ∪ findlinks(Δ)` by F-UDIST); over all links it is false (a link reaching neither). And survival holds because the post-result *equals* `findlinks(I_R)` (D-CWP bridge + F-INERT), which is not what F-IMONO (monotonicity in `I`) states. Scope the clause to result members and re-cite F-UDIST + the bridge. The actionable takeaway (candidates `= findlinks(Δ)`) is correct.

3. **[SHARPENING] Index default vs "the spanfilade is the proven structure."** The digest repeatedly endorses Green's spanfilade as *the proven* structure, yet defaults the index to a prefix trie and treats the range/interval form — which is what the spanfilade actually is — as the switch-to-later option. The conservatism (rest on the prefix structure the note proves, not the unproven subtree-contiguity) is sound, but make the tension explicit: the recommended default diverges from the very structure cited as proven, on the strength of an assumption Green satisfies. "Default trie" is a note-fidelity stance, not an engineering preference over Green.

4. **[SHARPENING] Type-slot divergence — storage vs query.** *Divergences from Green* says a faithful build "indexes the type slot too." Per the evidence Green's spanfilade *does* store the type slot (`LINKTHREESPAN`); the brokenness (EC-TYPE-FILTER-NONFUNCTIONAL) is on the type-*filter query* path. Sharpen to: the divergence to avoid is broken type *retrieval*, not missing type *storage* — the storage substrate is already faithful.

5. **[SHARPENING] Home-scoping attribution.** The digest twice files home-document scoping under "Q2." The note's Q2 is specifically the conjunctive per-slot matching question (which of F-UDIST/F-IMONO survive); home-scoping is a separate filter dimension (Green's `homedocids`, the 4th arg), belonging to the full `find-links-from-to-three` operation layer, not Q2 proper. Keep "full `find-links-from-to-three` layer"; drop the "Q2 /" prefix where the subject is specifically home-scoping.

6. **[SHARPENING] μ⁺_L shorthand.** The F-FULL footprint paragraph says the footprint/result "moves only under μ⁺/μ⁻"; a link-subspace extension (μ⁺_L) also grows `ran M(d)`. Write "μ⁺/μ⁺_L/μ⁻" to match the caching bullet's own usage.

Solid as-is, do not touch: the F-CIL-driven regime split (including the observation that fixed-filter Q2 inherits existence-stability for free), the `slotmatch` decomposition showing OR and AND both compose from it but not from each other, the Δ-bounded contraction maintenance with the `findlinks(Δ)=∅` free-skip, the injective-only-incomparable → "both sides always run" reorder-cost mapping, the refcounting caveat on footprint contraction, and the snapshot-coherence argument.

VERDICT: CONVERGED
