This digest is unusually strong: it reads the note accurately, grounds its Green claims scrupulously in the evidence channel (and explicitly fences off the one unexplained anomaly rather than leaning on it), stays at design altitude throughout, and converts the note's idealized invariants into a real builder-facing risk map. I went hunting for a material defect across accuracy, soundness, grounding, completeness, and altitude and did not find one. What remains is genuine but non-load-bearing.

**Revision list**

1. **[SHARPENING] Design commitments → "The read is pure, total…": the totality argument should name the precondition it rules out as *address-checkable*.** The digest writes "no satisfiable address-only predicate is sufficient for membership… *so the read cannot be correctly gated by a precondition*." Taken literally, a partial `readlink` gated on `a ∈ dom(Σ.L)` is perfectly well-defined — the note itself entertains exactly that shape ("a partial function gated by the precondition `a ∈ dom(Σ.L)`") and rejects it not because it's incorrect but because the precondition is *not address-checkable*, so the caller cannot discharge it and absence-handling collapses back onto the result anyway. Tighten to: gating on the membership predicate is correct-but-unusable (the caller can't pre-check it), so totality with in-contract `⊥` is the honest shape. This matches the note's "partial vs total" framing precisely; the build takeaway (make it total) is already right.

2. **[SHARPENING] Implementation approaches → "The link store": don't let "Green's permascroll/granfilade is the same pattern" read as "the granfilade is append-only."** The append-only/immutable property attaches to the permascroll (the journal) and to *link* orgls specifically (INV-LINK-PERMANENCE, the relevant part for READLINK's store) — but the granfilade as a whole also holds *mutable* document POOMs (DELETE edits the doc orgl). The sentence is defensible under a journal-then-replay reading, but since the digest is the link store's design doc, attribute immutability to the permascroll + link orgls rather than the granfilade wholesale, so a builder doesn't infer the whole structure is frozen.

3. **[SHARPENING] Implementation approaches → "Organizing decision": split the mega-paragraph.** A single block carries the raw-read-as-layer thesis, the `link2sporglset`/FOLLOWLINK/RETRIEVEENDSETS contrast, the orphaned-link cost, OQ1, OQ2, *and* the type-empty anomaly. The content is correct and load-bearing; the packaging buries the central engineering takeaway (factor the raw read as the bottom layer; resolution belongs only above it). Break it into the thesis, the Green-mapping, and the open-question reconciliation. Purely presentational.

**Sections that are solidly right** (stated to be fair, not to pad): the depth-vs-lineage/user-field *robustness asymmetry* in the caching discipline is the best thing in the digest — it correctly keeps the note's three families as equally-valid permanence *proofs* while using the Green evidence to flag that only depth is guaranteed by the allocator's arithmetic, and that lineage/user-field caching inherits the same trust-boundary caveat as the L0–L13 value invariants. The verbatim-disclosure / type-theory analysis (type rejects only arity < 3; `e₃ = ∅` type-checks; L3 is a *store* invariant inherited by disclosure, not a property of `Link`) is exactly right. RL4 nesting-locality handling, including offering the branched-history witness as a ready-made property test, is accurate and actionable. The forced/conventional split is correctly assigned throughout, and all three open questions are addressed (OQ1/OQ2 substantively in the organizing decision, OQ3 in the builder decisions).

VERDICT: CONVERGED
