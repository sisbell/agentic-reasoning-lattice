The digest is unusually strong. The forced/conventional split is accurate; the type-closure subtlety (`Link` rejects only arity < 3, `e₃ = ∅` type-checks, non-empty type is inherited L3) is exactly right; the Green evidence is used correctly throughout (`NOBERTREQUIRED` and the same-path-as-span-queries claim, the `'?'` failure reply, 2-endset `domakelink`, the empty-type golden read, ascending-order endsets, home-doc-by-truncation, the read-side validator stub); and the headline organizing decision — factor the raw read as its own layer, `FOLLOWLINK = resolve ∘ select ∘ readlink`, search as a separate index path — is both grounded and the right Lampson call. The left-to-right screen well-definedness point and the "never cache residual-class `⊥`" landmine are correctly load-bearing. Two items.

1. **[DEFECT] "How it fits" → final "Position in the stack" line contradicts the body.** It says "readlink is … the primitive that follow, **search, and count** are defined against," but the bullet immediately above states "FINDLINKS / COUNT — a *sibling*, not a consumer: it reaches links through the content-region index (the spanfilade), **not through readlink**." Search/count run the reverse direction (content region → links) and are not defined against readlink — the note's own problem statement and the evidence (Q13/Q16, the spanfilade path) confirm this. The closing line ships a false claim. Fix it to name only the genuine consumers: readlink is the floor that **FOLLOWLINK** (and any nested/faceted consumer) is defined against, with **search and count as siblings** reaching links through the spanfilade.

2. **[SHARPENING] "Implementation approaches" → permanence classification and caching: the three families are not equally solid, and the digest's own evidence says so.** The text presents depth (`#E > 2`), lineage (`N₁ ≠ 1`), and user-field (`#U ≥ 2`) as uniformly "address-computable permanence proofs" and asserts `⊥` is permanent "exactly when" the address falls in one of them — stated flatly to the builder, not "in the note's model." But only **depth** is structurally guaranteed: the evidence confirms Green's link allocator never deepens the element field, so `#E = 2` holds of every emittable address unconditionally. **Lineage** and **user-field** inherit their permanence from *allocation* invariants — node-lineage-under-`n₀` and single-component user fields — that the same evidence shows Green does **not** robustly enforce (lineage containment is enforced at the bert layer if at all, never by the allocator; the single-user-component shape is emergent from the reference client's seed and "breakable by a non-canonical seed"). This is the same trust-boundary caveat the digest already attaches to the *value* invariants (L0–L13), but it is missing for these *address-space* invariants — and it is soundness-critical: a builder whose allocator can emit a non-`n₀`-homed or multi-user-component link address would cache `⊥` for an allocatable address, reintroducing the exact read-stability bug the residual-class landmine warns against. Add: depth caching is unconditional; lineage/user-field `⊥`-caching is sound only where the allocator upholds the corresponding emission invariant.

Every other section — design commitments, what-must-be-built, the store/representation/concurrency approaches, guarantees-to-uphold, and the OQ1/OQ2/OQ3 handling (including the careful note that the raw-read layer *preserves* OQ2's distinguishing information without *discharging* OQ2 at FOLLOWLINK) — is accurate and actionable; I found nothing to fix there.

VERDICT: REVISE
