This digest is unusually strong: the layering thesis (raw read as the floor, FOLLOWLINK = resolve∘select∘readlink), the caching/permanence discipline, and the handling of Green's arity divergence (2-endset `domakelink`, silently-omitted empty types) are all accurate and well-grounded in the evidence. The permanence section's distinction — depth is *structurally* unconditional (inc-arithmetic cannot deepen `#E`, so no client can emit `#E>2`), while lineage and user-field permanence rest on allocation invariants Green leaves to the bert layer / client seed — is a genuine builder insight, correctly translating the note's model-theorems into trust-boundary caveats. I checked every Green claim against the evidence; only one is contradicted.

# Revision list

1. **[DEFECT] — Organizing decision: "the intact recorded relationship is invisible at the only operation Green exposed" is false.** Green exposed a *second* link-read, RETRIEVEENDSETS (opcode 28; exercised by the `retrieve_endsets` golden test on a link address), which returns all three endsets by the link's own address and *also* applies the same I→V resolution (`linksporglset2specset`, same silent drop — evidence Q1/Q12/Q13/Q14/Q17). So (a) there were two exposed reads, not one, and (b) the orphaned relationship is hidden at *both*. The omission also undercuts the framing that "assembling the raw read across all slots" is novel relative to Green — RETRIEVEENDSETS already assembled all slots, just resolved and via the spanfilade. Fix: name RETRIEVEENDSETS, state that both exposed reads resolve (so the recorded relationship is invisible at *every* exposed operation), and note RETRIEVEENDSETS is the *closer* structural analog to readlink (whole-link, by address) yet still resolves and reads through the spanfilade index rather than the link orgl — so Green exposed **no raw read at any operation**. This *strengthens* the digest's central thesis rather than weakening it; ship it as the sharper form.

2. **[SHARPENING] — Disclosure commitment: the closure-under-shrinking argument is over-attributed.** `Link`'s closure under shrinking a connective slot (`F' ⊊ F`) directly justifies only "drops from/to spans." "Reorders slots," "flattens a nested target," and "mis-preserves arity for `N > 3`" each type-check for *their own* reason (a permuted, dereferenced, or arity-3-truncated sequence of endsets is independently a valid `Link`), not because of the shrinking closure. The conclusion — the type rejects only arity below 3; all else is behavioral — is correct; tighten the "so" so closure-under-shrinking isn't stated as the sole cause of all four failure modes.

3. **[SHARPENING] — Forced/totality commitment: "no address-only predicate is sufficient for membership" should carry the note's "*satisfiable*" qualifier.** The trivially-unsatisfiable predicate is vacuously sufficient; the note says "no *satisfiable* address-computable predicate is sufficient" for exactly this reason (and the digest already supplies the witness — empty store at `Σ₀`). Restore "satisfiable" so the universal is precise rather than relying on the reader to supply it.

VERDICT: REVISE
