Overall this is a strong, unusually careful digest. Every Green source-level claim checks out against the evidence answers (NOBERTREQUIRED link reads, the `'?'` failure reply, per-endset `link2sporglset` with no POOM check, `linksporglset2specset` silent drop, ascending-order multi-span endsets, `domakelink`'s 2-endset store links, the empty-`type:[]` golden read, home-doc-by-truncation, the arity-3 cap). All of RL0–RL6 and the three open questions are accurately captured, the altitude holds (design, not code; Green internals cited only as grounded reference), and no proposed approach violates a commitment. The ghost-type-vs-empty-type distinction, the "type rejects only arity < 3, everything else is behavioral obligation" closure analysis, the orphaned-link → OQ1/OQ2 reasoning, and the caching/permanence discipline (with the residual-class landmine) are all sharp and correct. I found no material defect — only precision improvements.

**Revision list**

1. **[SHARPENING]** *Design commitments → forced #2 (totality):* "no address-only predicate is sufficient for membership" drops the note's **"satisfiable"** qualifier (RL0: "no *satisfiable* address-computable predicate is sufficient"). Stripped of it the universal is strictly defeated by the always-false predicate (vacuously sufficient), even though the intended point — no *useful/discriminating* address-only test settles membership, so the read cannot be usefully gated — is exactly right. Restore "satisfiable," or rephrase as "no address-only test can *positively establish* membership."

2. **[SHARPENING]** *Implementation approaches → organizing decision:* "the intact recorded relationship is invisible at **the only operation Green exposed**" (FOLLOWLINK) overlooks RETRIEVEENDSETS — Green's all-three-endset retrieval, which *also* applies I→V resolution and so *also* goes empty on an orphaned link. The core claim ("Green never exposed the *raw* read") survives and is in fact reinforced, but a builder examining Green will hit RETRIEVEENDSETS and wonder why it isn't the whole-link read. Either scope the phrase ("the only operation that reads the link orgl *directly* by its ISA" — RETRIEVEENDSETS goes via the spanfilade) or name it and note it resolves too.

3. **[SHARPENING]** *Implementation approaches → the link store (ordered map):* ordered/range traversal is sold as serving "allocation … the contiguous-initial-segment structure, **and search**," but the digest's own *How it fits* says search reaches links through the *spanfilade*, not the link store. Search needs only point lookup (a hash map gives that too); the genuine beneficiaries of *ordering* are allocation (frontier = chain max) and the contiguous-segment structure. Drop "search" from that list to stay internally consistent — the recommendation (ordered) is unaffected.

4. **[SHARPENING]** *Implementation approaches → structural screen:* "Green … just probes and returns FALSE on miss, and its **read-side address validator is a stub**" conflates two facts: the link-read path (`link2sporglset → findorgl`) has *no* validator at all (it just probes), while the stub validators (`acceptablevsa`, `validaccount`) sit on the V-copy/account paths. The takeaway — Green doesn't validate address shape on read, so screening is your optional optimization, not a fidelity requirement — is correct; just tighten the supporting clause.

VERDICT: CONVERGED
