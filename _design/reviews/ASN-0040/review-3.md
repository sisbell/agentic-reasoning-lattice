I worked the digest against the note, the claim statements, and the Green evidence. The core is genuinely strong and I want to say so before the list: the counter-representation analysis is *correct and sharp* — it nails that a `(p,d)`-keyed counter map cannot represent non-child seed roots (`[1] ∉ S(p,d)` for any valid `(p,d)`, so `B₁={[1],[1,0,1]}` is unrepresentable without the auxiliary set), and that empty-start genesis keeps that set empty. The B6(i) aliasing hinge, the arbitrary-precision/B9 warning, the per-namespace-serialization-preserves-B8 argument, and especially the **finality / reserve-then-confirm reconciliation** (rollback is pre-persistence via `deleteversion` gated by created/modified, so *persistent* baptisms stay irrevocable — consistent with B0, not a divergence) are all accurate and correctly navigate the evidence's internal one-mechanism-vs-two tension. No fabricated Green claims, no altitude slips into code, no approach that violates a commitment. What remains is tightening.

# Revision list

1. **Implementation approaches → Atomicity and concurrency:** the per-namespace option promises multi-owner baptisms become "contention-free," but the durability default ("**Start with one shared log**") reintroduces a global serialization point at the log append that undercuts that promise until the log is *also* sharded — the two recommendations read as opposite advice. Reconcile into one staged story: default to a single serializing writer/log (B-Seq verbatim, simplest, correct); shard **both** serialization and log per namespace (B7 licenses the independence) only when multi-owner write throughput demands it. State that a shared log is *safe* (more order than B7 requires) but is exactly the bottleneck the split removes. `[SHARPENING]`

2. **Design commitments:** "There is no batch-grow" is presented as *forced*, yet the note's open questions explicitly contemplate bulk allocation ("baptizing a contiguous range of k positions in a single operation") under B4/B1 — and your own Guarantees section says "any bulk/batch path you add must preserve gap-freeness," which reads as the opposite. Reframe: one-at-a-time is how Bop/B0a are specified *for the present Σ* (forced now); bulk is a flagged open extension that must preserve B4 atomicity and B1 contiguity. Cross-reference the open question so the two sections agree. `[SHARPENING]`

3. **Implementation approaches → Durability:** "the repo's own proven substrate (an append-only `links.jsonl` journal …)" misattributes the spec-authoring pipeline's link-extraction buffer as the production data substrate (the production substrate is the append-only Istream; `links.jsonl` is pipeline tooling). The recommendation already stands on the *verified* Green precedent (append-only granfilade, lazily checkpointed); drop or correct the `links.jsonl` citation rather than grounding a default in a misnamed in-repo example a builder can't verify from the note or evidence. `[SHARPENING]`

4. **What must be built → Tumbler arithmetic:** decomposition (`t → (parent, depth, ordinal)`) is listed as an unconditional build item, but it is required specifically by the *recommended counter representation's* membership query; an explicit-set/trie representation needs only forward construction of the `(p,d)` prefix plus subtree-max navigation. Tag decomposition as a counter-representation dependency so a builder choosing the set/trie doesn't build it needlessly. `[SHARPENING]`

5. **Design commitments → determinism:** "no external counter" sits awkwardly beside the later per-namespace-counter recommendation and can read as self-contradiction. Clarify that "external" means *independent-of-registry* (no session-local/clock-driven counter, per the Green evidence) — the recommended per-namespace counter *is* the registry state (`hwm`), a derived/recomputable representation, not a second source of truth. `[SHARPENING]`

VERDICT: CONVERGED
