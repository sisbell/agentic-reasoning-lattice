I worked through the digest against the note, the claim statements, and the evidence answers — checking the set-algebra reformulation for equivalence, the "forced" tags against what the note actually proves, every green claim against the evidence channel, and the recovery/concurrency reasoning for soundness.

The technical core is strong and I want to say so before the defects: the `(Rproj(d) ∩ ran(M(partner))) \ ran(M(self))` reformulation is exactly equivalent to the spec's comprehensions (and P7 correctly makes it content-only by construction); the torn-read analysis correctly isolates *R-ahead-of-M* as the phantom-DELETED hazard that D-WIT cannot catch; the D-SUBSP/CL-OWN "no document holds another's link as witness" reasoning is faithful; the `origin(a)`-is-derivable vs. green's `sporgladdress`-is-not-derivable contrast is subtle and correct; and the evidence grounding (DOCISPAN append-only, intersection-shipped/difference-not-composed, link-crash, COPY-vs-INSERT identity) is accurate throughout. The hint-vs-must-invalidate distinction between the R-index and the `ran(M(d))` cache is a clean, correct application of the discipline.

Two material problems and some sharpenings:

**1. [DEFECT] — Guarantees table, "Recoverability" coverage-limit: it falsely equates "content deleted from both compared documents" with "no live witness," and misroutes that case to the no-witness (direct-I-address) recovery path.** The note explicitly distinguishes its open Q3 ("deleted from both compared documents but remains **current in a third document** not in the pair") from its open Q2 ("removed from **every** document"). Content deleted from both compared docs may still be current in a third document — it *has* a live witness, just not in the pair, and is recoverable by the ordinary witness-based V-space COPY (from the third doc). The digest collapses Q3 into Q2 under "no live witness," which is factually false for the Q3 subcase and would steer a builder designing the restoration/generalization handoff to use direct-I-address recovery where ordinary witness recovery applies. This also contradicts the digest's own more-careful "How it fits" line (`Rproj(d) \ ran(M(d))` is "witness or not"). Fix: split the cases — "no witness *in the compared pair* but a live witness elsewhere" (Q3; the >2-doc generalization surfaces it; witness-based COPY) vs. "no live witness *anywhere*" (Q2; no live V-position; direct-I-address COPY) — and reserve the phrase "no-witness recovery path" for the latter.

**2. [DEFECT] — "Current arrangement range" approach: the parenthetical "green's deletion and comparison both collapse to I-space set membership" misstates the evidence.** Per evidence Q1, green's *comparison* collapses to I-space set membership, but green's *deletion* operates on **V-space** set membership (per-crum / per-V-occurrence) — the evidence says explicitly "not on I-address set membership." The load-bearing takeaway (multiplicity is discarded; `ran(M(d))` is a dedup set) survives on the note's own basis, but the claim as written is false for deletion. The fix strengthens the corroboration: green's *comparison* (I-space set) corroborates set-granularity classification, while green's *deletion* is per-V-occurrence in V-space — itself the per-occurrence operation the note scopes out — with occurrence count tracked nowhere. That maps green's two behaviors cleanly onto the note's two points instead of conflating them.

**3. [SHARPENING] — "Recoverability" guarantee framing:** SHOWDELETIONS does not itself *recover*; it guarantees witness-backed existence (output ⊆ `dom(C)` + a CURRENT witness in the pair), which *enables* the downstream COPY. The note frames the witness as making deletion "observable as recoverable." Relabel the guarantee to the existence-with-live-witness property and attribute the recovery act to the handed-off operation.

**4. [SHARPENING] — First design commitment cites only D-DISCR for "three-way classification."** The *exhaustive* three-way partition is D-EXH; D-DISCR forces specifically the irreducibility of the DELETED/NEVER_INCLUDED boundary (the "not a set-difference" part). Cite both: D-EXH for the partition, D-DISCR for why it can't be computed from `(C,L,E,M)`.

**5. [SHARPENING] — Torn-read analysis:** note that the benign *M-ahead-of-R* tear is exactly D-EXH's "impossible row" (`a ∈ ran(M(d)) ∧ (a,d) ∉ R`) transiently visible — tying the concurrency hazard back to the note's exhaustion table and reinforcing why composite-granular atomic commit is precisely what restores the boundary invariant the meaning rests on.

VERDICT: REVISE
