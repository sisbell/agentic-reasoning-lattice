I worked through the digest against the note, the claim statements, and the evidence answers. The core engineering content is unusually strong — the set-algebra reformulation `(Rproj(self) ∩ ran(M(partner))) \ ran(M(self))` is provably equivalent to the spec (and stays content-only by P7), the hint-vs-cache distinction (monotone `R`-index is self-correcting; mutable `ran(M)` view must be invalidated) is exactly right, the torn-read analysis correctly maps both tear directions onto D-EXH's rows, and the green grounding (spanfilade append-only, intersection-only `compare_versions`, sporgl-for-deleted, COPY-vs-INSERT identity) all checks out against the evidence. The digest even produces a correct sharper-than-D-DISJ non-emptiness predicate for the note's first open question. So most of this is sound. The problems:

**Revision list**

1. **`[DEFECT]` — "Decisions for the builder → Result caching": "D-STORE forbids persisting them as a document" misreads D-STORE.** D-STORE says the result is *not required* to be stored as a document (permissive — storage is optional); it does **not** forbid storage. The forced part — that the operation creates no persistent artefact *of its own accord* — comes from D-OBS, not D-STORE. A caller may legitimately persist a SHOWDELETIONS result via a separate write (a materialized view, an audit trail), and the note permits exactly that ("not required to be stored… it is a query result"). As written, the digest forecloses a build option the note leaves open. Fix: state that persistence is *optional* (not required); the operation itself writes nothing (D-OBS), so any persistence is a separate downstream write; a volatile cache needs none. Also correct the attribution in the same sentence — memoizability keyed by *state-version* rests on D-RECONS's state-functionality (history-independence), not on D-OBS (which gives repeatability).

2. **`[SHARPENING]` — "Implementation approaches → Output representation": "deleted content has no current V-position" is imprecise for the content this operation reports.** Every address SHOWDELETIONS returns is CURRENT in the partner — that *is* the witness condition — so it has a current V-position, in the witness. What it lacks is a V-position in the document it was *deleted from*. Qualify it ("…no current V-position in the document it was deleted from"), and ground the "return I-addresses" decision in D-IDENT (identity) plus the absent deleted-from position, rather than in the absence of any position anywhere. Green's sporgl/no-V-position case is strictly the *no-witness* case (your sub-case (b)), not the witness-backed report the operation emits.

3. **`[SHARPENING]` — "What must be built": listing "a per-document provenance projection" as a required component slightly overstates necessity and grates against the later "dom(C) scan vs. set algebra" decision.** The hard floor forced by D-NEED is a durable, monotone, *point-membership-queryable* `R`. The document-forward projection `{a : (a,d) ∈ R}` is forced by — and only by — the recommended set-algebra strategy, not by the spec (which is defined on point membership). Say that explicitly so the "must build" floor and the "decide the candidate strategy" choice don't read as inconsistent.

Everything else I checked holds: the "forced" tags are correctly assigned (three-way classification, D-NEED auxiliary state, identity/origin, boundary-licensed meaning), the disjointness pre-check is correctly framed as necessary-not-sufficient, the edge-case subsumption is arithmetically right, and the content-subspace/CL-OWN reasoning (including "this is where green crashes") is accurate and well-grounded. The one material misread is item 1.

VERDICT: REVISE
