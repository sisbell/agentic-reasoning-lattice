This digest is unusually strong. I checked the set-algebra derivation against the spec comprehensions (it is exactly equivalent, with the `dom(C)` restriction discharged for free by anchoring on `Rproj(self)` and P7), verified all four edge cases collapse out of the algebra, confirmed the D-DISJ short-circuit and its careful "necessary-not-sufficient" handling of open question 1, and walked the torn-read analysis (R-ahead-of-M fabricates a phantom DELETED on the *legitimate* row so D-WIT can't catch it; M-ahead-of-R is the self-announcing impossible row). All correct. Every green claim I could trace is grounded in the evidence answers (spanfilade append-only, intersection-only / no list-subtract primitive, COPY-shares vs INSERT-allocates, docISA-prefix origin, link-crash hazard removed by the subspace algebra). No misread "forced/conventional" tags, no altitude slips, no missing load-bearing commitment, no internal contradiction. I found only sharpenings.

**Revision list**

1. **`[SHARPENING]` Implementation approaches (R-journal index vs `ran(M)` cache):** the digest spells out *why* recompute-on-miss fails for the M-range cache ("a stale entry is a cache hit") but only *asserts* the R-index is a hint "recomputable on a miss" without the basis. The asymmetry is monotonicity: `R` is append-only (P2), so a lagging index manifests as a *missing* entry — a miss — which recompute catches; `M` is non-monotone (content added *and* removed), so staleness manifests as a retained-but-removed entry — a *hit* — which recompute-on-miss never reaches. Add that one clause so the contrast is self-justifying rather than asserted; it's the crisp reason the two derived structures get opposite maintenance disciplines.

2. **`[SHARPENING]` Output representation / Decisions (the sporgl aside):** the digest calls green's `sporgladdress` "the *holding* document … not derivable from the I-address," but the verified evidence labels that field "origin doc" / "which document originally placed it." The digest is right on the structure — `sporgladdress` is the docISA of the *vspec being converted*, which for transcluded content is the V-space holder, not the allocator the I-address prefix encodes — but as written it appears to contradict the cited evidence. Add a half-clause ("the document named in the converted vspec, which green's comment calls 'origin' but is structurally the holder, not the allocator") so the "not derivable" point is self-evident and the apparent clash with the evidence is resolved.

3. **`[SHARPENING]` Decisions (result caching):** the memo key is given as `(state-version, d_A, d_B)`. D-SYM makes the two operand orders component-swaps of each other, so one cache entry per *unordered* pair `{d_A, d_B}` serves both orders by swapping the returned halves. Note this as an optional lever — it halves the cache for the symmetric query.

VERDICT: CONVERGED
