Skeptic's pass complete. The digest is strong — the set-algebra reformulation, the per-document `R`-projection insight, and the evidence-grounded output-representation reasoning are genuinely good and need no change. The defects below are real; ordered most-important first.

## Revision list

**1. "Implementation approaches" → Snapshot and concurrency: the named torn-read is the *benign* one; you missed the corrupting one.** The digest calls out "`M` from after a composite, `R` from before" as "the one hazard," tying it to D-WIT's impossible row. But that tear is harmless to the output: `a ∈ ran(M(d))` makes `a` CURRENT regardless of `R`, and the algebra reads `ran(M)` directly for the witness/self conditions — so an `M`-ahead/`R`-stale read just reflects the *completed* composite. The output-corrupting tear is the opposite: **`R` ahead of `M`** (provenance appended, arrangement edit not yet visible). A freshly *placed* address then satisfies `DELETED`'s definition exactly — `(a,d) ∈ R ∧ a ∉ ran(M(d))` — fabricating a phantom deletion. Crucially this does *not* violate D-WIT (it lands on the legitimate DELETED row), which is why the D-WIT lens misses it. Fix: identify `R`-ahead-of-`M` as the hazard (false-positive DELETED), and keep the atomic-root-swap remedy — it closes both directions, but the motivation must point at the malignant tear.

**2. "Design commitments" / "Guarantees" → P4a is underplayed; it is what makes the word "DELETED" honest.** The digest's entire pitch is "content each document *deleted*," but it relegates P4a (historical fidelity) to a passing line in "How it fits." P4★ + P4a together are what make `(a,d) ∈ R ∧ a ∉ ran(M(d))` *mean* "was present and removed" rather than merely "is in the provenance set." Surface this: state that P4a is the guarantee underwriting the operation's semantics, and draw the builder consequence — the append-only `R`-journal must record provenance *only* for genuine placements (the `K.α;K.μ⁺;K.ρ` bundle), never spuriously, or DELETED degrades to a misnomer. (You correctly defer `R`-*maintenance* to ASN-0047, but the *consumer's reliance* on P4a's faithfulness deserves a line.)

**3. "Implementation approaches" → drop or attribute the `links.jsonl` / Rust / `im` specifics.** "This is exactly the substrate's own proven shape — the `links.jsonl` journal with a replayed registry" and the two invocations of "Rust's persistent `im` structures" are grounded in *none* of the note, the evidence, or Green structure, yet are asserted as established ("proven shape"). Naming the `im` crate is also below design altitude. The *grounded* analog — green's spanfilade DOCISPAN, verified append-only and untouched by deletion — is already cited; lead with that and the abstract pattern ("append-only journal with a replayed in-memory index"). If `links.jsonl`/Rust is real project substrate, mark it as project context rather than presenting it as validated by this note.

**4. "Guarantees" → Content-subspace restriction: give the *witness-sharing* reason, not just `output ⊆ dom(C)`.** The note's reason SHOWDELETIONS is meaningful only over `s_C` is structural: content addresses can be *shared* across documents (so a partner can serve as the still-current witness), whereas link material cannot — by CL-OWN, a document's link V-positions reference only its *own* link addresses, so no document ever holds another's link as witness. The digest reduces D-SUBSP to "output ⊆ `dom(C)`" plus "don't commingle link provenance." Add the CL-OWN witness-asymmetry — it is *why* cross-document deletion comparison is structurally impossible over `s_L`, not merely absent from the output.

**5. Missing D-DISJ — and the fast-path it licenses.** D-DISJ (disjoint `R`-projections ⟹ empty report) is a stated lemma the digest omits entirely, and it is also a real optimization the set-algebra should exploit: non-empty output requires `Rproj(d_A) ∩ Rproj(d_B) ≠ ∅` (at a boundary, `a ∈ ran(M(d_B)) ⟹ a ∈ Rproj(d_B)` by D-WIT). A cheap disjointness pre-check short-circuits unrelated documents before any arrangement read. Add it as a builder optimization and as the partial answer to the note's first open question ("what characterisation of shared content history… predicts non-empty results").

**6. "Design commitments" → set-granularity is mislabeled "forced by the predicate definitions."** That is circular — the predicates were *chosen* to read only `a ∈ ran(M(d))`. The actual forcing is deeper: the foundation's `R` is over element×document pairs and carries **no occurrence-level provenance**, so SHOWDELETIONS *cannot* see per-occurrence removal even in principle; the note then flags the finer operation as out of scope (a Vstream concern needing finer state). Reframe accordingly — it's forced by `R`'s element-granularity and deliberately scoped, not forced by the predicate forms.

**7. "Implementation approaches" / "Decisions" → reconcile the enriched output with D-ORIG.** The recommended `(content-address-span, origin-document)` encoding reads as if origin must be carried, but D-ORIG explicitly says "the output need carry no extra origin annotation" — origin is *derivable* from the address prefix (verified, Q18). State that the enriched form is a *denormalization for the restoration consumer's convenience* (matching green's sporgl, which carries `sporgladdress` even though it's structurally derivable), not a correctness requirement — otherwise it appears to contradict D-ORIG.

**8. "Implementation approaches" → "indexed address-forward" mischaracterizes green's spanfilade.** Per the evidence the spanfilade is *2D*-indexed (docISA-prefix `ORGLRANGE` × I-span `SPANRANGE`); document-forward enumeration is *structurally supported* (the prefix dimension is the document) but not *exposed* as a composed function — the gap is the missing composite (`retrievesporglsetinrange` + `retrieverestricted` are present), not the index orientation. The actionable advice (provide a doc-forward read path) is fine, but state the gap accurately: compose the read path green's structure already supports, rather than implying green is one-directional.

**9. "What this is" / "How it fits" → "first load-bearing consumer of `R`" isn't grounded in this note.** Plausible and consistent with the note's framing, but the note never claims precedence over the corpus. Soften to "the operation that makes `R` load-bearing," or cite where precedence is established.

Solid and to be left alone: the `( Rproj(d) ∩ ran(M(partner)) ) \ ran(M(self))` derivation (verified equivalent to the spec, and the `dom(C)`-scan-avoidance via P7 is a real improvement); the "what the operation actually consumes is the projection, not point membership" framing; and the I-address/sporgl output reasoning.
