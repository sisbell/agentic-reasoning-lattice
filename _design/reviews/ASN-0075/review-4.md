I went through the digest against the note, the formal claims, and the evidence. This is a strong digest: the central read (SHOWDELETIONS is what makes `R` load‑bearing; `(C,L,E,M)` provably can't discriminate) is accurate; the `forced` vs. `scoping` vs. `convention` tags are correctly assigned; the set‑algebra reformulation `(Rproj(self) ∩ ran(M(partner))) \ ran(M(self))` is provably equivalent to the spec's `dom(C)` comprehensions and correctly anchored on `Rproj` (content‑only by P7); the D‑DISJ short‑circuit, the edge‑case subsumption, and the torn‑read analysis (R‑ahead‑of‑M fabricates a phantom DELETED on the *legitimate* row D‑WIT can't catch; M‑ahead is benign) all check out. Every Green claim is grounded in the evidence (spanfilade append‑only/untouched‑by‑delete, intersection‑shipped‑not‑difference, sporgl for V‑position‑less content, COPY‑restores‑identity, I‑address‑prefix origin). It holds design altitude throughout. I found no material problem — only precision sharpenings.

## Revision list

1. **[SHARPENING] "Guarantees to uphold," Recoverability row — separate "no witness *in the pair*" from "no live copy *anywhere*."** The coverage‑limit parenthetical lumps "content deleted from both compared documents, *or* from every document" under "no live witness," then points both at "the no‑witness recovery path." But content deleted from both `d_A` and `d_B` yet *current in a third document* (the note's open question Q3) is not globally witness‑less — it has a live copy and is recoverable witness‑based from that third doc; only content deleted from *every* arrangement is truly orphaned and needs the direct‑I‑address COPY. Your "How it fits" section already scopes the no‑witness primitive correctly to content that "has left every arrangement"; align the guarantee row with it: (a) deleted‑from‑both‑but‑live‑elsewhere = the operation's coverage limit and Q3, (b) deleted‑from‑every‑arrangement = the no‑witness `Rproj(d)\ran(M(d))` + direct‑I‑address path. Naming Q3 here also strengthens the open‑question hand‑off.

2. **[SHARPENING] "Decisions for the builder," Result caching — soften the D‑STORE attribution.** "D‑STORE forbids persisting them as a document" reads it as a prohibition; D‑STORE actually *describes* the operation ("creates no persistent artefact *of its own accord*," a D‑OBS consequence) and says the result "is not required to be stored." A caller is not forbidden from snapshotting a result. Restate as: the operation auto‑persists nothing, so a *volatile* memoization cache keyed by (state‑version, `d_A`, `d_B`) is fine and integrating the result into the persistent store is outside the operation's contract. The actionable guidance is already right; only the "forbids" framing overstates the claim.

3. **[SHARPENING] "Implementation approaches" — the `R`‑index "self‑correcting hint" vs. `ran(M)` contrast is true for point‑membership but not for the projection read‑path you recommend.** You recommend reading `R` as the document‑forward projection `{a:(a,d)∈R}`; a lagging index then returns an *incomplete set* — a silent stale **hit**, exactly the failure mode you (correctly) flag for `ran(M)`. The genuine distinction isn't "self‑correcting vs. not," it's that `R` is append‑only (the index is rebuilt by **forward replay** with no removal logic, and is log‑backed for durability) whereas `ran(M)` is **mutable** (the cache must handle removals, so invalidate‑and‑recompute at commit). Recast on monotone‑vs‑mutable rather than self‑correcting‑vs‑not; both derived views still must be brought current at the composite commit.

4. **[SHARPENING] "Implementation approaches," set algebra — "precisely the set‑difference udanax‑green never composed" undersells your own point.** Green's missing bare POOM/I‑span difference would itself be *insufficient* here: per the evidence (Q15), "I‑addresses unique to D's POOM" conflates post‑fork additions with deletions‑from‑the‑other — a difference with no `R`‑anchor cannot name DELETED. What green lacked is the list‑level subtract *primitive*; what makes your composition *correct* is anchoring the subtract on `Rproj(self)` (the D‑DISCR result). Add a half‑clause to that effect so the reader doesn't infer green's absent primitive would have sufficed.

The remaining sections — "Design commitments," "What must be built," "Guarantees" (other rows), "How it fits," and the wp/D‑BOUND "meaning licensed only at a boundary" treatment — are sound and need no change.

VERDICT: CONVERGED
