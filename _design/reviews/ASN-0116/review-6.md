This is a strong, technically faithful digest. Before the revision list, the load-bearing pieces I checked and found sound:

- **The forced/conventional/free taxonomy is exactly right.** It correctly reports the note's *proof* that no single K-atomic can both rewrite existing-suffix images (forbidden by K.μ⁺ prior-domain agreement) and grow the domain (forbidden by K.μ⁻/K.μ~), so the contraction-then-extension structure is forced *given the atomics* — while correctly classing the physical *fusion* of the pair as free, and grounding that in Green's shift-in-place (`makegappm`) + add (`insertcbcnd`), which never literally vacates the suffix (Q12/Q19/Q20). This is the subtlest claim in the note and the digest nails it.
- **Allocation grounding is precise and load-bearing.** The "allocate from the content-store *history*, never the current arrangement, so a delete-all/re-insert never reuses addresses" distinction (note sub-case b) is correctly identified as the thing an allocator-from-arrangement would break, and the Lampson hint-cache recommendation honors don't-duplicate-authoritative-state. No allocator/representation approach violates origin-identity (coalescing merges representation by I-adjacency, never content).
- **Forbidden-approach hygiene holds:** content-addressed storage is correctly rejected as the very dedup the note forbids; backward-only coalescing is correctly licensed by the note's no-forward-merge proof (frontier argument); tight-endset lever (IP6c) and write-only-index over-reporting (Q16) are accurately imported with their tradeoffs.
- Green source-level claims (`findpreviousisagr`+increment, two-blade cut, +2/+0 coalescing cost, GRANTEXTLENGTH packing, APPEND-skips-DOCISPAN, CREATELINK-breaks-contiguity) are all grounded in the evidence answers; no fabricated function behavior. Citations to ASN-0093/0082/0047/0036/0058/0034/0098 are correct.

## Revision list

**[SHARPENING] §"Implementation approaches" Approach 4 and §"What must be built" (reverse-lookup):** The digest bundles two genuinely distinct reverse queries under one label — *"discoverability (which documents/links touch this content)"* — then attaches a link-specific correction to the bundle: *"the link store is required, not arrangements alone (LP12)."* That correction is true only for the facet the note actually formalizes: *link*-discoverability `D(d,Σ)`, whose test `coverage(e) ∩ ran(M(d)) ≠ ∅` (LP12) genuinely needs the link store. *Document*-discoverability — the evidence's `finddocscontaining` over `DOCISPAN.homedoc` — needs only the per-document arrangements (which `d'` have `a ∈ ran(M(d'))`); the link store is not required for it. Split the two facets: keep "one reverse index (spanfilade) serves both," but state that their recompute inputs differ (link query needs link store + arrangements; document query needs arrangements alone), so a builder doesn't over-require the link store for content-in-which-documents queries.

**[SHARPENING] §"Design commitments" / §"What must be built" / §Approach 4 (coverage invariant):** Tighten "every address carries a record" to "every *content* (element-level) address." `R ⊆ T_elem × E_doc` and its coverage invariant range over content addresses; INSERT leaves the link store frame-invariant (F-LINK), so the unqualified "every address" risks reading as if link addresses are covered by `R`.

(Two candidates I considered and dropped as non-issues: the "permascroll used loosely / one content layer, not a log-plus-index pair" framing is accurate — the evidence puts content directly in granfilade leaf crums, Q14/Q15/Q20, not in a separate log behind an index; and the atomicity/crash-recovery framing, though absent from the note's vocabulary, is a legitimate builder concern correctly derived from the note's coverage/density invariants and labeled as an approach, not a note guarantee.)

VERDICT: CONVERGED
