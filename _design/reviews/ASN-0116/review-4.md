The digest is, on the whole, unusually disciplined: the forced/conventional taxonomy is accurate (the `shift(t,0)=t` convention, the proven-mandatory two-step arrangement decomposition, the free physical fusion), every Green source-level claim is grounded in the evidence channel (query-and-increment allocation, no dedup, two-blade subspace cut, `homedoc`/DOCISPAN provenance, pull-model link resolution, `GRANTEXTLENGTH` packing, the contiguity-break from interleaved link-creation), and the hard subtleties — allocator-keys-off-content-region-not-arrangement, R-is-primary-not-decodable-under-transclusion, atomic provenance coupling, IP4/IP6 ghost-reference/resurrection — are all carried correctly and at the right altitude. The approaches are sound Lampson moves with defensible defaults and no approach violates a commitment (it explicitly forbids content-addressing, rejects the persisted counter, coalesces backward-only). I found no misread, no ungrounded claim, no altitude slip, and no missing load-bearing component. The remaining items are genuine but non-load-bearing tightenings.

**Revision list:**

1. **Approach 4 ("one structure, two facets")** — *`[SHARPENING]`* The digest labels both provenance *and* link-discoverability as served by "the same DOCISPAN reverse index," but the evidence (Q16) explicitly separates them: "INSERT writes to the DOCISPAN portion of the spanfilade but never touches the link index portion." DOCISPAN (content→document containment) is where `homedoc`=R lives and answers "which documents touch this content"; link discoverability (the note's `D(d,Σ)` via LP12) is served by the *link-endset* portion (Green's `sporglset2linkset`, Q18). The "one structure, don't build two stores" advice is correct (both are sub-indices of the single spanfilade), but reword to distinguish the two sub-indices rather than calling both "DOCISPAN." Relatedly, "recomputed by scanning arrangements on load" is incomplete for the link facet: recomputing `D(d,Σ)` joins the link store's coverages (L) against document ranges (M), not arrangements alone — say "scanning link coverages against document ranges."

2. **"Forced vs free" paragraph — the fusion description** — *`[SHARPENING]`* "Green … adds the new piece (`insertcbcnd`) **in a single pass**" overstates the mechanism: `makegappm` (shift-displacements-in-place) and `insertcbcnd` (add) are two phases within one insert, not one traversal. The load-bearing claim — that Green never materializes a literal contraction-then-extension — is correct and grounded; reword to "in one in-place displacement update plus an add, never materializing a contracted intermediate."

3. **Approach 3 — the "+2/+0" coalescing cost** — *`[SHARPENING]`* The digest attributes "sequential typing costs +2 for the first character and +0 thereafter" to `isanextensionnd`. The numbers are right, but Q15's explicit correction states the +0 on the content/GRAN side is `fillupcbcseq`, **not** `isanextensionnd` (which governs only the POOM half). Attribute the cost to "the two coalescing mechanisms (POOM `isanextensionnd` + GRAN `fillupcbcseq`)." (Credit where due: the digest already correctly avoids the `ONMYRIGHTBORDER` mis-attribution the same evidence warns against, describing the test as `reach == origin` under same `homedoc`.)

VERDICT: CONVERGED
