This is a strong digest — accurate to the note's two-layer model, scrupulously grounded in the evidence channel (it leans correctly on Q1, Q11–Q20 for every Green source-level claim rather than inventing behavior), at the right altitude (it cites Green's `makegappm`/`insertcbcnd`/`isanextensionnd`/`DOCISPAN` as *proven mechanisms* without dropping into types or signatures for the new system), and it carries real positions a builder can act on. The forced/conventional/free taxonomy is correct, the contraction-then-extension "forced by the K-vocabulary but physically fusible" reading is faithful, the discoverability-vs-provenance / write-only-over-reporting caveat is a genuine catch, and all four open questions are mapped. I could not find a material defect. The items below are precision improvements only.

# Revision list

**1. [SHARPENING] Implementation Approaches §3 / Decisions (coalescing): "share origin ↔ same `homedoc`" is imprecise — per evidence Q1, the POOM crum's `homedoc` is *cleared to 0* for fresh INSERT text, not set to the origin document.** The digest equates the conceptual conjunct "share origin" with Green's `homedoc` check. For fresh inserts both runs carry `homedoc = 0` (Q1: `tumblerclear`), so the substantive discriminator is `reach == origin` (I-adjacency), and the note's own backward-merge condition (M7) is *I-adjacency alone* — which already implies same-origin, since cross-origin entries lie on disjoint content chains (CrossDocumentDisjointness). Tighten to: coalesce backward on I-adjacency; present the `homedoc` field as the Green guard (and note its 0-for-fresh-text value), not as "the origin." The advice still produces correct behavior — this is precision, not a behavior fix.

**2. [SHARPENING] Implementation Approaches §3: the "+1 (piece table) vs +2 (Green)" first-unit cost comparison counts Green's content structure but not the piece-table design's.** Green's +2 is granfilade *crum* (content) + POOM *crum* (arrangement). A piece-table design still needs an I-address-keyed content store (Approach 1's "persistent ordered map keyed by tumbler address," and links resolve *by I-address*, so the I-keyed index can't be dropped) — so its honest first-unit cost is also content-entry + piece = +2, both coalescing to +0 after. The real win is *representational* (V-positions computed not stored; one fewer indirection on the read path), not a node-count halving. Restate the comparison on equal footing.

**3. [SHARPENING] "What this is": INSERT "records who created it" should read "records which document *placed* it."** The rest of the digest carefully distinguishes *placer* from *origin* (and correctly hangs the transclusion open-question on exactly that gap); the intro's "created" silently collapses the two. For INSERT-in-isolation they coincide, so this is only a consistency tightening.

Sections that are genuinely solid and need no change: the **Design commitments** (origin-not-value identity, allocation keyed off the content *region* not the arrangement, the delete/re-insert never-reuse consequence), the **allocator** decision (Lampson recomputable-hint over persisted-counter, with the correct "never reset on empty arrangement" warning), the **provenance/discoverability one-structure-two-facets** treatment with the write-only over-reporting caveat, the **pull-model link survival** and IP4/IP6 ghost-reference/tight-endset analysis, and the **backward-only coalescing** justification (the forward-merge-impossibility argument is reproduced correctly).

VERDICT: CONVERGED
