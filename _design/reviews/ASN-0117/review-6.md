This is an unusually strong digest. It reads the note accurately, separates spec-level "by construction" guarantees from what needs active enforcement (containment, subspace confinement), grounds every Green source-level claim in the evidence channel, and — notably — correctly resolves a genuine internal tension in the Q4 evidence (KB-synthesis says "entirely-before → no-op," code-exploration says "entirely-before → corruption") by recognizing the difference is the exponent guard, which fires only cross-subspace and so does *not* protect a same-granularity text delete. Coverage of the note's claims, open questions, and the evidence is complete, and the approaches are committal and sound (none violate a commitment; the discovery-index hint+post-filter is correct, the structural content/arrangement separation is right, the I-address-keyed link survival is right). I could not find a material defect. The items below are real but non-load-bearing.

**Revision list**

1. **[SHARPENING] Approach §3 and Decisions/"Within-document sharing": disentangle the two "last-witness" tests.** The "last-witness" label is stretched across two distinct computations feeding two distinct indexes: the *address-level* test (`A_del^{excl}` — did a deleted address's last occurrence in `d` vanish, i.e. did it leave `d`'s range), which drives the content→document index, and the *link-level* test (the wp — did a link lose its last anchor in `d`), which drives `find_links`-from-`d`. They are the same structural "is any witness left?" question at two granularities (per-address vs per-link), but the phrasing "the last-witness check for whether an address has truly left `d`'s range" / "the same one the within-document-sharing case needs" risks reading as one computation. §3 already draws the distinction ("the link index needs the extra last-witness layer"); make the Decisions bullet match by naming both tests and stating they are the same form applied per-address and per-link.

2. **[SHARPENING] Design commitments/"Two layers": make the identity principle explicit.** "an append-only content store `C : I-address ⇀ Value` whose identity is by origin" is correct but understates a principle the digest itself leans on (Approach 6's I-address-keyed links; the anti-dedup posture). State it as *identity by I-address (permanent, assigned at creation), never value-based* — this is exactly what forbids content-dedup and underwrites transclusion and link survival, and the note rests on it ("Sharing is by reference to immutable identity").

3. **[SHARPENING] How it fits: separate the content store from its frame lemma.** "Leans on the content store / Istream foundation (the ContentStoreFrame of ASN-0082)" conflates the content store (a state component the note takes as *given* from the arrangement-model foundation) with the ContentStoreFrame (the ASN-0082 lemma `Σ'.C = Σ.C`). Split them: DELETE leans on the immutable append-only content layer (state model) and *invokes* ASN-0082's ContentStoreFrame to hold it framed. Citation precision only.

4. **[SHARPENING] Approach §1 (absolute-key map): tidy the cost figures.** "O(suffix·log N) work and O(suffix) freshly path-copied nodes" mixes a work bound with a node-count bound — the remove-and-reinsert churn transiently allocates ~O(suffix·log N) nodes while the net retained delta is ~O(suffix). Minor; either qualify the two numbers or drop the exact counts, since the qualitative point (a front delete defeats suffix sharing) is what carries the recommendation.

5. **[SHARPENING] Approach §5: flag the past-end no-op as a deduction.** "a span lying entirely past the run's end is a silent no-op" is correct and grounded — but by deduction from Green's documented case-0 classification (crums before the cut are left untouched), not from a directly-worked evidence scenario (the evidence works the entirely-before and straddling-start cases explicitly). Optionally anchor it to the case-0 behavior so the grounding stays traceable. Marginal.

VERDICT: CONVERGED
