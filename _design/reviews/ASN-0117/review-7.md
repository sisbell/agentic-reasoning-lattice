This digest is unusually strong: accurate against the note, scrupulous about grounding (it consistently separates note-claims, evidence-settled claims, and its own design contributions — e.g., explicitly tagging persistent roots and the O(log N) shift as "efficiency to build in, not behavior Green demonstrates"), at design altitude throughout, and committal where the note supports a position. The §5 totalization analysis is actually sharper than the note — it correctly splits out the silent-no-op case (span past the run's end → all survivors classify before the cut) from the corruption case (span before/straddling the start), and correctly limits the exponent-guard "protection" to cross-subspace. The within-document-sharing / count-and-last-witness treatment and the per-document-vs-global-orphanhood (LP17/LP18) handling are complete and correct. I found no material defect.

# Revision list

1. **`[SHARPENING]` Approach §7 / "What must be built" — surface the *fusion* half of the span-decomposition re-cut, not just the split half.** The digest covers splitting a run at an interior delete endpoint (slicecbcpm), but the note's S8★ paragraph stresses the symmetric effect: closing the gap can *fuse* two previously-separate runs whose I-addresses become contiguous in V (`M'(d)(q_{J−1})=a_{J−1}` now abuts `M'(d)(q_J)=a_{J+c}`). This is exactly why a run-coalescing representation needs a recombine pass *after* the shift (the digest names `recombine` as machinery in §1 but never connects it to this semantics). Add one line: in a run-coalescing rep, gap-closure can merge adjacent newly-contiguous runs, so the post-delete normalization must coalesce as well as split; the abstract whole-position operation is unaffected.

2. **`[SHARPENING]` Approach §1 — the "delete loop splits" attribution conflates two Green phases.** The sentence makes "its delete loop" the subject of both *shifts* and *splits*, but per the evidence (Q11/Q12) the splitting is done by `makecutsnd`/`slicecbcpm` in a phase *before* the child-classification loop, while the loop only classifies/shifts/frees. The grounded substance ("Green splits only units straddled by a strictly-interior cut") is correct; just attribute the split to a prior cutting phase rather than to the same loop that shifts the suffix, so the phase structure reads accurately.

3. **`[SHARPENING]` Design commitments vs. Guarantees — reconcile "subspace confinement is principled, not incidental" with "Green achieves it only accidentally."** Both are true but read as in tension. Make the level explicit: the *abstract operation* is principled (D-SHIFT fixes the subspace component by construction), whereas *Green's implementation* secures it only by the exponent-guard accident — which is precisely why the builder must scope the shift explicitly and not replicate Green's mechanism. The two statements are then plainly consistent rather than apparently contradictory.

4. **`[SHARPENING]` Approach §3 — "the content→document index loses exactly the addresses that leave `d`'s range" is loosely worded.** The index is append-only and loses nothing on its own; it is `d`'s *range* (the ground truth) that loses `A_del^{excl}`, leaving the index stale on exactly that set until cleaned. The "drives its cleanup directly" clause makes the intent recoverable, but tightening the subject (truth changes; index goes stale) removes the only spot where the otherwise-precise hint-vs-truth distinction blurs.

These are all non-load-bearing; the digest is sound and actionable without them.

VERDICT: CONVERGED
