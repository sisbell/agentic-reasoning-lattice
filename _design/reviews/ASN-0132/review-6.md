The digest is accurate, well-grounded, at proper altitude, and genuinely useful. Every one of the note's eleven claims is covered; the forced/conventional markings are all correct; the Green source-level claims (count-costs-as-enumeration, the `TRUE||` dead-coded home filter, the `onlinklist` off-by-one yielding "counted twice," count disabled in multi-session) are each grounded in the evidence answers; and no proposed approach violates a note commitment. The dedup section (§2), the active-view/`nullified` handling (§3), the epoch-cache reasoning (§5), and the hint/authoritative persistence taxonomy (§7) are all sound. I found no material problem — only refinements.

**Revision list:**

1. **`Design commitments` (CN-UNIT bullet) and `What must be built`: name version-refraction explicitly and state fork-inertness.** [SHARPENING] The note singles out version-refraction as the canonical instance of appearance multiplicity ("a link to one version of a document is a link to all versions") and devotes a paragraph to forking being link-store-inert (J4, ASN-0047); the evidence (Q1) confirms fork creates *no* new link addresses and leaves the source-homed link as the sole copy. The digest folds this entirely under "how many places it surfaces" and "only `K.λ` changes the count" — correct, but it never says "version" or "fork." "Does forking double my count?" is exactly the concrete builder question a digest should preempt; add one line: versioning surfaces the same single link (appearance multiplicity → 1), and because fork touches only the content subspace and allocates no link address, it is `Σ.L`-preserving, so the count is invariant under it. Sharpening, not defect — the substance is already entailed by the stated locality/`K.λ` commitments.

2. **`Implementation approaches §1` (per-slot index bullet): tighten "It could make counting cheaper than enumeration."** [SHARPENING] As written it sits one clause before "but Green exploits no such asymmetry," and a builder reading the bullet in isolation could infer that a spanfilade-style coverage index *by itself* buys count-cheaper-than-enumeration. It does not: candidate-retrieval + intersect still materializes the matching set to count. The asymmetry requires the enfilade's internal nodes to carry *aggregate cardinalities* (or maintained per-region counts) — which is exactly §6's "counting index / maintained aggregate counts." Cross-reference §6 here, or qualify "could" as "could, *if* the index is enriched to answer cardinality without materializing." The takeaway survives (Green has the index and still pays full cost), so this is a crispness fix.

3. **`Implementation approaches §1` (Pick, final parenthetical): soften "Green's spanfilade was present from the start, not bolted onto a prior scan."** [SHARPENING] The verifiable substance — Green indexes natively, with no scan-first baseline — is supported by the evidence; the architectural-*history* claim ("present from the start") is not checkable against the note or the evidence answers. State the supported fact ("Green has no scan baseline; the spanfilade is its native link index") and drop the developmental-history flourish.

I considered whether §4's "`+1 − k`, net 0 in the common single-target case" should be tied to the unit-depth retraction discipline (which makes single-target the norm), but the general formulation is correct and safe, and the note does not clearly forbid a multi-span retractor, so leave it. The arithmetic and the "increment retractor unless self-nullifying / decrement newly-nullified" maintenance rule are right.

None of these is load-bearing; the digest is sound and correct as it stands.

VERDICT: CONVERGED
