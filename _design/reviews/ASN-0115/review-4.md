This is a strong, unusually careful digest. Before the list, what's genuinely solid so I don't bury it: the forced-vs-chosen split is accurate (ordered-sequence, pure-read, current-arrangement-only, no-dedup are correctly "forced"; ordinal-confinement, the depth-compat override, and the subspace identifiers correctly "chosen"); the depth-compatibility handling, the bindable-slice scoping of R6's no-interior-hole guarantee, the "don't hash-address content" warning against the S4 violation, the snapshot-vs-version distinction, the R7-as-sufficiency / cache-on-the-restriction reasoning, and the "delivery never reads Σ.L; a link reference *is* the resolved address" point are all correct and sharply stated. Green claims are grounded in the evidence throughout; no fabrication, no altitude slips, no missing R-claim. The defects I went looking for aren't there.

# Revision list

1. **[SHARPENING] "Implementation approaches" / "What must be built": reconcile the two-phase resolve-*then*-fetch framing with per-position subspace dispatch, and make the divergence from Green explicit.** The digest already says the fetch is "for content values only" and "a link position needs *no* store read," but the "resolve-all → fetch-all-by-address" framing sits right next to evidence (Q19) that Green's fetch (`ispanset2vstuffset`) is *subspace-blind* and returns garbage for link addresses. Spell out: the fetch phase applies to **content positions only**; a link position's resolved address *is* the output and never enters the content fetch. Note that the ordinal-level gate rejects *straddling* spans but does **not** reject a valid *link-only* spec (R10) — so the item-former's subspace dispatch, not the gate, is what keeps link-only specs from reproducing Green's documented footgun. This is the single place a builder copying Green wholesale would go wrong.

2. **[SHARPENING] "What this is": tighten "link references rather than the addresses … that every other span-taking operation returns."** Read literally this implies RETRIEVEV returns no addresses, but a link item's payload *is* an I-address (`a ∈ dom(Σ.L)`, R9) — as the digest's own commitment #3 says ("a reference (the address)"). The note locates RETRIEVEV's *uniqueness* (R1) specifically in delivering content **values** (not locations); for links it delivers the link entity's address *as a reference* — kind-distinct from a content value, but still an address. Reword so the value-not-location claim is content-scoped and the link case is "the entity's own address, delivered as a reference," consistent with commitment #3 / R9.

3. **[SHARPENING] "Design commitments"/"Guarantees": state R8's *non-disclosure* explicitly.** The digest covers no-dedup (R8.iii) and content-origin-not-in-output (R9), but not R8's guarantee that co-delivery of transcluded content is **byte-indistinguishable from coincidentally-equal content at distinct addresses** — the output discloses nothing about the sharing. It falls out of R1 (values, not addresses) and is touched in the caching discussion (S4), but it's a named R8 guarantee a builder should know not to break (e.g., by tagging co-referent items with a shared-origin marker). One line under no-dedup.

4. **[SHARPENING] "Content store": the atomic-writes / torn-reads caution is generic and slightly out of scope.** It concerns the store's *write* path, which the digest itself attributes to ASN-0036 in "How it fits"; for a pure-read operation it reads as imported infrastructure advice. Either tie it explicitly to the index-replay-on-load it's adjacent to (which RETRIEVEV does touch at startup) or trim it. Lowest priority.

VERDICT: CONVERGED
