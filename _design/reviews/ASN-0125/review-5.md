The digest is unusually accurate and complete. Every Green source-level claim is grounded in the evidence answers (Q11–Q19) and honestly scoped (e.g., the "log-plus-replay" shape is explicitly flagged as the digest's own, narrower than Green's enfilade). The forced/chosen tags are all defensible; the historical/operative split, EL14(e)'s activity-agnostic membership, and EL0's reference-survival criterion are rendered correctly; all nine open questions are surfaced as policy/scope with correct OQ numbers. Most importantly, the two highest-risk implementation traps — content-deduping links (forbidden by L11b/NonInjectivity) and taking the allocation max over the live listing rather than the append-only record (the Q12 hazard) — are both called out explicitly, as is the DC guard's real function (a retraction-class successor would silently nullify its to-target and break chainable discipline). I found **no material defect**. The items below are refinements only.

**Revision list**

1. **[SHARPENING] Implementation approaches → historical/operative read model.** "Derive `succ_o` by filtering edges whose claim address is nullified" is correct *only* if the `succ_h` structure is a **multigraph keyed by claim** (one edge per claim), because distinct claims can assert the *same* `(old, new)` pair — re-assertion after `Nullify` (R6c) and two parties independently asserting the same supersession both produce this. A pair is operative iff *some* supporting claim is non-nullified, not iff its (assumed unique) claim is. The digest's own phrasing ("edges whose claim address") and the currency bullet's "supporting claims" (plural) already imply the per-claim model, but state the multigraph nature outright so a builder doesn't collapse to a simple graph with one nullified-flag per `(old,new)` edge and wrongly drop a re-asserted pair from `succ_o`.

2. **[SHARPENING] Guarantees / How it fits → reader-facing contract.** The note crystallizes an executable reader protocol the digest leaves scattered: the conjunction of EL9(1) + EL11(b) + EL8(b) — the old link gives *no forced sign* of replacement (it is unchanged, by design), but the sign always exists, is findable archivally at every state regardless of arrangement, and arrives attributed; so the reader's rule is "before relying on a link, ask the record what targets it" (`in(a)`). All pieces are present but never stated as one line; doing so gives the reader-layer builder a single target.

3. **[SHARPENING] Discovery, two regimes → contextual.** "A claim surfaces in a document iff that document currently lists the endpoint" is true but under-specified: only a link's *home* can list it (CL-OWN/Df-LISTED), so contextual visibility of a claim is available *only at the endpoints' homes* — exactly one document's arrangement can volunteer a given claim's to-side. Add the "only at home" qualifier so a builder knows which arrangement to consult.

4. **[SHARPENING] Decisions for the builder → currency policy.** "The substrate returns `current(y)` entire" mislabels the layer: `current` is an *editing-layer* query built on substrate primitives, not a substrate operation — the digest is careful about this distinction everywhere else (e.g., "uniqueness is a reader policy, never a substrate promise"). Say "the layer returns."

VERDICT: CONVERGED
