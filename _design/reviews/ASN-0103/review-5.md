I reviewed the digest against ASN-0103, its claim statements, and the verified evidence answers. This is a strong digest: it correctly separates the forced commitments (place/content seam, level-aware allocator, structural ownership, atomic single transition) from the merely conventional (cached per-account counter), and the green integration is genuinely careful — the "single zero-width placeholder crum, *not* zero crums" correction (Q13/Q18), the find-then-insert crash hazard and the journal-as-commit fix (Q20), the BERT/open separation (Q17), and the structural place/content firewall (Q16) are all grounded and well-deployed. Every load-bearing commitment, component, guarantee, and open question is covered. I found no misreads, no ungrounded Green claims, no altitude slips, and no missing locks. The items below are refinements, not material problems.

## Revision list

1. **`[SHARPENING]` "How it fits" (Hands to → attribution): the P6 citation is coupled to the wrong guarantee.** The parenthetical "*(existential coherence, P6)*" is attached to "content origins derivable from the address alone," but P6 guarantees only that each content address *has* a persisting origin document (origin existence/coherence) — **not** that the origin is *derivable from the address*. Address-derivability is precisely open Q5, which the same sentence then flags as open — an internal tension (citing P6 for derivability, then calling derivability open). Reword: P6 gives origin-existence/coherence; making the origin derivable from the address alone is the attribution layer's job and is open (Q5).

2. **`[SHARPENING]` Design commitment 1: the cost characterization is stated as a lock but is implementation-dependent.** "*(a rightmost descent of the prefix-ordered registry tree, not a linear sweep)*" holds only under an ordered/prefix-keyed index — which you yourself list as a *builder decision* (hash-plus-ordering is offered as an alternative), not a lock downstream cannot violate. The genuine lock here is "stores nothing / no content written." Either qualify the cost as "with the recommended ordered index" or move the descent-vs-sweep detail to Implementation approaches, leaving only the forced part in the locks section.

3. **`[SHARPENING]` Implementation approaches (allocation): "hint" is loose for the pure-recompute frontier.** A value recomputed fresh from the authoritative registry on every call is *derived-not-stored* and always correct — not a Lampson hint. The genuine hint is the *cached per-account counter* (may be stale, checked by rescan) and the replay-rebuilt in-memory index. Reserve "hint" for those; describe the stateless frontier as derived state, so the Lampson framing stays exact.

4. **`[SHARPENING]` Subspace activation: the `A_L` ⊇ type mapping is an inference the evidence doesn't directly support.** Q14 shows the type subspace `3.x` as a *distinct* subspace with its own anchor, with the link floor at `2.x`; the note's `A_L(d)` carries a single anchor `[d.0.s_L]`. "The note's single link sub-allocator `A_L(d)` being realized by Green's link *and* type subspaces together" is the digest's reconciliation, not something the note or evidence states. The load-bearing point (note = 2 sub-allocators, green = 3 subspaces, all lazy, don't pre-build) is correct and already well-hedged; soften the specific claim to "green adds a third (type) subspace beyond the note's two-sub-allocator abstraction."

The Decisions-for-the-builder section (especially the idempotency analysis — no idempotency key, orphan-on-retry harmless-but-unintended, exactly-once must live at the session layer) and the Guarantees split (hold-by-construction vs require-active-enforcement, with uniqueness-across-failures correctly singled out as "the one that bites") are both sharp and need no change.

VERDICT: CONVERGED
