Reviewing the decomposition against the corpus. Completeness is strong: I walked every "what must be built" item across all 41 digests and every capability lands in a module (the three dependency inversions — M2 generic-over-keys, M7→M5 via a semantics-blind seating API, M3→M5 broken by lazy arrangement — are each explicitly engineered and sound, and the DAG is acyclic with a valid build order). The non-obvious cross-note catches (J0 forcing pred-def content through M5's placement composite; the level-aware version-vs-document frontier in M3; R as authoritative-but-non-recomputable history; Observe kept in M7 so PL doesn't route through M8) are right. But one factual characterization is wrong in a way a builder would act on.

---

**1. [DEFECT] — M2's J-coupling / M5's R-coupling description is factually wrong about which operations couple, and is internally self-contradicting.** M2 states the J-couplings "(content-allocation ⇒ placement ⇒ provenance) are M5-only, co-occurring **solely in M5's INSERT/COPY composite**." This is false on three counts: (a) **COPY allocates no content** (ASN-0118 CP1: "the content store does not grow on COPY"; "runs no allocate/mint step") — it triggers only J1★ (placement⇒provenance), never the content-allocation link J0, so naming it a site of the full chain is wrong; (b) **VERSION** (listed in M5's own components) records provenance for every transcluded address (ASN-0123 J1★) yet is omitted from "solely INSERT/COPY"; (c) the decomposition's **own M9 seam** says pred-def creation satisfies "ASN-0047's J0 content-allocation⇒placement coupling," directly contradicting "solely INSERT/COPY." Separately, M5's responsibility says "**every placement appends R atomically**" — but link placements (K.μ⁺_L) are uncoupled from R (ASN-0047 J-LV: "link placements neither trigger nor witness provenance"); M5 seats link V-positions (on M7's behalf) that must **not** append R. Fix: state that the enforcement point is M5's placement composite (this conclusion survives), but that **J0** (content-allocation⇒placement) fires in INSERT and M9-triggered pred-def creation; **J1★** (placement⇒provenance) fires in INSERT, COPY, VERSION, and pred-def creation; and **link** placement is deliberately uncoupled from R. A builder following the current text would drop provenance for versions, pollute R with link entries, or skip J0 for pred-defs — all material.

**2. [SHARPENING] — Move typed-reverse-lookup (BH3) execution from M8 into M7, eliminating the M9 → M8 edge.** BH3 reads the spanfilade reverse index, which already lives in M7; the other behaviors (BH1/BH2/BH4) and Observe are in M7. Putting BH3 in M8 forces **M9 → M8** solely so PL can reach its BH3 atom — yet ASN-0129 explicitly fences PL off from M8's content-region query layer ("Keep the two evaluators as separate modules"). BH3-in-M7 makes PL's *entire* read surface (Observe + all behaviors) live in M7, drops the M9 → M8 edge, and honors that boundary. The producer flagged this seam as the softest and it is sound either way; pick M7. At minimum, the M9 seam phrase "reads structural state via … M8's queries" must be narrowed to *BH3 only* — it currently reads as if PL consumes ASN-0127, which it explicitly does not.

**3. [SHARPENING] — Reword M5's S3★-routing component so it cannot be read as an M5 → M7 dependency.** "subspace-alignment / S3★ routing (… link V-positions into the link store M7)" invites a builder to make M5 verify a link V-position's target against M7's store — exactly the referential-integrity check that, if added, closes the M7 → M5 ↔ M5 → M7 cycle the design works to avoid. The DAG is correct (no M5 → M7), because the only operation that ever seats a link V-position is M7's MAKELINK, which allocates the link and seats it in one composite. State this contract explicitly: the **subspace-match invariant** is a pure M1 classification of the address; the link-side **referential-integrity** obligation is discharged by-construction inside M7's MAKELINK (M7 hands M5 an already-allocated link address through a semantics-blind seating API); M5 independently verifies referential integrity only on the content side (against M4/M3).

**4. [SHARPENING] — Two named build-targets are owned only implicitly; assign them to M8.** ASN-0117's pre-edit survival check ("this delete will break N links here") and ASN-0125's reverse-discovery `in(y)` are real capabilities not listed under any module. Both are discoverability/BH3 compositions squarely within M8's remit (findlinks-over-Δ + last-witness; reverse-lookup). Name them in M8 and cite ASN-0117/0125's contributions there, so a builder doesn't mistakenly try to fold the survival check into M5's DELETE (which ASN-0117 is emphatic must touch no link state).

**5. [SHARPENING] — Three small fidelity/clarity fixes.** (a) M4's Sources line lists "consumed by ASN-0115," a downstream consumer, not a source — move it to Seams. (b) M7 groups age-staleness (BH4) under "forward behaviors," but BH4 is neither forward nor reverse (a home-frontier/chain-index read); the M7 *placement* is right, only the label is loose. (c) M3 owns the entity registry and the parent-exists (P8) admission check, which ASN-0047 defines, but M3 does not cite ASN-0047 (only the indirect 0040/0093 path); add the citation.

The producer's six flagged partition questions (M4/M5 split, ownership-in-M3, M7/M8 boundary, M9 one-vs-two, distributed R, M2-as-module) are all sound calls — none is chosen wrong; my only divergence is the BH3 placement in #2.

VERDICT: REVISE
