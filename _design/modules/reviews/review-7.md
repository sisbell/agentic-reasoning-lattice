I walked the corpus against the manifest before judging. Completeness checks out: all 42 ASNs are assigned, and tracing each note's "what must be built" I found no orphan capability — even the awkward ones (the within-document I→V reverse lookup, the tight-endset constructor, R's two query directions, the lazy empty-arrangement) land somewhere defensible. The DAG is acyclic with every edge pointing to a lower-numbered module, and the four cycle-risk seams are all broken soundly: M7→M5 (semantics-blind seating, no return edge), M5→M3 lazy arrangement (no M3→M5), spanfilade-in-M7 (Observe and discovery share one index, no M7→M8), and M9 fenced from M8 per ASN-0129. The M2-as-generic-kernel / dependency-inversion move (stores register record-types and keys; M2 carries no M1 edge) is legitimate and is the cleanest mechanism/policy split in the design. Source attributions are accurate; I found no false claim about the corpus. The issues below are all refinements, not material problems.

**Revision list**

1. **[SHARPENING] M10: rescope to request-lifecycle-only; the "cross-family composite orchestration" component is vacuous against the corpus.** Every cross-family composite in the corpus self-orchestrates in its *initiating* module — MAKELINK (link store + arrangement seating) in M7 via M5's API, INSERT/COPY/VERSION in M5, register_pred (content placement + classifier emit) in M9 — each committing through M2 directly. M10's own qualifier ("never the per-store operation logic") confirms MAKELINK's seating stays in M7, leaving the orchestration component with no operation to apply to. Either delete it (M10 = parse/dispatch/linearize/commit-gate/marshal/reject) or name the specific cross-family operation it orchestrates (there is none). This is the producer's own OQ(g); my view is that store/initiating modules orchestrate and M10 dispatches.

2. **[SHARPENING] M2/M10: stop naming "commit-before-acknowledge" in both modules.** M10's sources say it was re-homed to M10 with M2 keeping "only the commit-gate mechanism," yet M2's key-components still list "commit-before-acknowledge gate." Relabel M2's component "durable-commit confirmation / commit point" and let M10 own the response-gating, so a builder doesn't double-implement the ack hold.

3. **[SHARPENING] Give ASN-0125's set-valued `current(y)` (currency) an explicit home.** It is owned only implicitly. It is M7's BH2 determinate-walk (`tip`/sinks over the operative `succ_o` graph, returning the set on a fork and ⊥ at branch/cycle), surfaced as a read alongside M8's archival in/out family. Name it under M7 (atom) and/or M8 (query) so "which is the current version(s)" isn't a capability a builder has to re-derive is covered.

4. **[SHARPENING] M3: state the input boundary of the in-model authorization gates.** The gates `owns(π,A)` (CREATENEWDOCUMENT) and the delegation conditions consume a principal `π` whose origin — the session/access layer — the corpus scopes out (ASN-0042/0103/0112/0115). The *mechanism* is correctly in M3, but say explicitly that `π` is supplied by an out-of-scope session layer through M10's request context, so the gate's parameter boundary is unambiguous rather than left dangling between M10 and M3.

5. **[SHARPENING] Flag M5 as the most likely future split.** Its responsibility joins three things with "together with"/"and": the POOM representation+primitives, five editing/versioning operations, and the authoritative (historical, non-presentational) relation R. It is coherent and well-justified (write-locality + the single-(M,R)-snapshot requirement of ASN-0075), and the producer flagged the R-extraction alternative — but it is the largest module, and POOM-primitives vs editing-operations vs provenance is the partition to revisit first if the design pass proves unwieldy.

(Minor, fold-in: CL-UNIQ is implied by M5's arrangement-invariant ownership but not listed beside CL-OWN/S3★; M2's "seven-primitive atomic-step model" attribution reads as ownership of primitive *semantics*, which actually live in the stores — its seam already says "never store semantics," so just align the source line.)

VERDICT: CONVERGED
