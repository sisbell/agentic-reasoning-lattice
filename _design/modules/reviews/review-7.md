I walked the full corpus against the manifest: every one of the 42 ASNs is assigned, and I checked the major "what-must-be-built" capabilities for orphans and the DAG for cycles/missing/spurious edges. The decomposition holds up well. The hard cycle-breaks (M7→M5 via a semantics-blind seating API; M5→M3 with lazy arrangement; spanfilade-in-M7 keeping Observe's index local; M9 fenced from M8 per ASN-0129) are all sound and faithfully reasoned, and the J-coupling distinctions (J0 in INSERT + predicate-def creation but not COPY/VERSION; J1★ in INSERT/COPY/VERSION; J-LV uncoupling links) are precisely correct against ASN-0047. I found no orphan capability, no spurious component, no false corpus statement, and no DAG defect.

The items below are improvements, none load-bearing.

**1. [SHARPENING] M3 — make the centralized/layered frontier rule explicit.** The DAG's `M3 → {M1,M2}` (no M4, no M7) is valid only because M3's registry is the authoritative allocation frontier for *every* subspace (content, link, document, version), recomputed from M3's own registry — never from M4's content store or M7's link store. But the notes describe the *unified* (Green) "query-and-increment over the content/link store" mechanism (ASN-0116/0093/0120), which a builder could follow literally and reintroduce `M3 → M4` (or a store-local link frontier), violating the DAG. The manifest's M3 components already imply the layered design, but state the rule outright in M3's responsibility/seam — "M3 owns all frontiers; stores never recompute a frontier from their own data" — so the absence of `M3 → M4`/`M3 → M7` is robust against the notes' unified-allocator language. (ASN-0040 sanctions exactly this layered alternative, so the choice is sound; it just needs spotlighting.)

**2. [SHARPENING] M7/M8 — explicitly assign ASN-0125's set-valued currency `current(y)`.** Its machinery is present (M7's BH2 determinate-walk, supersession claims, active/audit slices; M8's archival in(y)/out(x)), but the currency operation itself — "operative sinks reachable via succ_o, returned entire" — is enumerated nowhere, so it can fall between M7 and M8. Place it in M8 as a composed discovery query over M7's BH2 + supersession links (alongside in/out), and flag that its set-valued-on-branch semantics differ from BH2's `tip` (which returns ⊥ on a branch), so a builder doesn't conflate them.

**3. [SHARPENING] M7/M8 — factor the shared link-matching combiner into M7.** The per-slot-coverage-match + AND-of-ORs logic is implemented twice (inside Observe in M7, inside findlinks in M8). The producer flagged this and judged it "sound either way." Factoring it into M7 beside the spanfilade it reads, leaving M8 as pure discovery presentation (windowing/cursors, count, pagination, projection, RETRIEVEENDSETS), is a clean DRY win — below module altitude, so optional.

**4. [SHARPENING] M7/M8 — strengthen the spanfilade-in-M7 rationale beyond "softest seam."** The placement is not merely a presentation preference: it is *necessary* to keep the DAG acyclic. Observe lives in M7 and needs the coverage index; if the index lived in M8, `Observe → M8` plus `findlinks → M7` would close an M7↔M8 cycle. Recording that hardens the boundary against future second-guessing.

**5. [SHARPENING] Enumerate a few in-scope-but-unnamed sub-capabilities** so they don't slip through: the tight-endset constructor (within M7's MAKELINK, ASN-0098), the quiescence registration-lint / divergence monitor (within M9, ASN-0133), and the historical/operative view derivation that grounds `current(y)` (M7's active/audit slices, ASN-0125). Each lives in an already-assigned module; naming them prevents oversight.

**6. [SHARPENING] The five flagged partition questions are all sound — no change needed; document the strongest rationale for the load-bearing ones.** M3's ownership-folding is justified by structural coupling (delegation *is* a baptism, principals *are* registry labels — ASN-0042 O18/O17b), so a separate Authority module would share M3's registry anyway. M4/M5 should stay split (Istream/Vstream is the spine; merging bloats M5). Most usefully, M2-as-a-module rests on a stronger argument than the manifest gives it: the J-couplings demand *cross-store composite atomicity* (atomic content+arrangement+R commit across M4/M5), which one journal kernel delivers and per-store journals would need 2PC for — worth stating, since it converts M2 from "shared discipline, optional module" into "module that earns its boundary."

VERDICT: CONVERGED
